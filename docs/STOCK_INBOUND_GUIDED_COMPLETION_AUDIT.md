# Stock Inbound Guided Flow Completion Audit

Last updated: 2026-07-10

This audit maps the active guided Stock Inbound goal to current evidence. It is intentionally conservative: source checks and local builds prove implementation shape, but the goal is not fully complete until live Supabase and device evidence proves the worker flow end to end.

## Evidence Sources

- UI flow: `components/stock/workflow-forms.tsx`
- Scanner: `components/stock/barcode-scanner.tsx`
- Display names: `lib/stock/display-names.ts`, `lib/stock/data.ts`
- Server actions: `lib/stock/actions.ts`
- Labels: `components/stock/stock-label.tsx`, `components/stock/stock-unit-detail.tsx`
- Reports: `components/stock/stock-page.tsx`, `lib/stock/report-export.ts`
- Inbound RPC: `supabase/migrations/202606250004_stock_barcode_rule_sample_v1.sql`
- Undo RPC: `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`, `supabase/migrations/202606250008_stock_inbound_session_void_rpc_v1.sql`
- Duplicate cleanup: `supabase/migrations/202606250012_stock_manufacturer_merge_v1.sql`, `supabase/migrations/202606250013_stock_item_merge_v1.sql`
- Display-name sync: `supabase/migrations/202606250014_stock_item_display_name_trigger_v1.sql`
- Automated guards: `scripts/stock-inbound-guided-flow-coverage.mjs`, `scripts/stock-acceptance-coverage.mjs`, `scripts/stock-owner-qa-doc-coverage.mjs`

## Requirement Matrix

| Requirement | Current evidence | Status |
| --- | --- | --- |
| Product and manufacturer stay separate | `ItemSelect`, `BrandSelect`, `brand_id`, `origin_id`, `productName`, `manufacturerName`, and `displayProductName` are handled separately in `workflow-forms.tsx` and `actions.ts`. Manual typed product/manufacturer setup now also previews the generated display name before the item is created using the shared display-name helper. Custom manufacturer and quick product setup use stock-operator gates, canonical duplicate lookup, and audit logging before scanning starts; when they succeed, the server returns the saved/reused manufacturer name and the inbound setup immediately adds/selects the resolved manufacturer locally. The quick-create UI now also normalizes local product/manufacturer spacing and uses the shared display-name helper before showing the newly created product. Server actions normalize typed manufacturer/origin spacing/casing and block same product + manufacturer duplicates with different casing or spaces. RLS coverage now guards that stock users can insert/update manufacturer and origin master rows through `can_manage_stock()` while deletes stay admin/director-only. Stock Settings now has admin/director merge paths backed by `merge_stock_manufacturer` and `merge_stock_item`, which move safe stock references, audit the merge, and deactivate/rename the source record instead of deleting it. | Source-proven; live Supabase save still needs QA; live merge still needs QA |
| Display name is manufacturer + product | `stockDisplayItemName()` returns manufacturer + product, and now collapses extra casing/spacing noise from loaded item/manufacturer rows; QA docs use `Locks NV Belly Boneless`. Migration `202606250014_stock_item_display_name_trigger_v1.sql` adds a safe trigger/backfill so stored `items.display_name` stays synced from manufacturer + product after item inserts, edits, and manufacturer merge updates. The SQL helper now also collapses repeated spaces in section/name/manufacturer fields so direct database edits follow the same display-name rule. | Source-proven; migration still needs live SQL QA |
| Display name appears in UI/scanner/summary/labels/reports/search | Shared helper is used by Stock data, unit cards, movements, labels, scanner context, session summary, and report rows. Guided inbound/report coverage now guards that report rows use the generated display item name and expose Product, Manufacturer, and Origin columns. Demo-mode movement, stock-take, damage, and return-supplier rows now use manufacturer + product display names instead of the old category/section/product text. Live inbound movement rows are guarded by `202606250004_stock_barcode_rule_sample_v1.sql`, which writes `stock_movements.stock_unit_id`; `lib/stock/data.ts` then derives movement product/manufacturer display from the linked stock unit. Older movement rows without a linked stock unit now fall back to `stock_movements.brand_id` when present, so report/history display names can still show manufacturer + product. Stock-unit labels shown in outbound scan feedback, outbound scanned lists, return barcode details, and stock-take review sessions now also use the shared manufacturer + product display helper instead of the old category/section/product string. Stock item-master edit search and stock-take item search now include the generated default manufacturer + product display name, so stock search results align with the new naming rule while still keeping product/manufacturer separate. | Source-proven; visual QA still needed |
| Filter/report by product and manufacturer separately | Product and manufacturer fields remain separate in data. Guided inbound coverage now guards separate report filters for `report.itemName`, `report.brandName`, and `report.originName` instead of relying only on generic detail text. | Source-proven; live report QA still needed |
| Barcode inbound Page 1 setup | Guided steps include setup, read-only session code, recent templates, product/manufacturer selectors, custom entries, default location, and Change Location. Page 1 now also has a visible required-setup card with an inbound location dropdown, profile-default location cue, and generated manufacturer + product display name before the worker moves to Barcode Rule Page. Product search and exact-match checks use the normalized product name plus manufacturer display name so workers are less likely to create duplicate products. Product dropdown options now put the generated manufacturer + product display name first with item code second, plus `Other / custom product` wired to the same quick-save form as the visible `Use Other Product` button. Product, manufacturer, origin, and location dropdowns/buttons are sorted A-Z from copied arrays for stable mobile setup. The setup preset card now explicitly labels the generated display name as `Manufacturer + product` while keeping separate Product, Manufacturer, Origin, and Location rows for reporting/filtering. Recent template cards show only the manufacturer + product display name so the mobile setup stays light. Tapping a recent template now also fills the visible product, manufacturer, and origin search boxes so the setup page visibly matches the chosen template, and it clears stale custom manufacturer/origin text from earlier manual entries. The mode buttons and short guidance now use the requested worker labels `Inbound with Barcode` and `Inbound without Barcode`, with short active-flow copy such as `Choose once. Scan barcodes.` and `Keep scanning.` Custom manufacturer and custom origin now both use stock-operator quick-save actions before scanning; saved/reused records are added locally and selected immediately so barcode rules have real item + manufacturer + origin ids. Choosing custom product, manufacturer, or origin now focuses the new entry input immediately, including the quick `Use as product/manufacturer/origin` buttons. When setup is locked, the review box now shows the specific reason, such as a pending internal label or already-saved stock. | Source-proven; 390px browser QA still needed |
| Inbound session history | Page 1 now groups existing `stock_units.batchNo` records into inbound session history without adding a new table. It shows recent 10 sessions per page, batch code, display product, manufacturer, location, active saved count, active saved weight, compact barcode details, voided count kept for audit, and pagination. The session-history builder sorts a copied unit list by received time before building barcode details, so the compact detail panel's latest-barcode list does not depend on database return order. Voided units no longer feed reusable recent inbound templates and no longer inflate saved count/weight in history. It also exposes `Start new session` and a `Continue unfinished session` card for the current in-progress session. Starting a new session is guarded when saved stock exists and sends the worker to summary instead of silently losing work. | Source-proven; live Supabase session history QA still needed |
| Back navigation from later pages | Step buttons plus Back to setup / Back to Barcode Rule controls exist with guarded navigation. Going back after saved scans/weights is explicitly review-only, and the page now warns that saved entries are kept while product, manufacturer, origin, and location remain locked. The Summary step remains locked until the worker finishes the inbound session, and a mode-aware cue tells the worker whether to save a barcode/unit first or tap `Finish Session`. When setup or rule fields are locked because a session has saved stock, a pending internal label, or a finished state, safe previous pages are labeled as review pages such as `Review Setup` and `Review Barcode Rule`. If saved stock exists, setup shows a stronger change-protection warning: finish the current session or delete the whole session with confirmation before changing product/manufacturer/location. Finished sessions also relabel scan pages as `Review Scanner` or `Review Manual Weight` so workers know they are viewing read-only pages instead of reopening editable setup. | Source-proven; browser QA still needed |
| Barcode inbound Page 2 rule setup | Sample barcode, actual kg, weight start, digit length, decimals, extracted preview, internal-label fallback, and a save-rule summary that shows display product, product, manufacturer, origin, sample barcode, and barcode length separately exist. Page 2 now requires the full Page 1 setup, including inbound location and a saved origin id, before the worker can open barcode-rule learning. Page 2 starts with a selected product/manufacturer context card, explicitly lists camera scanner, handheld scanner + Enter, and manual typing as sample barcode input paths, shows a red short warning when the rule fields cannot extract a valid weight, and labels Back/Next navigation for setup and the scanner page. The saved-rule readiness cue now also shows expected barcode length so workers know why a different-length scan warns before saving. Decimal choice is now a 0.1 / 0.01 / 0.001 button group instead of a typed number. Fixed-weight fallback is an explicit checkbox with a fixed-kg field and now prefers the selected item master default fixed weight when one is set; if no item default exists, the checkbox stays disabled and tells the worker to set default fixed kg in Product Master first. If the same sample weight appears in multiple barcode positions, the rule helper now returns `ambiguous`, clears the ambiguous sample, and asks workers to scan another barcode instead of learning a bad rule or switching to manual labels. The first-barcode teaching cue now explicitly says the rule is for product + manufacturer + origin, matching the stored rule scope. The rule-save button now says `Save first barcode + rule`, and the Scanner step is blocked until a saved/session barcode rule exists, so first-time supplier barcode sessions must teach the rule before continuous scanning. | Source-proven; live rule-save QA still needed |
| Barcode rule stores length and sample | Migration `202606250004_stock_barcode_rule_sample_v1.sql` stores `barcode_length` and `sample_barcode`. | Migration source-proven; target database migration still needs confirmation |
| Different barcode length warning | UI emits `Barcode length changed.` and logs `BARCODE_LENGTH_MISMATCH`. The rule readiness panel shows `Expected length` before workers enter continuous scanning. If the saved rule still extracts a weight, the scan stays paused with `Confirm weight and save` so workers can verify before saving instead of being stuck. | Source-proven; browser/live scan QA still needed |
| Scanner popup/window | `BarcodeField` opens a camera scanner dialog using `@zxing/browser`, rear-camera constraints, permission/error messaging, stream cleanup, and a top current-setup card carrying display product, product, manufacturer, origin, and location context. The inbound scanner action now says `Use Phone Scanner` while other stock scanner fields keep the shared `Scan Barcode` default. The inbound scanner context now renders as separate lines so the display product name is prominent while product/manufacturer remain readable and separate. The active scanner/manual page now has a sticky session summary card with product, origin/location, saved count, and saved weight, so the setup remains visible while scanning or entering weights. The scanner popup now explicitly marks the camera window, external scanner input, last scanned barcode, last saved item/weight, live barcode count/session weight, and current camera detection count. Its camera frame turns green on success, red on failure, amber on warning, and shows a short retry/saved/check message while the popup stays open for continuous scanning. Successful supplier-barcode saves now include the saved kg in the green feedback message. The popup now uses the neutral cue `Stays open until Close.` so the barcode-rule sample page is not mistaken for a repeated auto-save loop. The scanner dialog keeps the primary scan action slot near live scan feedback, so inbound `Undo Last Scan` is visible while scanning, with mode-aware status text for no saved `scan` or no saved `unit`. Guided-flow coverage now proves the popup itself shows `Last saved item and weight`, `Inbound session total`, `Last scanned barcode`, and `Camera detections` cards instead of relying only on the page behind the scanner. Fixed-weight/manual-confirmation scans expose `Confirm weight and save` instead of leaving workers without a save action after the scan. The scan page also shows a finished-session review-only warning, supplier-barcode-specific `Save at least one barcode` finish guidance, a single short external-scanner cue, and disables scanner saving after `Finish Session`. | Source-proven; real phone/laptop camera QA still needed |
| External scanner keyboard input | Inline barcode field and scanner popup external input submit `event.currentTarget.value` on Enter. | Source-proven; real handheld scanner timing QA still needed |
| Continuous auto-save scanning | Continuous mode keeps scanner open, auto-submits valid scans, and shows no OK/Next cue. | Source-proven; live Supabase save QA still needed |
| Last scanned item/weight and live totals | Scanner popup now shows top cards for last saved item/weight, persisted inbound session total with live barcode count/session weight wording, current camera-window scan count, and the last scanned barcode detected in the popup; page cards still show saved session count and total weight. | Source-proven; browser QA still needed |
| Duplicate barcode blocks immediately | Client duplicate check and server duplicate check block duplicates and create failed scan issue records. | Source-proven; live duplicate insert QA still needed |
| Undo last scan safely | `undoInboundScanAction` calls `void_inbound_stock_unit`, writes `VOIDED`, `INBOUND_VOID`, scan log, and audit trail. The camera scanner dialog surfaces `Undo Last Scan` near live scan feedback so workers do not need to hunt for it while scanning. The current-session scan card also labels the newest saved scan as `Undo Last Scan` while older saved entries keep the generic `Undo scan` wording. | Source-proven; live RPC QA still needed |
| Barcode inbound Page 4 summary | Summary shows session status, inbound session code, display product, product, manufacturer, origin, inbound location, total barcode units, total weight, saved scans, error scans, scanned-by names, and print-ready area. The saved barcode/unit list is explicitly compact and shows the latest entries while stock movements and scan logs remain the full audit trail. The page helper now says `saved scans or labels` so the same guided summary cue works for supplier-barcode and internal-label sessions. `Print Session Summary` now renders a dedicated print-only A4 summary root with session fields, saved barcode rows, duplicate/error scan rows, and voided audit rows when present, while label printing remains separated so saved-label print CSS does not take over session-summary print. Summaries now show an explicit `Print Labels PDF` area for any saved session labels; supplier-barcode sessions use the short cue `Labels can be reprinted if needed.` while internal-label sessions still tell workers to print and attach saved labels. The visible summary actions now use large full-width touch targets at phone width, use a mode-aware action grid, and the print area explicitly tells workers to tap `Print Session Summary` and then use browser print/PDF export for the future-ready session summary. If a worker is sent to Summary before finishing, or taps Summary after saving stock, the same summary card renders with status `Open` and its `Finish Session` button calls the normal finish guard instead of leaving a blank/dead-end step. Supplier-barcode summaries now include a safe read-only `Review Barcode Rule` action, safe `Review setup (read-only)` navigation, and the scan review button says `Review Scanner` because finished sessions are read-only. | Source-proven; browser/print QA still needed |
| Whole-session delete/void | Summary has a guarded manager/admin `Delete Whole Session` action, disables it when there are no saved scans, shows a `Keep session` / `Delete Whole Session` confirmation, and calls `void_inbound_stock_session` to void eligible units and write `INBOUND_VOID` movements, scan logs, and audit logs. The server action is restricted to manager/admin/director roles; normal operators keep single-scan undo but cannot void a whole finished session. Locked setup pages link workers back to summary/delete once a session is finished, while unfinished saved sessions offer `Finish current session` before any setup change. After delete/void, the summary keeps a `Voided scans kept for audit` section so workers/managers can see which labels/barcodes were reversed without treating them as saved stock. Starting a new inbound session uses the shared draft-scan reset helper to clear barcode input, weight input, pending internal-label refs, error dedupe, and issue dedupe; it also clears saved scans, errors, print target, session-saved rule flags, and returns to setup. | Source-proven; live RPC QA still needed |
| No-barcode Page 1 setup | Internal-label mode uses the same setup page as a guided `Page 1: Inbound without barcode setup`, with read-only session code, recent templates with manual-weight-specific status text, product/manufacturer selectors, custom entries, product dropdown options with item code plus display name, and profile-default location dropdown. Tapping a recent template also fills the visible product, manufacturer, and origin search boxes before opening manual weight, while clearing stale custom manufacturer/origin text from earlier manual entries. The setup preset card explicitly previews the generated manufacturer + product display name while keeping Product and Manufacturer separate. The page title/help switches to `Inbound without Barcode`, and the setup mode guidance is now short: enter weight, save stock, then print and attach the label. Custom manufacturer/origin readiness now follows the same scan-readiness path as selected dropdown values, and choosing `Other` can seed the custom value from the current search text to reduce retyping. The no-barcode/internal-label step labels are guarded as a 3-step setup -> manual weight -> summary flow. | Source-proven; browser QA still needed |
| No-barcode Page 2 manual weight | Manual mode shows weight-first entry and keeps previous weight, saved count, and saved total together in the manual-weight progress card. The page explicitly shows the fast path with short copy: `Enter kg. Save, print, attach, repeat.` The save path clears and refocuses the weight field after server save, so workers do not reselect product/manufacturer for every unit. Manual mode now labels undo as `Undo Last Weight Entry` / `Undo weight entry` instead of scan-specific wording, while still using the safe inbound void flow and audit trail. Manual mode finish guidance says `Save at least one unit` instead of referring to supplier barcodes. | Source-proven; device/browser QA still needed |
| No-barcode label path | Numeric internal barcode, 50mm x 30mm label preview, Bluetooth print direction, PDF fallback, one-label-per-page print CSS, and immediate stock save are present. Saved labels are available in the recent saved-label list for print/reprint after the server save marks them `SAVED`. The supplier-barcode no-weight fallback now opens Inbound without Barcode before label generation, while the real `Generate internal label` action stays beside the visible kg field. Resumed manual sessions parse existing generated labels for the same session code and continue the next running serial instead of restarting at `0001`. If immediate save fails, the pending label panel keeps the generated barcode and exposes `Retry save` instead of forcing workers to cancel and retype the weight. A defensive pending-label guard remains for old in-progress UI state, but the normal worker flow is immediate save. | Source-proven; real print/PDF/device QA still needed |
| No-barcode Page 3 summary | Manual mode summary now titles the final step as `Page 3: Inbound without barcode session summary`, shows inbound session code, product name, manufacturer, generated display product name, total count, total weight, saved units, and session print fields. It uses the same mode-neutral `saved scans or labels` summary cue, exposes saved-label reprint actions separately from the session-summary print action, shows a clear no-saved-labels state when there is nothing to reprint, and can print the same A4 session summary root. | Source-proven; browser/print QA still needed |

## Local Command Evidence

Last recorded local pass in `HANDOFF.md`:

- `npm.cmd run smoke` passed
- `npm.cmd run lint` passed
- `npm.cmd run typecheck` passed
- `npm.cmd run build` passed

Additional local route evidence from 2026-06-30:

- The Next.js dev server responded to `/stock/inbound`, but without an authenticated session the route redirected to `/login` (`GET /stock/inbound 307`, then `GET /login 200`).
- A 390px logged-in browser walkthrough could not be completed in this environment because browser automation was unavailable: the local runtime had the `playwright` package wrapper but was missing `playwright-core`.
- This means source/build evidence is current, but logged-in phone-width layout and scanner behavior remain manual/live QA items.

Additional local browser evidence from 2026-07-09:

- A local dev server responded to `/stock/inbound` with the expected unauthenticated redirect to `/login`.
- In the in-app browser at 390px width, `/login` rendered with `window.innerWidth = 390`, `documentElement.scrollWidth = 375`, and no horizontal overflow.
- Authenticated `/stock/inbound` layout, phone camera, handheld scanner input, Supabase saves, and print/PDF output were not tested because signing in would transmit demo credentials to the configured Supabase project and needs owner approval.

## Remaining Proof Before Goal Completion

Do not mark the active guided Stock Inbound goal complete until all of these are captured:

1. A logged-in Stock user completes a supplier-barcode session on `/stock/inbound` against the target Supabase project.
2. The first supplier barcode teaches a rule and the second same-format barcode auto-extracts weight from the saved rule.
3. A different-length barcode shows the length warning without silently saving the wrong weight.
4. A duplicate barcode is blocked and appears in the failed scan/issue trail.
5. A phone camera scan opens, scans, continues scanning, and stops the stream when closed.
6. A handheld or Bluetooth scanner that sends Enter saves the latest barcode from both inline input and scanner popup input.
7. An internal-label/no-supplier session generates a numeric label, prints or exports PDF, scans the printed label back, and returns focus to the next weight.
8. Undo Last Scan voids one current-session stock unit and creates `INBOUND_VOID` movement/log/audit evidence.
9. Manager/admin/director whole-session undo/delete voids all eligible session stock with confirmation and `INBOUND_VOID` movement/log/audit evidence.
10. Session summary is readable at about 390px width and shows product, manufacturer, total count, total weight, saved scans, failed scans, and scanned-by names.
