# Elite Meat ERP Handoff

## 2026-07-09 - Stock Inbound setup finish handler reuse

Task completed:

- Continued the active Stock Inbound guided workflow goal by routing the setup warning's `Finish current session` button through the shared `finishInboundSession` handler.
- This keeps finish cleanup consistent with the scanner/manual Finish Session path.
- No schema, RLS, stock movement, barcode uniqueness, server action, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real long-session Supabase QA, phone camera, external scanner, Bluetooth/PDF label printing, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound history barcode detail cap

Task completed:

- Continued the active Stock Inbound guided workflow goal by removing the hidden 8-barcode cap from inbound session history data.
- View details now keeps all saved barcodes available, while the visible details panel shows the latest 20 with a short note when more exist.
- No schema, RLS, stock movement, barcode uniqueness, server action, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real long-session Supabase QA, phone camera, external scanner, Bluetooth/PDF label printing, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound full-session label totals

Task completed:

- Continued the active Stock Inbound guided workflow goal by removing the hidden 12-label cap from current-session label state.
- Session count, total weight, Print Labels PDF, and Delete Whole Session now use all saved scans in the current batch, while the visible recent list stays capped to the latest 12 for mobile usability.
- No schema, RLS, stock movement, barcode uniqueness, server action, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real long-session Supabase QA, phone camera, external scanner, Bluetooth/PDF label printing, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock external scanner overwrite fix

Task completed:

- Continued the active Stock Inbound guided workflow goal by improving handheld/external scanner keyboard input.
- After Enter submits a scanned barcode, the barcode input is selected so the next handheld scan overwrites the old value instead of appending to it.
- No scanner camera logic, stock save logic, server action, RLS, schema, barcode uniqueness, or stock movement behavior changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real handheld scanner, phone camera, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound continue unfinished route fix

Task completed:

- Continued the active Stock Inbound guided workflow goal by fixing the current unfinished-session continue button.
- Supplier-barcode sessions without a saved barcode rule now continue to Barcode Rule instead of trying to open Scanner and showing a warning.
- Manual sessions and supplier-barcode sessions with a rule still continue straight to the active scan/manual page.
- No schema, RLS, stock movement, barcode uniqueness, server action, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound generated-label preview restore

Task completed:

- Continued the active Stock Inbound guided workflow goal by restoring pending generated-label state before the immediate stock save submit.
- Manual weight entry now keeps the generated 50mm label data available for the pending preview/print area while stock save is submitting.
- No schema, RLS, stock movement, barcode uniqueness, server action, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock scanner popup short-copy cleanup

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening scanner popup helper text.
- Camera permission, one-sample mode, external scanner input, and blocked-camera messages now use shorter worker-facing copy.
- No scanner behavior, camera lifecycle, stock save logic, server action, RLS, schema, barcode uniqueness, or stock movement behavior changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound saved manufacturer gate

Task completed:

- Continued the active Stock Inbound guided workflow goal by requiring a saved manufacturer record before the worker can enter barcode-rule/scanner/manual-entry pages.
- Added the short setup warning `Save manufacturer before scanning.` beside the custom manufacturer save action.
- Updated Stock Inbound guided coverage so the setup readiness guard no longer treats raw `Other` manufacturer text as scan-ready.
- No schema, RLS, stock movement, barcode uniqueness, server action, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound manual duplicate previous-weight cleanup

Task completed:

- Continued the active Stock Inbound guided workflow goal by removing a duplicate previous-weight note from the manual weight helper card.
- The sticky summary and previous-entry card still show prior weight/barcode context and keep Undo Last Weight Entry available.
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound active page preset cleanup

Task completed:

- Continued the active Stock Inbound guided workflow goal by hiding the full current-scan preset card on active scan/manual pages.
- The active sticky summary now carries the needed setup context, so mobile workers do not see duplicate setup cards while scanning or entering weights.
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound sticky summary session code

Task completed:

- Continued the active Stock Inbound guided workflow goal by adding the inbound session code to the active sticky summary.
- Workers can now see the session code while scanning or entering weights without returning to setup/summary.
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound sticky summary product/manufacturer lines

Task completed:

- Continued the active Stock Inbound guided workflow goal by making the active sticky summary show product and manufacturer separately under the display name.
- This keeps item/manufacturer separation visible while scanning or entering weights on mobile.
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound manager-approved delete confirmation

Task completed:

- Continued the active Stock Inbound guided workflow goal by making the whole-session delete confirmation explicitly manager-approved.
- Confirmation title now says `Confirm manager-approved Delete Whole Session`.
- Existing manager/admin/director server-side action gate, RLS, stock movement, barcode uniqueness, and audit behavior were unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound summary Finish Session label

Task completed:

- Continued the active Stock Inbound guided workflow goal by aligning the session summary finish action with the requested wording.
- The disabled summary action now says `Finish Session` instead of `Session finished`.
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter scan hints

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening remaining barcode-rule and recent-scan helper text.
- Rule preview now says `Preview only. Net kg below.`
- First-rule helper now says `Save first barcode. Rule reused.`
- Recent barcode-mode scan note now says `Barcode labels already attached.`
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter display-name label

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening setup/preview display-name labels.
- `/stock/inbound` now shows `Display name` instead of longer generated-display wording while still showing product and manufacturer separately.
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound immediate-save QA alignment

Task completed:

- Continued the active Stock Inbound guided workflow goal by removing stale scan-back wording from current source guidance and QA docs.
- Barcode rule learning now says `Weight position found. Save rule.`
- `/stock/no-barcode-inbound` now says stock saves when the internal label is generated.
- Current owner/Vercel/mobile QA now checks immediate internal-label save before print/attach.
- No schema, RLS, stock movement, barcode uniqueness, or access-control logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound invalid-rule wording alignment

Task completed:

- Continued the active Stock Inbound guided workflow goal by aligning an invalid barcode-rule warning with the current mode name.
- The rule page now says `No valid weight extracted. Adjust rule or use Inbound without Barcode.`
- Current QA wording now says `Inbound with Barcode` instead of `Supplier barcode` for the mode name.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound immediate-label docs sync

Task completed:

- Continued the active Stock Inbound manual-weight goal by removing stale scan-back wording from current docs/handoff notes.
- Manual no-barcode QA now describes the current immediate-save flow: enter weight, generate label, save stock, print/attach, then continue.
- The live manual barcode field label now says `Generated barcode`.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound manual label save copy

Task completed:

- Continued the active Stock Inbound manual-weight goal by aligning manual label copy with the current immediate-save flow.
- Manual mode scanner helper now says `Weight saves label.` instead of telling workers to scan the printed label to save.
- Pending-label helper now says `After save, next weight.`
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock label reprint shorter copy

Task completed:

- Continued the active Stock Inbound label/reprint goal by shortening the stock-unit label reprint helper copy.
- Reprint page now says `Reprint label from phone. No reason needed.` and `Mobile reprint label.`
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/stock-unit-detail.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock scanner shorter worker copy

Task completed:

- Continued the active Stock Inbound mobile workflow goal by shortening shared stock scanner helper copy.
- Default manual input hint now says `Type barcode if needed.`
- Camera permission cue and external scanner cue are shorter while keeping the same camera/manual fallback behavior.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock manufacturer merge A-Z dropdown

Task completed:

- Continued the active Stock Inbound item/manufacturer naming goal by sorting the manager/admin manufacturer merge dropdown A-Z.
- This keeps duplicate manufacturer cleanup consistent with the inbound manufacturer dropdown.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter setup labels

Task completed:

- Continued the active Stock Inbound guided workflow goal by trimming setup/session-history helper labels.
- Shortened the session code, history count, quick product, quick manufacturer, and generated item-code helper copy.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter delete/fixed-weight copy

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening remaining fixed-weight and whole-session delete helper text.
- Fixed-weight fallback now says `Set default fixed kg first.`
- Whole-session delete confirmation now uses shorter manager/audit wording while keeping the same guarded delete flow.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter net-weight label

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening one remaining live scanner field label.
- Replaced `Actual net weight kg` with `Net weight kg` for the barcode scanner weight field.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter rule-weight hints

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening remaining rule-weight helper text.
- Replaced visible `actual kg` prompts with shorter `kg` prompts.
- Replaced `No supplier weight? Use internal label.` with `No weight? Use internal label.`
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound mode label alignment

Task completed:

- Continued the active Stock Inbound guided workflow goal by aligning visible mode labels to the requested wording.
- Replaced remaining worker-facing `Supplier barcode` / `No supplier barcode` mode labels with `Inbound with Barcode` / `Inbound without Barcode`.
- Updated the internal-label fallback and legacy no-barcode page wording to point workers to `Inbound without Barcode`.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound internal-label fallback cue fix

Task completed:

- Continued the active Stock Inbound guided workflow goal by aligning the fallback cue with the shortened worker copy.
- The internal-label fallback cue now listens for `Use internal label` messages, so no-weight/no-rule scans still show the helper after the copy cleanup.
- The rule-page escape button now says `Use Inbound without Barcode`, matching the Page 1 mode label.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter setup and rule hints

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening setup/rule helper copy.
- Replaced long setup prompts with `Choose setup first.`
- Replaced first-rule prompt with `Scan barcode, enter kg once.`
- Replaced summary helper with `Review session.`
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter barcode-rule error copy

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening barcode-rule/no-weight worker messages.
- `No weight found. Generate an internal label, print it, then attach it.` is now `No weight found. Use internal label.`
- `No matching weight position found...` paths now show `No weight position found. Use internal label.`
- Ambiguous sample copy now says `Weight appears twice. Scan another sample.`
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound active page duplicate summary cleanup

Task completed:

- Continued the active Stock Inbound guided workflow goal by removing duplicate saved-count/weight cards below the active scan/manual sticky summary.
- The active scanner/manual pages now rely on the sticky session summary for saved count and total weight, keeping the phone view lighter.
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter manual-label guidance

Task completed:

- Continued the active Stock Inbound guided workflow goal by shortening worker-facing manual-label helper text.
- The active manual-weight page now says `Setup locked. Save, print, attach.`
- Saved-label feedback now says `Print and attach labels.`
- No business logic, RLS, stock movements, barcode uniqueness, or session controls changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound summary finished-state label

Task completed:

- Continued the active Stock Inbound guided workflow goal with a small worker UX fix.
- The completed session summary action grid now shows `Session finished` instead of a disabled `Finish Session` action.
- This keeps the Session Summary page action set visible while making the finished state clearer on mobile.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, live manufacturer merge, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Settings manufacturer merge path

Task completed:

- Continued the active Stock Inbound naming goal by adding the requested admin cleanup path for duplicate manufacturers.
- Added an idempotent `merge_stock_manufacturer` RPC migration.
- Added an admin/director server action and a small Stock Settings form to merge one duplicate manufacturer into the manufacturer to keep.
- Merge is conservative: it updates known stock manufacturer references, audits the merge, renames/deactivates the source manufacturer, and does not delete production rows.

Files changed in this pass:

- `supabase/migrations/202606250012_stock_manufacturer_merge_v1.sql`
- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250012_stock_manufacturer_merge_v1.sql`

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-migration-safety.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The merge RPC intentionally aborts if a matching barcode rule already exists on the target manufacturer; admin should review those rare conflicts manually rather than risk silent data loss.
- Migration still needs to be applied to Supabase before the settings form can merge manufacturers on the live app.

## 2026-07-09 - Stock Inbound quick product resolved manufacturer display

Task completed:

- Continued the active guided Stock Inbound goal with a small quick-product naming fix.
- `createItemAction` now returns the resolved saved/reused manufacturer name with the new product response.
- The inbound quick product setup now uses that resolved name for local display name, search text, and selected manufacturer context.
- This keeps quick-created product display names aligned with the stored manufacturer + product rule.

Files changed in this pass:

- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase quick product creation still needs owner/staging QA.

## 2026-07-09 - Stock Inbound finished scanner helper copy

Task completed:

- Continued the active guided Stock Inbound goal with a final small scanner-copy trim.
- Changed the finished scanner disabled reason from `Session finished. Ask manager for changes.` to `Session finished. Review only.`
- Kept session locking, stock save, barcode uniqueness, RLS, movements, and undo/delete behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, live Supabase save/reuse behavior, and live RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound custom manufacturer saved-name return

Task completed:

- Continued the active guided Stock Inbound goal with a small item/manufacturer naming fix.
- `createInboundBrandAction` now returns the saved or reused manufacturer name with the manufacturer ID.
- The inbound setup now uses that returned name instead of fabricating an uppercase local name after saving/reusing a custom manufacturer.
- This keeps product/manufacturer separate and avoids messy local display names when the canonical duplicate lookup reuses an existing manufacturer.

Files changed in this pass:

- `lib/stock/action-state.ts`
- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase save/reuse behavior still needs owner/staging QA to prove the returned manufacturer name matches existing data.

## 2026-07-09 - Stock Inbound shorter locked-state helper copy

Task completed:

- Continued the active guided Stock Inbound goal with another small mobile-copy cleanup.
- Shortened locked setup, finished scan, and locked barcode-rule helper messages.
- Kept session lock, barcode rule, stock save, RLS, movement, void, and barcode uniqueness behavior unchanged.
- Updated Stock Inbound/mobile/acceptance guards to enforce the shorter worker copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound summary label PDF for saved scans

Task completed:

- Continued the active guided Stock Inbound goal with a small summary-label fix.
- `Print Labels PDF` on the inbound session summary now appears for any saved session labels, including supplier-barcode scans, not only internal-label sessions.
- Supplier-barcode sessions use the short cue `Labels can be reprinted if needed.`
- Reused the existing 50mm x 30mm label print path; no new stock logic, schema, RLS, movement, or barcode uniqueness behavior changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound shorter session-lock copy

Task completed:

- Continued the active guided Stock Inbound goal with one more worker-copy trim.
- Changed the active scanner lock cue to `Session locked. Finish first.`
- Changed the pending-label cue to `Saving label. Cancel first.`
- Updated Stock mobile/owner QA docs and coverage so the current short copy is tested.
- Kept the same session locking, pending-label safety, stock save, RLS, audit, barcode uniqueness, migration, and movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound QA docs match profile-default copy

Task completed:

- Continued the active guided Stock Inbound goal with a QA-doc alignment pass.
- Updated owner/Vercel/mobile/evidence docs to match the current short `/stock/inbound` location cue: `Using location: [location]. Profile default.`
- Added owner-QA doc coverage so stale `Default location` / `Default from your profile` wording does not return.
- No app behavior, stock action, RLS, audit, barcode uniqueness, migration, or movement logic changed in this pass.

Files changed in this pass:

- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound short worker helper copy

Task completed:

- Continued the active guided Stock Inbound goal with a mobile-worker copy cleanup.
- Shortened setup-lock, profile-default, session-code, and barcode-rule helper text on `/stock/inbound`.
- Kept the same session locking, barcode rule, stock save, RLS, audit, and movement behavior.
- Updated Stock Inbound/mobile/acceptance source guards to enforce the shorter copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound completion audit wording refresh

Task completed:

- Continued the active guided Stock Inbound goal with a documentation/evidence cleanup.
- Updated the completion audit so Page 2 documents the current `Save first barcode + rule` button.
- Updated the scanner-popup evidence so it matches the current `Scanner stays open until Close.` copy and no longer claims every popup scan auto-saves.
- No app code, stock action, RLS, migration, barcode uniqueness, or movement behavior changed.

Files changed in this pass:

- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks before the active Stock Inbound goal can be marked complete.

## 2026-07-09 - Stock Inbound unauthenticated 390px browser evidence

Task completed:

- Started a local dev server and opened `/stock/inbound` in the in-app browser at 390px width.
- Confirmed unauthenticated `/stock/inbound` redirects to `/login`.
- Confirmed the login page rendered at `window.innerWidth = 390` with `documentElement.scrollWidth = 375`, so the unauthenticated route/login surface had no horizontal overflow.
- Updated the Stock Inbound completion audit with this browser evidence.
- Did not sign in or transmit demo credentials to Supabase without owner approval.

Files changed in this pass:

- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `scripts/stock-inbound-guided-completion-audit-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- Browser check at 390px for `/stock/inbound` unauthenticated redirect - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Authenticated `/stock/inbound` layout, phone camera, handheld scanner input, Supabase saves, and print/PDF output still require owner-approved login/device QA.

Next recommended task:

- Run authenticated `/stock/inbound` QA with a safe demo/staging user and capture 390px phone/browser evidence.

## 2026-07-09 - Stock Inbound completion audit refresh

Task completed:

- Updated `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md` so the current evidence matches the latest Stock Inbound UI.
- Documented the fixed-weight fallback guard, the single short external-scanner cue, and the shorter Manual Weight Entry copy.
- Kept app behavior, stock saves, access control, RLS, and migrations unchanged.

Files changed in this pass:

- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- The audit still correctly treats live Supabase, phone camera, handheld scanner, and print/PDF testing as remaining proof before the overall Stock Inbound goal can be marked complete.

Next recommended task:

- Continue with live/device QA evidence for `/stock/inbound` rather than adding more code.

## 2026-07-09 - Stock Inbound fixed-weight fallback guard

Task completed:

- Tightened Stock Inbound barcode-rule fixed-weight fallback.
- The fallback checkbox now enables only when the selected item has a default fixed kg or the session already has a fixed kg value.
- Workers now see `Set default fixed kg in Product Master first.` when the selected product has no fixed-weight default.
- Kept barcode rule learning, position-rule decoding, manual weight entry, stock saves, access control, RLS, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Real browser QA should confirm products with default fixed kg can enable fallback, while products without default fixed kg show the Product Master prompt.

Next recommended task:

- Continue `/stock/inbound` phone QA for barcode-rule setup with a fixed-weight item and a normal supplier-barcode item.

## 2026-07-09 - Stock Inbound external scanner cue cleanup

Task completed:

- Removed a redundant conditional around the Stock Inbound external-scanner cue.
- The worker-facing text remains `External scanner: scan into this box.`
- Kept scanner behavior, phone scanner popup, sticky summary, undo, Finish Session, access control, RLS, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Real phone QA should confirm the scanner cue remains clear when using a handheld scanner keyboard input.

Next recommended task:

- Continue `/stock/inbound` phone QA for phone scanner popup and handheld scanner input.

## 2026-07-09 - Stock Inbound manual weight copy trim

Task completed:

- Shortened the Stock Inbound Manual Weight Entry helper from a four-step list to `Enter kg. Save, print, attach, repeat.`
- Kept manual weight saving, internal label generation, session lock, undo, summary, access control, RLS, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Real 390px phone QA should confirm Manual Weight Entry is still clear without the old instruction list.

Next recommended task:

- Continue `/stock/inbound` phone QA for manual weight entry: enter kg, print label, attach, repeat, undo latest, finish summary.

## 2026-07-09 - Stock Inbound supplier label note cleanup and deploy prep

Task completed:

- Shortened the Stock Inbound supplier-barcode recent scan note to `Supplier labels already attached.`
- Kept supplier barcode scanning, internal label printing, stock saves, access control, RLS, and migrations unchanged.
- Validated the full workspace before Vercel deployment.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Real phone QA should confirm supplier-barcode recent scans remain understandable and label printing is still obvious only for internal-label sessions.

Next recommended task:

- Continue `/stock/inbound` phone QA for supplier-barcode scanning vs internal-label printing.

## 2026-07-09 - Stock Inbound delete-session UI permission guard

Task completed:

- Added a Stock Inbound summary UI guard for `Delete Whole Session`.
- The delete button is now disabled unless the current stock page user has manager/admin/director permission.
- Kept the existing server-side `undoInboundSessionAction` role guard unchanged, so RLS/business protection still lives server-side.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after rerun; first run happened before `.next/types` was refreshed and failed on missing generated Next type files.

Risks / remaining checks:

- Real role QA should confirm workers see the summary delete button disabled, while manager/admin/director can open the confirmation and server-side permission still applies.

Next recommended task:

- Continue `/stock/inbound` role QA for worker vs manager summary actions.

## 2026-07-09 - Stock Inbound manual setup copy cleanup

Task completed:

- Shortened Stock Inbound Page 1 manual product/manufacturer setup copy.
- Replaced longer helper phrases with shorter worker labels: `New product`, `Product only.`, `Use as product`, `Manufacturer not found.`, and `Use as manufacturer`.
- Kept quick product save, quick manufacturer save, duplicate checks, generated display name, access control, RLS, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after rerun; first run happened before `.next/types` was refreshed and failed on missing generated Next type files.

Risks / remaining checks:

- Real 390px browser QA should confirm the shorter copy still makes it obvious that product and manufacturer are saved separately.

Next recommended task:

- Continue `/stock/inbound` phone QA from new product/new manufacturer setup into barcode rule learning.

## 2026-07-09 - Stock Inbound quick product save wording

Task completed:

- Clarified the Stock Inbound manual product entry action so new products show `Save product now`.
- This matches the existing `Save manufacturer now` flow and makes the immediate-save behavior clearer for workers using `Other/custom` setup.
- Kept product/manufacturer save actions, duplicate prevention, generated item code, setup flow, access control, RLS, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after rerun; first run happened before `.next/types` was refreshed and failed on missing generated Next type files.

Risks / remaining checks:

- Real Supabase/browser QA should create a new manufacturer and product from `/stock/inbound`, then confirm the product is selected immediately and the display name is manufacturer + product.

Next recommended task:

- Continue `/stock/inbound` phone QA for Other/custom manufacturer -> Save manufacturer now -> Save product now -> barcode rule setup.

## 2026-07-09 - Stock Inbound setup mode cue cleanup

Task completed:

- Simplified the Stock Inbound Page 1 mode guidance under the big mode buttons.
- Replaced two helper boxes with one short active-mode cue: supplier barcode scanning or internal labels.
- Kept the existing Inbound with Barcode / Inbound without Barcode buttons, setup flow, scanner flow, label flow, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after rerun; first run happened before `.next/types` was refreshed and failed on missing generated Next type files.

Risks / remaining checks:

- Real 390px phone QA should confirm Page 1 now feels less crowded and the active mode cue is still clear to workers.

Next recommended task:

- Continue `/stock/inbound` phone QA for Page 1 setup through mode selection, recent templates, and Next navigation.

## 2026-07-09 - Stock Inbound shorter worker copy

Task completed:

- Shortened remaining Stock Inbound worker-facing helper text on barcode-rule and session-summary areas.
- Kept the same guided pages, scanner behavior, label flow, session summary, delete approval guard, access control, RLS, and migrations.
- Updated stock coverage checks so the shorter mobile wording is protected.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after rerun; first run happened before `.next/types` was refreshed and failed on missing generated Next type files.

Risks / remaining checks:

- Real phone QA should confirm the shorter messages are still clear at 390px, especially no-weight fallback, summary print, and delete-session approval warning.

Next recommended task:

- Continue `/stock/inbound` phone QA through supplier-barcode rule learning and internal-label summary printing.

## 2026-07-09 - Stock Inbound rule-page scanner controls

Task completed:

- Added the existing scanner popup action controls to the Stock Inbound barcode-rule page.
- The rule-page phone scanner now stays open until Close and also shows Undo Last Scan and Finish Session controls, with Finish still blocked until a stock entry exists.
- Preserved barcode rule learning, actual-kg entry, stock saves, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after rerun; first run happened before `.next/types` was refreshed and failed on missing generated Next type files.

Risks / remaining checks:

- Real phone QA should confirm the barcode-rule scanner popup shows the controls clearly at 390px, Close returns to actual-kg entry, and disabled Finish is understandable before the first saved stock entry.

Next recommended task:

- Continue `/stock/inbound` phone QA for sample scan -> Close -> actual kg -> Save -> continuous scanning.

## 2026-07-09 - Stock Inbound finish-session transient cleanup

Task completed:

- Cleared transient undo, delete-confirmation, delete-message, and print-target state when a worker finishes an inbound session.
- The summary page now opens without stale Undo/Delete/Print UI state carried from the active scanner/manual page.
- Preserved stock saves, session summary, delete whole session flow, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real QA should finish a session immediately after undo/delete-warning interactions and confirm the summary page is clean.

Next recommended task:

- Continue `/stock/inbound` QA around Finish Session, summary print, label print, and Delete Whole Session confirmation.

## 2026-07-09 - Stock Inbound internal-label serial reset

Task completed:

- Reset the internal-label running serial whenever a fresh inbound session code is generated.
- Covered both `Start new session` and `Use same setup` from Inbound Session History.
- This keeps generated internal barcodes aligned to session code + running number + weight for each new batch.
- Preserved barcode uniqueness checks, stock saves, label printing, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real QA should generate labels in one manual-weight session, start a new session, and confirm the new barcode serial starts from the first available serial for the new session code.

Next recommended task:

- Continue `/stock/inbound` QA for manual weight sessions: generate label, save stock, start new session, generate the next label, and confirm barcode uniqueness.

## 2026-07-09 - Stock Inbound phone scanner stays open on rule sample

Task completed:

- Changed Stock Inbound `BarcodeField` usage to keep phone scanner continuous on the barcode-rule page too.
- The scanner popup now stays open after a sample barcode scan until the worker taps Close, matching the active Stock Inbound scanner requirement.
- Updated inbound, scanner, and acceptance coverage to prevent reverting to `continuousScan={inboundStep !== "rule"}`.
- Preserved barcode rule learning, actual-kg entry, internal-label fallback, stock saves, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera QA should confirm the sample scan leaves the popup open, shows the success frame/message, and Close returns to actual-kg entry.

Next recommended task:

- Continue `/stock/inbound` phone QA for barcode-rule sample scan, popup Close, actual kg entry, rule save, then continuous scanning.

## 2026-07-09 - Stock Inbound stale draft restore guard

Task completed:

- Tightened same-device Stock Inbound draft restore.
- A local draft is now used only if current stock data contains a non-voided stock unit for that saved batch.
- Stale localStorage drafts no longer provide the initial batch number when the matching saved stock no longer exists or was fully voided.
- Preserved same-device recovery for real unfinished batches, stock-unit history, barcode uniqueness, stock movements, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Same-device recovery still depends on current stock data being loaded in the page props. True cross-device resume still needs a persisted inbound-session table.
- Real browser QA should confirm a fully voided saved batch does not reopen as the active draft after refresh.

Next recommended task:

- Continue `/stock/inbound` QA for draft recovery after scan, after voiding latest scan, after whole-session delete, and after Finish Session.

## 2026-07-09 - Stock Inbound draft only after saved work

Task completed:

- Tightened same-device unfinished-session draft persistence.
- The browser now stores the active inbound draft only after the session has at least one saved stock entry.
- Empty page visits no longer resurrect unused batch numbers after refresh.
- Preserved same-device recovery for real unfinished batches, stock-unit history, barcode uniqueness, stock movements, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Same-device recovery starts only after the first successful stock save. If operations need to resume a setup-only draft before any saved stock, add a real persisted session table later.
- Real browser QA should confirm refreshing before the first save gets a fresh batch, while refreshing after one save restores the unfinished batch.

Next recommended task:

- Continue `/stock/inbound` QA for refresh behavior before first save, after first save, and after Finish Session.

## 2026-07-09 - Stock Inbound same-device unfinished session draft

Task completed:

- Added same-device local draft persistence for unfinished Stock Inbound sessions.
- The browser now remembers the active inbound session code, start time, and inbound mode until the worker finishes the session.
- On refresh, saved stock units for the remembered batch are rebuilt into the current session list so the worker can continue the unfinished batch on the same device.
- Preserved stock-unit history, barcode uniqueness, stock movements, access control, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This is same-device recovery only. True cross-device unfinished-session resume still needs a persisted inbound-session table.
- Real browser QA should scan at least one item, refresh `/stock/inbound`, and confirm the same batch code and saved scan list return before Finish Session.

Next recommended task:

- Continue `/stock/inbound` QA for refresh recovery, then test Finish Session removes the local draft.

## 2026-07-09 - Stock Inbound internal-label fallback copy trim

Task completed:

- Shortened the internal-label fallback cue from a paragraph to `Enter kg, save, print label.`
- Preserved the no-weight supplier barcode fallback, immediate internal-label save flow, scanner behavior, RLS, and migrations.
- Updated Stock mobile and acceptance coverage guards for the shorter worker copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - first run failed on stale Stock acceptance copy guard, passed after updating it.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm workers still understand the internal-label fallback after the shorter copy.

Next recommended task:

- Continue `/stock/inbound` phone QA for no-weight supplier barcode fallback into manual weight/internal label flow.

## 2026-07-09 - Stock Inbound barcode-rule helper copy trim

Task completed:

- Shortened the Barcode Rule teaching helper from a three-step list to one line: `Scan sample, enter kg, save.`
- Preserved rule learning, actual-kg entry, internal-label fallback, scanner behavior, RLS, and migrations.
- Updated Stock inbound/mobile coverage guards for the shorter worker copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed after updating stale copy guard.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm the Barcode Rule page remains understandable with the shorter helper copy.

Next recommended task:

- Continue Stock Inbound phone QA around Page 2 rule learning: scan sample, enter actual kg, save first scan, then confirm the next barcode auto-extracts weight.

## 2026-07-09 - Stock Inbound summary finish-session cue

Task completed:

- Added a `Finish Session` action cue to the Stock Inbound session summary action grid.
- Preserved the existing behavior where the actual finish action happens from the active scanner/manual-entry page before the summary opens.
- Added guided inbound coverage for the summary finish-session cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real worker QA should confirm the summary action grid is clear at phone width and that workers understand the session is already finished when summary is shown.

Next recommended task:

- Continue `/stock/inbound` mobile QA for the summary page: labels PDF, print summary, review setup/rule/scan, delete whole session, and start new session.

## 2026-07-09 - Stock Inbound barcode-rule decimal buttons mobile stacking

Task completed:

- Changed the Barcode Rule decimal selector from forced three columns to one-column on phone and three columns from the `sm` breakpoint.
- Added guided inbound coverage so the decimal selector stays mobile-first.
- Preserved barcode rule learning, fixed-weight fallback, scanner behavior, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm the Barcode Rule page feels clean at about 390px width.

Next recommended task:

- Continue `/stock/inbound` mobile QA for Page 2 Barcode Rule: sample scan, actual kg entry, decimal selection, fixed-weight fallback, and Next to scanner.

## 2026-07-09 - Stock Inbound history reuse scope normalization

Task completed:

- Normalized the `Use same setup` history action before it starts a fresh inbound session.
- The reused setup now goes through the same active item, manufacturer, origin, and default stock-location guard used by saved inbound presets.
- Added guided inbound coverage so history reuse cannot bypass that normalization again.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm `Use same setup` opens the correct page for saved-rule and no-rule batches at about 390px width.
- True cross-device session resume still needs a persisted inbound-session table if operations require it later.

Next recommended task:

- Continue Stock Inbound QA around history reuse, scanner/rule routing, and manual-label session routing.

## 2026-07-09 - Vercel deploy blocked by unverified destination

Task attempted:

- Tried to deploy the current workspace to Vercel after local smoke, lint, typecheck, and build passed.
- Checked the local Vercel link and found no `.vercel/project.json`.
- Listed the accessible Vercel team `ELITE MEAT's projects`; it only showed `import-control-dashboard` and `frozen-pork-whatsapp-bot`.
- Checked `elite-meat-erp.vercel.app` through Vercel deployment lookup; it returned deployment not found.
- The Vercel deploy action was rejected because the target project could not be verified safely.

Files changed in this pass:

- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Do not deploy until the correct Vercel project is linked or the owner explicitly confirms the destination despite the current Vercel lookup mismatch.
- The current repo is on branch `review/codex-half-finished-progress` with many pre-existing uncommitted changes; do not make a broad commit without owner review.

Next recommended task:

- Link the repo to the correct Vercel project with `vercel link`, or confirm the correct Vercel team/project/URL before retrying deployment.

## 2026-07-09 - Stock Inbound history reuse setup

Task completed:

- Added a `Use same setup` action to Stock Inbound session history rows.
- History rows now carry item, manufacturer, origin, location, and saved-rule availability from existing stock units, so workers can start a fresh session using a previous batch setup.
- The action generates a new inbound session code and does not pretend old history rows have persisted open/finished state.
- Preserved stock-unit history, barcode uniqueness, movements, scanner behavior, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Because there is no persisted inbound-session status table, history rows use `Use same setup` to start a fresh batch instead of resuming old batches. Add persisted session status only if operations need true cross-device resume.
- Real phone QA should confirm the history row button is easy to tap at about 390px width and that it opens scanner/rule/manual page correctly.

Next recommended task:

- Continue `/stock/inbound` QA for history reuse: tap an old supplier-barcode batch with saved rule, tap an old batch without rule, and tap the same setup while in manual-label mode.

## 2026-07-09 - Stock Inbound sticky summary phone stacking

Task completed:

- Changed the Stock Inbound active sticky session summary so Saved and Weight cards stay one-column on phone width and only split at the `sm` breakpoint.
- Added guided inbound coverage so the forced two-column phone layout does not return.
- Preserved inbound session logic, scanner behavior, label generation, undo/delete flows, RLS, and migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is still needed to confirm the sticky summary does not cover the scanner/manual input while scrolling at about 390px width.

Next recommended task:

- Continue `/stock/inbound` phone QA on active scan/manual pages: sticky summary, large scanner button, manual input, Undo Last Scan, and Finish Session.

## 2026-07-09 - Stock scanner popup shorter helper copy

Task completed:

- Shortened the shared Stock scanner popup camera, continuous-scan, and one-sample helper copy.
- Preserved camera permission handling, rear-camera preference, external scanner input, continuous scanning, success/error frames, Undo Last Scan, Finish Session slot, stream cleanup, RLS, and migrations.
- Updated Stock scanner/mobile/acceptance coverage guards for the shorter worker-facing copy.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating one stale acceptance guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera QA is still needed to confirm the shorter popup messages remain clear when camera permission is blocked and during continuous scans.

Next recommended task:

- Continue `/stock/inbound` phone QA around 390px: permission allowed/blocked, continuous scan success frame, failed scan red frame, Undo Last Scan, Close, and Finish Session.

## 2026-07-09 - Stock Inbound shorter active-flow hints

Task completed:

- Shortened Stock Inbound worker hints on setup, barcode-rule, fixed scanner/manual entry, and internal-label guidance.
- Kept labels, validation, scanner behavior, barcode rule learning, label saving, stock actions, RLS, and migrations unchanged.
- Updated Stock coverage guards to match the shorter mobile-first copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating one stale label-coverage copy guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm the shorter hints are enough for first-time workers during barcode-rule learning and manual internal-label entry.

Next recommended task:

- Continue `/stock/inbound` device QA: first-sample barcode rule learning, internal-label save/print, and summary/delete controls at 390px width.

## 2026-07-09 - Stock scanner popup one-column mobile layout

Task completed:

- Kept the shared Stock barcode scanner popup one-column around 390px phone width by moving popup two-column grids to the normal `sm` breakpoint.
- Preserved continuous scan behavior, camera permission/error handling, offline blocking, stream cleanup, scanner actions, stock actions, RLS, and migrations.
- Added scanner/mobile coverage guards so `min-[390px]:grid-cols-*` does not return inside the shared scanner popup.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px phone camera QA is still needed to confirm popup card stacking, frame visibility, Close, Finish Session, and Undo Last Scan.

Next recommended task:

- Run `/stock/inbound` on a real phone with camera permission allowed/blocked and verify the scanner popup stack.

## 2026-07-09 - Stock Inbound normalized search filters

Task completed:

- Stock Inbound product, manufacturer, and origin search filters now normalize casing and repeated spaces before matching.
- This matches the existing duplicate-prevention behavior, so worker searches like `tican  belly` still find `TICAN Belly`.
- Kept item/manufacturer storage, quick create actions, RLS, stock actions, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm search still feels fast with actual Supabase item/manufacturer/origin lists.

Next recommended task:

- Continue `/stock/inbound` phone QA, especially item/manufacturer search and quick-create flows with mixed casing/spaces.

## 2026-07-09 - Stock Inbound immediate label-save wording

Task completed:

- Removed stale `scan-back` wording from the manual internal-label inbound UI.
- Manual label flow now says the label is saving, matching the current behavior where generated internal labels submit stock immediately and return workers to the next weight.
- Added coverage to prevent `scan-back` wording from returning.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real printer/PDF QA is still needed to confirm generated labels are usable from phone after the immediate save.

Next recommended task:

- Test `/stock/inbound` manual-weight flow on phone width: enter weight, generate/save label, print/PDF, confirm weight field resets, repeat.

## 2026-07-09 - Stock Inbound scanner popup cleanup

Task completed:

- Removed duplicate scanner popup context/summary cards so the phone scanner popup shows one clean set of current setup, last saved item/weight, session total, and camera scan count.
- Preserved continuous scanner behavior: supplier-barcode inbound keeps the popup open until `Close`, while one-sample rule setup still closes after one scan for review.
- Kept scanner actions, camera permission handling, stream cleanup, offline blocking, stock actions, RLS, and migrations unchanged.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera QA is still needed to confirm the popup framing, green/red feedback, continuous scans, `Undo Last Scan`, `Close`, and `Finish Session` feel right at 390px width.

Next recommended task:

- Run `/stock/inbound` phone QA with camera permission allowed/blocked and a handheld scanner input path.

## 2026-07-09 - Stock Inbound shorter worker hints

Task completed:

- Shortened worker-facing helper text inside Stock Inbound setup, barcode-rule, manual-weight, pending-label, and summary sections.
- Kept labels, warnings, actions, validation, scanner behavior, RLS, stock actions, and migrations unchanged.
- Updated stock inbound/mobile/acceptance coverage guards to match the shorter copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed after updating stale copy guards.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Copy was shortened only where obvious; real 390px phone QA should still confirm the guided pages feel clean and not cramped.
- Live Supabase/device QA remains needed for first-sample barcode rule learning, camera scanner popup, internal label print/PDF, and whole-session delete.

Next recommended task:

- Run live `/stock/inbound` QA on phone width and fix only confirmed runtime/mobile issues.

## 2026-07-09 - Stock Inbound quick product display name

Task completed:

- Quick-created Stock Inbound products now keep the generated `manufacturer + product` display name in local session state immediately after save.
- This keeps the current inbound session consistent with the stored item display-name contract without waiting for a reload.
- Kept server-side item creation, duplicate prevention, RLS, stock actions, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/browser QA should quick-create a new product under an existing and custom manufacturer, then confirm the generated display name stays visible through rule setup, scanner/manual entry, summary, and labels.

Next recommended task:

- Continue Stock Inbound live QA, especially first barcode rule learning and custom manufacturer/product creation on phone width.

## 2026-07-09 - Stock Inbound normalized setup matching

Task completed:

- Improved Stock Inbound setup matching so product, manufacturer, and origin exact-match checks normalize extra spaces and casing before showing `Other/custom` prompts.
- Kept server-side duplicate prevention, item/manufacturer storage, display-name generation, RLS, stock actions, and migrations unchanged.
- Added coverage so guided inbound checks prove the UI uses normalized matching.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This only fixes local UI matching prompts; existing duplicate cleanup/merge remains an admin follow-up outside this pass.
- Real phone QA is still needed for `/stock/inbound` custom manufacturer save, rule learning, scanner popup, label print/PDF, and whole-session delete.

Next recommended task:

- Continue Stock Inbound live QA and fix only confirmed runtime/mobile issues.

## 2026-07-09 - Stock Inbound QA docs current labels

Task completed:

- Updated current Stock Inbound owner/Vercel QA docs to use `Finish Session` and `Undo Last Scan`.
- Added coverage so current owner/remote QA docs reject stale `Finish Inbound Session` wording.
- Did not change app runtime logic, RLS, stock actions, schema, or migrations.

Files changed in this pass:

- `docs/REMOTE_QA_STOCK_INBOUND.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Historical handoff and older evidence docs may still mention old labels as historical notes; current owner/Vercel QA docs are updated.
- Real phone QA is still needed for `/stock/inbound`: custom manufacturer save, barcode rule learning, scanner popup, label print/PDF, and whole-session delete.

Next recommended task:

- Continue live phone/Supabase QA for `/stock/inbound`, then fix only issues found during that QA.

## 2026-07-09 - Stock Inbound current-session undo wording

Task completed:

- Aligned the current-session saved scan cards with the scanner popup wording.
- The newest saved scan/unit now shows `Undo Last Scan` or `Undo Last Weight Entry`.
- Older saved entries keep the generic `Undo scan` / `Undo weight entry` wording.
- Kept the existing safe void action, `INBOUND_VOID` movement/audit behavior, RLS, schema, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-inbound-guided-completion-audit-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - failed once on stale audit row title, then passed after guard update.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should save two scans, confirm the newest says `Undo Last Scan`, and confirm older saved rows still void safely if used.

Next recommended task:

- Continue live device QA for `/stock/inbound`: custom manufacturer save, barcode rule learning, continuous scanner popup, label print/PDF, and whole-session delete.

## 2026-07-09 - Stock Inbound Undo Last Scan wording

Task completed:

- Renamed the Stock Inbound scanner undo action from `Undo Previous Scan` to `Undo Last Scan`.
- Renamed the manual-weight undo action from `Undo Previous Weight Entry` to `Undo Last Weight Entry`.
- Kept the existing undo action, `INBOUND_VOID` movement/audit behavior, scanner popup action slot, RLS, schema, and migrations unchanged.
- Updated source coverage and the guided inbound audit doc.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-completion-audit-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm `Undo Last Scan` appears inside the scanner popup and on the active scanner page after at least one saved scan.

Next recommended task:

- Continue live phone QA of the scanner popup: scan success frame, failed scan message, Undo Last Scan, Close, and Finish Session.

## 2026-07-09 - Stock Inbound saved manufacturer selects immediately

Task completed:

- Improved the custom inbound manufacturer save flow so `Save manufacturer now` immediately adds the saved manufacturer to the local inbound setup list.
- After save/reuse, the inbound setup selects the returned manufacturer id, updates the search text, and clears the custom input.
- Kept the existing server action, duplicate normalization, audit log, barcode save behavior, RLS, schema, and migrations unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should verify the saved manufacturer appears selected immediately after `Save manufacturer now`, then persists after page reload through server data.

Next recommended task:

- Continue live/browser QA for custom manufacturer -> quick product create -> barcode rule -> continuous scan.

## 2026-07-09 - Stock Inbound Finish Session wording

Task completed:

- Shortened Stock Inbound worker-facing finish actions from `Finish Inbound Session` to `Finish Session`.
- Updated the summary-lock cue to tell workers to tap `Finish Session`.
- Kept internal action identifiers, finish logic, saved-session protection, labels, barcode rules, RLS, and migrations unchanged.
- Updated stale source coverage guards that still expected the old wording.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed once on stale `stock-label-coverage`, failed once on stale `stock-owner-qa-doc-coverage`, then passed after updating those guards.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm the shorter label is visible in both inline scanner controls and the phone scanner popup action slot.

Next recommended task:

- Continue live/browser QA for `/stock/inbound` at 390px, especially setup -> barcode rule -> scanner -> summary.

## 2026-07-09 - Stock Inbound immediate custom manufacturer save

Task completed:

- Added a stock-operator server action for saving a custom inbound manufacturer before scanning.
- Reused existing canonical duplicate lookup, name normalization, stock module access check, and audit logging.
- Added `Save manufacturer now` beside the `Other / custom manufacturer` inbound setup input.
- Kept admin/director-only manufacturer settings unchanged.
- Did not change stock save behavior, barcode uniqueness, RLS, schema, or migrations.

Files changed in this pass:

- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should verify a worker can choose `Other / custom manufacturer`, tap `Save manufacturer now`, see duplicate names reused, then continue inbound scanning with the saved manufacturer.

Next recommended task:

- Continue live/device QA for `/stock/inbound`, especially custom manufacturer -> quick product create -> barcode rule -> continuous scan.

## 2026-07-09 - Stock Inbound setup guidance wording

Task completed:

- Aligned the short Stock Inbound setup guidance with the worker-facing buttons.
- Changed guidance to `Inbound with Barcode` and `Inbound without Barcode`.
- Kept the underlying supplier-barcode/internal-label modes, save flow, barcode rules, labels, undo, RLS, and migrations unchanged.
- Updated guided-flow coverage and the guided inbound audit note.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px phone QA should confirm the longer labels and guidance stay readable without crowding the setup page.

Next recommended task:

- Continue with real browser/phone QA for `/stock/inbound`, especially setup -> barcode rule -> scanner and setup -> manual weight -> summary.

## 2026-07-09 - Stock Inbound exact mode button labels

Task completed:

- Renamed the Stock Inbound setup mode buttons to `Inbound with Barcode` and `Inbound without Barcode`.
- Kept the existing supplier-barcode/internal-label modes, setup guidance, scan flow, label generation, undo, and save logic unchanged.
- Added source coverage so the requested button labels are guarded.
- Updated the guided Stock Inbound audit note for the exact worker-facing labels.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px phone QA should confirm both longer labels wrap cleanly and remain easy to tap.

Next recommended task:

- Continue mobile visual QA of `/stock/inbound`, especially the setup page, scanner popup, and summary actions on a real phone.

## 2026-07-09 - Stock Inbound saved-weight scanner feedback

Task completed:

- Confirmed generated internal barcode format already uses session code digits + running serial + weight grams, with numeric-only regression coverage.
- Improved supplier-barcode inbound success feedback so the scanner/popup now shows the saved kg in the green success message.
- First barcode rule save now says `Rule saved. Saved [kg]. Keep scanning.`
- Normal supplier barcode saves now say `Saved [kg]. Keep scanning.`
- Did not change server actions, generated barcode format, stock save behavior, barcode uniqueness, RLS, or migrations.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm the camera popup green frame shows the saved weight after a successful supplier-barcode scan.

Next recommended task:

- Continue with mobile visual QA of `/stock/inbound` scanner popup, especially repeated scans and Undo Last Scan.

## 2026-07-09 - Stock Inbound summary Print Labels PDF cue

Task completed:

- Made the internal-label session summary explicitly show a `Print Labels PDF` area when saved labels exist.
- Kept the existing `StockLabelPrintActions` behavior: Bluetooth/phone print first and PDF fallback through the browser print sheet.
- Kept supplier-barcode summaries unchanged because there are no generated internal labels to print.
- Did not change label generation, stock save, audit, scan, or session delete/void behavior.
- Updated inbound/mobile/acceptance coverage and the guided inbound audit doc.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/phone QA should confirm the summary `Print Labels PDF` action opens print/PDF for saved internal labels and stays readable at 390px.

Next recommended task:

- Continue mobile visual QA for `/stock/inbound` summary actions and sticky scanner/manual entry cards.

## 2026-07-09 - Stock Inbound phone scanner button wording

Task completed:

- Added an optional `scanButtonLabel` prop to the shared Stock barcode scanner.
- Set Stock Inbound to show `Use Phone Scanner` on the camera scanner button, matching the guided inbound requirement.
- Kept the shared default as `Scan Barcode` for outbound, transfer, receive-transfer, return, damage, and stock-take workflows.
- Did not change camera behavior, external scanner input, manual fallback, scan validation, or stock save logic.
- Updated scanner/inbound coverage and the guided inbound audit doc.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm the `Use Phone Scanner` button opens the camera popup, prefers the rear camera, and keeps the popup open for continuous scans.

Next recommended task:

- Continue mobile visual QA for the active scanner/manual sticky summary at 390px.

## 2026-07-09 - Stock Inbound shorter mobile helper text

Task completed:

- Trimmed verbose Stock Inbound helper text on setup, barcode rule, scanner, and summary-adjacent panels.
- Kept short labels, warnings, and blocked-action messages.
- Did not change inbound save logic, barcode uniqueness, access control, location scope, session undo, or barcode rule behavior.
- Updated stock inbound/mobile/acceptance coverage and audit docs to match the shorter copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual 390px QA should confirm the shorter helper copy still gives workers enough context on first-time barcode-rule setup and internal-label flow.

Next recommended task:

- Continue with mobile visual QA of the sticky scanner/manual summary and phone scanner popup.

## 2026-07-09 - Stock Inbound setup A-Z choices

Task completed:

- Sorted Stock Inbound setup choices A-Z for product, manufacturer, origin, and location.
- Applied the same stable order to quick mobile buttons and dropdowns.
- Kept existing custom `Other` handling, profile-default location, duplicate prevention, access control, and scan logic unchanged.
- Updated guided inbound audit docs and coverage guards.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual 390px QA should confirm workers see stable A-Z setup choices after searching and after picking a recent template.

Next recommended task:

- Continue checking `/stock/inbound` on mobile for long helper text and the active scanner/manual entry sticky summary behavior.

## 2026-07-09 - Stock Inbound session history voided-unit cleanup

Task completed:

- Kept voided inbound stock units out of reusable recent inbound templates.
- Changed inbound session history to count active saved units/weight separately from voided units.
- Added voided-count detail to the session history detail panel so old deleted/voided sessions do not look like active saved stock.
- Updated guided inbound audit docs and source coverage.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after rerun. First parallel run raced with `npm.cmd run build` while `.next/types` was being regenerated.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm voided session rows appear as audit history but do not inflate worker-visible saved counts/weights after reload.

Next recommended task:

- Test `/stock/inbound` session history on mobile with a saved session, a voided scan, and a whole-session delete/void.

## 2026-07-09 - Stock Inbound summary action labels

Task completed:

- Renamed the guided inbound summary action to `Print Session Summary`.
- Renamed the manager/admin whole-session control to `Delete Whole Session` and `Confirm Delete Whole Session`.
- Kept the actual safety behavior as void-with-audit using the existing whole-session void flow; no destructive delete was added.
- Updated guided inbound docs and coverage checks so the worker-facing labels match the required wording.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-completion-audit-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The UI says `Delete Whole Session`, but implementation intentionally voids stock units/movements and preserves audit trail. Live Supabase QA should confirm the `void_inbound_stock_session` RPC records the reversal correctly.
- Deployment was not completed from this workspace because the Vercel target is not locally verified and the worktree contains many unrelated modified/untracked files. Confirm the deploy target and intended file set before uploading the current workspace snapshot.

Next recommended task:

- Test the finished `/stock/inbound` session summary at 390px and confirm `Print Session Summary` plus `Delete Whole Session` behave clearly for worker and manager/admin roles.

## 2026-07-09 - Stock label print extra-page guard

Task completed:

- Added a print CSS guard so the last 50mm x 30mm label does not force an extra blank page.
- Kept one label per page for multi-label output.
- Updated label coverage to assert the final-label page-break override.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser print/PDF QA should confirm multiple labels produce one label per page without a trailing blank page.

Next recommended task:

- Continue with Stock Inbound session-summary print view QA/refinement.

## 2026-07-09 - Stock label PDF action wording

Task completed:

- Renamed the reusable label actions to `Print labels` and `Print Labels PDF`.
- Kept the existing browser/phone print behavior; both actions still call the same print path.
- Changed label action layout from `min-[390px]` to `sm:` so phone width stays one-column.
- Updated owner QA docs and source coverage for the new wording.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone/browser QA should confirm both label buttons open the print sheet and that saving as PDF works for 50mm x 30mm labels.

Next recommended task:

- Continue auditing Stock Inbound print output itself: one label per page, readable 50mm x 30mm layout, and session-summary print view.

## 2026-07-09 - Stock display name shown in item list

Task completed:

- Updated the shared stock display helper to use stored `items.display_name` when no scanned-unit manufacturer is provided.
- Updated Product Master rows/cards to show `Display name` while keeping product and manufacturer as separate fields.
- Added source coverage for stored display-name usage in item data, helper, and Product Master list.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `lib/stock/display-names.ts`
- `scripts/stock-item-master-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None in this pass. This relies on `supabase/migrations/202606250011_stock_item_display_name_v1.sql` from the previous pass.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Apply migration `202606250011` before relying on stored `items.display_name` in Supabase.
- Stock-unit balances/reports still use the actual scanned-unit manufacturer when one is present, which preserves real stock traceability.

Next recommended task:

- Continue auditing `/stock/inbound` against the remaining goal, especially browser/mobile behavior for session history pagination and print/PDF label output.

## 2026-07-09 - Stock item stored display name

Task completed:

- Added safe migration `202606250011_stock_item_display_name_v1.sql` to store `items.display_name`.
- Backfilled display name as manufacturer + product name from `items.default_brand_id`, `items.section`, and `items.name`.
- Updated item create/update actions to write `display_name` when product or default manufacturer changes.
- Updated stock item data mapping and source coverage.
- Updated seed data so demo items also populate `display_name` after default manufacturer assignment.

Files changed in this pass:

- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/types.ts`
- `scripts/stock-item-master-coverage.mjs`
- `supabase/migrations/202606250011_stock_item_display_name_v1.sql`
- `supabase/seed.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250011_stock_item_display_name_v1.sql`

SQL run order:

- Run after `supabase/migrations/202606250010_stock_item_default_weight_v1.sql`.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-migration-safety.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Apply migration `202606250011` before deploying code that writes `items.display_name`.
- Runtime stock-unit displays still use the scanned unit manufacturer when it differs from the item default manufacturer.

Next recommended task:

- Test quick product creation from `/stock/inbound` with a custom manufacturer and confirm the stored item display name is manufacturer + product.

## 2026-07-09 - Stock Inbound scanner popup finish action

Task completed:

- Added `Finish Inbound Session` inside the Stock Inbound scanner popup action area.
- Reused the existing finish guards: at least one saved barcode/unit is required, and pending labels must be cleared first.
- Finishing from the popup uses the same session-summary path as the page-level finish button; scanner logic and server actions were not changed.
- Added source coverage for the popup finish action.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA should confirm the popup closes cleanly after finishing because the scanner becomes disabled when the session enters summary.

Next recommended task:

- Test `/stock/inbound` on a phone-width browser: open phone scanner, save one scan, tap `Finish Inbound Session` inside the popup, and confirm the summary opens.

## 2026-07-09 - Stock Inbound 390px one-column worker layout

Task completed:

- Changed guided Stock Inbound responsive splits from `min-[390px]` to `sm:` so 390px phone screens stay stacked and easier to scan.
- Updated remaining stale internal-label guard text so workers see the current flow: enter weight, save stock, then print/attach the label.
- Updated Stock Inbound coverage scripts to assert the current mobile layout and label guidance.
- Kept stock save logic, scanner behavior, undo, label generation, and server actions unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual phone QA should still confirm `/stock/inbound` feels comfortable on a real 390px device with camera and external scanner input.
- The old pending-label state remains as defensive stale-state protection, but the normal manual-weight path saves stock immediately and then prints the saved label.

Next recommended task:

- Open `/stock/inbound` on Vercel or local mobile width and test setup, barcode rule, scan, manual weight, undo, and summary pages in one worker session.

## 2026-07-09 - Stock Inbound shorter worker helper text

Task completed:

- Shortened long worker-facing helper text on Stock Inbound setup, rule, scan/manual, summary, and whole-session undo sections.
- Kept labels, warnings, disabled reasons, barcode save logic, label print logic, and manager/admin undo rules unchanged.
- Updated Stock Inbound source guards to assert the shorter copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Phone-width QA should confirm the shorter helper text still gives enough guidance for first-time workers.

Next recommended task:

- Open `/stock/inbound` at 390px and confirm setup, barcode rule, manual weight, and summary pages feel less text-heavy.

## 2026-07-09 - Stock manual weight label saves immediately

Task completed:

- Updated the Stock Inbound manual-weight path so `Generate internal label` creates the session-code barcode and submits the existing inbound save immediately.
- Kept barcode uniqueness, stock-unit creation, stock movement creation, scan log, and existing inbound server validation through the current `barcodeInboundAction`.
- Updated worker-facing Manual Weight copy to say stock saves first, then labels are printed/attached from the saved-label list.
- Left the defensive old pending-label guard in place for stale in-progress UI state, but it is no longer the normal worker path.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Phone/PDF label QA should confirm a generated manual-weight label saves immediately, appears in the saved-label list, prints, and can be scanned later as normal stock.

Next recommended task:

- Test `/stock/inbound` Manual Weight: enter 1.250kg, press Done/Enter, confirm saved count increments and the generated label appears for print.

## 2026-07-09 - Stock internal label barcode uses inbound session code

Task completed:

- Changed internal label barcode generation to use the inbound session code digits, running number, and weight in grams.
- Kept generated labels numeric-only and kept duplicate avoidance against existing stock-unit barcodes plus current-session labels.
- Updated the no-supplier-barcode inbound caller to pass the current inbound session code.
- Updated stock regression/coverage checks and business-rule docs to match the current guided inbound objective.

Files changed in this pass:

- `lib/stock/barcode-label.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-item-master-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-workflow-regression.mjs` - passed with existing Node module-type warning.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Phone/PDF label QA should confirm the new barcode value prints and scans back correctly in the manual-weight flow.

Next recommended task:

- Test `/stock/inbound` manual weight entry with one generated label and confirm the saved barcode starts with the digits from the inbound session code.

## 2026-07-09 - Stock item default fixed-weight fallback

Task completed:

- Added an optional item master default fixed weight in kg for fixed-weight barcode fallback.
- Wired item create/edit to save `default_weight_kg` without changing existing barcode save logic.
- Updated Stock Inbound Barcode Rule Page so `Use fixed-weight fallback` prefers the selected item master default weight when one is set, then falls back to the sample actual kg.
- Updated demo/seed data with a fixed-weight demo item and documented the migration.

Files changed in this pass:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/actions.ts`
- `lib/stock/demo-data.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `supabase/seed.sql`
- `supabase/migrations/202606250010_stock_item_default_weight_v1.sql`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- `202606250010_stock_item_default_weight_v1.sql` adds nullable `items.default_weight_kg` and a nonnegative check constraint. It has not been applied to live Supabase from Codex.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase must run the new migration before item create/edit can save `default_weight_kg`.
- Phone QA should confirm the fixed-weight checkbox fills the item default weight and still allows manual correction.

Next recommended task:

- Run the full check suite, then test `/stock/items` and `/stock/inbound` with a fixed-weight item.

## 2026-07-09 - Stock Inbound whole-session undo manager approval

Task completed:

- Restricted whole inbound session undo/delete server-side to stock manager/admin/director roles.
- Normal stock operators still keep `Undo Previous Scan` for current-session single-scan mistakes.
- Updated the session summary warning and button text to say manager/admin approval is required for whole-session undo.
- Kept the existing `void_inbound_stock_session` RPC, `INBOUND_VOID` movement/audit behavior, and single-scan undo logic unchanged.

Files changed in this pass:

- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should verify a worker is blocked from whole-session undo and a manager/admin can still complete it.

Next recommended task:

- Log in as worker and manager, finish an inbound session, then confirm only manager/admin/director can perform whole-session undo.

## 2026-07-09 - Stock scanner popup success/failure frame

Task completed:

- Added visible scan-feedback framing inside the shared Stock barcode scanner popup.
- Camera frame now turns green for success, red for failed/error, amber for warning, and keeps the normal white frame when idle.
- The camera overlay now shows short worker text such as `Scan saved. Keep scanning.` or `Scan failed. Try again.`
- Kept the existing continuous scanner, camera stream cleanup, manual entry fallback, undo slot, and save logic unchanged.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera QA still needs to confirm the colored frame is visible over actual camera video and does not hide the barcode target.

Next recommended task:

- Open `/stock/inbound`, tap Scan Barcode, scan one valid and one invalid barcode, and confirm the popup stays open with green/red frame feedback.

## 2026-07-09 - Stock Inbound sticky active-session summary

Task completed:

- Added a sticky active-session summary card to the Stock Inbound scan/manual pages.
- The card stays near the top while workers scan supplier barcodes or enter manual weights.
- It shows mode, display product, origin/location, saved count, and saved total weight.
- Reused the existing active-session card; no new component, server action, migration, RLS, stock save, or movement logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Phone-width QA should confirm the sticky card does not cover the barcode input or action buttons at around 390px.

Next recommended task:

- Open `/stock/inbound` on a phone-width screen, enter Scanner or Manual Weight, scroll, and confirm the session summary stays visible without blocking scanning.

## 2026-07-09 - Stock Inbound ambiguous barcode rule samples

Task completed:

- Refined supplier-barcode rule learning for ambiguous samples.
- `inferBarcodeWeightRuleWithStatus` now distinguishes unique, ambiguous, and not-found rule inference while keeping the old `inferBarcodeWeightRule` return shape for existing callers/tests.
- Inbound rule setup now asks the worker to scan another sample when the same entered weight appears in more than one barcode position.
- Kept existing no-match behavior: if no position is found, the worker is guided to internal-label/manual-weight inbound.
- No migration, RLS, stock save RPC, or stock movement logic was changed.

Files changed in this pass:

- `lib/stock/barcode-weight.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts\stock-workflow-regression.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live QA should test two real supplier barcodes where the first sample is ambiguous and the second sample makes the weight position clear.

Next recommended task:

- Test `/stock/inbound` rule setup with an ambiguous barcode sample such as one where the same weight digits appear twice, then scan a second sample and confirm the saved rule is correct.

## 2026-07-09 - Stock Inbound barcode rule controls simplified

Task completed:

- Refined the Stock Inbound Barcode Rule page controls only.
- Replaced the typed `Weight decimals` number field with large 0.1 / 0.01 / 0.001 buttons.
- Added an explicit `Use fixed-weight fallback` checkbox that enables the existing fixed-kg fallback field.
- Kept existing barcode decode, save-rule, stock-unit, stock-movement, RLS, and migration behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Item master still has no dedicated default-weight field, so this uses the existing fixed-weight fallback value rather than a true item-master default. Add a safe migration later if fixed-weight items become common.
- Phone-width QA should confirm the 3 decimal buttons and fixed-weight checkbox are easier than the old number inputs.

Next recommended task:

- Test one supplier barcode rule with each decimal option and one fixed-weight fallback scan on `/stock/inbound`.

## 2026-07-09 - Stock Inbound session history from saved batches

Task completed:

- Added a small inbound session history panel to the existing `/stock/inbound` setup screen.
- Reused existing saved stock-unit batch numbers instead of adding a new session table.
- History now shows recent sessions 10 per page with batch code, display product, manufacturer, location, unit count, total weight, and compact barcode details.
- Added a `Start new session` action and a `Continue unfinished session` card for the current in-progress inbound session.
- Guarded `Start new session` so saved current-session stock is not silently lost; workers are sent to summary to finish or delete safely.
- No migration, RLS, stock save RPC, barcode rule, or stock movement logic was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- Fresh Vercel deploy attempt after these changes was not run because the safety reviewer blocked uploading the large dirty workspace without explicit confirmation of target and payload. Earlier preview deploy before this small history update was `https://elite-meat-afz7dpwcb-elite-meat-s-projects.vercel.app`.

Risks / remaining checks:

- Live Supabase QA should confirm the history list matches saved inbound batches from the real database.
- Phone-width QA should confirm the history cards, details toggle, and pagination are readable around 390px.

Next recommended task:

- Test `/stock/inbound` with a worker login, save two inbound sessions, reload the page, confirm both appear in session history, then continue an unfinished session without losing saved scans.

## 2026-06-30 - Stock Inbound back navigation and session protection

Task completed:

- Added clearer safe Back navigation/session protection for both supplier-barcode inbound and no-supplier-barcode inbound.
- Going back to earlier pages after saved scans/weights is now explicitly shown as review-only.
- Saved scans/weights are kept when navigating back.
- Product, manufacturer, origin, and location controls remain locked once a session has saved stock or a pending internal label.
- Setup now shows a stronger warning when saved stock exists:
  - saved stock belongs to the current product/manufacturer/origin/location;
  - changing setup requires finishing the current session or deleting the whole session with confirmation.
- Added a `Finish current session` action from locked setup when saved stock exists and there is no pending label.
- Finished locked setup can send the worker back to summary, where the existing `Undo/Delete whole session` confirmation lives.
- Confirmed `Undo Previous Scan` / `Undo Previous Weight Entry` only targets `latestSavedScan`, the latest saved entry in the current session.
- Confirmed whole-session delete still calls `void_inbound_stock_session`, which voids all eligible session units and writes `INBOUND_VOID` movement, scan log, and audit records.
- No migration, RLS, barcode uniqueness, stock save RPC, or stock movement logic was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA still needs to verify `void_inbound_stock_session` reverses the full session and creates `INBOUND_VOID` movement/log/audit records.
- Browser QA should confirm a worker can go back to setup/rule/manual pages after saving stock without losing the saved list.
- Phone-width QA should confirm the locked setup warning and `Finish current session` button are easy to understand at around 390px.

Next recommended task:

- On `/stock/inbound`, save two supplier barcode scans and two no-barcode weight entries, go back to setup, confirm fields are locked with the warning, undo the latest entry only, finish the session, then test whole-session delete confirmation.

## 2026-06-30 - Stock Inbound without barcode guided session refined

Task completed:

- Refined Inbound Without Barcode as the existing `No supplier barcode` / internal-label guided session inside `/stock/inbound`.
- Kept the no-barcode flow to three steps: setup, manual weight entry, and session summary.
- Page 1 now explicitly labels the setup card as `Page 1: Inbound without barcode setup` when no-supplier-barcode mode is selected.
- Page 1 keeps:
  - uneditable auto-generated inbound session code;
  - recent inbound templates;
  - inbound location dropdown with profile-default cue;
  - product dropdown/search;
  - manufacturer dropdown/search;
  - manual product/manufacturer entry;
  - generated display product name as manufacturer + product;
  - `Next: Manual Weight`.
- Page 2 keeps manual weight entry optimized for continuous entry:
  - type weight and press Enter/phone Done;
  - generated internal label preview;
  - print/attach label;
  - scan label back to save;
  - saved input clears and refocuses weight field;
  - previous entered weight, total count, and total weight stay visible;
  - setup remains locked so product/manufacturer are not reselected per unit.
- Manual undo wording now says `Undo Previous Weight Entry` / `Undo weight entry` while still using the safe inbound void flow.
- Page 3 now titles the final no-barcode summary as `Page 3: Inbound without barcode session summary`.
- Page 3 shows inbound session code, product, manufacturer, display product name, total count, total weight, compact saved units, and future-ready print/session-summary plus label reprint area.
- No migration, RLS, stock movement logic, barcode uniqueness logic, label generation format, or save RPC behavior was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA still needs to confirm the weight field clears/refocuses after scan-back save.
- Printer/PDF QA still needs to confirm 50mm x 30mm label preview/print remains usable from phone.
- Live Supabase QA still needs to confirm each scan-back creates the stock unit, inbound movement, barcode scan log, and audit trail.

Next recommended task:

- Open `/stock/inbound`, select `No supplier barcode`, choose product/manufacturer/location once, enter weight, print label, scan it back, confirm the field clears for the next unit, undo one weight entry, then finish and review Page 3 summary.

## 2026-06-30 - Stock Inbound Page 4 session summary refined

Task completed:

- Refined Inbound With Barcode Page 4: Session Summary for the active guided Stock Inbound goal.
- Added an explicit Page 4 summary container and top inbound session code card.
- Made required summary fields clearer:
  - inbound session code;
  - product name;
  - manufacturer;
  - generated display product name;
  - inbound location;
  - total barcode/unit count;
  - total weight;
  - scanned by, started, and finished timestamps.
- Kept the scanned barcode/unit list compact by showing the latest entries and explaining that full details remain in stock movements and barcode scan logs.
- Kept the future-ready `Print session summary` action and print-only A4 summary root.
- Kept `Undo/Delete whole session` behind confirmation.
- Confirmed whole-session undo still calls `void_inbound_stock_session`, which voids eligible stock units and writes `INBOUND_VOID` movement, scan log, and audit records instead of silently deleting data.
- Added safe review/back actions from summary to setup, barcode rule, and scanner/manual pages.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA still needs to confirm whole-session undo reverses only eligible current-session stock and records `INBOUND_VOID` movement/log/audit rows.
- Browser print/PDF QA still needs to verify the summary print view is readable on desktop and phone.
- Phone-width QA still needs to confirm the compact barcode list and action grid fit cleanly around 390px.

Next recommended task:

- Complete a supplier-barcode inbound session, tap Finish Inbound Session, confirm the Page 4 summary fields, print summary preview, compact barcode list, safe review actions, and whole-session undo confirmation.

## 2026-06-30 - Stock Inbound Page 3 scanner popup refined

Task completed:

- Refined Inbound With Barcode Page 3: Scanner Popup/Window for the active guided Stock Inbound goal.
- Kept the camera scanner window powered by `@zxing/browser`.
- Kept handheld/external barcode scanner keyboard input in both the inline barcode field and scanner popup.
- Confirmed scanner context shows:
  - selected display product name;
  - selected product;
  - selected manufacturer;
  - origin;
  - stock location;
  - batch/session code.
- Added explicit popup markers/labels for:
  - camera scanner window;
  - last scanned barcode;
  - live barcode count and session weight;
  - last saved item and weight.
- Kept continuous scan behavior: valid scans auto-submit and workers do not need to click OK, Next, or Proceed after each scan.
- Confirmed valid inbound scans still route through `barcodeInboundAction` and the `inbound_stock_unit` RPC, which saves stock unit, inbound movement, barcode scan log, and optional barcode rule data.
- Kept duplicate barcode blocking and barcode-length mismatch warning behavior.
- Kept `Undo Previous Scan` in the scanner popup; it calls the existing safe void RPC and keeps audit trail.
- Kept Back to Barcode Rule and Finish Inbound Session actions.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real camera scanning still needs phone/laptop device QA.
- Real handheld scanner timing still needs device QA, especially scanners that send suffix keys other than Enter.
- Live Supabase QA still needs to confirm the first saved barcode creates stock unit, inbound movement, barcode scan log, and live totals update as expected.

Next recommended task:

- Open `/stock/inbound`, choose a supplier-barcode setup with a saved barcode rule, open the scanner popup, scan two valid barcodes continuously, confirm last scanned barcode, last saved item/weight, count/weight totals, duplicate blocking, Undo Previous Scan, Back, and Finish Session.

## 2026-06-30 - Stock Inbound Page 2 barcode rule refined

Task completed:

- Refined Inbound With Barcode Page 2: Barcode Rule Page for the active guided Stock Inbound goal.
- Kept the selected product/manufacturer context visible at the top of the rule page.
- Added explicit sample barcode input guidance for:
  - camera scanner;
  - handheld scanner with Enter;
  - manual typing fallback.
- Kept editable rule fields for weight start position, digit count, and decimals.
- Kept the extracted weight preview read-only and recalculating from the current sample barcode/rule fields.
- Added a short red invalid-preview warning. Current copy is `No valid weight extracted. Adjust rule or use Inbound without Barcode.`
- Kept the no-supplier-barcode/internal-label escape path for supplier barcodes that cannot be decoded.
- Kept Back navigation to setup and updated the forward action to `Next: Scanner Page - Open scanner`.
- Did not change stock saving logic, barcode uniqueness, stock movement logic, RLS, migrations, or approval behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA still needs to confirm saving a first sample barcode actually persists the rule for product + manufacturer + origin and stores barcode length/sample barcode.
- Phone/device QA still needs to confirm camera scanner, handheld external scanner Enter input, manual typing fallback, invalid-rule warning, and no-supplier-barcode escape at about 390px width.

Next recommended task:

- Open `/stock/inbound`, select a supplier-barcode setup, go to Barcode Rule Page, scan/type one sample barcode, change weight position/digit/decimal fields, confirm the read-only preview updates, then save and verify the next scan uses the saved rule.

## 2026-06-30 - Stock Inbound Page 1 setup refined

Task completed:

- Refined Inbound With Barcode Page 1: Session Setup for the active guided Stock Inbound goal.
- Kept the uneditable inbound session code and recent inbound templates.
- Added a visible Page 1 required-setup card with:
  - inbound location dropdown list;
  - default-from-profile location cue when the assigned stock location is used;
  - generated display product name preview using manufacturer + product;
  - separate Product and Manufacturer values under the display name.
- Kept existing product dropdown, manufacturer dropdown, manual product entry, and manual manufacturer entry.
- Renamed the supplier-barcode setup action to `Next: Barcode Rule Page` so the next step is explicit.
- Preserved later-page back/review navigation and did not change barcode save, stock movement, undo, RLS, or approval logic.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm the Page 1 required-setup card fits cleanly at 390px and does not feel duplicated with the older Change Location details below.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Open `/stock/inbound` on a phone-width screen and confirm Page 1 shows session code, recent templates, visible location dropdown, product dropdown, manufacturer dropdown, manual entry options, generated display name, and `Next: Barcode Rule Page`.

## 2026-06-30 - Stock product/manufacturer naming duplicate prevention

Task completed:

- Refined Stock Module product naming around the active guided Stock Inbound goal.
- Kept product and manufacturer stored separately through item/product fields and brand/manufacturer fields.
- Preserved generated display names as manufacturer + product for worker-facing Stock UI surfaces.
- Tightened server-side manual-entry safety:
  - typed manufacturer/origin names now normalize spacing and casing before matching/creating records;
  - existing manufacturer/origin records are matched by normalized name so `Locks NV`, `locks nv`, and extra-space variants reuse the existing record;
  - item create/update now collapses product spacing and checks for an existing same product + manufacturer + category before saving;
  - duplicate product/manufacturer attempts now fail with `Product already exists for this manufacturer. Select the existing product instead.`
- Updated stock coverage so smoke checks guard normalized naming and product/manufacturer duplicate prevention.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-item-master-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm manual manufacturer creation reuses existing records across different casing/spaces.
- Live Supabase QA should confirm attempting to create the same product/manufacturer with different casing/spaces shows the clear duplicate message.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- In `/stock/inbound`, manually try `Locks NV`, `locks nv`, and a spacing variant, then create/search `Belly Boneless` to confirm the app reuses manufacturer data and blocks duplicate messy product records.

## 2026-06-30 - Stock display-name search coverage tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the current guided inbound flow against the completion matrix and found one remaining source-level naming gap in stock product search.
- Updated stock item-master edit search and stock-take item search so they include the generated default manufacturer + product display name in addition to item code, category, section, and product.
- This makes stock search results better aligned with the product naming rule while preserving separate product and manufacturer fields for filtering/reporting.
- Updated guided inbound coverage and the completion audit to guard/document the search-result display-name behavior.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm searching by manufacturer + product display name works cleanly in stock item setup and stock-take selectors around 390px.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Run the remaining live guided Stock Inbound QA from `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`, especially supplier-barcode rule learning, handheld scanner Enter handling, internal-label print/scan-back, and undo RPC evidence.

## 2026-06-30 - Stock issue logging context de-duplication tightened

Task completed:

- Refined Stock Module issue handling so workers still see short scan messages while the app auto-creates manager-review issue records in the background.
- Audited the existing barcode-scan issue flow for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged review.
- Confirmed the existing issue record model stores issue type, barcode, detected/selected item, expected/scanned location and status, related session/order/customer/transfer context, creator, and timestamp through `barcode_scan_logs`.
- Tightened worker-side issue de-duplication so the same barcode/problem can be logged again when the order, customer, transfer, location, status, or selected item context changes during the same page session.
- Confirmed manager review remains review-evidence only: approve, reject, or mark corrected updates the scan issue row and does not directly adjust stock. Stock-take mismatch/manual correction still requires the stock-take approval workflow before database adjustment.
- No database schema, migrations, RLS, stock movement behavior, approval workflow, or worker long-form issue entry was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA is still required to prove scoped RLS issue visibility with real worker, manager, admin, and director users.
- Manual QA should trigger one issue from inbound, outbound, transfer/receive-transfer, return/damage, and stock take, then confirm the manager dashboard review queue shows the correct context.

Next recommended task:

- Run real role-based Stock issue QA after migrations are applied: duplicate inbound, barcode length mismatch, barcode not found, wrong-location outbound, unavailable outbound, unexpected receive-transfer, missing transfer item, unknown stock-take barcode, stock-take mismatch, and spoiled/damaged return.

## 2026-06-30 - Stock unit display names aligned with manufacturer + product

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Found a remaining stock UI helper that still rendered barcode stock units as `category / section / name`.
- Updated stock-unit labels to use the shared display-name helper, so barcode unit labels now render as manufacturer + product where manufacturer is known.
- This affects stock UI feedback that workers see in outbound previous scan, outbound scanned list, return barcode details, and stock-take review session scope.
- Updated guided inbound coverage and the completion audit so this display-name consistency is guarded/documented.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm the manufacturer + product display name reads cleanly in outbound, return, stock-take, and inbound screens around 390px.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Run one real Stock Inbound supplier-barcode session and one no-supplier/internal-label session, then confirm the saved unit display names match manufacturer + product in movements, labels, reports, and follow-up stock workflows.

## 2026-06-30 - Stock issue handling rechecked and smoke-stabilized

Task completed:

- Refined Stock Module issue handling by auditing the existing issue paths rather than adding worker issue forms.
- Confirmed the app already auto-creates reviewable `barcode_scan_logs` issue records for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged stock requiring review.
- Confirmed workers see short scan messages and can continue scanning where the workflow allows; the issue context is logged in the background through the existing scan-log issue actions.
- Confirmed manager review shows all open issue types with approve, reject, and mark-corrected actions.
- Confirmed scan-log review does not directly adjust stock; stock-take mismatch/manual correction still depends on the stock-take manager review and director approval workflow before database adjustment.
- Fixed two stale stock coverage guards that still expected the old one-line inbound scanner setup text after the scanner context was changed to a readable multi-line layout.
- No stock database schema, migrations, RLS policies, stock movement behavior, approval workflow, worker forms, or unrelated module business logic were changed.

Files changed in this pass:

- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-issue-handling-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed; coverage now also guards manager issue rows showing timestamp and creator/scanner identity.
- `node scripts\stock-acceptance-coverage.mjs` - failed once on stale scanner-context coverage, then passed after updating the guard.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed once on stale scanner-context coverage in `scripts/stock-mobile-ux-coverage.mjs`, then passed after updating the guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA is still required to prove RLS-scoped issue creation and manager review visibility with real worker, manager, admin, and director profiles.
- Manual QA should create one issue from inbound, outbound, transfer/receive-transfer, return/damage, and stock take to confirm manager queue context and review buttons in a real deployed session.

Next recommended task:

- Run role-based Stock issue QA on Vercel/local after migrations are applied: duplicate inbound, barcode length mismatch, barcode not found, wrong-location outbound, unavailable outbound, unexpected receive-transfer, missing transfer item, unknown stock-take barcode, stock-take mismatch, and spoiled/damaged return.

## 2026-06-30 - Stock inbound scanner setup context made readable

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Improved the Stock Inbound scanner popup setup context so it renders as separate lines instead of one long slash-separated sentence.
- The scanner now shows:
  - batch number
  - display product name
  - product
  - manufacturer
  - origin
  - location
- This keeps the manufacturer + product display name prominent while preserving separate product/manufacturer context for traceability.
- Updated guided-flow coverage and the Stock Inbound completion audit to guard/document the multi-line scanner context.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scan save behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/barcode-scanner.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed after updating stale scanner-context coverage.
- `npm.cmd run smoke` - passed after updating stale scanner-context coverage in `scripts/stock-mobile-ux-coverage.mjs`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm the multi-line scanner setup card is readable around 390px and in the camera popup.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Open the Stock Inbound scanner on a phone-width browser and confirm the locked setup card clearly shows display product, product, manufacturer, origin, and location before scanning.

## 2026-06-30 - Stock inbound summary locked cue added

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Added a short, mode-aware cue below the guided step buttons while the Summary step is locked:
  - supplier-barcode mode tells workers to save at least one barcode before summary.
  - no-supplier/internal-label mode tells workers to save at least one unit before summary.
  - once saved stock exists, the cue tells workers to tap `Finish Inbound Session` to open summary.
- This makes the disabled Summary step understandable on phone-width worker screens without changing scan, save, undo, movement, or approval behavior.
- Tightened guided-flow coverage and the Stock Inbound completion audit to guard/document the locked-summary cue.
- Attempted a 390px browser QA run with the local app; the local server printed `Ready` but exited before the in-app browser could connect, so live browser QA remains pending.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm the new locked-summary cue fits cleanly around 390px and does not crowd the guided stepper.
- The local browser QA attempt could not complete because the local Next server exited after startup in this shell session.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Complete one supplier-barcode session and one no-supplier/internal-label session in a live browser, then confirm the Summary button stays locked until Finish and the cue changes from save-first to finish-first.

## 2026-06-30 - Stock inbound pending label product confirmation guarded

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Verified the no-supplier/internal-label pending scan-back card now shows the generated label display product name before the worker scans the printed label.
- Tightened guided-flow coverage so the pending internal label card must keep:
  - `Label product`
  - the generated display product name from the pending label
  - the expected scan-back barcode
- Updated the guided Stock Inbound completion audit to record that the pending-label card shows both product and expected barcode before stock is saved.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm the product-name cue fits at about 390px and matches the printed 50mm x 30mm label.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Generate an internal label, compare the pending-card product and expected scan-back barcode with the printed label, then scan it to confirm stock saves and the next weight field is focused.

## 2026-06-30 - Stock issue handling worker/manager scope verified

Task completed:

- Refined Stock Module issue handling checks only.
- Verified the existing Stock issue model auto-creates reviewable `barcode_scan_logs` records for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged stock requiring review.
- Confirmed worker scan flows keep short messages and send issue context in the background instead of asking workers to fill long issue forms.
- Confirmed manager review uses approve/reject/correct evidence on scan-log issue records and does not apply stock adjustments directly; stock-take mismatch/manual correction still depends on the proper approval workflow before database adjustment.
- Tightened `scripts/stock-issue-handling-coverage.mjs` so smoke now guards that the worker Stock Home does not expose the manager issue review queue, while the dashboard keeps the manager issue queue.
- No database schema, migrations, RLS policy, stock movement behavior, approval workflow, scanner behavior, or unrelated ERP module code was changed.

Files changed in this pass:

- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA is still required to prove RLS-scoped issue visibility with real worker, manager, admin, and director profiles.
- Manual QA should create one issue from inbound, outbound, stock take, transfer, and return/damage to confirm the manager queue shows the expected issue context and review buttons.

Next recommended task:

- Run role-based Stock issue QA on Vercel/local after migrations are applied: duplicate inbound, barcode length mismatch, wrong-location outbound, unexpected transfer receive, missing transfer item, unknown stock-take barcode, stock-take mismatch, and spoiled/damaged return.

## 2026-06-30 - Stock inbound summary action grid made mode-aware

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound finished-session summary layout, guided-flow coverage, audit documentation, and handoff.
- The summary action grid is now mode-aware:
  - no-supplier/internal-label summaries use a five-column desktop layout for their five actions.
  - supplier-barcode summaries use a six-column wide layout for their extra `Review Barcode Rule` action.
  - phone width stays at two columns around 390px.
- This keeps the finished summary action area cleaner after adding supplier barcode rule review.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px and desktop width should confirm the summary action buttons fit cleanly in supplier-barcode and no-supplier/internal-label modes.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Finish one supplier-barcode session and one internal-label session, then confirm the summary action buttons fit cleanly on phone and desktop widths.

## 2026-06-30 - Stock inbound summary can review barcode rule

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound finished-session summary navigation, guided-flow coverage, audit documentation, and handoff.
- Supplier-barcode session summaries now include a `Review Barcode Rule` action.
- This lets workers safely inspect the saved barcode-rule page from the finished summary without reopening editable setup or scanning.
- No-supplier/internal-label summaries remain focused on setup, manual weight review, label reprint, session print, undo, and new session actions.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the summary action grid still fits cleanly with the extra supplier-only review button.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Finish one supplier-barcode session and confirm the summary can open `Review Barcode Rule` while keeping rule/setup fields read-only.

## 2026-06-30 - Stock inbound undo messages made mode-aware

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound undo status wording, guided-flow coverage, audit documentation, and handoff.
- The scanner/action-area undo status messages now use the active inbound mode:
  - supplier-barcode sessions refer to saved `scan`.
  - no-supplier/internal-label sessions refer to saved `unit`.
- The `Undo Previous Scan` action remains visible for scanner consistency, but the supporting status text now better matches the worker's current flow.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the scanner action slot remains readable in supplier-barcode and no-supplier/internal-label modes.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Open supplier-barcode mode and no-supplier/internal-label mode before saving anything, then confirm the disabled undo helper says `scan` in supplier mode and `unit` in no-supplier mode.

## 2026-06-30 - Stock inbound finish warning made mode-specific

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound finish-state wording, guided-flow coverage, audit documentation, and handoff.
- The warning shown before `Finish Inbound Session` is now mode-specific:
  - supplier-barcode sessions say `Save at least one barcode before finishing this session.`
  - no-supplier/internal-label sessions say `Save at least one unit before finishing this session.`
- This avoids telling manual-weight workers to save a barcode when their flow is weight entry, label print, scan-back, and saved unit.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating stale stock acceptance/mobile coverage guards from the old generic finish-warning text to the new mode-specific messages.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after changing the finish-warning helper to use `inboundMode === "internal_label"` instead of reading `manualMode` before declaration.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the finish warning is clear in both supplier-barcode and no-supplier/internal-label modes.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Open supplier-barcode mode and no-supplier barcode mode with no saved units, then confirm the Finish Inbound Session warning matches the active flow.

## 2026-06-30 - Stock inbound setup lock reason surfaced

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound setup review messaging, guided-flow coverage, audit documentation, and handoff.
- The read-only setup review box now shows the specific lock reason:
  - pending internal label must be scanned or cancelled before setup can change.
  - already-saved stock requires finishing or undoing current scans before setup can change.
- This makes safe back navigation clearer when workers return to setup after scanning or generating labels.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the extra lock reason fits without crowding the setup page.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Save one inbound scan, go back to setup, and confirm the read-only setup box explains why setup cannot be changed.

## 2026-06-30 - Stock inbound locked-session review labels clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound navigation wording, guided-flow coverage, audit documentation, and handoff.
- When a session is locked because it already has saved stock, a pending internal label, or a finished state, safe back-navigation now labels locked pages as review-only pages:
  - `Review Setup`
  - `Review Barcode Rule`
  - `Review Scanner`
  - `Review Manual Weight`
- The locked-session warning now distinguishes between a pending internal label and already-saved stock:
  - Pending label: scan or cancel the pending label before changing setup.
  - Saved stock: finish or undo current scans before changing setup.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the review labels fit cleanly and that locked setup/rule fields remain read-only after saved scans or pending labels.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Save one supplier-barcode scan and one internal-label pending label, then navigate back to setup/rule pages to confirm workers see review-only wording and cannot accidentally change the locked session setup.

## 2026-06-30 - Stock issue auto-log coverage tightened

Task completed:

- Refined Stock Module issue handling only.
- Confirmed current stock issue handling already auto-creates reviewable `barcode_scan_logs` issue records for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged review.
- Confirmed workers see short scan messages while issue context is logged in hidden action payloads instead of asking workers to fill long issue forms.
- Tightened `scripts/stock-issue-handling-coverage.mjs` so smoke coverage now explicitly guards the auto issue payload fields: issue type, item, selected item, expected/scanned location, expected/scanned status, related session/order/customer/transfer, expected/received barcode, created-by, and persisted review context.
- Confirmed manager review remains review-evidence only: approve/reject/correct updates `barcode_scan_logs` review status and does not directly adjust stock units, stock movements, or stock-take adjustments.
- No app business behavior, database schema, migrations, RLS policy, approval workflow, scanner behavior, or unrelated module code was changed.

Files changed in this pass:

- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass strengthens source/smoke coverage; it does not replace live Supabase QA with real roles and RLS.
- Owner/manual QA should still create one issue from inbound, outbound, stock take, transfer, and return/damage to confirm manager review visibility and scoped access.
- Stock take mismatch/manual correction still requires the proper manager/director approval workflow before any database adjustment, as intended.

Next recommended task:

- Run role-based Stock issue QA on Vercel/local after migrations are applied: duplicate inbound, length mismatch, wrong-location outbound, unexpected transfer receive, missing transfer item, unknown stock-take barcode, stock-take mismatch, and spoiled/damaged return.

## 2026-06-30 - Stock inbound internal-label expected barcode shown

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the no-supplier/internal-label inbound scan-back card, guided-flow coverage, audit documentation, and handoff.
- The pending internal-label card now shows `Expected scan-back barcode` with the generated barcode before the worker scans it.
- This helps phone and handheld-scanner workers confirm they are scanning the printed internal label that will save the stock unit.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm the expected barcode text is readable at about 390px and matches the printed 50mm x 30mm label.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Generate an internal label, compare the expected scan-back barcode in the card with the printed label, then scan it to confirm stock saves and the next weight field is focused.

## 2026-06-30 - Stock inbound internal-label wrong-scan recovery clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the no-supplier/internal-label inbound scan-back path, guided-flow coverage, audit documentation, and handoff.
- When a worker scans the wrong barcode after generating an internal label, the blocked message now shows the exact generated barcode that must be scanned to save the stock unit.
- This keeps the no-barcode flow worker-friendly: generate label, print/attach, scan the expected generated label, then continue.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm the wrong-label warning is readable on phone width and the correct generated-label scan still saves stock.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- In a no-supplier barcode session, generate one internal label, intentionally scan a different barcode, confirm the warning shows the expected generated barcode, then scan the correct label to save.

## 2026-06-30 - Stock inbound finished-session review navigation clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound step navigation, guided-flow coverage, audit documentation, and handoff.
- When an inbound session is finished, the guided step buttons now relabel safe previous pages as `Review Setup`, `Review Scanner`, or `Review Manual Weight`.
- This makes the allowed back navigation clearer without reopening editable scanning: the existing finished-session stop message and scan-disabled state still prevent new stock saves after Finish Inbound Session.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - first run failed because the coverage still expected the old ternary `? "3 Summary"` fragment after the step-label function was expanded.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed after updating the coverage guard.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm finished-session step labels fit and that review pages remain read-only.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Finish one supplier-barcode session and one internal-label session, then tap Review Setup / Review Scanner or Review Manual Weight to confirm workers cannot accidentally continue scanning.

## 2026-06-30 - Stock inbound summary status clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound session summary, guided-flow coverage, audit documentation, and handoff.
- Added a visible session-status cue to the inbound summary so workers/managers can see whether the session is `Open`, `Finished`, or `Voided`.
- Added `Session status` to the print-ready session summary rows so browser print/PDF output carries the same state.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the session-status cue fits cleanly on the summary page and appears in browser print/PDF output.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Finish one supplier-barcode inbound session and one internal-label session, then confirm both visible and printed summaries show the correct session status.

## 2026-06-30 - Stock issue length-context logging verified

Task completed:

- Refined Stock Module issue handling only.
- Confirmed the existing issue-record model uses `barcode_scan_logs` with issue type, barcode, detected/selected item context, expected/scanned location/status, related session/order/customer/transfer context, creator, timestamp, and review status.
- Confirmed issue auto-creation covers inbound, outbound, stock take, transfer, return, and damage/spoilage review paths.
- Added expected/scanned barcode-length context to inbound `BARCODE_LENGTH_MISMATCH` logs so managers can see the saved rule length and actual scanned length without asking workers to fill a long form.
- Updated Stock issue, Stock acceptance, and Stock mobile coverage guards for the new context-bearing length-mismatch log call.
- No database schema, migrations, RLS policies, approval rules, or stock adjustment behavior were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-issue-handling-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed after updating the issue coverage guard.
- First `npm.cmd run smoke` - failed because Stock acceptance/mobile coverage still guarded the old exact `BARCODE_LENGTH_MISMATCH` call without context.
- Final `npm.cmd run smoke` - passed after updating those coverage guards.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm scoped managers see open issue rows for their stock locations only, while admin/director can see broader stock issues.
- Manual QA should create issue rows from inbound, outbound, stock take, transfer, and return/damage to confirm manager review messages are understandable.

Next recommended task:

- Run a role-based Stock issue QA pass after the stock issue-context migrations are applied: duplicate inbound, length mismatch, wrong-location outbound, unexpected transfer receive, unknown stock-take barcode, stock-take mismatch, and spoiled/damaged return.

## 2026-06-30 - Stock inbound barcode-rule scope wording clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound barcode-rule page, guided-flow coverage, audit documentation, and handoff.
- Updated the first-barcode teaching cue to say the rule is for `product + manufacturer + origin`, matching the saved barcode-rule scope.
- Clarified that future scans auto-fill weight only for the same setup.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, approval logic, server actions, scanner behavior, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed after updating the stale rule-scope wording guard.
- First `npm.cmd run smoke` - failed because `scripts\stock-mobile-ux-coverage.mjs` still guarded the old `First barcode teaches this product` / `Save. Next scans auto-fill weight.` wording.
- Final `npm.cmd run smoke` - passed after updating the mobile coverage guard and QA docs.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA should confirm the rule page copy fits at about 390px and is clear before saving the first sample barcode.
- Live Supabase/device QA remains required for real barcode rule save/reuse, barcode length warnings, duplicate blocking, camera/external scanner input, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Run one first-time supplier-barcode inbound session and confirm the rule is saved/reused only for the selected product + manufacturer + origin.

## 2026-06-30 - Stock inbound scanner undo action clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound scanner action area, guided-flow coverage, audit documentation, and handoff.
- Made the scanner popup/action area show `Undo Previous Scan` during scan/manual scanner steps even before the first saved scan.
- Before any saved scan exists, the button is disabled and shows `No saved scan to undo yet.` so workers understand the undo action is available after a successful save.
- Existing safe undo behavior is unchanged: once a scan is saved, `Undo Previous Scan` still calls the server-side void action and keeps audit/movement history.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the disabled undo cue is visible in the scanner popup before the first saved scan and becomes active after a saved scan.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Open `/stock/inbound`, enter Scanner, confirm `Undo Previous Scan` is disabled before saving, save one scan, then confirm it becomes active and voids the latest scan safely.

## 2026-06-30 - Stock inbound manual summary no-label state

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound internal-label session summary, guided-flow coverage, and audit documentation.
- Added a clear manual/no-supplier summary empty state when there are no saved internal labels available to reprint.
- The session summary still keeps the browser print/PDF session-summary action available, while label reprint remains separate and only appears when saved labels exist.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the no-saved-labels message is readable after an internal-label session with no saved labels or after a whole-session undo.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, whole-session undo, and print/PDF summary evidence.

Next recommended task:

- Finish or undo a no-supplier barcode inbound session and confirm the summary clearly separates label reprint from session-summary print.

## 2026-06-30 - Stock issue handling review context tightened

Task completed:

- Refined Stock Module issue handling only.
- Confirmed stock issue records already cover duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.
- Kept issue rows on `barcode_scan_logs`, preserving barcode, detected/selected item, expected/scanned location and status, related session/order/customer/transfer context, creator, timestamp, and review status.
- Added clearer manager-review context to damaged/spoiled request issue rows: expected location and expected status now show directly as `manager review`.
- Strengthened the stock issue-handling smoke coverage so the damage/spoilage review context is guarded.
- No database schema, migrations, RLS policy, stock adjustment, stock take approval, or worker UI behavior was changed.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm scoped managers see issue rows for their stock locations only, while admin/director can see broader stock issues.
- Manual QA should create one issue from inbound, outbound, stock take, transfer, and return/damage to confirm the manager queue is understandable for real staff.

Next recommended task:

- Run role-based Stock issue QA from Vercel/local after the stock issue-context migrations are applied.

## 2026-06-30 - Stock inbound movement display-name fallback tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock display-name data mapping, Stock report/guided-flow coverage, and audit documentation.
- Tightened movement display mapping so older `stock_movements` rows without a linked stock unit can still use the movement row `brand_id` as the manufacturer fallback.
- This improves the goal requirement that manufacturer + product display names appear in stock movement history/reports without losing separate manufacturer filters.
- Current linked stock-unit behavior is preserved: linked barcode movement rows still prefer the stock unit's item/manufacturer identity.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history writes, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `lib/stock/data.ts`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live report QA should confirm stock movement history shows manufacturer + product names for both current linked barcode movements and any older movement rows with `brand_id`.
- Live Supabase/device QA remains required for real scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, and whole-session undo evidence.

Next recommended task:

- In `/stock/movements` or `/stock/reports`, compare a current inbound barcode movement and an older movement row to confirm manufacturer + product display and manufacturer filtering behave correctly.

## 2026-06-30 - Stock inbound custom selector state cleaned up

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound setup selector behavior, guided-flow coverage, and audit documentation.
- Fixed a setup-state edge case where stale custom manufacturer/origin text could remain after workers switched back to an existing dropdown value or tapped a recent inbound template.
- Selecting an existing manufacturer now clears the custom manufacturer text; selecting `Other / custom manufacturer` can seed the custom field from the current manufacturer search text.
- Selecting an existing origin now clears the custom origin text; selecting `Other / custom origin` can seed the custom field from the current origin search text.
- Applying a recent inbound template clears old custom manufacturer/origin text while syncing the visible product, manufacturer, and origin search fields.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm switching between existing manufacturer/origin and custom `Other` values is clear and does not retain stale custom text.
- Live Supabase/device QA remains required for real recent-template data, scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, and whole-session undo evidence.

Next recommended task:

- In `/stock/inbound`, type a custom manufacturer/origin, then switch back to an existing one and confirm the generated display name and hidden submitted values match the selected setup.

## 2026-06-30 - Stock inbound recent template fields sync visibly

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound recent-template setup behavior, guided-flow coverage, and audit documentation.
- Recent inbound templates now carry the manufacturer name as well as product/display label and origin.
- When a worker taps a recent inbound template, the visible Product, Manufacturer, and Origin search fields now update to match that template.
- This makes Page 1 setup clearer in both Supplier barcode and No supplier barcode modes, while preserving separate product/manufacturer/origin IDs and filters.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm tapping a recent template visibly fills product, manufacturer, and origin before moving to Barcode Rule, Scanner, or Manual Weight.
- Live Supabase/device QA remains required for real recent-template data, scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, and whole-session undo evidence.

Next recommended task:

- Open `/stock/inbound`, tap a recent template in both Supplier barcode and No supplier barcode modes, and confirm the visible setup fields match the chosen template.

## 2026-06-30 - Stock inbound scanner popup proof tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound guided-flow coverage and audit documentation.
- Strengthened the guided inbound source guard so it now proves the scanner popup itself shows:
  - `Last saved item and weight`
  - `Inbound session total`
  - `Camera window scans`
- This specifically protects the Page 3 scanner-window requirement for clearly showing previous saved weight and live count/weight totals while scanning.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or worker UI behavior were changed.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass strengthens source evidence only; real phone/laptop camera QA is still required to prove the scanner popup renders and updates correctly during real scans.
- Live Supabase/device QA remains required for scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, and whole-session undo evidence.

Next recommended task:

- Open the scanner on `/stock/inbound` at about 390px width and confirm the popup cards for last saved item/weight, inbound session total, and camera-window scan count update during continuous scanning.

## 2026-06-30 - Stock inbound summary print action made mobile-friendly

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound session-summary actions, guided-flow coverage, and audit documentation.
- Made the summary action buttons full-width, large touch targets so the 390px worker summary page remains usable.
- Clarified the session-summary print area as a future-ready browser print/PDF export action, while keeping label printing separate from summary printing.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed after restoring the existing exact summary-label separation phrase.
- `npm.cmd run smoke` - first run failed on the existing stock acceptance text guard; passed after wording adjustment.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the summary buttons fit cleanly and print/PDF output is readable.
- Live Supabase/device QA remains required for scanner save, barcode rule reuse, duplicate blocking, label printing, previous-scan undo, and whole-session undo evidence.

Next recommended task:

- Finish one supplier-barcode session and one internal-label session, then check the summary buttons and print/PDF summary output on phone width.

## 2026-06-30 - Stock issue handling coverage hardened

Task completed:

- Reviewed stock issue handling for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged review.
- Confirmed issue rows are written through `barcode_scan_logs` with issue type, barcode, detected/selected item context, expected/scanned location/status, related session/order/customer/transfer context, scanner user, timestamp, and review status.
- Confirmed workers see short scanner messages and manager review uses the existing `Manager scan issue review` queue with `APPROVED`, `REJECTED`, and `CORRECTED` review states.
- Confirmed issue review does not directly adjust `stock_units`, `stock_movements`, or stock take adjustments; stock take mismatch/manual correction still needs the protected approval workflow.
- Hardened `scripts/stock-issue-handling-coverage.mjs` so it now fails if `npm run smoke` no longer includes the stock issue-handling coverage check.
- No business logic, database schema, RLS policies, migrations, or worker UI flows were changed.

Files changed in this pass:

- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm issue rows are visible only to the scanner, scoped stock managers, admin, and director according to RLS.
- Device QA should confirm scanner screens keep workers moving after safe issue cases and block only unsafe actions.

Next recommended task:

- Run a real stock issue QA pass from Vercel/local after migrations are applied: duplicate inbound, wrong-location outbound, unexpected transfer receive, unknown stock-take barcode, and spoiled/damaged return.

## 2026-06-30 - Stock inbound manual-confirmation scan save path fixed

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to supplier-barcode inbound scanner save behavior, guided-flow coverage, and audit documentation.
- Fixed a scanner-flow dead end for fixed-weight/manual-confirmation barcode results.
- When a supplier-barcode scan decodes as `manual_confirmation_required`, the page now shows `Confirm weight and save` so workers can save the confirmed weight without leaving continuous scanning flow.
- The confirm action remains blocked for duplicate barcode, offline state, or finished sessions.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/device QA should confirm fixed-weight/manual-confirmation barcode scans show the confirm action and save correctly.
- Live Supabase/device QA remains required for rule save/reuse, camera scanning, external scanner input, duplicate blocking, print/PDF output, and undo RPC evidence.

Next recommended task:

- Test a supplier-barcode setup with fixed-weight fallback: scan a barcode, confirm the weight, save, and verify the scanner stays ready for the next barcode.

## 2026-06-30 - Stock inbound scanner step requires barcode rule

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to supplier-barcode inbound step navigation, guided-flow coverage, and audit documentation.
- Tightened Page 3 Scanner navigation so supplier-barcode sessions cannot jump to the scanner when no saved/session barcode rule exists.
- Workers now see `Set barcode rule before opening scanner.` if they try to open Scanner before teaching/saving the rule.
- The top step button for Scanner is disabled until product, manufacturer, origin, location, and a usable barcode rule are ready.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA should confirm the Scanner step unlocks after the first successful rule-save inbound and the barcode input receives focus.
- Live Supabase/device QA remains required for real rule save/reuse, camera scanning, external scanner input, duplicate blocking, print/PDF output, and undo RPC evidence.

Next recommended task:

- Run a first-time supplier-barcode inbound session: choose product/manufacturer/origin/location, verify Scanner is blocked before rule save, save one sample barcode with actual kg, then confirm Scanner unlocks for continuous scanning.

## 2026-06-30 - Stock inbound summary helper made mode-neutral

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound page-helper copy, guided-flow coverage, and audit documentation.
- Fixed the Session Summary helper text so it no longer says only `saved labels` in supplier-barcode mode.
- The summary cue now says `saved scans or labels`, which matches both Supplier barcode and No supplier barcode guided flows.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm the summary cue is clear in both supplier-barcode and no-supplier/manual-weight sessions.
- Live Supabase/device QA remains required for real summary data, print/PDF output, camera/external scanner behavior, rule save/reuse, and undo RPC evidence.

Next recommended task:

- Finish one supplier-barcode session and one no-supplier/manual-weight session, then confirm the summary wording and totals match each flow.

## 2026-06-30 - Stock inbound recent template mode text fixed

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound Page 1 recent-template copy, guided-flow coverage, and audit documentation.
- Fixed recent inbound template status text so no-supplier/manual-weight mode no longer says a template opens the scanner or barcode-rule setup.
- Supplier-barcode mode still shows `Saved rule ready: opens scanner` or `No rule yet: opens rule setup`.
- No-supplier/manual-weight mode now shows `Template ready: opens manual weight`, matching the actual guided flow after tapping a template.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px should confirm recent template buttons read clearly in both supplier-barcode and no-supplier/manual-weight modes.
- Live Supabase/device QA remains required for real saved-template selection, camera scanning, external scanner Enter behavior, barcode rule save/reuse, label printing/PDF, and undo RPC evidence.

Next recommended task:

- Open `/stock/inbound`, switch between Supplier barcode and No supplier barcode, tap a recent template, and confirm the status text and next page match the selected mode.

## 2026-06-30 - Stock inbound generated display-name preview clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound setup preview, source coverage, and guided-flow audit documentation.
- Renamed the setup preset preview from `Display product name` to `Generated display name` so workers see that the final inbound name is generated from manufacturer + product.
- Added helper copy under the setup preview: `Manufacturer + product. Reports can still filter them separately.`
- Added a source marker for the generated display-name preview and tightened guided-flow coverage around it.
- Updated stale Stock acceptance/mobile guards that expected the older setup-preview wording.
- No database schema, migrations, RLS, role/scope rules, stock save behavior, barcode uniqueness, movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA at about 390px is still required to confirm the setup card reads cleanly in both supplier-barcode and no-supplier/manual-weight inbound modes.
- Live Supabase/device QA remains required for camera scanning, external scanner Enter behavior, barcode rule save/reuse, duplicate issue rows, label printing/PDF, undo RPC evidence, and whole-session undo evidence.

Next recommended task:

- Test `/stock/inbound` setup with an existing product/manufacturer and a custom typed product/manufacturer, then confirm the preview shows `Manufacturer Product` while reports still filter by product and manufacturer separately.

## 2026-06-30 - Stock issue handling worker message tightened

Task completed:

- Re-checked the existing Stock issue-handling implementation against the latest requirement to keep workers out of long issue forms.
- Confirmed the existing issue model auto-creates structured `barcode_scan_logs` issue rows for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged review.
- Confirmed issue rows carry issue type, barcode, detected/selected item context, expected/scanned location and status, related session/order/customer/transfer context, created-by scanner user, timestamp, and review status.
- Confirmed the manager issue review queue shows all open issue types with approve, reject, and mark-corrected actions, while stock-take mismatch/manual correction still uses the proper approval workflow before database adjustment.
- Tightened the spoiled/damaged return success message so workers see that the return was saved for manager review.
- Added a source smoke guard for the clearer spoiled/damaged return review message.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - first run failed on a stale Stock acceptance wording guard; updated the guard from the old spoiled/damaged return success message to the new manager-review wording.
- `node scripts\stock-acceptance-coverage.mjs` - passed after the stale guard update.
- `npm.cmd run smoke` - passed after the stale guard update.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should still create one issue from inbound, outbound, stock take, transfer/receive-transfer, return, and damage/spoilage, then verify scoped manager visibility and review actions.
- The generic manager issue review action records review evidence only; stock corrections still require the proper approval workflow by design.

Next recommended task:

- Run real scanner/manual barcode QA from a worker profile, trigger one issue in each Stock workflow, and verify the manager queue is readable without asking workers for long issue details.

## 2026-06-30 - Stock inbound print summary includes voided scans

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound print-only session summary and source coverage.
- Added voided audit rows to the A4 `Print session summary` output so printed/PDF summaries match the on-screen summary after whole-session undo.
- The printed summary now includes saved barcode rows, duplicate/error rows, and voided rows when those rows exist.
- Added guided-flow coverage for printed voided rows and status column.
- Updated the guided inbound completion audit to include voided audit rows in the print-summary evidence.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, server actions, route access rules, or stock-save behavior were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser print/PDF QA is still required to confirm the printed A4 summary looks clean after voiding a whole session.
- Live Supabase/device QA remains required for camera scanning, external scanner Enter behavior, saved rule reuse, duplicate issue rows, label printing/PDF, undo RPC evidence, whole-session undo evidence, and 390px authenticated layout.

Next recommended task:

- Finish and undo a real inbound session, then use `Print session summary` and confirm the PDF includes voided rows plus saved/error rows where applicable.

## 2026-06-30 - Stock inbound whole-session undo summary clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound session summary and source coverage.
- Added a `Voided scans kept for audit` section to the inbound session summary so whole-session undo still shows which barcodes/labels were reversed.
- The summary now separates saved stock rows from voided audit rows, avoiding the impression that undone scans are still available stock.
- Added coverage to require the voided summary section and audit wording.
- Updated the guided inbound completion audit with the voided-scan summary behavior.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, server actions, route access rules, or stock-save behavior were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating the stale Stock acceptance guard for the new `voidedSessionScans` implementation.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA is still required to confirm `void_inbound_stock_session` voids the expected database rows and the UI receives the expected success result.
- Real phone/camera/external-scanner/label-printing QA remains required before the full guided Stock Inbound goal can be marked complete.

Next recommended task:

- Run a real inbound session, finish it, use whole-session undo, then verify the summary shows voided entries while stock movements and scan logs show `INBOUND_VOID`.

## 2026-06-30 - Stock inbound manual-weight shortcut clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to the Stock Inbound no-supplier/manual-weight page and source coverage.
- Made the manual-weight page explicitly show the fast worker path: type weight, press Enter or phone Done, print/attach the generated label, scan the printed label to save, then continue to the next unit.
- Added a source guard so the visible Enter/Done shortcut, `handleNetWeightKeyDown`, and continuous scan-back wording stay protected.
- Updated the guided inbound completion audit to show that the no-barcode/manual-weight page is source-proven for reduced typing/clicks.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, server actions, route access rules, or stock-save behavior were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is still required to confirm the decimal keyboard shows a Done action and Enter/Done feels natural for workers.
- Live Supabase/device QA remains required for camera scanning, external scanner Enter behavior, saved rule reuse, duplicate issue rows, label printing/PDF, undo RPC evidence, whole-session undo evidence, and 390px authenticated layout.

Next recommended task:

- Test `/stock/inbound` no-supplier/manual-weight mode on a phone: type a weight, press Done, confirm the label preview appears, print/export, scan the generated label, and verify the weight field clears for the next unit.

## 2026-06-30 - Stock inbound movement display-name linkage guarded

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound/report source coverage and documentation.
- Added source coverage proving the latest inbound RPC links `stock_movements.stock_unit_id` to the newly created `stock_units.id`.
- Added report coverage proving movement rows derive product/manufacturer display names from the linked stock unit, so inbound movement reports can show manufacturer + product while keeping Product, Manufacturer, and Origin as separate report fields.
- Updated the guided inbound completion audit with the live inbound movement display-name linkage evidence.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, server actions, route access rules, or UI behavior were changed.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/browser QA is still required to prove actual saved inbound movement rows render correctly in reports after migrations are applied.
- Real phone/camera/external-scanner/label-printing QA remains required before the full guided Stock Inbound goal can be marked complete.

Next recommended task:

- Run a logged-in `/stock/inbound` supplier-barcode session on staging/Vercel, then verify the saved stock unit, inbound movement, scanner summary, and stock report all show manufacturer + product display name with separate manufacturer filtering.

## 2026-06-30 - Stock issue handling guard tightened

Task completed:

- Refined the Stock issue-handling regression guard so workers do not need long issue forms while managers still get structured review rows.
- Kept the existing `barcode_scan_logs` issue record model; no new issue table or migration was added.
- Confirmed issue records are auto-created for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged review.
- Tightened coverage so issue rows must carry issue type, barcode, detected/selected item, expected/scanned location, expected/scanned status, related session/order/customer/transfer context, created-by user, timestamp, and review status.
- Confirmed workers keep short blocked-scan messages and manager review supports approve, reject, and mark corrected without directly changing stock.
- Confirmed stock-take mismatch/manual correction still requires the stock-take review/approval workflow before any database adjustment.

Files changed in this pass:

- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should still trigger real scan issues from inbound, outbound, stock take, transfer, receive-transfer, return, and spoiled/damaged review, then verify they appear in the manager queue with useful context.
- The manager issue review queue records review decisions only; real stock correction remains in the proper approval workflows by design.

Next recommended task:

- Use demo users on Vercel/staging to create one issue from each stock workflow, then review them as manager/admin and confirm scoped visibility.

## 2026-06-30 - Stock demo display names aligned

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock demo/report naming and source coverage.
- Updated Stock demo movement, stock-take, damage/spoilage, and return-supplier rows so item names use the manufacturer + product display-name rule instead of the old `category / section / product` format.
- Added report coverage that requires demo Stock rows such as `TICAN BELLY BONELESS`, `SEABOARD LOIN BONELESS`, `RIVASAM BELLY BONELESS`, and `RIVASAM LOIN BONELESS`, and blocks the old slash-style demo item names from returning.
- Updated the guided inbound completion audit to include demo-mode report/display-name evidence.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `lib/stock/demo-data.ts`
- `scripts/stock-report-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase data still needs QA because real rows can contain older movement names from historical records.
- Browser QA should confirm Stock demo/report pages show the manufacturer + product display name and still keep Manufacturer as a separate column/filter.

Next recommended task:

- Continue source/browser hardening for `/stock/inbound`, then verify real Supabase rows through the saved barcode-rule and session-summary flows.

## 2026-06-30 - Stock inbound mode guidance clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Kept changes scoped to Stock Inbound worker-facing copy and source coverage.
- Made the inbound card title and help text switch by mode:
  - `Supplier barcode inbound` for supplier barcode rule/scanning flow.
  - `No supplier barcode inbound` for internal-label/manual-weight flow.
- Added setup-page mode guidance cards so workers can quickly understand the two paths:
  - Supplier barcode: scan sample, save rule, then scan continuously.
  - No supplier barcode: enter weight, print 50mm x 30mm label, attach it, then scan label to save.
- Updated guided inbound coverage and completion audit to guard the mode-specific guidance.
- No database schema, migrations, RLS, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, server actions, or route access rules were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed after updating the stale title guard.
- `npm.cmd run smoke` - passed after updating the related Stock scanner coverage guard for the new mode-specific titles.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/device QA remains required for saved barcode-rule reuse, duplicate issue rows, camera scanning, external scanner Enter timing, label print/PDF, undo previous scan RPC evidence, whole-session undo RPC evidence, and 390px layout.
- Browser QA should confirm the mode guidance cards fit cleanly at phone width and make the no-supplier barcode path obvious to workers.

Next recommended task:

- Continue source/browser hardening for `/stock/inbound`, then run the full smoke/lint/typecheck/build gate again.

## 2026-06-30 - Stock issue handling coverage tightened

Task completed:

- Refined the Stock issue handling smoke guard so the worker/manager issue contract stays protected.
- Confirmed the current implementation auto-creates manager-review issue rows in `barcode_scan_logs` for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, missing transfer item, unexpected transfer item, and spoiled/damaged review.
- Confirmed issue rows carry issue type, barcode, detected/selected item context, expected/scanned location and status, related session/order/customer/transfer context, scanned-by user, and timestamp.
- Confirmed workers still see short blocked-scan messages while manager review shows the full issue context.
- Confirmed manager review can mark issues approved, rejected, or corrected, and the generic issue review action does not directly adjust `stock_units`, `stock_movements`, or stock-take adjustments. Stock take mismatch/manual correction remains approval-workflow-only.

Files changed in this pass:

- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm scoped managers see only open issue rows for their own stock locations while admin/director can review all.
- Real scanner QA should confirm issue rows are created from actual camera/external-scanner blocked scans and workers can continue scanning where the workflow allows.

Next recommended task:

- Generate real issue rows from inbound, outbound, transfer, receive-transfer, return/damage, and stock take, then review them from the manager scan issue queue.

## 2026-06-30 - Stock inbound scanner undo action moved up

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the barcode scanner dialog against the scanner-window requirements: camera scanning, external scanner input, live feedback, last saved item/weight, live totals, continuous scanning, and undo.
- Moved the scanner `scanActionSlot` up near live scan feedback so the inbound `Undo Previous Scan` action is visible while workers are scanning.
- Removed the lower duplicate action-slot position from the dialog.
- Tightened scanner and guided inbound coverage to require the primary scanner action slot marker.
- Updated the guided completion audit to record that scanner-window undo visibility is source-proven.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan RPC evidence, whole-session undo RPC evidence, and 390px layout.
- Browser QA should confirm the scanner dialog at phone width shows live feedback, Undo Previous Scan, last saved item/weight, and total count/weight without awkward scrolling.

Next recommended task:

- Test `/stock/inbound` with at least one saved scan, open the camera scanner, and verify `Undo Previous Scan` is visible near the scanner feedback and still voids the latest scan safely.

## 2026-06-30 - Stock inbound recent template status clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited recent inbound templates against the setup-page requirement.
- Confirmed saved-rule templates already set the selected item/manufacturer/origin and jump straight to the scanner, while templates without a rule open barcode-rule setup.
- Clarified the worker-facing template status text to say `Saved rule ready: opens scanner` or `No rule yet: opens rule setup`.
- Added a source marker for recent inbound template cards and tightened guided inbound coverage around the template status behavior.
- Updated the guided completion audit for barcode-inbound setup and no-supplier/internal-label setup.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating the related Stock mobile coverage guard for the clearer template wording.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan, whole-session undo RPC evidence, and 390px layout.
- Browser QA should confirm recent template cards remain readable at phone width and route workers to scanner/rule setup as the status line says.

Next recommended task:

- Test `/stock/inbound` with one saved-rule recent template and one no-rule recent template, then verify the first opens scanner and the second opens barcode-rule setup.

## 2026-06-30 - Stock inbound product dropdown display clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the product/manufacturer setup search and dropdown path.
- Confirmed product search already checks product name, generated manufacturer + product display name, default-manufacturer display name, selected-manufacturer display name, and item code.
- Improved the product dropdown option label so workers see `item code / category / section / generated display name` directly in the selector.
- Tightened guided inbound coverage so the product selector keeps item code and generated display name together.
- Updated the guided completion audit for barcode-inbound setup and no-supplier/internal-label setup.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan, whole-session undo RPC evidence, and 390px layout.
- Browser QA should confirm the product dropdown remains readable at phone width with long item codes and long manufacturer/product names.

Next recommended task:

- Test `/stock/inbound` at about 390px width and verify product search/dropdown results show item code plus generated display name without hiding separate manufacturer/product fields.

## 2026-06-30 - Stock inbound barcode-rule length cue added

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the barcode-rule page against the objective's sample barcode, weight-position rule, extracted preview, rule save, and length-mismatch requirements.
- Added a saved-rule readiness cue showing the expected barcode length before workers enter continuous scanning.
- The cue explains that a different barcode length shows a warning before saving, matching the existing `BARCODE_LENGTH_MISMATCH` behavior.
- Tightened guided inbound coverage so the rule-save summary must keep sample barcode and barcode length rows, and the saved-rule page must keep the expected-length cue.
- Updated the guided completion audit with the expected-length evidence.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan, whole-session undo RPC evidence, and 390px layout.
- Browser QA should confirm the expected-length cue appears after choosing a setup with a saved rule or after saving the first sample rule.

Next recommended task:

- Test `/stock/inbound` after login with a saved barcode rule, then scan a different-length barcode and confirm the warning appears before any mistaken save.

## 2026-06-30 - Stock issue handling review guard tightened

Task completed:

- Refined Stock issue handling so workers stay on short scan messages while issue rows carry manager-review context in `barcode_scan_logs`.
- Confirmed issue auto-create coverage for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, missing transfer item, unexpected transfer item, and spoiled/damaged review.
- Tightened manager issue review so the server action only updates currently open issue rows.
- Clarified the manager review queue copy: `Mark corrected` records review evidence only; real stock changes still use the proper stock take, damage/spoilage, return, transfer, or correction workflow.
- Tightened Stock issue coverage so inbound, outbound, transfer, receive-transfer, return/damage, and stock take issue sources plus manager review behavior remain guarded by smoke checks.
- No database adjustment behavior, stock movement rules, approval workflow, RLS policy, or migration SQL was changed.

Files changed in this pass:

- `lib/stock/actions.ts`
- `components/stock/stock-page.tsx`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm RLS lets scoped stock managers review only open issue rows for their own stock locations, while admin/director can review all.
- Real scanner QA should confirm workers see only short blocked-scan messages and can keep scanning where safe.
- Stock take mismatch/manual correction still requires the existing approval/correction workflows before database adjustment; the issue review queue intentionally does not adjust stock by itself.

Next recommended task:

- Test the manager scan issue review queue with real rows from inbound, outbound, transfer, return/damage, and stock take, then verify `Approve`, `Reject`, and `Mark corrected` only update issue review evidence.

## 2026-06-30 - Stock inbound printed summary includes scan errors

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the dedicated print-only A4 session summary against the on-screen session summary.
- Added duplicate/error scan rows to the printable session summary when the session has blocked/error scans.
- Tightened guided inbound coverage so the print summary must keep the duplicate/error section, message column, and per-error rows.
- Updated the guided completion audit to record the printed duplicate/error row evidence.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated browser QA should confirm `Print session summary` includes blocked/duplicate scan rows only when they exist.
- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan, whole-session undo RPC evidence, and 390px layout.

Next recommended task:

- Test `/stock/inbound` after login: create a duplicate/error scan, finish the session, and verify the printed summary includes that blocked scan section.

## 2026-06-30 - Stock inbound session summary print output added

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Found that `Print session summary` set the summary print target, but only label printing had a dedicated print-only output root.
- Added a dedicated print-only A4 inbound session summary root for `inboundPrintTarget === "summary"`.
- The printable summary includes Elite Meat title, display product, batch/session fields, total count/weight fields, and saved barcode/unit rows with barcode, product, weight, scanned-by, and time.
- Kept label printing separate: saved labels still use `StockLabelPrintArea`, while session summaries use the new summary print root.
- Tightened guided inbound coverage to guard the summary print root, A4 page setup, session field rows, saved barcode rows, and label-print separation.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated browser QA should confirm `Print session summary` opens the browser print sheet with the A4 session summary, while `Print label` still prints 50mm x 30mm labels.
- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan, whole-session undo RPC evidence, and 390px layout.

Next recommended task:

- Test `/stock/inbound` after login: finish a supplier-barcode and internal-label session, then verify `Print session summary` and `Print label` produce different print layouts.

## 2026-06-30 - Stock inbound reset helper coverage tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Re-audited the `New inbound session` reset path and confirmed barcode, weight, pending label, last-error, and logged-issue dedupe state belong in the shared `clearInboundDraftScan()` helper.
- Removed duplicate direct draft-state clearing from `startNewInboundSession()` so reset behavior stays centralized.
- Tightened guided inbound coverage to prove `clearInboundDraftScan()` clears barcode input, weight input, pending internal-label state, pending label refs, last-error dedupe, and logged issue dedupe; `startNewInboundSession()` now only needs to call that helper and clear full-session state.
- Updated the guided completion audit to record the shared reset-helper evidence.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated browser QA should confirm `New inbound session` starts cleanly after summary/undo flows and repeated duplicate/error scans are logged in the new session.
- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan, whole-session undo RPC evidence, and 390px layout.

Next recommended task:

- Test `/stock/inbound` after login: trigger a blocked/duplicate scan, finish or reset the session, start a new session, and confirm the same error can appear again for the new session if repeated.

## 2026-06-30 - Stock inbound new-session reset hardened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the `New inbound session` reset path from the session summary.
- Fixed a small reset gap so starting a new inbound session now explicitly clears barcode input, weight input, pending internal label state, pending label refs, pending internal-label submit ref, and print target.
- Tightened guided inbound coverage so future changes must keep the new-session reset clearing saved scans, errors, finished status, batch number, draft scan, pending label state, print target, session-saved rule flags, and return to setup.
- Updated the guided completion audit with the stronger new-session reset evidence.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated browser QA should confirm `New inbound session` starts cleanly after summary/undo flows and keeps the chosen setup only where intended.
- Live Supabase/device QA remains required for camera scanning, external scanner Enter timing, label printing/PDF, barcode rule save/reuse, duplicate issue row creation, Undo Previous Scan, whole-session undo RPC evidence, and 390px layout.

Next recommended task:

- Test `/stock/inbound` after login: finish a supplier-barcode session, use `New inbound session`, and confirm the next session has a new code with empty barcode/weight/pending-label state.

## 2026-06-30 - Stock inbound display-name report guard tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Re-audited the manufacturer + product display-name path through Stock report rows and report filters.
- Tightened `scripts/stock-inbound-guided-flow-coverage.mjs` so the guided inbound goal now directly guards report display names plus separate Product, Manufacturer, and Origin report filters.
- Updated the guided completion audit to record that report/search evidence is source-guarded by the guided inbound coverage, not only by the broader stock report checks.
- No UI behavior, database schema, migrations, RLS, role/scope rules, barcode validation, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live report QA is still required to prove real Supabase Stock report rows show the generated manufacturer + product display name and that Product/Manufacturer/Origin filters behave correctly with production data.
- Authenticated 390px browser QA, real phone camera scanning, external scanner Enter timing, Bluetooth/PDF label printing, live Supabase rule save/reuse, duplicate issue row creation, and undo RPC evidence remain device/live-data QA.

Next recommended task:

- Complete logged-in `/stock/inbound` and `/stock/reports` QA with real sample stock so the display-name and separate-filter behavior is verified end to end.

## 2026-06-30 - Stock inbound whole-session undo guard tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Re-audited the session summary and whole-session undo/delete path.
- Added explicit Stock Inbound action markers for opening the whole-session undo confirmation and starting a new inbound session.
- Tightened guided inbound coverage so future changes must keep the whole-session undo button disabled when there are no saved scans, keep the safe confirmation copy/buttons, and preserve the new-session reset action.
- Updated the guided completion audit with the stronger whole-session undo/delete evidence.
- No database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, undo RPC behavior, approval logic, or server actions were changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live RPC QA is still required to prove whole-session undo voids real saved stock units and writes `INBOUND_VOID` movement/log/audit evidence in the target Supabase project.
- Authenticated 390px browser QA, real phone camera scanning, external scanner Enter timing, Bluetooth/PDF label printing, live Supabase rule save/reuse, and duplicate issue row creation remain device/live-data QA.

Next recommended task:

- Complete the logged-in `/stock/inbound` live QA path, especially whole-session undo after saving at least two supplier/manual-label units.

## 2026-06-30 - Stock inbound label print evidence tied to guided flow

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Re-audited the internal-label/no-supplier inbound path against the print-label part of the objective.
- Confirmed the label component already supports `thermal-50x30`, 50mm x 30mm dimensions, PDF/phone print fallback, Bluetooth-printer wording, company/product/weight/barcode fields, and one label per printed page.
- Tightened `scripts/stock-inbound-guided-flow-coverage.mjs` so guided inbound coverage directly guards the 50mm x 30mm one-label-per-page print contract instead of relying only on the separate label coverage script.
- Updated the guided completion audit to record the one-label-per-page print CSS evidence.
- No business logic, database schema, migrations, RLS, role/scope rules, barcode validation, stock movements, undo RPCs, or approval workflows were changed.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated 390px browser QA is still required for the logged-in Stock Inbound scanner, summary, and no-horizontal-scroll checks.
- Real phone camera scanning, external scanner Enter timing, Bluetooth/PDF label printing, live Supabase rule save/reuse, duplicate issue row creation, Undo Previous Scan, and whole-session undo remain device/live-data QA.

Next recommended task:

- Complete the live `/stock/inbound` supplier-barcode and no-supplier/internal-label QA paths from `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md` after login/device access is available.

## 2026-06-30 - Stock inbound local route QA attempted

Task completed:

- Continued the active guided Stock Inbound multi-page session goal with a local route/browser-readiness check.
- Started the Next.js dev server locally and requested `/stock/inbound`.
- Confirmed the route is reachable in the app, but the unauthenticated request redirects to `/login`; the local response chain was `GET /stock/inbound 307` then `GET /login 200`.
- Attempted browser automation for a 390px check, but the available local automation runtime does not include a usable Playwright core package.
- No business logic, database schema, RLS, role/scope rules, barcode validation, stock movements, undo RPCs, or approval workflows were changed.

Files changed in this pass:

- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- Local dev route check for `/stock/inbound` - reachable, redirected to `/login` without an authenticated session.
- Browser automation availability check - blocked because Playwright core is missing in the local runtime.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated 390px browser QA is still required for the logged-in Stock Inbound scanner, summary, and no-horizontal-scroll checks.
- Real phone camera scanning, external scanner Enter timing, Bluetooth/PDF label printing, live Supabase rule save/reuse, duplicate issue row creation, Undo Previous Scan, and whole-session undo remain device/live-data QA.

Next recommended task:

- After logging in locally or on Vercel as a stock user, open `/stock/inbound` at phone width and complete the supplier-barcode and internal-label manual QA paths listed in `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`.

## 2026-06-30 - Stock inbound summary review wording clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Improved the finished-session summary action wording so workers see `Review Scanner` or `Review Manual Weight` instead of `Back to Scanner` / `Back to Manual Weight`.
- This keeps the summary navigation aligned with the existing review-only guard after `Finish Inbound Session`; it does not reopen saving or scanning.
- Tightened `scripts/stock-inbound-guided-flow-coverage.mjs` to guard the safer summary review wording.
- Updated the guided completion audit to record that finished-session summary navigation is review wording, not an active workflow prompt.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, undo RPCs, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating the related stock acceptance/mobile coverage guards for the new review wording.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The active guided Stock Inbound goal still needs live/device QA for phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- Browser-test `/stock/inbound` summary navigation at about 390px width.

## 2026-06-30 - Stock inbound finished-session scan lock guarded

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Re-audited the current source against the scanner and summary parts of the objective.
- Confirmed the camera scanner popup already shows locked setup, last saved item/weight, inbound session total, camera-window scan count, external scanner input, continuous auto-save copy, and no OK/Next step for continuous scanning.
- Strengthened `scripts/stock-inbound-guided-flow-coverage.mjs` so future changes must preserve the finished-session scan lock: after `Finish Inbound Session`, scanning is review-only and the worker is told to start a new session or ask a manager for changes.
- Updated the guided completion audit to record the finished-session review-only scanner guard.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, undo RPCs, or server actions.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The active guided Stock Inbound goal still needs live/device QA for phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- Test `/stock/inbound` in a browser at about 390px width to verify finished sessions show review-only scanner/manual pages.

## 2026-06-30 - Stock issue handling receive-transfer context tightened

Task completed:

- Refined Stock issue handling so workers continue to see short scan messages instead of filling long issue forms.
- Audited the existing structured issue path using `barcode_scan_logs` for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged stock requiring review.
- Fixed the receive-transfer server action so unexpected-barcode issues now include the receiving location, expected status, and scanned status. This keeps the manager review queue useful and keeps scoped RLS able to show the issue to the correct receiving-location manager.
- Tightened `scripts/stock-issue-handling-coverage.mjs` so the receive-transfer unexpected-barcode context cannot regress.
- Confirmed generic issue review only changes issue review status/note/reviewer/timestamp; stock take mismatch/manual correction database adjustment remains in dedicated approval workflows.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA is still needed with worker and manager demo accounts to trigger one issue from each stock flow and confirm each issue appears in the manager queue under RLS.
- Manual QA should confirm manager issue review does not itself change stock quantity, location, or status outside stock-take, damage/spoilage, return-supplier, and correction approval flows.

Next recommended task:

- In staging/Vercel, trigger one issue each from inbound, outbound, stock take, transfer, and return, then confirm the manager queue shows context and the worker sees only a short blocked/warning message.

## 2026-06-30 - Stock inbound completion audit coverage tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Re-audited the guided inbound source, completion audit, and handoff from current files.
- Tightened `scripts/stock-inbound-guided-completion-audit-coverage.mjs` so the completion audit must explicitly mention that the no-supplier/internal-label flow is guarded as a 3-step setup -> manual weight -> summary flow.
- Rechecked the display-name/report path: visible stock identities use the generated manufacturer + product display name, while reports still expose and filter product, manufacturer, and origin separately.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, scanner behavior, undo behavior, or server actions.

Files changed in this pass:

- `scripts/stock-inbound-guided-completion-audit-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The active guided Stock Inbound goal still needs live/device QA for real phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- Use the Vercel or local browser at about 390px width to visually verify the internal-label 3-step path and the supplier-barcode 4-step path side by side.

## 2026-06-30 - Stock inbound internal-label step guard tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the current guided inbound source against the remaining source-verifiable requirements.
- Confirmed the no-supplier/internal-label path is already mode-aware as a 3-step flow: setup -> manual weight -> summary.
- Strengthened guided inbound coverage so future changes must preserve the internal-label summary label as `3 Summary` instead of accidentally using the supplier-barcode `4 Summary` label.
- Updated the guided completion audit to record that the no-barcode/internal-label 3-step page sequence is source-guarded.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, scanner behavior, undo behavior, or server actions.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - failed once because it was run in parallel with `npm.cmd run build`, while `.next/types` was being regenerated.
- `npm.cmd run typecheck` - passed after build completed.

Risks / remaining checks:

- The active guided Stock Inbound goal still needs live/device QA for real phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.
- Real browser QA should confirm internal-label mode shows Page 1/3 setup, Page 2/3 manual weight, and Page 3/3 summary with matching step labels.

Next recommended task:

- Run `/stock/inbound` in internal-label mode at about 390px width and verify the worker sees a clean 3-page setup -> manual weight -> summary flow before testing real label scan-back.

## 2026-06-30 - Stock issue handling re-verified

Task completed:

- Refined the Stock issue-handling task by auditing the existing stock issue model instead of adding another long worker issue form.
- Confirmed blocked/problem scans create structured `barcode_scan_logs` issue rows for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged stock requiring review.
- Confirmed issue rows carry the requested context where available: issue type, barcode, detected item, selected item, expected/scanned location, expected/scanned status, related session/order/customer/transfer context, creating user, and timestamp.
- Confirmed worker UI keeps short messages and can continue scanning where safe; workers are not asked to fill a long issue form.
- Confirmed the manager scan issue review queue shows all open issue types and allows approve, reject, or mark corrected.
- Confirmed issue review only updates review status/note/reviewer/timestamp; stock-take mismatch and manual correction adjustments remain in their dedicated approval workflows and are not applied by generic issue review.
- Confirmed `npm.cmd run smoke` includes `scripts/stock-issue-handling-coverage.mjs`, so inbound, outbound, stock take, transfer, return, and manager-review issue coverage is guarded by the main smoke suite.

Files changed in this pass:

- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA is still needed with real/demo worker and manager accounts to trigger one issue from each flow and confirm the manager review queue displays the saved context correctly under RLS.
- Manual QA should confirm manager issue review does not change stock quantity/location/status outside the dedicated stock-take, damage/spoilage, return-supplier, and correction workflows.

Next recommended task:

- In staging/Vercel, trigger duplicate inbound, supplier barcode length mismatch, barcode rule detection failure, outbound barcode not found, wrong-location transfer receive, transfer missing/unexpected item, return unavailable stock, unknown stock-take barcode, stock-take mismatch, and spoiled/damaged review, then verify each appears in Manager scan issue review.

## 2026-06-30 - Stock inbound product search exact-match tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited manufacturer-based display names in Stock Inbound setup/search.
- Tightened product search exact-match behavior so the normalized product name from `stockProductName()` is treated as an exact match, not only raw `item.name`, display name, or item code.
- Added the normalized product name into product search text so section + name products such as `Belly Boneless` are easier to find before workers create a new product.
- This reduces the chance of accidental duplicate product creation during guided inbound setup while keeping product and manufacturer separate.
- Updated guided inbound coverage and the completion audit to guard the search/display-name behavior.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, scanner behavior, undo behavior, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should confirm searching a normalized product name such as `Belly Boneless` finds the existing product and does not push the worker toward duplicate creation.
- The active guided Stock Inbound goal still needs live/device QA for real camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In `/stock/inbound`, search for an existing section + product name, choose a manufacturer, and confirm the quick-create prompt stays hidden when the product already exists.

## 2026-06-30 - Stock inbound internal-label pending state fixed

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the supplier barcode autosave and internal-label scan-back paths.
- Fixed generated internal-label state so a label created for printing starts as `PENDING`, not `SAVED`.
- Confirmed the server-success path still changes that pending label to `SAVED` only after the worker scans the printed label back and the inbound save succeeds.
- Strengthened guided inbound coverage to inspect the actual `generateLabelBarcode()` block and `inboundFormAction()` save transition.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, scanner behavior, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live no-supplier/internal-label QA should confirm a generated label appears as pending/not saved, then becomes saved only after scan-back and successful server save.
- The active guided Stock Inbound goal still needs live/device QA for real camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In `/stock/inbound`, switch to No supplier barcode, enter a weight, generate an internal label, confirm it is pending before scan-back, then scan the printed/generated barcode and verify the saved count and total weight increase only after save succeeds.

## 2026-06-30 - Stock inbound barcode-rule origin wording aligned

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the guided inbound setup, recent templates, summary back-navigation, and Barcode Rule copy.
- Confirmed the summary already has safe read-only/back actions for setup and scanner/manual review.
- Fixed the Barcode Rule detected-preview instruction so it tells workers the saved rule applies to future scans for the selected product, manufacturer, and origin.
- Updated guided inbound, Stock acceptance, and Stock mobile UX coverage to guard the product + manufacturer + origin wording.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, scanner behavior, undo behavior, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Barcode Rule QA should confirm a worker understands the saved rule is scoped to product + manufacturer + origin and that a different origin needs a different rule.
- The active guided Stock Inbound goal still needs live/device QA for real camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In `/stock/inbound`, save a sample barcode rule for one product/manufacturer/origin, then test another origin for the same product/manufacturer and verify the UI asks for its own rule.

## 2026-06-30 - Stock inbound scanner setup context surfaced

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Improved the Stock camera scanner popup so the top status area now shows the current inbound setup before and during scanning.
- The scanner popup now keeps display product, product, manufacturer, origin, and location context visible through the `Current inbound setup` card, instead of leaving that context only below the camera.
- This makes continuous scanning safer because workers can confirm the locked session context while the camera window is open.
- Updated scanner, guided inbound, and mobile UX coverage to guard the top scanner setup card and the display/product/manufacturer/origin context passed into it.
- Updated the guided Stock Inbound completion audit with the stronger scanner-window evidence.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, auto-save behavior, undo logic, or server actions.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone/laptop camera QA should confirm the top current-setup card is readable in the scanner popup around 390px width and does not crowd the camera area too much.
- The active guided Stock Inbound goal still needs live/device QA for real camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- Open `/stock/inbound`, choose product/manufacturer/origin/location, open the scanner, and confirm the popup top cards show the current setup plus live session totals before scanning the next barcode.

## 2026-06-30 - Stock inbound barcode-rule save summary clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Tightened the Barcode Rule page so the `Rule values to save` card now shows display product, product, manufacturer, and origin as separate fields before the worker saves the barcode rule.
- This keeps the product/manufacturer separation visible at the rule-save decision point while still using the generated display name for worker readability.
- Updated guided inbound, Stock acceptance, and Stock mobile UX coverage so future changes cannot collapse the barcode-rule save summary back into one combined target row.
- Updated the guided Stock Inbound completion audit to reflect the clearer rule-save summary evidence.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, scanner save behavior, undo logic, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should confirm the Barcode Rule page is readable at about 390px width and that the save summary clearly shows display product, product, manufacturer, and origin before saving.
- The active guided Stock Inbound goal still needs live/device QA for phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In `/stock/inbound`, choose or type product `Belly Boneless`, manufacturer `Locks NV`, and an origin; go to Barcode Rule and confirm `Rule values to save` shows `Locks NV Belly Boneless`, `Belly Boneless`, `Locks NV`, and the selected origin separately before saving.

## 2026-06-30 - Stock issue handling verification

Task completed:

- Refined Stock issue handling review without adding long worker issue forms.
- Verified the current Stock issue model uses `barcode_scan_logs` as the issue/audit record for blocked or review-needed stock scans.
- Confirmed issue rows cover duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown barcode during stock take, stock take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged review.
- Confirmed issue rows include barcode, issue type, detected item, selected item, expected/scanned location, expected/scanned status, related session/order/customer/transfer context, scanned-by user, timestamp, and manager review status.
- Confirmed workers keep short messages and can continue scanning where the workflow allows; managers see the consolidated scan issue review queue with approve, reject, and mark-corrected actions.
- Confirmed issue review itself does not apply stock adjustments. Stock take mismatch and manual correction remain routed through the existing approval/adjustment workflows.

Files changed in this pass:

- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA is still needed with a worker and manager account to create one issue from each stock flow and verify the manager review queue displays the saved context correctly.
- Manual QA should confirm manager review actions do not accidentally adjust stock balances outside the dedicated approval workflows.

Next recommended task:

- In staging/Vercel with real demo data, trigger duplicate inbound, barcode not found outbound, wrong-location receive-transfer, unknown stock-take barcode, transfer missing/unexpected item, and spoiled/damaged review, then verify each appears in Manager scan issue review.

## 2026-06-30 - Stock inbound custom manufacturer/origin readiness aligned

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the setup readiness path for custom manufacturer and custom origin values.
- Aligned the template/setup auto-advance helper with the main scan-readiness rule, so custom manufacturer/origin sessions are treated as ready when the custom names are filled.
- This keeps the guided flow consistent between dropdown-selected manufacturer/origin and Other/custom manufacturer/origin setup.
- Updated guided inbound coverage and completion audit to guard custom manufacturer/origin readiness.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, label content, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should confirm an Other/custom manufacturer and Other/custom origin can proceed through the guided inbound flow without confusing auto-advance behavior.
- The active guided Stock Inbound goal still needs live/device QA for phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In `/stock/inbound`, choose Other/custom manufacturer and Other/custom origin, fill both names, then confirm the page can continue into Barcode Rule or Manual Weight without forcing dropdown-only values.

## 2026-06-30 - Stock inbound typed product display preview fixed

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Fixed the manual product/manufacturer setup preview.
- When a worker types a new product that is not yet in the product list, the setup preview now generates the display name from typed manufacturer + typed product instead of showing `No product selected`.
- Product and manufacturer remain separate fields; scanning still requires a real selected/created item before stock can be saved.
- Updated guided inbound coverage and completion audit to guard the manual typed product/manufacturer display-name preview.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, label content, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should confirm the setup page preview updates clearly when typing a new product and choosing/typing a manufacturer.
- The active guided Stock Inbound goal still needs live/device QA for phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In `/stock/inbound`, type a new product such as `Belly Boneless`, choose or type manufacturer `Locks NV`, and confirm the setup preview shows `Locks NV Belly Boneless` before creating the product.

## 2026-06-30 - Stock inbound pending internal label state clarified

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Audited the supplier-barcode fallback into no-supplier/internal-label inbound.
- Confirmed failed barcode-rule detection switches workers into internal-label mode, logs `BARCODE_RULE_DETECTION_FAILURE`, and guides them to generate/scan an internal label.
- Clarified local internal-label state so a generated label is `PENDING` until the worker scans the printed label back and the server save succeeds.
- Saved internal labels are still marked `SAVED` only after successful inbound save; summary/list totals still count only `SAVED` labels.
- Updated guided inbound coverage and completion audit to guard the pending-versus-saved distinction.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, label content, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/device QA is still needed to prove the pending label becomes a saved stock unit only after scan-back and server save.
- The active guided Stock Inbound goal still needs live/device QA for phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In `/stock/inbound`, generate an internal label, confirm the UI shows it as pending/not saved, scan it back, and verify the saved session count/weight updates only after the server save succeeds.

## 2026-06-30 - Stock inbound summary and label print separated

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Fixed a print-intent conflict in the inbound session summary.
- Label print buttons now explicitly activate the hidden 50mm x 30mm label print area.
- The `Print session summary` button now uses a separate summary print intent, so saved internal-label print CSS does not take over summary printing.
- Added saved-label reprint actions directly inside the manual/internal-label session summary when saved labels exist.
- Updated guided inbound and stock label coverage to guard the separate print intents.
- Updated the guided Stock Inbound completion audit to document the summary/label print separation.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, label content, or server actions.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser print/PDF behavior still needs real device QA for both actions:
  - saved internal-label print/PDF should output 50mm x 30mm labels;
  - session-summary print should print the summary, not labels.
- The active guided Stock Inbound goal still needs live/device QA for phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- In a logged-in `/stock/inbound` internal-label session, save at least one unit, finish the session, then test `Print session summary` separately from saved-label `Print label` / `PDF fallback`.

## 2026-06-30 - Stock scanner popup live status cards tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Improved the shared Stock scanner popup used by inbound scanning.
- Continuous scanner popup now shows top status cards for:
  - last saved item and weight;
  - persisted inbound session total;
  - current camera-window scan count.
- This makes the camera window itself clearer for workers during continuous scanning, instead of relying only on page content behind the popup.
- Updated Stock scanner coverage to guard the new camera-window scan counter.
- Updated the guided Stock Inbound completion audit to reflect stronger scanner popup evidence.
- Did not change scanning logic, camera permissions, stream cleanup, server actions, database schema, migrations, RLS, barcode uniqueness, stock movements, label generation, or approval logic.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass is source/build verified, but real scanner UX still needs live device QA.
- Remaining proof gates include real phone/laptop camera scanning, handheld scanner Enter timing, live Supabase barcode-rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- Open `/stock/inbound` on a real phone or laptop camera, start a continuous inbound scan, and confirm the popup top cards update clearly while the scanner remains ready for the next barcode.

## 2026-06-30 - Stock inbound manual-weight progress card tightened

Task completed:

- Continued the active guided Stock Inbound multi-page session goal.
- Tightened the no-supplier/internal-label manual weight page so workers see the previous or pending entered weight, saved unit count, and saved total weight together in the main manual progress card.
- When an internal label is generated but not yet scan-confirmed, the page now shows that pending label weight with a short scan-back note.
- Kept the existing fast flow: enter weight, generate label, print/attach, scan label to save stock, then clear/refocus the weight input for the next unit.
- Updated guided inbound source coverage so future changes must keep the manual-weight progress card and pending scan-back weight signal.
- Updated the guided completion audit to reflect the stronger no-supplier/manual-weight evidence.
- Did not change database schema, migrations, RLS, role/scope rules, barcode uniqueness, stock movement history, approval logic, label generation, or server actions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass is source/build verified, but the active guided Stock Inbound goal still needs live/device QA before it can be marked complete.
- Remaining proof gates include real phone/laptop camera scanning, handheld scanner Enter timing, live Supabase rule save/reuse, duplicate issue row creation, internal-label print/PDF output, scan-back save, Undo Previous Scan, whole-session undo, and 390px summary readability.

Next recommended task:

- Run a logged-in `/stock/inbound` no-supplier/internal-label session on a phone-width browser: enter one weight, generate the label, confirm the pending weight card, print/export PDF, scan the label back, and verify the field clears/focuses for the next unit.

## 2026-06-30 - Stock issue handling smoke coverage tightened

Task completed:

- Refined Stock issue handling verification so workers do not need long issue forms.
- Confirmed the existing Stock issue queue uses `barcode_scan_logs` with issue type, barcode, detected/selected item, expected/scanned location, expected/scanned status, related session/order/customer/transfer context, created-by user, timestamp, and review status.
- Strengthened `scripts/stock-issue-handling-coverage.mjs` so `npm.cmd run smoke` now separately guards automatic issue creation paths for:
  - inbound duplicate barcode, barcode length mismatch, and barcode rule detection failure;
  - outbound duplicate, wrong-location, and unavailable-stock scans;
  - transfer unexpected/missing item and wrong receive-location scans;
  - return/damage review issue scans;
  - stock-take unknown barcode, wrong item, and mismatch reporting.
- Added a guard that manager issue review only updates `barcode_scan_logs` review status/metadata and does not directly adjust `stock_units`, `stock_movements`, or stock-take adjustment state.
- Did not change Stock workflow behavior, database schema, migrations, RLS, role/scope rules, or UI layout in this pass.

Files changed in this pass:

- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.
- Existing prepared migrations remain required for live Supabase issue context/review behavior if not already applied:
  - `supabase/migrations/202606250007_stock_scan_issue_context_v1.sql`
  - `supabase/migrations/202606250009_stock_scan_issue_review_scope_v1.sql`

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass strengthens static/regression coverage. Live Supabase QA is still needed to create real issue rows from inbound, outbound, transfer, return/damage, and stock-take flows and verify the manager queue shows useful context.
- Stock-take mismatch/manual correction still depends on the existing manager/director approval workflows for database adjustment; scan issue review itself intentionally does not adjust stock.

Next recommended task:

- Run one live Supabase-backed worker scan for each issue type group, then review all rows from the manager dashboard and confirm only approved stock-take/manual-correction workflows change stock balances.

## 2026-06-30 - Stock reports use separate product/manufacturer fields

Task completed:

- Continued the active Stock Inbound guided multi-page session goal.
- Tightened the product/manufacturer display-name requirement on Stock reports.
- Stock report rows now carry explicit `itemName`, `brandName`, and `originName` fields in addition to the existing detail/category text.
- Stock reports now show separate Product, Manufacturer, and Origin columns, while keeping Detail for report-specific status/signature/movement notes.
- Stock report filters now match product, manufacturer, and origin against those separate report fields instead of relying only on generic detail text.
- Updated Stock report coverage to guard the new report columns and separate filter fields.
- Updated the guided completion audit to reflect stronger report/display-name evidence.
- Preserved Stock business logic, database schema, role/scope rules, RLS intent, barcode uniqueness, movement/audit behavior, approval workflows, and inbound actions.

Files changed in this pass:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real `/stock/reports` browser QA is still needed to confirm Product, Manufacturer, Origin, and Detail columns are readable at phone/desktop widths and export correctly from the deployed app.
- The active guided Stock Inbound goal remains open because live/device evidence is still required for supplier-barcode rule save/reuse, phone camera scanning, handheld scanner Enter timing, internal-label print/PDF, undo previous scan, whole-session undo, and 390px summary readability.

Next recommended task:

- Run the guided Stock Inbound proof path from `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`, then check `/stock/reports` with Product and Manufacturer filters against the same test stock.

## 2026-06-30 - Stock inbound guided completion audit added

Task completed:

- Continued the active Stock Inbound guided multi-page session goal.
- Audited the current source against the full guided Stock Inbound objective rather than narrowing the goal to existing smoke checks.
- Added `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md` with a requirement-by-requirement matrix for:
  - Separate product/manufacturer data with generated display name.
  - Supplier-barcode setup, barcode-rule learning, scanner, undo, and summary pages.
  - No-supplier/internal-label setup, manual weight entry, label scan-back, and summary pages.
  - Whole-session undo/delete safety.
  - Remaining live/device proof gates before the goal can be called complete.
- Added `scripts/stock-inbound-guided-completion-audit-coverage.mjs` so `npm.cmd run smoke` now verifies that the focused completion audit remains present and conservative.
- Wired the new completion-audit guard into `package.json`.
- Did not change Stock business logic, database schema, role/scope rules, RLS intent, barcode uniqueness, movement/audit behavior, approval workflows, or UI behavior in this pass.

Files changed in this pass:

- `docs/STOCK_INBOUND_GUIDED_COMPLETION_AUDIT.md`
- `scripts/stock-inbound-guided-completion-audit-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-completion-audit-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed, including the new completion-audit guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The active guided Stock Inbound goal remains open. Source evidence is strong, but completion still requires real logged-in Vercel/local QA for supplier-barcode rule save/reuse, length warning, duplicate block, phone camera scanning, handheld scanner Enter timing, internal-label print/PDF, undo previous scan, whole-session undo, and 390px summary readability.
- Playwright is not installed in this workspace, so this pass did not add browser screenshots or route automation.

Next recommended task:

- Run the new audit's `Remaining Proof Before Goal Completion` section against the deployed Vercel app or a local app connected to the target Supabase project.

## 2026-06-30 - Stock inbound guided QA handoff tightened

Task completed:

- Continued the active Stock Inbound guided multi-page session goal without adding new business features.
- Added a focused owner QA section for the two required guided inbound paths:
  - Supplier barcode flow: setup, barcode rule learning, continuous scanner, duplicate/length warnings, external scanner Enter input, undo scan, summary, and whole-session undo.
  - No-supplier/internal-label flow: setup once, manual weight entry, numeric internal barcode, 50mm x 30mm label preview, Bluetooth/PDF print path, scan-back confirmation, and repeated weight entry.
- Added the same focused guided Stock Inbound QA pass to the Vercel remote QA document so the deployed app can be tested without local access.
- Updated the Stock owner QA coverage guard so `npm.cmd run smoke` now requires these guided-flow QA checkpoints to remain documented.
- Preserved current Stock business logic, barcode uniqueness, movement/audit history, role/scope logic, RLS intent, and approval workflows.

Files changed in this pass:

- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-owner-qa-doc-coverage.mjs` - initially failed on a wording mismatch, then passed after aligning the guard with the documented warning text.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass improves owner/device QA documentation and smoke coverage only; it does not replace real logged-in Vercel testing.
- Real phone camera scanning, Bluetooth label printer selection, PDF fallback output, live Supabase barcode-rule save/reuse, undo previous scan, and whole-session undo still need owner/device evidence.
- The active Stock Inbound goal remains open until those runtime/device checks prove the full guided flow end to end.

Next recommended task:

- Run the focused `Guided Stock Inbound Vercel QA` section from `docs/STOCK_REMOTE_QA_VERCEL.md` with a real Stock test user and capture screenshots or short videos for supplier-barcode and internal-label sessions.

## 2026-06-30 - Stock scanner popup external Enter reliability

Task completed:

- Continued the active Stock Inbound guided multi-page session goal.
- Fixed the shared scanner popup external-handheld input so Enter submits `event.currentTarget.value` instead of the previous React state value.
- This matters for fast external barcode scanners that send all digits plus Enter quickly; the popup can now submit the live scanned value instead of an older value.
- The earlier inline barcode input already had this protection; this pass brings the scanner popup/window input to the same reliability level.
- Updated Stock scanner coverage to require the popup external scanner to submit the live typed value.
- Preserved camera scanning, continuous scanning, duplicate prevention, auto-save behavior, manual fallback, undo behavior, whole-session undo, barcode uniqueness, movement/audit history, role/scope logic, RLS intent, and approval rules.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real handheld scanner QA is still required in the scanner popup at `/stock/inbound` to confirm rapid barcode+Enter input saves the current barcode reliably in supplier-barcode and printed-internal-label flows.
- Live Supabase-backed supplier-barcode save/reuse, manual internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user and a handheld scanner, open the Stock Inbound scanner popup, scan quickly into the external scanner input, and confirm the barcode saved is the latest scanned value.

## 2026-06-30 - Stock inbound exact manufacturer product display name

Task completed:

- Continued the active Stock Inbound guided multi-page session goal.
- Tightened the shared stock display-name helper to follow the exact requested rule when a manufacturer is present: `manufacturer + " " + product name`.
- The helper no longer suppresses the manufacturer prefix just because the product name already starts with the same text.
- This affects the existing Stock Inbound surfaces that use the shared helper: recent templates, product search/dropdowns, scanner context, session summary, labels, stock reports, stock unit result rows, and scan issue display names.
- Product and manufacturer remain separate fields for filtering/reporting; only the generated display name changed.
- Updated the focused Stock Inbound guided-flow coverage to assert the exact `manufacturer product` display-name contract.
- No database schema, RLS, role logic, barcode uniqueness, stock movement history, approval logic, or scanner behavior was changed.

Files changed in this pass:

- `lib/stock/display-names.ts`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Existing legacy product names that already include the manufacturer may now display with the manufacturer repeated. This matches the explicit goal because product and manufacturer should be stored separately; legacy data should be cleaned by editing the product name, not by hiding the manufacturer prefix in display logic.
- Real logged-in browser QA at `/stock/inbound` is still needed to confirm the display name appears correctly through setup, barcode rule, scanner, manual-label, summary, labels, and reports at phone width.
- Live Supabase-backed supplier-barcode save/reuse, manual internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a stock user, create/select product `Belly Boneless` and manufacturer `Locks NV`, then confirm `/stock/inbound` shows `Locks NV Belly Boneless` in setup, scanner, label preview, session summary, stock unit result, and stock report surfaces.

## 2026-06-30 - Stock inbound guided-flow coverage guard

Task completed:

- Continued the active Stock Inbound guided multi-page session goal.
- Audited the current Stock Inbound source against the requested supplier-barcode and no-supplier-barcode flows.
- Added a focused coverage script for the exact guided Stock Inbound contract so future changes cannot accidentally flatten the flow back into one dense form.
- The new guard checks:
  - Supplier barcode pages: setup -> barcode rule -> scanner -> summary.
  - No-supplier-barcode pages: setup -> manual weight -> summary.
  - Read-only session code, recent templates, profile-default location with Change Location, product/manufacturer dropdown and manual entry, and manufacturer + product display names.
  - Barcode rule sample scan, weight position/digits/decimals, extracted preview, rule save, barcode length warning, and internal-label fallback.
  - Continuous camera/external scanner behavior, auto-submit for valid scans, duplicate block, live count/weight totals, and Undo Previous Scan.
  - Manual weight label flow, scanner hidden until label generation, 50mm x 30mm label support, clearing/refocusing weight after save.
  - Session summary, print-ready area, saved/error lists, and safe whole-session undo with movement/audit trail.
- Wired the new guard into `npm.cmd run smoke`.
- No Stock business behavior, database schema, RLS, role logic, movement logic, approval logic, or label logic was changed in this pass.

Files changed in this pass:

- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed, including the new Stock Inbound guided-flow guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass improves automated source/regression evidence only. Real logged-in browser QA at `/stock/inbound` is still needed at 390px.
- The local `agent-browser` CLI was not installed, so Codex could not capture a runtime phone-width screenshot in this pass without adding tooling.
- Live Supabase-backed tests remain needed for supplier-barcode rule save/reuse, manual internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, run `/stock/inbound` on a phone-width browser and complete one supplier-barcode session and one no-supplier-barcode/internal-label session, then confirm the new guided-flow smoke guard matches the real user experience.

## 2026-06-30 - Stock issue handling context hardening

Task completed:

- Refined Stock issue handling without adding long worker forms.
- Confirmed the existing issue model uses `barcode_scan_logs` as the stock issue/audit queue with issue type, barcode, item, selected item, expected/scanned location, expected/scanned status, related context, created-by user, timestamp, and review status.
- Confirmed issue records are auto-created from Stock Inbound, Outbound, Transfer, Receive Transfer, Return/Damage, and Stock Take blocked/exception paths.
- Kept worker-facing messages short so workers can continue scanning where safe.
- Kept manager review queue visibility for all open issue types, with approve/reject/correct actions.
- Tightened duplicate inbound barcode logging so the server records the detected existing stock item plus inbound session/manufacturer/origin context, not only the selected item.
- Updated issue-handling coverage to guard the richer duplicate inbound issue context.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.
- Existing prepared migrations remain the issue-handling schema/RLS basis if they have not been run yet:
  - `supabase/migrations/202606250007_stock_scan_issue_context_v1.sql`
  - `supabase/migrations/202606250009_stock_scan_issue_review_scope_v1.sql`

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- These checks are static/regression coverage and production build checks; live Supabase issue-row insert behavior still needs to be tested after the issue migrations are applied.
- Manager review actions mark issue rows approved/rejected/corrected; stock take adjustment/manual correction still stays in its separate approval workflow, as required.
- Real worker QA should scan duplicate, missing, wrong-location, unavailable, transfer unexpected/missing, return blocked, and stock-take unknown/mismatch cases and confirm the Manager scan issue review queue shows each row with useful context.

Next recommended task:

- In a Supabase-backed test environment, run the stock issue manual QA path: create one issue from each Stock flow, verify normal workers only see short messages, then verify a manager can see and review each issue without changing stock balances except through the existing approval workflows.

## 2026-06-30 - Stock scanner external Enter reliability

Task completed:

- Continued the active guided Stock Inbound multi-page session goal with a focused external-scanner reliability fix.
- The shared Stock barcode input now reads `event.currentTarget.value` on Enter instead of relying on the last rendered React state value.
- This helps fast handheld/external barcode scanners that send digits and Enter quickly, reducing the chance that the inbound scan submits the previous barcode or misses the current one.
- Applies to Stock Inbound and the other Stock scanner pages that use the shared `BarcodeField`.
- Preserved camera scanning, continuous scan behavior, duplicate prevention, stock save behavior, undo, whole-session void, barcode uniqueness, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Updated Stock scanner coverage to require the live input value path.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real handheld scanner QA is still required at `/stock/inbound` to confirm rapid barcode+Enter input saves the current barcode reliably in supplier-barcode and printed-internal-label flows.
- Live Supabase-backed supplier-barcode save, barcode-rule reuse, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user and a handheld scanner, test `/stock/inbound` by scanning quickly into the barcode input and scanner popup external-input field, then confirm the latest barcode is the one saved and shown in the recent scan list.

## 2026-06-30 - Stock inbound manual confirmation cleanup

Task completed:

- Continued the active guided Stock Inbound multi-page session goal with a focused no-supplier-barcode usability cleanup.
- The manual/internal-label path now keeps the supplier-barcode fallback `Generate internal label` button out of the printed-label scanner confirmation panel.
- Manual Weight Entry still shows the dedicated weight-entry generate-label control, then shows the scanner only after a label is generated and ready to confirm.
- Manual Weight Entry now shows the online-only connection warning even before the printed-label scanner appears, so workers understand why label generation/scanning is blocked.
- Preserved printed-label confirmation, stock save behavior, continuous next-weight behavior, undo, whole-session void, barcode uniqueness, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance coverage for supplier-only fallback controls and manual offline feedback.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required to confirm the Manual Weight page is clean at 390px, shows connection loss before label generation, and shows only printed-label scanning after a label is generated.
- Live Supabase-backed supplier-barcode save, barcode-rule reuse, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, switch `/stock/inbound` to `No supplier barcode`, briefly go offline to confirm the connection warning appears, reconnect, generate an internal label, then confirm the printed-label scanner panel is clean and focused on scanning the generated barcode.

## 2026-06-30 - Stock inbound manual-weight-first flow

Task completed:

- Continued the active guided Stock Inbound multi-page session goal with a focused no-supplier-barcode flow alignment.
- Manual Weight Entry now keeps the barcode scanner panel hidden until an internal label has been generated and is ready to be scanned.
- This makes the no-supplier-barcode path match the requested worker sequence: enter weight first, generate/print/attach label, then scan the printed label to save stock.
- The existing printed-label confirmation, stock save, label preview, continuous next-weight behavior, undo, whole-session undo, barcode uniqueness, movement/audit history, role/scope logic, RLS intent, and approval rules were preserved.
- Added Stock acceptance coverage for the manual page scanner visibility condition.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required to confirm the Manual Weight page feels weight-first at 390px and the scanner appears only after an internal label is generated.
- Live Supabase-backed supplier-barcode save, barcode-rule reuse, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, switch `/stock/inbound` to `No supplier barcode`, select product/manufacturer/origin/location, confirm the Manual Weight page starts with weight entry, generate a label, print/attach it, then confirm the printed-label scanner appears and saves the unit.

## 2026-06-30 - Stock inbound recent-template display-name fallback

Task completed:

- Continued the active guided Stock Inbound multi-page session goal with a focused recent-template naming fix.
- Recent inbound template display names now use the shared manufacturer/product helper with a clean product fallback.
- If a historical template references a missing manufacturer row, the UI no longer fabricates `No manufacturer Product`; it shows the product fallback while preserving separate manufacturer data where the brand exists.
- This keeps recent inbound selections aligned with scanner context, session summary, labels, stock-unit result surfaces, and stock reports.
- Preserved stock saving, barcode uniqueness, barcode-rule behavior, scanner flow, undo behavior, whole-session void behavior, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance coverage for the recent-template display-name helper contract.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required to confirm recent templates show the expected manufacturer + product names and never show a fake `No manufacturer` prefix.
- Live Supabase-backed supplier-barcode save, barcode-rule reuse, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, inspect recent templates, then complete one supplier-barcode and one internal-label session to confirm recent-template names, scanner names, summary names, labels, and reports all match.

## 2026-06-30 - Stock inbound summary scanned-by audit

Task completed:

- Continued the active guided Stock Inbound multi-page session goal with a focused Session Summary audit improvement.
- Saved barcode/unit entries in the finished inbound session summary now show `Scanned by` for each saved entry.
- The summary already showed barcode, display product name, weight, location, and time; this pass makes the saved-entry list match the audit visibility of the scan list and print-ready summary.
- Applies to supplier-barcode sessions and no-supplier-barcode/internal-label sessions because both use the shared saved-session entry list.
- Preserved stock saving, barcode uniqueness, barcode-rule behavior, scanner flow, undo behavior, whole-session void behavior, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance coverage so saved summary entries keep showing the scanner/user name.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required to confirm the finished Session Summary shows `Scanned by` cleanly at 390px for both supplier-barcode and internal-label sessions.
- Live Supabase-backed supplier-barcode save, barcode-rule reuse, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, complete one supplier-barcode inbound session and one internal-label session, finish each session, then confirm every saved summary entry shows display product, barcode, weight, location, time, and scanned-by user.

## 2026-06-30 - Stock inbound display-name result alignment

Task completed:

- Continued the active guided Stock Inbound multi-page session goal with a focused manufacturer-based display-name fix.
- Stock barcode-unit result rows now use the shared display-name helper instead of manually building `manufacturer + item.name`.
- This keeps stock-unit cards/tables aligned with inbound scanner context, session summaries, labels, stock reports, and search/result naming when a product section is part of the product name.
- Product and manufacturer remain separate fields for filtering/reporting; only the displayed product name generation changed.
- Preserved stock saving, barcode uniqueness, barcode-rule behavior, scanner flow, undo behavior, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Added Stock report coverage so barcode-unit result surfaces keep using the shared display-name rule.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` and `/stock/balance` is still required to confirm display names match in scanner, summary, label preview/reprint, stock-unit cards, and stock reports.
- Live Supabase-backed supplier-barcode save, barcode-rule reuse, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print/PDF output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, inbound a product where manufacturer and product section/name differ, then confirm `/stock/inbound`, `/stock/balance`, `/stock/units/[id]`, label reprint, and `/stock/reports` all show the same manufacturer + product display name while still allowing manufacturer filtering.

## 2026-06-30 - Stock issue wrong-location server logging

Task completed:

- Refined Stock issue handling so workers continue to see short blocked-scan messages while the system creates manager-review issue rows automatically.
- Added a shared server-side wrong-location issue path for barcode stock actions.
- Order outbound, direct outbound, transfer out, damage/spoilage request, and return-supplier request now log `WRONG_LOCATION` barcode scan issues before blocking a barcode from another stock location.
- The logged issue includes barcode, item id when detected, expected location, scanned location, scanned status, issue type, created-by user, timestamp, and related order/customer/request context where available.
- Preserved existing business rules: wrong-location stock is still blocked, workers do not fill issue forms, damage/spoilage still requires manager/director approval before deduction, and stock-take mismatch/manual correction still requires the stock-take approval workflow before adjustment.
- Extended stock issue-handling coverage so future changes keep the wrong-location issue helper and issue context contract.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/RLS QA is still needed to confirm scoped managers can see wrong-location issues for their allowed stock locations and cannot see other outlets' issues.
- Manual QA should scan one wrong-location barcode from outbound, transfer, damage/spoilage request, and return-supplier request, then confirm the manager scan issue review panel shows the expected/scanned location and status.
- Issue review still marks the issue as approved, rejected, or corrected only; database stock adjustments remain in the dedicated approval workflows.

Next recommended task:

- Run a live worker/manager test for all requested stock issue types: duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.

## 2026-06-30 - Stock inbound visible barcode rule fields

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused Barcode Rule page alignment fix.
- Page 2 barcode-rule fields are now a fixed visible panel instead of a collapsible `details` section.
- The visible panel keeps weight start position, weight digit length, weight decimals, fallback fixed kg, inbound source, batch number, reference, notes, and save-rule control directly on the Barcode Rule page.
- This better matches the requirement that the Barcode Rule page shows the sample barcode field, weight position field, digit field, decimals field, and extracted preview without workers needing to open an optional panel.
- Preserved barcode-rule save behavior, item/manufacturer/origin rule scope, duplicate blocking, internal-label fallback, continuous scanner flow, undo actions, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the fixed visible rule-fields panel.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required to confirm the visible rule field panel fits cleanly at 390px.
- Live Supabase-backed barcode-rule save, supplier-barcode continuous scan, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, choose setup, go to Barcode Rule, confirm the sample barcode, actual weight, weight start, digit length, decimals, extracted preview, and rule values are all visible without opening an optional section, then save the first barcode and scan the next one continuously.

## 2026-06-30 - Stock inbound barcode rule save summary

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused Barcode Rule page clarity improvement.
- Page 2 now shows a visible `Rule values to save` block before the worker saves the first supplier barcode.
- The visible rule summary includes rule target, sample barcode, barcode length, weight start position, weight digit length, weight decimals, and extracted preview weight.
- This makes the barcode-rule setup auditable on screen instead of leaving the save values hidden in form fields/details.
- Preserved the existing barcode rule save behavior, item/manufacturer/origin rule scope, duplicate blocking, no-barcode fallback, continuous scanner flow, undo actions, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the visible rule-save summary.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required to confirm the Barcode Rule summary fits at 390px and matches actual worker-entered sample/weight values.
- Live Supabase-backed barcode-rule save, supplier-barcode continuous scan, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and print output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, choose product/manufacturer/origin/location, scan one sample supplier barcode, enter actual kg, confirm `Rule values to save` shows the expected barcode length/start/digits/decimals/preview, then save and verify the next barcode scans continuously.

## 2026-06-30 - Stock inbound print-ready session summary

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused Session Summary print hardening.
- Replaced the placeholder Session Summary print area with a real print-ready field block.
- Print-ready summary now includes display product name, separate product, separate manufacturer, origin, location, inbound mode, saved count, total weight, duplicate/error count, scanned by, batch code, started time, and finished time.
- Reused the generated display product name rule while preserving separate product/manufacturer values for reporting and filtering.
- Kept label printing separate from session summary printing, as required.
- Preserved stock saving, barcode-rule behavior, duplicate blocking, current-scan undo, whole-session undo, movement/audit history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the real print-ready summary fields.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required to confirm the print-ready summary renders cleanly at 390px and in browser print/PDF output.
- Live Supabase-backed supplier-barcode save, internal-label save, undo previous scan, whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, complete one supplier-barcode session and one no-supplier-barcode/internal-label session, open Session Summary, use Print session summary, and confirm printed/PDF output contains display product, product, manufacturer, origin, location, count, weight, scanned by, and batch time.

## 2026-06-30 - Stock scan issue context hardening

Task completed:

- Refined Stock issue handling so workers still see short blocked-scan messages and do not fill long issue forms.
- Kept the existing `barcode_scan_logs` issue queue and manager review panel; no new table or migration was added.
- Duplicate inbound barcode logging now records the detected existing stock unit item, scanned location, scanned status, and expected `unused barcode` status where the barcode already exists.
- Receive-transfer server-side rejections now include selected expected barcode, scanned barcode, item, expected/scanned location, and expected/scanned status for wrong-location and unavailable-transfer scans.
- Stock-take wrong-item server-side issue rows now include expected `selected item/manufacturer` scope.
- Updated stock smoke, acceptance, mobile UX, and issue-handling coverage so inbound, outbound, stock take, transfer, and return issue creation remains guarded.
- Preserved approval behavior: issue review can mark scan issues approved/rejected/corrected, but stock-take mismatch/manual correction still requires the stock-take approval workflow before database adjustment.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-issue-handling-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed to confirm issue rows created by workers appear only in the correct scoped manager/admin/director review queues.
- Manual QA should trigger duplicate inbound, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review issues.
- Manager review intentionally does not perform stock adjustments; stock-take mismatch/manual correction must still go through manager review and director approval.

Next recommended task:

- Use a worker account on Vercel/local to trigger one blocked issue from inbound, outbound, transfer, receive-transfer, return/damage, and stock take, then use a manager account to confirm the issue review queue shows issue type, barcode, item/selected item, expected/scanned location/status, related context, created by, and timestamp.

## 2026-06-30 - Stock movement manufacturer reporting

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal by hardening the product/manufacturer display-name rule outside the Inbound form.
- Stock movement history now keeps the generated display product name while also carrying manufacturer as a separate field.
- Movement tables now show a `Manufacturer` column.
- Mobile movement cards now show `Manufacturer: ...` below the display product name.
- Movement filtering/search now includes manufacturer separately through the existing `brand`/manufacturer filter.
- Demo stock movement rows now include manufacturer values so local/demo reports exercise the same contract.
- Preserved stock movement history, barcode uniqueness, role/scope logic, RLS intent, approval rules, and existing stock report export behavior.

Files changed in this pass:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real report QA should confirm `/stock/movements` and `/stock/reports` searches find manufacturer names from live Supabase movement/unit data.
- Real logged-in `/stock/inbound` browser/device QA remains required for end-to-end guided inbound save, undo, whole-session undo, camera scanner, handheld scanner, and label output.
- The active Stock Inbound goal remains open until runtime/device evidence proves the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, inbound one barcode with a manufacturer, then confirm `/stock/movements` shows the display product name plus separate Manufacturer and that the Manufacturer filter/search finds the movement.

## 2026-06-30 - Stock inbound summary saved-entry audit

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused Session Summary hardening.
- The finished inbound summary now shows a compact saved-entry list for the current session.
- Supplier-barcode sessions show saved barcodes; no-supplier-barcode/manual sessions show saved units.
- Each summary entry shows display product name, barcode, weight, location, and scan/save time.
- The summary now also shows duplicate/error scans directly inside the summary when they happened in the session.
- Kept the future print-summary area, internal-label reprint area, whole-session undo confirmation, stock movement history, role/scope logic, RLS intent, barcode uniqueness, and approval rules unchanged.
- Added Stock acceptance and mobile UX coverage for the saved-entry summary list and duplicate/error summary list.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, finish one supplier-barcode session and one no-supplier-barcode/internal-label session, then confirm the summary shows saved barcode/unit rows, total count/weight, duplicate/error scans, and whole-session undo behavior.

## 2026-06-30 - Stock issue handling server context hardening

Task completed:

- Refined Stock issue handling so workers still see short scan warnings and do not fill long issue forms.
- Kept the existing `barcode_scan_logs` issue-record queue and manager review flow; no new issue table or migration was added.
- Added reusable server-side blocked-scan context for barcode lookup helpers.
- Missing/unavailable barcode issue rows from server actions now carry expected location/status and related workflow context where available.
- Covered order/direct outbound, transfer out, receive-transfer, stock return, damage/spoilage request, and return-supplier request paths.
- Preserved business rules: manager review can mark scan issues approved/rejected/corrected, but stock-take mismatch/manual correction still does not adjust stock from issue review alone.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed to confirm worker-created issue rows appear only in the correct scoped manager/admin/director review queues.
- Manual QA should trigger one issue from inbound, outbound, stock take, transfer, and return, then confirm the manager queue shows issue type, barcode, item where available, expected/scanned location/status, related context, created by, and timestamp.
- Issue review intentionally does not apply stock adjustments; stock-take mismatch/manual correction must still use the manager-review then director-approval workflow.

Next recommended task:

- Use a worker account to trigger duplicate barcode, barcode not found, wrong location, unavailable stock, stock take mismatch, transfer missing/unexpected item, and spoiled/damaged review issues, then use a manager account to approve, reject, and mark corrected from the Stock dashboard manager scan issue review queue.

## 2026-06-30 - Stock inbound manual unit-weight entry clarity

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused Manual Weight Entry clarity fix.
- The weight input label is now mode-specific:
  - No-supplier-barcode/manual mode shows `Enter one unit weight kg`.
  - Supplier-barcode mode shows `Actual net weight kg`.
- The manual helper text now explicitly says to type the next unit weight, press Enter to generate the label, print/attach it, then scan it.
- The helper also states that after save, the field clears and stays focused for the next unit.
- Preserved stock saving, internal label confirmation, barcode uniqueness, duplicate blocking, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the mode-specific unit-weight label and continuous-entry helper text.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, choose `No supplier barcode`, confirm the weight input says `Enter one unit weight kg`, save one printed-label unit, and confirm the input clears and remains focused for the next weight.

## 2026-06-30 - Stock inbound manual-weight previous entry wording

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused no-supplier-barcode/manual-weight UX alignment.
- Updated the Manual Weight Entry page to show `Previous entered weight` instead of generic `Previous saved` wording.
- Updated the previous-scan feedback card so no-supplier-barcode sessions label the latest saved unit as `Previous entered weight`, while supplier-barcode sessions still say `Previous scan`.
- This better matches the required manual-weight workflow: worker enters weight, prints/attaches label, scans the printed label, then sees previous entered weight plus total count/weight before entering the next unit.
- Preserved stock saving, internal label confirmation, barcode uniqueness, duplicate blocking, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the manual previous-entered-weight wording.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, use `No supplier barcode`, enter a weight, generate/print/attach/scan the internal label, and confirm the page shows `Previous entered weight`, clears and focuses the weight field, and keeps total count/weight updated.

## 2026-06-30 - Stock inbound new-session setup cue

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused session-boundary UX hardening.
- Starting `New inbound session` now shows a clear setup-page status cue after the fresh session code is generated.
- If the previous product/manufacturer/origin/location setup is still selected, the cue tells the worker the new inbound session is ready and that the setup was kept for faster repeated batches.
- If setup is incomplete, the cue tells the worker to choose product, manufacturer, origin, and location.
- The cue clears automatically when the worker changes product, manufacturer, origin, location, or applies a recent inbound template.
- Preserved stock saving, barcode rule behavior, duplicate blocking, internal label confirmation, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the new session-ready cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, save one supplier-barcode or internal-label session, finish it, tap `New inbound session`, and confirm the page shows the new session-ready cue, fresh batch code, zero totals, and preserved setup only where appropriate.

## 2026-06-30 - Stock issue handling transfer context hardening

Task completed:

- Refined Stock issue handling so workers still see short scan warnings instead of long issue forms.
- Reused the existing `barcode_scan_logs` issue-record flow and manager review queue; no new issue table or migration was added.
- Extended generic stock scan issue logging to carry `expectedBarcode` and `receivedBarcode` in the related context.
- Receive-transfer blocked scans now pass the selected expected barcode and the actually scanned barcode into the auto-created issue row.
- Updated Stock smoke, acceptance, issue-handling, and mobile UX coverage so inbound, outbound, stock take, transfer, and return issue creation remains guarded.
- Preserved existing approval behavior: manager issue review can mark scan issues approved, rejected, or corrected, but stock-take mismatch/manual correction still does not adjust stock from scan issue review alone.

Files changed in this pass:

- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-issue-handling-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/RLS QA is still needed to confirm issue rows created by workers appear only in the correct scoped manager/admin/director review queues.
- Manual QA should trigger one issue from inbound, outbound, stock take, transfer, and return, then confirm the manager queue shows issue type, barcode, item where available, expected/scanned location/status, related context, created by, and timestamp.
- Transfer QA should specifically scan an unexpected receive-transfer barcode and confirm the manager queue shows expected versus received barcode.

Next recommended task:

- Use a worker account to trigger blocked scan issues across stock workflows, then use a manager/admin account to approve, reject, and mark corrected from the Stock dashboard manager scan issue review queue.

## 2026-06-30 - Stock inbound rule-page safe back and no-barcode escape

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused rule-page navigation/recovery fix.
- Added a clear `Back to setup` action while the worker is on the Barcode Rule page.
- Updated the `Use No supplier barcode` escape path on the Barcode Rule page to use the shared `switchInboundMode("internal_label")` helper instead of directly changing mode.
- This clears stale sample barcode, typed weight, pending internal label, and hidden draft state before moving from rule learning into manual/internal-label inbound.
- Preserved saved scans, saved session totals, product/manufacturer display naming, barcode-rule setup, continuous scanning, external scanner support, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the Barcode Rule back button and reset-backed no-barcode escape path.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, go to Barcode Rule, verify `Back to setup` returns safely, then scan a sample and use `Use No supplier barcode` to confirm the sample barcode/weight are cleared before manual label entry.

## 2026-06-30 - Stock inbound recent-template draft reset

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused recent-template safety fix.
- Recent inbound template selection now clears unsaved draft scan state before applying the template and jumping to rule/scanner/manual pages.
- This aligns the fast template path with normal product/manufacturer/origin/location changes.
- The cleared state includes unsaved barcode, typed weight, pending internal label, label refs, previous hidden error key, and hidden issue dedupe keys.
- This prevents a worker from entering a sample barcode/actual kg, tapping a recent template, and accidentally carrying the old draft scan into the new template session.
- Preserved saved scans, saved session totals, product/manufacturer display naming, barcode-rule setup, continuous scanning, external scanner support, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage that checks the reset happens inside the recent-template path itself.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, type a supplier barcode sample and weight, tap a recent inbound template, and confirm the old sample/weight is cleared before saving or scanning under the selected template.

## 2026-06-30 - Stock inbound setup-change draft scan reset

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused session setup safety fix.
- Added a shared `clearInboundDraftScan` helper for unsaved inbound draft state.
- Changing product, manufacturer, origin, or location before the session is locked now clears the current unsaved barcode, typed weight, pending internal label, label refs, previous hidden error key, and hidden issue dedupe keys.
- Applying a recent inbound template also clears the old unsaved barcode/weight state before moving to rule/scanner/manual pages.
- This prevents a supplier barcode sample, manual actual kg, or generated-but-unsaved internal label from being accidentally reused after the worker changes the inbound setup.
- Preserved saved scans, saved session totals, product/manufacturer display naming, barcode-rule setup, continuous scanning, external scanner support, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for clearing draft scan state on setup changes.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, enter a supplier barcode sample and actual kg, change product/manufacturer/origin/location before saving, and confirm the old barcode/weight is cleared before continuing.

## 2026-06-30 - Stock inbound mode switch stale-state reset

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused setup-mode safety fix.
- Added a single `switchInboundMode` helper for moving between `Supplier barcode` and `No supplier barcode`.
- Switching inbound mode now clears stale scan-only state: barcode, net weight, pending internal label, pending label refs, and internal-label submit ref.
- The helper respects locked/finished sessions and shows the existing read-only/session-finished warning instead of changing mode.
- This prevents a supplier barcode sample or pending internal label from accidentally carrying into the other inbound path.
- Preserved product/manufacturer display naming, barcode-rule setup, continuous scanning, external scanner support, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the mode-switch reset.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, start a supplier-barcode rule sample, switch to `No supplier barcode`, confirm the sample barcode/weight is cleared, generate an internal label, then switch back to supplier barcode and confirm the pending internal label cannot accidentally save.

## 2026-06-30 - Stock issue handling return wrong-location guard

Task completed:

- Refined Stock issue handling so workers still do not fill long issue forms.
- Reused the existing `barcode_scan_logs` issue-record flow and manager review queue; no new issue table or migration was added.
- Added an explicit server-side Return Stock wrong-location guard. If a returned barcode belongs to a different location than the selected return location, the action now writes a `WRONG_LOCATION` issue row with barcode, item, expected/scanned location, expected/scanned status, created by, and timestamp, then blocks the save.
- Added the matching mobile worker warning on Return Stock. Workers see a short wrong-location message and the manager-review issue is auto-created from the scan screen.
- Preserved the manager review actions (`APPROVED`, `REJECTED`, `CORRECTED`) and did not add any automatic database adjustment from scan issue review. Stock take mismatch/manual correction still requires the existing approval workflow.
- Strengthened the Stock issue handling coverage script so the return wrong-location worker message and server issue creation are checked.

Files changed in this pass:

- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/RLS QA is still needed to confirm worker-created return wrong-location issue rows appear only in the correct scoped manager/admin/director review queues.
- Manual QA should scan a Return Stock barcode from another location and confirm the worker sees the short blocked message while the Stock dashboard manager queue shows the issue with expected/scanned location.

Next recommended task:

- Use a worker account to trigger each stock issue type once from inbound, outbound, receive-transfer, return, stock take, and damage/spoilage, then verify a manager can approve, reject, or mark corrected without applying stock adjustments except through the dedicated approval workflows.

## 2026-06-30 - Stock inbound finished-session review stop cue

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused finished-session safety refinement.
- When a worker opens the scanner/manual-weight page after finishing a session, the page now shows a clear review-only stop message beside the scan controls.
- The message says the session is finished and the worker must start a new inbound session before scanning or changing weight.
- This keeps the summary back-navigation useful for review while making it clear that no more stock should be scanned into the finished batch.
- Preserved product/manufacturer display naming, barcode-rule setup, continuous scanning, external scanner support, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for the finished-session scan stop cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - first run failed because stale `.next/types` entries referenced missing generated files; after `npm.cmd run build` regenerated Next types, rerun passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, save one inbound scan, finish the session, use `Back to Scanner` or `Back to Manual Weight`, and confirm the review-only stop message is visible and scanning remains blocked.

## 2026-06-30 - Stock inbound new-session scan state reset

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused session-boundary reliability fix.
- Starting `New inbound session` now clears hidden inbound scan error tracking as well as visible session state.
- This prevents a duplicate/no-weight/length-warning scan in a new batch from being suppressed because the same issue was logged in the previous batch.
- The reset intentionally does not reset the internal label serial ref, so generated internal barcodes remain unique across multiple inbound sessions on the same mounted page.
- Preserved product/manufacturer display naming, barcode-rule setup, continuous scanning, external scanner support, undo actions, whole-session undo, stock movement history, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance and mobile UX coverage for clearing the hidden issue/error tracking refs at new-session reset.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - first run failed because stale `.next/types` entries referenced missing generated files; after `npm.cmd run build` regenerated Next types, rerun passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed save/undo/whole-session undo, phone camera scanning, handheld scanner timing, and Bluetooth/PDF label output remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full guided flow end to end.

Next recommended task:

- With a logged-in stock user, complete two inbound sessions back-to-back. In the second session, intentionally trigger the same duplicate/no-weight warning as the first session and confirm it logs/shows again instead of being suppressed.

## 2026-06-30 - Stock issue handling transfer context refinement

Task completed:

- Refined Stock issue handling so workers still see short scan messages while manager-review issue rows get better transfer context.
- Reused the existing `barcode_scan_logs` issue-record flow; no long worker issue form and no new table were added.
- Added expected/received barcode context support to the shared issue context helper.
- Receive-transfer now records `expectedBarcode`, `receivedBarcode`, and transfer context for:
  - scanning before tapping a pending transfer card,
  - scanning an unexpected transfer barcode,
  - successful receive when another transfer item remains missing.
- Strengthened the Stock issue handling coverage script so the expected/received transfer barcode context is guarded.
- Preserved existing approval behavior: stock-take mismatch/manual correction still does not adjust stock from issue review alone; adjustment still waits for the existing approval workflow.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None. Existing issue fields in `barcode_scan_logs` are reused.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - first run failed because stale `.next/types` entries referenced missing generated files; after `npm.cmd run build` regenerated Next types, rerun passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/RLS QA is still needed to confirm worker-created issue rows appear only in the correct scoped manager/admin/director review queues.
- Real device QA should still trigger duplicate inbound, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.

Next recommended task:

- On Vercel/staging, use a worker account to trigger one blocked issue in inbound, outbound, receive-transfer, return, and stock take, then use a manager account to approve/reject/correct each issue from the Stock dashboard review card.

## 2026-06-30 - Stock inbound internal-label save-path hardening

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused no-supplier-barcode reliability fix.
- When a worker scans or enters the printed internal label, the pending label data is now pinned before the form submits.
- The submit handler also preserves the pending internal label when `pendingInternalLabelRef` is active, avoiding dependence on React state flush timing for barcode/weight fields.
- This makes the internal-label flow more reliable: enter kg, generate label, save stock, print/attach, clear weight, and focus the next weight entry.
- Preserved barcode uniqueness, stock save behavior, movement history, undo actions, role/scope logic, RLS intent, approval rules, camera scanning, external scanner input, live scanner feedback, and session summary behavior.
- Added Stock label, acceptance, and mobile UX coverage for the pinned pending-label save path.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Live Supabase-backed internal-label save, phone/handheld scanner timing, Bluetooth/PDF label output, and undo/whole-session undo remain unproven in this pass.
- The active Stock Inbound goal remains open until runtime/device checks prove the full flow end to end.

Next recommended task:

- With a logged-in stock user, test no-supplier-barcode inbound: enter kg, generate label, confirm stock saves with the generated barcode/weight, print/attach, then verify the weight input clears and focuses for the next unit.

## 2026-06-30 - Stock inbound popup external-scanner availability

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused handheld-scanner usability fix.
- The scanner popup external-scanner input now stays enabled while the camera is starting.
- This lets workers scan with a handheld scanner immediately instead of waiting for camera permission/startup, while still blocking scanning when the browser is offline.
- Preserved camera scanning, manual fallback, live scanner feedback, shared duplicate throttle, barcode rule persistence, stock save behavior, duplicate prevention, movement history, undo actions, role/scope logic, RLS intent, and approval rules.
- Added Stock scanner coverage for keeping the popup external-scanner input gated by online status only.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Real handheld scanner behavior while camera permission/startup is pending, phone camera scanning, Bluetooth/PDF label output, and live Supabase-backed save/undo remain unproven.
- The active Stock Inbound goal remains open until those runtime/device checks prove the full flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, open the scanner popup, and scan with a handheld scanner before camera permission is granted or while the camera is starting. Confirm the scan is accepted when online and blocked with `Connection lost. Please reconnect before scanning.` when offline.

## 2026-06-30 - Stock inbound popup scanner duplicate-throttle hardening

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused scanner reliability fix.
- The camera scanner and popup external-scanner input now share the same short duplicate-throttle reference.
- This prevents a handheld scanner submission from being immediately echoed by the camera scanner as a second scan while the popup is open.
- Continuous scanning still clears/refocuses the popup external-scanner input after a valid external scan.
- One-sample barcode-rule mode still closes after one external scanner sample.
- Preserved camera scanning, manual fallback, live scanner feedback, barcode rule persistence, stock save behavior, duplicate prevention, movement history, undo actions, role/scope logic, RLS intent, and approval rules.
- Added Stock scanner coverage for the shared external/camera duplicate throttle.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Real handheld scanner behavior in the popup, phone camera scanning, Bluetooth/PDF label output, and live Supabase-backed save/undo remain unproven.
- The active Stock Inbound goal remains open until those runtime/device checks prove the full flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, open the scanner popup, scan one barcode with a handheld scanner while the camera is also pointed at the label, and confirm only one scan/save attempt is produced.

## 2026-06-30 - Stock inbound popup external-scanner input

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused external scanner refinement.
- The camera scanner popup now includes an `External scanner input` field for handheld barcode scanners while the popup is open.
- When the popup opens, the external scanner input receives focus so a handheld scanner can send the barcode with Enter without the worker tapping the page behind the popup.
- In continuous mode, Enter submits the scan, clears the popup input, and refocuses it for the next barcode.
- In one-sample mode, Enter submits one sample scan and closes the popup for rule checking.
- Preserved camera scanning, manual fallback, live scanner feedback, barcode rule persistence, stock save behavior, duplicate prevention, movement history, undo actions, role/scope logic, RLS intent, and approval rules.
- Added Stock scanner, acceptance, and mobile UX coverage for popup external scanner input.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Render / runtime evidence:

- Local unauthenticated probe of `http://127.0.0.1:3912/stock/inbound` reached the app and returned `307` redirect to `/login`, so authenticated rendered Stock Inbound QA is still not proven in this pass.
- Temporary dev server cleanup via command-line process lookup was denied by Windows permissions, but a follow-up `netstat -ano | Select-String ':3912'` showed no listener left on the temporary port.

Risks / remaining checks:

- Real logged-in browser QA at `/stock/inbound` is still required.
- Real handheld scanner Enter timing inside the popup, phone camera scanning, Bluetooth/PDF label output, and live Supabase-backed save/undo remain unproven.
- The active Stock Inbound goal remains open until those runtime/device checks prove the full flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound`, open the scanner popup, and test both camera scanning and a handheld scanner in the new popup input for one-sample barcode-rule mode and continuous scanning mode.

## 2026-06-30 - Stock inbound scanner live feedback in popup

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused scanner-popup feedback refinement.
- The reusable barcode scanner now accepts live scan feedback message/status props and shows them inside the camera scanner popup.
- Stock Inbound now passes its decode/warning/error state into the scanner popup, so barcode length mismatch, no-weight/internal-label guidance, duplicate blocks, and save feedback are visible where workers are scanning.
- Preserved camera permission flow, rear-camera preference, stream shutdown on close, manual fallback, external scanner input, continuous scanning, undo actions, barcode rule persistence, stock save behavior, duplicate prevention, movement history, role/scope logic, RLS intent, and approval rules.
- Added scanner, Stock acceptance, and Stock mobile UX coverage for the popup feedback panel.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Attempted a local demo-mode render by clearing Supabase env vars for a temporary dev server, but PowerShell/Windows failed before Next started with `Start-Process : Item has already been added. Key in dictionary: 'Path' Key being added: 'PATH'`. No live Supabase SQL or production action was run.
- Authenticated rendered Stock Inbound QA is still not proven in this pass.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label output, and live Supabase-backed save/undo still need staging or owner QA.
- The active Stock Inbound goal remains open until those runtime/device checks prove the full flow end to end.

Next recommended task:

- With a logged-in stock user, open `/stock/inbound` at phone width, start the scanner, scan a wrong-length barcode, and confirm the warning appears inside the camera popup while the worker is still scanning.

## 2026-06-30 - Stock inbound read-only setup review clarity

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused read-only review refinement.
- Added a worker-facing read-only banner on the Session Setup page once an inbound session is locked or finished.
- Updated the summary action from `Review setup` to `Review setup (read-only)` so workers can safely go back and inspect product, manufacturer, origin, location, and session setup without thinking they can edit a completed/locked session.
- Preserved barcode rule persistence, stock save behavior, internal-label fallback, duplicate prevention, movement history, undo previous scan, whole-session undo, role/scope logic, RLS intent, and approval rules.
- Added Stock acceptance/mobile UX coverage for the read-only setup review state.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven in this pass.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label output, and live Supabase-backed save/undo still need staging or owner QA.
- The active Stock Inbound goal remains open until those runtime/device checks prove the full flow end to end.

Next recommended task:

- Run `/stock/inbound` with a real logged-in stock user at about 390px width. Save one supplier-barcode scan and one internal-label scan, finish each session, use `Review setup (read-only)`, confirm fields are locked, then test undo previous scan and whole-session undo against real Supabase rows.

## 2026-06-30 - Stock issue handling smoke coverage

Task completed:

- Refined Stock issue handling verification so workers do not need long issue forms.
- Confirmed existing worker flows auto-create issue records through `barcode_scan_logs` for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.
- Confirmed issue records carry issue type, barcode, detected/selected item, expected/scanned location, expected/scanned status, related session/order/customer/transfer context, scanned-by user, timestamp, and manager review fields where the workflow has that data.
- Confirmed manager review queue shows all open issue types and supports approve, reject, or mark corrected.
- Added a focused smoke coverage script so inbound, outbound, stock take, transfer, and return issue creation paths stay protected.
- No stock business logic, approval logic, RLS, or database schema was changed in this pass.

Files changed in this pass:

- `package.json`
- `scripts/stock-issue-handling-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-issue-handling-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The new coverage is static/smoke coverage. It proves the expected issue paths remain wired, but it does not insert real Supabase rows.
- Live Supabase QA should still scan real inbound/outbound/stock-take/transfer/return blocked examples and confirm the manager queue receives the open issue rows with correct scope.

Next recommended task:

- Run full `npm.cmd run smoke`, `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd run build`, then test one real blocked scan per stock workflow on the deployed/staging app.

## 2026-06-30 - Stock inbound scanner one-sample cue

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused camera-scanner clarity refinement.
- The scanner modal now shows a one-sample cue when continuous mode is off: `One sample scan. Camera closes after one barcode so you can check details before saving.`
- Continuous mode still shows the existing no-OK/no-Next auto-save cue for actual inbound scanning.
- This makes Page 2 Barcode Rule sample scanning visibly different from Page 3 continuous scanner saving.
- Kept camera scanning, manual fallback, external scanner input, barcode rule persistence, stock save behavior, internal-label fallback, duplicate prevention, movement history, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added Stock scanner/mobile UX coverage for the one-sample scanner cue.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then open the Page 2 scanner and confirm the one-sample cue appears, the camera closes after one barcode, and Page 3 still shows continuous scanning.

## 2026-06-30 - Stock inbound barcode-rule sample scan mode

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused Page 2 vs Page 3 scanner refinement.
- Barcode Rule page now uses a one-sample scan mode instead of the continuous scanner mode.
- Scanner page still keeps continuous scanning for actual inbound saving.
- Updated Page 2 helper text so external scanner users know to scan one supplier barcode sample, then enter actual kg.
- Kept barcode rule persistence, stock save behavior, internal-label fallback, duplicate prevention, movement history, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added Stock acceptance/mobile UX/scanner coverage for the one-sample rule page and continuous scanner page split.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then test Page 2 at 390px with a phone camera and handheld scanner: scan one sample barcode, confirm scanner exits sample mode, enter actual kg, save rule, then continue on Page 3 with continuous scanning.

## 2026-06-30 - Stock inbound internal-label confirmation hardening

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused no-supplier-barcode safety fix.
- After generating an internal label, the barcode scan field is now cleared instead of being prefilled with the generated barcode.
- Workers must scan the printed label, or manually enter that printed label as fallback, before the stock unit is saved.
- Updated the label-ready message to say `Print, attach, then scan or enter printed label to save stock.`
- This preserves label generation, stock save behavior, duplicate prevention, movement history, undo behavior, role/scope logic, RLS intent, and approval rules while making the print/attach/scan confirmation step real.
- Added Stock acceptance/mobile UX/label coverage for this confirmation guard.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then test no-supplier-barcode inbound at 390px and confirm generated labels do not save until the printed label is scanned or manually entered.

## 2026-06-30 - Stock inbound internal-label continuous-entry cue

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused no-supplier-barcode refinement.
- The pending internal-label confirmation panel now tells workers that after the printed label is scanned and saved, the weight box clears and stays ready for the next unit.
- This reinforces the Page 2 Manual Weight Entry workflow for continuous weight entry without changing stock save behavior, label generation, duplicate prevention, movement history, undo behavior, role/scope logic, RLS intent, or approval rules.
- Confirmed existing display-name plumbing already uses manufacturer + product across inbound setup, scanner context, labels, summaries, stock-unit detail, reports, and scan issue context.
- Added Stock acceptance/mobile UX coverage for the continuous-entry cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then test no-supplier-barcode inbound at 390px: enter kg, generate label, confirm stock saves, print/attach, and confirm focus returns to the weight field for the next unit.

## 2026-06-30 - Stock inbound barcode-rule internal-label escape

Task completed:

- Continued the active guided Stock Inbound multi-page flow goal with a focused Barcode Rule page refinement.
- Added an explicit `rule-page-internal-label-escape` cue on the Barcode Rule page when no usable saved supplier-barcode rule exists yet.
- The cue tells workers that if the supplier barcode has no usable weight, they should switch to internal labels, enter kg, print the 50mm x 30mm label, attach it, then scan the printed label.
- Kept stock save behavior, barcode rule persistence, label generation, duplicate prevention, movement history, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added Stock acceptance/mobile UX coverage for the new rule-page internal-label escape cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm Page 2 clearly offers the internal-label escape path before and after an unusable supplier barcode sample.

## 2026-06-30 - Stock issue handling verification

Task completed:

- Reviewed Stock issue handling so workers do not need long issue forms.
- Confirmed issue records are auto-created through `barcode_scan_logs` for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.
- Confirmed issue records carry barcode, detected/selected item, expected/scanned location, expected/scanned status, related session/order/customer/transfer context, created-by user, and timestamp where the workflow has that data.
- Confirmed worker-facing forms show short blocked/warning messages and continue scanning where the workflow allows it.
- Confirmed the Stock manager review queue shows all open issue types and supports approve, reject, or mark corrected.
- Confirmed stock-take mismatch/manual correction remains approval-led; database adjustment is not applied by the issue log itself.
- No stock business logic changes were needed in this pass because the requested issue framework and smoke coverage already existed.

Files changed in this pass:

- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/RLS QA should still verify manager visibility for cross-location issue rows without exposing other outlet data.
- Real worker-device QA should trigger one issue from inbound, outbound, stock take, transfer, receive-transfer, and return to confirm the worker sees only short messages and can continue where safe.

Next recommended task:

- In staging/Vercel, trigger duplicate inbound, barcode-not-found outbound, wrong-location receive-transfer, unknown stock-take barcode, and spoiled/damaged return, then confirm each appears in the manager scan issue review queue with the expected context.

## 2026-06-30 - Stock inbound internal-label fallback guidance

Task completed:

- Continued the active guided Stock Inbound goal with a focused rule-failure recovery refinement.
- When a supplier barcode cannot be decoded or no matching weight position can be found, the flow now clears the unusable supplier barcode from the printed-label scan field before moving to internal-label mode.
- Added a visible `Use internal label inbound` fallback cue that tells workers to enter kg, generate an internal label, print and attach it, then scan the printed label.
- Added an `Enter weight` action that focuses the manual weight field.
- Kept barcode rule persistence, stock writes, duplicate prevention, scan issue logging, label generation, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added Stock acceptance/mobile UX coverage for the internal-label fallback cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - initially failed because the fallback cue was declared before `manualMode` and `scannerVisible`.
- `npm.cmd run typecheck` - passed after moving the cue calculation below those declarations.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then scan a supplier barcode with no usable weight rule and confirm the page moves to internal-label mode, clears the supplier barcode field, and shows the fallback cue at 390px.

## 2026-06-30 - Stock inbound scanner auto-save cue

Task completed:

- Continued the active guided Stock Inbound goal with a focused scanner-window refinement.
- The Stock camera scanner popup now explicitly tells workers: continuous scan is on, valid scans save automatically, and no OK/Next step is needed.
- Added a source marker for the continuous-scan auto-save cue so scanner/mobile coverage protects this behavior.
- Kept camera scanning, manual fallback, external scanner input, duplicate prevention, stock writes, barcode rules, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the scanner popup shows the auto-save/no-next-step cue before scanning multiple labels continuously.

## 2026-06-30 - Stock inbound guided page cue

Task completed:

- Continued the active guided Stock Inbound goal with a focused orientation refinement.
- Added a `Page X/Y` cue below the inbound step buttons so workers can clearly see the current guided page.
- Supplier-barcode inbound now presents the cue across the 4-page flow: Session Setup, Barcode Rule, Scanner, and Session Summary.
- No-supplier-barcode/internal-label inbound now presents the cue across the 3-page flow: Session Setup, Manual Weight Entry, and Session Summary.
- Each cue includes a short worker-facing instruction for the current page, including automatic continuous scanning and safe summary/undo review.
- Kept item/manufacturer storage, barcode rules, stock writes, label printing, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added Stock acceptance/mobile UX coverage for the guided page cue and page names.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the page cue correctly changes between supplier-barcode pages 1/4 through 4/4 and internal-label pages 1/3 through 3/3.

## 2026-06-30 - Stock inbound product-to-manufacturer focus shortcut

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 setup refinement.
- When a worker searches/enters a new product before choosing a manufacturer, the warning now includes a large `Choose manufacturer` action.
- Tapping that action scrolls to and focuses the manufacturer search field, reducing setup friction while preserving the manufacturer + product display-name rule.
- Kept item creation, product/manufacturer storage, barcode rules, stock writes, label printing, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added Stock source coverage for the manufacturer search ref, scroll/focus shortcut, and visible button.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the new `Choose manufacturer` action scrolls/focuses the manufacturer search from the manual product entry warning.

## 2026-06-30 - Stock scan issue scope logging hardening

Task completed:

- Refined Stock issue handling so worker scan problems continue to auto-create manager-review issue rows without long worker forms.
- Kept `barcode_scan_logs` as the stock issue record model for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.
- Fixed a wrong-location logging gap: the generic worker issue logger now allows a scan issue to be recorded when the worker has access to any related location on the issue, including selected, expected, or scanned location.
- This preserves location restrictions while preventing wrong-location receive/transfer/outbound issues from being silently dropped when the expected destination is outside the worker's own location.
- Manager review queue behavior remains unchanged: managers can approve, reject, or mark issue rows corrected; stock take/manual correction database adjustments still wait for the proper approval workflow.
- Added smoke/source coverage for the new stock issue location-scope guard.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-workflow-regression.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - initially failed because it ran in parallel before `.next/types` were regenerated; rerun after build passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed on rerun.

Risks / remaining checks:

- This is source/build verified only; live Supabase/RLS QA should still confirm wrong-location issue rows are visible to the proper manager/admin queue without exposing other outlets.
- Real worker-device QA should create one issue from inbound, outbound, stock take, transfer, receive-transfer, and return, then confirm workers see only short messages and can continue where safe.

Next recommended task:

- In staging/Vercel with demo users, trigger wrong-location receive-transfer and wrong-location outbound scans, then verify the issue appears in the manager scan issue review queue with expected/scanned location details.

## 2026-06-30 - Stock inbound barcode-rule scope cue

Task completed:

- Continued the active guided Stock Inbound goal with a focused Barcode Rule page refinement.
- The Barcode Rule page now shows a `Rule target` cue explaining that the saved barcode rule belongs to the selected product + manufacturer + origin.
- The cue also tells workers that a different manufacturer or origin needs its own barcode rule.
- This makes Page 2 clearer without changing the existing item/brand/origin rule key, barcode decoding, stock writes, role/scope logic, RLS intent, or approval rules.
- Updated Stock source coverage for the barcode-rule scope cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-workflow-regression.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the Barcode Rule page clearly shows the rule target for product + manufacturer + origin before saving the first sample scan.

## 2026-06-30 - Stock inbound manual manufacturer setup guidance

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 manufacturer-entry refinement.
- Manufacturer search now shows a worker-facing cue when no exact manufacturer match is found: `Manufacturer not found exactly. Use the search text as a custom manufacturer if this is the correct brand.`
- Added a source marker for the manual manufacturer entry path so future Stock coverage guards this guided setup behavior.
- This pairs with the manual product entry path and makes custom manufacturer input clearer without changing brand creation, item creation, stock writes, barcode rules, role/scope logic, RLS intent, or approval rules.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm searching for a missing manufacturer shows the custom-manufacturer cue and keeps the generated display name as manufacturer + product.

## 2026-06-30 - Stock inbound continuous scan saving feedback

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 3 scanner feedback refinement.
- When a valid supplier barcode scan is ready to auto-save, the scanner now immediately shows `Saving barcode. Keep scanner ready.` before submitting the save.
- This gives workers fast feedback during continuous camera or external-scanner input without adding an OK/Next/Proceed step.
- Kept the existing automatic save path, duplicate blocking, barcode-rule decoding, stock writes, label printing, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Updated Stock source coverage for the continuous-scan saving feedback.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm a valid continuous scan shows the saving message, saves, clears the barcode, and keeps focus ready for the next scan.

## 2026-06-30 - Stock inbound quick product requires manufacturer

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 product/manufacturer setup guard.
- Inbound quick product creation now requires a selected or typed manufacturer before the product can be created.
- The manual product entry section now tells workers: `Choose manufacturer before creating product so the display name is saved as manufacturer + product.`
- The client quick-create action also returns `Choose manufacturer before creating product.` if the action is attempted without a manufacturer.
- This keeps the manufacturer + product display-name model intact for newly created products from inbound setup.
- Kept the existing item creation server action, stock writes, barcode rules, label printing, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Updated Stock source coverage for the manufacturer-required quick product creation path.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm a missing-product search requires manufacturer selection before quick product creation.

## 2026-06-30 - Stock inbound manual product setup guidance

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 manual product-entry refinement.
- Product search now detects whether the search text exactly matches an active product/display name/item code.
- When the worker searches for a product that is not found exactly and no product is selected yet, the manual product entry section opens as `New product / manual entry`.
- The manual entry section now shows a worker-facing cue: `Product not found exactly. Use the search text as a new product name if this is the correct item.`
- This makes the existing quick product creation path clearer without changing item creation rules, manufacturer storage, barcode rules, stock writes, role/scope logic, RLS intent, or approval rules.
- Updated Stock source coverage for the manual product entry path.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm searching for a missing product opens the manual product entry path while preserving separate manufacturer selection.

## 2026-06-30 - Stock inbound setup session-code clarity

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 setup refinement.
- The top inbound session-code card now has a source guard and explicitly says the code is auto-generated for this inbound session and cannot be edited.
- This makes the start of both supplier-barcode and no-supplier-barcode guided sessions clearer before workers choose product, manufacturer, origin, and location.
- The submitted `batchNo` field remains read-only in the form details area, so existing stock-unit, movement, scan-log, and undo/session-void behavior is unchanged.
- Kept product/manufacturer display naming, assigned-location default, recent templates, barcode-rule learning, continuous scanning, label printing, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Updated Stock source coverage for the read-only Page 1 session-code card.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the first setup screen shows the auto-generated, non-editable session code before either supplier-barcode or no-supplier-barcode setup.

## 2026-06-30 - Stock inbound no-barcode summary label reprint cue

Task completed:

- Continued the active guided Stock Inbound goal with a focused no-supplier-barcode summary refinement.
- The finished inbound summary now keeps session-summary printing separate from label printing and says label printing stays in the saved-label section.
- For internal-label/no-barcode sessions, the summary now shows `Saved labels ready` with the count of saved internal labels that can be reprinted before stock is moved.
- Supplier-barcode sessions remain free of new-label prompts, preserving the existing supplier-label behavior.
- Kept stock writes, barcode uniqueness, internal label generation, saved-label previews, whole-session undo, role/scope logic, RLS intent, and approval rules unchanged.
- Updated Stock source coverage for the internal-label summary reprint cue.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm an internal-label/no-barcode session summary shows the saved-label reprint cue while supplier-barcode summaries do not prompt label printing.

## 2026-06-30 - Stock inbound barcode-rule first barcode guidance

Task completed:

- Continued the active guided Stock Inbound goal with a focused Barcode Rule page clarity fix.
- The detected-rule card now tells workers that `Save rule and inbound` saves the first/sample barcode and then reuses the rule for future scans of the selected product and manufacturer.
- The success message after saving a rule from Page 2 now says `Rule saved and first barcode saved. Continue scanning.`
- This makes the supplier-barcode guided flow clearer: Page 2 is both rule learning and the first saved scan, then Page 3 continues continuous scanning without repeated confirmation.
- Kept stock writes, barcode uniqueness, barcode rule persistence, display product naming, session summary, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Updated Stock source coverage for the clarified first-barcode save behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth has been unreliable in prior browser attempts.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the Barcode Rule page explains that the first/sample barcode is saved before continuous scanning continues.

## 2026-06-30 - Stock scan issue handling review queue hardening

Task completed:

- Refined Stock issue handling without adding worker-facing long issue forms.
- Confirmed the existing `barcode_scan_logs` issue model is the issue record path for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.
- Manager scan issue review now uses the full open issue set instead of only the first 8 open records, so older issue types are not hidden when many scan issues exist.
- Updated the manager review copy to state that all open issue types are shown for manager review.
- Added smoke/source coverage that verifies issue creation paths from inbound, outbound, transfer, receive-transfer, return, damage/spoilage, and stock take, plus the related session/order/customer/transfer context mapping.
- Kept worker flows short: workers still see short scan messages and can continue where the existing workflow allows.
- Kept database adjustment rules unchanged: stock take mismatch/manual correction still requires review/approval paths before adjustment.

Files changed in this pass:

- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed after one coverage-fragment correction.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-workflow-regression.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass is source and build verified; live Supabase/RLS manager review should still be tested with real demo users.
- The manager dashboard now shows all open scan issues that are loaded into Stock page data. If production scan logs become very large, a paginated dedicated issue queue should be added later.

Next recommended task:

- In Vercel or local Supabase, create one failed scan from inbound, outbound, stock take, transfer, and return, then confirm a stock manager can mark each open issue as Approved, Rejected, or Corrected without seeing other outlets' issues.

## 2026-06-30 - Stock inbound current preset product/manufacturer fields

Task completed:

- Continued the active guided Stock Inbound goal with a focused product/manufacturer visibility refinement.
- The `Current scan preset` card now shows the generated display product name plus separate `Product` and `Manufacturer` lines.
- This helps workers verify the display name while preserving the separate reporting/filtering fields before barcode-rule learning, scanner use, or manual-label entry.
- Kept the product/manufacturer storage model, generated display naming, recent templates, barcode-rule learning, stock writes, label printing behavior, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added source coverage for the separate product/manufacturer lines on the current scan preset.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the current scan preset shows display name plus separate product/manufacturer before completing both supplier-barcode and no-supplier-barcode sessions.

## 2026-06-30 - Stock inbound label printing scoped to internal-label sessions

Task completed:

- Continued the active guided Stock Inbound goal with a focused scan-history/label-printing refinement.
- Supplier-barcode sessions still show recent inbound scans, but no longer show label-print actions or label previews for those supplier barcode scans.
- Supplier-barcode scan history now explains: `Supplier barcode scans use the existing supplier label. No new label printing is needed for this session.`
- No-supplier-barcode/internal-label sessions still show saved labels, label previews, Bluetooth label printer guidance, PDF fallback, and printable 50mm x 30mm labels.
- This keeps Page 3 and Page 4 clearer: supplier barcode sessions use existing supplier labels; internal-label sessions are the label printing path.
- Stock writes, current-session scan recording, summary totals, display product naming, undo behavior, role/scope logic, RLS intent, and approval rules were not changed.
- Added source coverage for the supplier-vs-internal-label print separation.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm supplier-barcode sessions show scan history without label print prompts, while no-supplier-barcode sessions still show label preview/print/PDF fallback.

## 2026-06-30 - Stock inbound summary mode label

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 4 summary refinement.
- Added `Inbound mode` to the finished inbound session summary.
- Supplier-barcode sessions show `Supplier barcode`; generated-label/no-barcode sessions show `No supplier barcode`.
- This makes Page 4 clearer when checking whether the saved count/weight came from supplier barcode scanning or internal-label/manual weight entry.
- Kept summary totals, product/manufacturer display naming, print summary area, label printing, whole-session undo confirmation, stock writes, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added source coverage for the inbound-mode summary label.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm Page 4 shows the correct inbound mode after both supplier-barcode and no-supplier-barcode sessions.

## 2026-06-30 - Stock inbound finish-session blocked message

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 3 scanner/manual-entry refinement.
- Added a clear warning when the worker is on the scan/manual page but has not saved any barcode or label yet: `Save at least one barcode or label before finishing this session.`
- The existing `Finish Inbound Session` button remains disabled until there is at least one saved scan/label, but workers now see why the summary page cannot open yet.
- Kept pending-label protection, duplicate blocking, auto-save scanning, manual-label confirmation, undo behavior, stock writes, role/scope logic, RLS intent, and approval rules unchanged.
- Added source coverage for the blocked-finish message.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm workers see the finish-session blocked message before saving stock, then can finish and view summary after the first saved barcode/label.

## 2026-06-30 - Stock inbound session summary print area

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 4 summary refinement.
- Added a clear `Session summary print area` block on the finished inbound summary page.
- The block explains that the session summary print includes product, manufacturer, origin, location, total count, total weight, scanned by, and batch time.
- It also clarifies that label printing stays separate below, reducing confusion between barcode labels and the future-ready session summary print action.
- Kept the existing `Print session summary` button, label printing area, whole-session undo confirmation, stock writes, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added source coverage for the future-ready session summary print area.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the session summary print area is clear, separate from label printing, and appears after both supplier-barcode and internal-label sessions.

## 2026-06-30 - Stock inbound recent template origin clarity

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 recent-template refinement.
- Recent inbound templates still show the main manufacturer + product label for quick selection.
- Each recent template now also shows its origin as a small second line, so workers can distinguish the same manufacturer/product from different origins before scanning.
- The template data now carries `originName` while preserving separate item, manufacturer, and origin ids for rule lookup, scanning, reporting, and filtering.
- Kept product/manufacturer display naming, assigned stock-location default, product/manufacturer search, custom product/manufacturer entry, barcode-rule learning, stock writes, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added source coverage for the recent-template origin line.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm recent templates show manufacturer + product plus origin, then complete barcode-rule learning, continuous scanning, manual-label entry, summary print, previous-scan undo, and whole-session undo.

## 2026-06-30 - Stock inbound empty product setup clarity

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 setup clarity refinement.
- When no product has been selected yet, the current setup preview now shows `No product selected` instead of `Unknown product`.
- This pairs with the deliberate product-selection change so workers see a clear setup state before choosing a product or recent template.
- Kept the assigned stock-location default, recent templates, product/manufacturer search, custom product/manufacturer entry, display product naming after selection, barcode-rule learning, stock writes, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added source coverage for the clearer empty product state.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm the empty setup state reads clearly, selected product/manufacturer produce the final display name, and the worker can complete barcode-rule learning, continuous scanning, manual-label entry, summary print, previous-scan undo, and whole-session undo.

## 2026-06-30 - Stock inbound deliberate product selection

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 setup refinement.
- Stock Inbound no longer auto-selects the first active product when the page opens or when a saved local preset references an inactive/missing product.
- The assigned stock location still defaults from the logged-in user's profile when available.
- Added a short setup cue: `Choose a product or tap a recent template before scanning.`
- This makes the guided setup safer: workers must intentionally choose a product/recent template before barcode-rule learning, manual-label entry, or scanning can proceed.
- Kept recent templates, product/manufacturer search, custom product/manufacturer entry, display product naming, barcode-rule learning, stock writes, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Read the local Next.js 16 docs for `use client` and mutating data before the TSX edit, per project instructions.
- Added source coverage for deliberate product selection.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px and confirm Page 1 starts with no product selected, defaults location from profile, accepts recent templates/product dropdown/manual product, and continues through barcode-rule learning, continuous scanning, manual-label entry, summary print, previous-scan undo, and whole-session undo.

## 2026-06-30 - Stock inbound detected barcode-rule preview

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 2 barcode-rule refinement.
- Added a `Detected barcode rule` card that appears after the worker scans a supplier barcode and enters the actual sample weight.
- The card shows sample barcode length, detected weight digit range, digit count, and decimal rule before the worker taps `Save rule and inbound`.
- Added short worker guidance: `Tap Save rule and inbound to use this rule for future scans of this product and manufacturer.`
- Kept barcode-rule inference, save behavior, continuous scanning, duplicate blocking, internal-label fallback, stock writes, undo behavior, role/scope logic, RLS intent, and approval rules unchanged.
- Added source coverage so the detected barcode-rule preview remains guarded.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px through Page 1 setup, Page 2 supplier-barcode rule learning with the detected-rule preview, Page 3 continuous scanning, no-barcode manual-label entry, Page 4 summary print, previous-scan undo, and whole-session undo.

## 2026-06-29 - Stock inbound manual-label next-unit guidance

Task completed:

- Continued the active guided Stock Inbound goal with a focused no-barcode/manual-label UX refinement.
- Added a compact `Enter next unit` card on the Manual Weight step.
- The card shows the locked product/manufacturer display name, origin, location, saved unit count, and saved total kg before the worker enters the next weight.
- Added short guidance: `Session locked to this setup. Enter weight, print label, attach, then scan the printed label.`
- Kept the existing save flow unchanged: generated internal label still waits for printed-label scan confirmation before stock is saved, then returns to the weight entry loop.
- Preserved barcode uniqueness, stock unit/movement history, undo behavior, role/scope logic, RLS intent, and approval rules.
- Added source coverage so the manual-label next-unit guidance is checked by stock smoke coverage.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable in the prior browser attempt.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then run `/stock/inbound` at 390px through supplier-barcode rule learning, continuous scanning, manual-label entry, printed-label confirmation, previous-scan undo, session summary print, and whole-session undo.

## 2026-06-29 - Stock inbound mode-aware session summary

Task completed:

- Continued the active guided Stock Inbound goal by refining Page 4 session summary copy.
- Finished supplier-barcode sessions now say `Supplier barcode session saved. Move stock when ready, or use browser print for a session summary.`
- Finished internal-label/no-barcode sessions now say `Internal label session saved. Reprint labels if needed, attach labels before moving stock, or use browser print for a session summary.`
- This avoids telling supplier-barcode workers to print/attach labels when they only need the future-ready print session summary action.
- Kept the existing `Print session summary` button, internal label print/reprint actions, whole-session undo confirmation, stock movement history, barcode uniqueness, role/RLS behavior, and approval logic unchanged.
- Added source coverage for the mode-aware summary guidance.

Browser QA attempted:

- Successfully connected the in-app browser and set a temporary 390px viewport.
- Local dev/production preview launches from PowerShell were cleaned up after the command ended, so the browser initially saw connection refused.
- A persistent helper-launched preview reached `/login`, proving local render/server reachability at 390px.
- Login with documented local QA credentials could not proceed because the configured Supabase endpoint returned `fetch failed`.
- A demo-mode preview attempt still redirected to `/login` because the production build had Supabase public env baked in.
- The browser viewport was reset and helper preview processes were stopped.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated rendered Stock Inbound QA is still not proven because local Supabase/Auth was unreachable from this run.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Restore local/staging Supabase auth reachability, then rerun `/stock/inbound` at 390px through setup, barcode-rule learning, scanner popup, internal-label entry, summary print, previous-scan undo, and whole-session undo.

## 2026-06-29 - Stock inbound whole-session undo confirmation

Task completed:

- Continued the active guided Stock Inbound goal by making Page 4 session summary safer for whole-session undo/delete.
- Replaced the immediate browser confirm path with an inline `Confirm undo whole inbound session` panel.
- The panel shows the exact saved unit/barcode count, total kg, batch number, and explains that stock units will be voided while movement history and audit records are kept.
- Workers must tap `Undo/Delete whole session` first, then `Confirm undo whole session`; they can also tap `Keep session`.
- Existing `void_inbound_stock_session` RPC, `INBOUND_VOID` movement history, barcode uniqueness, role/RLS logic, scanner behavior, and approval logic were not changed.
- Added source coverage so the inline confirmation panel and final confirm action remain guarded.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Rendered 390px browser QA is still needed to confirm the new confirmation panel fits and reads clearly on phone width.
- Real phone camera scanner, external scanner Enter timing, Bluetooth/PDF label printing, and live Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue the completion audit for guided Stock Inbound with live/browser QA where possible, especially Page 3 scanner popup and Page 4 whole-session undo against real Supabase data.

## 2026-06-29 - Stock issue record hardening

Task completed:

- Refined Stock Module issue handling so issue-row insert failures are no longer silently ignored by server actions.
- Kept the existing compatibility fallback for projects that have not applied the scan issue context migration yet, but the fallback now fails clearly if the basic scan-log insert also fails.
- Confirmed no new workflow form is needed for workers: inbound, outbound, transfer, receive-transfer, return, damage/spoilage, and stock take continue to show short worker messages while issue records are created for manager review.
- Tightened smoke and stock acceptance coverage for every required issue type, required manager-review fields, timestamp/created-by fields, and manager approve/reject/correct review actions.
- Did not change stock movement, stock take adjustment, damage approval, RLS policies, or worker scan behavior.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None. Existing migration `supabase/migrations/202606250007_stock_scan_issue_context_v1.sql` already adds issue type, item/selected item, expected/scanned location and status, related context, review status, reviewer fields, and review note.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should verify actual `barcode_scan_logs` rows are created for blocked inbound, outbound, stock take, transfer, receive-transfer, return, and damage/spoilage paths.
- Manager review queue should be manually tested with seeded users to confirm scoped managers see only their own stock-location issues while admin/director can review globally.
- Stock take mismatch/manual correction adjustments remain approval-gated; this pass did not add new adjustment behavior.

Next recommended task:

- Run live Stock Module QA for manager scan issue review using real Supabase data: duplicate inbound, wrong-location outbound, unknown stock-take barcode, missing transfer item, unexpected receive-transfer scan, invalid return, and spoiled/damaged review.

## 2026-06-29 - Stock inbound scanner top session total

Task completed:

- Continued the active guided Stock Inbound goal by making the camera scanner popup show both key live confirmations above the camera preview.
- The scanner popup now mirrors `Inbound session total` above the camera beside the `Last saved item and weight` strip when total data is available.
- This makes the live saved count/weight visible before scrolling while continuous scanning stays open.
- Kept the existing detailed total panel below the camera.
- Continuous scanning, external scanner/manual input, auto-save behavior, duplicate blocking, stock movement history, undo actions, RLS, and approval logic were not changed.
- Added scanner source coverage for the above-camera session total strip.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Rendered 390px browser QA is still not proven because the local dev server could not be kept running from this environment.
- Real phone camera scanner, external scanner, label printing, and Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue rendered/browser QA when a dev server can stay running, or refine the session summary/whole-session undo path based on source audit.

## 2026-06-29 - Stock inbound scanner above-camera last weight

Task completed:

- Continued the active guided Stock Inbound goal by making the camera scanner popup clearer on phone-height screens.
- Added a compact `Last saved item and weight` strip above the camera preview when a previous scan exists, so workers do not need to scroll below the video to confirm the last saved product and weight.
- Kept the existing detailed last-saved panel below the camera as well.
- Continuous scanning, external scanner/manual input, auto-save behavior, duplicate blocking, stock movement history, undo actions, RLS, and approval logic were not changed.
- Added scanner source coverage for the above-camera last saved item/weight strip.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Browser QA attempt:

- Tried to start local dev server on `127.0.0.1:3901`.
- `Start-Process` still failed with the Windows duplicate `Path`/`PATH` environment bug.
- A normalized hidden .NET launch using `cmd.exe /c npm.cmd run dev -- --hostname 127.0.0.1 --port 3901` returned a PID but exited silently with empty log files; `Test-NetConnection 127.0.0.1:3901` failed.

Risks / remaining checks:

- Rendered 390px browser QA is still not proven because the local dev server could not be kept running from this environment.
- Real phone camera scanner, external scanner, label printing, and Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue rendered/browser QA when a dev server can stay running, or keep refining small source-proven gaps in the guided Inbound flow.

## 2026-06-29 - Stock inbound scanner last-weight clarity

Task completed:

- Continued the active guided Stock Inbound goal by improving the camera scanner window.
- The scanner popup now shows `Last saved item and weight` as a prominent bordered panel with larger text, using the existing Inbound scan summary passed from the guided session.
- This makes the last saved product/weight easier for workers to confirm during continuous camera scanning without adding any extra OK/Next/Proceed step.
- External scanner/manual input behavior, auto-save behavior, duplicate blocking, stock movement history, undo actions, RLS, and approval logic were not changed.
- Added scanner source coverage for the prominent last saved item/weight panel.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA should confirm the scanner popup at 390px clearly shows the last saved item/weight while the camera remains usable.
- Real phone camera scanner, external scanner, label printing, and Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound browser QA or refine the session summary/whole-session undo path based on rendered behavior.

## 2026-06-29 - Stock inbound display product naming

Task completed:

- Continued the active guided Stock Inbound goal by centralizing stock display names.
- Added a shared stock display-name helper so worker-facing names use manufacturer plus product section/name, for example `TICAN BELLY BONELESS`, while product and manufacturer remain stored/filterable separately.
- Stock Inbound templates, scanner context, barcode-rule setup, scan issue context, session summary, generated labels, stock-unit detail labels, dashboard/report item names, and stock report rows now share the same display-name assembly through the stock data layer or inbound form.
- The helper skips generic `GENERAL` section and avoids duplicating a section/manufacturer that is already present in the product text.
- No database schema, RLS, barcode uniqueness, movement history, undo RPC, or approval logic was changed.

Files changed in this pass:

- `lib/stock/display-names.ts`
- `lib/stock/data.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser QA should confirm recent inbound templates, scanner context, session summary, and labels show the fuller manufacturer + section/name display at 390px width.
- Live Supabase QA should confirm reports/search preserve separate product/manufacturer filters while displaying the combined worker-facing name.
- Phone camera scanner, external scanner, label printing, and Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound live/browser QA for supplier barcode rule learning, no-barcode internal-label confirmation, and whole-session undo.

## 2026-06-29 - Stock scan issue handling refinement

Task completed:

- Refined Stock Module issue handling so a known receive-transfer blocked scan logs a manager-review issue and stops the extra failed submit.
- This keeps the worker message short, avoids noisy duplicate server errors, and lets the worker continue scanning where safe.
- Confirmed existing stock issue handling covers duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.
- Manager review queue already shows scan issue records and supports approve, reject, and mark corrected without changing stock quantities.
- Database adjustment remains approval-gated through the stock-take/damage workflows; this pass did not add destructive migration SQL or weaken RLS.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual Supabase QA should verify actual `barcode_scan_logs` rows contain the expected context fields for inbound, outbound, stock take, transfer, receive transfer, return, and damage/spoilage.
- Browser/device QA should confirm workers see only short blocked-scan messages while managers see the full review queue.

Next recommended task:

- Run live Stock Module QA against seeded Supabase data for the manager scan issue review queue and approve/reject/correct buttons.

## 2026-06-29 - Stock inbound pending label phone layout

Task completed:

- Continued the active guided Stock Inbound goal by improving the no-barcode/internal-label pending label step at phone width.
- The pending label preview and print actions now stay stacked at 390px width and only switch to side-by-side on wider screens.
- This keeps the worker's `Print, attach, then scan` step readable and tappable on phones without changing barcode generation, save timing, stock movement history, undo behavior, RLS, or approval logic.
- Added source coverage for the stacked pending-label layout.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Rendered 390px browser QA should confirm the pending internal label preview, print buttons, and cancel action are stacked and readable.
- Phone camera scanner, external scanner, label printing, and Supabase-backed save/undo remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound live/browser QA for supplier barcode rule learning and no-barcode internal-label confirmation at 390px width.

## 2026-06-29 - Stock inbound internal-label setup guard

Task completed:

- Continued the active guided Stock Inbound goal by tightening internal-label generation prerequisites.
- `Generate internal label` now uses the same `scanSetupReady` requirement as scanning, so workers cannot generate a label before product, manufacturer, origin, and location are all truly ready.
- This specifically prevents the custom `Other / custom` manufacturer/origin path from generating an unsavable label before the worker types the required custom name.
- Server-side validation, barcode uniqueness, stock movement history, undo behavior, RLS, and approval logic were not changed.
- Added source coverage for the setup-ready label generation guard.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live QA should try internal-label mode with `Other / custom manufacturer` and no typed name, confirming label generation is blocked until the custom name is entered.
- Rendered 390px browser QA, phone camera scanner, external scanner, and label printing remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound live QA for custom manufacturer/origin, supplier barcode rule learning, internal-label confirmation, and session undo against a real Supabase project.

## 2026-06-29 - Stock inbound locked custom-name payload

Task completed:

- Continued the active guided Stock Inbound goal by fixing the first-save payload for custom manufacturer/origin in locked no-barcode/internal-label sessions.
- When a worker selects `Other / custom manufacturer` or custom origin, generates an internal label, and the session locks before the first save, the hidden locked form now still sends `brandName` and `originName`.
- This lets the server resolve/create the typed manufacturer/origin during the first printed-label scan instead of losing the custom names while the setup fields are hidden.
- Product/manufacturer display naming, barcode uniqueness, stock movement history, undo behavior, RLS, and approval logic were not changed.
- Added source coverage for locked custom-name payload fields.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should use a custom manufacturer and custom origin in no-supplier-barcode mode, generate a label, scan it, and confirm the first save creates/resolves the custom records.
- Rendered 390px browser QA, phone camera scanner, external scanner, and label printing remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound live QA for custom manufacturer/origin, supplier barcode rule learning, internal-label confirmation, and session undo against a real Supabase project.

## 2026-06-29 - Stock inbound custom origin display fallback

Task completed:

- Continued the active guided Stock Inbound goal by fixing custom-origin display in inbound review paths.
- Added a centralized `selectedOriginName` fallback so custom typed origins keep showing in the scanner context, current scan preset, barcode rule review, and session summary even before the local origin list is refreshed.
- This prevents custom-origin sessions from showing `No origin` after a successful save/review path.
- Product/manufacturer naming, barcode rule learning, barcode uniqueness, stock movement history, undo behavior, RLS, and approval logic were not changed.
- Added source coverage for custom-origin display in guided inbound review/summary.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should create a custom origin during inbound and confirm scanner, rule review, current preset, and summary all show that custom origin.
- Rendered 390px browser QA, phone camera scanner, external scanner, and label printing remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound live QA for custom manufacturer/origin, supplier barcode rule learning, internal-label confirmation, and session undo against a real Supabase project.

## 2026-06-29 - Stock inbound custom manufacturer/origin session scope

Task completed:

- Continued the active guided Stock Inbound goal by hardening custom manufacturer/origin handling during continuous inbound.
- `barcodeInboundAction` now returns the resolved manufacturer (`brandId`) and `originId` after the server resolves a dropdown or custom typed name.
- The inbound form now replaces temporary `Other / custom` markers with the resolved IDs after the first successful save, so later scans stay scoped to the real manufacturer/origin.
- Inbound scan-issue logging no longer sends the temporary `__other` marker as a manufacturer/origin ID; it keeps the readable typed names in issue context instead.
- Product/manufacturer display naming, barcode uniqueness, stock movement history, undo behavior, RLS, and approval logic were not changed.
- Added source coverage for resolved custom manufacturer/origin session scope.

Files changed in this pass:

- `lib/stock/action-state.ts`
- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should create a new custom manufacturer and/or origin during inbound, save the first scan, then confirm later scans continue under the resolved manufacturer/origin without retyping.
- Rendered 390px browser QA, phone camera scanner, external scanner, and label printing remain unproven for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound live QA for custom manufacturer/origin, supplier barcode rule learning, internal-label confirmation, and session undo against a real Supabase project.

## 2026-06-29 - Stock inbound supplier-session review navigation

Task completed:

- Continued the active guided Stock Inbound goal with safer back navigation for finished supplier-barcode sessions.
- The supplier scanner step now allows `Back to Barcode Rule` even after the session is finished, so workers can review the barcode rule/setup trail from the summary path.
- The finished summary now also has `Review setup`, which returns to the locked setup view for product/manufacturer/origin/location review.
- Product/manufacturer/origin/location still remain locked after saved scans or finish; workers must start `New inbound session` before changing setup.
- Barcode-rule learning, continuous scanning, duplicate blocking, label generation, stock movement history, undo RPCs, RLS, and approval logic were not changed.
- Added source coverage so the scanner review navigation remains available.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Rendered 390px browser QA and real device scanner/label tests are still needed for the full guided Stock Inbound goal.

Next recommended task:

- Continue guided Stock Inbound verification in a rendered browser when a dev server can stay running, then test supplier-barcode rule learning and no-supplier-label confirmation end to end against Supabase.

## 2026-06-29 - Stock inbound manual setup review navigation

Task completed:

- Continued the active guided Stock Inbound goal with a small navigation fix for the no-supplier-barcode/manual-weight flow.
- Manual Weight mode now allows `Back to setup` even after the session is locked or finished, because the setup view remains read-only/locked and is safe for review.
- Workers still cannot change product/manufacturer/origin/location mid-session or after finish; they must use `New inbound session` before changing setup.
- Supplier-barcode learning, continuous scanning, label generation, barcode uniqueness, stock movements, undo RPCs, RLS, and approval logic were not changed.
- Attempted local rendered 390px browser QA for `/stock/inbound`. Foreground dev server starts and reports ready, but background dev-server launch attempts in this sandbox either failed with the Windows duplicate `Path`/`PATH` process environment issue or exited without a listening port, so rendered browser/device QA was not completed in this pass.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA is still needed for `/stock/inbound` at 390px, camera scanning, external scanner Enter input, Bluetooth/PDF label flow, and Supabase-backed save/undo behavior.

Next recommended task:

- Continue guided Stock Inbound live QA once a dev server can stay running in the browser environment, then test one supplier-barcode rule-learning session and one no-supplier-barcode internal-label session end to end.

## 2026-06-29 - Stock inbound finished-session setup lock

Task completed:

- Continued the active guided Stock Inbound goal with safer navigation after a session is finished.
- Finished inbound sessions now keep product/manufacturer/origin/location setup locked until `New inbound session` is clicked.
- This prevents workers from changing setup details after finishing while the summary and saved scans still belong to the original session.
- Worker messages now distinguish active locked sessions from finished sessions:
  - Active: finish this session before changing setup.
  - Finished: start a new inbound session before changing setup.
- Supplier-barcode and no-supplier-barcode guided flows, label generation, barcode validation, stock movements, undo RPCs, RLS, and approval logic were not changed.
- Added source coverage for the finished-session setup lock.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should finish one supplier-barcode session and one internal-label session, then confirm setup fields cannot be changed until `New inbound session` starts a fresh batch.

Next recommended task:

- Continue guided Stock Inbound live/browser QA, especially camera scanning, external scanner Enter input, internal label confirmation, and whole-session undo against a real Supabase project.

## 2026-06-29 - Stock inbound voided-session summary state

Task completed:

- Continued the active guided Stock Inbound goal with safer session-summary behavior after whole-session undo.
- Finished inbound sessions now detect when all saved scans have been voided by `Undo/Delete whole session`.
- A voided summary now shows `Session undone. Saved stock units were voided and movement history kept. Start a new inbound session before moving stock.` instead of the normal green print/move-stock next step.
- The `Back to Scanner` / `Back to Manual Weight` summary button is disabled after the whole session is voided, so workers do not continue scanning into a reversed session.
- Stock units, movement records, undo RPC behavior, barcode validation, role/RLS logic, label generation, and approval logic were not changed.
- Added source coverage for the voided-session summary state.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should save a session, finish it, use `Undo/Delete whole session`, and confirm the summary changes to the voided-session message while stock units are voided with `INBOUND_VOID` movement history.

Next recommended task:

- Continue guided Stock Inbound live/browser QA, especially camera scanning, external scanner Enter input, internal label confirmation, and whole-session undo against a real Supabase project.

## 2026-06-29 - Stock issue auto-create and manager review scope

Task completed:

- Refined Stock issue handling so blocked worker scans continue to use short messages while issue details are saved for manager review.
- Kept the existing barcode scan log issue model and confirmed it covers duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule detection failure, unknown stock-take barcode, stock-take mismatch, transfer missing/unexpected item, and spoiled/damaged review.
- Added a scoped scan-issue RLS migration so stock managers can read and review open scan issues for their own stock locations; admin/director access remains broad, worker access remains own logs, and delete remains unchanged.
- Review updates now fail clearly if the issue was already reviewed or outside the manager's stock-location scope instead of reporting success with no updated row.
- Added source guards for issue creation paths from inbound, outbound, stock take, transfer, and return, plus manager approve/reject/correct review and scoped RLS.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-rls-policy-coverage.mjs`
- `supabase/migrations/202606250009_stock_scan_issue_review_scope_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250009_stock_scan_issue_review_scope_v1.sql`
  - Replaces barcode scan log select/update policies.
  - Allows scoped stock managers to review open scan issues only when the issue is linked to a stock location they can access.
  - Keeps admin/director access, own-log worker read access, insert policy, and delete policy unchanged.

Exact SQL order:

- Run all prior Stock migrations first through `202606250008_stock_inbound_session_void_rpc_v1.sql`.
- Then run `202606250009_stock_scan_issue_review_scope_v1.sql`.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-migration-safety.mjs` - passed.
- `node scripts\stock-rls-policy-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA still needs manager-role testing to confirm a scoped manager can review issues in their own stock location and cannot review another location's issue.

Next recommended task:

- Run live Stock issue QA after applying the new migration: trigger one blocked scan in each inbound, outbound, transfer, receive-transfer, return, and stock-take flow, then verify the manager queue shows the issue and approve/reject/correct works only for the manager's scope.

## 2026-06-29 - Stock inbound finish requires saved units

Task completed:

- Continued the active guided Stock Inbound goal with safer session completion.
- `Finish Inbound Session` now requires at least one current-session `SAVED` stock unit.
- If a worker undoes every saved scan and the recent list only contains `VOIDED` labels, the session cannot be finished into a misleading zero-unit summary.
- Pending printed labels still block finishing until scanned or cancelled.
- No database writes, barcode validation, stock movement history, RLS, label generation, undo RPC, or approval logic changed.
- Added source coverage so finish-session gating checks `savedSessionScans.length === 0`.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live QA should save one scan, undo it, and confirm `Finish Inbound Session` is disabled until another scan is saved.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock inbound barcode length mismatch confirmation

Task completed:

- Continued the active guided Stock Inbound goal with safer supplier-barcode scanning.
- When a scanned supplier barcode length differs from the saved/sample rule, camera/external scanner input now logs `BARCODE_LENGTH_MISMATCH`, shows the warning, and does not auto-submit the inbound save.
- Workers can still manually press `Save inbound` after checking the barcode if the decoded weight is correct.
- Normal valid scans still auto-save and continue scanning.
- Duplicate blocking, no-weight fallback, barcode uniqueness, stock movement history, RLS, label generation, undo, and approval logic were not changed.
- Added source coverage so length-mismatch scans require deliberate manual save instead of automatic save.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live scanner QA should confirm a different-length supplier barcode leaves the barcode/weight visible, shows the warning, and waits for a manual save.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock product wording alignment

Task completed:

- Continued the active guided Stock Inbound/product-naming goal by aligning Stock user-facing `Item` labels to `Product` where the screen is showing product names.
- Stock balance, movement, barcode unit, report filter, product master, mobile product cards, stock-take product selection, and stock issue context now use Product wording in visible labels/fallbacks.
- Internal identifiers and database names such as `itemId`, `itemCode`, `items`, and `customer_order_items` were not renamed.
- No database writes, barcode validation, stock movement history, RLS, label generation, undo, or approval logic changed.
- Added/updated source coverage for Product labels in item master, reports, mobile cards, and smoke routes.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/stock-units-table-client.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-item-master-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\smoke-routes.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should confirm Stock navigation still feels natural where route names remain `/stock/items` and internal item-code fields remain item-code based.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock manufacturer wording sweep

Task completed:

- Continued the active guided Stock Inbound/product-naming goal with a Stock-wide wording consistency sweep.
- User-facing Stock item master, report filters, stock settings, stock-take cards, stock-unit tables, and stock-unit detail now use `Manufacturer` wording instead of `Brand` where the field is shown to staff.
- Fallbacks now use `No manufacturer`.
- Internal field names, database tables, RLS, barcode validation, movement history, label generation, undo, and approval logic were not changed.
- Staff-facing inbound error text now says `Choose a manufacturer before receiving inbound stock.`
- Added/updated source coverage for item master, report, stock-take warning, mobile UX, and route smoke wording.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `scripts/smoke-routes.mjs`
- `scripts/stock-item-master-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\smoke-routes.mjs` - initially failed on the old `Other / custom brand` guard, then passed after updating it to `Other / custom manufacturer`.
- `node scripts\stock-mobile-ux-coverage.mjs` - initially failed on old owner QA wording, then passed after updating QA docs to `item/manufacturer/location`.
- `node scripts\stock-take-lock-coverage.mjs` - passed.
- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should confirm report filters, item master, stock take, and stock unit detail still read naturally with Manufacturer wording and still filter/save correctly.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock unit manufacturer wording

Task completed:

- Continued the active guided Stock Inbound/product-naming goal by aligning stock-unit/report surfaces with the manufacturer wording used in inbound.
- Barcode stock unit tables now show the `brandName` field under the user-facing header `Manufacturer`.
- Stock unit detail now labels the field as `Manufacturer`.
- Stock unit/report fallbacks now say `No manufacturer` instead of `Unbranded` / `No brand` in the relevant display paths.
- This preserves the existing database intent and `brand_id` field while making the UI/report language match the product naming rule.
- No database writes, barcode validation, stock movement history, RLS, label generation, undo, or approval logic changed.
- Added source coverage for stock-unit manufacturer display wording.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/stock-unit-detail.tsx`
- `components/stock/stock-units-table-client.tsx`
- `lib/stock/data.ts`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live report/unit-detail QA should confirm all stock unit and inbound-age report rows still filter correctly while showing manufacturer wording.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock inbound scanner context fields

Task completed:

- Continued the active guided Stock Inbound goal with stronger scanner-popup context.
- The inbound scanner popup context now shows display product name, separate product name, separate manufacturer, origin, and location.
- Scanner session totals now say `saved unit(s)` in no-barcode/manual-label mode and `saved scan(s)` in supplier-barcode mode.
- This keeps the manufacturer + product display name visible while preserving product/manufacturer as separate worker-facing fields.
- No database writes, barcode validation, stock movement history, RLS, label generation, undo, or approval logic changed.
- Added source coverage for the separate scanner context fields and mode-specific scanner totals.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone/camera QA should confirm the longer scanner context remains readable in the scanner popup around 390px width.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock inbound manual session wording

Task completed:

- Continued the active guided Stock Inbound goal with no-barcode/manual-label clarity.
- Manual/internal-label inbound sessions now title the current-session list as `Recent saved labels`.
- Supplier-barcode inbound still titles the same list as `Recent inbound scans`.
- Manual/internal-label inbound now titles the latest saved unit as `Previous saved label`, while supplier-barcode inbound still says `Previous scan`.
- The finished-session summary now gives a clearer worker next action: `Next: print labels, attach them to product, then move stock.`
- No database writes, barcode validation, stock movement history, RLS, label generation, undo, or approval logic changed.
- Added source coverage for the mode-specific manual headings and summary next action.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone-width QA should confirm the heading change makes manual label sessions easier to understand after the first saved unit.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock inbound scan field cleanup after save

Task completed:

- Continued the active guided Stock Inbound goal with continuous-scanning clarity.
- After a successful inbound save, the form now clears both the barcode field and the net-weight field.
- The saved product/weight remains visible in the existing Previous Scan card and scanner summary, so workers compare against the actual saved scan instead of a stale weight input.
- Internal-label/manual mode still returns focus to the weight input for the next unit; supplier-barcode mode still returns focus to the barcode field for continuous scanning.
- No database writes, barcode validation, RLS, stock movements, labels, undo, or approval logic changed.
- Added source coverage for clearing barcode and net weight after save.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone/device QA should confirm the field clears immediately after successful saves while the Previous Scan card still shows the saved weight clearly.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock inbound setup display-name card

Task completed:

- Continued the active guided Stock Inbound goal with Page 1 setup clarity.
- The current inbound preset card now explicitly shows the generated `Display product name` as a read-only value.
- Origin and location remain shown separately underneath, so workers can see the final manufacturer + product display name without losing separate setup fields.
- This applies to both supplier-barcode inbound and no-barcode/manual-label inbound because both modes share the same setup card.
- No database writes, barcode validation, RLS, stock movements, label generation, undo, or approval logic changed.
- Added source coverage for the explicit setup display-name card.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone-width QA should confirm the setup card fits cleanly and makes the manufacturer + product display name obvious before scanning.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially live camera/external scanner and Supabase-backed save/undo.

## 2026-06-29 - Stock inbound guided step context titles

Task completed:

- Continued the active guided Stock Inbound goal with clearer page context.
- The green context card above the inbound barcode field now changes title by guided step:
  - `Learning barcode rule` on the Barcode Rule page.
  - `Manual label inbound` on the no-barcode/manual label confirmation page.
  - `Scanning inbound` on normal supplier-barcode continuous scanning.
- This keeps the same product/manufacturer display name, origin, and location context while making each guided page clearer for workers.
- No database writes, barcode validation, RLS, stock movements, undo, label generation, or approval logic changed.
- Added source coverage for the step-specific context titles.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone-width QA should confirm the context title changes correctly when moving between Setup, Barcode Rule, Manual Weight, Scan, and Summary.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially real camera scanner and live Supabase-backed saves/undo.

## 2026-06-29 - Stock inbound sample barcode helper text

Task completed:

- Continued the active guided Stock Inbound goal with Page 2 barcode-rule clarity.
- The reusable barcode field now accepts custom helper text while keeping its default scanner fallback message for other workflows.
- Stock Inbound now shows step-specific scanner guidance:
  - Barcode Rule page: `Scan or paste one supplier barcode sample to learn this rule.`
  - Manual/no-barcode page: `Scan the printed internal label after attaching it.`
  - Supplier-barcode scanner page: `Continuous scanning is on. Handheld scanner Enter saves each valid barcode.`
- This makes the rule-learning, internal-label confirmation, and continuous scan pages clearer without changing database writes, barcode uniqueness, RLS, movement history, undo, or approval logic.
- Added source coverage for the reusable helper prop and the inbound-specific helper messages.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone-width QA should confirm workers understand the Page 2 sample-barcode step, printed-label confirmation step, and continuous scan step without extra explanation.

Next recommended task:

- Continue requirement-by-requirement QA of guided Stock Inbound, especially real camera scanner and live Supabase-backed session undo.

## 2026-06-29 - Stock inbound manual session counter wording

Task completed:

- Continued the active guided Stock Inbound goal with the no-barcode/manual-weight page.
- Manual Weight Entry now labels the live counters as `Saved units` and `Saved total weight` instead of barcode-specific scan wording.
- Supplier-barcode inbound still shows `Saved scans` and `Saved weight`.
- This makes Page 2 of the no-barcode inbound flow match the requested continuous weight-entry workflow while preserving the existing save, label confirmation, undo, summary, barcode uniqueness, movement history, and role/location logic.
- Added source coverage so the manual-mode counter wording is guarded by Stock acceptance and Stock mobile UX checks.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone-width QA should confirm the manual-weight page clearly shows saved unit count, saved total weight, previous saved item/weight, pending label confirmation, and focus returning to the weight input after save.

Next recommended task:

- Continue requirement-by-requirement QA of the guided Stock Inbound pages, especially the live camera/external scanner and real Supabase session undo paths.

## 2026-06-29 - Stock issue handling review log refinement

Task completed:

- Refined Stock issue handling so workers keep short scan/return messages while manager-review issue rows are created automatically.
- Confirmed the existing issue record model is `barcode_scan_logs` with `issue_type`, barcode, item, selected item, expected/scanned location/status, related context, creator, timestamp, and review status.
- Added a missing return-path issue log: when a worker saves a return as `Spoiled / Damaged`, the action now records `SPOILED_DAMAGED_REVIEW` with barcode, item, expected/scanned location, expected/scanned status, and return condition context.
- Kept business logic unchanged: stock-take mismatch/manual correction still requires approval before database adjustment, and the manager queue can mark scan/workflow issues approved, rejected, or corrected.
- Strengthened stock acceptance coverage for barcode length mismatch issue logging and spoiled/damaged return review issue logging.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The review queue is still implemented through `barcode_scan_logs`, not a separate `stock_issues` table. This is intentional for this small scoped change, but live Supabase QA should confirm managers see all open issue types under dashboard/reports.
- Manager `approve/reject/correct` on scan issues updates issue review status only. Real stock corrections still need the existing stock-take/manual correction approval workflow.

Next recommended task:

- Run live Stock QA for duplicate inbound, barcode length mismatch, outbound invalid scan, stock-take unknown/mismatch, transfer missing/unexpected, return invalid scan, and spoiled/damaged return review visibility.

## 2026-06-29 - Stock inbound scanner current setup context

Task completed:

- Continued the active guided Stock Inbound goal with scanner popup clarity.
- Added optional scanner context support to the reusable barcode scanner.
- Stock Inbound now passes the current display product name, origin, and location into the scanner popup.
- The scanner popup shows `Current inbound setup` before/alongside saved scan summaries, so workers know which item/session they are scanning into even before the first successful scan.
- Existing last saved item/weight, inbound total, continuous scan, manual entry, and undo previous scan behavior remain unchanged.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - initially failed because `inboundScannerContextSummary` referenced `scanSetupReady` before declaration, then passed after moving the context summary below `scanSetupReady`.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real camera QA should confirm the scanner popup shows current setup, last saved item/weight, inbound totals, and undo previous scan clearly on phone width.

Next recommended task:

- Continue guided Stock Inbound QA for camera permission/rear camera behavior and live Supabase scan saving.

## 2026-06-29 - Stock inbound barcode-rule scope card

Task completed:

- Continued the active guided Stock Inbound goal with Page 2 barcode-rule clarity.
- Added a read-only Barcode Rule Setup card on the barcode-rule page.
- The card shows product, manufacturer, origin, location, and generated display name before the worker teaches/saves the supplier barcode rule.
- This makes it clearer that the rule is tied to the current product + manufacturer + origin setup while keeping those fields stored separately.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live phone-width QA should confirm the barcode-rule setup card fits cleanly around 390px and helps workers verify the selected setup before saving a rule.

Next recommended task:

- Continue guided Stock Inbound QA around real camera scanner behavior, external scanner Enter input, and live Supabase rule saving.

## 2026-06-29 - Stock inbound product search display names

Task completed:

- Continued the active guided Stock Inbound goal with manufacturer-based product naming.
- Product dropdown/search in Barcode Inbound now uses `manufacturer + product` display names even before the worker selects a session manufacturer.
- When no session manufacturer is selected, product options use the item's default manufacturer. When a session manufacturer is selected, options show the active session manufacturer + product.
- Product search now matches item code, raw product name, the item's default manufacturer display name, and the active session manufacturer display name.
- This keeps product and manufacturer separate while making search/results align with the display-name rule.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - initially failed on an old Stock mobile UX guard that expected the previous raw product-name expression, then passed after updating the guard for `formatProductName`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live browser QA should confirm workers can search products by default manufacturer name before selecting the inbound session manufacturer.

Next recommended task:

- Continue guided Stock Inbound QA around setup search, manufacturer selection, supplier-barcode rule creation, and manual-weight label confirmation.

## 2026-06-29 - Stock inbound quick-create manufacturer session binding

Task completed:

- Continued the active guided Stock Inbound goal with the quick-create setup flow.
- When a worker quick-creates a product after choosing `Other / custom manufacturer`, the created item action already resolves the typed manufacturer into a real brand row.
- The inbound page now immediately replaces the `__other` manufacturer placeholder with the resolved manufacturer id in the same session.
- Product id and resolved manufacturer id are applied together through the same barcode-rule matching path, so the current session can save/reuse rules by product + manufacturer + origin without requiring a reload.
- This preserves separate product/manufacturer storage while keeping display names as `manufacturer + product name`.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm that a typed new manufacturer can be quick-created and then used immediately for the first barcode rule and inbound scan without refreshing.

Next recommended task:

- Continue guided Stock Inbound QA around the live supplier-barcode rule page and no-barcode/manual-weight label confirmation flow.

## 2026-06-29 - Stock issue handling auto-log hardening

Task completed:

- Refined Stock issue handling so workers continue to see short scan messages while the system creates reviewable issue records automatically in `barcode_scan_logs`.
- Supplier-barcode rule learning now logs `BARCODE_RULE_DETECTION_FAILURE` when the worker enters actual kg but no barcode weight position can be detected, then guides the worker to internal-label inbound.
- Stock-take submit now creates a `STOCK_TAKE_MISMATCH` issue when expected active barcode stock for the selected item/location was not counted. This only creates a manager-review issue; it does not adjust stock until the existing review/approval flow.
- Manager dashboard wording now treats the queue as open scan/workflow issues, not only failed scans, so success-with-review cases such as damage review or stock-take exceptions are still visible.
- Added coverage checks for inbound issue creation, missing expected stock-take mismatch logging, and the existing outbound/transfer/receive/return issue paths.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm manager review rows appear for barcode rule detection failure and missing-stock-take mismatch.
- Manager approve/reject/correct currently updates the issue review status only. Any real stock correction still needs the existing approved stock-take/manual correction workflow.

Next recommended task:

- Run owner QA through Stock Inbound rule-failure, Stock Take missing barcode, Transfer unexpected/missing item, Outbound invalid barcode, and Return invalid barcode from the Vercel app after migrations are current.

## 2026-06-29 - Stock inbound quick custom manufacturer resolve

Task completed:

- Continued the active guided Stock Inbound goal with the custom manufacturer quick-create gap.
- Quick product creation inside Barcode Inbound now submits `defaultBrandName` when the worker chooses `Other / custom manufacturer`.
- `createItemAction` now resolves the selected default manufacturer id or typed default manufacturer name through the existing brand resolver before inserting the item.
- The created item action returns the resolved `brandId`, and the inbound page keeps that id on the local item copy so display names continue to use manufacturer + product.
- This keeps product and manufacturer separate while allowing a typed manufacturer to become a real manufacturer row during quick product creation.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/action-state.ts`
- `lib/stock/actions.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-item-master-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `docs/BUSINESS_RULES.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - initially failed on the old item-master coverage guard, then passed after aligning the guard with resolved default manufacturer creation.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should confirm a worker can type a new manufacturer in Barcode Inbound, quick-create a product, and see the manufacturer created/resolved without losing stock-location or role isolation.

Next recommended task:

- Continue guided Stock Inbound browser/device QA for the full supplier-barcode and no-barcode flows.

## 2026-06-29 - Stock inbound quick product manufacturer default

Task completed:

- Continued the active guided Stock Inbound goal with product/manufacturer storage hardening.
- Quick-created products inside Barcode Inbound now save the currently selected existing manufacturer as the item default manufacturer.
- The in-page local item copy also keeps that default manufacturer id, so search/results can immediately show the generated display name as `manufacturer + product`.
- Custom typed manufacturers still remain separate inbound manufacturer context until they are created/resolved by the existing brand path; no schema or RLS changes were made.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `docs/BUSINESS_RULES.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- If the worker types a brand-new custom manufacturer and has not created/resolved it yet, quick product creation cannot store that custom manufacturer as item default until the manufacturer exists as a brand row.

Next recommended task:

- Continue guided Stock Inbound QA around quick product creation, custom manufacturer creation, and display names in live Supabase data.

## 2026-06-29 - Stock inbound scan issue manufacturer context

Task completed:

- Continued the active guided Stock Inbound goal with stronger product/manufacturer traceability for inbound scan issues.
- Browser-side Stock Inbound issue logging now includes product name, manufacturer name, display product name, brand id, and origin id in the existing `barcode_scan_logs.related_context` JSON.
- Server-side barcode inbound failures now include resolved brand/origin ids in the same related context path.
- Manager scan issue review now prefers the captured display product name, then captured manufacturer + product, then exact related brand id, then item default manufacturer.
- No schema change was needed; barcode uniqueness, stock movement history, RLS, and approval logic remain unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after adding the missing empty related-context fallback in `lib/stock/data.ts`.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Existing old scan issue rows still only have whatever context was logged at the time; new inbound issue rows will carry richer manufacturer/product context.

Next recommended task:

- Continue guided Stock Inbound browser/device QA and verify live Supabase issue rows show the captured display product name in manager review.

## 2026-06-29 - Stock inbound manager issue display names

Task completed:

- Continued the active guided Stock Inbound goal with product/manufacturer display-name hardening.
- Manager scan issue review rows now use the Stock display-name helper path, so item references prefer `manufacturer + product name` where the item has a default manufacturer.
- This keeps issue review/report UI aligned with the guided inbound naming rule without changing barcode uniqueness, stock movement history, RLS, or schema.
- Existing stock balance, movement, label, stock-unit detail, scanner, and inbound summary display-name behavior remains unchanged.

Files changed in this pass:

- `lib/stock/data.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Existing scan issue rows do not store the exact scanned stock-unit manufacturer id, so issue alerts use the item default manufacturer when only `item_id` / `selected_item_id` is available. Exact per-unit manufacturer display for historical issue rows would need extra issue context captured at scan time.

Next recommended task:

- Continue guided Stock Inbound QA and close remaining proof gaps around real browser/device scanner behavior and Supabase-backed session undo.

## 2026-06-29 - Stock inbound atomic whole-session undo

Task completed:

- Continued the active guided Stock Inbound goal with a safer session-summary undo.
- Added a server-side `void_inbound_stock_session` RPC so `Undo/Delete whole session` validates the whole inbound batch and voids all eligible saved scans in one database transaction.
- Updated the Stock Inbound summary button to call `undoInboundSessionAction` once instead of looping through individual scan undo calls in the browser.
- The RPC preserves the current safety rules: signed-in stock operator, allowed stock location, original scanner unless manager/admin, only unmoved inbound stock, `VOIDED` stock unit status, `INBOUND_VOID` movements, scan logs, and audit logs.
- Kept barcode uniqueness, single-scan undo, label confirmation, role/location isolation, and existing Stock Inbound flow unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250008_stock_inbound_session_void_rpc_v1.sql`

SQL run order:

1. Run existing Stock migrations through `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`.
2. Then run `supabase/migrations/202606250008_stock_inbound_session_void_rpc_v1.sql`.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-migration-safety.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase QA should create an inbound session with multiple saved barcodes, click `Undo/Delete whole session`, then confirm every unit becomes `VOIDED` and every saved barcode has an `INBOUND_VOID` movement/audit trail.

Next recommended task:

- Continue guided Stock Inbound QA on a real browser/device, especially scanner popup, external scanner Enter input, label confirmation, and summary undo.

## 2026-06-29 - Stock inbound manual template mode

Task completed:

- Continued the active guided Stock Inbound goal with a no-barcode/manual-weight flow fix.
- Recent inbound templates are now mode-aware.
- In `No supplier barcode` mode, tapping a recent template keeps the session in Manual Weight instead of forcing Supplier Barcode mode.
- When setup is complete, template selection moves directly to Manual Weight and focuses the weight field with `Ready. Enter weight.`
- Existing Supplier Barcode template behavior remains unchanged: saved-rule templates go to scanner, and no-rule templates go to barcode-rule setup.
- Kept barcode uniqueness, stock movement history, RLS, approval behavior, label confirmation, and undo behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm a manual-weight worker can choose a recent template and immediately enter kg without being switched to supplier barcode scanning.

Next recommended task:

- Continue hardening guided Stock Inbound with browser/device QA and final requirement-by-requirement proof.

## 2026-06-29 - Stock inbound pending label finish guard

Task completed:

- Continued the active guided Stock Inbound goal with a no-barcode/manual-weight safety fix.
- Stock Inbound now blocks `Finish Inbound Session` while an internal barcode label save is pending.
- Current worker copy says `Cancel pending label before finishing this session.` while the save is still pending.
- `New inbound session` now clears pending internal-label state and refs, preventing stale printed-label state from leaking into the next session.
- Kept barcode uniqueness, immediate label save, stock movement history, RLS, approval behavior, and existing void/undo behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm the Finish button is blocked with a pending printed label, then becomes available after scanning the label or cancelling it.

Next recommended task:

- Continue hardening guided Stock Inbound with real browser/device QA and final requirement-by-requirement proof.

## 2026-06-29 - Stock inbound scanner popup undo previous scan

Task completed:

- Continued the active guided Stock Inbound goal with a popup-level undo action.
- The reusable scanner dialog now accepts a contextual `scanActionSlot`.
- Stock Inbound passes an `Undo Previous Scan` button into the scanner popup when the current session has a latest saved scan.
- The popup button calls the existing `undoInboundScanAction`, so the latest scan is voided through the existing inbound void RPC and audit/movement history is kept.
- The popup shows `Previous scan undone. Audit trail kept.` after a successful undo.
- Kept barcode detection, manual/external scanner input, duplicate blocking, session totals, stock movement history, RLS, and approval behavior unchanged.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm the scanner popup shows `Undo Previous Scan`, voids the latest saved scan, updates the total, and remains usable for continuous scanning.

Next recommended task:

- Continue hardening guided Stock Inbound against the remaining session-summary and whole-session undo proof requirements.

## 2026-06-29 - Stock inbound scanner popup session totals

Task completed:

- Continued the active guided Stock Inbound goal with a scanner popup total improvement.
- Stock Inbound now computes an `inboundScanTotalSummary` from current-session saved scans.
- The inbound scanner passes that total into the reusable scanner dialog as `scanTotalSummary`.
- Workers can now keep the camera scanner open and see `Inbound session total` with saved count and total kg while continuous scanning.
- Kept barcode detection, manual/external scanner input, duplicate blocking, undo previous scan, inbound save behavior, stock movement history, RLS, and approval behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm the scanner popup updates `Inbound session total` after each saved scan without closing the camera.

Next recommended task:

- Continue hardening guided Stock Inbound against the remaining session-summary and whole-session undo requirements.

## 2026-06-29 - Stock issue auto-create server metadata

Task completed:

- Refined Stock issue handling so workers still see short blocked-scan messages while manager review gets structured issue records.
- Added explicit issue type and context to every server-side `rejectBarcodeScan` path in Stock actions.
- Covered duplicate outbound, mixed-source outbound, same-location transfer, unavailable transfer/receive/return stock, receive-transfer missing selected transfer, inspection-release invalid status, and stock-take invalid/session/status blocks.
- Existing client-side auto issue logging remains for inbound duplicate/length/rule failures, outbound scan blocks, transfer, receive-transfer, return, stock take, and damage/spoilage review.
- Manager scan issue review still supports approve, reject, and mark corrected. Stock take mismatch/manual correction still requires approval before database adjustment.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase QA should verify migration `202606250007_stock_scan_issue_context_v1.sql` is applied so issue context columns exist.
- Manual QA should create one issue each from inbound, outbound, stock take, transfer/receive-transfer, return, and damage/spoilage, then confirm the manager queue shows the correct type, item, location/status, and related context.

Next recommended task:

- Run live/staging Stock issue manual QA with a worker user and manager user before piloting warehouse scanning.

## 2026-06-29 - Stock inbound scanner popup last saved item weight

Task completed:

- Continued the active guided Stock Inbound goal with a scanner popup improvement.
- The reusable barcode scanner dialog now accepts an optional `scanSummary`.
- Stock Inbound passes the latest saved manufacturer/product display name and kg into the camera scanner dialog.
- Workers can now see `Last saved item and weight` in the scanner popup while continuous scanning remains open.
- Kept barcode detection, manual/external scanner input, duplicate blocking, inbound save behavior, stock movement history, RLS, and approval behavior unchanged.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm the scanner popup shows `Last saved item and weight` after a saved scan and stays open for continuous scanning.

Next recommended task:

- Continue hardening the guided Stock Inbound flow, especially real 390px browser/device QA when available.

## 2026-06-29 - Stock inbound manual weight Enter-to-label

Task completed:

- Continued the active guided Stock Inbound goal with a focused no-barcode/manual-weight speed improvement.
- Manual Weight now lets workers press Enter in the weight field to generate the internal barcode label.
- The print/attach/scan confirmation flow remains unchanged, so stock is still saved only after the generated label is scanned.
- If a label is already pending, pressing Enter tells the worker to scan the printed label and focuses the barcode field.
- Kept barcode uniqueness, internal label generation, stock movement history, RLS, and approval behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm pressing Enter in Manual Weight generates a label and that scanning the printed label saves stock, clears the field, and focuses the next weight entry.

Next recommended task:

- Continue hardening the guided Stock Inbound flow, especially real 390px browser/device QA when available.

## 2026-06-29 - Stock inbound custom origin search

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 origin-entry improvement.
- When a worker searches for an origin that is not in the active list, the setup page now offers `Use search text as custom origin`.
- The action selects `Other / custom origin` and fills the custom origin field.
- Kept the existing inbound server action, named-record resolution, barcode rules, stock movement history, RLS, and approval behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm a missing origin search can be copied into custom origin, then remains visible separately in scanner and summary.

Next recommended task:

- Continue hardening the guided Stock Inbound flow, especially real 390px browser/device QA when available.

## 2026-06-29 - Stock inbound custom manufacturer search

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 manufacturer-entry improvement.
- When a worker searches for a manufacturer that is not in the active list, the setup page now offers `Use search text as custom manufacturer`.
- The action selects `Other / custom manufacturer` and fills the custom manufacturer field, while product selection/creation remains separate.
- Kept the existing inbound server action, named-record resolution, barcode rules, stock movement history, RLS, and approval behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm a missing manufacturer search can be copied into custom manufacturer, then combined with a selected/created product as `manufacturer + product`.

Next recommended task:

- Continue hardening the guided Stock Inbound flow, especially real 390px browser/device QA when available.

## 2026-06-29 - Stock inbound quick product manual entry

Task completed:

- Continued the active guided Stock Inbound goal with a focused Page 1 setup improvement.
- When a worker searches for a product that is not in the list, the `New product` panel can now reuse the current search text as the product name.
- Added worker-facing guidance that product and manufacturer are stored separately, so product name stays product-only and manufacturer is selected/customized below.
- Kept the existing item-create server action, item-code generation, barcode rules, stock movement history, RLS, and approval behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm a missing product search can be copied into `New product`, created, selected, and then combined with a separate manufacturer for the display name.

Next recommended task:

- Continue hardening the guided Stock Inbound flow against the original requirement audit, especially real 390px browser/device QA when available.

## 2026-06-29 - Stock issue auto-create receive-transfer polish

Task completed:

- Refined Stock issue handling without adding worker issue forms or changing stock deduction/approval rules.
- Receive Transfer now auto-logs client-side blocked scan issues with structured context before the worker continues.
- Wrong-location receive-transfer scans now create `WRONG_LOCATION` issues with expected/scanned location and status context.
- Unexpected, duplicate, not-found, and unavailable receive-transfer scans now create the matching issue type for manager review.
- Existing server-side `rejectBarcodeScan` safeguards remain in place, so this is an audit/usability hardening layer rather than a permission change.
- Manager review remains approve/reject/correct only; database adjustment for stock-take mismatch/manual correction still happens only through approval workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase QA should confirm the target database has migration `202606250007_stock_scan_issue_context_v1.sql`, especially `issue_type`, context columns, and `review_status`.
- Manual QA should create one issue each from inbound, outbound, receive-transfer, transfer, return, stock take, and damage/spoilage, then verify the manager queue shows useful context.

Next recommended task:

- Run stock issue manual QA on Vercel/staging with a worker and manager user pair.

## 2026-06-29 - Stock inbound rule failure manual fallback

Task completed:

- Continued the active guided Stock Inbound goal by tightening the Page 2 failed-rule fallback.
- When a worker scans a sample barcode and enters actual kg but no matching weight position can be inferred, the UI now switches to `Manual Weight`.
- The typed kg is kept so the worker can immediately generate an internal label, print/attach it, and scan it.
- This makes the required `rule cannot be used/found -> no-barcode inbound` path active instead of only showing a warning.
- Existing barcode rule save logic, barcode uniqueness, stock movement history, RLS, and approval behavior were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm a non-inferable sample barcode moves from Barcode Rule to Manual Weight and lets the worker generate/print/scan an internal label.

Next recommended task:

- Run the full command set, then browser-test one barcode sample where the typed kg cannot infer a rule.

## 2026-06-29 - Stock inbound rule preview derivation

Task completed:

- Continued the active guided Stock Inbound goal by correcting the Page 2 extracted weight preview.
- The Barcode Rule page now derives `Extracted weight preview` from the sample barcode plus current rule fields.
- The preview no longer simply mirrors the manually entered actual kg.
- The actual kg input remains the value saved for the sample inbound stock unit/rule learning flow.
- The preview now shows its decode source when a barcode is present.
- Existing barcode rule fields, barcode rule RPC save path, duplicate checks, stock movements, RLS, and approval behavior were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm changing weight start/digits/decimals updates the read-only preview while actual kg stays editable.

Next recommended task:

- Run the full command set, then browser-test Page 2 barcode-rule setup with one sample barcode.

## 2026-06-29 - Stock movement report manufacturer display

Task completed:

- Continued the active Stock Inbound/product-naming goal by extending manufacturer + product display names into movement-based reports.
- Supabase stock movement rows now use linked stock unit `brand_id` where available, based on `stock_unit_id` or barcode.
- Demo movement rows are also enriched from demo stock units so local/demo movement history matches the same display-name rule.
- Inbound, outbound, movement history, transfer pending, and report search now benefit from manufacturer + product names where the movement is linked to a barcode stock unit.
- The old stock helper for item/location totals now sums manufacturer-aware balance rows by `itemId` and location instead of matching the old item display string.
- No schema, RLS, stock movement write, barcode uniqueness, approval, or server-action behavior changed.

Files changed in this pass:

- `lib/stock/data.ts`
- `scripts/stock-report-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed after updating a stale guard string.
- `npm.cmd run smoke` - failed once on the stale label guard, then passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Movement rows that are not linked to a stock unit or barcode still fall back to the existing item-only movement name.
- Real browser/report QA should confirm `/stock/movements` and `/stock/reports` show manufacturer + product names for barcode-linked movements.

Next recommended task:

- Run the full command set, then browser-test movement history and report search for a manufacturer name.

## 2026-06-29 - Stock inbound barcode-rule sample save guard

Task completed:

- Continued the active guided Stock Inbound goal by tightening Page 2 barcode-rule learning.
- Sample barcode scans on the Barcode Rule page now stop and ask the worker to enter actual kg instead of auto-saving from a guessed/default decode.
- Typing actual kg on the Barcode Rule page now always tries to infer the barcode weight position from the sample barcode, even if the default fields could decode some number.
- The regular scanner page still auto-saves valid scans continuously; only the barcode-rule setup page waits for actual kg and a save action.
- `Save inbound` becomes `Save rule and inbound` on the Barcode Rule page and is disabled until both sample barcode and actual kg are present.
- Existing barcode uniqueness, stock movement, barcode rule RPC, RLS, and approval behavior were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm Page 2 sample scan focuses actual kg, learns the rule after save, and Page 3 still auto-saves continuous scans.

Next recommended task:

- Run the full command set, then browser-test a first-time supplier barcode rule on `/stock/inbound`.

## 2026-06-29 - Stock balance manufacturer display preservation

Task completed:

- Continued the active guided Stock Inbound/product-naming goal by fixing a report/balance gap.
- Stock balance rows now group by product + manufacturer + location instead of product + location only.
- Balance display names now use the generated manufacturer + product name while preserving manufacturer as a separate `brandName` field.
- The stock balance table now shows a separate `Manufacturer` column.
- Mobile stock balance cards now show manufacturer alongside location/category.
- Stock reports now include explicit `Stock balance` rows using manufacturer + product display names.
- Existing location/category totals, barcode uniqueness, stock movement history, approval logic, and database schema were not changed.

Files changed in this pass:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/report QA should confirm `/stock/balance` and `/stock/reports` show manufacturer + product names correctly under live Supabase data.

Next recommended task:

- Run the full command set, then browser-test stock balance/report filters with two manufacturers for the same product.

## 2026-06-29 - Stock inbound manufacturer wording alignment

Task completed:

- Continued the active guided Stock Inbound goal with a focused worker-facing wording fix.
- Inbound setup now labels the manufacturer selector as `Manufacturer` instead of `Manufacturer / Brand`.
- The shared brand selector now supports custom display labels, so inbound can show `Select manufacturer` and `Other / custom manufacturer` while existing Item Master and Stock Take brand wording stays unchanged.
- Preserved the existing database intent: product remains `items`, manufacturer remains existing `brands`/`brand_id`, and filters/reporting can still use the separate product and manufacturer fields.
- No schema, RLS, barcode uniqueness, stock movement, approval, or server-action behavior changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA is still needed for the complete guided inbound flow, camera scanner, external scanner Enter input, and label print/scan confirmation.

Next recommended task:

- Run the full command set, then browser-test `/stock/inbound` at 390px with one supplier-barcode session and one no-supplier-barcode/internal-label session.

## 2026-06-29 - Stock manager issue review context polish

Task completed:

- Refined Stock issue handling without adding worker forms or changing stock workflow rules.
- Kept existing automatic issue creation through `barcode_scan_logs` for inbound, outbound, transfer, receive-transfer, return, stock take, and damage/spoilage review paths.
- Extended the Stock data model to carry `related_context` from existing issue records.
- Manager scan issue review now shows readable item names, selected item names, expected/scanned location names, expected/scanned status, and related context such as session/order/customer/transfer IDs when available.
- Workers still see short scan messages only; manager review remains approve/reject/correct.
- No database adjustment logic changed. Stock take mismatch/manual correction still requires approval before adjustment through existing workflows.

Files changed in this pass:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase QA should confirm `barcode_scan_logs.related_context` is present in the target database by running migrations through `202606250007_stock_scan_issue_context_v1.sql`.
- Manual QA should create one issue each from inbound duplicate, outbound unavailable/wrong location, transfer unexpected/missing, return blocked, stock-take unknown/mismatch, and damage/spoilage request, then verify the manager review card shows useful context.

Next recommended task:

- Run the Stock issue manual QA set on Vercel/staging with seeded worker and manager users.

## 2026-06-29 - Stock inbound no-weight fallback mode switch

Task completed:

- Continued the Stock Inbound guided-flow goal with a small workflow alignment fix.
- When a supplier barcode scan cannot decode weight, the UI now switches to `No supplier barcode` / Manual Weight mode instead of leaving the worker on the supplier scan page.
- When a typed sample weight cannot infer a barcode weight position, the UI also switches to Manual Weight mode and focuses the weight field.
- Existing duplicate logging, barcode rule failure logging, internal-label generation, and barcode inbound save action were reused.
- No schema, RLS, stock movement, barcode uniqueness, or approval logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm a no-weight supplier barcode jumps to Manual Weight mode and focuses the weight field.

Next recommended task:

- Test one supplier barcode with no weight and one sample barcode whose actual kg cannot infer a rule.

## 2026-06-29 - Stock inbound label display-name fix

Task completed:

- Continued the Stock Inbound guided-flow goal with a small label consistency fix.
- Inbound saved-label records now use the same generated manufacturer + product display name shown in the scanner and summary.
- Generated internal-label preview/print records now also use the same display name.
- This fixes custom manufacturer text in labels; previously label creation used only the selected brand record.
- No schema, RLS, stock movement, barcode uniqueness, or approval logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-label-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real label/PDF QA should confirm custom manufacturer names print correctly on the 50mm x 30mm label.

Next recommended task:

- Test one custom-manufacturer inbound label in browser/device QA.

## 2026-06-29 - Stock inbound 390px browser QA attempt

Task completed:

- Attempted to verify `/stock/inbound` in the in-app browser at `390 x 844`.
- Started the local app on port `3900` far enough for one direct route check.
- Captured evidence that `/stock/inbound` compiled and redirected unauthenticated access to `/login`.
- Documented the browser QA blocker in `docs/STOCK_QA_EVIDENCE.md`.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands / checks:

- Direct local route check: `/stock/inbound` returned `307`, then `/login` returned `200`.
- Browser viewport setup: `390 x 844`.
- In-app browser route check: blocked by local dev server exiting before browser navigation.

Blocker:

- `Start-Process` failed with Windows duplicate environment key `Path` / `PATH`.
- PowerShell job and detached `cmd` launches did not keep the dev server reachable long enough for browser automation.
- Real authenticated phone-width/browser QA remains required.

Next recommended task:

- When a stable dev server or Vercel deployment is available, test `/stock/inbound` at 390px while signed in and capture supplier-barcode plus no-supplier-barcode flows.

## 2026-06-29 - Stock inbound manual-label save guard

Task completed:

- Continued the Stock Inbound guided-flow goal with a small safety fix.
- Manual/no-supplier-barcode mode no longer shows the generic `Save inbound` button.
- Manual/no-supplier-barcode mode now blocks accidental form submission unless the generated printed label was scanned first.
- Barcode rule setup now has a direct `Use No supplier barcode` fallback that sends the worker to Manual Weight mode.
- The fallback tells the worker to enter weight, print/attach the label, then scan the label.
- Kept the existing barcode inbound action and internal-label stock save path; no schema/RLS change.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA should confirm manual mode saves only after scanning the generated printed label.
- Real printer/PDF QA is still needed for the 50mm x 30mm label flow.

Next recommended task:

- Browser-test `/stock/inbound` manual mode at 390px: enter weight, generate label, print/PDF, scan generated label, confirm stock saves and focus returns to weight.

## 2026-06-29 - Stock inbound manufacturer-product search display

Task completed:

- Continued the Stock Inbound guided-flow goal with a narrow search/display update.
- Inbound product search now accepts generated manufacturer + product names when a manufacturer is selected.
- Quick inbound product buttons now show the generated display name, for example `Tican Belly Boneless`, instead of only the raw product name.
- Inbound product dropdown options now show manufacturer + product where a manufacturer is selected.
- Kept the existing item/product and brand/manufacturer fields separate; no database or RLS change.
- Removed a tiny `useMemo` around product filtering because React Compiler could not preserve it with the manufacturer dependency; the simple inline filter is smaller and passed lint.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed after removing the product-filter memo.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm the search result labels fit at phone width and that custom manufacturer text updates the displayed product names.
- Dedicated product/manufacturer tables were not added; this still uses existing `items` + `brands`.

Next recommended task:

- Browser-test `/stock/inbound` setup/search and the full scan/manual-label session flow at 390px.

## 2026-06-29 - Stock inbound previous-scan undo and summary back

Task completed:

- Continued the Stock Inbound guided-flow goal with a small worker-control refinement.
- Added an explicit `Undo Previous Scan` action directly on the previous-scan card in barcode scan and manual-weight modes.
- The new previous-scan undo reuses the existing `undoInboundScanAction`, so stock units are voided with audit/movement history instead of deleted.
- Added a summary back action: `Back to Scanner` for supplier-barcode sessions and `Back to Manual Weight` for no-supplier-barcode/internal-label sessions.
- Summary back navigation is view-safe after finish; existing save/undo controls remain disabled once `sessionFinishedAt` is set.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm the previous-scan undo form works after a saved scan and that summary back navigation cannot save more scans after finish.
- Device QA is still needed for camera scanner, external scanner Enter input, and label print/scan confirmation.

Next recommended task:

- Run browser QA on `/stock/inbound` at phone width for supplier barcode and no-supplier-barcode sessions.

## 2026-06-29 - Stock inbound display-name and session-code polish

Task completed:

- Continued the Stock Inbound guided-flow goal with a display-only polish pass.
- Made the inbound batch/session code read-only in the rule/details area while keeping it submitted as `batchNo`.
- Added a shared stock data formatter for manufacturer + product display names.
- Stock unit list rows now show manufacturer + product as the main item name while keeping the separate Brand column.
- Stock unit detail now uses manufacturer + product for label/detail title display.
- Stock age alerts, transfer pending alerts, stock inbound-age report rows, stock-take lines, damage requests, and return-supplier requests now use manufacturer + product display names where an existing brand/manufacturer id is available.
- Kept existing `itemId` and `brandId` data fields, filters, and report grouping intent intact; no schema or RLS changes.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/data.ts`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Existing stock movement rows do not always carry `brand_id`, so movement history remains product/category based unless a future migration adds brand/manufacturer linkage there.
- Product/manufacturer still map to existing `items` + `brands`; dedicated product/manufacturer storage was not added in this pass.
- Real browser QA should confirm `/stock/inbound`, `/stock/units/[id]`, `/stock/dashboard`, and `/stock/reports` display names look right at phone width.

Next recommended task:

- Add browser/phone QA evidence for the guided inbound flow, especially external scanner Enter behavior and label print/scan confirmation.

## 2026-06-29 - Stock inbound no-supplier-barcode guided mode

Task completed:

- Continued the Stock Inbound guided-flow goal without changing schema, RLS, or loose no-barcode stock behavior.
- Added an explicit inbound setup mode choice: `Supplier barcode` or `No supplier barcode`.
- `No supplier barcode` now uses a guided `1 Setup -> 2 Manual Weight -> 3 Summary` flow inside `/stock/inbound`.
- Manual Weight keeps the selected product/manufacturer/origin/location locked, lets the worker enter one weight, generate a 50mm x 30mm internal label, print/attach it, then scan the printed label to save normal barcode stock.
- After a printed internal label is scanned and saved, the weight field clears and focuses again so the worker can enter the next unit.
- Manual mode shows previous saved product/weight, saved count, saved total weight, pending label confirmation, recent scans, current-session undo, and session summary.
- Existing `/stock/no-barcode-inbound` remains a guidance page; new loose no-barcode balances are still disabled.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone/browser QA is still needed for camera scan, handheld scanner Enter behavior, Bluetooth/PDF label printing, and 390px layout.
- Product/manufacturer still use existing item + brand storage. Dedicated product/manufacturer columns/tables were not added in this slice.
- Whole-session undo remains client-orchestrated over existing per-scan void RPC.

Next recommended task:

- Browser-test `/stock/inbound` at 390px for both `Supplier barcode` and `No supplier barcode`, then decide whether a persisted inbound session table/RPC is needed for server-side whole-session undo.

## 2026-06-29 - Stock issue handling hardening

Task completed:

- Refined stock issue handling so workers still see short blocked-scan messages while managers get structured issue records in `barcode_scan_logs`.
- Kept the existing issue-review table/model; no new issue table or migration was added.
- Expanded automatic duplicate issue classification to cover “already scanned”, “already recorded”, and “already returned” messages.
- Open damage/spoilage or return-supplier requests now create an `UNAVAILABLE_STOCK` scan issue before blocking further transfer/return/outbound-style use of that barcode.
- Stock-take duplicate scans now log `DUPLICATE_BARCODE` with selected item, expected location, and session context.
- Stock-take sessions without an item scope now log a structured `WRONG_ITEM` issue before blocking scanning.
- Smoke coverage now checks that issue creation remains wired for inbound, outbound, stock take, transfer, and return flows.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\smoke-routes.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase QA should still confirm RLS allows worker-created issue rows and manager review updates for each stock location scope.
- Manager review still records approve/reject/correct status only; stock quantity or location changes continue to happen through existing approval/correction workflows.
- Manual QA should scan one blocked case from each flow: inbound duplicate, outbound unavailable/wrong location, transfer unexpected or wrong destination, return blocked status/open request, and stock-take duplicate/wrong item/unknown barcode.

Next recommended task:

- Continue the Stock mobile MVP by finishing browser/phone QA for issue logging and the guided inbound/no-supplier-barcode flow.

## 2026-06-29 - Stock inbound guided page-flow slice

Task completed:

- Continued the Stock Inbound guided-flow goal without changing database schema or RLS.
- Added a guided inbound step bar for `1 Setup`, `2 Barcode Rule`, `3 Scan`, and `4 Summary`.
- Page 1 now shows the uneditable inbound session code, recent selections, product setup, manufacturer/brand setup, origin, and location.
- Page 2 reuses the existing barcode/rule fields as the barcode-rule page and adds a read-only extracted weight preview.
- Page 3 keeps continuous camera/external scanner input, duplicate blocking, previous scan, saved count/weight, generated internal label flow, and current-session undo.
- Page 4 summary now shows product, manufacturer, generated display name, total count, total weight, origin, location, scanner, batch, start/end time, print summary action, and safe whole-session undo.
- Whole-session undo uses the existing `void_inbound_stock_unit` RPC through `undoInboundScanAction` for each saved scan; stock units/movements are voided, not deleted.
- Inbound labels and session cards now use manufacturer + product display naming through the existing item + brand data model.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This is still source/build verified; real browser/phone QA is needed for the four-step navigation, camera popup, external scanner input, and whole-session undo.
- The product/manufacturer storage model still uses existing `items.name` and `brands.name`; no dedicated manufacturer table/column was added.
- Inbound without barcode is still routed through the existing internal-label flow inside Barcode Inbound, not a separate three-page no-barcode session.
- Whole-session undo is client-orchestrated over the existing per-scan void RPC. A persisted inbound-session table/RPC would be cleaner later if audit/reporting needs one-click server-side session reversal.

Next recommended task:

- Finish the no-barcode/manual-weight guided flow, or add a small persisted inbound-session table/RPC if one-click server-side session delete must survive browser refresh.

## 2026-06-29 - Stock scan issue auto-log refinement

Task completed:

- Kept the existing `barcode_scan_logs` issue-review model instead of adding a second issue table.
- Added a generic `logStockScanIssueAction` so client-side scan blocks can create manager-review issue rows without showing workers long forms.
- Inbound issue logging now includes selected item, expected location, batch/session context, and explicit issue types for duplicate barcode, barcode length mismatch, and barcode rule/no-weight failure.
- Outbound Without Order now auto-logs manager-review issues for duplicate session barcode, barcode not found, wrong worker location, unavailable stock, and same-destination transfer blocks.
- Transfer Out now auto-logs duplicate, barcode not found, unavailable stock, and wrong source-location scan blocks before the worker can continue.
- Return Stock now auto-logs barcode-not-found and unavailable/blocked return scans, and blocks saving invalid returns after the issue is created.
- Manager scan issue review now describes all issue sources and shows expected/scanned location context when available.
- Existing stock take, transfer receive, transfer missing-item, stock take mismatch, and damage/spoilage issue paths remain intact.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/types.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Migration notes:

- This refinement depends on existing migration `supabase/migrations/202606250007_stock_scan_issue_context_v1.sql`.
- Run migrations in filename order through `202606250007_stock_scan_issue_context_v1.sql` before expecting structured issue fields/review status to persist in Supabase.
- No live Supabase migration was run by Codex.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase QA must confirm issue rows are created under RLS for worker accounts in each scoped stock location.
- Manager review currently marks issues `APPROVED`, `REJECTED`, or `CORRECTED`; stock quantity/status changes still happen only through existing approval/correction workflows.
- Browser/device QA should scan one issue from each flow: inbound duplicate, inbound length mismatch, outbound missing barcode, outbound wrong location, transfer wrong source, return blocked status, stock take unknown barcode, transfer receive unexpected barcode, and damage/spoilage request.

Next recommended task:

- Apply migration `202606250007_stock_scan_issue_context_v1.sql` in Supabase if not already applied, then test `/stock/dashboard` manager scan issue review with worker-created issues from inbound, outbound, transfer, return, and stock take.

## 2026-06-25 - Stock scan issue auto-create and manager review

Task completed:

- Reused `barcode_scan_logs` as the stock issue record instead of adding a second issue table.
- Added structured issue context for duplicate barcode, barcode length mismatch, barcode not found, wrong location, wrong item, unavailable stock, barcode rule failure, unknown stock-take barcode, stock-take mismatch, transfer missing item, transfer unexpected item, and spoiled/damaged review.
- Shared scan logging now infers `issue_type` from short worker messages and accepts extra item/location/status/session context where the flow knows it.
- Manager scan issue queue now shows issue type, expected/scanned status, and buttons to approve, reject, or mark corrected.
- Stock take mismatch only logs an issue at submit; database adjustment still waits for the existing manager review and director approval path.
- Transfer receive logs a missing-transfer issue when another barcode remains pending for the receiving location after a receive scan.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/types.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `supabase/migrations/202606250007_stock_scan_issue_context_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250007_stock_scan_issue_context_v1.sql`
  - Adds nullable issue context columns to `barcode_scan_logs`.
  - Adds `review_status`, reviewer fields, and constraints/index for issue review.

SQL run order:

- Run all existing migrations in timestamp order.
- Latest new migration: `202606250007_stock_scan_issue_context_v1.sql`.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-migration-safety.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - first run failed because `.next/types` files were stale/missing.
- `npm.cmd run build` - passed and regenerated `.next/types`.
- `npm.cmd run typecheck` - passed after build.

Risks / remaining checks:

- Live Supabase must run migration `202606250007_stock_scan_issue_context_v1.sql` before structured issue fields and manager review status persist.
- Manager `Corrected` marks the issue reviewed only; actual stock changes still belong in existing stock take/correction/approval workflows.
- The first insert tries structured issue columns and falls back to plain scan logs if the migration is not applied yet.

Next recommended task:

- Apply migration `202606250007_stock_scan_issue_context_v1.sql`, then test `/stock/dashboard` manager issue queue with one inbound duplicate, one outbound unavailable scan, one receive-transfer unexpected scan, one return blocked scan, and one stock-take mismatch.

## 2026-06-25 - Return Stock worker condition flow

Task completed:

- Return Stock now follows the worker flow: default location is selected, worker scans a barcode, checks item/weight/status/previous movement, chooses a simple condition, then saves.
- Return condition buttons are now `Good`, `Need Check`, and `Spoiled / Damaged`.
- Server action sends the selected condition to the stock return RPC.
- New return RPC maps `Good` to `IN_STOCK`, `Need Check` to `HOLD`, and `Spoiled / Damaged` to `DAMAGED`.
- Invalid returns still log failed barcode scan records for manager review before the RPC blocks the action.
- Smoke coverage was updated for good return, hold return, spoiled/damaged return, invalid return blocking, and the five-argument return RPC.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `supabase/migrations/202606250006_stock_return_condition_flow_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250006_stock_return_condition_flow_v1.sql`
  - Adds a forward-only five-argument `return_stock_unit` RPC with `p_return_condition`.
  - Leaves the older four-argument historical RPC in place.

SQL run order:

- Run all existing migrations in timestamp order.
- Latest new migration: `202606250006_stock_return_condition_flow_v1.sql`.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - first run failed because `.next/types` files were stale/missing; reran after build and passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after build regenerated `.next/types`.

Risks / remaining checks:

- Live Supabase must run migration `202606250006_stock_return_condition_flow_v1.sql` before the new condition mapping works against the database.
- The UI uses existing barcode scan failure logs as the manager-review issue trail for invalid returns; no new manager-issue table was added.
- Manual QA should test one barcode for each condition plus an invalid barcode/status on `/stock/return`.

Next recommended task:

- Apply migration `202606250006_stock_return_condition_flow_v1.sql`, then test `/stock/return` on phone width with Good, Need Check, Spoiled / Damaged, missing barcode, and blocked-status barcode cases.

## 2026-06-25 - Stock transfer and receive worker flow refinement

Task completed:

- Transfer Out now keeps source implicit from the worker assigned stock location and only asks for destination.
- Transfer Out pre-validates scanned barcodes for missing barcode, duplicate page-session scan, non-available status, wrong source location, and same destination.
- Transfer confirmation still uses the existing atomic `transfer_stock_unit` RPC and now enforces `IN_STOCK` only before transfer.
- Receive Transfer now shows pending transfers for the worker receiving location only.
- Worker must tap a pending transfer card before scanning; the selected expected barcode is sent to the server.
- Receive Transfer rejects/logs unexpected barcodes through the server action and shows missing pending barcodes as manager-review follow-up.
- New transfer RPC migration makes received stock return to `IN_STOCK` instead of staying `TRANSFERRED`.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250005_stock_transfer_worker_flow_v1.sql`
  - Replaces `transfer_stock_unit` so only `IN_STOCK` barcode units can be transferred out.
  - Replaces `receive_stock_transfer` so received transfer stock becomes `IN_STOCK` at the destination.

SQL run order:

- Run all existing migrations in timestamp order.
- Latest new migration: `202606250005_stock_transfer_worker_flow_v1.sql`.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed after updating stale receive-location coverage.
- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts\stock-workflow-regression.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA must run the new migration before verifying receive-transfer status as `IN_STOCK`.
- Current transfer model is one barcode unit per pending transfer card; no separate multi-line transfer header was added.
- Missing receive items remain visible as pending transfer cards; unexpected scans are logged through barcode scan logs for manager review.

Next recommended task:

- Run migration `202606250005_stock_transfer_worker_flow_v1.sql`, then test `/stock/transfer` and `/stock/receive-transfer` with two stock locations and one worker-scoped account.

## 2026-06-25 - Stock take item-first counting refinement

Task completed:

- Stock Take now presents item selection first while still defaulting the location from the logged-in user profile.
- Stock Take now uses the loaded stock units to show expected stock for the selected item/brand/location.
- Worker scan summary now shows counted quantity, counted weight, extra scanned count, missing expected count, wrong item count, and wrong location count.
- Wrong item/brand scans remain server-blocked; unknown barcodes and wrong-location barcodes remain exception records for review.
- The worker submit action now reads `Finish Count`.
- Existing manager review and director approval flow remains intact; stock adjustment is still applied only through the approved stock-take RPC.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-take-lock-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase QA should verify expected-count accuracy with real scoped stock units and RLS-filtered worker data.
- Manual QA should scan a matched barcode, missing expected barcode case, unknown barcode, wrong item barcode, and wrong-location barcode, then confirm manager review and director approval apply the adjustment.

Next recommended task:

- Test `/stock/stock-take` on a 390px phone viewport with a worker account, manager account, and director account.

## 2026-06-25 - Stock outbound without order refinement

Task completed:

- Stock Outbound is now explicitly direct-only; order item checklist, substitution warnings, and order-picking UI were removed from the Stock Outbound component.
- Sales outbound keeps customer selection first, then scans barcodes through the shared camera/external-scanner `BarcodeField`.
- Direct scan add now blocks missing barcode, duplicate session barcode, unavailable stock status, same transfer destination, and wrong default worker location before adding to the outbound list.
- Page still shows selected customer, previous outbound scan item/weight, scanned count, and total outbound weight; confirm still uses the existing direct outbound server action that updates stock and movements.
- Shared outbound availability now only allows `IN_STOCK` for outbound confirmation.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/outbound-rules.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-workflow-regression.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts\stock-workflow-regression.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase/RLS QA should confirm allowed-location behavior for a normal worker with real scoped units.
- Manual phone QA should scan one valid barcode, wrong-location barcode, unavailable barcode, duplicate barcode, and external scanner Enter input.

Next recommended task:

- Test `/stock/outbound` on a phone-width viewport with a worker account and a real barcode scanner.

## 2026-06-25 - No-barcode inbound label scan confirmation

Task completed:

- Barcode Inbound no-barcode/internal-label flow now locks the selected item, brand, origin, and location while a generated label is pending.
- `Generate internal label` now creates a numeric internal barcode and 50mm x 30mm label preview, but stock is not saved until the worker prints/attaches the label and scans that printed barcode back.
- After label confirmation saves successfully, the form clears the barcode/weight and returns to the weight field for the next unit under the same session.
- Previous saved item/weight, saved count, saved total weight, duplicate blocking, current-session undo, label print/PDF fallback, and server-side inbound validation remain in place.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera and Bluetooth printer QA are still required.
- Live Supabase QA should confirm the confirmed label scan writes the stock unit, inbound movement, scan log, and audit row.

Next recommended task:

- Test `/stock/inbound` on a 390px phone viewport with a generated label, printed/PDF fallback, and scan-back confirmation.

## 2026-06-25 - Inbound barcode rule sample metadata

Task completed:

- Barcode Inbound now sends barcode length and sample barcode when saving a learned item + brand + origin weight-position rule.
- Added forward-only migration `202606250004_stock_barcode_rule_sample_v1.sql` for nullable `barcode_length` and `sample_barcode` on `barcode_weight_rules`, plus the updated inbound RPC.
- Future scans still use the saved start position, digit length, and decimal rule; different barcode length warnings now prefer the saved rule length.
- Existing duplicate-barcode blocking, camera scanner, external keyboard scanner Enter handling, inbound stock unit/movement/scan-log writes, RLS intent, and approval flows were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-workflow-regression.mjs`
- `supabase/migrations/202606250004_stock_barcode_rule_sample_v1.sql`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606250004_stock_barcode_rule_sample_v1.sql`

Commands run and results:

- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts\stock-workflow-regression.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-migration-safety.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Migration `202606250004_stock_barcode_rule_sample_v1.sql` must be applied before the new RPC parameters work against Supabase.
- Manual live QA still needs a real supplier barcode, same-format barcode, different-length barcode, duplicate barcode, camera scan, and external keyboard scanner.

Next recommended task:

- Continue Inbound mobile QA by testing the learned-rule flow against a staged Supabase database.

## 2026-06-25 - Refined Order UX final QA

Task completed:

- Completed the refined intuitive Order UX pass through Follow-up Tasks 1-8.
- Audited the existing Order screens against the new flow before changing code.
- Kept create order as a 4-step flow with no required final total price at creation.
- Added/confirmed item brand/manufacturer preference wording and stock-linked brand availability checks.
- Confirmed item-master customization chips are required on create and visible through create, picking, order/ready views, and delivery progress.
- Added delivery upcoming-order item/customization summaries so delivery workers can see customer requirements before Pending Delivery.
- Verified picking remains item-first with scan/manual actions, progress bars, duplicate/mismatch warnings, manual reasons, and tolerance blocking.
- Separated internal transfer orders from the Ready for Pickup section after final price.
- Verified delivery progress visibility before Pending Delivery and disabled driver accept until the canonical available delivery exists.
- Updated smoke guards for the refined Order/Delivery UX and two stale Stock guard strings that no longer matched current Stock UI output.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `components/orders/orders-page.tsx`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/delivery-module-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `node scripts\delivery-module-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Browser/mobile screenshot QA was not run in this environment; this pass used code inspection and smoke/coverage guards.
- Manual controlled UAT should still check phone-width `/orders`, `/orders/create`, `/orders/picking`, `/orders/ready`, `/orders/customers`, and `/delivery`.
- Verify with seeded users that workers see task/card views, admin/manager can access advanced sections, no-orders users remain blocked, and no finance/payment/outstanding/cost/profit fields are visible in worker/driver screens.

Readiness recommendation:

- Ready for controlled UAT after one manual browser/mobile visual pass.

## 2026-06-25 - Order delivery progress visibility verification

Task completed:

- Verified delivery workers can see delivery-required customer orders before they become Pending Delivery through `getUpcomingOrderDeliveries`.
- Verified upcoming delivery orders load statuses `NEW`, `PREPARING`, and `READY`, with customer, delivery address, customer remarks, picked/estimated weight, progress percent, and item customization summaries.
- Verified the upcoming delivery card shows `Not ready to accept yet` and does not expose an accept/start action before Pending Delivery.
- Verified driver acceptance remains on canonical available deliveries after the order reaches `READY_FOR_DELIVERY`.
- Verified the driver mobile page does not contain payment, outstanding, cost, or profit strings.
- No code or business logic changes were required for this verification pass.

Files changed in this pass:

- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\delivery-module-coverage.mjs` - passed.
- Full `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run build`, and `npm.cmd run smoke` had already passed on the current code state immediately before this docs-only verification.

Risks / remaining checks:

- Manual delivery mobile QA should confirm the upcoming delivery card is readable with multiple customized items.

Next recommended task:

- Continue with Follow-up Task 8: final role-based visual QA and smoke-guard confirmation.

## 2026-06-25 - Order Price Required internal-transfer separation

Task completed:

- Verified completed picking moves orders to `READY` with final price still required.
- Verified `FinalOrderPriceForm` remains a single total-price field with no item-level price, payment status, or outstanding amount.
- Kept pickup final-price behavior as `Ready for Pickup` and delivery final-price behavior as `Pending Delivery`.
- Fixed `/orders/ready` so internal transfer orders no longer also appear in the `Ready for Pickup` section after final price.
- Clarified final-price helper copy: internal transfers move to the `Internal Transfer` section.
- Backend status logic was not changed; internal transfer continues to use the existing non-delivery ready status while the UI groups it separately.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm internal transfer ready cards now appear only in the Internal Transfer section after final price is saved.

Next recommended task:

- Continue with Follow-up Task 7: verify delivery progress visibility before Pending Delivery and driver accept gating.

## 2026-06-25 - Order picking brand/manufacturer wording

Task completed:

- Verified `/orders/picking` is already item-first: workers choose/open an order, select one item, then use barcode scan or manual weight.
- Verified selected item progress and total order progress bars already update from ordered vs picked weight.
- Verified manual weight still requires the predefined reason list, duplicate barcode remains blocked, wrong item/brand scans record mismatch, and Complete Picking is blocked until picked weights are within tolerance.
- Updated the picking item card label from `Brand` to `Brand / manufacturer`.
- Updated no-preference picking helper copy to `No preference - scan the brand/manufacturer you pick`.
- No picking server action behavior, reservation behavior, edit lock behavior, or ready/price workflow was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - initially failed on unrelated stale Stock coverage strings, then passed after test-only guard updates.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed after updating stale guard strings to match the current Stock UI.
- `node scripts\stock-report-coverage.mjs` - passed after updating stale guard strings to match the current Stock UI.

Risks / remaining checks:

- Manual picking QA should confirm workers understand brand/manufacturer no-preference picking on phone width.
- The Stock guard updates were test-only and matched existing Stock component output; no Stock UI or business behavior was changed.

Next recommended task:

- Continue with Follow-up Task 6: verify the Price Required queue and post-picking final total price flow.

## 2026-06-25 - Stock worker home and assigned location defaults

Task completed:

- Worker Stock Home now uses the requested task labels: Inbound, Outbound Without Order, Transfer Out, Receive Transfer, Return Stock, Stock Take, and Item / Barcode Setup when allowed.
- Barcode Inbound now shows the assigned/current stock location first and tucks location editing under `Change Location`.
- Existing route permissions, manager/admin reports, review queues, setup pages, stock movement logic, RLS intent, and schema were not changed.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-role-scope-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-role-scope-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual worker QA should confirm live Supabase account scope shows the assigned stock location and no other outlet/location data.

Next recommended task:

- Continue Stock worker defaults by checking other scan pages for location-first UI.

## 2026-06-25 - Order customization delivery visibility

Task completed:

- Confirmed create-order customization chips are item-master driven and required per configured group/default.
- Confirmed order detail and picking already show selected customization chips.
- Added selected customization visibility to the Delivery upcoming-order/progress card so drivers can see item requirements before the order becomes pending delivery.
- Added read-only delivery upcoming item summaries with item label and customization chips.
- No schema, order creation logic, picking logic, price logic, or driver action permissions were changed.

Files changed in this pass:

- `lib/delivery/types.ts`
- `lib/delivery/queries.ts`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `node scripts\delivery-module-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual delivery mobile QA should confirm the added customization chips do not make the upcoming-order card too long on phone width.

Next recommended task:

- Continue with Follow-up Task 5: refine and verify item-first picking progress, wrong-item warning, and brand preference behavior.

## 2026-06-25 - Order item brand/manufacturer wording

Task completed:

- Confirmed order item brand preference is already backed by stock/brand data through `brandOptions`.
- Confirmed selected unavailable brand is blocked before create with a clear warning, while `No brand preference` lets the picker decide later.
- Updated the create-order item label from brand-only wording to `Brand / manufacturer preference`.
- Updated the no-preference helper text to say the picker chooses the brand/manufacturer during picking.
- No order schema, reservation logic, RLS, or picking logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm workers understand that `No brand preference` means the picker decides during picking.

Next recommended task:

- Continue with Follow-up Task 4: verify item-master customization chips and make any small UI guard refinements needed.

## 2026-06-25 - Order create no-price flow wording

Task completed:

- Kept `/orders/create` as a 4-step guided flow with no required final total price during creation.
- Confirmed the create form posts `totalOrderPrice` as `0`; final price remains a post-picking task.
- Updated the create success message to `Order created. Pick items next.`
- Renamed the optional staff note on the create review step to `Special price remark (optional)` while reusing the existing remarks field.
- Updated smoke/coverage guards for the new success copy and optional special-price remark wording.
- Backend order creation, reservation, RLS, picking, delivery handoff, pickup, cancellation, and final-price logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt was blocked by an orphaned Next build lock; after stopping only the stale `npm run build` / `next build --webpack` processes, rerun passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual create-order QA should confirm the optional special-price remark wording is understandable to workers.
- No database field was added for a separate special-price remark; it uses the existing order remarks storage.

Next recommended task:

- Continue with Follow-up Task 3: verify and refine item-line brand/manufacturer preference UX.

## 2026-06-25 - Retail launcher and report labels

Task completed:

- Retail worker Home now shows `Today Status` and keeps Record Processing first.
- Manager Home now shows Daily Sales, Cash Closing, Expense Review, Cleaning Setup, Outlet Report, and Picking Order.
- Admin/director Home and global Retail nav now show All Outlet Report, Cash Variance, Missing Tasks, Processing Yield, Expenses, and Export Center.
- Added `/retail/reports/expenses` as a table-first admin/director Expenses report using existing filtered Retail report data.
- Processing preset buttons now use Minced Meat, Slice, Cut, Pack, Repack, and Other.
- Manager Outlet Report checklist now orders pending work as expenses, cleaning, then cash closing.
- Existing outlet isolation, RLS, upload security, calculations, export security, and audit logic were not changed.

Files changed in this pass:

- `app/(erp)/retail/reports/expenses/page.tsx`
- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Retail checks on unrelated Order guard: `Order form UX guard missing: Order created. Next step:`.

Risks / remaining checks:

- Full smoke must be rerun after the unrelated Order guard is fixed or aligned.
- Manual phone QA should confirm the renamed Home buttons fit at 390px and that `/retail/reports/expenses` is accessible only to admin/director users.

Next recommended task:

- Fix the unrelated Order smoke guard, then rerun the full smoke gate before final Retail UAT sign-off.

## 2026-06-25 - Direct Outbound type-to-scan focus

Task completed:

- Stock Outbound now focuses barcode entry after a worker selects a direct outbound type that is already scan-ready.
- Processing and Sample/Testing go straight to scanning; Sales, Transfer, Damage/Spoilage, and Return Supplier still wait for their required setup.
- Sales now focuses customer search when no customer is selected, so the next required setup field is active.
- Existing outbound validation, stock writes, approval workflows, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm scan-ready direct outbound types focus barcode input around 390px width, and Sales focuses customer search until a customer is selected.

Next recommended task:

- Continue Stock worker speed by trimming one more scan/setup path with extra taps.

## 2026-06-25 - Order UX new-flow audit

Task completed:

- Audited `/orders`, `/orders/create`, `/orders/picking`, `/orders/ready`, `/orders/customers`, and the Delivery order-progress view against the new intuitive Order UX request.
- No business logic, database, RLS, or UI behavior was changed in this pass.

Audit findings:

- `/orders` is already mostly aligned with the requested task home: big action buttons, task cards, `Price Required`, `Ready for Pickup`, `Pending Delivery`, stock/process alerts, and advanced list/report access are gated away from the default worker view.
- `/orders/create` already uses a 4-step guided flow without required final price: `Customer / Transfer`, `Order Type`, `Items`, and `Create Order`. It sends `totalOrderPrice` as `0`, keeps quick customer create, supports brand preference, and requires item-master customization selections/defaults. Remaining UX gaps are label-level: no explicit `Special Price Remark` label, and the success copy is close but not exactly `Order created. Pick items next.`
- `/orders/picking` is already item-first with order cards, selected item cards, scan/manual actions, order and item progress bars, required manual reasons, duplicate warning flow, and brand-preference checks. Remaining UX gap: wrong-item scan currently records a mismatch and guides the user to scan the correct item or use manual weight; it does not present a literal "proceed anyway" confirmation.
- `/orders/ready` already has a `Price Required` section with final total price entry before `Ready for Pickup` / `Pending Delivery`, plus card sections for pickup, delivery, and transfer-ready work.
- `/orders/customers` is already search-first with simple customer cards, name/phone display, add customer action, and advanced details separated from the worker default view.
- Delivery progress visibility is already present through upcoming order deliveries: delivery users can see created delivery orders before acceptance with customer, delivery time, progress, picking status, remarks, and picked/estimated weight, while accept remains unavailable until the order becomes `Pending Delivery`.
- No finance/payment/outstanding/cost/profit fields were found in the worker Order create, picking, ready, customer, or delivery progress screens. Customer credit warning is gated to advanced order users and does not show outstanding amount.

Exact files/components to change for the next tasks:

- `components/orders/orders-forms.tsx`: `CreateOrderForm`, create success state, item brand/customization cards, `PickingForms`, mismatch guidance, and `FinalOrderPriceForm`.
- `components/orders/orders-page.tsx`: `OrdersTaskHome`, `ReadyOrdersBoard`, ready/order cards, customer cards, and role-gated advanced sections.
- `components/orders/orders-table-client.tsx`: advanced-only order list labels if table wording needs to follow the new status language.
- `lib/orders/actions.ts`: only if backend state transitions need adjustment, especially final-price transition and wrong-item proceed behavior.
- `lib/orders/data.ts` and `lib/orders/types.ts`: only if extra order-line or delivery-progress fields are needed by UI.
- `components/delivery/driver-mobile-delivery-page.tsx` and `lib/delivery/queries.ts`: delivery-progress wording or visibility refinements.
- `scripts/order-ux-simplification-coverage.mjs`, `scripts/delivery-module-coverage.mjs`, and `scripts/smoke-routes.mjs`: update guards after any UI/flow changes.

Data model notes:

- No required data model change was identified for the audited flow. Existing order-line `preferred_brand_id` and `customization` support brand preference and item-master customization.
- Existing `total_order_price = 0` during creation plus `setCustomerOrderFinalPriceAction` supports post-picking final price entry.
- A new column is only needed if product requires a separate persisted `Special Price Remark` field instead of reusing existing staff/order remarks.
- A schema/log change is only needed if wrong-item scan must persist an explicit "proceed anyway" operator decision beyond the current mismatch log.

UI-only changes possible:

- Rename or add the create-flow remark label to `Special Price Remark` while using existing remark storage.
- Update create success copy to exactly `Order created. Pick items next.`
- Tighten wrong-item scan copy or add an explicit confirmation UI before continuing, if accepted as UI-only guidance.
- Tune labels in ready/delivery progress sections without changing permissions or state transitions.

Backend changes required:

- Price-after-picking is mostly implemented already: create does not require a final price, completed picking moves to `Price Required`, and final price moves pickup/delivery forward.
- Review `setCustomerOrderFinalPriceAction` before Task 6 implementation: it currently branches by `delivery_required`, so internal transfer may follow the pickup-ready branch unless existing transfer-ready behavior is explicitly acceptable.
- If the new wrong-item requirement means "allow picking the wrong item after confirmation", backend picking logic must change; current behavior records mismatch only, which matches the older Order V1 rule.

Commands run and results:

- Not run; this was a documentation-only audit with no runtime or code behavior changes.

Risks / remaining checks:

- Manual visual QA should still verify phone-width create, picking, ready, customer, and delivery progress screens after the next UI edits.
- Product should confirm whether `Special Price Remark` can reuse existing staff/order remarks and whether wrong-item scans should remain mismatch-only or allow an explicit override.

Next recommended task:

- Proceed with Follow-up Task 2 as a small UI pass: remove/rename any remaining final-price creation wording, add optional special-price remark wording if required, and adjust create success copy.

## 2026-06-25 - Receive Transfer scan focus

Task completed:

- Receive Transfer now focuses barcode entry after selecting a receiving location.
- Tapping a pending transfer barcode now fills the barcode and focuses the field so workers can receive without an extra tap.
- Existing wrong-location blocking, receive-transfer server validation, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm receiving-location selection and pending-barcode taps focus the barcode field around 390px width.

Next recommended task:

- Continue Stock worker speed by checking another scan-heavy setup page with long lists.

## 2026-06-25 - Order create Step 1 transfer label

Task completed:

- Updated `/orders/create` Step 1 label from `Customer` to `Customer / Transfer`.
- Updated the Step 1 heading to `Step 1 Customer / Transfer` so internal-transfer orders do not look like they are stuck on a customer-only step.
- Existing customer selection, internal-transfer skip-customer behavior, validation, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual phone QA should confirm internal-transfer creation feels natural from Step 1.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Retail worker fixed phone actions

Task completed:

- Retail worker submit buttons now stay fixed at the bottom on phone width and return to normal placement on larger screens.
- Complete Cleaning uses the same fixed phone action pattern.
- Retail worker forms now leave bottom padding so fixed action buttons do not cover form messages or fields.
- Existing Retail RLS, upload security, calculations, export, audit, and workflow permissions were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating the stale cleaning smoke guard to the fixed phone action.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual 390px/real-phone QA should confirm the fixed bottom action does not clash with the device browser bar on Submit Expense, Complete Cleaning, Record Processing, Daily Sales, and Cash Closing.

Next recommended task:

- Continue the Retail objective by checking one remaining worker or manager page against the 38 workflow refinements.

## 2026-06-25 - Order delivery type progress wording

Task completed:

- Updated `/orders/create` Delivery order-type helper from `Ready order goes to Delivery` to `Delivery sees progress, accepts later`.
- This matches the requested flow: Delivery team can see delivery orders after creation with progress, but cannot accept until Pending Delivery.
- Existing create-order flow, delivery visibility logic, final-price behavior, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Order smoke checks due unrelated Cleaning guard: `Cleaning worker UX must keep tap-to-complete flow, optional photo copy, success next step, and empty state`.

Risks / remaining checks:

- Manual create-order QA should confirm the Delivery button wording makes the progress-first handoff clear.
- Full smoke should be rerun after the Cleaning workstream fixes its worker UX guard.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Transfer/receive location search

Task completed:

- Stock Transfer and Receive Transfer now show `Search stock location` before quick destination/receiving buttons.
- Search filters location buttons and the fallback dropdown by stock location or outlet name.
- Existing wrong-location blocking, transfer receive logic, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts\stock-workflow-regression.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first two attempts were blocked by a stale generated Next build diagnostic marker; after clearing only `.next\diagnostics\build-diagnostics.json`, build passed. Latest rerun passed without cleanup.
- Initial `npm.cmd run smoke` - failed before Stock checks on an unrelated Cleaning guard. Latest rerun passed after the Cleaning workstream updated its guard.

Risks / remaining checks:

- Manual mobile QA should confirm searching by outlet/location filters the Transfer and Receive Transfer quick buttons around 390px width and wrong-location receive remains blocked.

Next recommended task:

- Continue Stock worker speed by checking another scan-heavy setup page with long lists.

## 2026-06-25 - Order demo remarks finance wording cleanup

Task completed:

- Removed `Credit warning only.` from Order demo customer remarks and demo order customer remarks.
- Replaced it with the operational note `Call receiver before pickup.` so worker-visible demo remarks do not expose finance/credit wording.
- Kept overdue-credit demo flags and manager/admin credit alerts intact.
- Existing Order UI, data loading, RLS, reports, and backend logic were not changed.

Files changed in this pass:

- `lib/orders/data.ts`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first attempt hit the existing Next build lock; retry passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual demo-mode QA should confirm worker-visible customer remarks stay operational and do not mention credit/finance.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Barcode Inbound brand/origin selection sync

Task completed:

- Barcode Inbound brand/origin dropdown choices now update the matching search box, matching quick-button behavior.
- Selecting Other/custom clears the search box and keeps the custom entry field.
- Existing barcode-rule matching, setup locking, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm dropdown and quick-button brand/origin choices keep the search box readable and still focus scanning once setup is complete.

Next recommended task:

- Continue Stock worker speed by checking another single scan-heavy workflow.

## 2026-06-25 - Order header ready nav label alignment

Task completed:

- Updated the shared Order page header nav button from `Ready` to `Price / Ready`.
- This keeps the `/orders/ready` navigation aligned with the Price Required queue and final-price-after-picking flow.
- Existing header routes, Order pages, final-price behavior, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual phone QA should confirm the header nav makes the final-price/ready task clear on all Order pages.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Retail processing no-draft server guard

Task completed:

- Retail Processing now rejects direct-post draft status server-side.
- Processing records always save as submitted immediately with submitted-by/submitted-at metadata.
- Existing processing calculations, worker form, reports, outlet isolation, RLS, storage, export, and audit behavior were not changed.

Files changed in this pass:

- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed on retry after the first attempt reported an existing Next build lock.

Risks / remaining checks:

- Manual worker phone QA should confirm Record Processing still saves normally from the form.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Barcode Inbound brand/origin search

Task completed:

- Barcode Inbound now has `Search brand` and `Search origin` before quick brand/origin buttons.
- Search filters the quick buttons only, keeping existing dropdowns and Other/custom entry available.
- Existing barcode-rule matching, setup locking, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm filtered quick brand/origin buttons still lead into barcode scanning once setup is complete.
- Full smoke may still fail before Stock checks because unrelated Order/Cleaning guards are changing in this shared worktree.

Next recommended task:

- Continue Stock worker speed by checking another single scan-heavy workflow.

## 2026-06-25 - Order ready page price label alignment

Task completed:

- Renamed the `/orders/ready` page header from `Ready Orders` to `Price / Ready`.
- Updated the picking success next-step copy and button from `Ready Orders` to `Price / Ready`.
- Updated both Order UX and base smoke guards so the stale ready-only wording does not return.
- Existing routes, final-price behavior, pickup/delivery handoff, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed with existing unrelated Retail warnings in `lib/retail/actions.ts`.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual phone QA should confirm the `/orders/ready` page title and picking success next step make the final-price task obvious.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Retail disabled POS actions hardening

Task completed:

- Old Retail POS sale, payment, register cash-session, and item-level price server actions now return disabled V1 messages.
- Removed dead POS/register helper logic from Retail actions so those actions no longer write to old POS/register/price tables.
- Existing Daily Sales, Cash Closing, expenses, cleaning, processing, reports, outlet isolation, RLS, storage, export, and audit behavior were not changed.

Files changed in this pass:

- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed after moving the cash-expense formula guard to the active Cash Closing action.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed after marking disabled action parameters as intentionally unused.
- `npm.cmd run typecheck` - passed on retry after build refreshed generated Next types.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual QA should continue using Daily Sales and Cash Closing; old POS/payment/register routes remain redirects only.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Stock Take item search

Task completed:

- Stock Take session creation now has `Search item or code` before the quick item buttons.
- Quick item buttons filter by item code, category, section, or product name.
- Existing stock-take session creation, scope rules, barcode-only counting, approval flow, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-take-lock-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed with existing unrelated Retail warnings.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Order assertion: `Order form UX guard missing: Go to Ready Orders`.

Risks / remaining checks:

- Manual mobile QA should confirm item search filters quick item buttons before starting a stock-take session.
- Full smoke is still expected to fail before Stock checks until the unrelated Cleaning guard is fixed.

Next recommended task:

- Continue Stock worker speed by checking another single scan-heavy workflow.

## 2026-06-25 - Order advanced ready label alignment

Task completed:

- Updated the advanced Order status filter label for `READY` from `Ready` to `Price / Ready`.
- Updated the advanced order card shortcut to `/orders/ready` from `Ready` to `Price / Ready`.
- This keeps manager/admin advanced views aligned with the worker Price Required flow while preserving existing filter values, routes, queries, RLS, and backend logic.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-table-client.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual manager QA should confirm the advanced filter wording is clearer for orders waiting on final price.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Stock Direct Sales QA wording correction

Task completed:

- Corrected the owner Stock QA checklist so Direct Sales says scanning is blocked until a customer is selected.
- Added a doc coverage guard so the stale `customer selection is optional before scanning` wording does not return.
- App behavior, server validation, RLS, stock movements, and schema were not changed.

Files changed in this pass:

- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.

Risks / remaining checks:

- Manual QA should confirm `/stock/outbound` Sales still blocks scanning until a customer is selected.
- Full smoke is still expected to fail before Stock checks until the unrelated Cleaning guard is fixed.

Next recommended task:

- Continue Stock worker speed by checking another single scan-heavy workflow.

## 2026-06-25 - Order home price-ready shortcut wording

Task completed:

- Updated the `/orders` worker shortcut from `Ready / Pickup` to `Price / Ready`.
- Updated the shortcut detail to `Final price, pickup, delivery` so workers see the Price Required step before pickup/delivery.
- Existing Orders Home routes, task queues, final-price action, delivery handoff, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Order smoke checks due unrelated Cleaning guard: `Cleaning worker UX must keep tap-to-complete flow, optional photo copy, success next step, and empty state`.

Risks / remaining checks:

- Manual phone QA should confirm workers notice the final-price queue from the Orders Home shortcut.
- Full smoke should be rerun after the Cleaning workstream fixes its worker UX guard.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Stock Outbound Direct Sales customer search safety

Task completed:

- Stock Outbound Direct Sales now shows the selected customer name in the customer search box.
- If the worker edits customer search after selecting a customer, the selected customer is cleared and scanning stays blocked until a customer is chosen again.
- Existing direct Sales customer-required validation, barcode scanning, stock movement actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Cleaning assertion: `Cleaning worker UX must keep tap-to-complete flow, optional photo copy, success next step, and empty state`.

Risks / remaining checks:

- Manual mobile QA should confirm customer search edit clears the selected customer and blocks scanning until a new customer is chosen.
- Real Supabase/RLS QA is still needed for scoped customer visibility.

Next recommended task:

- Continue Stock worker speed by checking another single scan-heavy workflow.

## 2026-06-25 - Retail cleaning today-only wording

Task completed:

- Worker Complete Cleaning now receives only cleaning tasks required today.
- Worker cleaning guidance now uses red `Missing` status wording instead of late/history wording.
- Existing cleaning completion action, manager cleaning setup, cleaning reports, outlet isolation, RLS, storage, export, and audit behavior were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed after updating the stale cleaning wording guard.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating the shared stale cleaning wording guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed on retry after build refreshed generated Next types.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual worker phone QA should confirm Complete Cleaning shows only today-required tasks and red Missing cards are easy to understand.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Order create fast-path transfer wording

Task completed:

- Updated `/orders/create` worker fast-path Step 1 from `Search customer` to `Customer or transfer`.
- This keeps the pickup/delivery path obvious while making the internal-transfer skip-customer path less surprising.
- Existing create-order validation, customer search, transfer flow, final-price-after-picking behavior, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual phone QA should confirm workers understand Step 1 covers both customer orders and internal transfers.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Barcode Inbound template location guard

Task completed:

- Barcode Inbound recent templates now focus the barcode field only when product, brand, origin, and location are ready.
- If location is missing, workers see `Choose location, then scan.` instead of landing in a disabled scanner field.
- Existing template loading, assigned-location default, barcode-rule learning, duplicate blocking, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm recent-template scanning still starts immediately when the assigned/default location is present.
- Real phone/external-scanner QA is still needed.

Next recommended task:

- Continue Stock worker speed by checking another single scan-heavy workflow.

## 2026-06-25 - Order create brand-stock review wording

Task completed:

- Updated `/orders/create` final review wording so `Stock check` now says `Brand checked before save`.
- This matches the existing selected-brand behavior: no-stock brand choices are blocked before order save, while final total price is still entered only after picking.
- Existing create-order validation, reservation behavior, final-price-after-picking flow, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual phone QA should confirm workers understand that selected-brand stock is checked before saving, and final price still happens after picking.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Retail worker history route gate

Task completed:

- Worker direct access to dense Cleaning History and Processing Records pages is now manager-gated.
- Workers stay on focused Retail paths: Complete Cleaning, Record Processing, Today Summary, and submitted-expense edit before review.
- Existing outlet isolation, Retail reports, expense edit, processing calculations, RLS, storage, export, and audit behavior were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed on retry after build refreshed generated Next types.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual worker direct-URL QA should confirm `/retail/cleaning/history` and `/retail/processing/history` show the manager-access block.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Barcode Inbound setup-to-scan focus

Task completed:

- Barcode Inbound now uses one setup selector path for product, brand, origin, and location.
- Once all required setup values are complete, the barcode field is focused so workers can scan immediately after the last setup tap.
- Existing inbound session locking, default location, barcode-rule learning, duplicate blocking, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile/external-scanner QA should confirm the barcode field is ready after the final product/brand/origin/location setup choice.
- Custom brand/origin typing still requires the worker to finish typing before scan setup is complete.

Next recommended task:

- Continue Stock worker speed by checking saved-template repeat scanning or another single scan-heavy workflow.

## 2026-06-25 - Order create internal-transfer skip clarity

Task completed:

- Made `/orders/create` Step 1 clearer for internal transfer orders by saying staff can skip customer selection for internal transfer.
- Renamed the Step 1 shortcut from `Internal transfer - no customer` to `Internal transfer - skip customer`.
- Simplified the worker fast path labels from `Add quick items / Create Order / Price after picking` to `Add items / Save order / Price later`.
- Existing create-order validation, final-price-after-picking flow, reservation behavior, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed after Order checks passed; unrelated Stock mobile coverage failure: `Stock worker mobile workflow forms missing: function selectInboundLocation`.

Risks / remaining checks:

- Manual phone QA should confirm internal-transfer creation is understandable from Step 1 without training.
- Full smoke remains blocked by the existing Stock mobile coverage mismatch until the Stock workstream repairs `selectInboundLocation`.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Retail processing remarks optional disclosure

Task completed:

- Record Processing header remarks now sit behind `Processing remarks (optional)`.
- The main phone path stays focused on processing type, raw material, finished product, and wastage.
- Existing remarks field name, submit action, calculations, RLS, storage, audit, and reports were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed on retry after a transient unrelated Order UX guard failure.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed on retry after build refreshed generated Next types.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual worker phone QA should confirm Record Processing Step 1 feels shorter and remarks are still available when needed.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Order brand preference server guard

Task completed:

- Added an Order UX regression guard proving server order creation checks selected-brand stock before save.
- The guard also proves no-brand orders keep brand unassigned until picking, while picking still blocks wrong-brand barcode scans for brand-specific lines.
- Existing create-order UI, reservation behavior, picking behavior, RLS, and backend logic were not changed.

Files changed in this pass:

- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm selected no-stock brands block order creation with clear wording, while no brand preference lets picking choose the brand later.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Barcode Inbound location-to-scan focus

Task completed:

- Barcode Inbound now focuses the barcode field after workers choose an inbound location.
- This helps the normal setup path flow straight into scanning after product, brand, origin, and location are ready.
- Existing inbound session locking, default location, barcode-rule learning, duplicate blocking, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile/external-scanner QA should confirm the barcode field is ready after choosing location.
- Custom brand/origin typing flow is unchanged.

Next recommended task:

- Continue Stock worker speed by checking inbound brand/origin quick selection or saved-template repeat scanning.

## 2026-06-25 - Stock Item Master create-to-inbound shortcut

Task completed:

- Stock Item Master now shows a success-only `Next: open inbound and scan stock.` card after creating an item.
- The card links to `/stock/inbound` so workers can continue into barcode stock capture.
- Existing item creation, generated item code, optional fields, role access, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the success card appears only after item creation and the link opens Barcode Inbound.
- Brand/origin setup is still done in inbound, unchanged.

Next recommended task:

- Continue Stock worker speed by checking inbound saved-template reuse or Item Master edit recovery wording.

## 2026-06-25 - Order Home advanced-alert visibility guard

Task completed:

- Added an Order UX regression guard proving `/orders` worker task home does not render dashboard alerts.
- The same guard confirms dashboard alerts stay inside the manager/admin/director `Advanced Order List` section.
- This protects the rule that worker views should not show customer credit warnings or finance-style alert clutter.
- Existing Orders home UI, advanced list UI, dashboard data, RLS, and backend logic were not changed.

Files changed in this pass:

- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed after correcting the guard slice.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm worker accounts see task queues only on `/orders`, while manager/admin accounts can open Advanced Order List alerts.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Retail Submit Expense receipt label cleanup

Task completed:

- Submit Expense receipt upload label now says `Receipt`.
- Selected filename display, required receipt validation, accepted file types, upload handling, storage isolation, RLS, and expense action behavior were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating the Retail expense smoke guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual worker phone QA should confirm Receipt upload remains obvious and required.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Order Customers finance-info guard

Task completed:

- Added an Order UX regression guard proving `/orders/customers` worker cards do not include customer category or credit-term details.
- The same guard confirms category and credit-term details stay inside the manager/admin/director `Advanced Customer Details` section.
- Existing customer search, quick add, create-order preselect, advanced details UI, RLS, and backend logic were not changed.

Files changed in this pass:

- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially hit local Next build lock, passed on retry.
- `npm.cmd run smoke` - Order and delivery/retail checks passed, then failed in unrelated Stock acceptance coverage: `Acceptance 6 transfer and receive missing: Choose another destination or remove it.`

Risks / remaining checks:

- Full smoke is currently blocked by unrelated Stock coverage in this dirty worktree.
- Manual QA should confirm worker accounts see only customer name, phone, and Create Order on `/orders/customers`.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Stock Outbound blocked-scan recovery wording

Task completed:

- Stock Outbound missing, blocked, and wrong-destination scan messages now tell workers to remove the bad scan from the scanned list.
- The messages then point workers to scan the correct/next barcode or choose another destination.
- Existing outbound validation, status blocking, destination checks, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed after updating the old wording guard.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed on unrelated Retail guard: `Retail expense UX must stay simple: amount, category, cash default, required receipt, and home return`.

Risks / remaining checks:

- Manual mobile QA should trigger missing, blocked, and wrong-destination scans and confirm recovery wording is clear.
- This only improves UI copy; manager issue review and server-side blocking are unchanged.
- Full smoke needs a Retail follow-up outside this Stock task; Stock-specific guards passed.

Next recommended task:

- Continue Stock worker speed by checking Item Master quick edit flow or inbound saved-rule repeat scanning.

## 2026-06-25 - Retail manager checklist icon status cards

Task completed:

- Manager Outlet Report `Today checklist` cards now show simple icons for Cash Closing, Cleaning, Expenses, and Daily Sales.
- Existing pending-first sorting, Draft Daily Sales pending status, outlet report scope, export, RLS, and report calculations were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual manager phone QA should confirm checklist cards remain readable at narrow width.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Order Create item-brand readiness label

Task completed:

- `/orders/create` blocked-create checklist now says `Items and brand ready` instead of only `Items ready`.
- This makes the no-stock brand blocker clearer when staff choose a brand with no available stock.
- Existing brand stock check, item validation, order creation, final-price-after-picking flow, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the blocked-create checklist is easy to understand when a selected brand has no stock.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Stock Outbound continuous scan focus

Task completed:

- Stock Outbound now returns focus to the barcode field after each accepted barcode is added to the batch.
- This supports manual fallback and external barcode scanners without another tap.
- Existing scan validation, duplicate blocking, missing/blocked barcode handling, outbound save actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile/external-scanner QA should confirm focus stays in the barcode field after accepted scans.
- Blocked scans still leave the worker on the same page with the existing red/amber recovery messages.

Next recommended task:

- Continue Stock worker speed by checking direct outbound blocked-scan recovery wording or Item Master quick edit flow.

## 2026-06-25 - Order Picking wrong-scan next choice copy

Task completed:

- `/orders/picking` now tells workers that wrong item scans show a warning with the next choice.
- The scan card now points staff to either scan the correct barcode or use Manual Weight after a wrong item scan.
- Existing mismatch logging, duplicate barcode blocking, barcode picking, manual weight reasons, ready tolerance, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the wrong-scan guidance is readable and visible near the scan action.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking Order worker screens for dense ERP-style copy or hidden finance/credit leakage.

## 2026-06-25 - Barcode Inbound rule scope copy

Task completed:

- Barcode Inbound save-rule setup now tells workers the rule is saved for the current product, brand, and origin.
- The same note says rule changes affect future scans only.
- Existing barcode-rule inference, save checkbox behavior, inbound save RPC, duplicate blocking, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the note is visible and readable inside `Set barcode rule once`.
- Real supplier-barcode QA still needs to prove first-scan learning works with actual formats.

Next recommended task:

- Continue Stock worker speed by checking blocked-scan recovery wording or direct outbound customer selection.

## 2026-06-25 - Retail Today Summary icon status cards

Task completed:

- Worker Today Summary cards now show simple icons for Today sales, Cash closing, Cash expenses, Cleaning missing, and Processing records.
- Existing today filtering, worker report hiding, cash closing status, cleaning missing red state, RLS, and reports were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed on retry after a transient local Next build lock.

Risks / remaining checks:

- Manual worker phone QA should confirm Today Summary cards remain readable at narrow width.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Order Create credit-warning guard

Task completed:

- Added an Order UX regression guard proving `/orders/create` overdue-credit warning remains manager/admin/director-only.
- This supports the active information-hiding rule that workers should not see customer credit warnings.
- Existing create-order UI, customer selection, quick add, final-price-after-picking flow, RLS, and backend logic were not changed.

Files changed in this pass:

- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially hit local Next build lock, passed on retry.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should still confirm worker accounts do not see credit-warning copy in `/orders/create`.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking Order worker views for finance/credit leakage or dense ERP-style sections.

## 2026-06-25 - Stock Outbound confirm button batch summary

Task completed:

- Stock Outbound final confirm button now shows scanned count and known kg after barcodes are added.
- This gives workers one last direct batch check before submitting.
- Existing outbound validation, customer/destination/photo/supplier prerequisites, scan blocking, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the confirm button remains readable at 390px with several scanned units.
- Unknown/missing barcode cases still require removing blocked scans before confirm, unchanged.

Next recommended task:

- Continue Stock worker speed by checking direct outbound customer-selection-to-scan or blocked-scan recovery wording.

## 2026-06-25 - Retail Cash Closing optional remarks wording

Task completed:

- Cash Closing remarks label now says `Remarks optional`.
- This matches the V1 rule that variance is warning-only and does not require remarks before saving.
- Existing field name, submit action, variance calculation, RLS, audit, and reports were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual manager phone QA should confirm Cash Closing variance still reads as warning-only.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Order Create processing checkbox wording

Task completed:

- `/orders/create` item card checkbox now says `Needs processing` instead of `Processing required`.
- This keeps the worker-facing create flow task-like while preserving the existing `processingRequired` field, item defaults, order-line payload, picking flow, RLS, and backend logic.
- Order UX and smoke guards now expect the new wording.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm staff read `Needs processing` correctly in the item card.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking `/orders/create` item card density or the `/orders/ready` Price Required queue.

## 2026-06-25 - Stock label phone print-sheet guidance

Task completed:

- Stock label print/reprint guidance now says both label buttons open the phone print sheet.
- This keeps the MVP path clear: choose the Bluetooth label printer from the print sheet, or use PDF fallback there.
- Existing label generation, Code 128 rendering, 50mm x 30mm print CSS, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Real Bluetooth printer selection and PDF output still need phone/device QA.
- This does not add a direct Web Bluetooth printer integration; browser print remains the MVP path.

Next recommended task:

- Continue Stock worker speed by checking direct outbound scan confirmation or customer-selection-to-scan focus.

## 2026-06-25 - Retail post-success submit cleanup

Task completed:

- Retail workflow forms now hide the original submit button after a successful save when next-action buttons are shown.
- Staff now choose the explicit next action, such as Back to Retail Home, Go to Cash Closing, Go to Outlet Report, or record another item, instead of accidentally resubmitting the same record.
- Forms without success actions are unchanged.
- Existing form fields, server actions, RLS, storage, audit, exports, and calculations were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed on retry after a transient unrelated Order guard failure.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed on retry after a transient local Next build lock.

Risks / remaining checks:

- Manual phone QA should submit an expense/processing/cash closing and confirm only next-action buttons remain after success.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Barcode Inbound finish-summary next step

Task completed:

- Barcode Inbound session summary now tells workers the immediate next action after finishing: print labels, attach them to product, then move stock.
- Existing session totals, correction warning, label print actions, undo lockout after finish, stock writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should finish one inbound session and confirm the next-step card is visible and readable at 390px.
- Real label printing still needs phone/Bluetooth/PDF fallback testing.

Next recommended task:

- Continue Stock worker speed by checking generated-label print fallback clarity or direct outbound scan confirmation.

## 2026-06-25 - Order Create item customer-request wording

Task completed:

- `/orders/create` item free-text field now says `Customer request` instead of `Item remarks`.
- The field aria label now says `Customer request for item`.
- Existing order line remarks storage, customer remarks, staff remarks, item search, brand/customization flow, order creation, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm staff understand line-level customer requests go in this field.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking `/orders/create` item card density or `/orders/ready` pickup/delivery card actions.

## 2026-06-25 - Retail Today Summary missing-cleaning red status

Task completed:

- Worker Today Summary now turns the Cleaning missing card red when required cleaning is still incomplete.
- The card now labels the issue as `Missing tasks today`; completed days show `All required tasks completed`.
- Existing Today Summary data filtering, cleaning calculations, role scope, reports, RLS, and worker actions were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual worker phone QA should confirm missing cleaning is obvious on `/retail/today`.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Barcode Inbound first-scan learning guidance

Task completed:

- Barcode Inbound now shows a first-scan learning card when item + brand + origin are selected but no saved barcode rule exists.
- The worker instructions are short: scan one supplier barcode, enter actual kg once, then save so next scans auto-fill weight.
- Existing barcode decode, duplicate blocking, weight-rule saving, stock-unit/movement/scan-log writes, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first two attempts saw another Next build process running; passed after retry.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the no-rule first-scan card is visible and readable at 390px before scanning.
- Real supplier-barcode testing is still needed to confirm workers can learn a rule once, then continue scanning without extra typing.

Next recommended task:

- Continue Stock worker speed by checking Inbound session finish/summary clarity or the generated-label print fallback path.

## 2026-06-25 - Order Picking tolerance action wording

Task completed:

- `/orders/picking` tolerance warnings now match the worker action label `Complete Picking`.
- Changed `weight before marking Ready` to `weight before completing picking`.
- Changed `Ready will be blocked` to `Complete Picking is blocked`.
- Existing tolerance calculation, mark-ready action, barcode/manual picking, mismatch logging, duplicate warning, RLS, and backend picking logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially blocked by another local Next build lock, then passed after waiting.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm out-of-tolerance messaging is clear before tapping `Complete Picking`.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking `/orders/create` item card density or `/orders/ready` pickup/delivery card actions.

## 2026-06-25 - Retail Submit Expense field order

Task completed:

- Retail Submit Expense now follows the worker entry order: Amount, Category, Payment method, then Receipt.
- Assigned outlet context still shows first without asking outlet workers to select an outlet.
- Existing field names, receipt requirement, Cash default, edit-before-review path, server action, RLS, storage, audit, and export behavior were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual phone QA should confirm Submit Expense feels faster with Amount first.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Stock item master product-name focus

Task completed:

- Stock Item Master now focuses Product name after a successful item create so workers can enter the next product faster.
- Stock Item Master now focuses edit Product name after a worker selects an item from quick item buttons or the full item list.
- Existing item-code generation, duplicate item-code validation, item master role access, optional fields, advanced settings, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed after waiting.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/items`, create one item, and confirm Product name is ready for the next item without another tap.
- Manual mobile QA should select an item to edit and confirm edit Product name is ready without another tap.

Next recommended task:

- Continue Stock worker speed by checking Barcode Rule setup wording and first-scan learning guidance.

## 2026-06-25 - Retail numeric phone keyboard hints

Task completed:

- Retail numeric inputs now request the phone decimal keyboard.
- This covers Daily Sales, Cash Closing, expense amount/edit amount, processing raw/finished weight and quantity, wastage weight, and optional BOM yield/wastage fields.
- Existing Retail calculations, validation, server actions, RLS, storage, audit, and export behavior were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual phone QA should confirm Retail amount/weight fields open a decimal-friendly keyboard.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue only with confirmed Retail mobile friction or wording cleanup.

## 2026-06-25 - Order Create customization chip touch targets

Task completed:

- `/orders/create` customization option chips now use 44px-tall mobile touch targets.
- Chip text can wrap and stays left-aligned so longer customization labels remain readable on phone width.
- Existing customization validation, default customization behavior, item search, brand preference, no-stock brand warning, order creation, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially blocked by another local Next build lock, then passed after waiting.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm customization chips remain readable with long labels and are easy to tap.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking `/orders/picking` ready/tolerance messaging or `/orders/create` item card density.

## 2026-06-25 - Stock take session-to-scan focus

Task completed:

- Stock Take now focuses the barcode field after a worker selects an active stock-take session.
- After a successful stock-take scan, the barcode value is cleared and the barcode field is focused for the next scan.
- Existing barcode-only counting, wrong item/brand blocking, unknown-barcode exception creation, manager/director approval flow, stock-take adjustment timing, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/stock-take`, select an active session, and confirm barcode entry is ready for scanner input.
- Manual mobile QA should scan one stock-take barcode and confirm the next scan can start without another tap.

Next recommended task:

- Continue Stock worker speed by checking Item Master quick-create/edit setup for remaining avoidable typing.

## 2026-06-25 - Order Picking manual weight mobile hints

Task completed:

- `/orders/picking` manual picked kg input now uses decimal keyboard mode and `Picked kg` placeholder.
- Manual picked quantity input now uses decimal keyboard mode and `Picked quantity` placeholder.
- Existing manual reason requirement, scan/manual quick actions, barcode picking, mismatch logging, duplicate warning, tolerance, Ready flow, RLS, and backend picking logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially blocked by another local Next build lock, then passed after waiting.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm manual picked kg/quantity fields open the expected decimal keyboard.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking `/orders/create` customization chip readability or `/orders/picking` ready/tolerance messaging.

## 2026-06-25 - Retail phone-friendly dropdown controls

Task completed:

- Shared Retail dropdown controls now use phone-friendly height and text size.
- Expense category/payment, processing item, outlet, review/status, and setup selects now match the taller worker input controls.
- Existing Retail form fields, server actions, RLS, storage, audit, exports, and calculations were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual phone QA should confirm Retail dropdowns are easier to tap without crowding worker pages.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue trimming confirmed Retail worker phone friction only.

## 2026-06-25 - Stock return supplier setup-to-scan focus

Task completed:

- Stock Outbound Direct Return Supplier now focuses the barcode field after a worker selects or finishes typing a supplier.
- The dedicated Stock Return Supplier workbench now focuses the barcode field after a worker selects or finishes typing a supplier.
- After a successful supplier-return request, the barcode field is focused for the next scan.
- Existing supplier requirement, hold-before-approval behavior, manager approval deduction, stock movement history, scan logging, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/outbound`, choose Return Supplier, select or type a supplier, and confirm barcode entry is ready for scanner input.
- Manual mobile QA should open `/stock/return`, select or type a supplier in the Return Supplier task, and confirm barcode entry is ready for scanner input.

Next recommended task:

- Continue Stock worker speed by checking Stock Take active-session selection and scan focus.

## 2026-06-25 - Retail processing line typing cleanup

Task completed:

- Retail worker Record Processing raw/finished line cards no longer show per-line Remarks fields.
- Worker processing line entry now stays to item, manual item name, weight, and quantity.
- Existing processing calculations, submit action, audit/storage behavior, and optional wastage details were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual worker phone QA should confirm raw/finished processing cards feel shorter and still allow manual item names, weight, and quantity.
- Limited UAT should still include real phone/browser worker and manager one-task page passes.

Next recommended task:

- Continue trimming only confirmed Retail worker typing or navigation friction.

## 2026-06-25 - Order Picking scan/manual quick actions

Task completed:

- Added two large quick-action buttons above `/orders/picking` scan/manual forms: `Scan Barcode` and `Manual Weight`.
- The buttons jump to the existing scan and manual weight forms so both picking paths are equally visible on mobile.
- Added stable anchors on the existing scan and manual cards.
- Existing barcode picking, manual reason requirement, mismatch logging, duplicate warning, tolerance, Ready flow, RLS, and backend picking logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially blocked by another local Next build lock, then passed after waiting.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the quick-action buttons jump to the right form and make scan/manual equally discoverable.
- Broader Order UX simplification remains active; continue auditing picking and create-order item cards on phone width.

Next recommended task:

- Continue checking `/orders/picking` manual weight inputs for mobile numeric hints and clear reason selection.

## 2026-06-25 - Stock damage photo-to-scan focus

Task completed:

- Stock Outbound Direct Damage/Spoilage now focuses the barcode field after a worker selects a damage photo.
- The dedicated Stock Damage/Spoilage workbench now focuses the barcode field after a worker selects a damage photo.
- Changing damage reason focuses barcode entry when a photo is already present.
- After a successful damage request, the barcode field is focused for the next scan.
- Existing photo requirement, damage approval flow, no-immediate-deduction rule, stock movement history, scan logging, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed after waiting.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/outbound`, choose Damage/Spoilage, select a photo, and confirm barcode entry is ready for scanner input.
- Manual mobile QA should open `/stock/return`, select a damage photo, and confirm barcode entry is ready for scanner input.

Next recommended task:

- Continue Stock worker speed by checking whether Return Supplier supplier selection can focus the barcode field after setup selection.

## 2026-06-25 - Retail admin/director nav uses report tasks

Task completed:

- Retail admin/director sub-navigation now shows the same focused report/export tasks as their Retail Home launcher.
- Admin/director nav now uses All Outlet Report, Cash Variance, Missing Tasks, Processing Report, and Export Center instead of outlet operation shortcuts.
- Worker and manager Retail nav behavior from the previous simplification pass was kept unchanged.
- Retail access checks, outlet isolation, exports, file storage rules, and backend role gates were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual admin/director mobile QA should tap each Retail nav report/export button and confirm it lands on the focused report page.
- Limited UAT should still include a real phone/browser pass for worker and manager one-task Retail pages.

Next recommended task:

- Run the full Retail validation set, then continue only if the checks reveal a real Retail regression.

## 2026-06-25 - Order Create item number-entry mobile hints

Task completed:

- `/orders/create` Step 3 item quantity and estimated kg fields now use decimal keyboard hints for mobile.
- Quantity input now shows `Quantity` placeholder.
- Estimated kg input now shows `Estimated kg` placeholder.
- Existing item search, recent items, brand preference, no-stock brand warning, customization chips, order creation, final-price-after-picking, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially blocked by another local Next build lock, then passed after waiting.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm quantity and estimated kg fields open the expected numeric/decimal keyboard.
- Broader Order UX simplification remains active; continue auditing create-order item cards and picking actions.

Next recommended task:

- Continue with `/orders/picking` scan/manual action balance or `/orders/create` customization chip readability on phone width.

## 2026-06-25 - Stock return setup-to-scan focus

Task completed:

- Stock Return now focuses the barcode field after a worker selects a return location.
- Stock Return now focuses the barcode field after a worker selects a return condition.
- After a successful return save, the barcode field is focused for the next scan.
- Existing return condition recording, return RPC, barcode status validation, stock movement history, scan logging, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/return`, choose a condition or change return location, and confirm the barcode field is ready for camera/manual/external scanner input.
- Manual mobile QA should save one return and confirm the next barcode can be scanned without another tap.

Next recommended task:

- Continue Stock worker speed by checking whether Damage/Spoilage photo selection can focus the barcode field after photo selection.

## 2026-06-25 - Order Ready final-price task wording

Task completed:

- Simplified the `/orders/ready` final-price form title from `Price required` to the action `Enter final price`.
- Added worker-facing helper copy: `One total for the whole order. No item prices.`
- Added mobile input hints for the final total field with decimal keyboard mode and `Final total only` placeholder.
- Changed Pending Delivery helper copy to `Delivery team can accept these orders in Delivery.`
- Existing final-price save action, Price Required queue, Ready for Pickup / Pending Delivery transition, delivery integration, RLS, and backend order logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially blocked by local Next build lock, then passed on retry.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the final-price field is easy to tap and staff understand it is one total for the whole order.
- Broader Order UX simplification remains active; continue auditing one Order workflow at a time.

Next recommended task:

- Continue checking `/orders/create` item/customization chips and `/orders/picking` scan/manual action balance on mobile.

## 2026-06-25 - Retail manager nav uses focused task pages

Task completed:

- Retail sub-navigation now sends non-worker users to focused task pages for expense review, cleaning setup, and processing records.
- Manager/admin nav label `Processing Records` now opens `/retail/processing/history` instead of the worker `/retail/processing` entry form.
- Worker nav still keeps Submit Expense, Complete Cleaning, and Record Processing task labels.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual manager mobile QA should tap the Retail nav `Processing Records`, `Review Expenses`, and `Cleaning Setup` buttons and confirm each opens the focused page.

Next recommended task:

- Continue checking Retail direct routes for old module-style labels that bypass the focused task launcher.

## 2026-06-25 - Stock transfer destination-to-scan focus

Task completed:

- Stock Outbound Direct Transfer now focuses the barcode field after a worker selects a destination stock location.
- Dedicated Stock Transfer now focuses the barcode field after a worker selects a destination stock location.
- Destination selection clears the previous local scan blocker message.
- Existing transfer destination requirement, same-destination blocking, transfer outbound movement, receive-transfer logic, server actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/outbound`, choose Transfer, select a destination, and confirm the barcode field is ready for camera/manual/external scanner input.
- Manual mobile QA should open `/stock/transfer`, select a destination, and confirm the barcode field is ready for camera/manual/external scanner input.

Next recommended task:

- Continue Stock worker speed by checking whether Return/Damage location and condition selection can focus the barcode field after setup selection.

## 2026-06-25 - Delivery preparing-order accept copy

Task completed:

- Simplified the Delivery mobile Preparing Orders card copy for customer-order deliveries that are visible before drivers can accept.
- Changed the locked action from `Waiting for Pending Delivery` to `Not ready to accept yet`.
- Changed the helper text so drivers see plain next-step wording while still understanding Accept appears only after picking/final price are done.
- Existing delivery visibility, Pending Delivery accept gating, picked-weight/progress display, customer remarks, delivery actions, RLS, and order business logic were not changed.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\delivery-module-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm delivery workers understand that preparing orders are visible but cannot be accepted yet.
- Broader Order UX simplification remains active; continue auditing `/orders/ready` and delivery tabs for worker-facing clutter.

Next recommended task:

- Continue checking `/orders/ready` card actions and final-price entry for mobile worker clarity.

## 2026-06-25 - Retail blocks processing review server-side

Task completed:

- Removed `retail_manager` from the processing review action role list.
- The shared processing review action now requires Processing module access instead of Retail module access.
- Retail processing stays report-only even if someone tries to post directly to the review action.

Files changed in this pass:

- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.
- `npm.cmd run typecheck` - first run hit stale missing `.next/types`; passed after build regenerated route types.

Risks / remaining checks:

- Manual Processing manager QA should still save one review from `/processing/dashboard`.
- Manual Retail manager QA should confirm Retail Processing Records remain view-only.

Next recommended task:

- Continue checking Retail server actions for permissions that still allow retired V1 UI behavior.

## 2026-06-25 - Order customer cards preselect create customer

Task completed:

- `/orders/customers` customer cards now open `/orders/create` with that customer preselected.
- `CreateOrderForm` accepts an `initialCustomerId` from the URL and initializes the selected customer, delivery address, and customer remarks from the existing customer record.
- Workers no longer need to search the same customer again after tapping `Create Order` from a customer card.
- Existing customer search, quick-add, order creation, final-price-after-picking, RLS, and backend order logic were not changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should tap a customer card `Create Order` action and confirm Step 1 shows the selected customer clearly.
- Broader Order UX simplification remains active; continue auditing one worker flow at a time.

Next recommended task:

- Continue checking `/orders/ready` and the Delivery available view for worker-facing clutter or finance/payment leakage.

## 2026-06-25 - Stock direct sales customer-to-scan focus

Task completed:

- Stock Outbound Direct Sales now focuses the barcode field after a worker selects a customer from quick customer buttons or the full customer list.
- Customer selection also clears the previous local scan blocker message.
- Existing direct Sales customer requirement, outbound scan blocking, batch confirmation, server actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/outbound`, choose Sales, tap a customer, and confirm the barcode field is ready for camera/manual/external scanner input.

Next recommended task:

- Continue Stock worker speed by checking whether Direct Outbound transfer destination selection can focus the barcode field after destination selection.

## 2026-06-25 - Retail removes stale processing review success link

Task completed:

- The shared processing review form no longer sends reviewers to `/retail/processing/review` after save.
- `Review another processing` now returns to `/processing/dashboard`, where that shared review form is actually rendered.
- Retail processing remains report-only; no Retail review page or Retail review branch was restored.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual Processing manager QA should save one review from `/processing/dashboard` and confirm the next action stays on the Processing dashboard.

Next recommended task:

- Continue checking Retail routes for old links that point to retired V1 pages.

## 2026-06-25 - Order header removes hand-off wording

Task completed:

- Changed the Order module header copy from `Create, pick, price, and hand off orders from one simple workflow.` to `Create, pick, price, and move orders through one simple workflow.`
- Changed the Orders setup-error fallback header from `Create, reserve, pick, ready, and hand off orders.` to `Create, pick, price, and move orders through the next task.`
- Added Order UX coverage to block worker-facing `hand off` wording from returning.
- Existing customer page, order creation, picking, pricing, delivery integration, RLS, and backend data flow were not changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the shared Order header reads naturally across `/orders`, `/orders/create`, `/orders/picking`, `/orders/ready`, and `/orders/customers`.
- Broader Order UX simplification remains active; continue checking one Order screen at a time.

Next recommended task:

- Continue auditing `/orders/customers` and `/orders/ready` for dense/admin-style controls visible to workers.

## 2026-06-25 - Stock inbound internal-label weight button

Task completed:

- Barcode Inbound now has a second `Generate internal label` button beside `Net weight kg`.
- Current no-barcode/internal-label inbound generates a pending label first, then saves only after the printed label is scanned back.
- Existing generated barcode format, duplicate checks, label printing, server actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/inbound`, select one item/brand/origin/location, enter a weight, tap the weight-field label button, and confirm the saved label appears.
- Generate a second label and confirm focus returns to `Net weight kg` for the next weight.

Next recommended task:

- Continue Stock worker speed by checking Direct Outbound customer selection and scan readiness for extra typing.

## 2026-06-25 - Retail Processing Review redirects to records

Task completed:

- Removed the dead Retail `processing-review` render branch from the Retail page.
- `/retail/processing/review` now redirects to `/retail/processing/history` so managers see Processing Records, not a separate review task.
- The shared non-Retail processing review form/action was not changed.

Files changed in this pass:

- `app/(erp)/retail/processing/review/page.tsx`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual manager QA should open `/retail/processing/review` and confirm it lands on Processing Records.

Next recommended task:

- Continue checking Retail focused pages for old review/history routes that duplicate the report pages.

## 2026-06-25 - Order dashboard alert wording removes handoff

Task completed:

- Removed remaining user-facing `handoff` wording from Order dashboard KPI and alert copy.
- Changed Ready KPI detail from `Pickup or delivery handoff` to `Pickup or delivery next`.
- Changed delivery-ready alert detail from `ready for delivery handoff` to `ready for delivery`.
- Added Order UX coverage so Order page/data copy does not reintroduce `handoff`.
- Existing delivery integration, status mapping, RLS, reports, picking, pickup, and pricing logic were not changed.

Files changed in this pass:

- `lib/orders/data.ts`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual dashboard QA should confirm the Ready KPI and Order ready alerts read naturally for workers.
- Broader Order UX simplification remains active and should continue screen by screen.

Next recommended task:

- Continue auditing `/orders/ready` and `/orders/customers` for dense or admin-style controls visible to workers.

## 2026-06-25 - Stock inbound template next-action wording

Task completed:

- Recent Barcode Inbound template buttons now say `Tap to scan` when a saved barcode rule exists.
- Templates without a saved rule now say `Tap to learn rule`.
- Selecting a saved-rule template now shows the shorter message `Ready. Scan next barcode.`
- Selecting a no-rule template now shows `Scan supplier barcode, then enter actual kg once.`
- Existing template loading, barcode-field focus, barcode decode, rule learning, duplicate blocking, server actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/inbound`, tap a saved-rule recent template, and confirm the next message says `Ready. Scan next barcode.`
- Tap a no-rule recent template and confirm it says `Scan supplier barcode, then enter actual kg once.`

Next recommended task:

- Continue Stock worker speed by checking Barcode Inbound no-barcode/internal-label repeated weight entry for any remaining extra taps.

## 2026-06-25 - Order Picking simpler selected-item wording

Task completed:

- Renamed `/orders/picking` hidden fallback selector from `Compact order selector` to `Simple order list`.
- Renamed the scan card helper from `Selected item to pick` to `Picking item`.
- Changed the empty selected-item hint from `Select item below first` to `Tap an item below first`.
- Existing barcode scan, manual weight, mismatch logging, duplicate warning, tolerance, mark-ready, RLS, and delivery/pickup logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the selected item card is obvious before scanning or manual weight entry.
- Broader Order UX simplification remains active; continue removing ERP-style labels and dense controls where workers see them.

Next recommended task:

- Continue with `/orders/create` or `/orders/ready` review for any remaining worker-facing ERP-style copy.

## 2026-06-25 - Retail Submit Expense shows assigned outlet

Task completed:

- Worker Submit Expense now displays the assigned outlet using the existing scope display instead of hiding outlet context completely.
- The worker still does not select an outlet; the form submits the assigned outlet through the existing hidden field.
- Existing expense submit/edit actions, receipt requirement, Cash default, file upload, RLS, storage isolation, audit, and review permissions were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.

Risks / remaining checks:

- Manual worker phone QA should open `/retail/expenses` and confirm the assigned outlet is visible but not selectable.

Next recommended task:

- Continue checking worker forms for hidden context that should be visible without becoming editable.

## 2026-06-25 - Order Create stock-check wording

Task completed:

- Simplified the `/orders/create` final review card from internal `Reservation` wording to worker-facing `Stock check`.
- Changed the helper copy from `Runs immediately after create` to `Runs after create`.
- Existing order creation, immediate confirmation, immediate stock reservation/check, RLS, picking, delivery handoff, and final-price-after-picking logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the final create-order review reads clearly for workers.
- Broader Order UX simplification remains active; continue checking remaining ERP-style wording and dense controls screen by screen.

Next recommended task:

- Continue with the next smallest Order UX cleanup, likely `/orders/picking` selected-item action wording or `/orders/create` final review wording density.

## 2026-06-25 - Stock inbound weight-field guidance

Task completed:

- Barcode Inbound now tells workers beside `Net weight kg` whether the value will auto-fill from a saved barcode rule after scanning, or whether they should enter actual kg once to teach a new supplier-barcode rule.
- Existing barcode decode, weight-rule save, internal-label generation, duplicate blocking, server actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed after waiting for it to finish.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/inbound`, choose an item + brand + origin with a saved barcode rule and confirm the weight helper says it auto-fills after scan.
- Choose an item + brand + origin without a saved rule and confirm the helper says to scan supplier barcode, then enter actual kg once to learn the rule.

Next recommended task:

- Continue Stock worker speed by checking whether the Barcode Inbound product/brand/origin setup can be further reduced after selecting a recent template.

## 2026-06-25 - Retail Daily Sales history redirects to reports

Task completed:

- `/retail/sales/history` now redirects to `/retail/reports`.
- Removed the separate `sales-history` branch from the Retail page shell so Daily Sales stays today-only and saved sales history lives in Retail Reports.
- Existing Daily Sales entry, worker draft, manager confirmation, report tables, RLS, audit, storage, and calculations were not changed.

Files changed in this pass:

- `app/(erp)/retail/sales/history/page.tsx`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual manager/admin QA should open `/retail/sales/history` and confirm it lands on Retail Reports.

Next recommended task:

- Continue removing old Retail history/task duplicates where a focused report page already owns the record view.

## 2026-06-25 - Order Picking item-list task wording

Task completed:

- Changed `/orders/picking` item-list card title from `Items` to `Pick items`.
- Changed item-list helper copy from a status/flag description to `Tap an item, then scan barcode or enter weight.`
- Updated Order UX coverage for the task-worded item list.
- Existing item selection, scan/manual weight actions, progress bars, stock badges, tolerance checks, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed after waiting for it to finish.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders/picking` and confirm the item list clearly tells workers to tap an item before scanning or entering weight.

Next recommended task:

- Continue the Order mobile-app pass by checking `/orders/create` final review cards for any remaining technical/reservation wording.

## 2026-06-25 - Stock item edit quick item buttons

Task completed:

- Stock Item Master edit now shows quick item buttons from the search result, so workers can tap an item instead of opening a long dropdown.
- The full edit-item dropdown now sits under `Full item list` and opens automatically only when search has no matches.
- Existing item update action, validation, RLS, item-code uniqueness, active flag behavior, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/items`, search an item, tap a quick item button, and confirm the edit fields load.
- Search a missing item and confirm `Full item list` opens as the fallback.

Next recommended task:

- Continue Stock worker speed by checking Barcode Inbound saved-rule sessions for any optional fields still visible before scan.

## 2026-06-25 - Order Picking finish action wording

Task completed:

- Changed `/orders/picking` final action card title from `Ready` to `Finish picking`.
- Changed helper copy from `Any picker can mark the order ready after all items are picked.` to `Complete picking after all items are picked.`
- Updated Order UX coverage for the task-worded finish-picking panel.
- Existing ready action, 10kg tolerance checks, picked-weight validation, barcode/manual picking, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders/picking` and confirm the finish-picking card reads as the next task, not just a status label.

Next recommended task:

- Continue the Order mobile-app pass by checking selected-item text and item-card density on `/orders/picking`.

## 2026-06-25 - Retail expense submit returns home first

Task completed:

- Submit Expense success actions now show `Back to Retail Home` before `Edit submitted expense`.
- The worker can still edit a submitted expense before manager review, but the normal post-submit path now matches the Retail workflow: return home and continue daily tasks.
- Existing expense submit/edit actions, receipt requirement, file upload, RLS, storage isolation, audit, and review permissions were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.

Risks / remaining checks:

- Manual worker phone QA should submit one expense and confirm the first success action is `Back to Retail Home`, with `Edit submitted expense` still available.

Next recommended task:

- Continue scanning Retail worker success states for places where the next normal task should be first.

## 2026-06-25 - Order Picking recent picks wording

Task completed:

- Changed `/orders/picking` collapsed history section from `Recent picking log` to `Recent picks`.
- Simplified the helper copy from `Scans, manual weights, duplicate attempts, and mismatches.` to `Saved scans and manual weights appear here.`
- Updated Order UX coverage for the simpler picking history wording.
- Existing picking actions, barcode scan, manual weight reason validation, mismatch logging, duplicate barcode warnings, progress bars, ready rules, RLS, and backend logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders/picking`, expand `Recent picks`, and confirm the section feels like worker history rather than a debug log.

Next recommended task:

- Continue the Order mobile-app pass by checking `/orders/picking` selected-item and ready-card labels for remaining training-heavy wording.

## 2026-06-25 - Stock item edit optional details tucked away

Task completed:

- Stock Item Master edit now shows item code, category, and product name first after a product is selected.
- Default brand, product section, alternate names, and low-stock kg now sit under `Optional item details`.
- Existing active/barcode-required advanced settings remain under `Advanced item settings`.
- Existing item update action, validation, RLS, item-code uniqueness, active flag behavior, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after `RetailProcessingReviewForm` export was restored for the separate Processing module.
- `npm.cmd run build` - passed with the same unrelated Processing/Retail import warning.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/items`, select an item, and confirm only item code/category/product name are visible before optional details are opened.

Next recommended task:

- Continue Stock worker speed by checking Barcode Inbound rule setup for any remaining optional fields that can stay hidden during saved-rule sessions.

## 2026-06-25 - Stock direct sales customer fallback tucked away

Task completed:

- Stock Outbound direct Sales keeps customer search and quick customer buttons first.
- The dense full customer dropdown now sits under `Full customer list` and opens automatically only when search has no matches.
- Existing direct Sales customer requirement, server validation, outbound action, RLS, movement history, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/outbound`, choose Sales, search a customer, and confirm scanning stays blocked until a customer is selected.
- Search a missing customer and confirm `Full customer list` opens as the fallback.

Next recommended task:

- Continue Stock worker speed by checking Item Master / Barcode Rule setup for remaining dense fallback controls.

## 2026-06-25 - Order Create recent-items wording

Task completed:

- Changed `/orders/create` item-step copy from `Quick item templates` to `Recent items`.
- Changed helper text from `Tap recent items to add a line...` to `Tap a recent item to add it...`.
- Changed the no-match item search message from `adding a line` to `adding an item`.
- Changed the fast-path helper from `recent item templates` to `recent items`.
- Updated Order UX and smoke coverage guards for the simpler create-step wording.
- Existing Order create flow, item selection, brand preference, customization, estimated kg, final-price-after-picking behavior, server actions, RLS, and stock logic were not changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders/create` step 3 and confirm recent-item buttons and item search read naturally for workers.

Next recommended task:

- Continue the Order mobile-app pass by checking `/orders/picking` for remaining form/debug wording in worker flow.

## 2026-06-25 - Retail Cash Closing hides status choice

Task completed:

- Removed the visible Draft/Submitted/Reviewed status dropdown from the Retail Cash Closing task page.
- Cash Closing now submits with hidden `SUBMITTED` status, keeping the manager page focused on one daily closing action.
- Existing Cash Closing calculation, audit, RLS, storage, export, and same-day edit behavior were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual manager phone QA should open `/retail/cash-closing`, confirm no status choice is shown, save a closing, and confirm it appears as submitted in history/reports.

Next recommended task:

- Continue the Retail worker/manager phone pass for any remaining crowded labels after the one-task page split.

## 2026-06-25 - Stock transfer hides source from destination picker

Task completed:

- Transfer Out no longer offers the worker's assigned source stock location as a destination option.
- The existing same-location transfer block remains in place as server/UI safety if a stale value is submitted.
- Receive Transfer still shows receiving locations normally.
- Existing transfer action, receive action, RLS, movement history, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed after waiting for the active build to finish.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/transfer`, confirm `Sending from assigned location` appears, and confirm that location is not available in the destination buttons/dropdown.

Next recommended task:

- Continue Stock worker speed by checking direct outbound customer selection against real 390px phone flow.

## 2026-06-25 - Order Customers simple heading

Task completed:

- Changed `/orders/customers` worker section heading from `Customer Cards` to `Customers`.
- Confirmed customer credit warning remains gated behind `advancedOrderUser`; general workers do not see it on customer cards.
- Updated Order UX coverage for the simpler customer-page wording.
- Updated Delivery coverage guard to match the newer Order linked-delivery copy (`Delivery created` / `Open it from the Delivery module list.`), because smoke still expected the removed `Delivery handoff` phrase.
- Existing Order backend logic, RLS, customer search, quick customer create, credit data loading, delivery linking, and finance hiding behavior were not changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `node scripts\delivery-module-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders/customers` as a worker and confirm it feels search-first, shows name/phone cards, and does not show credit/finance details.

Next recommended task:

- Continue the Order mobile-app pass by checking `/orders/create` item-card labels for remaining technical wording.

## 2026-06-25 - Retail expense submit edit path

Task completed:

- Added `Edit submitted expense` to the Submit Expense success actions, linking to `/retail/expenses/history`.
- This keeps the submit page simple while making the before-manager-review edit path discoverable after a worker submits a receipt.
- Existing Retail expense submit/edit actions, receipt requirement, RLS, outlet scope, file storage, audit, calculations, routes, and migrations were not changed.
- Updated Retail source and smoke coverage for the submit-success edit link.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempt was blocked by another active Next build; passed on retry.

Risks / remaining checks:

- Manual mobile UAT should submit an expense and confirm the success actions show both `Edit submitted expense` and `Back to Retail Home`.

Next recommended task:

- Continue checking Retail worker success states for places where the next safe action is hidden.

## 2026-06-25 - Stock receive-transfer idle warning removed

Task completed:

- Receive Transfer no longer shows a red wrong-location warning before a worker has entered a wrong-location pending-transfer barcode.
- The red message still appears with the required destination when a known pending-transfer barcode belongs to another receiving location.
- Existing server-side wrong-location blocking, scan-issue logging, receive action, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first two attempts saw another Next build process running; passed after waiting for the active build to finish.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/receive-transfer` and confirm the normal page is not showing a red warning before scanning.
- Then enter a pending-transfer barcode for another location and confirm the red destination-specific warning appears.

Next recommended task:

- Continue Stock worker speed by checking direct outbound customer selection and transfer destination selection against real 390px phone flow.

## 2026-06-25 - Order detail delivery wording

Task completed:

- Removed remaining worker-visible `handoff` wording from Order detail shortcuts, timeline copy, and linked-delivery cards.
- Changed manager delivery shortcut detail to `Delivery overview`.
- Changed Ready/Pickup shortcut detail to `Pickup and delivery tasks`.
- Changed order timeline delivery event title to `Delivery created` and timeline description to `delivery notes`.
- Changed linked delivery guidance to `Enter final price before Delivery can accept this order.` and `Open it from the Delivery module list.`
- Existing Order backend logic, RLS, final-price flow, delivery visibility, Delivery module handoff behavior, pickup completion, and status transitions were not changed.
- Updated Order UX and smoke guards to use the new wording and ensure the Order page source no longer contains `handoff`.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.
- `npm.cmd run smoke` - failed after the Order guard passed, on unrelated Retail guard: `Retail expense status page must let workers edit own submitted expenses before manager review` in `scripts/smoke-routes.mjs:1315`.

Risks / remaining checks:

- Manual mobile QA should open `/orders/[id]` for a delivery order and confirm delivery cards read like simple delivery tasks, not internal handoff steps.
- Full smoke should be rerun after the unrelated Retail expense status guard is resolved.

Next recommended task:

- Continue the Order mobile-app pass by checking `/orders/customers` for worker-visible advanced/customer credit wording.

## 2026-06-25 - Retail worker nav task labels

Task completed:

- Changed worker Retail sub-navigation labels from module nouns to task labels: `Submit Expense`, `Complete Cleaning`, and `Record Processing`.
- Routes, role gates, outlet scope, RLS, calculations, file storage, exports, audit, and migrations were not changed.
- Updated Retail source coverage and QA evidence so worker navigation stays task-worded.
- Fixed brittle Retail expense-history guard slicing in Retail coverage and smoke coverage; no app behavior changed for that guard fix.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed after fixing a brittle expense-history route slice in the guard.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - failed after Retail checks on unrelated Delivery/Order coverage: `Order page linked delivery display missing: Delivery handoff`.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual mobile UAT should confirm the horizontal Retail nav still fits and scrolls naturally around 390px width.

Next recommended task:

- Continue checking active Retail worker pages for leftover module/report wording that can be changed to task wording without behavior changes.

## 2026-06-25 - Stock receive-transfer wrong-location destination copy

Task completed:

- Receive Transfer now shows the exact destination location when a known pending-transfer barcode is scanned or typed at the wrong receiving location.
- The existing server-side wrong-location receive rejection and scan-issue logging were not changed.
- Existing transfer/receive RPCs, RLS, pending-transfer filtering, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - failed on unrelated Orders guard: `Orders task home must keep role-aware worker actions and avoid sending delivery workers to manager delivery dashboard`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Retail guard: `Retail expense status page must let workers edit own submitted expenses before manager review`.

Risks / remaining checks:

- Full smoke needs the current non-Stock Orders/Retail guard failures resolved by their owning module pass.
- Manual mobile QA should open `/stock/receive-transfer`, choose a receiving location, enter a pending-transfer barcode for another location, and confirm the red message names the required destination.

Next recommended task:

- Continue Stock worker speed by checking direct outbound customer selection and transfer destination selection against real 390px phone flow.

## 2026-06-25 - Retail Today Summary self-filters today

Task completed:

- Updated Retail Today Summary so it filters its own sales, cash closing, expenses, cleaning, and processing records to today.
- This prevents manager/admin access to `/retail/today` from accidentally showing the first historical row when unfiltered Retail data is passed in.
- Updated Retail source coverage and QA evidence for the today-scoped summary invariant.
- Existing Retail RLS, calculations, storage, exports, audit, routes, and migrations were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - failed before Retail checks on unrelated Orders guard: `Orders task home must keep role-aware worker actions and avoid sending delivery workers to manager delivery dashboard`.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual UAT should open `/retail/today` as worker, manager, admin, and director and confirm only today's summary values are shown.

Next recommended task:

- Continue checking active Retail views for places where the route relies on caller-side filtering instead of page-level task scope.

## 2026-06-25 - Order Ready plain delivery wording

Task completed:

- Changed `/orders/ready` Price Required copy from `delivery handoff` wording to `Enter final price so pickup or delivery can continue.`
- Changed the empty Pending Delivery message to `No delivery orders are ready for delivery.`
- Changed Internal Transfer copy to `Transfer orders stay visible here until staff open them.`
- Existing Order backend logic, RLS, final-price status transitions, Delivery module handoff, pickup completion, and transfer behavior were not changed.
- Updated Order UX coverage to guard the ready board against worker-visible `handoff` wording.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders/ready` as a worker and confirm Price Required, Pending Delivery, and Internal Transfer read like simple task cards.

Next recommended task:

- Continue the Order mobile-app pass by checking order detail delivery cards for remaining internal handoff wording.

## 2026-06-25 - Stock inbound first-time barcode rule setup

Task completed:

- Barcode Inbound now opens the weight-rule panel automatically when product, brand, and origin are selected but no saved barcode rule exists.
- Saved-rule inbound sessions still keep the setup panel collapsed, preserving the scan-first repeat-batch flow.
- Existing decode, duplicate blocking, internal-label fallback, server actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/inbound`, choose an item + brand + origin with no saved barcode rule, and confirm `Set barcode rule once` is already open.
- Also confirm a saved-rule template keeps the panel collapsed and goes straight to scanning.

Next recommended task:

- Continue Stock worker speed by checking if direct outbound and transfer scan pages still expose any optional fields before the main scan action.

## 2026-06-25 - Order Home delivery wording

Task completed:

- Changed the `/orders` Pending Delivery queue description from `Priced delivery orders waiting for handoff.` to `Delivery team can see these orders now.`
- Changed the Pending Delivery queue button from `Open Handoff` to `Open Delivery`.
- Existing Order backend logic, RLS, delivery visibility, final-price status transitions, and Delivery module handoff behavior were not changed.
- Updated Order UX coverage to guard the simpler delivery wording.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders` as a worker and confirm Pending Delivery reads like a delivery task, not an internal handoff process.

Next recommended task:

- Continue the Order mobile-app pass by simplifying remaining `/orders/ready` delivery/internal-transfer handoff copy where it is worker-visible.

## 2026-06-25 - Retail QA guards aligned to draft sales and report-only processing

Task completed:

- Updated Retail business rules and QA docs to match the current task-launcher direction: worker Daily Sales drafts, manager confirmation before Cash Closing, and Retail processing as report-only.
- Updated Retail E2E coverage so it no longer preserves the old "worker cannot create Daily Sales at all" assumption.
- Updated staging Retail QA so processing manager evidence is a scoped read/report check, not a `REVIEWED` status update.
- Removed the Retail coverage requirement for same-day cash-session policies from active V1 checks.
- Existing Retail UI routes, RLS policies, calculations, file storage, exports, audit logs, and migrations were not changed.

Files changed in this pass:

- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/SUPABASE_QA_PLAN.md`
- `docs/manual-qa-checklist.md`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `scripts/retail-v1-staging-real-qa.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real staging QA should rerun `scripts\retail-v1-staging-real-qa.mjs` after the next migration/seed pass to capture worker draft evidence on a clean same-day outlet row.

Next recommended task:

- Continue checking active Retail QA scripts/docs for stale old-POS or cash-session assumptions only where they affect V1 evidence.

## 2026-06-25 - Stock item edit advanced settings

Task completed:

- Stock Item Master edit now keeps `Barcode required` and `Active` toggles under `Advanced item settings`.
- Normal edit fields still appear only after a worker intentionally selects an item.
- Existing item create/update actions, role checks, RLS, item-code validation, active flag behavior, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.

Risks / remaining checks:

- Manual mobile QA should open `/stock/items`, select a product, and confirm active/barcode-required toggles are tucked under `Advanced item settings`.

Next recommended task:

- Continue Stock worker speed by checking barcode-rule setup for any remaining visible fields that can be defaulted or tucked away.

## 2026-06-25 - Order Home final price wording

Task completed:

- Changed `/orders` task-home copy from `Enter Price` to `Enter final price` in the worker task strip.
- Changed the Price Required queue action from `Enter Price` to `Enter final price`.
- Changed the shared Order page header from technical `Manual ERP order flow...` copy to `Create, pick, price, and hand off orders from one simple workflow.`
- Existing Order backend logic, RLS, create flow, picking, final-price action, delivery handoff, and finance hiding rules were not changed.
- Updated Order UX coverage to guard the task-home final-price wording and plain header copy.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/orders` as a worker and confirm the header and Price Required task use simple workflow/final-price wording without finance/payment/outstanding language.

Next recommended task:

- Continue the Order mobile-app pass by checking `/orders/create` step labels and item cards for old ERP/form language.

## 2026-06-25 - Stock return default-location focus

Task completed:

- Stock Return now shows the selected `Returning to` location card first and tucks location selection under `Change return location`.
- This keeps the worker path closer to the target flow: choose condition, scan barcode, done.
- Existing return location defaulting, editable location selection, return-condition choices, barcode scan/save action, inspection warning, RLS, and return RPC behavior were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt saw another Next build process running; passed on retry.

Risks / remaining checks:

- Manual mobile QA should open `/stock/return`, confirm `Returning to` appears first, then open `Change return location` and verify location can still be changed when needed.

Next recommended task:

- Continue Stock worker speed by checking another scan-heavy form for optional controls that can be tucked behind a disclosure.

## 2026-06-25 - Retail Cash Closing confirmed-sales guidance

Task completed:

- Updated Cash Closing readiness copy when no sales summary is available.
- The page now says `Record and confirm Daily Sales first.` instead of mentioning a draft, matching the rule that Cash Closing only uses confirmed Daily Sales.
- Existing Daily Sales draft/confirmation behavior, Cash Closing server validation, calculations, RLS, audit, and outlet scope were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Limited UAT should confirm managers understand draft Daily Sales must be confirmed before Cash Closing.

Next recommended task:

- Continue trimming Retail wording that implies worker drafts can feed manager-only closing.

## 2026-06-25 - Order final price wording cleanup

Task completed:

- Changed the worker-facing `/orders/[id]` order summary label from `Total` to `Final price`.
- Changed the `/orders/ready` Price Required card action from `Enter Price` to `Enter final price`.
- Kept the existing worker price behavior: missing price shows after-picking / Price Required, and final price appears only after it is entered.
- Existing Order backend logic, RLS, reservation, picking, delivery handoff, pickup, cancellation, and finance hiding rules were not changed.
- Added Order UX coverage so the detail summary and ready-card action keep final-price wording.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed. Earlier first attempt was blocked by another active Next build, retry passed before the final green run.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should open an order detail and `/orders/ready` as a worker and confirm final-price wording still hides finance/payment/outstanding information.

Next recommended task:

- Continue the Order mobile-app pass by checking `/orders/create` and `/orders/ready` for any remaining old ERP/table wording visible to workers.

## 2026-06-25 - Stock Sales confirm prompt order

Task completed:

- Direct Stock Sales outbound confirm guidance now tells workers to choose a customer before asking them to scan a barcode.
- The scan block, confirm disable state, and server-side missing-customer rejection from the prior Stock Sales pass remain unchanged.
- Existing direct Processing, Transfer, Sample/Testing, Damage/Spoilage, Return Supplier, stock-location checks, duplicate blocking, and approval request flows were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`
- `docs/STOCK_QA_EVIDENCE.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual mobile QA should open `/stock/outbound`, select `Sales`, and confirm the yellow helper says to choose a customer before scanning.

Next recommended task:

- Continue Stock worker speed by checking the next direct outbound setup message or transfer/receive default that can reduce worker hesitation.

## 2026-06-25 - Retail dead POS/register form cleanup

Task completed:

- Removed unused Retail form exports for old POS sale entry, payment entry, cash-session open/close, retail price rule setup, and price hints.
- Removed their private register, cash-session, sale, stock-unit, no-barcode-stock, item/brand/origin, and stock-location selector helpers where they were only used by the deleted forms.
- Trimmed unused processing form props from Retail and Processing callers.
- Existing visible Retail V1 pages, Daily Sales, Expenses, Cleaning, Processing records, Cash Closing, reports, RLS, file security, calculations, storage isolation, export permissions, and audit protections were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `components/processing/processing-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Limited UAT should continue to use AutoCount Daily Sales and ERP Cash Closing only; no POS/register form is available in Retail V1.

Next recommended task:

- Continue checking direct Retail route/copy for old POS/register concepts only if users can still reach them.

## 2026-06-25 - Stock direct Sales customer required

Task completed:

- Direct Stock Sales outbound now requires a selected customer before barcode scanning or final confirmation.
- The `/stock/outbound` worker UI blocks Sales scanning with `Choose customer before scanning.` until a customer is selected.
- The server action also rejects direct Sales confirmation without a customer, so the rule is not only frontend validation.
- Existing direct Processing, Transfer, Sample/Testing, Damage/Spoilage, Return Supplier, stock-location checks, duplicate blocking, approval request flows, and movement RPC intent were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - failed in unrelated Retail file `components/retail/retail-forms.tsx`: `SaleSelect`, `RegisterSelect`, `ItemSelect`, `BrandSelect`, and `OriginSelect` are not defined.
- `npm.cmd run typecheck` - failed on stale `.next/types` missing generated files before build; build then regenerated Next types and failed on unrelated Retail types.
- `npm.cmd run build` - failed in unrelated Retail file `components/retail/retail-forms.tsx:1208`: `Cannot find name 'RetailBrand'`.

Risks / remaining checks:

- Manual mobile QA should open `/stock/outbound`, select `Sales`, confirm scanning is blocked before customer selection, then choose a customer and scan normally.
- Full lint/typecheck/build should be rerun after the unrelated Retail form symbol/type errors are fixed.

Next recommended task:

- Continue Stock worker speed after the unrelated Retail build/lint blockers are cleared or explicitly assigned to the Retail workstream.

## 2026-06-25 - Order detail source badge advanced-only

Task completed:

- Hid the technical `MANUAL_ERP` source badge from worker/default `/orders/[id]` summaries.
- Manager/admin/director users still see the order source in the detail summary.
- Existing worker price display, status badges, stock warning, customer remarks, order data, RLS, and server actions were not changed.
- Added Order UX coverage to keep the source badge behind the advanced-user summary gate.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - failed in unrelated Retail file `components/retail/retail-forms.tsx`: `SaleSelect`, `RegisterSelect`, `ItemSelect`, `BrandSelect`, and `OriginSelect` are not defined.
- `npm.cmd run build` - failed in unrelated Retail file `components/retail/retail-forms.tsx:1046`: `Cannot find name 'RetailSale'`.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual detail QA should confirm workers still see the useful status badge while managers still see source metadata.
- Full lint/build should be rerun after the unrelated Retail form symbol/type errors above are fixed.

## 2026-06-25 - Order detail action card cleanup

Task completed:

- Simplified `/orders/[id]` action cards so irrelevant disabled cards no longer appear.
- `Pickup completed` now renders only when a ready pickup order is present.
- `Cancel order` now renders only when the shown order is still cancellable.
- Existing pickup completion, cancellation, reservation release, RLS, status checks, and server actions were not changed.
- Added Order UX coverage for the conditional detail action cards.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual detail QA should confirm delivery orders no longer show a disabled pickup card, while ready pickup orders still show `Picked up` and cancellable orders still show cancel.

## 2026-06-25 - Retail Today Summary sales status

Task completed:

- Worker Today Summary now shows the actual Daily Sales status beside today sales amount.
- The card now renders `Status: Draft`, `Status: Confirmed`, or `Status: Not Entered` instead of only entered/not-entered copy.
- Existing Today Summary outlet filtering, Cash Closing `Closed` / `Not Closed`, cash expenses, missing cleaning, processing count, RLS, file security, calculations, and export permissions were not changed.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed on retry after Next reported another build process was already running.

Risks / remaining checks:

- Limited UAT should confirm workers understand Draft means manager confirmation is still pending.

Next recommended task:

- Continue checking worker-visible Retail wording for places that hide status or imply POS/register behavior.

## 2026-06-25 - Order detail edit form gate

Task completed:

- Removed dead edit controls from `/orders/[id]` after picking starts.
- The `Edit before picking` form now renders only while the order status is `NEW`, matching the existing server/action rule.
- Existing edit action validation, picking lock behavior, cancel flow, final price flow, pickup flow, RLS, and order data were not changed.
- Added Order UX coverage for the detail edit gate.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm brand-new orders still show edit controls, while picked/ready/closed orders no longer show disabled edit fields.

## 2026-06-25 - Stock item master phone keyboard hints

Task completed:

- Stock Item Master create/edit text and numeric fields now avoid browser autofill and request the right phone keyboard (`Done`, numeric, or decimal) where applicable.
- This covers generated item code, product name, optional section/alternate names, edit item code/name, and low-stock kg fields.
- Existing item create/update actions, role checks, RLS, item-code validation, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempt failed because another Next build process was already running; passed on retry after the worker cleared.

Risks / remaining checks:

- Manual mobile QA should open `/stock/items`, create/edit one item, and confirm the phone keyboard does not show distracting autofill suggestions.

Next recommended task:

- Continue Stock worker speed by checking the next Stock setup form for unnecessary typing.

## 2026-06-25 - Retail cash-session routes redirected for V1

Task completed:

- Redirected old Retail cash-session entry routes into the V1 Daily Cash Closing flow.
- `/retail/cash`, `/retail/cash/open`, and `/retail/cash/close` now redirect to `/retail/cash-closing`.
- `/retail/cash/history` and `/retail/cash/history/sessions` now redirect to `/retail/cash/history/closings`.
- Removed old cash-session render branches and cash-session count from the Retail page shell.
- Worker Today Summary now stays to today sales, Cash Closing `Closed` / `Not Closed`, cash expenses, missing cleaning, and processing count.
- Existing Cash Closing calculation, variance warning-only behavior, audit, RLS, outlet scope, file security, and export security were not changed.

Files changed in this pass:

- `app/(erp)/retail/cash/page.tsx`
- `app/(erp)/retail/cash/open/page.tsx`
- `app/(erp)/retail/cash/close/page.tsx`
- `app/(erp)/retail/cash/history/page.tsx`
- `app/(erp)/retail/cash/history/sessions/page.tsx`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Limited UAT should confirm managers use Cash Closing and Cash Closing Records only, with no need for old register session pages.

Next recommended task:

- Continue checking direct Retail routes for old POS/register concepts that are no longer part of V1.

## 2026-06-25 - Order detail worker activity log collapse

Task completed:

- Simplified `/orders/[id]` for worker/default users by collapsing the full status timeline under `Activity log`.
- Manager/admin/director users still see the status timeline directly on the detail screen.
- Existing order summary, item cards, customer remarks, picking/final-price/pickup actions, advanced records, data loading, RLS, and server actions were not changed.
- Added Order UX coverage for the detail route role split.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed in unrelated Retail guard: `Retail V1 page shell must not render old cash-session open forms.`

Risks / remaining checks:

- Manual mobile QA should confirm workers can still expand `Activity log` when troubleshooting, while the default detail screen stays focused on order/action cards.
- Smoke should be rerun after the unrelated Retail coverage failure above is resolved.

## 2026-06-25 - Stock item edit search

Task completed:

- Stock Item Master edit now has item-code/product search before the edit dropdown.
- The edit form still starts with no selected item and hides update fields/toggles until a product is selected.
- Existing item create/update actions, role checks, RLS, item-code validation, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Order coverage: `/orders detail route missing end marker: } : route === "detail" ?`.

Risks / remaining checks:

- Manual mobile QA should search one existing and one missing product on `/stock/items`.
- Full smoke needs the unrelated Order coverage marker fixed by the Order workstream before it can pass end to end.

Next recommended task:

- Continue Stock worker speed by trimming the next item/inbound setup friction.

## 2026-06-25 - Order delivery Price Required driver cue

Task completed:

- Refined the Delivery driver Available tab for Order-module delivery handoff.
- Delivery workers still see created delivery orders before they can accept them, with picked/estimated weight progress and customer remarks.
- Orders in `Price Required` now show `Final price needed` instead of the generic `Preparing order` text.
- The card still keeps accept locked until the order becomes `Pending Delivery`.
- Existing delivery query/RLS, accept/load/start/proof actions, order status sync, and handoff logic were not changed.
- Added Delivery coverage for the new Price Required driver cue.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\delivery-module-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual driver QA should confirm `Price Required` orders are visible but not acceptable, then move to `Pending Delivery` after final price is saved.

## 2026-06-25 - Retail cash closing one-per-day success cleanup

Task completed:

- Removed the `Submit another closing` success shortcut from Retail Cash Closing.
- Cash Closing success now points managers to `Go to Outlet Report` or `Back to Retail Home`.
- This matches the V1 rule of one cash closing per outlet/date.
- Existing Cash Closing calculations, variance warning-only behavior, audit, RLS, and outlet scope were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed on retry after Next reported another build process was already running.

Risks / remaining checks:

- Limited UAT should confirm the Cash Closing success path feels clear for managers after the daily close is saved.

Next recommended task:

- Continue trimming direct Retail routes that still expose dormant V1/POS-style work if users can reach them.

## 2026-06-25 - Stock item edit starts blank

Task completed:

- Stock Item Master edit now starts with no selected item.
- Update fields and active/barcode-required toggles appear only after the worker intentionally selects a product.
- Existing item create/update actions, role checks, RLS, item-code validation, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm `/stock/items` starts with `Select item` and toggles only appear after product selection.

Next recommended task:

- Continue Stock worker speed by trimming the next item/inbound setup friction.

## 2026-06-25 - Order picking manual weight quantity cleanup

Task completed:

- Simplified `/orders/picking` manual weight entry for weight-based order items.
- Weight items no longer show a confusing manual `Quantity` field; pickers enter picked kg only.
- Carton, packet, and quantity-estimated-kg items still show a unit-specific quantity field.
- Existing picking server actions, barcode scan behavior, mismatch logging, duplicate barcode blocking, tolerance checks, RLS, and order status flow were not changed.
- Added Order UX coverage for the weight-item manual picking wording.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm pickers understand kg-only manual entry for weight items and still see quantity when picking carton/packet items.

## 2026-06-25 - Retail processing phone wastage cleanup

Task completed:

- Simplified the worker Record Processing wastage step for phone use.
- The main Wastage step now keeps `Total wastage weight` visible first.
- Optional wastage reason, photo upload, and remarks are tucked behind `Wastage details (optional)`.
- Hidden Retail price-form success links no longer point back into disabled Retail price setup routes.
- Existing Retail calculations, file upload handling, outlet isolation, RLS, export security, and audit behavior were not changed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed on retry after Next reported another build process was already running.

Risks / remaining checks:

- Limited UAT should confirm workers can find optional wastage photo/remarks when needed and that the default phone path feels quicker.

Next recommended task:

- Run the focused real worker/manager phone pass on the simplified Retail one-task pages during Limited UAT.

## 2026-06-25 - Stock internal-label repeat weight focus

Task completed:

- Barcode Inbound now returns focus to `Net weight kg` after an internal-label save.
- Supplier barcode saves still return focus to the barcode field for continuous scanning.
- Existing generated-label save, barcode-rule learning, duplicate blocking, inbound RPC, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should generate two internal labels in one inbound session and confirm the weight field is ready for the next entry after each save.

Next recommended task:

- Continue Stock worker speed by trimming the next scan-path friction.

## 2026-06-25 - Order create weight-line quantity cleanup

Task completed:

- Simplified `/orders/create` Step 3 item cards for weight orders.
- Weight lines no longer show a confusing `Quantity` field; workers enter estimated kg only.
- Switching an item/unit back to `Weight` clears stale quantity and the create payload submits quantity as `0` for weight orders.
- Existing item selection, customization chips, brand preference, stock checks, final-price-after-picking flow, RLS, and server order logic were not changed.
- Added Order UX coverage for the weight-order wording and KG branch.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm workers understand that weight orders need only estimated kg, while carton/packet orders still need quantity plus estimated kg.

## 2026-06-25 - Retail cash variance warning-only copy

Task completed:

- Changed Cash Closing variance copy from a remarks-before-save instruction to warning-only wording.
- The readiness card no longer marks non-zero variance as a blocker.
- Existing Cash Closing action behavior stayed unchanged; it already saves non-zero variance and returns a warning message.
- Added coverage so the old blocking remarks wording does not return.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Limited UAT should confirm managers still add remarks voluntarily when variance needs explanation.

## 2026-06-25 - Stock external scanner Enter handling

Task completed:

- Shared Stock barcode fields now treat Enter in the manual barcode input as a scan.
- USB/Bluetooth scanners that type the barcode and send Enter now use the same scan handler as camera scans.
- Existing camera scanner, online-only blocking, duplicate checks, server validation, stock actions, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual hardware QA should scan with a USB/Bluetooth scanner that sends Enter on inbound, outbound, transfer, receive-transfer, return, and stock-take.

Next recommended task:

- Continue Stock worker speed by trimming the next shared scan-path friction.

## 2026-06-25 - Order create no-brand preference cue

Task completed:

- Added a worker-facing cue under `No brand preference` on `/orders/create`.
- The create form now states: `Picker chooses the brand during picking.`
- Existing brand preference storage, stock checks, picking brand mismatch handling, RLS, pricing, and delivery handoff logic were not changed.
- Added Order UX coverage for the no-brand preference cue.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm staff understand that leaving brand blank means the picker chooses the actual barcode brand later.

## 2026-06-25 - Retail Settings visual V1 cleanup

Task completed:

- Retail Settings now uses icon action buttons for the remaining V1 setup tasks.
- Removed stale copy that said managers can manage prices, since Retail price setup is hidden for V1.
- Kept price rule data/actions dormant for future item-level Retail/POS work.
- Added Retail coverage for the Settings icons and hidden-price wording.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Limited UAT should confirm manager/admin users can still find categories, BOMs, and audit quickly without the hidden price setup links.

## 2026-06-25 - Order create step-2 location validation

Task completed:

- Step 2 of `/orders/create` now blocks missing delivery address before workers move to Items.
- Quick-add delivery customers get a clearer message: `Enter delivery address for new delivery customer.`
- Pickup location and internal transfer from/to location checks also stop on Step 2 instead of waiting until final Create.
- Existing backend validation, order creation, RLS, picking, pricing, and delivery handoff logic were not changed.
- Added Order UX coverage for the Step 2 validation messages.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first run hit an active Next build lock; retry after waiting passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm Delivery, Pickup, and Internal Transfer are stopped on Step 2 with clear messages when their location/address fields are missing.

## 2026-06-25 - Stock inbound no-rule label guidance

Task completed:

- Barcode Inbound now gives a clear worker message when actual kg is entered but no supplier barcode weight-position rule can be inferred.
- The message sends the worker to the existing internal-label flow: `No matching weight position found. Generate an internal label instead.`
- Existing generated-label save, barcode-rule learning, duplicate blocking, inbound RPC, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should scan a supplier barcode with no usable weight position, enter actual kg, confirm the internal-label guidance appears, then generate/print the internal label.

Next recommended task:

- Continue Stock worker speed by trimming the next scan-path friction after the current checks.

## 2026-06-25 - Order create no auto-selected customer

Task completed:

- `/orders/create` no longer preselects the first A-Z customer on page load.
- Customer, delivery address, and customer remarks now start blank so workers must choose an existing customer or quick add one.
- Existing customer selection still fills saved address and remarks after the worker picks that customer.
- Existing quick-add, validation, order creation, RLS, picking, pricing, and delivery handoff logic were not changed.
- Added Order UX coverage to keep the create form from silently defaulting to the first customer.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first run hit an active Next build lock; retry after waiting passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm Step 1 starts with no selected customer, shows the validation message when empty, and fills address/remarks only after selecting a customer.

## 2026-06-25 - Retail V1 price setup hidden

Task completed:

- Hid Retail price setup links from Retail Settings because V1 does not run item-level retail sales or POS.
- Redirected `/retail/settings/prices` and `/retail/settings/prices/records` back to `/retail/settings`.
- Kept existing price rule code/data in place for future V2 use; no schema or RLS changes.
- Updated Retail coverage so V1 keeps price setup out of the visible Settings workflow.

Files changed in this pass:

- `app/(erp)/retail/settings/prices/page.tsx`
- `app/(erp)/retail/settings/prices/records/page.tsx`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- If UAT asks for retail price reference setup before POS, restore the links deliberately.

## 2026-06-25 - Order picking choose-order default

Task completed:

- `/orders/picking` no longer auto-opens the first picking order.
- Workers now start by choosing an order card, matching the one-task-at-a-time picking flow.
- Opening picking from a specific order detail still preselects that order.
- Existing barcode picking, manual weight, tolerance, ready, RLS, reservation, pricing, and delivery handoff logic were not changed.
- Added Order UX coverage for the choose-order default.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm `/orders/picking` starts at the order-card choice and `/orders/[id]` picking still opens the selected order.

## 2026-06-25 - Order final price single-order cleanup

Task completed:

- Simplified final price entry when the worker is already on a single Price Required order.
- The form now shows the selected order card and uses a hidden order id instead of asking the worker to select the same order from a dropdown.
- Multi-order Ready page pricing still keeps the order dropdown.
- Existing final price action, READY_FOR_PICKUP / READY_FOR_DELIVERY transitions, RLS, reservations, picking, and delivery handoff were not changed.
- Added Order UX coverage for the single-order final price path.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed in existing Retail cash-closing guard: `Retail cash closing UX must keep readiness checks, daily sales fallback, variance guidance, and manager-controlled access copy`.

Risks / remaining checks:

- Manual QA should confirm `/orders/[id]` Price Required entry no longer asks for order selection, while `/orders/ready` still supports pricing one of many orders.
- Smoke needs the unrelated Retail cash-closing worktree change resolved before full-suite green.

## 2026-06-25 - Retail cash session worker scope cleanup

Task completed:

- Made Retail cash session open, close, and history routes manager/admin/director controlled.
- Changed the worker-facing Cash Closing block copy so workers are pointed to Today Summary for `Closed` / `Not Closed` status instead of an Open Cash page.
- Tightened `openRetailCashSessionAction` to use the same manager/admin/director role list as other cash-session controls.
- Added Retail coverage for the route guard and action role guard.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed after updating the cash-closing smoke guard to the new worker Today Summary copy.
- `npm.cmd run build` - first attempt hit an active Next build lock; retry passed.

Risks / remaining checks:

- Limited UAT should confirm workers only need Today Summary for cash status and do not need an open-cash action in Retail V1.

## 2026-06-25 - Order detail worker summary cleanup

Task completed:

- Simplified the worker-facing order detail summary.
- `Created by`, `Outlet`, and `Reservation expires` now stay visible only for manager/admin/director advanced order users.
- Worker order detail keeps the useful customer, required date/time, order type, total price state, phone, and order detail fields.
- Existing backend logic, RLS, reservations, picking, pricing, and delivery handoff were not changed.
- Added Order UX coverage for the advanced-only detail fields.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual role QA should confirm a worker sees the simpler detail summary while manager/admin still see outlet, creator, and reservation expiry.

## 2026-06-25 - Retail processing V1 label prompt cleanup

Task completed:

- Removed the worker Processing after-submit prompt to print barcode labels.
- Kept the V1-safe reminder that finished stock is not added automatically and must be handled as a separate stock step.
- Added Retail coverage so barcode-label generation wording does not return to the Retail processing worker page.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Limited UAT should confirm workers understand Retail Processing is record-only and any stock inbound happens separately.

## 2026-06-25 - Stock outbound scan prerequisite guard

Task completed:

- Direct Stock Outbound scan handling now enforces the same required setup blockers that the UI shows before adding a scanned barcode.
- Direct Sales customer selection is optional for movement notes only; Sales can scan without choosing a customer.
- Damage/Spoilage scans require a photo, Return Supplier scans require supplier, and Transfer scans require destination inside the scan handler.
- Existing outbound server actions, damage approval-only behavior, direct remarks, RLS, migrations, and database schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Retail coverage: `Retail focused page wrapper missing route="settings-prices"`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm direct Sales can scan without a customer, then try camera/manual scanning before Damage photo, Return Supplier supplier, and Transfer destination to confirm no barcode row is added.
- Full smoke needs the unrelated Retail `settings-prices` coverage issue resolved by the Retail workstream before it can pass end to end.
- Real phone camera and authenticated Vercel/Supabase workflow evidence remain required.

Next recommended task:

- Continue Stock worker speed by finding the next small scan-path friction issue, or run owner/device QA once Supabase/Vercel access is available.

## 2026-06-25 - Retail worker Cleaning bottom action

Task completed:

- Updated the worker Complete Cleaning button to stay as a sticky bottom phone action, matching the other Retail worker submit pages.
- Kept Cleaning tap-only for V1; no worker photo/remarks fields, manager setup, reports, or review controls were added back.
- Added Retail coverage so the sticky mobile Cleaning action is protected.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Limited UAT should still include a real phone pass over worker Expense, Cleaning, Processing, and Today Summary.
- New Retail migrations from this UX phase still need to be applied to staging before authenticated UAT if they have not already been pushed.

## 2026-06-25 - Order create worker wording cleanup

Task completed:

- Removed stock-reservation jargon from the worker-facing `/orders/create` copy.
- Create success now says `Stock checked`; item entry now says estimated kg is for picking.
- Existing create action, stock reservation behavior, RLS, picking, pricing, and delivery handoff logic were not changed.
- Updated Order UX coverage for the simpler worker wording.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - first run failed on the old wording guard; after updating the guard, rerun passed.

Risks / remaining checks:

- Manual mobile QA should confirm `/orders/create` reads like a worker picking/order app and no longer exposes reservation jargon in the create flow.

## 2026-06-25 - Order create step blocker cleanup

Task completed:

- Removed the final create-readiness checklist from non-final `/orders/create` steps.
- Workers now see only the current step and get validation when they tap Next/Create, instead of seeing future-step blockers early.
- Existing create action, order status, reservation, picking, pricing, RLS, and delivery handoff logic were not changed.
- Added Order UX coverage to prevent the non-final step blocker from returning.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm `/orders/create` Step 1/2/3 no longer shows future-step warnings before the worker taps Next.

## 2026-06-25 - Stock inbound rule saving default

Task completed:

- Barcode Inbound now turns `Save weight rule for future scans` back on when the selected item + brand + origin has no saved rule.
- This prevents an old browser-saved checkbox state from blocking first-scan rule learning.
- Existing saved-rule loading, barcode decoding, inbound RPC, duplicate blocking, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual QA should confirm a no-rule item + brand + origin has rule saving on before the first learned scan.
- Real scanner QA still needs to prove the next same-format supplier barcode auto-extracts weight.

Next recommended task:

- Continue Stock worker speed by trimming any remaining optional typing in damage/spoilage or direct outbound.

## 2026-06-25 - Retail manager Daily Sales draft status

Task completed:

- Manager Today checklist now shows Daily Sales as `Draft` and pending until it is confirmed.
- Confirmed Daily Sales shows `Confirmed`.
- Missing Daily Sales still shows `Not Entered`.
- Cash Closing gating remains unchanged and still requires confirmed Daily Sales.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual manager UAT should confirm draft Daily Sales appears before Cash Closing as pending work.

## 2026-06-25 - Order manager progress first

Task completed:

- Moved `Today order progress` above the worker task card for manager/admin/director Order Home.
- Workers still see the simplified task card first.
- Added Order UX coverage to keep manager progress before `Today's order work`.
- No backend, RLS, migration, reservation, picking, pricing, status, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempts hit an active Next build lock; retry after worker processes cleared passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Manual manager/admin QA should confirm `/orders` opens with today progress visible before task actions.

## 2026-06-25 - Retail Daily Sales today-only

Task completed:

- Removed the editable Daily Sales date picker.
- Daily Sales now saves only today's date from the page.
- Worker draft success now returns to Retail Home instead of pointing workers at manager-only Cash Closing.
- Manager confirm success still offers `Go to Cash Closing`.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual UAT should confirm workers see today's date as display-only and managers can still continue to Cash Closing after confirming.

## 2026-06-25 - Stock transfer assigned source cue

Task completed:

- Stock Transfer now receives the logged-in profile stock location.
- `/stock/transfer` shows `Sending from assigned location: [location].`
- Selecting that assigned source as the destination blocks scanning with `Choose a different destination.`
- Existing transfer RPC, scoped barcode/source validation, receive-transfer logic, RLS, and schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual QA should confirm a worker with an assigned stock location sees the assigned-source cue and cannot select it as destination.
- Real Supabase/RLS QA still needs to prove barcode source scope is enforced server-side for transfer.

Next recommended task:

- Continue Stock worker speed by simplifying damage photo flow or transfer destination discovery if needed.

## 2026-06-25 - Order Home worker task copy cleanup

Task completed:

- Replaced redesign/meta wording on `/orders` worker task home with plain operational copy.
- The top worker card now says `Today's order work` and points staff to create, pick, price, pickup, or delivery.
- Task chips now use worker actions: `Create Order`, `Pick Items`, `Enter Price`, and `Pickup / Delivery`.
- Updated smoke and Order UX coverage to guard the new role-aware task home wording.
- No backend, RLS, migration, reservation, picking, pricing, status, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the `/orders` top task card reads naturally for workers and does not feel like an ERP instruction panel.

## 2026-06-25 - Retail processing evidence wording

Task completed:

- Corrected the Retail V1 QA evidence route list so `/retail/processing/review` is described as report-only, not a review workflow.
- No app code, database logic, RLS, or permissions changed.

Files changed in this pass:

- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.

Risks / remaining checks:

- None beyond the existing staging migration/UAT pass.

## 2026-06-25 - Retail cleaning tap-only copy

Task completed:

- Removed worker Cleaning copy that mentioned adding remarks or photo.
- Error recovery now tells workers to check the task/connection and tap Complete Cleaning again.
- Worker Cleaning remains tap-only with hidden empty photo/remarks fields for server compatibility.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual phone UAT should confirm workers see only the big Complete Cleaning action.

## 2026-06-25 - Order picking scan button mobile visibility

Task completed:

- Made the `/orders/picking` barcode submit action a full text button instead of an icon-only button.
- The scan row now keeps the barcode input and `Scan Barcode` action clear on mobile and desktop.
- Manual Weight remains equally visible beside/under the scan card depending on screen width.
- No backend, RLS, migration, reservation, picking calculations, status, pricing, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Manual mobile QA should confirm the `Scan Barcode` and `Save manual weight` actions feel equally discoverable at phone width.

## 2026-06-25 - Stock direct Sales customer search

Task completed:

- Added `Search customer or phone` to direct Sales outbound.
- Search filters the quick customer buttons so workers can tap the customer before scanning.
- The full customer dropdown remains available.
- Kept existing server-side customer validation, outbound RPC, barcode checks, notes behavior, RLS, and schema unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first two attempts hit an existing Next build lock; retry after waiting passed.

Risks / remaining checks:

- Manual QA should search by customer name and phone, tap a filtered customer, and confirm direct Sales scanning unlocks.
- Real Supabase/RLS QA still needs to prove workers only see customers in scope.

Next recommended task:

- Continue Stock worker speed by simplifying direct outbound Transfer destination selection or damage photo flow.

## 2026-06-25 - Order form status selector wording

Task completed:

- Added a shared Order form label helper for workflow status text.
- Cancel-order and customer-order delivery selectors now show staff wording:
  - `Price Required`
  - `Ready for Pickup`
  - `Pending Delivery`
- Kept stored order statuses and delivery/order actions unchanged.
- No backend, RLS, migration, reservation, picking, pricing, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed after removing stale generated `tsconfig.tsbuildinfo`.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Order coverage in unrelated Retail coverage at the time; current smoke passes after the Retail warning cleanup.

Risks / remaining checks:

- Manual browser QA should confirm order action dropdowns show `Pending Delivery` instead of raw `READY_FOR_DELIVERY`.
- The Retail warning-message guard was fixed later in this handoff; current smoke passes.

## 2026-06-25 - Order worker pending-delivery status wording

Task completed:

- Added a worker-facing Order status label helper.
- Order cards/details now show:
  - `Price Required` for picked orders waiting on final price.
  - `Ready for Pickup` after pickup orders are priced.
  - `Pending Delivery` after delivery orders are priced.
- Kept stored order statuses unchanged (`READY`, `READY_FOR_PICKUP`, `READY_FOR_DELIVERY`).
- No backend, RLS, migration, reservation, picking, pricing, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed on rerun after build completed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed in unrelated Retail E2E coverage: `Scenario 25 failed: Processing warning flag appears when values look abnormal` / `Processing action must create warning text. Missing: processingWarnings`.

Risks / remaining checks:

- Manual browser QA should confirm Order detail/cards show `Pending Delivery` instead of raw ready-for-delivery wording after final price is entered.
- The Retail processing warning guard was fixed later in this handoff; current smoke passes.

## 2026-06-25 - Stock return condition buttons

Task completed:

- Added large mobile return-condition buttons to normal Stock Return:
  - `Good / sellable`
  - `Failed delivery, product ok`
  - `Customer rejected, product ok`
- The selected condition is submitted with the return action and recorded through the existing return notes path.
- Kept the existing atomic `return_stock_unit` RPC, barcode checks, scoped location checks, movement/audit behavior, and `IN_STOCK` return behavior unchanged.
- No migration or RLS change was added.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt hit an existing Next build lock; retry after a short wait passed.

Risks / remaining checks:

- Manual QA should scan one return per condition and confirm the movement notes include the chosen condition.
- Customer returns that need inspection still belong in the existing inspection flow, not normal Stock Return.

Next recommended task:

- Continue Stock worker speed by reducing remaining optional typing on `/stock/outbound` direct Sales and Return/Damage.

## 2026-06-25 - Retail processing no weight warning

Task completed:

- Removed Retail processing save warnings for yield, wastage, and unaccounted weight differences.
- Processing still calculates and stores raw weight, finished weight, wastage, yield %, wastage %, and unaccounted difference.
- Worker success now just says the processing record was saved.
- Demo Retail processing warning text now stays `OK`.
- Removed the Retail Processing Report warning card/export rows.
- Added a migration to clear old Retail processing warning text and flags.

Files changed in this pass:

- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/reports.ts`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `supabase/migrations/202606250003_retail_processing_no_warning_v1.sql`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Migration `202606250003_retail_processing_no_warning_v1.sql` needs staging application before the next authenticated UAT pass.

## 2026-06-25 - Order customer A-Z coverage guard

Task completed:

- Audited the Order customer list/search path.
- Confirmed `lib/orders/data.ts` already sorts active customers A-Z by customer name.
- Added an Order UX coverage guard so `/orders/create` and `/orders/customers` keep inheriting A-Z customer ordering.
- No UI, backend, RLS, migration, reservation, picking, pricing, or delivery handoff logic was changed.

Files changed in this pass:

- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual Supabase/browser QA should confirm the customer picker/cards remain A-Z with real customer data.

## 2026-06-25 - Retail worker home processing first

Task completed:

- Moved `Record Processing` to the first worker Retail Home shortcut.
- Simplified the label from `Record Processing / Packing` to `Record Processing`.
- Kept the same destination `/retail/processing` and existing role/outlet restrictions.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual phone UAT should confirm workers notice Processing first on Retail Home.

## 2026-06-25 - Retail Daily Sales draft confirmation

Task completed:

- Added `DRAFT` / `CONFIRMED` status to Retail Daily Sales.
- Retail workers can save same-day Daily Sales only as draft.
- Retail managers/admin/directors confirm Daily Sales from today's existing summary.
- Cash Closing now uses only confirmed Daily Sales and blocks draft sales with a clear message.
- Updated Retail coverage and QA notes.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `supabase/migrations/202606250002_retail_daily_sales_draft_confirm_v1.sql`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempt hit an existing Next build lock; retry passed.

Risks / remaining checks:

- Migration `202606250002_retail_daily_sales_draft_confirm_v1.sql` still needs staging application before live UAT retest.
- Manual phone/browser UAT should confirm worker draft entry, manager confirm, and Cash Closing draft block.

## 2026-06-25 - Order worker price visibility guard

Task completed:

- Fixed a worker-facing Order detail display mismatch where a stored positive total could appear before final pricing.
- `OrderSummary` now shows:
  - `After picking` for `NEW` / `PREPARING` / unpriced workflow states.
  - `Price required` for picked orders waiting on final price.
  - The RM total only after final-price statuses such as ready for pickup/delivery or later.
- Stored order totals, advanced manager/admin tables, reports, RLS, and order state logic were not changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before Order-specific smoke checks on unrelated Retail coverage: `Retail daily sales UX must keep readiness checks, AutoCount guidance, controlled outlet choice, and cash-closing next step`.

Risks / remaining checks:

- Manual QA should open a `NEW` or `PREPARING` order with a stored total and confirm workers see `After picking`, not RM amount.

## 2026-06-25 - Stock item and barcode rule setup mobile simplification

Task completed:

- Simplified Stock Item Master create setup for worker/mobile use.
- The create form now shows generated item code, category, and product name first.
- Optional default brand, product section, Chinese/Iban names, low-stock level, and barcode-required toggle are tucked under `Optional item details`.
- Barcode Inbound now shows `Saved barcode rule ready` when a selected item + brand + origin already has a saved rule.
- Barcode Inbound now shows `No saved barcode rule yet` with one-scan setup guidance when no saved rule exists.
- Stock Return/Damage now hides the manager-only inspection-release form from normal workers.
- Item creation/update actions, inbound save actions, return/damage actions, duplicate item-code validation, item delete permissions, RLS, and database schema were not changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-item-master-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-item-master-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt hit an existing Next build lock; retry after a short wait passed.

Risks / remaining checks:

- Real 390px browser QA should confirm `/stock/items` keeps the create form readable and still allows optional fields when opened.
- Real inbound QA should select item + brand + origin and confirm the saved-rule/no-rule card gives the correct next step before scanning.
- Real role QA should confirm workers do not see `Release inspected return`, while managers/admins still do.
- Live Supabase QA should confirm scoped workers with Stock module access can create/edit item master but cannot delete items unless admin/director.

Next recommended task:

- Continue Stock worker speed by reviewing barcode-rule setup on `/stock/inbound` for any remaining typing before continuous scanning.

## 2026-06-25 - Order create internal-transfer customer bypass

Task completed:

- Fixed a guided-flow edge where Step 1 Customer could block internal transfer orders before staff could choose `Internal Transfer`.
- Added an `Internal transfer - no customer` action on Step 1.
- The action selects Internal Transfer and moves staff to Step 2 without requiring customer name/phone.
- Added a Step 2 guard so if staff switches back to Pickup or Delivery, customer selection/quick-add is still required.
- No backend, RLS, migration, reservation, stock, picking, order status, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should create an internal transfer with no customer selected, then try switching back to Pickup/Delivery and confirm customer validation appears.

## 2026-06-25 - Order picking no-brand-preference cue

Task completed:

- Added a picker-facing cue for order lines with no brand preference.
- `/orders/picking` item cards now show: `No preference - scan the brand you pick`.
- This makes the existing behavior clear: when the customer has no brand preference, the scanned barcode determines the picked brand.
- Delivery visibility was rechecked and already includes preparing orders, progress percentage, picked weight, customer remarks, and disabled accept until Pending Delivery.
- No backend, RLS, migration, reservation, stock movement, order status, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm workers understand that scanning a barcode chooses the brand only when the order line has no preferred brand.

## 2026-06-25 - Retail processing report-only route

Task completed:

- Changed `/retail/processing/review` from a manager review form to a report-only processing records view.
- Updated the page title/description to state that Retail processing does not require manager review.
- Removed the unused Retail processing review queue from `retail-page.tsx`.
- Kept existing processing action/data/RLS behavior unchanged; this pass only removed the outdated review UI path.
- Updated Retail source/smoke coverage and Retail QA notes.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Manual phone/browser UAT should confirm managers understand that Retail processing is report-only and no longer has a review/reject step.

## 2026-06-25 - Stock take pending exception review card

Task completed:

- Added a `Pending stock take exceptions` card above Stock Take review sessions.
- The card shows unresolved `UNKNOWN_BARCODE` and `WRONG_LOCATION` exception lines before final approval.
- Each exception row shows type, barcode, session/location, and the next step: manager review then director approval.
- Demo stock-take data now includes a pending wrong-location exception in addition to the existing unknown-barcode exception.
- No migration, RLS, approval action, server action behavior, or stock adjustment behavior was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/demo-data.ts`
- `scripts/stock-take-lock-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-take-lock-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt hit an existing Next build lock; retry after a short wait passed.

Risks / remaining checks:

- Real Supabase QA still needs pending exception rows under manager/admin/director users to confirm RLS and real data visibility.
- Manual QA should confirm approving a session still resolves unknown/wrong-location exceptions only after manager review and director approval.

Next recommended task:

- Continue Stock worker speed by simplifying Item Master / Barcode Rule setup for general workers while keeping dangerous item deletion/admin fields protected.

## 2026-06-25 - Order picking selected-item scan flow

Task completed:

- Tightened `/orders/picking` so pickers select an order item before barcode or manual picking.
- Item cards are now selectable; the selected item feeds both:
  - Barcode scan hidden `orderItemId`.
  - Manual weight item selector.
- Barcode scan now records a mismatch if the scanned barcode belongs to a different order line than the selected item.
- Existing duplicate-barcode, barcode-not-found, wrong-brand, manual reason, tolerance, and Ready behaviors were preserved.
- No schema, RLS, reservation, stock movement, order status, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `lib/orders/actions.ts`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should select item A, scan item B from the same order, and confirm the mismatch prompt appears and no picked weight is added to item A.

## 2026-06-25 - Retail worker submitted expense edit

Task completed:

- Added worker edit support for submitted expenses before manager review.
- Added `updateRetailExpenseAction` with server-side guards:
  - Only `SUBMITTED` expenses can be edited.
  - Only the submitting worker can edit, unless admin/director.
  - Existing outlet scope checks remain enforced.
  - Existing receipt is preserved unless a replacement receipt is uploaded.
- Added `submittedById` to Retail expense data so the UI can show only editable submitted expenses.
- Added `RetailExpenseEditForm` to `/retail/expenses/history`, keeping Submit Expense simple.
- Added source/smoke coverage for the edit-before-review workflow.
- Updated `docs/RETAIL_V1_E2E_TEST_RESULTS.md`.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - first run overlapped with build and failed on missing `.next/types`; rerun after build passed.

Risks / remaining checks:

- Manual mobile QA should verify the edit form is usable from `/retail/expenses/history` and that reviewed expenses disappear from the edit selector after manager review.

## 2026-06-25 - Order picking total progress bar

Task completed:

- Added total order progress to `/orders/picking` selected-order summary.
- Progress uses existing picked weight vs estimated weight and displays:
  - Percent complete.
  - Mobile-friendly progress bar.
- Existing item-level progress bars, scan/manual cards, mismatch prompt, manual reason requirement, and Ready behavior were preserved.
- No backend, RLS, migration, order status, stock, picking action, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed with one unrelated Retail warning: `components/retail/retail-forms.tsx` unused `updateRetailExpenseAction`.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm the progress bar is readable on phone width and updates after scans/manual weight entries.

## 2026-06-25 - Stock manager scan issue review

Task completed:

- Widened the Stock manager dashboard scan issue panel from duplicate/decode-only to all failed barcode scan logs.
- The panel now appears as `Manager scan issue review` and covers duplicate barcode, wrong location, unknown barcode, unavailable stock, and barcode rule failures.
- Added a direct link from the manager panel to `/stock/reports?q=Barcode%20scan%20errors`.
- Barcode scan error report rows now include the failed barcode in the category text.
- Demo stock scan logs now include wrong-location receive, unknown stock-take barcode, and unavailable-stock examples.
- No schema, RLS, server action, or stock movement behavior was changed.

Files changed in this pass:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase RLS still needs manager/admin/director QA to confirm they can see scoped failed scan logs while normal workers only see allowed/own data.
- Manual Vercel QA should trigger at least one duplicate, wrong-location, unknown-barcode, and unavailable-stock scan and confirm each appears in `Manager scan issue review` and the barcode scan error report.

Next recommended task:

- Continue Stock manager review hardening by making unresolved stock-take exception rows easier to review from the stock-take page.

## 2026-06-25 - Order create customer UX cleanup

Task completed:

- Sorted `/orders/create` customer search/picker options A-Z by customer name.
- Kept customer picker display as customer name + phone.
- Hid overdue credit warning from worker create-order flow; it now appears only for manager/admin/director-style roles.
- Clarified delivery quick-add behavior: when a new delivery customer is being created, the delivery address field is labeled as saved to the new customer.
- No backend, RLS, migration, order status, stock, picking, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm worker users do not see credit warnings during customer selection, and manager/admin users still see the warning when applicable.

## 2026-06-25 - Retail manager checklist and report order refinement

Task completed:

- Added Cash Closing final checklist shown only after a successful save.
- Changed `/retail/dashboard` to open the manager Outlet Report checklist/report directly.
- Added `Today checklist` to the top of manager Outlet Report.
- Sorted manager checklist items so pending tasks appear first.
- Reordered admin/director report pages so detailed tables appear before KPI cards.
- Added Retail source coverage for the cash final checklist, manager checklist, pending-first sort, and dashboard route.
- Updated `docs/RETAIL_V1_E2E_TEST_RESULTS.md`.

Files changed in this pass:

- `app/(erp)/retail/dashboard/page.tsx`
- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results so far:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Worker edit-before-manager-review remains the largest unfinished Retail workflow decision.

## 2026-06-25 - Order manager today progress card

Task completed:

- Added a manager/admin-only `Today order progress` card to the `/orders` task home.
- The card shows:
  - Today orders.
  - Need action.
  - Pending delivery.
  - Completed.
  - Completion percentage progress bar.
- Workers still see the simple task home without manager progress/report UI.
- No backend, RLS, migration, order status, stock, picking, or delivery handoff logic was changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - initially blocked by a stale local Next dev server lock; after stopping that repo dev server, rerun passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual QA should confirm manager/admin see the progress card on `/orders` and normal workers do not.

## 2026-06-25 - Order reports separated from worker task home

Task completed:

- Moved Order reports out of the default task-heavy Order UI.
- Added `/orders/reports` as the separate manager/admin reports page using the existing Order data loader and role gate.
- `/orders` still opens with worker-first task actions and task queues.
- Advanced users now see a Reports action card plus an `Open Order Reports` link from `Advanced Order List`.
- `OrdersTableClient` now renders operational records only; the reports card is split into `OrderReportsClient`.
- No backend, RLS, migration, order status, picking, stock, or delivery handoff logic was changed.

Files changed in this pass:

- `app/(erp)/orders/reports/page.tsx`
- `components/orders/orders-page.tsx`
- `components/orders/orders-table-client.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed after Order checks on unrelated Retail coverage: `Retail expense UX must keep submission readiness, receipt guidance, checker handoff, and blocked-submit recovery`.

Risks / remaining checks:

- Manual QA should confirm manager/admin can open `/orders/reports`, while workers keep the simple `/orders` task home.
- Full smoke remains blocked by the unrelated Retail expense guard.

## 2026-06-25 - Retail workflow/mobile refinement pass

Task completed:

- Refined Retail worker Home into shortcut buttons only.
- Added `Picking Order` shortcut to existing `/orders/picking`; no Retail picking rebuild.
- Changed manager Home processing shortcut from review to report-only `Processing Records`.
- Simplified worker Submit Expense to the real required fields only:
  - Category.
  - Amount.
  - Payment method defaulting to Cash.
  - Required receipt upload.
- Removed worker expense date/outlet/supplier/remarks clutter from the visible worker path; date/outlet still submit as hidden/scoped fields.
- Changed expense success state to return to Retail Home only.
- Simplified Complete Cleaning to tap Complete only; no photo or remarks on the worker path.
- Changed missed cleaning badge/status wording to `Missing`; normal incomplete work shows `Pending`.
- Added worker Today Summary cards for today sales amount/status, Cash Closing `Closed` / `Not Closed`, cash expenses, missing cleaning, and processing count.
- Added processing preset buttons, including `Minced Meat`, while keeping manual processing type entry.
- Removed worker processing draft selection and abnormal-yield warning language. Processing now submits immediately and only displays calculated yield/wastage/unaccounted difference.
- Made the shared Retail submit button sticky on mobile so worker task pages keep the main action near the thumb.
- Updated Retail source coverage for the refined workflow decisions.
- Updated `docs/RETAIL_V1_E2E_TEST_RESULTS.md`.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results so far:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full goal still has remaining manager/admin report refinements and live mobile UAT follow-up; this pass only tightened the highest-use worker flow.

## 2026-06-25 - Stock receive-transfer pending list

Task completed:

- Added a receive-transfer pending list using existing `stock_units.transfer_to_location_id`.
- `/stock/receive-transfer` now shows `Pending to this location` for the selected receiving stock location.
- Workers can tap a pending barcode to fill the receive barcode field, or still use camera/external scanner input.
- The receive location still defaults to the logged-in user's assigned stock location and remains editable.
- Added demo transfer destination data so demo mode can show a pending receive item.
- No migration was added.

Files changed in this pass:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live RLS must confirm receiver users can read transfer-pending units where `transfer_to_location_id` equals their assigned location.
- `scripts/smoke-routes.mjs` also had stale non-Stock Retail source guards aligned to current Retail copy so the full smoke suite can run again; no Retail app behavior was changed in this pass.

Next recommended task:

- Continue Stock exception-review evidence by checking manager/admin barcode scan error report usability.

## 2026-06-25 - Order editable unit and picking mismatch prompt

Task completed:

- Finished two small Order UX requirement gaps.
- `/orders/create` order item unit is now editable instead of read-only:
  - Weight
  - Carton
  - Packet
  - Quantity + estimated kg
- Existing line validation still requires estimated kg for stock reservation and quantity for non-weight units.
- `/orders/picking` now shows a clear mismatch proceed prompt after wrong item, wrong brand, or unknown barcode scan errors:
  - Mismatch remains recorded.
  - Worker is told to scan the correct barcode or use Manual Weight with a required reason.
  - Backend behavior remains unchanged: mismatched barcode scans do not add picked weight.
- Updated Order UX source coverage for editable units and the mismatch prompt.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - first attempt failed while `npm.cmd run build` was running in parallel and regenerating `.next/types`; rerun after build passed.
- `npm.cmd run smoke` - failed in unrelated Stock coverage: `Stock worker mobile workflow forms missing: Inbound scan issue logged for manager review.` No Stock files were changed for this Order pass.

Risks / remaining checks:

- Manual QA should confirm editable units still serialize correctly into order lines.
- Manual QA should scan a wrong item/brand barcode and confirm the mismatch prompt is visible and no picked weight is added.

## 2026-06-25 - Stock inbound blocked-scan issue logging

Task completed:

- Reused existing `barcode_scan_logs` as the lightweight issue trail for Stock Inbound blocked scans.
- Added `logInboundScanIssueAction` with existing stock role/module/location checks.
- Barcode Inbound now logs failed scan records when the worker hits:
  - duplicate inbound barcode blocked in the browser,
  - no-weight/barcode-rule failure blocked in the browser.
- No migration and no new issue table were added.

Files changed in this pass:

- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This is not a normalized issue table. Managers see these as failed scan logs / barcode scan error report rows. Add a dedicated issue queue only if the scan-log report is not enough for daily review.
- Live QA still needs to confirm manager/admin report views show these failed scan logs under real RLS.

Next recommended task:

- Continue with receive-transfer pending-list scoping or Stock Take mismatch review evidence, whichever is weakest in live QA.

## 2026-06-25 - Delivery driver visibility for preparing orders

Task completed:

- Added delivery-team visibility for customer delivery orders before final handoff without changing canonical Delivery status semantics.
- Added `getUpcomingOrderDeliveries()` in `lib/delivery/queries.ts`:
  - Reads today delivery-required customer orders in `NEW`, `PREPARING`, or `READY`.
  - Computes picked/estimated weight and progress percentage from `customer_order_items`.
  - Includes customer remarks and delivery address for the driver view.
- Added `DeliveryUpcomingOrder` type in `lib/delivery/types.ts`.
- `/delivery/driver` now loads upcoming order deliveries alongside real available delivery jobs.
- Driver mobile Available tab now shows:
  - `Preparing Orders` read-only section.
  - Picking progress bar and picked/estimated kg.
  - Customer remarks.
  - Disabled `Waiting for Pending Delivery` button.
  - Existing `Pending Delivery` section for accept-ready `AVAILABLE` delivery jobs.
- Driver accept remains possible only through existing `AvailableCard` / `acceptDelivery` path for real `AVAILABLE` deliveries.
- Updated Delivery module coverage to guard the preparing-order visibility and no-early-accept behavior.

Files changed in this pass:

- `app/(erp)/delivery/driver/page.tsx`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `node scripts\delivery-module-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempt failed because another Next build process was already running; rerun passed.

Risks / remaining checks:

- Manual role QA should confirm delivery workers can see scoped preparing delivery orders but still cannot accept them until they become Pending Delivery.
- This pass intentionally avoids adding a new delivery status. Preparing visibility is read-only from customer orders; accept-ready work remains canonical `deliveries.status = AVAILABLE`.

## 2026-06-25 - Stock inbound saved-rule barcode length warning

Task completed:

- Added a Stock Inbound worker warning when the scanned barcode length differs from the saved rule's recent barcode format.
- Kept the change UI-only/client-side: no migration, no new column, no workflow block.
- The warning uses the latest saved stock-unit barcode for the same item + brand + origin when a barcode weight rule already exists.
- Added source coverage so the warning remains guarded.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - failed once on an unrelated Delivery driver prop mismatch, then passed after restoring the existing upcoming-order prop path.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Barcode weight rules do not store a canonical full barcode length. This warning infers expected length from the latest matching saved stock unit; add a rule-level barcode length column only if managers need strict format enforcement.
- Real inbound QA should confirm the warning appears on a saved-rule template with a mismatched barcode length.

Next recommended task:

- Continue Stock Inbound issue-record hardening for barcode rule failures, using the existing scan-log/audit path before adding any new table.

## 2026-06-25 - Order item brand preference and customization

Task completed:

- Added schema-backed order item brand preference and customization support.
- Added migration `202606250001_order_line_brand_customization_v1.sql`:
  - `items.order_customization_options`
  - `items.order_default_customization`
  - `customer_order_items.preferred_brand_id`
  - `customer_order_items.customization`
  - `order_stock_reservations.preferred_brand_id`
  - indexes for preferred-brand lookups.
- `/orders/create` item cards now include:
  - Brand preference selector per line.
  - `No brand preference` option.
  - Available kg shown beside each brand.
  - Clear warning/block when selected brand has no stock.
  - Required grouped customization chips from item-master customization defaults.
- Create action now persists `preferred_brand_id` and `customization` on each order line.
- Reservation rows now keep `preferred_brand_id`.
- Brand-aware availability now checks physical stock minus active reservations for the selected brand path.
- Barcode picking now blocks and logs a mismatch when an order line has a preferred brand and staff scans another brand.
- Order detail and picking item cards now show brand preference and customization chips so picker/driver-facing context is visible.
- Added source coverage for brand/customization UI, action persistence, mismatch handling, and migration fields.

Files changed in this pass:

- `supabase/migrations/202606250001_order_line_brand_customization_v1.sql`
- `lib/orders/types.ts`
- `lib/orders/data.ts`
- `lib/orders/actions.ts`
- `components/orders/orders-forms.tsx`
- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - initially failed while `npm.cmd run build` was running in parallel and regenerating `.next/types`; rerun after build passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Fresh Supabase migration reset was not run in this pass.
- Item-master UI for editing customization options/defaults is not built here; the migration provides persisted item-master fields and defaults.
- Delivery-team visibility immediately after order creation, before Pending Delivery acceptance, still needs a dedicated Delivery/Orders pass.
- Manual browser QA should create an order with preferred brand, create one with no brand preference, verify zero-stock brand warning, pick matching brand, and verify wrong-brand scan records mismatch without adding picked weight.

## 2026-06-25 - Stock direct sales customer selection finalization

Task completed:

- Continued the Stock worker-speed pass without adding a migration.
- `/stock/outbound` now keeps Stock outbound direct-only and requires a customer before direct `Sales` scanning starts.
- Direct `Sales` shows quick customer buttons and a customer dropdown before the barcode scanner.
- The worker sees the short blocked message `Choose customer first.` until a customer is selected.
- Server-side direct outbound confirmation validates the selected customer exists and is active before confirming `SALES`.
- Successful direct sales outbound writes the selected customer name into the existing movement notes, avoiding a schema change for this pass.
- Kept customer-order picking routed to `/orders/picking`.
- Fixed narrow compile blockers from the existing Orders brand/customization fields so the full app build passes.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `lib/orders/actions.ts`
- `lib/orders/data.ts`
- `components/orders/orders-page.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - failed on existing Orders brand/customization type mismatches, then passed after narrow compile fixes.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed again after compile fixes.

Risks / remaining checks:

- Real authenticated Vercel QA still needs to confirm the customer list is available under the intended worker/admin roles and RLS scopes.
- Customer traceability for direct stock sales is currently stored in movement notes, not a structured stock-outbound customer column. Add a dedicated column later only if reports need customer filtering.
- Real phone scanning and Bluetooth/PDF label QA remain manual owner/device checks.

Next recommended task:

- Test `/stock/outbound` on Vercel as a stock worker: select `Sales`, confirm scanning is blocked until a customer is chosen, choose a customer, scan multiple barcodes, confirm outbound, then verify the movement notes show the customer.

## 2026-06-25 - Order price-required flow refinement

Task completed:

- Refined the Order create/picking/ready path toward the requested mobile ordering flow without adding a new order status or migration.
- `/orders/create` no longer asks workers for final total price. It saves a confirmed order with `total_order_price = 0` as the internal placeholder so existing database constraints keep working.
- Picking completion now moves orders to existing status `READY`, treated by the UI as `Price Required`.
- Added final total price entry after picking:
  - Any staff passing existing Order module access can enter final price through the new `FinalOrderPriceForm`.
  - Pickup orders move from `READY` to `READY_FOR_PICKUP`.
  - Delivery orders move from `READY` to `READY_FOR_DELIVERY` and create/link the Delivery job only after price is saved.
- Blocked the manual delivery-job creation action until the order has final price and is `READY_FOR_DELIVERY`.
- Added `Price Required` task card on Order Home and a `Price Required` section on `/orders/ready`.
- Updated order detail alerts and card actions so `READY` with missing price is shown as `Price Required`, not as pickup/delivery ready.
- Hid pre-picking total price editing from the edit form and preserved the existing value via hidden field.
- Changed worker-facing delivery wording from `Ready for Delivery` to `Pending Delivery` while keeping the existing stored status.
- Hid customer credit warning badges from worker/default customer cards; manager/admin still see them.
- Updated advanced order tables to show `Price required` instead of `RM 0.00` when final price has not been entered.
- Updated Order UX source coverage to guard the new price-required flow.

Files changed in this pass:

- `lib/orders/actions.ts`
- `components/orders/orders-forms.tsx`
- `components/orders/orders-page.tsx`
- `components/orders/orders-table-client.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None. Existing `READY` status is used as the price-required queue when `total_order_price <= 0`.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual browser QA should create pickup and delivery orders, pick them, confirm they enter `Price Required`, enter final price, then verify pickup becomes Ready for Pickup and delivery appears in the Delivery available flow.
- Brand/manufacturer preference and item-master-managed customization were not implemented in this pass because current order-line persistence has no brand/customization fields. That should be a separate schema-backed change, not a UI-only fake.
- Delivery-team progress visibility immediately after order creation still needs a dedicated pass if required beyond the existing Delivery handoff after final price.

## 2026-06-25 - Stock outbound direct-only worker flow and inbound rule learning

Task completed:

- Continued the Stock Module worker-speed pass.
- Kept the work scoped to Stock workflow code, Stock QA docs, and source guards.
- Barcode Inbound can now infer a 5-digit imported barcode weight position after the worker scans a supplier barcode and manually enters the actual kg once.
- When the inferred rule is clear, Inbound fills the rule fields and turns on `Save weight rule for future scans`; future same-format scans can reuse the saved item + brand + origin rule.
- Stock Outbound now opens as direct stock outbound only.
- Customer-order picking is routed to `/orders/picking` through an `Open Orders picking` link instead of showing order-picking controls inside `/stock/outbound`.
- Direct Sales now requires a customer from the loaded customer list before scanning and still starts with a default `Direct sales stock out` remark to reduce typing.
- Stock owner/Vercel QA docs and source guards were updated to test the new direct-only Stock outbound split.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/barcode-weight.ts`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `scripts/delivery-module-coverage.mjs`
- `scripts/order-ux-simplification-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts\stock-workflow-regression.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - initially failed when run before/alongside build because `.next/types` was stale; rerun after build passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated phone QA is still needed around 390px width for `/stock/inbound`, `/stock/outbound`, `/orders/picking`, and label printing.
- Bluetooth printer behavior still needs owner/device testing; PDF fallback remains the safe browser fallback.
- The Stock page no longer exposes order-picking controls, but the older order-picking-related helper code remains dormant inside the component to avoid a larger unrelated cleanup.
- Direct Sales customer traceability uses the current movement notes field instead of a new column; add a dedicated stock outbound customer column only when reporting needs structured customer filtering.
- Two non-Stock coverage guards were stale from previous workflow changes and were updated without app behavior changes: delivery handoff is now checked after final price, and Orders create-flow text now matches the current create-then-price-after-picking workflow.

Next recommended task:

- Test `/stock/outbound` and `/orders/picking` on Vercel with one worker account: confirm direct stock-out is simple in Stock, and customer-order picking remains usable in Orders.

## 2026-06-25 - Delivery expense review queue refinement

Task completed:

- Refined the Delivery expense review screen toward a clearer manager/admin pending-review workflow.
- Kept the work scoped to Delivery expense review UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, driver expense submission behavior, receipt upload behavior, expense status rules, delivery status transitions, OA/claim wiring, or role/module restrictions.
- Added an `Expense review queue` guide above the expense list:
  - Shows the first pending expense to start with.
  - Shows driver, vehicle, claim type, and amount context.
  - Reminds reviewers to open receipt proof before approve/reject.
  - Shows an all-clear state when no pending expenses match the current filters.
- Added `ExpenseReviewReadiness` inside each pending expense review form:
  - Receipt checked
  - Scope checked
  - Decision selected
- Added blocked-review recovery guidance for receipt, scope, rejection reason, outlet, and delivery team review blocks.
- Made the approve/reject buttons full-width on mobile and made reject reason required only when the reviewer selects `REJECTED`, matching the existing server action requirement.
- Added source coverage guards in both route smoke and Delivery module coverage for the new pending-first guide, standalone V1 review timeline, receipt/scope checks, and error recovery copy.

Files changed in this pass:

- `components/delivery/delivery-expense-review-page.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery/expenses` around 390px for no expenses, no pending expenses, one pending expense, multiple pending expenses, receipt link opening, linked delivery opening, approve, reject with reason, reject without reason, and scope-blocked review.
- Real role/scope QA should confirm delivery managers can review only their outlet/team expenses while admin scope behaves as expected.
- This pass is UI guidance/client-side state only; final pending-only review, rejection reason validation, outlet/team scope enforcement, status update persistence, and audit logging remain enforced by existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Order fast order creation clarity or Delivery driver expense submission readiness.

## 2026-06-25 - Retail expense review timeline refinement

Task completed:

- Refined the Retail expense review workflow toward a clearer manager/admin review queue.
- Kept the work scoped to Retail expense review UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, receipt upload behavior, expense status rules, reviewer separation, cash closing behavior, payment methods, or role/module restrictions.
- Added an `Expense review guide` above the submitted expense queue:
  - Shows the first submitted expense to start with.
  - Shows submitted-by and outlet context.
  - Reminds reviewers to open receipt proof first.
  - Shows an all-clear state when no submitted expenses need review.
- Added an `ExpenseReviewReadiness` timeline inside the review form:
  - Expense selected
  - Receipt opened
  - Different checker
  - Decision selected
- Added a direct `Open selected receipt` action for the selected expense.
- Made rejection reason required in the client form only when the reviewer selects `REJECTED`, matching the existing server action requirement.
- Added blocked-review recovery guidance for missing selection, missing receipt review, missing rejection reason, and same-manager review blocks.
- Added route smoke coverage guards for the new review guide, timeline, selected receipt action, different-checker warning, and recovery copy.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - failed once on JSX literal `>` characters in the new timeline copy; fixed by escaping them.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/expenses/review` around 390px for no submitted expenses, one submitted expense, multiple submitted expenses, receipt link opening, approve, reject with reason, reject without reason, cancel, and same-manager review blocked by server action.
- Real role/scope QA should confirm retail managers can review only their intended outlet scope while admin/director scope behaves as expected.
- This pass is UI guidance/client-side state only; final submitted-only review, different-checker rule, rejection reason validation, status update persistence, and audit logging remain enforced by existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Delivery expense review clarity or Order fast order creation clarity.

## 2026-06-25 - Retail expense submission readiness refinement

Task completed:

- Refined the Retail expense submission form toward a clearer mobile outlet worker flow.
- Kept the work scoped to Retail expense UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, receipt upload behavior, expense approval rules, payment methods, cash closing behavior, or role/module restrictions.
- Added an `ExpenseSubmissionReadiness` guide above the expense fields:
  - Outlet selected
  - Expense category selected
  - Amount entered
  - Receipt checked
  - Payment method checked
  - Different checker next
- Added a blocked-submit recovery panel so staff know to check category/amount, attach receipt proof, and submit again if the server action rejects the form.
- Made the expense outlet choice controlled and required for admin/director users on this form so the readiness guide accurately shows whether the expense has an outlet before submit.
- Preserved existing receipt proof requirement, category fallback entry, payment method options, supplier/remarks details, success actions, and manager/admin/director review separation.
- Added route smoke coverage guards for the new expense readiness, checker handoff, receipt guidance, and recovery copy.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/expenses` around 390px for assigned outlet users, admin/director outlet selection, missing category, zero amount, missing receipt, each payment method, successful submit, and failed submit recovery.
- Real role/scope QA should confirm outlet staff can submit only within their assigned outlet while admin/director users must choose the intended outlet before submitting.
- This pass is UI guidance/client-side state only; final receipt requirement, expense validation, reviewer separation, upload behavior, and persistence remain enforced by existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Retail expense review clarity or Delivery expense review clarity.

## 2026-06-25 - Delivery manager exception priority refinement

Task completed:

- Refined the Delivery manager dashboard issue-clearing guide toward an action-first manager workflow.
- Kept the work scoped to Delivery manager UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, delivery status transitions, proof upload behavior, GPS/address suggestion behavior, failed-return behavior, expenses, or role/module restrictions.
- Added a priority action panel inside `DeliveryIssueReviewGuide`:
  - Shows `Start here` for the first non-empty issue bucket.
  - Shows clear steps for opening the first issue, reviewing proof/note, recording follow-up, and continuing the next bucket.
  - Shows an all-clear state when there are no failed proof, GPS/address, late, or slow delivery issues.
- Preserved the existing failed proof, GPS/address, late/slow issue buckets, issue counts, failed-return reminder, pending GPS/address reminder, issue lists, address suggestion review, filters, KPI cards, delivery list, and driver workload cards.
- Added route smoke coverage guards for the new delivery manager priority and all-clear guidance.

Files changed in this pass:

- `components/delivery/manager-delivery-dashboard.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery` around 390px for no issues, failed proof, GPS unavailable, address suggestion, late delivery, slow delivery, and mixed issue buckets.
- Real role/scope QA should confirm delivery managers/admins retain dashboard access while routine driver proof/status flows remain on the driver page.
- This pass is UI guidance only; failed-return follow-up, address/GPS suggestion approval, delivery status transitions, and proof validation remain enforced by existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Retail expense request readiness or Delivery expense review clarity.

## 2026-06-25 - Retail Daily Sales readiness refinement

Task completed:

- Refined the Retail Daily Sales workflow toward a clearer mobile AutoCount summary entry flow.
- Kept the work scoped to Retail Daily Sales UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, sales calculations, cash closing behavior, expense behavior, or role/module restrictions.
- Added a `DailySalesReadiness` guide above the Daily Sales fields:
  - Outlet selected
  - AutoCount totals entered
  - Payment split checked
  - Next step ready
- Added a clear zero-total warning so staff know to check AutoCount before saving.
- Made the Daily Sales outlet choice controlled for admin/director users so the readiness guide accurately shows whether an outlet has been selected.
- Preserved the existing cash, bank transfer, e-wallet, credit, calculated total, optional AutoCount attachment, optional remarks, and Go to Cash Closing success action.
- Added route smoke coverage guards for the new Daily Sales readiness and controlled outlet guidance.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/sales` around 390px for assigned outlet users, admin/director outlet selection, zero totals, cash-only totals, multiple payment totals, optional AutoCount attachment, successful save, and Go to Cash Closing next action.
- Real role/scope QA should confirm workers still cannot create Daily Sales when manager-controlled access applies, while retail managers/admin/directors retain intended access.
- This pass is UI guidance/client-side state only; final Daily Sales validation, same-day operator limits, duplicate-date behavior, and persistence remain enforced by existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Retail expense request readiness or Delivery manager exception clearing.

## 2026-06-25 - Retail cash closing readiness refinement

Task completed:

- Refined the Retail Cash Closing workflow toward a clearer mobile daily close flow for outlet managers/admin/directors.
- Kept the work scoped to Retail Cash Closing UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, cash calculations, daily sales behavior, expense behavior, review rules, or role/module restrictions.
- Added a `CashClosingReadiness` guide above the closing fields:
  - Outlet selected
  - Daily sales found
  - Cash counted
  - Variance checked
- Added a clear Daily Sales fallback action when no sales summary is found for the selected outlet/date.
- Preserved the existing Expected Cash formula, pulled cash/non-cash sales references, cash expenses, variance display, variance warning, status options, remarks field, and manager-controlled access copy.
- Added route smoke coverage guards for the new cash closing readiness and fallback guidance.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/cash-closing` around 390px for assigned outlet, no daily sales summary, daily sales present, zero variance, non-zero variance with remarks, draft save, submitted save, and manager-reviewed status where allowed.
- Real role/scope QA should confirm retail workers still cannot submit daily cash closing and retail managers/admin/directors retain the intended access.
- This pass is UI guidance only; final cash closing validation, same-day operator limits, reviewer separation, and variance persistence remain enforced by existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Retail Daily Sales readiness or Delivery manager exception clearing.

## 2026-06-25 - OA request status guide refinement

Task completed:

- Refined the OA Actions worker request status experience toward the requested approval-timeline workflow.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval rules, payment rules, payslip upload behavior, or role/module restrictions.
- Added a worker-facing `WorkerRequestStatusGuide` above the request timelines on the worker dashboard and My Requests page:
  - Shows waiting, approved/paid, and stopped request counts.
  - Highlights the newest request first.
  - Tells the worker who reviews or pays next.
  - Provides a large Submit new request action.
- Preserved the existing Submitted -> Manager Review -> Admin Review -> Director Approval -> Approved/Rejected timeline, worker fast-path buttons, success/error next-step guidance, reviewer queue guides, and worker-only/reviewer data separation.
- Added route smoke coverage guards for the new OA request status guide.

Files changed in this pass:

- `components/oa-actions/oa-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/dashboard` and `/oa-actions/my-requests` around 390px for no requests, submitted advance, submitted claim, manager-reviewed claim, admin-reviewed request, director-approved request pending payment, approved leave, rejected request, and paid request.
- Real role/scope QA should confirm workers still see only their own requests and payslips, while managers/admin/director/account retain their review and control queues.
- This pass is UI guidance only; approval transitions, payment state, payslip visibility, and review permissions remain enforced by existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Delivery manager exception clearing or Retail cash closing readiness guidance.

## 2026-06-25 - Processing submit readiness refinement

Task completed:

- Refined the Retail Processing / Packing submit step toward the requested worker-first mobile flow.
- Kept the work scoped to Retail Processing UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, yield calculations, wastage calculations, processing review behavior, barcode inbound behavior, or role/module restrictions.
- Added a worker-facing `ProcessingSubmitReadiness` panel in the final Review and Submit section:
  - Shows whether processing is ready to submit or still missing required weights.
  - Shows raw weight entered, finished weight entered, wastage checked, and yield calculated checks.
  - Reminds workers to pack, label, and inbound finished stock after saving.
- Preserved the existing guided steps, manual/BOM processing type, multi-line raw and finished item entry, wastage photo/remarks, calculated totals, abnormal-yield alert-only warning, error next-step guide, and barcode inbound reminder.
- Added route smoke coverage guards for the new processing readiness and submit guidance.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/processing` and `/processing/dashboard` around 390px for empty weights, raw-only, finished-only, normal yield, abnormal yield below 85%, successful submit, and server-side validation errors.
- Real workflow QA should confirm staff still understand that finished goods require barcode inbound before sale.
- This pass is UI guidance only; final processing validation, review status, yield warnings, and barcode inbound remain enforced by existing server actions and Stock workflows.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as OA request history clarity or Delivery manager exception clearing.

## 2026-06-25 - Attendance clock readiness refinement

Task completed:

- Refined the Attendance clock workflow toward the requested big Clock In / Clock Out worker experience.
- Kept the work scoped to Attendance UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, GPS radius rules, late grace rules, no-clock-out rules, department review, settings, or role/module restrictions.
- Added a worker-facing clock readiness guide:
  - Clock In / Clock Out selected
  - Work location selected
  - GPS or manual location ready
- Added a clear blocked setup state when no active work locations are available.
- Disabled submit until the worker has selected a work location and captured or typed GPS coordinates, matching the existing required server-side data.
- Preserved the existing big Clock In / Clock Out buttons, Use Current Location button, manual location fallback, success next-step guide, error next-step guide, My Attendance links, and manager-only department/settings split.
- Added route smoke coverage guards for the new attendance readiness and disabled-submit guidance.

Files changed in this pass:

- `components/attendance/attendance-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/attendance/clock` around 390px for Clock In, Clock Out, missing work location, GPS denied, manual latitude/longitude, successful clock in, and successful clock out.
- Real phone QA should verify browser GPS permission prompts, location capture accuracy, and the 50m radius warning path.
- Real Supabase role/scope QA should confirm workers still see only Clock/My Attendance and managers/admin retain department/settings controls.
- This pass is UI guidance/client-side blocking only; final attendance validation, GPS radius, late status, and no-clock-out handling still depend on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Delivery proof upload error guidance or Processing packing next-action guidance.

## 2026-06-25 - Cleaning today focus refinement

Task completed:

- Refined the Cleaning worker task page toward the requested simple tap-to-complete workflow.
- Kept the work scoped to Cleaning/Retail UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, completion timing, late-cleaning rules, photo requirements, manager setup, reports, or role/module restrictions.
- Added a worker-facing `Do this first` guide above the cleaning task cards:
  - Shows the first task to complete.
  - Shows late and due-today counts.
  - Reminds workers that no photo is needed.
- Added a stronger all-clear state with next actions when all required cleaning is complete:
  - Return home
  - Check later
  - Tell manager if asked
- Preserved the existing late-first sorting, one large Complete Cleaning button, optional remarks/photo panel, success next-step guide, error next-step guide, and all-required-cleaning-done behavior.
- Added route smoke coverage guards for the new cleaning focus and all-clear guidance.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/cleaning/tasks` and `/retail/cleaning` around 390px with late tasks, due-today tasks, and no remaining tasks.
- Real Supabase role/scope QA should confirm workers still see only assigned outlet/department cleaning tasks and managers retain setup/history access.
- This pass is UI guidance only; task completion, late status, and cleaning completion-rate behavior still depend on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Attendance blocked/error guidance or Delivery proof upload error guidance.

## 2026-06-25 - Delivery driver next-action refinement

Task completed:

- Refined the Delivery driver mobile workflow toward the requested one-big-action driver experience.
- Kept the work scoped to Delivery driver UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, delivery status transitions, proof upload behavior, GPS behavior, failed-return behavior, expenses, manager review, or role/module restrictions.
- Added a clear current-action panel on every driver delivery card:
  - Accept this job
  - Tap Loaded
  - Start Delivery
  - Upload proof photo
  - Check next job
  - Tell manager
- Added clearer empty-tab guidance for Available, My Deliveries, Completed, Failed, and Expenses, including next action chips for drivers.
- Preserved the existing Today delivery list, Google Maps/Call/WhatsApp quick links, one-tap accept, Loaded, Start Delivery, Delivered proof, Failed proof, address issue, GPS suggestion, and expense receipt flows.
- Added route smoke coverage guards for the new driver next-action and empty-state guidance.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery/driver` around 390px for Available, My Deliveries, Completed, Failed, and Expenses tabs.
- Real phone QA should verify camera proof upload, GPS denied/unavailable behavior, WhatsApp/tel/map links, and receipt photo upload.
- Real Supabase role/scope QA should confirm drivers still see only allowed today jobs and cannot access manager/account finance details.
- This pass is UI guidance only; status transitions, proof save, failed return follow-up, and address/GPS suggestion behavior still depend on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Delivery proof upload error guidance or admin/account setup empty states.

## 2026-06-25 - Order create blocked-state refinement

Task completed:

- Refined the Order create workflow blocked and empty guidance for worker-first mobile order entry.
- Kept the work scoped to Order UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, stock reservation logic, customer creation logic, order pricing logic, delivery handoff behavior, or role/module restrictions.
- Added an always-visible blocked checklist before final submit when the order is not ready:
  - Customer ready
  - Order type ready
  - Items ready
  - Price ready
  - Location ready
- Replaced the final-step plain blocked message with the same checklist so staff can see the missing step before trying again.
- Added clear item setup and item search empty states when no active order items are available or no item matches the search/filter.
- Preserved the existing staged order create flow, quick customer add, recent item templates, total order price requirement, Manual ERP confirmation behavior, and immediate reservation behavior.
- Added route smoke coverage guards for the new order create blocked and item empty-state guidance.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/orders/create` around 390px for missing customer, missing order type, missing item kg/quantity, missing total price, delivery address missing, pickup location missing, and internal transfer same-location states.
- Real Supabase role/scope QA should confirm order creation still respects outlet, department, stock location, customer, active item, and role restrictions.
- This pass is UI guidance only; reservation and stock-short behavior still depends on existing order server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as Order ready/delivery handoff empty states or admin/account setup empty states.

## 2026-06-25 - OA payslip upload error refinement

Task completed:

- Refined the OA Actions payslip upload failure path for account/admin control screens.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, payslip storage behavior, employee visibility, worker request routing, payment behavior, or role/module restrictions.
- Added an account/admin-facing failed-upload guide after a payslip upload error:
  - Select employee
  - Check payroll month
  - Attach file again
- Added clear copy explaining that the payslip is not saved yet and staff should check employee, payroll month, pay numbers, and file before uploading again.
- Made payslip upload fields use the same larger mobile-friendly input sizing as the worker request forms.
- Preserved the existing payslip role split, employee select, pay fields, file upload, Save payslip action, staff own payslip view, and director/all-view behavior.
- Added route smoke coverage guards for the new payslip upload error guide.

Files changed in this pass:

- `components/oa-actions/oa-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/payslip` around 390px for account/admin successful upload, missing employee, missing period, missing file, and server-side validation errors.
- Real Supabase role/scope QA should confirm only account/admin can upload payslips, staff see only their own payslips, and directors retain all-view access without routine upload permissions.
- This pass is UI guidance only; real payslip file upload, storage, and access still depends on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as admin/account setup empty states or Order create blocked-state guidance.

## 2026-06-25 - Director approval clear-state refinement

Task completed:

- Refined the Director dashboard and approvals queue empty state toward the requested all-in-one company overview workflow.
- Kept the work scoped to Director Reports UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, director approval routing, finance calculations, OA workflow, retail expense workflow, or role/module restrictions.
- Added a director-facing no-approval guide when OA, finance, and outlet expense decisions are clear:
  - Check operations
  - Check finance aging
  - Save/share report
- Added clear copy telling directors to continue with the company overview, finance aging, and report snapshot when no approvals are waiting.
- Preserved the existing company overview, alerts-first panel, module links, KPI cards, pending approval table, activity focus, report snapshots, CSV export, WhatsApp summary, and director approval forms.
- Added route smoke coverage guards for the new Director approval clear-state guide.

Files changed in this pass:

- `components/director/director-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/director-reports/dashboard` and `/director-reports/approvals` around 390px and desktop widths with pending approvals and with an empty approval queue.
- Real Supabase role/scope QA should confirm only director/admin profiles can access director reports and that underlying OA, finance, and retail approval visibility remains unchanged.
- This pass is UI guidance only; real approval counts, report snapshots, finance aging, and module links still depend on existing data loaders, server actions, and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as payslip upload blocked/error guidance or admin/account setup empty states.

## 2026-06-25 - Delivery manager issue guide refinement

Task completed:

- Refined the Delivery manager dashboard issue-review area toward the requested role-based manager workflow.
- Kept the work scoped to Delivery manager UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, delivery proof behavior, failed-return behavior, address/GPS approval behavior, expenses, or role/module restrictions.
- Added a manager-facing `Delivery issues to clear today` guide before the issue lists:
  - Failed proof
  - GPS/address
  - Late or slow
- Added clear empty/pending copy explaining when no delivery issues need follow-up and reminding managers that failed deliveries need stock return follow-up.
- Preserved the existing failed delivery, GPS unavailable, address/GPS suggestion, late delivery, slow delivery, KPI, filter, driver performance, and dashboard delivery list sections.
- Added route smoke coverage guards for the new Delivery manager issue guide.

Files changed in this pass:

- `components/delivery/manager-delivery-dashboard.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery` around 390px and desktop widths for delivery manager, admin, and director-like view-only profiles with issue and no-issue data.
- Real Supabase role/scope QA should confirm managers still see only allowed delivery team/outlet issues and address/GPS suggestions.
- This pass is UI guidance only; real proof upload, failed-return status, address/GPS suggestion approval, and delivery issue counts still depend on existing server actions, queries, and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as payslip upload blocked/error guidance or director overview empty/alert states.

## 2026-06-25 - OA Actions request error next-step refinement

Task completed:

- Refined the OA Actions worker request failure path toward the requested guided worker workflow.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval routing, reviewer permissions, payment behavior, payslip behavior, or role/module restrictions.
- Added a worker-facing failed-submit guide after an advance, claim, or leave request error:
  - Check request type
  - Fix missing details
  - Submit request again
- Added clear copy explaining that the request is not saved yet and workers should fix the missing type, amount, date, or attachment before submitting again.
- Preserved the existing request fast path, large mobile controls, success next-step guide, reviewer queue guide, selected request next action, and approval timeline.
- Added route smoke coverage guards for the new OA worker request error next-step guide.

Files changed in this pass:

- `components/oa-actions/oa-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/advance`, `/oa-actions/claim`, and `/oa-actions/leave` around 390px for successful submit, missing required fields, attachment issues, and server-side validation errors.
- Real Supabase role/scope QA should confirm worker-only profiles still see only their own OA request history and elevated users keep reviewer/payment controls.
- This pass is UI guidance only; real request save, review routing, payment status, payslip upload, and role/module access still depend on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as delivery manager issue empty states or payslip upload blocked/error guidance.

## 2026-06-25 - Retail processing error next-step refinement

Task completed:

- Refined the Retail Processing / Packing worker failure path toward the requested guided worker workflow.
- Kept the work scoped to Retail Processing UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, yield/loss calculations, abnormal-yield behavior, barcode inbound behavior, review routing, or role/module permissions.
- Added a worker-facing failed-submit guide after a processing submit error:
  - Check raw and finished weights
  - Check processing type
  - Submit processing again
- Added clear copy explaining that processing is not saved yet and workers should fix the missing type, item, or weight before submitting again.
- Preserved the existing step headers, raw/finished/wastage sections, larger numeric controls, yield and wastage calculations, alert-only abnormal yield warning, success actions, and barcode inbound reminder after packing.
- Added route smoke coverage guards for the new Retail Processing error next-step guide.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/processing` and `/processing/dashboard` around 390px for successful processing, missing required fields, abnormal yield, and server-side validation errors.
- Real Supabase role/scope QA should confirm retail/processing workers still see only allowed outlet/team processing records.
- This pass is UI guidance only; real processing save, yield/loss math, review status, and finished-stock barcode inbound behavior still depends on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as delivery manager issue empty states or OA worker request blocked/error states.

## 2026-06-25 - Cleaning completion error next-step refinement

Task completed:

- Refined the Cleaning tap-to-complete worker failure path toward the requested simple guided worker workflow.
- Kept the work scoped to Cleaning/Retail Cleaning UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, cleaning frequency rules, late-cleaning behavior, photo requirements, manager reports, or role/module permissions.
- Added a worker-facing failed-submit guide after a cleaning completion error:
  - Check the task
  - Add remarks if needed
  - Tap Complete Cleaning again
- Added clear copy explaining that cleaning is not saved yet and workers should fix the blocked task or remarks before submitting again.
- Preserved the existing late-first ordering, big `Tap Complete Cleaning` button, optional remarks/photo panel, no-photo-required V1 copy, success next-step guide, Complete another task action, and Home link.
- Added route smoke coverage guards for the new Cleaning error next-step guide.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/cleaning/tasks` and `/retail/cleaning` around 390px for successful completion, late task completion, no tasks remaining, and server-side validation errors.
- Real Supabase role/scope QA should confirm workers still see only allowed cleaning tasks for their outlet/team scope.
- This pass is UI guidance only; real cleaning completion, late status, frequency scheduling, and reporting behavior still depends on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as retail processing/packing empty/error states or delivery manager issue empty states.

## 2026-06-25 - Attendance clock error next-step refinement

Task completed:

- Refined the Attendance Clock In / Clock Out worker failure path toward the requested big-button guided worker workflow.
- Kept the work scoped to Attendance Clock UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, GPS validation, attendance rules, location radius rules, manager status controls, or role/module permissions.
- Added a worker-facing failed-submit guide after a Clock In / Clock Out error:
  - Check Clock In or Clock Out
  - Capture location again
  - Ask manager if still blocked
- Added clear copy explaining that attendance is not saved yet and workers should fix the action, location, or work location before submitting again.
- Preserved the existing big Clock In / Clock Out buttons, GPS capture button, manual location fallback, success next-step guide, My Attendance link, and Home link.
- Added route smoke coverage guards for the new Attendance Clock error next-step guide.

Files changed in this pass:

- `components/attendance/attendance-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/attendance/clock` around 390px for successful Clock In, successful Clock Out, GPS permission denied, missing work location, and server-side validation errors.
- Real phone GPS QA should confirm location permission prompts and manual latitude/longitude fallback remain usable.
- This pass is UI guidance only; real attendance save, late status, location radius, no-clock-out, and manager status behavior still depends on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as cleaning blocked/error states or retail processing/packing empty states.

## 2026-06-25 - Manager activity empty-state refinement

Task completed:

- Refined the shared Home manager dashboard toward the requested today-team-activity and task-completion workflow.
- Kept the work scoped to Dashboard Home UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, role/module access rules, manager scopes, attendance logic, cleaning logic, delivery logic, processing logic, or OA approval behavior.
- Added a clear empty state for the `Today team activity` panel when a manager profile has no visible team shortcuts:
  - Confirm module access
  - Check team in person
  - Ask admin if missing
- Added manager-facing copy explaining that no visible team screens are available for today and what to do next.
- Preserved the existing manager today board, routine steps, scope line, priority action cards, team issue watch list, task completion sweep, and completion checklist.
- Added route smoke coverage guards for the new manager activity empty-state guide.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/home` and `/dashboard` around 390px and desktop widths for retail manager, delivery manager, and processing manager profiles with full and limited module access.
- Real Supabase role/scope QA should confirm managers still see only allowed team/outlet/department/module shortcuts.
- This pass is UI guidance only; real activity counts, missing-task state, approvals, delivery issues, attendance gaps, and processing yield alerts still depend on each module's existing data and access policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as admin setup/blocked-state guidance or module-specific manager dashboard empty states.

## 2026-06-25 - Account/admin queue clear refinement

Task completed:

- Refined the Accounting/Finance dashboard operational review board for account/admin staff.
- Kept the work scoped to Accounting/Finance dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval routing, invoice payment behavior, OA payment behavior, aging calculations, container status logic, or role/module permissions.
- Added a compact account/admin queue-clear guide when no urgent review or payment cards are present:
  - Reviews clear
  - Payments clear
  - Check aging next
- Added account/admin-facing copy that staff should check aging and container follow-up before normal finance reports when urgent review/payment cards are clear.
- Preserved the existing operational queue cards, daily control order, finance navigation links, OA claim/advance links, AR/AP links, and container follow-up link.
- Added route smoke coverage guards for the new queue-clear guide.

Files changed in this pass:

- `components/finance/finance-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/accounting-finance/dashboard` around 390px and desktop widths for account/admin/director profiles with urgent, warning, and clear queues.
- Real Supabase role/scope QA should confirm finance users still see only permitted OA, invoice, aging, and container actions.
- This pass is UI guidance only; real invoice review, payment release, OA payment, aging, and container behavior still depends on existing loaders, server actions, and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as manager dashboard empty/error states or admin setup/blocked-state guidance.

## 2026-06-25 - Director alert handling refinement

Task completed:

- Refined the Director all-in-one dashboard alert column toward the requested alert-first company overview workflow.
- Kept the work scoped to Director dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, report calculations, approval routing, finance behavior, or role/module permissions.
- Added a compact director alert handling guide:
  - Find urgent exception
  - Open owner module
  - Record follow-up
- Added director-facing copy reminding directors to clear urgent exceptions before reading normal KPI reports.
- Preserved the existing all-in-one company overview, alerts-first badge, director daily review order, alert cards, module links, approvals, finance/accounting separation, reports, export/share toolbar, and print/PDF-ready surfaces.
- Added route smoke coverage guards for the new director alert handling guide.

Files changed in this pass:

- `components/director/director-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/director-reports/dashboard` around 390px and desktop widths for director/admin profiles.
- Real Supabase role/scope QA should confirm only admin/director roles can access Director reports and that finance/accounting data remains restricted.
- This pass is UI guidance only; real KPI values, approval counts, finance exposure, report snapshots, and print/share behavior still depend on existing loaders, server actions, and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as admin/account control empty states or manager dashboard empty/error states.

## 2026-06-24 - OA Actions reviewer empty queue refinement

Task completed:

- Refined the OA Actions reviewer empty queue state toward the requested manager/admin approval workflow.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval routing, payment transitions, payslip behavior, or role/module permissions.
- Replaced the plain empty selected-request message with a compact empty queue guide:
  - Queue clear
  - Check timeline later
  - Return to dashboard
- Added reviewer-facing copy explaining that nothing needs review right now and new staff requests will appear when the current role is the next reviewer.
- Preserved the existing reviewer guide, approval timeline, selected request next action, worker request fast path, post-submit guidance, and manager/admin/director queue separation.
- Added route smoke coverage guards for the new empty queue copy and steps.

Files changed in this pass:

- `components/oa-actions/oa-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/dashboard` around 390px for manager/admin/director profiles with empty and non-empty review queues.
- Real Supabase role/scope QA should confirm reviewers/directors/accounts still see only actions allowed by existing policies and server actions.
- This pass is UI guidance only; real approval routing, rejection, director approval, payment, and payslip behavior still depends on existing server actions and data.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as director overview alert guidance or admin/account control empty states.

## 2026-06-24 - Delivery proof blocked next-step refinement

Task completed:

- Refined the Delivery driver blocked proof state toward the requested one-big-action-at-a-time driver workflow.
- Kept the work scoped to Delivery driver UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, delivery status transitions, proof validation, GPS behavior, failed-return behavior, or finance/payment exposure.
- Added a visible proof-blocked guide for accepted or loaded deliveries before proof upload is allowed:
  - Accepted: Accept done, Tap Loaded, Start Delivery, Proof buttons appear
  - Loaded: Loaded done, Tap Start Delivery, Visit customer, Proof buttons appear
- Added driver-facing copy that proof photos are locked until Start Delivery and that Delivered/Failed proof buttons appear after Start Delivery.
- Preserved the existing accept flow, load/start delivery buttons, address issue flow, proof photo camera flow, failed reason flow, GPS attempt, proof success next-step cards, and no-finance driver surface.
- Added Delivery module and route smoke coverage guards for the new blocked-proof guide.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery/driver` around 390px for Accepted, Loaded, Out for Delivery, Delivered, and Failed jobs.
- Real phone QA should confirm the blocked guide disappears after Start Delivery and proof camera buttons still open correctly.
- Real Supabase role/scope QA should confirm delivery users still cannot see finance/accounting data and proof upload remains server-blocked until `OUT_FOR_DELIVERY`.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as manager approval queue empty states or director overview alert guidance.

## 2026-06-24 - Order create error next-step refinement

Task completed:

- Refined the Order create worker failure state toward the requested big-button guided worker flow.
- Kept the work scoped to Order create UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, stock reservation logic, picking logic, delivery handoff behavior, pricing rules, or role/module permissions.
- Added an order-specific failure guide when create order returns an error:
  - Read the red message
  - Fix the highlighted step
  - Try Create Order again
- Added worker-facing copy that the order is not saved yet and the missing or blocked details should be fixed before asking the customer to wait.
- Preserved the existing server action message, five-step create flow, quick customer add, quick item templates, final order check, success next-step checklist, and `Go to Picking` / `Back to Orders` actions.
- Added Order UX and route smoke coverage guards for the new error next-step guide.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/orders/create` around 390px for a successful create, missing required inputs, and a server-side create failure.
- Real Supabase role/scope QA should confirm worker-only users still see only allowed order actions and managers/admin/directors retain advanced views.
- This pass is UI guidance only; real validation, reservation, shortage, picking, ready, pickup, and delivery handoff behavior still depends on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as manager approval queue empty states or delivery driver blocked-action guidance.

## 2026-06-24 - Order create success next-step refinement

Task completed:

- Refined the Order create worker success state toward the requested fast order creation workflow.
- Kept the work scoped to Order create UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, stock reservation logic, picking logic, delivery handoff behavior, pricing rules, or role/module permissions.
- Added a post-create checklist after a successful order:
  - Order saved
  - Stock reservation checked
  - Open picking
- Added worker-facing copy that stock shortage stays visible in picking and staff should not create the same order again.
- Preserved the existing five-step create flow, quick customer add, quick item templates, total-price-only V1 behavior, final order check, and `Go to Picking` / `Back to Orders` actions.
- Added Order UX and route smoke coverage guards for the new post-create checklist and duplicate-order warning.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/orders/create` around 390px for pickup, delivery, and internal transfer order creation.
- Real Supabase role/scope QA should confirm worker-only users still see only allowed order actions and managers/admin/directors retain advanced views.
- This pass is UI guidance only; real reservation, shortage, picking, ready, pickup, and delivery handoff behavior still depends on existing server actions and database policies.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as order create blocked/error states or manager approval queue empty states.

## 2026-06-24 - Manager task completion sweep refinement

Task completed:

- Refined the Manager dashboard home screen toward the requested manager-first daily workflow.
- Kept the work scoped to Manager dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval routing, delivery status transitions, processing yield logic, or role/module permissions.
- Added a compact manager task completion sweep inside the existing task completion list:
  - Attendance checked
  - Cleaning checked
  - Approvals cleared
  - Delivery issues checked
- Added manager-facing copy reminding managers to clear missing tasks before normal reports.
- Preserved the existing manager today board, action-first steps, team issue watch list, task completion links, scope line, and Today/This week/This month badges.
- Added route smoke coverage guards for the new completion sweep and closeout copy.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/dashboard` around 390px for retail, delivery, and processing manager profiles.
- Real Supabase role/scope QA should confirm managers still see only allowed outlet, department, team, and module actions.
- This pass is UI guidance only; real attendance, cleaning, approval, delivery, and processing completion state still depends on existing module data and server actions.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

Next recommended task:

- Continue with another non-Stock mobile workflow refinement, such as manager approval queue empty/error states or worker dashboard blocked-action guidance.

## 2026-06-24 - OA Actions request success next-step refinement

Task completed:

- Refined the OA Actions worker request success state toward the requested approval-timeline workflow.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval transitions, payment transitions, payslip behavior, or role/module permissions.
- Added a compact post-submit guide after a successful advance, claim, or leave request:
  - Request submitted
  - Timeline visible
  - Wait for reviewer
- Added worker-facing copy warning staff not to submit the same request again unless a reviewer asks for a fix.
- Preserved the existing worker request fast path, `Go to My Requests` action, approval timeline, reviewer queue guides, selected-review next action, and phone-friendly controls.
- Added route smoke coverage guards for the new post-submit guide and duplicate-request warning.

Files changed in this pass:

- `components/oa-actions/oa-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/advance`, `/oa-actions/claim`, `/oa-actions/leave`, and `/oa-actions/my-requests` around 390px for worker and reviewer profiles.
- Real Supabase role/scope QA should confirm worker-only users still see only their own requests and reviewers/directors/accounts only see actions allowed by existing policies and server actions.
- This pass is UI guidance only; real approval routing, rejection, director approval, payment, and payslip behavior still depends on existing server actions and data.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Delivery proof upload next-step refinement

Task completed:

- Refined the Delivery driver proof upload flow toward the requested one-tap proof photo workflow.
- Kept the work scoped to Delivery driver UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, proof validation, GPS capture behavior, delivery status transitions, failed-delivery return logic, or finance/payment exposure.
- Added a proof-specific success next-step guide after Delivered or Failed proof upload:
  - Delivered: Delivery complete, Check next job, Completed tab
  - Failed: Failed proof saved, Tell manager, Return follow-up
- Added worker-facing copy that failed proof requires manager review and stock return follow-up, while delivered proof means the job is complete.
- Preserved the existing camera-open proof button, photo watermarking, automatic GPS attempt, failed reason buttons, Other remark requirement, and large mobile upload buttons.
- Added Delivery module and route smoke coverage guards for the new proof next-step guide.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery/driver` around 390px for out-for-delivery jobs with Delivered proof, Failed proof, GPS allowed, GPS denied, and failed reason `Other`.
- Real phone QA should verify the camera opens from the proof buttons, the watermarked photo uploads, GPS failure remains allowed, and success next-step cards appear after upload.
- Real Supabase role/scope QA should confirm delivery users still cannot see finance/accounting data and proof upload remains blocked until `OUT_FOR_DELIVERY`.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Cleaning completion success next-step refinement

Task completed:

- Refined the Retail Cleaning completion card toward the requested worker-first tap-to-complete workflow.
- Kept the work scoped to Retail Cleaning UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, cleaning frequency rules, late-cleaning logic, photo optional behavior, or role/module permissions.
- Added a cleaning-specific success next-step guide after a task is completed:
  - Task saved
  - Complete next task
  - Late status stays in history, or Today count updated
- Added worker-facing copy that if another task is shown, complete it now; otherwise return home.
- Preserved the existing late-first sorting, late/due badges, optional remarks/photo section, big `Tap Complete Cleaning` button, `Complete another task`, and `Back to Home` actions.
- Added Retail module and route smoke coverage guards for the new cleaning success next-step guide.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/cleaning/tasks` and `/retail/cleaning/tasks` around 390px for retail worker and manager profiles with late, due-today, completed, and no-task states.
- Real Supabase role/scope QA should confirm Cleaning completion still respects outlet scope, retail/cleaning module access, and same-day completion behavior.
- This pass is UI guidance only; real cleaning completion history, late status, and optional photo persistence still depends on existing server actions and data.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Attendance clock success next-step refinement

Task completed:

- Refined the Attendance clock form toward the requested worker-first Clock In / Clock Out workflow.
- Kept the work scoped to Attendance clock UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, GPS validation, attendance calculations, status rules, or role/module permissions.
- Added a clock-specific success next-step card after successful Clock In or Clock Out:
  - Continue work
  - Clock out before leaving
  - Check My Attendance
  - Return Home
- Added large mobile actions for `Check My Attendance` and `Back to Home` so workers know what to do after submitting.
- Preserved the existing big Clock In / Clock Out buttons, GPS capture, manual location fallback, selected-action display, and submit labels.
- Added route smoke coverage guards for the new success next-step card and actions.

Files changed in this pass:

- `components/attendance/attendance-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/attendance/clock` around 390px for worker and manager profiles, including Clock In success, Clock Out success, GPS allowed, GPS denied, and manual location fallback.
- Real Supabase role/scope QA should confirm Attendance clock still respects work-location rules, profile scope, and existing module access.
- This pass is UI guidance only; real GPS distance, late status, no-clock-out, and approved-leave behavior still depends on existing server actions and data.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Retail processing packing next-step refinement

Task completed:

- Refined the Retail processing form toward the requested worker-first processing and packing workflow.
- Kept the work scoped to Retail processing UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, processing calculations, yield rules, stock inbound behavior, or role/module permissions.
- Added an After processing guide inside the final review step:
  - Pack finished goods
  - Print barcode labels
  - Inbound finished stock
  - Review yield alert
- Added worker-facing copy that finished stock is not added automatically and must be packed, labeled, and inbounded before sale.
- Preserved the existing Type / Raw / Finished / Wastage / Submit flow, barcode inbound reminder, alert-only abnormal yield rule, current submit action, and larger mobile numeric controls.
- Added Retail module and route smoke coverage guards for the new packing handoff.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/processing` around 390px for retail worker and manager/admin/director profiles.
- Real Supabase role/scope QA should confirm Retail processing still respects outlet scope, module access, review roles, and existing processing save behavior.
- This pass is UI guidance only; real barcode inbound and stock availability evidence still belongs to the Stock workflow.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Order creation final check refinement

Task completed:

- Refined the Order create form toward the requested fast worker order creation workflow.
- Kept the work scoped to Order create UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, order creation logic, pricing rules, reservation behavior, picking rules, delivery handoff, or role/module permissions.
- Added a compact Final order check guide inside Step 5 before the large Create Order action:
  - Check customer
  - Check items kg
  - Create order
  - Go picking
- Added worker-facing copy to check customer, items, estimated kg, and price before creating.
- Preserved the existing five-step create flow, worker fast path, customer search/quick add, order type buttons, quick item templates, total order price entry, review cards, success next-step links, and large mobile buttons.
- Added order UX coverage guards for the final-check labels.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/orders/create` around 390px for retail, delivery, and processing worker profiles with pickup, delivery, and internal transfer orders.
- Real Supabase role/scope QA should confirm order creation still respects outlet, department, stock-location, module access, customer access, and current reservation behavior.
- This pass is UI guidance only; real order creation, stock availability, and picking handoff evidence is still needed in deployed environments.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Delivery one-tap accept guide refinement

Task completed:

- Refined the Delivery driver available-job card toward the requested one-tap accept workflow.
- Kept the work scoped to Delivery driver UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, status transitions, proof upload rules, GPS behavior, failed-delivery return logic, or finance/payment exposure.
- Added a compact Driver accept steps guide before the large Accept Delivery button:
  - Tap Accept
  - Load goods
  - Tap Loaded
  - Start delivery
- Added clear worker-facing guidance that normal accept needs no typing and Change Vehicle is only for exceptions.
- Preserved the existing one-tap `Accept Delivery` action, optional vehicle override, loaded/start delivery flow, proof photo flow, address issue flow, customer quick links, and large mobile buttons.
- Added delivery coverage guards for the accept-step labels and no-typing guidance.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery/driver` around 390px for a delivery driver with available, accepted, loaded, out-for-delivery, delivered, and failed jobs.
- Real phone QA should verify one-tap accept, optional vehicle override, Loaded, Start Delivery, maps/call/WhatsApp links, camera proof upload, and GPS allowed/denied paths.
- This pass is UI guidance only; real Supabase delivery transition evidence is still needed in deployed environments.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Account/admin daily control order refinement

Task completed:

- Refined the Accounting Dashboard toward the requested admin/account operational review and control screen.
- Kept the work scoped to Accounting/Finance dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, role checks, module permissions, invoice transitions, OA payment transitions, or container logic.
- Added a compact Account/admin daily control order inside the operational review board:
  - Review invoices
  - Release payments
  - Check aging
  - Update containers
- Preserved the existing review/control queue for admin invoice review, payment release, OA admin review, OA payment, AR overdue, AP overdue, and container control.
- Preserved existing urgent/check badges, finance KPI cards, invoice review/payment forms, recent invoices, debtor aging, and creditor aging.
- Added smoke coverage guards for the account/admin control order labels.

Files changed in this pass:

- `components/finance/finance-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/accounting-finance/dashboard` around 390px and desktop widths for account, admin, and director profiles.
- Real Supabase role/scope QA should confirm account/admin users still only see and act on finance and OA payment data allowed by existing policies and server actions.
- This pass is UI guidance only; real invoice, OA payment, aging, and container workflow evidence is still needed in deployed environments.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Director daily review order refinement

Task completed:

- Refined the Director Reports dashboard toward the requested all-in-one director role dashboard.
- Kept the work scoped to Director dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, role checks, module permissions, report calculations, or approval logic.
- Added a compact Director daily review order inside the all-in-one company overview:
  - Open alerts first
  - Review approvals
  - Check operations
  - Save/share report
- Preserved the existing company overview cards for sales, stock, orders, delivery, attendance, cleaning, processing, OA approvals, finance/accounting, and alerts.
- Preserved the existing Director alerts first panel, pending approvals table, director activity focus, report snapshots, reporting period badge, module links, and finance separation.
- Added smoke coverage guards for the director review order labels.

Files changed in this pass:

- `components/director/director-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/director-reports/dashboard` around 390px and desktop widths for director/admin profiles.
- Real Supabase data QA should confirm KPI totals and links still reflect current sales, stock, orders, delivery, attendance, cleaning, processing, OA approvals, finance/accounting, and alerts.
- This pass is UI guidance only; real deployed director evidence is still needed.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - OA Actions reviewer timeline refinement

Task completed:

- Refined the OA Actions approval review forms toward the requested timeline-style approval UX.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, role checks, module permissions, review transitions, or payment rules.
- Added a reviewer queue guide inside manager/admin and director review forms:
  - Open request / Open oldest
  - Check details / Check timeline
  - Move to next stage / Approve or reject
  - Save note
- Added a pending count badge for each review queue.
- Added selected-request next-action guidance so reviewers can see whether the next step is manager review, admin review, director approval, approval, or rejection.
- Added a clear no-pending queue message.
- Preserved the existing `Submitted -> Manager Review -> Admin Review -> Director Approval -> Approved/Rejected` timeline, worker fast path, My Requests timeline, large mobile controls, and success next-step link.
- Added smoke coverage guards for the review guide, selected next action, and empty queue guidance.

Files changed in this pass:

- `components/oa-actions/oa-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/dashboard` around 390px for manager/admin/director profiles with pending and no-pending queues.
- Real Supabase role/scope QA should confirm managers, admin, account, and director still only see and act on requests allowed by existing policies and server actions.
- This pass is UI guidance only; real approval transition evidence is still needed in deployed environments.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Manager daily routine refinement

Task completed:

- Refined the manager dashboard home toward the requested manager role dashboard.
- Kept the work scoped to home dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, role checks, module permissions, or module-access filtering.
- Added an explicit manager routine strip:
  - Check missing tasks
  - Open team activity
  - Clear approvals
  - Finish completion list
- Preserved the existing Manager today board, Team issues to clear today, Today team activity, Task completion list, scope line, Today/This week/This month badges, module-access filtering, and no-actions empty state.
- Added smoke coverage guards for the manager routine labels.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify the manager dashboard around 390px for retail_manager, delivery_manager, processing_manager, admin, and director profiles.
- Real role/scope QA with Supabase should confirm only accessible module links show.
- This pass is UI guidance only; real Supabase evidence is still needed in deployed environments.
- Stock Module files remain untouched because active Stock work is still present in the worktree.

## 2026-06-24 - Module access blocked-state refinement

Task completed:

- Refined the shared module-access blocked state toward the requested worker-friendly error/empty-state UX.
- Kept the work scoped to shared access-state UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, route guards, module-access checks, role checks, or permission logic.
- Replaced the vague `module is not enabled` message with a clear account/scope message:
  - role, outlet, department, or module access does not allow the page.
  - no work was changed.
- Added simple next steps:
  - Go back to Home
  - Ask manager or admin
  - Check role, outlet, and module access
- Added a worker-sized `Back to Home` button.
- Added source coverage guards so the blocked state keeps permission/scope guidance and the large home action.

Files changed in this pass:

- `components/erp/module-access-state.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify blocked module routes around 390px for worker, manager, account, admin, and director profiles.
- Real role/module QA should confirm the UI message matches actual Supabase role, outlet, department, and module-access setup.
- This pass is UI guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Delivery proof photo guide refinement

Task completed:

- Refined the Delivery driver proof-photo card toward the requested one-action-at-a-time mobile workflow.
- Kept the work scoped to Delivery driver UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, delivery status rules, proof upload rules, GPS behavior, failed-delivery return logic, or finance/payment exposure.
- Added proof-specific guide steps inside the driver proof card:
  - Delivered proof:
    - Take proof photo
    - GPS tried
    - Delivery complete
  - Failed proof:
    - Choose reason
    - Take proof photo
    - Manager follow-up
- Added clear proof badges:
  - `Photo required`
  - `GPS tried automatically`
  - `Auto-completes delivery`
  - `Return follow-up needed`
- Added worker-facing copy explaining:
  - delivered proof completes the delivery after upload.
  - failed proof records the failed reason and tells the manager to check stock return follow-up.
- Preserved the existing camera capture, GPS attempt, failure reason buttons, `Other` remark requirement, watermarking, and large Upload Proof buttons.
- Added delivery coverage guards for the new proof guide, badges, and delivered/failed next-step copy.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated phone-width QA should verify `/delivery/driver` proof upload for Delivered and Failed around 390px, including camera opening, GPS allowed/denied, failed reason selection, and `Other` remarks.
- Real delivery QA should confirm failed customer-order delivery still triggers the correct stock return follow-up and failed standalone delivery still requires manager manual stock follow-up.
- This pass is UI guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Cleaning late-task worker refinement

Task completed:

- Refined the Cleaning worker completion flow toward the requested worker-first, low-familiarity mobile workflow.
- Kept the work scoped to Retail/Cleaning UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, cleaning completion rules, photo rules, manager setup permissions, or report permissions.
- Updated the Cleaning worker fast-path guide so workers see the correct order:
  - Late first
  - Check area
  - Tap complete
  - Next task
- Sorted pending cleaning tasks so missed/late tasks appear before normal due-today tasks.
- Added clear task badges:
  - `Late task`
  - `Due today`
- Added worker-facing late-task copy:
  - late cleaning stays late in the record.
  - complete the task now, but history still shows it was late.
- Preserved the existing one-big-button completion action, optional remarks/photo drawer, success next steps, and no manager setup controls on the worker completion page.
- Added smoke and retail coverage guards for late-first sorting, late/due badges, and late-history guidance.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/cleaning/tasks` and `/retail/cleaning` around 390px with a mix of late, due-today, completed, and no-task states.
- Real data QA should confirm historical cleaning reports still show late completion as late according to the business rule.
- This pass is UI ordering and guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Login worker-first entry refinement

Task completed:

- Refined the login screen toward the requested worker-first ERP direction.
- Kept the work scoped to Auth/Login UI and source coverage; no authentication logic, database schema, RLS, server actions, role/module permissions, or Stock Module files were changed.
- Replaced the old stock/sales-oriented login headline with a staff entry message:
  - `Start your work day from one ERP.`
- Updated role/scope copy so workers, managers, account, admin, and director users understand that modules, outlet, department, and stock-location access are assigned by role/scope.
- Added a small mobile-friendly workflow strip for:
  - Clock
  - Stock
  - Orders
  - Delivery
  - Processing
  - OA
- Made the login form more touch-friendly with larger email/password inputs and a larger submit button.
- Added an accessible `role="alert"` error state and worker-friendly missing-module guidance.
- Added smoke coverage guards for the new login copy, touch-friendly form controls, and removal of the old owner/sales/customer-service login framing.

Files changed in this pass:

- `app/(auth)/login/page.tsx`
- `components/auth/login-form.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/whatsapp-crm-auth-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed once because `scripts/whatsapp-crm-auth-coverage.mjs` still expected the old login wording: `Login screen must explain role-based access.`
- `npm.cmd run smoke` - passed after updating the stale coverage assertion.

Risks / remaining checks:

- Manual browser QA should verify `/login` around 390px and desktop widths for text fit, touch target comfort, and error-state readability.
- Real auth QA should confirm failed login messages remain useful and successful login redirects still go to `/dashboard`.
- This pass is UI guidance only; real Supabase role/module evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Attendance worker page role-aware refinement

Task completed:

- Refined Attendance page-level UX toward the requested worker-first Clock In / Clock Out workflow.
- Kept the work scoped to Attendance UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, attendance clock rules, GPS validation, leave logic, or manager action permissions.
- Added an `Attendance worker daily actions` card on `/attendance/today` for non-manager users:
  - Clock In / Clock Out
  - My Attendance
- Added a simple worker step strip:
  - Open Clock
  - Use GPS
  - Submit
  - Next step shown
- Made Attendance navigation role-aware:
  - workers see Today, Clock, and My Attendance.
  - managers/admin see Today, Clock, My Attendance, Department, and Settings.
- Changed worker Today view to show personal attendance summary/logs only instead of team-wide rows.
- Added clear permission cards when a worker opens Department or Settings directly.
- Added smoke coverage guards for worker daily actions, personal Today data, and manager/admin-only Department/Settings UI.

Files changed in this pass:

- `components/attendance/attendance-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Existing dirty files observed but not edited in this pass:

- `components/attendance/attendance-forms.tsx`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/attendance/today`, `/attendance/clock`, `/attendance/department`, and `/attendance/settings` around 390px for worker, manager, admin, account, and director profiles.
- Real role/module QA should confirm server-side Attendance actions still enforce manager/admin-only edits and that workers only see their own attendance rows in the Today view.
- This pass is UI guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Orders worker launcher role-aware delivery refinement

Task completed:

- Refined `/orders` toward the requested worker-first order workflow.
- Kept the work scoped to Orders UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, order reservation logic, picking logic, delivery status rules, or finance exposure rules.
- Added an `Order worker daily actions` guide above the task buttons:
  - Create
  - Pick
  - Ready
  - Next step shown
- Made the Orders task launcher role-aware:
  - delivery general workers see `Driver Delivery` linking to `/delivery/driver`.
  - manager/admin/director users keep `Delivery Dashboard` linking to `/delivery`.
  - non-delivery general workers no longer get a delivery-manager dashboard shortcut from the Orders launcher.
- Kept the advanced order list and report section behind the existing advanced-order role gate.
- Added coverage guards for the role-aware delivery shortcut, worker guide, and manager-only advanced report wording.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/orders` around 390px for retail worker, delivery worker, processing worker, manager/admin, and director profiles.
- Real role/module QA should confirm only delivery workers with Delivery access can use `/delivery/driver`; other workers should continue via create/pick/ready/customer order actions.
- This pass is UI routing guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Retail worker daily actions refinement

Task completed:

- Refined `/retail` worker entry toward the requested worker-first, low-typing daily outlet workflow.
- Kept the work scoped to Retail UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, retail same-day rules, manager review rules, report permissions, or settings permissions.
- Added a dedicated `Retail worker daily actions` entry point for worker-only profiles.
- Kept worker actions as large phone-friendly buttons for:
  - Submit Expense
  - Complete Cleaning
  - Record Processing / Packing
  - Today Summary
- Added a simple worker flow strip:
  - Choose task
  - Fill short form
  - Submit
  - Next step shown
- Preserved the existing worker restriction that sales reports, cash closing, reports, and settings stay with manager/admin roles.
- Added coverage guards for the dedicated worker home, processing/packing language, guided step strip, and no report/settings shortcuts in the worker launcher.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail` around 390px for retail worker, retail manager, admin, and director profiles.
- Real outlet-user QA should confirm worker-only profiles see the new daily buttons first and cannot access report/settings/cash-closing controls.
- This pass is UI guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Delivery driver fast-path orientation refinement

Task completed:

- Refined `/delivery/driver` toward the requested one-tap delivery worker experience.
- Kept the work scoped to Delivery driver UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, delivery status rules, proof upload rules, expense rules, or route permissions.
- Added a `Driver fast path` panel at the top of the driver page:
  - Accept Delivery
  - Loaded
  - Start Delivery
  - Proof photo
- Added simple today counters for:
  - Available today
  - In progress
  - Completed
  - Failed follow-up
- Changed the initial driver tab so drivers with active accepted/loaded/out-for-delivery jobs start on `My Deliveries` instead of `Available`.
- Kept existing one-tap `Accept Delivery`, `Loaded`, `Start Delivery`, maps/call/WhatsApp shortcuts, delivered proof photo, failed proof photo, and address issue flows.
- Added accessible live-region semantics to driver action messages so success and blocked/error states are announced.
- Added delivery coverage guards for the fast-path panel, counts, active-job initial tab, and message live-region semantics.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Existing dirty files observed but not edited in this pass:

- `app/(erp)/delivery/[id]/page.tsx`
- `app/(erp)/delivery/driver/page.tsx`
- `components/delivery/delivery-page.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `scripts/smoke-routes.mjs`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/delivery/driver` around 390px for delivery worker, delivery manager, and admin profiles.
- Real phone QA is still needed for camera proof capture, GPS allowed/denied/unavailable states, and external map/phone/WhatsApp links.
- This pass is UI guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Worker home fixed daily actions refinement

Task completed:

- Refined `/dashboard` worker home toward the requested WhatsApp-style, worker-first mobile landing page.
- Kept the work scoped to Home dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, module access rules, or route permissions.
- Added a fixed `Worker daily actions` board so workers always see the same main daily action positions:
  - Clock In
  - Stock
  - Order
  - Delivery
  - Processing
  - Cleaning
  - OA Action
- Kept role/module restrictions intact:
  - available actions are active links.
  - unavailable actions are shown as non-clickable `Missing` tiles with guidance to ask a manager.
- Added a simple worker pattern strip:
  - Open workflow
  - Follow guided steps
  - Submit
  - Next step shown
- Added explicit worker-facing copy that the worker home has big buttons only and no finance, cost, stock value, or advanced reports.
- Preserved the existing viewing-scope line and empty setup state.
- Added smoke coverage guards for the fixed worker action list, guided-step strip, missing-button guidance, route links, and finance/advanced-report exclusion copy.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/dashboard` around 390px for retail worker, delivery worker, processing worker, manager/admin, and director profiles.
- Missing action tiles intentionally do not link anywhere; real role/module QA should confirm workers only get active links for allowed modules.
- This pass is UI guidance only; real Supabase role/scope evidence is still needed in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Account/admin operational review dashboard refinement

Task completed:

- Refined `/accounting-finance/dashboard` toward the requested admin/account operational review and control screen.
- Kept the work scoped to Accounting / Finance dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, finance approval rules, OA approval rules, payment rules, or route permissions.
- Added an `Account/admin operational review` board above the existing KPI cards and finance forms.
- Added a `Review and control queue` with clear action cards for:
  - Admin invoice review
  - Payment to release
  - OA admin review
  - OA payment
  - AR overdue
  - AP overdue
  - Container control
- Reused existing finance and OA status rules so the queue reflects current action contracts:
  - submitted invoices for admin invoice review
  - director-approved unpaid invoices for payment release
  - submitted/manager-reviewed claims and advances for OA admin review
  - director-approved claims and advances for account payment
  - AR/AP invoices older than 30 days and unpaid for overdue review
  - open containers and missing ETA for container control
- Added large mobile-friendly links from the queue to the existing finance review/control screens.
- Preserved existing KPI cards, admin invoice review, paid invoice form, recent invoices, debtor/creditor aging, and recent finance activity.
- Added smoke coverage guards for the account/admin queue, review/payment/overdue/container controls, and route links.

Files changed in this pass:

- `components/finance/finance-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/accounting-finance/dashboard` around 390px and desktop width for account, admin, and director profiles.
- This pass reuses existing loaded finance/OA data; it does not add new filters, new payment logic, or separate live aging calculations.
- Real Supabase role/scope evidence is still needed to prove account/admin/director access and non-finance-role blocking in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Director dashboard all-in-one overview refinement

Task completed:

- Refined `/director-reports/dashboard` toward the requested director all-in-one company overview.
- Kept the work scoped to Director dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, finance calculations, approval rules, or route permissions.
- Added a `Director company overview` section above the existing KPI cards and tables.
- Grouped existing dashboard data into director-readable areas:
  - Sales
  - Stock
  - Orders
  - Delivery
  - Attendance
  - Cleaning
  - Processing
  - OA approvals
  - Finance/accounting
  - Alerts
- Added an alerts-first panel for approval waiting, low stock, delivery follow-up, attendance gaps, finance exposure, and container ETA.
- Added large module links from the overview cards to the existing report/workflow pages.
- Preserved the existing detailed KPI cards, pending approvals table, activity focus list, saved reports, report export, print/PDF, and WhatsApp summary features.
- Added smoke coverage guards for the director all-in-one overview, alerts-first panel, module links, OA approvals, and finance/accounting separation.

Files changed in this pass:

- `components/director/director-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/director-reports/dashboard` around 390px and desktop width for director and admin profiles.
- This pass reuses existing director data loaders; it does not add live order-module counts beyond the existing delivery/order view already loaded on the page.
- Real Supabase role/scope evidence is still needed to prove director/admin access and non-director blocking in deployed environments.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Attendance clock worker fast-path refinement

Task completed:

- Refined `/attendance/clock` toward the requested worker-first mobile experience.
- Kept the work scoped to Attendance clock UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, GPS radius logic, attendance rules, or route permissions.
- Added an `Attendance worker fast path` guide above the clock form:
  - Choose Clock In or Clock Out
  - Use Current Location
  - Submit attendance
- Added clear worker copy that the flow uses one action, GPS capture, and no typing unless GPS is blocked.
- Added a visible selected-action line so workers can confirm whether they are submitting `Clock In` or `Clock Out`.
- Changed the location-captured message to tell workers the exact next button to tap: `Submit Clock In` or `Submit Clock Out`.
- Preserved the existing big Clock In / Clock Out buttons, current-location capture, manual latitude/longitude fallback, and success next-step messages.
- Added smoke coverage guards for the fast-path guide, selected action, GPS next-step copy, and existing large-button/manual-fallback requirements.

Files changed in this pass:

- `components/attendance/attendance-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/attendance/clock` around 390px for retail worker, delivery worker, processing worker, manager/admin, and director profiles.
- Real phone QA is still needed for location permission allowed, denied, unavailable, and timeout states.
- This pass is UI guidance only; real Supabase RLS and server-action authorization still need role/scope QA evidence with real users.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Cleaning tap-to-complete worker refinement

Task completed:

- Refined the shared Cleaning completion flow toward the requested worker-first mobile experience.
- Kept the work scoped to Cleaning/Retail cleaning UI and source coverage; did not change Stock Module files, database schema, RLS, server actions, cleaning completion rules, photo rules, or route permissions.
- Added a `Cleaning worker fast path` guide above the task cards:
  - Check area
  - Tap complete
  - Next task
- Made the worker instruction explicit that each cleaning task uses one big button and no photo is required for V1.
- Added a remaining-task count with `Tasks left today:` so workers can see how much cleaning work is still open.
- Changed the main completion button copy to `Tap Complete Cleaning` while preserving the existing `Complete Cleaning` action wording and form contract.
- Kept optional remarks/photo tucked away behind `Remarks or photo (optional)`.
- Added smoke and Retail module coverage guards for the fast-path guide, task count, no-photo copy, large button, and tap wording.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Existing dirty files observed but not edited in this pass:

- `components/retail/retail-page.tsx`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/cleaning/tasks` and `/retail/cleaning` around 390px for retail worker, retail manager, admin, and director profiles.
- This pass is UI guidance only; real Supabase RLS and server-action authorization still need role/scope QA evidence with real users.
- The global `/cleaning/tasks` route currently reuses the Retail cleaning page with `route="cleaning"`; no separate non-retail cleaning data model was added.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Manager dashboard issue-watch refinement

Task completed:

- Refined the shared Home manager dashboard toward the requested manager role UX.
- Kept the work scoped to dashboard UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, module access rules, or business calculations.
- Strengthened the `Manager today board` so urgent manager actions show:
  - visible `Priority` badges.
  - simple action labels such as `Needs action`, `Approval`, `Follow up`, and `Check yield`.
  - a clear empty state when the manager profile has no available action modules.
- Added a `Team issues to clear today` watch-list section for:
  - Late attendance
  - Missed cleaning
  - Failed delivery
  - Abnormal yield
  - Approval waiting
- Kept each issue card linked only when the manager already has the relevant module shortcut, preserving existing role/module filtering.
- Added clearer completion-list guidance with `Open and clear` next-step text and an empty state when no checklist items are available.
- Added smoke coverage guards for the new manager watch list, priority labels, check-now action, open-and-clear guidance, and manager empty state.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/dashboard` around 390px and desktop width for retail manager, delivery manager, processing manager, admin/account, director, and worker profiles.
- This pass uses existing shortcut/module visibility as evidence of manager access; it does not add live counts, new filters, or new data loaders.
- Real Supabase role/scope evidence is still needed to prove manager dashboards only expose assigned outlet/team/department data.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Order picking worker fast-path refinement

Task completed:

- Refined `/orders/picking` toward the requested worker-first mobile app experience.
- Kept the work scoped to Order picking UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, reservation behavior, picking action contracts, or route permissions.
- Added an `Order picking fast path` guide with four guided steps:
  - Select order
  - Scan barcode
  - Confirm picked weight
  - Mark ready
- Added worker-facing reminders that picking is one order at a time, order edits should stop after picking starts, duplicate picked barcodes are blocked, wrong item scans are saved as mismatches, and manual weight needs a reason.
- Added success next-step guidance:
  - after scan/manual pick, keep scanning until each item is within tolerance.
  - after ready, go to Ready Orders for pickup or delivery handoff.
- Added smoke coverage guards for the picking fast path, barcode duplicate/mismatch guidance, and ready next-step guidance.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Existing dirty files observed but not edited in this pass:

- `app/(erp)/orders/new/page.tsx`
- `app/(erp)/orders/prepare/page.tsx`
- `components/orders/orders-page.tsx`
- `components/orders/orders-table-client.tsx`
- `lib/orders/actions.ts`
- `lib/orders/data.ts`

Migration SQL added:

- None.

Commands run and results:

- `git status --short -- components\orders lib\orders app\(erp)\orders scripts\smoke-routes.mjs scripts\order-ux-simplification-coverage.mjs HANDOFF.md` - failed because PowerShell interpreted `(erp)` as syntax; rerun with quoted path passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - first run failed because the new picking helper was outside the order UX script slice; coverage was corrected.
- `npm.cmd run lint` - passed after coverage correction.
- `npm.cmd run smoke` - passed after coverage correction.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/orders/picking` and `/orders/ready` around 390px for order worker, delivery worker, manager/admin, and director profiles.
- This pass is UI guidance only; real Supabase RLS and server-action authorization still need role/scope QA evidence with real users.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Processing worker fast-path dashboard refinement

Task completed:

- Refined the standalone Processing dashboard toward the requested worker-first mobile app experience.
- Kept the work scoped to Processing UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, Retail processing action contracts, Processing calculations, or route permissions.
- Added a `Processing worker fast path` card with guided steps:
  - Record raw
  - Record finished
  - Check yield
  - Submit
  - Barcode inbound
- Added large mobile actions for:
  - Record processing
  - Check batches
  - Barcode inbound
- Added clear worker guidance that finished stock still needs barcode inbound after packing and abnormal yield below 85% is alert-only.
- Split Processing record and review UI by role:
  - processing worker/manager/admin profiles can see the record form.
  - processing manager/admin/director profiles can see the review form.
  - plain processing worker profiles no longer see manager review controls.
- Added smoke coverage guards for the Processing fast path, large actions, barcode inbound next step, alert-only yield guidance, and record/review role split.

Files changed in this pass:

- `components/processing/processing-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Existing dirty files observed but not edited in this pass:

- `lib/processing/types.ts` was already modified before this pass.

Migration SQL added:

- None.

Commands run and results:

- `Get-ChildItem -Recurse app\(erp)\processing | Select-Object FullName` - failed because PowerShell interpreted `(erp)` as syntax; rerun with `-LiteralPath` passed.
- `rg -n "processing|Processing|yield|barcode inbound|abnormal|worker|guided|processing-page" scripts components\processing app\(erp)\processing lib\processing` - failed for the same unquoted `(erp)` path; rerun with quoted path passed.
- `git status --short -- components\processing lib\processing app\(erp)\processing scripts\smoke-routes.mjs HANDOFF.md` - failed for the same unquoted `(erp)` path; rerun with quoted path passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/processing/dashboard` and `/processing/batches` around 390px for processing worker, processing manager, admin, and director profiles.
- The Barcode inbound action links to the existing Stock inbound route but does not change Stock behavior or Stock files.
- This pass is UI separation only; real Supabase RLS and server-action authorization still need role/scope QA evidence with real users.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - OA Actions role-aware worker dashboard refinement

Task completed:

- Refined OA Actions dashboard and history views toward the requested worker-first, role-based UX.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval rules, payment rules, or route permission rules.
- Added a worker-only OA dashboard view for general worker roles:
  - OA Action request shortcuts
  - own request timeline
  - worker-focused page header copy
- Kept approval KPIs, approval queue timeline, admin review, director decision, recent requests, and approval log views for elevated OA profiles.
- Added worker-only data selection for advance, claim, and leave history so worker-only profiles see their own request rows in the UI instead of all request rows.
- Gated payslip upload and mark-paid controls to account/admin/director UI views.
- Non-payroll-control users on the payslip route now see only their own payslip table.
- Added smoke coverage guards for worker-only OA dashboard rendering, own-history filtering, and payslip control gating.

Files changed in this pass:

- `components/oa-actions/oa-forms.tsx`
- `components/oa-actions/oa-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/dashboard`, `/oa-actions/advance`, `/oa-actions/claim`, `/oa-actions/leave`, `/oa-actions/payslip`, and `/oa-actions/my-requests` around 390px for worker, manager, account, admin, and director profiles.
- This pass improves UI separation only; real Supabase RLS and server-action authorization still need role/scope QA evidence with real users.
- Demo mode still uses an admin/director-like profile, so it will show elevated OA dashboard behavior.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - OA Actions worker request fast-path UX refinement

Task completed:

- Refined OA Actions request entry toward the requested worker-first mobile app experience.
- Kept the work scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, approval rules, payment rules, payslip upload rules, or route permissions.
- Added a reusable `Worker request fast path` guide for staff request pages.
- The fast path helps staff choose between:
  - Need cash advance
  - Claim expense
  - Apply leave
- Added a direct `Check My Requests` action so staff can quickly return to their own timeline after choosing a request type.
- Added success next-step guidance after request submission with a `Go to My Requests` action.
- Increased key advance, claim, and leave inputs to larger phone-friendly controls.
- Added smoke coverage guards for the OA fast-path guide, large mobile controls, request success next step, and route wiring.

Files changed in this pass:

- `components/oa-actions/oa-forms.tsx`
- `components/oa-actions/oa-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/advance`, `/oa-actions/claim`, `/oa-actions/leave`, and `/oa-actions/my-requests` around 390px for worker, manager/admin, and director/account profiles.
- Real Supabase role/scope evidence is still needed to prove the OA request pages and approval queues only expose the intended records.
- This pass is UI guidance only; it does not change approval routing, reviewer requirements, or payment behavior.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Mobile worker quick-action shell refinement

Task completed:

- Refined the shared ERP mobile shell toward the requested WhatsApp-style, worker-first app experience.
- Kept the work scoped to shared navigation shell UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, business logic, or route permission rules.
- Added a phone-only `Worker quick actions` bottom action strip for general worker roles.
- The worker action strip is filtered through the existing `canSee(profile, item)` module/role helper and includes only daily worker actions:
  - Home
  - Clock
  - Stock
  - Order
  - Delivery
  - Processing
  - Cleaning
  - OA
- Excluded finance/accounting, director reports, and advanced Stock reports from the worker quick-action strip.
- Added bottom padding to mobile content when worker quick actions are visible so the fixed action strip does not cover forms or cards.
- Preserved the existing mobile hamburger drawer and desktop sidebar behavior.
- Added smoke coverage guards for the worker quick-action list, role/module filtering, excluded advanced/finance links, and mobile bottom padding.

Files changed in this pass:

- `components/erp/app-shell.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify the bottom action strip at phone width around 390px for retail worker, delivery worker, and processing worker profiles.
- Manual QA should confirm the bottom strip does not cover submit buttons, scanner controls, upload controls, or long forms on real phones.
- The quick-action strip is UI navigation only; it does not add new access rights or route behavior.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Order create fast-path worker UX refinement

Task completed:

- Refined the `/orders/create` guided form toward the requested fast, worker-first mobile order creation flow.
- Kept the work scoped to Order create UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, order reservation timing, pricing rules, validation rules, or permission checks.
- Preserved the existing `createCustomerOrderAction` form contract, including:
  - `itemsJson`
  - `customerId`
  - `fulfillmentType`
  - quick-add customer fields
  - outlet/department scope fields
  - pickup/delivery/transfer location fields
  - total order price
  - customer/staff remarks
- Added a `Worker fast path` guide above the form steps:
  - Search customer
  - Tap order type
  - Add quick items
  - Enter total price
  - Create Order
- Made Step 3 recent item buttons read as `Quick item templates` with larger mobile touch targets and clearer copy so workers can add items with less typing.
- Added a success next-step panel after order creation with links to `Go to Picking` and `Back to Orders`.
- Added smoke/Order coverage guards for the fast-path guide, quick item templates, touch-friendly recent item buttons, and success next-step links.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed after fixing the Order coverage slice to check helper UI text in the full Order forms source.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/orders/create` around 390px with retail, delivery, processing, account/admin, and no-access profiles.
- This pass is source/build verified only; real Supabase role/scope evidence, live customer search, quick-add customer creation, and order creation with real stock items remain manual.
- Existing Order V1 reservation timing remains unchanged and still follows current app behavior/business documentation in `docs/BUSINESS_RULES.md`.
- The success next-step links are UI guidance only; no automatic redirect was added.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Role dashboard manager/director UX refinement

Task completed:

- Refined the Home dashboard role panels toward the requested worker-first, role-based UX without touching Stock Module files.
- Kept the work scoped to dashboard UI/source coverage and did not change database schema, RLS, server actions, module access rules, Stock logic, or finance visibility rules.
- Added a clearer manager `Manager today board` with:
  - action-first review cards
  - Attendance gaps
  - Missing cleaning
  - Review requests
  - Delivery issues
  - Yield alerts
  - visible manager scope
  - Today / This week / This month filter chips
- Kept manager team activity and task completion sections, and fixed the attendance completion item so it appears when the manager has Attendance access through the clock shortcut.
- Split the director Home overview into an alerts-first area, a finance/accounting area, and daily company sections for sales, stock, orders, delivery, attendance, cleaning, processing, and approvals.
- Added smoke coverage guards for manager action-first dashboard requirements and director all-in-one overview requirements.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/dashboard` at phone width around 390px and desktop width for manager, director, admin/account, and worker profiles.
- The new manager filter chips are static navigation context only; no live counts or date-filtered metrics were added in this UI-only pass.
- This pass uses existing module/role access as the source of truth and does not add new manager/director data loaders.
- Stock Module files remain untouched because the current worktree still shows active Stock changes.

## 2026-06-24 - Retail processing guided worker UX refinement

Task completed:

- Refined the Retail processing/packing record form toward the requested worker-first, guided mobile workflow.
- Kept the work scoped to Retail processing UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, Retail processing math, review rules, or stock movement behavior.
- Preserved the existing `createRetailProcessingBatchAction` form contract, including:
  - processing type/BOM fields
  - multiple raw material lines
  - multiple finished product lines
  - wastage fields
  - draft/submitted status field
- Added a compact five-step guide:
  - Type
  - Raw
  - Finished
  - Wastage
  - Submit
- Added reusable step headers so each processing section reads as a guided task on phone width.
- Increased key processing text/number controls to larger mobile touch/readability classes.
- Added worker-facing copy reminding staff that finished stock still needs barcode inbound after packing.
- Added an alert-only abnormal-yield warning when yield is below 85%, matching the business rule that abnormal yield is not approval-blocked.
- Added smoke/Retail coverage guards for the guided steps, packing barcode reminder, alert-only yield copy, and larger processing controls.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/retail/processing` at phone width around 390px for retail worker and manager/admin profiles.
- This pass is source/build verified only; live Supabase role/scope evidence and real outlet processing data entry remain manual.
- The form still records Retail V1 processing only; it does not create Stock inbound/outbound or finished product barcode labels automatically.

## 2026-06-24 - Delivery driver one-tap accept and proof photo UX refinement

Task completed:

- Refined the Delivery driver page toward the requested worker-first, WhatsApp-style mobile workflow.
- Kept the work scoped to the Delivery driver UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, delivery status transition rules, proof upload rules, or return/reinbound business logic.
- Preserved the existing driver sequence:
  - Accept Delivery
  - Loaded
  - Start Delivery
  - Upload Delivered Proof or Upload Failed Proof
- Made `Accept Delivery` a full-width large primary action with simple copy explaining that vehicle change is optional.
- Added per-delivery next-step guidance for Available, Accepted, Loaded, Out for Delivery, Delivered, and Failed states.
- Made Loaded and Start Delivery larger full-width actions for phone use.
- Added proof-photo guidance that the camera opens and GPS is attempted automatically during upload.
- Added clearer success copy after accept, loaded, start delivery, delivered proof upload, and failed proof upload.
- Added Delivery module coverage guards for the one-tap accept copy, next-step guidance, larger touch targets, and proof-photo guidance.

Files changed in this pass:

- `components/delivery/driver-mobile-delivery-page.tsx`
- `scripts/delivery-module-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated phone/browser QA should verify `/delivery/driver` at around 390px with delivery worker, delivery manager, and admin profiles.
- Real device QA is still needed for camera launch, proof photo watermarking, GPS allowed/denied/unavailable, Google Maps, Call, and WhatsApp links.
- This pass is UI/source/build verified only; live Supabase RLS and delivery status transition evidence remains manual.

## 2026-06-24 - Cleaning worker tap-to-complete mobile UX refinement

Task completed:

- Refined the Cleaning task completion workflow toward the requested worker-first, simple mobile app pattern.
- Kept the work scoped to the Cleaning task completion UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, role/module restrictions, or cleaning business rules.
- Preserved the existing `completeRetailCleaningTaskAction` form contract:
  - `taskId`
  - `status=DONE`
  - optional `photoFile`
  - optional `remarks`
- Made each cleaning task card task-first with a large icon, clear task/outlet/frequency/due information, and a large `Complete Cleaning` button.
- Kept photo/remarks optional and de-emphasized inside a collapsible details area, matching the V1 rule that cleaning does not require photos.
- Added a clearer success next step after completion and redirected follow-up actions to `/cleaning/tasks` and `/dashboard` instead of Retail-only links.
- Improved the empty state so workers know all required cleaning is done for the current schedule.
- Added smoke coverage guards for the tap-to-complete button, optional-photo copy, success next step, dedicated Cleaning route link, and empty state.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed after updating the Retail cleaning coverage assertion to the new optional-photo copy and larger button class.

Risks / remaining checks:

- The dedicated `/cleaning/tasks` route is backed by the Retail cleaning form component; this pass only changed the Cleaning completion section inside that shared file.
- Manual authenticated browser QA should verify `/cleaning/tasks` and `/retail/cleaning` at phone width around 390px with a worker profile.
- Real Supabase role/scope checks for outlet/department cleaning access remain manual because this pass did not change server-side permissions.

## 2026-06-24 - Attendance worker clock mobile UX refinement

Task completed:

- Refined the Attendance clock screen toward the requested worker-first, phone-friendly Clock In / Clock Out workflow.
- Kept the work scoped to Attendance UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, attendance rules, or GPS radius business logic.
- Replaced the clock event dropdown with two large action buttons:
  - Clock In for starting a shift.
  - Clock Out for ending a shift.
- Added a large Use Current Location control, clearer GPS success/blocked/unavailable messages, and a collapsible manual latitude/longitude/notes fallback.
- Added next-step success copy after clocking in or clocking out so workers know what to do next.
- Increased shared Attendance select and submit control heights for phone-width use.
- Added smoke coverage guards for the large clock buttons, current-location flow, and manual fallback.

Files changed in this pass:

- `components/attendance/attendance-forms.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/attendance/today?view=clock` or the clock route used by staff on a real phone/browser with location allowed, blocked, and unavailable.
- Real outlet GPS radius behavior still needs live location testing because this pass only changes UI around the existing server action.
- Role/module access and manager attendance review behavior are unchanged and should be spot-checked with real profiles.

## 2026-06-24 - OA Actions approval timeline UX refinement

Task completed:

- Refined OA Actions approval UX toward the requested worker-first, timeline-style approval model.
- Kept changes scoped to OA Actions UI/source coverage and did not change Stock Module files, database schema, RLS, server actions, or approval business rules.
- Added a reusable OA request stage timeline showing:
  - Submitted -> Manager Review -> Admin Review -> Director Approval -> Approved/Rejected.
- Added request-level timeline cards to `/oa-actions/dashboard` before the admin/director action forms so approvers can see the current stage before deciding.
- Added the same stage timeline to `/oa-actions/my-requests` so workers can see what happened and what is next after submitting a request.
- Added selected-request timelines inside Admin Review, Director Approval, and Mark Paid forms.
- Kept request-type differences visible without changing logic:
  - Advance skips manager review.
  - Leave skips admin/director review.
  - Claim follows manager then admin then director.
- Increased OA select controls and submit buttons to phone-friendly heights while preserving existing field names, actions, validation, and role/module restrictions.
- Added smoke coverage guards for the OA timeline requirement and mobile-friendly OA controls.

Files changed in this pass:

- `components/oa-actions/approval-timeline.tsx`
- `components/oa-actions/oa-forms.tsx`
- `components/oa-actions/oa-page.tsx`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/oa-actions/dashboard` and `/oa-actions/my-requests` for worker, manager, admin, director, and account roles at phone width around 390px and desktop width.
- This pass is source/build verified only; it does not add live Supabase role/scope evidence for OA approval transitions.
- Existing OA workflow rules remain as-is, including leave manager approval and advance admin/director approval.

## 2026-06-24 - Home dashboard worker-first role UX refinement

Task completed:

- Refined the ERP Home screen toward the requested simple WhatsApp-style, worker-first mobile experience.
- Kept the work scoped to the Home/dashboard workflow and did not change Stock Module files, business logic, database schema, RLS, or server actions.
- Added a worker-first Home section with large action buttons for the main allowed workflows:
  - Clock In, Stock, Order, Delivery, Processing, Cleaning, and OA Action.
- Kept existing module and role checks through `canAccessModule` and per-shortcut role filters, so workers only see allowed action shortcuts and do not see finance, stock reports, director reports, or admin control panels.
- Added manager Home panels for today's team activity and a task completion checklist across attendance, cleaning, processing, delivery, and OA review where the manager has access.
- Added director Home overview links for sales, stock, orders, delivery, attendance, cleaning, processing, OA approvals, finance/accounting, and alerts.
- Added admin/account operational review panels for finance, orders, retail, delivery, OA Actions, WhatsApp CRM, and director reports where accessible.
- Preserved the existing generic shortcut grid for non-worker roles and existing smoke-guarded labels/routes.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt reported another Next build was already running; retry after waiting passed.
- `npm.cmd run smoke` - passed.
- Local dev server HTTP check - `http://127.0.0.1:3000/login` returned 200 with rendered login content; `/dashboard` redirects without an authenticated browser session.

Risks / remaining checks:

- Manual authenticated browser QA should verify `/dashboard` for worker, manager, admin/account, and director profiles at phone width around 390px and desktop width.
- Visual browser automation could not be completed because the `agent-browser` CLI/tool was not available in this session.
- This pass uses existing route/module access as the source of truth; it does not add live counts for manager or director summaries.
- Stock Module work remains untouched in this pass because Stock appears active in the current dirty worktree.

## 2026-06-24 - Follow-up Task 1 critical/high stability fixes

Task completed:

- Used `docs/CURRENT_ERP_QA_AUDIT.md` as the source of truth and fixed only the two High severity fail-open stability/security bugs identified there.
- Kept changes isolated to app profile/module access behavior and smoke regression coverage.
- Did not change database schema, RLS policies, Supabase auth foundation, routing architecture, module architecture, UI design, or core business workflow logic.
- Fixed authenticated missing-profile behavior in `getCurrentProfile()` so a real signed-in user without a `profiles` row now fails closed instead of receiving the privileged demo admin/director profile.
- Fixed module access lookup error behavior so `outlet_module_access` query failures now fail closed with no modules instead of granting all module keys.
- Added smoke guards so both fail-open patterns fail future `npm run smoke` runs if reintroduced.
- Updated `docs/CURRENT_ERP_QA_AUDIT.md` with fixed issues, remaining Critical/High issues, files changed, and test result status.

Files changed in this pass:

- `lib/auth/session.ts`
- `scripts/smoke-routes.mjs`
- `docs/CURRENT_ERP_QA_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed; Next.js 16.2.9 production build completed and route table generated.
- `npm.cmd run smoke` - passed; full route, WhatsApp CRM, Delivery, Retail, Orders, and Stock coverage suite completed.

Risks / remaining checks:

- The Orders reservation timing conflict remains unresolved because it is a product/business-rule decision and would change core workflow behavior.
- Real Auth users should still verify missing-profile, missing-role, disabled-module, and wrong-scope states fail closed cleanly in the deployed/staging app.
- Medium/Low audit findings were intentionally not fixed in this pass.

## 2026-06-24 - Current ERP QA audit

Task completed:

- Audited the current ERP worktree as a senior QA/product pass without changing runtime code, database schema, RLS policies, routing architecture, module architecture, or core business logic.
- Discovered the current app surface: 110 app page routes, 7 API routes, 45 script files, and no conventional `*.test.*` / `*.spec.*` files.
- Ran the safe verification gates: lint, typecheck, production build, and full smoke suite all passed.
- Reviewed major modules for workflow readiness, role/module gating, loading/empty/error states, user friendliness, and data leakage risk.
- Created `docs/CURRENT_ERP_QA_AUDIT.md` with the requested executive summary, command results, module QA table, UX findings by role, stability/security findings, top 10 priority issues, changed files, and next Codex tasks.
- Documented two high-priority access risks for a future auth-hardening pass instead of changing auth foundation in this audit:
  - Authenticated users without a matching `profiles` row currently receive the privileged demo admin/director profile.
  - `outlet_module_access` query errors currently grant all module keys.
- Documented the Order reservation timing conflict for product decision: current code/smoke coverage reserve stock at confirmed order creation, while the supplied agent guide says stock should reserve only when picking/preparation starts.

Files changed in this pass:

- `docs/CURRENT_ERP_QA_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; Next.js 16.2.9 production build listed the current route table.
- `npm.cmd run smoke` - passed; all route, WhatsApp CRM, Delivery, Retail, Orders, and Stock coverage scripts completed.
- `git ls-files .env .env.local .env.example .vercel` - no tracked env files returned.
- `git check-ignore -v .env.local .env .vercel` - confirmed local env/deployment files are ignored.
- Secret string scan - reviewed; service-role references are server/API helper, script, doc, and SQL grant oriented, with no frontend component exposure found in this audit.
- `rg --files -g "*.test.*" -g "*.spec.*"` - no conventional test/spec files found; coverage remains script-based.

Risks / remaining checks:

- Fix the profile/module fail-open behavior before production readiness sign-off.
- Decide and align Order reservation timing before Order module readiness sign-off.
- Keep `/debug/profile` out of shared QA environments or add a non-production admin/developer gate.
- Real authenticated browser QA is still required for worker, manager, account, admin, director, and driver users.
- Real phone/device QA is still required for Stock scanner/camera, Delivery proof/GPS, Attendance GPS, Retail uploads, and label printing/PDF fallback.
- Live Supabase RLS/role/outlet isolation should be re-run as a matrix across all major modules.
- WhatsApp CRM remains dependent on live Meta webhook/account mapping/tester-phone evidence.

## 2026-06-24 - Retail V1 UX simplification continuation

Task completed:

- Continued Retail V1 one-task-per-page UX simplification.
- Kept existing Retail role/outlet isolation, file upload security, RLS assumptions, and report export security unchanged.
- Updated `/retail/processing` into the requested guided worker flow:
  - Step 1: Processing Type.
  - Step 2: Raw Material.
  - Step 3: Finished Product.
  - Step 4: Wastage.
  - Step 5: Review & Submit.
- Added live processing review totals for raw weight, finished weight, wastage weight, yield %, wastage %, and unaccounted difference while preserving the existing server-side calculations.
- Updated Daily Sales to use the simplified `Daily Sales` title, show today's saved summary, and offer `Go to Cash Closing` after save.
- Updated Cash Closing to show the expected-cash formula clearly, keep non-cash sales as reference values, and offer `Go to Outlet Report` after save.
- Added focused manager context sections:
  - Submitted expense review queue with receipt links.
  - Expandable submitted processing records with yield/wastage/warning and raw/finished/wastage details.
  - Cleaning setup active/inactive task list.
- Added manager outlet-scoped CSV export control to Outlet Report.
- Updated `docs/RETAIL_V1_E2E_TEST_RESULTS.md` with the UX simplification evidence and validation status.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results so far:

- `npm.cmd run lint` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/phone Limited UAT should still verify real tap flow and file-picker behavior on the simplified worker and manager pages.

## 2026-06-24 - Order Create guided-flow full validation refinement

Task completed:

- Reviewed `/orders/create` after the Step 1 fix and kept changes scoped to the Order create UI/source guards.
- Confirmed the Step 1 root cause was UI state/validation: the guided form did not model existing-customer and quick-add paths separately enough, and blocked movement lacked an inline error.
- Kept the selected existing customer ID in `customerId`, kept quick-add fields separate, and kept the submitted form field aligned with `name="customerId"`.
- Refined Step 2 so order type starts unselected, uses the existing big buttons, and shows `Select order type.` if Next is blocked.
- Refined Step 3 so a blank item card is not submitted, at least one valid item is required, and the blocked message is `Add at least one item.`
- Refined Step 4 so total order price must be greater than zero and the blocked message is `Enter total order price.`
- Refined Step 5 review to show customer, order type, required date/time, location/address/transfer detail, item count, item cards, estimated weight, total price, and immediate reservation wording.
- Changed the final button label to `Create Order` per the requested UX.
- Updated Order UX and route smoke source guards for the refined labels and validation messages.
- No backend order action, RLS, stock reservation, picking, delivery handoff, pickup, cancellation, schema, or migration logic was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - Order and route checks passed, then failed on unrelated Retail coverage: `Retail processing form must tuck optional details away from the main worker path.`

QA notes:

- In-app browser reproduction could not run because the browser connector still fails on this Windows session with `CreateProcessAsUserW failed: 5`.
- Local app responds at `http://127.0.0.1:3000`; `.env.local` is pointed at local Supabase `http://127.0.0.1:54321`.
- Source-level QA confirms existing customer, quick-add customer, missing customer, missing order type, missing item, invalid total price, final review, no item-level price, no payment status, and no outstanding amount guards.

Remaining issues:

- Manual browser QA is still required for admin/worker/no-orders role flows and mobile layout because browser automation is blocked by the local Windows permission issue.
- Full `npm.cmd run smoke` remains blocked by the unrelated Retail coverage assertion above; this pass did not modify Retail.
- Create Order guided flow is source/build ready for controlled UAT after manual browser role QA confirms the click paths.

## 2026-06-24 - WhatsApp CRM owner live-UAT runbook

Task completed:

- Continued the WhatsApp CRM Phase 1 readiness work after the local source validation was already complete.
- Added a concise owner/admin runbook for executing live WhatsApp Cloud API UAT with real tester phones.
- Converted the remaining external blockers into a practical sequence: deployment/env checks, webhook challenge, account mapping, duplicate/media/send helper scripts, real CRM reply, failed-send evidence, and masked screenshot capture.
- Added explicit stop conditions so owner/admin does not continue live UAT when the public webhook, verify challenge, account mapping, CRM login, or first real incoming message is not working.
- Kept this pass documentation-only; no WhatsApp CRM runtime behavior, database schema, UI, or smoke assertions were changed.

Files changed in this pass:

- `docs/WHATSAPP_CRM_OWNER_UAT_RUNBOOK.md`
- `HANDOFF.md`

Validation:

- All WhatsApp CRM coverage scripts - passed.
- WhatsApp CRM live helper scripts - passed safe local mode; live credential/deployment setup remains incomplete as expected in this local session.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- First `npm.cmd run build` attempts were blocked by an existing Next build lock; later retry passed.
- First `npm.cmd run smoke` attempt stopped on transient Retail source coverage while the working tree was changing; `node scripts\retail-module-v1-coverage.mjs` then passed and final `npm.cmd run smoke` passed.
- Final `npm.cmd run build` - passed; build output includes `/api/whatsapp/send`, `/api/whatsapp/webhook`, and `/whatsapp-crm`.

Remaining risks / next steps:

- Owner/admin still needs the correct public deployment, live Meta/Supabase environment configuration, Meta webhook challenge success, verified `whatsapp_accounts.phone_number_id` rows, tester phones, and masked real-device evidence before WhatsApp CRM can be marked ready for limited internal UAT.
- The latest accepted local validation remains documented in the WhatsApp CRM Phase 1 UAT readiness closure entry below.

## 2026-06-24 - Order Create guided-flow Step 1 validation fix

Task completed:

- Fixed the `/orders/create` guided-flow customer step so an existing selected customer can proceed to Step 2 without requiring quick-add fields.
- Added explicit step validation for the 5-step create flow: Customer, Order Type, Items, Total Price, and Create Order.
- Added the required Step 1 validation message: `Select a customer or enter name and phone.`
- Made quick-add customer fields controlled so name + phone only are required when no existing customer is selected.
- Added a clear selected-customer card and quick-add preview so workers can see what will be used before pressing Next.
- Preserved existing order server action behavior, immediate confirmation/reservation, RLS, total-price-only rule, and no finance/payment/outstanding display.
- Extended Order UX source coverage to guard the guided-flow validation and selected customer display.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `scripts/order-ux-simplification-coverage.mjs`
- `HANDOFF.md`

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- First `npm.cmd run typecheck` attempt failed while `.next/types` contained stale missing generated route files.
- First `npm.cmd run build` attempt hit an existing Next build lock; retry passed and regenerated `.next/types`.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed after the successful build regenerated generated Next route types.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser/manual QA should confirm admin and worker can use both existing-customer and quick-add paths on `/orders/create` at desktop and mobile widths.
- No backend Order business logic was changed in this pass.

## 2026-06-24 - WhatsApp CRM Phase 1 UAT readiness closure

Task completed:

- Reviewed WhatsApp CRM Phase 1 Tasks 13-19 and created the final UAT readiness summary.
- Confirmed source-level CRM readiness is strong, but live UAT remains unaccepted because real WhatsApp device evidence is missing.
- Confirmed mobile, tablet, and desktop CRM layout coverage exists and remains in the smoke suite.
- Confirmed owner dashboard coverage exists and remains in the smoke suite.
- Confirmed webhook/send/Cloud API hardening coverage exists and remains in the smoke suite.
- Confirmed live UAT evidence exists at `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`, with all live scenarios marked not run and exact blockers documented.
- Confirmed `.env`, `.env.local`, and `.vercel` are ignored; reviewed WhatsApp CRM files/templates for obvious committed secrets and found only placeholder/template env values.
- Final decision: not ready for limited internal UAT until live deployment, live env, Meta webhook, Supabase mapping, real phones, and masked evidence are completed.

Files changed in this pass:

- `docs/WHATSAPP_CRM_PHASE1_UAT_READINESS_SUMMARY.md`
- `HANDOFF.md`

Commands run and results:

- `git ls-files .env .env.local .env.example .vercel` - no tracked env/secret files returned.
- `git check-ignore -v .env.local .env .vercel` - confirmed ignored by `.gitignore`.
- Secret-pattern scan over WhatsApp CRM files/docs found only placeholder/template values in `.env.example`; no live secrets were identified.
- All WhatsApp CRM coverage scripts - passed.
- WhatsApp CRM live helper scripts - passed safe local mode; live credential setup remains incomplete as expected in this session.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/send`, `/api/whatsapp/webhook`, and `/whatsapp-crm`.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Owner/admin must deploy the current CRM build to the correct public Meta-reachable URL, configure live server env vars, verify `whatsapp_accounts.phone_number_id`, complete Meta webhook challenge, run the live helper scripts, and capture masked real-phone UAT evidence.
- Phase 1 limitations remain: staff ranking by sender name, placeholder media unless secure media download/storage is added, no full chatbot, no quotation/invoice/payment gateway, and no ERP sync.

## 2026-06-24 - WhatsApp CRM failed-send evidence helper

Task completed:

- Continued the WhatsApp CRM Phase 1 UAT goal without changing runtime CRM behavior.
- Added an optional live/staging failed-send evidence script for Task 19 scenario 9.
- The script checks persisted outbound `crm_messages` after a controlled CRM browser failure test.
- It verifies a failed outbound message exists and that the stored payload includes a useful failed reason.
- It can narrow evidence by `WHATSAPP_UAT_CUSTOMER_ID` and `WHATSAPP_UAT_FAILED_AFTER`.
- Output is count/status focused and does not print customer phone numbers or private message bodies.
- Updated the live UAT checklist and readiness script to reference the failed-send evidence helper.

Files changed in this pass:

- `scripts/whatsapp-crm-failed-send-evidence.mjs`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-failed-send-evidence.mjs` - passed safe local mode; evidence not checked because `SUPABASE_SERVICE_ROLE_KEY` is not configured locally.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential groups remain incomplete and failed-send evidence guidance is shown.
- `node scripts\whatsapp-crm-send-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/send` and `/api/whatsapp/webhook`.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Task 19 remains not accepted because real phones, Meta dashboard access, live server secrets, correct public deployment, webhook challenge success, duplicate replay success, media replay success, outgoing browser send success/failure evidence, and Supabase `whatsapp_accounts.phone_number_id` verification are still required.
- In the live/staging runtime, trigger a controlled failed CRM reply through the browser UI, then run `node scripts\whatsapp-crm-failed-send-evidence.mjs` to capture stored failed-send evidence.

## 2026-06-24 - Retail Daily Sales remarks simplification

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Tucked Daily Sales optional remarks behind a `Remarks (optional)` disclosure.
- Kept the AutoCount attachment upload visible because missing attachment warnings are part of the Daily Sales workflow.
- Kept existing Retail permissions, outlet filtering, RLS, upload security, total-sales calculation, audit logging, server action field names, exports, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempts hit an active Next build lock; retry after the active process cleared passed.

Risks / remaining checks:

- Real manager phone/browser QA should confirm the optional remarks disclosure is discoverable when a sales note is needed.

## 2026-06-24 - Retail review form simplification

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Simplified manager review forms so the common review path shows only the record selector and decision first.
- Moved Expense Review rejection reason and remarks into `Rejection reason and remarks (if needed)`.
- Moved Processing Review rejection reason and remarks into the same optional disclosure.
- Added Processing Review success actions for `Review another processing` and `Back to Retail Home`.
- Kept existing Retail permissions, outlet filtering, RLS, review validation, audit logging, server actions, calculations, exports, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.
- First `npm.cmd run build` attempt hit an active Next build lock; retry after waiting passed.

Risks / remaining checks:

- Real manager phone/browser QA should confirm the rejection/remarks disclosure is easy to find when rejecting a record.

## 2026-06-24 - WhatsApp CRM media webhook replay helper

Task completed:

- Continued the WhatsApp CRM Phase 1 UAT goal without changing runtime CRM behavior.
- Added an optional live/staging media webhook replay script for Task 19 scenarios 3-6 prep.
- The script requires explicit `WHATSAPP_CRM_DEPLOYMENT_URL`, `WHATSAPP_APP_SECRET`, `WHATSAPP_UAT_PHONE_NUMBER_ID`, and `WHATSAPP_UAT_CUSTOMER_PHONE` before it posts anything.
- It sends signed synthetic image, document, audio, and location webhook payloads and expects all four to be stored or safely skipped as duplicates.
- Output is count/status focused and does not print tokens, app secrets, private messages, or full customer data.
- Updated the live UAT checklist and readiness script to reference the media/location replay helper.

Files changed in this pass:

- `scripts/whatsapp-crm-media-webhook-replay.mjs`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-media-webhook-replay.mjs` - passed safe local mode; replay not checked because deployment URL, app secret, UAT phone-number ID, and UAT customer phone are not configured locally.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential groups remain incomplete and media replay guidance is shown.
- `node scripts\whatsapp-crm-webhook-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/webhook`.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Task 19 remains not accepted because real phones, Meta dashboard access, live server secrets, correct public deployment, webhook challenge success, duplicate replay success, media replay success, outgoing browser send success, and Supabase `whatsapp_accounts.phone_number_id` verification are still required.
- In the live/staging runtime, run the mapping and challenge checks first, then run `node scripts\whatsapp-crm-media-webhook-replay.mjs` with an approved tester phone. It may create synthetic UAT messages in the target CRM database.

## 2026-06-24 - WhatsApp CRM outgoing send readiness helper

Task completed:

- Continued the WhatsApp CRM Phase 1 UAT goal without changing runtime CRM behavior.
- Added an optional live/staging outgoing-send readiness script for Task 19 scenarios 2 and 9 prep.
- The script verifies Supabase live env, active `whatsapp_accounts`, linked active CRM customers, and WhatsApp Cloud API token presence.
- It can target a specific CRM customer with `WHATSAPP_UAT_CUSTOMER_ID`.
- It intentionally does not send WhatsApp messages or bypass the staff-authenticated CRM `/api/whatsapp/send` route; the actual send still must be performed through the CRM UI by an authorized staff user.
- Output is count/status focused and masks phone numbers.
- Updated the live UAT checklist and readiness script to reference the outgoing readiness helper.

Files changed in this pass:

- `scripts/whatsapp-crm-outgoing-send-readiness.mjs`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-outgoing-send-readiness.mjs` - passed safe local mode; outgoing readiness not checked because `SUPABASE_SERVICE_ROLE_KEY` is not configured locally.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential groups remain incomplete and outgoing readiness guidance is shown.
- `node scripts\whatsapp-crm-send-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/send` and `/api/whatsapp/webhook`.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Task 19 remains not accepted because real phones, Meta dashboard access, live server secrets, correct public deployment, webhook challenge success, duplicate replay success, outgoing browser send success, and Supabase `whatsapp_accounts.phone_number_id` verification are still required.
- In the live/staging runtime, run `node scripts\whatsapp-crm-outgoing-send-readiness.mjs`, then perform the actual reply from the authenticated CRM UI and confirm the tester phone receives it.

## 2026-06-24 - Retail cash records route split

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Split the crowded `/retail/cash/history` page so it no longer shows both daily closing records and cash session records together.
- Changed `/retail/cash/history` into a two-button cash-record chooser.
- Added focused records pages:
  - `/retail/cash/history/closings`
  - `/retail/cash/history/sessions`
- Kept existing Retail permissions, outlet filtering, RLS, cash calculations, server actions, exports, and schema unchanged.

Files changed in this pass:

- `app/(erp)/retail/cash/history/closings/page.tsx`
- `app/(erp)/retail/cash/history/sessions/page.tsx`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- First parallel `npm.cmd run typecheck` hit stale/generated Next route metadata: `.next/types/validator.ts(5,79): error TS2307: Cannot find module './routes.js'`.
- First `npm.cmd run build` attempt hit an active Next build lock; retry after waiting passed and build output includes `/retail/cash/history/closings` and `/retail/cash/history/sessions`.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Real manager/admin phone/browser QA should confirm the cash records chooser makes it easier to find the desired table.

## 2026-06-24 - Retail setup form optional-detail simplification

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Reduced visible typing on Retail setup forms without changing submitted fields or server actions.
- Moved BOM expected yield min/max, expected wastage, and remarks into `Future yield settings and remarks (optional)`.
- Moved Retail price brand, origin, and effective end date into `Brand, origin, and end date (optional)`.
- Kept core setup fields visible: assigned outlet scope, category/BOM/price identity, raw/finished item names, unit price, active status, and effective start date.
- Kept existing Retail permissions, manager/admin gates, RLS, upload security, calculations, exports, server actions, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real manager/admin phone/browser QA should confirm the optional setup disclosures are easy to find when needed.

## 2026-06-24 - Retail settings setup page split

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Split the remaining crowded Retail Settings pages so setup form routes no longer show records tables on the same page.
- Kept `/retail/settings/boms`, `/retail/settings/categories`, and `/retail/settings/prices` focused on saving one setup record.
- Added separate records-only routes for setup reference tables:
  - `/retail/settings/boms/records`
  - `/retail/settings/categories/records`
  - `/retail/settings/prices/records`
- Added post-success actions on setup forms to add another record or view the matching records list.
- Kept existing Retail permissions, manager/admin gates, RLS, upload security, calculations, exports, server actions, and schema unchanged.

Files changed in this pass:

- `app/(erp)/retail/settings/boms/records/page.tsx`
- `app/(erp)/retail/settings/categories/records/page.tsx`
- `app/(erp)/retail/settings/prices/records/page.tsx`
- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes the new `/retail/settings/*/records` routes.
- `npm.cmd run smoke` - Retail checks passed, then full smoke failed on an unrelated Stock mobile coverage assertion: `Stock mobile item cards missing: className="shrink-0"`.

Risks / remaining checks:

- Real manager/admin phone/browser QA should confirm Settings still feels easy with setup and records split into separate routes.
- Full smoke should be rerun after the unrelated Stock mobile coverage issue is resolved.

## 2026-06-24 - WhatsApp CRM duplicate webhook replay helper

Task completed:

- Continued the WhatsApp CRM Phase 1 UAT goal without changing runtime CRM behavior.
- Added an optional live/staging duplicate webhook replay script for Task 19 scenario 8.
- The script requires explicit `WHATSAPP_CRM_DEPLOYMENT_URL`, `WHATSAPP_APP_SECRET`, `WHATSAPP_UAT_PHONE_NUMBER_ID`, and `WHATSAPP_UAT_CUSTOMER_PHONE` values before it posts anything.
- It sends the same signed synthetic WhatsApp text payload twice and expects the second response to report a skipped duplicate.
- Output is count/status focused and does not print tokens, app secrets, private messages, or full customer data.
- Updated the live UAT checklist and readiness script to reference the replay helper.

Files changed in this pass:

- `scripts/whatsapp-crm-duplicate-webhook-replay.mjs`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-duplicate-webhook-replay.mjs` - passed safe local mode; replay not checked because deployment URL, app secret, UAT phone-number ID, and UAT customer phone are not configured locally.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential groups remain incomplete and duplicate-replay guidance is shown.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-webhook-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/webhook`.
- First `npm.cmd run smoke` attempt failed on unrelated Stock mobile coverage while the worktree was moving: `Stock mobile item cards missing: className="shrink-0"`.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed immediately afterward without a WhatsApp or Stock edit.
- `npm.cmd run smoke` - passed on rerun.

Remaining risks / next steps:

- Task 19 remains not accepted because real phones, Meta dashboard access, live server secrets, correct public deployment, webhook challenge success, duplicate replay success, and Supabase `whatsapp_accounts.phone_number_id` verification are still required.
- In the live/staging runtime, run the mapping and challenge checks first, then run `node scripts\whatsapp-crm-duplicate-webhook-replay.mjs` with an approved tester phone. It may create one synthetic UAT customer/message in the target CRM database.

## 2026-06-24 - WhatsApp CRM webhook challenge readiness check

Task completed:

- Continued the WhatsApp CRM Phase 1 UAT goal without changing runtime CRM behavior.
- Added an optional live/staging webhook challenge script for Meta verification prep.
- The script uses `WHATSAPP_CRM_DEPLOYMENT_URL` plus `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_WEBHOOK_VERIFY_TOKEN`, or `VERIFY_TOKEN`.
- It expects the real token to return the one-time challenge text and a fake token to return route-level `403 Forbidden`.
- Output intentionally lists only configured env names and response status summaries; it does not print the verify token.
- Updated the live UAT checklist and readiness script to reference the challenge checker.

Files changed in this pass:

- `scripts/whatsapp-crm-webhook-challenge-check.mjs`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-webhook-challenge-check.mjs` - passed safe local mode; challenge not checked because deployment URL and verify token are not configured locally.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential groups remain incomplete and challenge-check guidance is shown.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/webhook`.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Task 19 remains not accepted because real phones, Meta dashboard access, live server secrets, correct public deployment, webhook challenge success, and Supabase `whatsapp_accounts.phone_number_id` verification are still required.
- In the live/staging runtime, run `node scripts\whatsapp-crm-webhook-challenge-check.mjs`; it should pass before entering the callback URL in Meta or before rerunning Meta webhook verification.

## 2026-06-24 - Retail Submit Expense receipt error polish

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification work for the worker Submit Expense page.
- Kept `/retail/expenses` focused on submitting one expense with date, category, amount, payment method, required receipt upload, optional supplier/payee, and optional remarks only.
- Added a visible inline `Receipt is required.` message on the receipt file input when the worker tries to submit without a receipt.
- Kept the server-side receipt requirement, outlet scope hidden field for assigned-outlet staff, upload path handling, manager review route, reports, RLS, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real worker phone/browser QA should confirm the receipt picker, selected filename row, inline missing-receipt error, and upload state feel clear on the actual target devices.

## 2026-06-24 - WhatsApp CRM account mapping readiness check

Task completed:

- Continued the WhatsApp CRM Phase 1 UAT goal without changing runtime CRM behavior.
- Added a live/staging `whatsapp_accounts` mapping readiness script for Task 19 second-number routing prep.
- The script checks `whatsapp_accounts` with Supabase service-role credentials only when `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are configured in the runtime.
- The script can compare expected Meta `phone_number_id` values from `WHATSAPP_EXPECTED_PHONE_NUMBER_IDS`, `WHATSAPP_PHONE_NUMBER_IDS`, or `WHATSAPP_PHONE_NUMBER_ID`.
- Output is intentionally masked/count-only: it does not print Supabase keys, customer phone numbers, private messages, or full Meta phone-number IDs.
- Updated the live UAT checklist and readiness script so the account-mapping command is part of the live setup flow.

Files changed in this pass:

- `scripts/whatsapp-crm-account-mapping-readiness.mjs`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-account-mapping-readiness.mjs` - passed safe local mode; mapping not checked because `SUPABASE_SERVICE_ROLE_KEY` is not configured locally.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential groups are still incomplete and mapping helper guidance is shown.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/webhook`.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Task 19 remains not accepted because real phones, Meta dashboard access, live server secrets, correct public deployment, and Supabase `whatsapp_accounts.phone_number_id` verification are still required.
- In the live/staging runtime, set `WHATSAPP_EXPECTED_PHONE_NUMBER_IDS` and rerun `node scripts\whatsapp-crm-account-mapping-readiness.mjs`; it should pass before second-number routing UAT.

## 2026-06-24 - Retail audit trail page simplification

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Simplified `/retail/settings/audit` so it shows only the Retail audit trail task.
- Removed the extra static `Manager controls` setup-status card from the audit page.
- Kept audit log data, table columns, manager/admin/director access rules, RLS, server actions, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - initial attempts hit an active Next build lock; retry passed.
- `npm.cmd run typecheck` - passed after build completed.

Risks / remaining checks:

- Real manager/admin phone/browser QA should confirm Audit Trail remains clear without the removed static controls panel.

## 2026-06-24 - Retail Today Summary quick-check simplification

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Changed `/retail/today` into a read-only Today Summary quick-check page.
- Removed the `Next actions` card from Today Summary so it no longer acts as a second task launcher.
- Kept task launching on Retail Home and kept worker outlet/today filtering unchanged.
- Kept Retail permissions, RLS, outlet/team isolation, upload security, calculations, export rules, server actions, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - initial attempts hit an active Next build lock; retry passed.
- `npm.cmd run typecheck` - passed after build completed.

Risks / remaining checks:

- Real worker phone/browser QA should confirm Today Summary still gives enough outlet status context without duplicating Home actions.

## 2026-06-24 - WhatsApp CRM live UAT reachability helper

Task completed:

- Continued the WhatsApp CRM Phase 1 UAT goal without changing runtime CRM behavior.
- Confirmed the old Processing route smoke blocker is not current in this workspace: `node scripts\smoke-routes.mjs` passes.
- Added an optional deployment webhook reachability check to `scripts/whatsapp-crm-live-uat-readiness.mjs`.
- The check only runs when `WHATSAPP_CRM_DEPLOYMENT_URL` is configured, uses a fake verify token, and does not print secrets.
- The helper now reports whether the public deployment returns the expected route-level wrong-token response, is blocked by deployment authentication, or does not serve `/api/whatsapp/webhook`.
- Tested the inspected Vercel deployment URL with the helper; it reports deployment authentication blocks Meta webhook verification.
- Re-listed the linked Vercel project deployments for `elite-meat-s-projects/frozen-pork-whatsapp-bot`; the newest production deployment in the first page is still about 14 days old, which reinforces that the current ERP CRM build has not been deployed there yet or that this is not the correct CRM project.
- Updated the live UAT checklist to document the optional `WHATSAPP_CRM_DEPLOYMENT_URL` readiness helper.

Files changed in this pass:

- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\smoke-routes.mjs` - passed.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; no deployment URL configured locally, so public reachability was not checked.
- `WHATSAPP_CRM_DEPLOYMENT_URL=https://frozen-pork-whatsapp-ba3gy1txy-elite-meat-s-projects.vercel.app node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness and reported `blocked by deployment authentication; Meta webhook verification cannot reach this URL`.
- `vercel.cmd ls --scope elite-meat-s-projects` - passed after sandbox escalation; latest listed production deployments for `frozen-pork-whatsapp-bot` are roughly 14 days old.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - rerun after the deployment listing; passed source readiness and still reports local live credential groups missing.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/webhook`.
- `npm.cmd run smoke` - passed, including all WhatsApp CRM coverage scripts and the Processing route smoke guard.

Remaining risks / next steps:

- Task 19 remains not accepted because real phones, Meta dashboard access, live server secrets, correct public deployment, and Supabase `whatsapp_accounts.phone_number_id` verification are still required.
- The correct deployment should return `403 Forbidden` for the helper's fake verify token before trying the real Meta challenge.
- Confirm whether `frozen-pork-whatsapp-bot` is the intended project for the ERP WhatsApp CRM. If it is, deploy the current workspace build there before Meta webhook verification; if not, link the correct Vercel project and rerun the readiness helper against that URL.
- The global ERP smoke suite is currently clean in this workspace.

## 2026-06-24 - Stock unit movement card layout

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock unit detail mobile movement cards so movement type/date and weight stack on very narrow phones and only sit side-by-side at the 390px breakpoint.
- Kept movement data, label reprint behavior, RLS, server actions, stock movements, and permissions unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for barcode movement history readability around 390px width.

Files changed in this pass:

- `components/stock/stock-unit-detail.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated `scripts/retail-v1-e2e-coverage.mjs`: `Scenario 25 failed: Processing warning flag appears when values look abnormal` / `Reports must render abnormal processing warnings. Missing: Processing abnormal warning`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm Stock unit movement cards stay readable with long movement/location/reference values.
- Full smoke needs the unrelated Retail V1 E2E abnormal-processing warning assertion resolved or isolated before it can be green again.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Retail Reports launcher simplification

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Changed `/retail/reports` from the old all-in-one report dashboard into a role-based report launcher.
- Retail managers now get a large shortcut to the focused assigned-outlet report.
- Admin/director users now get large shortcuts to focused report/export tasks: All Outlet Report, Cash Variance, Missing Tasks, Processing Report, and Export Center.
- Kept workers blocked from full Retail reports.
- Kept the existing focused report pages, filters, CSV export rules, RLS, outlet scoping, calculations, server actions, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after build completed.

Risks / remaining checks:

- Real manager/admin phone/browser QA should confirm `/retail/reports` now feels like a clear report chooser and that focused report pages remain reachable from navigation.

## 2026-06-24 - WhatsApp CRM live UAT deployment reachability check

Task completed:

- Continued Task 19 live WhatsApp CRM UAT readiness checks without changing CRM behavior.
- Rechecked the inspected production URL for the WhatsApp webhook path using a fake verify token only.
- Public browser-style access to the deployment is blocked by Vercel Authentication, so Meta webhook verification cannot reach `/api/whatsapp/webhook` until the webhook URL is public or an approved webhook-access approach is configured.
- Authenticated `vercel curl` generated a deployment protection bypass token for project `prj_Z4kP1Kn4TyG3gDDINOGbVV4CVJ91`, but the underlying deployment returned `Cannot GET /api/whatsapp/webhook`.
- The current workspace does contain `app/api/whatsapp/webhook/route.ts`, so the inspected deployment likely does not contain the current CRM build, is pointing at the wrong project/deployment, or needs a fresh deployment before Meta webhook verification.
- No secrets, real customer phone numbers, or private messages were used, printed, or committed.

Files changed in this pass:

- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential groups are still incomplete for service-role key, WhatsApp token, API version, verify token, and app secret.
- Public webhook GET with fake verify token - blocked by Vercel Authentication Required page.
- `vercel.cmd curl` to the inspected webhook URL - reached deployment through Vercel bypass, but returned `Cannot GET /api/whatsapp/webhook`.
- `npm.cmd run lint` - passed with one existing warning in `components/retail/retail-page.tsx` for unused `RetailReportsView`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed; build output includes `/api/whatsapp/webhook`.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Confirm the correct Vercel project/deployment for the CRM, redeploy the current workspace if needed, and make the webhook callback URL publicly reachable by Meta.
- Re-run Meta webhook challenge only after the deployed route returns the expected route-level response, such as `403 Forbidden` for a wrong token and the challenge text for the real token.
- Task 19 remains not accepted until the real-device scenarios are run with masked evidence.

## 2026-06-24 - Stock label preview wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the on-screen Stock label preview so company name, product name, and weight wrap instead of clipping in the mobile preview card.
- Kept generated barcode format, label size, print CSS, PDF output, RLS, server actions, stock movements, and permissions unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for label preview readability around 390px width.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long product label previews stay readable before Bluetooth print/PDF fallback.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - WhatsApp CRM live env alias compatibility

Task completed:

- Added server-side fallback support for existing deployed WhatsApp env aliases found on the inspected Vercel project.
- `WHATSAPP_ACCESS_TOKEN` remains the preferred access token name, with `WHATSAPP_CLOUD_API_TOKEN` and `WHATSAPP_TOKEN` accepted as fallbacks.
- `WHATSAPP_VERIFY_TOKEN` remains the preferred webhook verify token name, with `WHATSAPP_WEBHOOK_VERIFY_TOKEN` and `VERIFY_TOKEN` accepted as fallbacks.
- Updated live readiness and Cloud API coverage scripts to detect/guard the fallback aliases.
- Updated `.env.example` with the older fallback alias names as documentation only.
- No secrets were added, printed, or committed. No WhatsApp CRM UI behavior changed.

Files changed in this pass:

- `.env.example` (ignored template file)
- `lib/whatsapp-crm/cloud-api.ts`
- `scripts/whatsapp-crm-cloud-api-coverage.mjs`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `HANDOFF.md`

Commands run and results:

- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; local live credential setup still incomplete because `.env.local` lacks service-role key, WhatsApp token, API version, verify token, and app secret.
- `node scripts\whatsapp-crm-webhook-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-send-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Remaining risks / next steps:

- This compatibility change helps the inspected Vercel project use existing `WHATSAPP_TOKEN` / `VERIFY_TOKEN` names, but live UAT still requires Supabase server env vars, Meta app secret, webhook verification, tester phones, and `whatsapp_accounts.phone_number_id` mapping.
- Task 19 remains not accepted until real phone scenarios are executed and masked evidence is captured.

## 2026-06-24 - Retail manager Sales, Review, and Cash task-only pages

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Simplified `/retail/sales` so Daily Sales entry no longer shows a sales-history related action; sales history remains on `/retail/sales/history`.
- Simplified `/retail/expenses/review` so Expense Review no longer shows a history related action; expense history remains on `/retail/expenses/history`.
- Simplified `/retail/cash/open`, `/retail/cash-closing`, and `/retail/cash/close` so each page focuses on one cash task and no longer shows adjacent cash task or history links.
- Added post-success actions to Daily Sales, Expense Review, Open Cash, Cash Closing, and Close Cash forms for repeat work plus `Back to Retail Home`.
- Kept Retail permissions, RLS, outlet/team isolation, upload security, calculations, export rules, server actions, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed after build completed.

Risks / remaining checks:

- Real manager phone/browser QA should confirm the removed related links do not slow the normal Daily Sales, review, and Cash Closing workflow.

## 2026-06-24 - Task 19 live WhatsApp CRM UAT attempt

Task status:

- Task 19 live real-device UAT was attempted from the Codex session, but it is not accepted yet.
- Real WhatsApp phone scenarios were not executed because live Meta app access, configured WhatsApp/Supabase server env vars, confirmed `whatsapp_accounts.phone_number_id` rows, and tester phones are not available to Codex.
- No customer phone numbers, private messages, access tokens, app secrets, or service-role keys were printed or committed.
- WhatsApp CRM source/regression validation remains clean.

Files changed in this pass:

- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md`
- `HANDOFF.md`

Evidence saved:

- `docs/qa-evidence/whatsapp-crm-live-uat/task-19-attempt-2026-06-24.md` records all 13 requested scenarios as `Not run`, with blocker notes for each scenario.

Live scenario status:

- Real incoming text: not run, blocked by missing live webhook/env/device access.
- CRM reply received by customer: not run, blocked by missing live access token/runtime env and tester phone.
- Image/file/audio/location handling: not run, blocked by missing live phone/webhook test.
- Second WhatsApp number routing: not run, blocked by missing second-number live test and Supabase account mapping verification.
- Duplicate webhook replay: not run, source hardening exists but live/staging replay was not possible.
- Failed outgoing state: not run live, source coverage exists.
- Auto-assignment and owner dashboard update: not run live, source coverage exists.
- Mobile and desktop screenshots: not captured because real live message flow did not run.

Commands run and results:

- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; reported missing live credential groups for service-role key, WhatsApp access token, API version, webhook verify token, and app secret.
- `npm.cmd run smoke` - passed, including all WhatsApp CRM coverage scripts.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Risks / remaining checks:

- Task 19 cannot be accepted until the exact live env vars are configured, Meta webhook verification succeeds, real phones send/receive messages, and masked screenshots/notes are captured.
- The inspected Vercel project still needs confirmation as the correct ERP WhatsApp CRM deployment target.
- `whatsapp_accounts.phone_number_id` mapping must be verified against the target Supabase staging/production database before second-number UAT can pass.

## 2026-06-24 - Task 18 WhatsApp CRM live UAT environment audit

Task status:

- Prepared/audited the live UAT environment path, but Task 18 is not accepted yet.
- The local workspace is linked to Vercel project `elite-meat-s-projects/frozen-pork-whatsapp-bot` for inspection only.
- Production deployments exist and the latest inspected deployment is READY at `frozen-pork-whatsapp-ba3gy1txy-elite-meat-s-projects.vercel.app`.
- Exact CRM env names required by Task 18 are not fully configured in the inspected Vercel production environment.
- No secrets were printed, committed, or copied into tracked files.

Vercel production env audit:

- Present legacy/older WhatsApp env names: `VERIFY_TOKEN`, `WHATSAPP_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`.
- Present non-CRM/bot env names include Blob, Google Sheets, admin, price-list, and message-cost settings.
- Missing exact Task 18 CRM env names from production listing: `WHATSAPP_VERIFY_TOKEN`, `WHATSAPP_APP_SECRET`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_GRAPH_API_VERSION`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`.
- Local `.env.local` contains only `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` among the Task 18 required names.

Files changed in this pass:

- `HANDOFF.md`

Local ignored files touched/created:

- `.vercel/` was created by `vercel.cmd link` and is ignored by git.

Commands run and results:

- `vercel.cmd link --yes --project frozen-pork-whatsapp-bot --scope elite-meat-s-projects` - passed after escalation for CLI network/cache access.
- `vercel.cmd env ls production --scope elite-meat-s-projects` - passed after escalation; only env names/scopes were reviewed, no values were printed.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; reported missing live credential groups for service-role key, WhatsApp access token, API version, webhook verify token, and app secret.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed.

Remaining risks / next steps:

- Confirm whether `frozen-pork-whatsapp-bot` is the intended deployed target for the new WhatsApp CRM ERP page, or provide/link the correct deployment project.
- Configure the exact Task 18 env names in the deployment dashboard or provide approval to safely duplicate existing legacy encrypted values into the new names.
- Add/provide `WHATSAPP_APP_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, and the target Supabase staging/production public URL and anon key.
- Set Meta webhook callback to `https://<deployment-url>/api/whatsapp/webhook` after the verify token is configured.
- Verify webhook challenge and `whatsapp_accounts.phone_number_id` mapping with the real WhatsApp number before live device UAT.

## 2026-06-24 - Retail worker Cleaning and Processing task-only pages

Task completed:

- Continued the Retail V1 one-task-per-page UX simplification goal.
- Simplified `/retail/cleaning` into a focused `Complete Cleaning` worker page.
- Removed cleaning setup/history related actions from the Complete Cleaning page; cleaning setup remains on `/retail/cleaning/tasks` and cleaning history remains on `/retail/cleaning/history`.
- Added post-completion actions for cleaning: `Complete another task` and `Back to Retail Home`.
- Simplified `/retail/processing` into a focused `Record Processing` worker page.
- Removed processing review/history/order-picking links and explanatory side content from the Record Processing page; review and history remain on `/retail/processing/review` and `/retail/processing/history`.
- Updated processing form language from module-style `Daily processing record` / `Save processing` to worker task language: `Record Processing` / `Submit processing`.
- Added post-submit actions for processing: `Record another processing` and `Back to Retail Home`.
- Kept Retail permissions, RLS, outlet/team isolation, upload security, calculations, export rules, server actions, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\smoke-routes.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed; one parallel rerun briefly failed while build regenerated `.next/types`, then passed when rerun after build completed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first attempts hit an existing Next build lock; retry passed.

Risks / remaining checks:

- Real worker phone/browser QA should confirm the simplified Cleaning and Processing pages feel faster and do not need the removed secondary links.

## 2026-06-24 - Stock workflow disclosure tap targets

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated remaining Stock workflow disclosure headers to use phone-size tap targets across optional/reference sections.
- Covered worker scan flows beyond the inbound setup pass, including outbound, transfer, receive-transfer, return/damage, and supplier-return optional/reference sections where present.
- Kept hidden fields, validation, RLS, server actions, stock movements, workflow rules, and permissions unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - initial attempts hit an active Next build lock; after waiting for active workers to settle, rerun passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm optional/reference sections open comfortably on Stock worker pages.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Order UX source coverage gate

Task completed:

- Added a focused Order UX source coverage script to guard the simplified worker experience.
- Wired `scripts/order-ux-simplification-coverage.mjs` into `npm.cmd run smoke` after the existing Order QA environment coverage.
- The coverage now checks that:
  - `/orders` has the task-home buttons and task queues.
  - Worker/default `/orders` does not render the advanced table directly.
  - Advanced Order List and Reports remain gated to manager/admin/director roles, not general workers or account-only users.
  - `/orders/create` keeps the five-step guided flow, big order type choices, name+phone quick add, and total-price-only wording.
  - `/orders/picking` keeps the card-based order chooser, scan/manual/ready actions, required/picked/remaining/tolerance display, manual reasons, and hidden recent log disclosure.
  - `/orders/ready` keeps Ready for Pickup, Ready for Delivery, and Internal Transfer card sections, with one main card action.
  - `/orders/customers` keeps search, Add Customer, name+phone quick add, simple cards, and manager/admin Advanced Customer Details.
  - Worker-facing Order UI does not include customer outstanding amount, finance invoice wording, payment status, item-level price, stock value, or profit wording.
  - `/orders/new` and `/orders/prepare` still redirect to canonical `/orders/create` and `/orders/picking`.
- No Order Module business logic, server actions, migrations, RLS, reservations, picking behavior, delivery handoff, pickup completion, or cancellation behavior was changed.

Files changed in this pass:

- `scripts/order-ux-simplification-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\order-ux-simplification-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed, including the new Order UX simplification coverage.
- `npm.cmd run build` - passed.

Remaining issue:

- Real browser/mobile visual QA is still blocked in this Codex session by the Windows browser launch permission error `CreateProcessAsUserW failed: 5`. The source/build/smoke gates now guard the simplified UX structure, but final human/mobile visual acceptance still needs a working browser session.

## 2026-06-24 - Task 17 Processing smoke blocker fix

Task completed:

- Verified the smoke assertion remains active in `scripts/smoke-routes.mjs`.
- Confirmed the Retail processing surface includes the required `/orders/picking` link labelled `Prepare customer orders`.
- Confirmed the Processing dashboard also has an order-preparation card linking to `/orders/picking`.
- Confirmed the Retail processing surface clearly states finished goods only enter stock after packing, barcode labelling, and inbound scan.
- Kept the smoke assertion unchanged and did not change WhatsApp CRM behavior.

Files changed in this pass:

- `HANDOFF.md`

Existing relevant source now satisfying the assertion:

- `components/retail/retail-page.tsx`
- `components/processing/processing-page.tsx`
- `scripts/smoke-routes.mjs`

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - initial parallel run failed while build was regenerating `.next/types`; rerun after successful build passed.

Risks / remaining checks:

- Task 17 is accepted from source and validation evidence.
- WhatsApp CRM live UAT is still pending real Meta credentials, public webhook URL, deployment env vars, and tester phones.

## 2026-06-24 - Retail Submit Expense focused worker page

Task completed:

- Simplified `/retail/expenses` into a single-purpose worker page titled `Submit Expense`.
- Removed expense review/history/report actions from the submit page; manager expense review remains on `/retail/expenses/review`.
- Kept only the needed submit fields on the worker path: date, category, amount, payment method, required receipt upload, and optional supplier/payee plus remarks behind an optional disclosure.
- Improved the receipt upload UX with a clearer selected filename row, existing `Uploading...` submit state, and simple missing-receipt error text: `Receipt is required.`
- Added success actions after submit: `Submit another expense` and `Back to Retail Home`.
- Preserved outlet/team isolation and existing Retail upload/storage handling.
- Added ESLint ignore coverage for `.worktrees/**` so validation skips generated build artifacts from local secondary worktrees.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `eslint.config.mjs`
- `HANDOFF.md`

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed, including Retail Module V1 coverage and Retail V1 E2E coverage.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- No database, RLS, or storage policy behavior changed in this pass.
- Real worker phone/browser click-through is still recommended before broad staff rollout, especially receipt upload from camera/gallery.

## 2026-06-24 - Order Module ready/customers UX final pass

Task completed:

- Continued the Order Module UX simplification goal only.
- Refactored `/orders/ready` into a clearer card-based task view with sections: Ready for Pickup, Ready for Delivery, and Internal Transfer.
- Changed ready order cards so each card shows only order number, customer/internal-transfer label, order type, required date/time, picked/estimated weight, status badge, and one main action.
- Moved pickup completion onto the pickup order card through the existing `markPickupCompletedAction`; no pickup backend behavior changed.
- Delivery ready cards now use one Open/View Delivery action and preserve the existing delivery handoff link behavior.
- Removed the generic select-based ready/pickup/cancel action area from `/orders/ready` worker view.
- Kept the Advanced Order List and Reports disclosure available on `/orders/ready` for manager/admin/director-style users.
- Simplified `/orders/customers` default cards to name, phone, credit warning badge, and one Create Order action.
- Added a name+phone-only quick customer form for the simplified customers page, using the existing `quickAddCustomerAction`.
- Kept optional customer details behind an Advanced Customer Details disclosure for manager/admin/director-style users.
- Did not change Order Module server actions, RLS, reservations, picking logic, delivery handoff logic, pickup completion logic, cancellation logic, or migrations.
- Confirmed source scan still avoids customer outstanding amount, finance invoice wording, payment status, item-level price, cost, profit, and stock value in worker-facing Order UI.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

QA / validation:

- `/orders/ready`, `/orders/customers`, and `/delivery` unauthenticated HTTP checks redirect to `/login` as expected.
- Browser visual QA could not run because the in-app browser failed to start with the existing Windows permission error: `CreateProcessAsUserW failed: 5`.
- A non-browser login POST route check was attempted, but the already-running dev server returned a Next dev manifest error while posting `/login`; this was not reproduced in production build.
- Source-level role checks confirmed worker-facing Order UI keeps Advanced sections out of default worker paths while manager/admin/director paths have advanced disclosures available.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - initially failed inside webpack hashing because of stale generated `.next` output; removed generated `.next` and reran successfully.

Risks / remaining checks:

- Real browser/mobile visual QA with the listed QA users is still required before claiming the simplified Order UX is fully UAT-ready.
- Backend workflow was not changed in this pass; create/reserve/pick/ready/delivery/pickup behavior relies on the previously passing Order V1 backend/RLS QA.
- Recommendation: ready for controlled product-manager review of source and build, but not ready for full UAT sign-off until manual visual QA is completed in a working browser session.

## 2026-06-24 - WhatsApp CRM UAT goal smoke unblock

Task status:

- Continued the WhatsApp CRM Phase 1 UAT-readiness goal.
- Cleared the previously reported global smoke blocker for the Processing/Retail order-preparation link; current `node scripts\smoke-routes.mjs` passes.
- Full `npm.cmd run smoke` now passes again, including all WhatsApp CRM coverage scripts.
- Task 16 live WhatsApp device QA remains not accepted because live Meta credentials, a public webhook URL, deployment env vars, and tester phones are still required.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `lib/retail/actions.ts`
- `HANDOFF.md`

Summary:

- Confirmed the current Retail/Processing source contains the Orders picking handoff and finished-goods stock-entry warning expected by smoke.
- Restored the exact Delivery handoff instruction on the Orders linked-delivery summary so Delivery coverage passes.
- Aligned Retail expense receipt validation text so both Retail coverage suites recognize the required receipt proof rule.
- Kept WhatsApp CRM behavior unchanged.

Commands run and results:

- `node scripts\smoke-routes.mjs` - passed.
- `node scripts\delivery-module-coverage.mjs` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; still reports missing live server credential setup for service-role key, WhatsApp access token, API version, webhook verify token, and optional app secret.
- `node scripts\whatsapp-crm-owner-dashboard-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after a fresh build regenerated `.next/types`.
- `npm.cmd run build` - passed once before the final two tiny coverage-copy edits; the final rerun is currently blocked by an active Next build lock/process in the shared workspace.
- `npm.cmd run smoke` - passed.

Remaining risks / next steps:

- Rerun `npm.cmd run build` once the active `.next\lock` clears.
- Configure live WhatsApp deployment env vars and Meta webhook callback URL, then execute the scenarios in `docs/WHATSAPP_CRM_LIVE_UAT.md` with real tester phones.
- Do not mark Task 16 accepted until real incoming text, outgoing CRM reply delivery, media/location handling, second-number routing, duplicate webhook handling, failed-send display, dashboard update, and mobile/desktop screenshots are captured.

## 2026-06-24 - Stock inbound disclosure tap targets

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound disclosure headers for `New product` and `Weight rule and notes` to use phone-size tap targets.
- Updated quick product `Create` to be a phone-size full-width action.
- Updated inbound decode and duplicate warning cards so worker messages wrap cleanly at phone width.
- Kept item creation logic, barcode decoding, duplicate blocking, RLS, server actions, stock movements, and permissions unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `eslint.config.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - first run entered generated `.worktrees\delivery-v1-deploy\.next` output; added explicit generated-worktree ignores in `eslint.config.mjs`, rerun passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - stale Next build-lock retries failed, then the lock cleared and rerun passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm inbound disclosure headers, quick product creation, and scan feedback are easy to tap/read.
- Full smoke passed in this pass.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Retail Task 2 Submit Expense page simplification

Task completed:

- Simplified `/retail/expenses` into one page for one task: submit one expense with receipt.
- Removed secondary review/history links from the Submit Expense page; manager review remains on `/retail/expenses/review`.
- Kept the page title and form task as `Submit Expense`.
- Preserved the required fields and outlet context while keeping optional supplier/payee and remarks behind a small optional disclosure.
- Kept receipt upload required and made the selected receipt filename clearer with `Selected file: ...`.
- Kept the submit button large and full-width on phone.
- Kept the upload pending state as `Uploading...` when a file is selected.
- Changed the missing receipt message to `Receipt is required.`
- Added success actions after submit: `Submit another expense` and `Back to Retail Home`.
- Kept Retail permissions, RLS, outlet/team isolation, upload security, calculations, export rules, actions, and schema unchanged.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- Full lint/smoke/build validation - pending this pass.

Risks / remaining checks:

- Real phone QA should confirm workers understand the optional supplier/remarks disclosure and the selected receipt filename row.

## 2026-06-24 - Stock shortcut flexible-height buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock worker home and shared Stock shortcut buttons to use flexible minimum height with padding instead of fixed height, so labels can wrap without clipping on phone width or larger browser text.
- Kept shortcut routes, role visibility, RLS, stock workflows, server actions, and permissions unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for shortcut readability around 390px width.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated `scripts/delivery-module-coverage.mjs`: `Order page linked delivery display missing: Delivery handoff will be available from the Delivery module list.`
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt hit an existing Next build lock; retry passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm Stock shortcut labels stay large, readable, and unclipped.
- Full smoke needs the unrelated Delivery/Order coverage assertion resolved or isolated before it can be green again.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Retail worker optional-field simplification

Task completed:

- Continued Retail V1 one-task UX simplification after the role-based Home launcher.
- Kept Retail actions, permissions, outlet/team isolation, RLS, upload security, calculations, exports, and schema unchanged.
- Tucked optional worker fields behind disclosures so the main task stays visible on phone width:
  - Expense supplier/payee and remarks now sit under `Supplier / remarks (optional)`.
  - Cleaning photo and remarks now sit under `Photo / remarks (optional)`.
  - Processing draft status, wastage reason, wastage photo, wastage remarks, and general remarks now sit under `Optional details`.
- Preserved required expense receipt upload, cleaning Complete button, processing raw/finished lines, wastage weight, and file upload wiring.
- Extended Retail source coverage for the optional-field simplification.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- Later `npm.cmd run typecheck` - failed on unrelated Orders work: `components/orders/orders-page.tsx(1444,16): error TS2304: Cannot find name 'QuickCustomerForm'.`
- `npm.cmd run smoke` - failed before Retail checks on unrelated Delivery/Orders coverage: `Order page linked delivery display missing: Delivery handoff will be available from the Delivery module list.`
- `npm.cmd run build` - failed on unrelated Orders syntax in `components/orders/orders-page.tsx` around line 1038: `Expected '</', got ':'`.

Risks / remaining checks:

- Real phone QA should confirm workers notice optional disclosure rows without thinking they are required.
- Processing remains a longer form because V1 intentionally supports multiple raw and finished lines; extra lines and optional details are collapsed to reduce first-screen load.

## 2026-06-24 - Stock alert card wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock dashboard alert cards so negative-stock, stock-age, and overdue-transfer item/location/reason/route text wraps on phone width.
- Kept alert data, thresholds, visibility, RLS, server actions, stock movements, approvals, and permissions unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for alert card readability around 390px width.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long Stock dashboard alert content stays inside red/yellow/orange cards.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Retail Home role-based task launcher

Task completed:

- Continued Retail V1 one-task UX simplification.
- Rebuilt Retail Home as a role-based task launcher using large mobile-friendly buttons with short descriptions.
- Worker Retail Home now shows only:
  - Submit Expense
  - Complete Cleaning
  - Record Processing
  - Today Summary
- Manager Retail Home now shows:
  - Daily Sales
  - Cash Closing
  - Review Expenses
  - Review Processing
  - Cleaning Setup
  - Outlet Report
- Admin/director Retail Home now shows:
  - All Outlet Report
  - Cash Variance
  - Missing Tasks
  - Processing Report
  - Export Center
- Removed the old manager/admin report tables from Retail Home so Home is a launcher only.
- Added focused report targets for the admin/director launcher buttons and manager Outlet Report.
- Kept outlet/team isolation, RLS, upload security, calculations, exports, actions, and database schema unchanged.
- Updated Retail coverage and docs for the role-based launcher.

Files changed in this pass:

- `app/(erp)/retail/today/page.tsx`
- `app/(erp)/retail/reports/outlet/page.tsx`
- `app/(erp)/retail/reports/all/page.tsx`
- `app/(erp)/retail/reports/cash-variance/page.tsx`
- `app/(erp)/retail/reports/missing-tasks/page.tsx`
- `app/(erp)/retail/reports/processing/page.tsx`
- `app/(erp)/retail/reports/export/page.tsx`
- `components/retail/retail-page.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- First `npm.cmd run build` attempt was blocked by a concurrent Next build lock.
- `npm.cmd run build` - passed on retry.

Risks / remaining checks:

- Real phone/browser QA should confirm the role-based launcher feels clear for worker, manager, and admin/director users.
- Focused report pages reuse the existing report builder and filters; no new report data model was added.

## 2026-06-24 - Stock table value wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated shared Stock table cells so long barcodes, locations, references, item names, and status text wrap instead of being clipped in the fallback table below mobile cards.
- Kept table data, sorting behavior, RLS, server actions, stock movements, reports, and permissions unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for table-header and table-value usability around 390px width.

Files changed in this pass:

- `components/stock/data-table.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm Stock fallback table headers and long table values are usable below the mobile cards.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Order Module UX audit follow-up

Task completed:

- Performed a scoped Order Module UI audit and documented the findings in `docs/order-ux-simplification-notes.md`.
- Kept Order Module business logic, server actions, stock reservation behavior, delivery handoff behavior, cancellation behavior, migrations, and RLS unchanged.
- Confirmed `/orders` is now task-first by default, with dense tables, KPIs, filters, reports, logs, reservations, and notifications kept in Advanced view for manager/admin/director roles.
- Removed account-only roles from the default Advanced Order List UI exposure; account users still follow existing backend permissions, but the worker-facing Orders home no longer treats account as a manager/admin advanced-view role.
- Refined `/orders/picking` so workers choose orders from simple cards before scanning or entering manual weight.
- Kept the compact picking selector as a secondary fallback and moved Recent Picking Log into a disclosure.
- Confirmed Order V1 UI continues to avoid customer outstanding amount, finance invoice data, payment status, item-level pricing, cost, profit, and stock value.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `docs/order-ux-simplification-notes.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - first final attempt hit Next's transient "Another next build process is already running" lock; retry passed.

Risks / remaining checks:

- Real browser visual QA around 390px is still recommended for `/orders/create`, `/orders/picking`, `/orders/[id]`, `/orders/ready`, `/orders/customers`, and the Delivery available list.
- This pass did not change or re-test backend order creation/reservation/picking transactions beyond preserving the existing action wiring.

## 2026-06-24 - Order Module worker UX simplification

Task completed:

- Simplified Order Module worker-facing screens without changing Order backend logic, RLS, reservations, picking actions, delivery handoff actions, pickup actions, or cancellation actions.
- Changed `/orders` from table-first to task-first with big shortcuts for Create Order, Pick Order, Ready / Pickup, Delivery Orders, and Customers.
- Added simple task queues on `/orders`: Need Picking, Ready for Pickup, Ready for Delivery, Stock Not Enough, and Processing Required.
- Moved dense order tables, filters, KPIs, reports, reservations, picking logs, and notifications behind an `Advanced Order List and Reports` disclosure for manager/admin-style roles.
- Converted `/orders/create` into a guided five-step flow: Customer, Order Type, Items, Total Price, Create Order.
- Kept create-order field names and server action wiring intact; total price remains order-level only, with no item-level price fields.
- Kept quick customer create in the create flow to name and phone only.
- Kept `/orders/picking` as a single-order picking workstation with selected order, weights, Scan Barcode, Manual Weight, and Mark Ready actions.
- Kept `/orders/ready` card-based with pickup, delivery, and internal-transfer groups.
- Changed `/orders/customers` to search-first layout with Add Customer disclosure and simple customer cards.
- Confirmed worker and delivery-worker views do not show the Advanced Orders table/report disclosure.
- Confirmed no Orders page renders customer outstanding amount, payment status, or finance invoice wording.
- Restored a small guarded Retail processing cross-link/copy required by the existing smoke suite; no Retail data/actions/permissions were changed.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `components/retail/retail-page.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- Authenticated route sweep for `qa.admin@example.test`, `qa.retail.worker.jc@example.test`, `qa.delivery.worker.jc@example.test`, and `qa.noorders@example.test` across `/orders`, `/orders/create`, `/orders/picking`, `/orders/ready`, and `/orders/customers` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser visual QA at 390px is still recommended because the Windows in-app browser has previously been unreliable in this workspace.
- Full create/pick/ready transactional workflow was not re-run manually in browser; this pass kept existing server actions and field names intact and verified routes/permissions.

## 2026-06-24 - Stock table sort button touch targets

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock table sort buttons so table headers use phone-size tap targets with wrapping labels and non-shrinking sort icons.
- Kept table data, sorting behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for table-header usability around 390px width.

Files changed in this pass:

- `components/stock/data-table.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- Local `/stock` route preview check - HTTP 200.
- 390px automated browser visual QA - blocked by sandbox browser startup error; manual owner QA remains required.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm Stock table sort headers are easier to tap below the mobile cards.
- Full smoke passed in this pass.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - WhatsApp CRM Task 16 live UAT setup

Task status:

- Task 16 is prepared for live UAT but not accepted yet.
- Real WhatsApp device testing was not executed in this Codex session because there is no public webhook URL, Meta app access, loaded live WhatsApp server credentials, or real tester phones available here.
- The CRM is source-ready for live UAT: webhook hardening, outgoing send failure state, duplicate-message protection, multi-number routing markers, dashboard recomputation, and mobile/desktop CRM coverage all remain in place.

Files changed in this pass:

- `.env.example`
- `docs/WHATSAPP_CRM_LIVE_UAT.md`
- `docs/qa-evidence/whatsapp-crm-live-uat/.gitkeep`
- `scripts/whatsapp-crm-live-uat-readiness.mjs`
- `HANDOFF.md`

Summary:

- Added recommended live WhatsApp Cloud API env names to `.env.example` while keeping the existing legacy aliases supported by the code.
- Added a live UAT runbook and evidence table for real text, CRM reply, image/file/audio/location handling, second WhatsApp number routing, duplicate webhook replay, failed-send state, auto-assignment, dashboard update, mobile layout, and desktop layout.
- Added a readiness script that checks source hardening and reports which live env groups are configured without printing secret values.

Live QA results:

- Real incoming WhatsApp text in CRM: not run, pending live device test.
- Real outgoing CRM reply to WhatsApp user: not run, pending live device test.
- Image/file/audio/location handling: not run, pending live media tests.
- Second WhatsApp number routing: not run, pending second-number test.
- Duplicate webhook replay: not run, pending live/staging replay evidence.
- Failed send state: source coverage passed; controlled live failure not run.
- Dashboard update after real messages: source coverage passed; live dashboard evidence not run.
- Mobile and desktop layout after real messages: source coverage passed; real device screenshots not captured.

Commands run and results:

- `node scripts\whatsapp-crm-live-uat-readiness.mjs` - passed source readiness; reported live credential setup incomplete for service-role key, WhatsApp access token, API version, webhook verify token, and optional app secret.
- `node scripts\whatsapp-crm-auth-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-mobile-inbox-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-chat-screen-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-webhook-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-send-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-profile-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-order-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-assignment-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-notification-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-broadcast-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-ai-suggestion-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-owner-dashboard-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-desktop-layout-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before reaching WhatsApp CRM coverage on unrelated Processing assertion: `Processing page must link to order preparation and clarify stock entry`.

Remaining risks / next manual step:

- Configure live deployment env vars and Meta webhook callback URL `<deployment-url>/api/whatsapp/webhook`.
- Run the scenarios in `docs/WHATSAPP_CRM_LIVE_UAT.md` with real tester phones and save masked screenshots/notes under `docs/qa-evidence/whatsapp-crm-live-uat/`.
- Task 16 should only be accepted after the live evidence table is updated with pass/fail results.

## 2026-06-24 - Retail V1 one-task UX split

Task completed:

- Continued the Retail V1 UX simplification goal only.
- Split crowded Retail task pages into focused routes so each page does one main job.
- Worker home now shows only operational shortcuts: Submit Expense, Complete Cleaning, Record Processing, and Open Cash.
- Moved manager review/history/setup work out of worker task pages:
  - Expense review/history: `/retail/expenses/review`, `/retail/expenses/history`
  - Cleaning task master/history: `/retail/cleaning/tasks`, `/retail/cleaning/history`
  - Processing review/history: `/retail/processing/review`, `/retail/processing/history`
  - Cash open/closing/close/history: `/retail/cash/open`, `/retail/cash-closing`, `/retail/cash/close`, `/retail/cash/history`
  - Settings launcher/setup/audit: `/retail/settings`, `/retail/settings/boms`, `/retail/settings/categories`, `/retail/settings/prices`, `/retail/settings/audit`
- Kept Retail actions, RLS, outlet scope, upload security, calculations, exports, and database schema unchanged.
- Made worker forms lighter: phone-size submit buttons, expense date defaults to today, cleaning completion avoids nested cards, and extra processing lines are collapsed by default while keeping multiple raw/finished lines.
- Updated Retail source coverage and status docs for the one-task route split.

Files changed in this pass:

- `app/(erp)/retail/cash/page.tsx`
- `app/(erp)/retail/cash/open/page.tsx`
- `app/(erp)/retail/cash/close/page.tsx`
- `app/(erp)/retail/cash/history/page.tsx`
- `app/(erp)/retail/cleaning/tasks/page.tsx`
- `app/(erp)/retail/cleaning/history/page.tsx`
- `app/(erp)/retail/expenses/review/page.tsx`
- `app/(erp)/retail/expenses/history/page.tsx`
- `app/(erp)/retail/processing/review/page.tsx`
- `app/(erp)/retail/processing/history/page.tsx`
- `app/(erp)/retail/sales/history/page.tsx`
- `app/(erp)/retail/settings/boms/page.tsx`
- `app/(erp)/retail/settings/categories/page.tsx`
- `app/(erp)/retail/settings/prices/page.tsx`
- `app/(erp)/retail/settings/audit/page.tsx`
- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed after rerun; the first parallel attempt hit a Windows sandbox helper startup error before script execution.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed after restoring the guarded order-preparation link as a small related link on Retail Processing.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real worker/manager phone QA should verify the simplified routes feel faster at staff rollout width and that related-action links do not confuse the main task.
- Cash history still shows daily closings and cash sessions together as a read-only history page; split further only if managers find it crowded.
- Settings still groups setup choices on one launcher page, but individual setup/audit tasks are separated.

## 2026-06-24 - Stock item card wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock mobile item-master cards so long item code, item name, category, section, brand, and barcode requirement values wrap inside the card.
- Kept item master data, item create/edit behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for item-card readability around 390px width.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated `scripts/smoke-routes.mjs`: `Processing page must link to order preparation and clarify stock entry`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long item-master values wrap cleanly in Stock mobile cards.
- Full smoke is currently blocked by the unrelated Processing smoke assertion above; this Stock-only pass did not edit Processing.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock filter button touch targets

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock movement/report filter buttons so they use phone-size tap targets and non-shrinking search icons.
- Kept filter behavior, report access, RLS, server actions, and stock data loading unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for filter button usability around 390px width.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm Stock movement/report filter buttons are easy to tap.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock unit detail field wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock unit detail/reprint fields so long item name, barcode, status, item code, brand, origin, location, inbound source, and batch values wrap inside the detail card.
- Kept stock-unit data, label generation, label reprint behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for stock-unit detail readability around 390px width.

Files changed in this pass:

- `components/stock/stock-unit-detail.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long stock-unit detail fields wrap cleanly.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock mobile card field wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock mobile balance, barcode-unit, movement-history, and stock-unit detail cards so long item, brand, origin, status, and location names wrap inside worker cards.
- Kept stock data loading, report data, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for card field wrapping around 390px width.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/stock-unit-detail.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long item, brand, origin, status, and location names wrap cleanly in Stock mobile cards.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock mobile product name wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock mobile movement cards and outbound order item checklist rows so long product names wrap instead of truncating.
- Kept outbound validation, stock movement behavior, RLS, and server actions unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for full product-name visibility around 390px width.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long product names wrap cleanly in movement cards and outbound order item checklist rows.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - WhatsApp CRM Task 15 Cloud API QA and Webhook Hardening

Task completed:

- Hardened WhatsApp webhook verification to support `WHATSAPP_VERIFY_TOKEN` and the legacy `WHATSAPP_WEBHOOK_VERIFY_TOKEN` name.
- Added optional `WHATSAPP_APP_SECRET` signature verification through `x-hub-signature-256` without logging secrets.
- Updated webhook POST parsing to read the raw body for signature checks, tolerate malformed JSON, and return safe responses.
- Added production-safe webhook logs that report setup/verification states without exposing tokens or payload secrets.
- Kept multi-entry and multi-change inbound parsing and added status webhook parsing for `sent`, `delivered`, `read`, and `failed`.
- Status webhooks now update existing outbound `crm_messages` by `external_message_id` instead of creating duplicate rows.
- Incoming unsupported WhatsApp message types now store a safe placeholder such as `Unsupported WhatsApp message type: sticker`.
- Incoming phone numbers are normalized for matching, with plus-prefixed variants checked to reduce duplicate CRM customers.
- Incoming messages still route by WhatsApp `phone_number_id` to the correct `whatsapp_accounts` record.
- Incoming media/location messages preserve raw payload metadata and still render through the existing CRM image/file/audio/location placeholders.
- Outgoing sends now support `WHATSAPP_ACCESS_TOKEN` / `WHATSAPP_GRAPH_API_VERSION` aliases while preserving existing Cloud API env names.
- Outgoing replies normalize the recipient phone number for WhatsApp API calls.
- Outgoing successful sends store the returned WhatsApp external message id and phone number id on the CRM message row.
- Outgoing failed sends store a friendly failed reason in `raw_payload`, return the error to the UI, and show `failed to send` in the chat bubble.
- Location live-send requests now fail clearly as unsupported instead of being misclassified.
- Added `scripts/whatsapp-crm-cloud-api-coverage.mjs` and wired it into `npm.cmd run smoke`.
- No secrets were added, no UI redesign was done, and no database migration was added.

Files changed in this pass:

- `app/api/whatsapp/webhook/route.ts`
- `app/api/whatsapp/send/route.ts`
- `lib/whatsapp-crm/cloud-api.ts`
- `lib/whatsapp-crm/webhook.ts`
- `lib/whatsapp-crm/send.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `scripts/whatsapp-crm-cloud-api-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None. Existing `crm_messages.external_message_id` uniqueness is used for inbound duplicate prevention; outbound/status matching uses the returned WhatsApp message id.

Manual real WhatsApp QA checklist:

1. Send real WhatsApp text to company number -> appears in CRM.
2. Reply from CRM -> customer receives message.
3. Send image from customer -> CRM handles it safely.
4. Send document/file from customer -> CRM handles it safely.
5. Send voice/audio from customer -> CRM handles it safely.
6. Send location from customer -> CRM handles it safely.
7. Send message to second WhatsApp number -> correct account routing.
8. Replay the same webhook payload twice in a test -> no duplicate CRM message.
9. Force or simulate failed outgoing send -> chat shows `failed to send` and user sees a friendly error.
10. Confirm customer auto-assignment still works for existing and new customers.
11. Confirm owner dashboard updates after real inbound/outbound messages.
12. Confirm mobile WhatsApp-style layout still works.
13. Confirm desktop 3-column layout still works.

Commands run and results:

- `node scripts\whatsapp-crm-cloud-api-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-desktop-layout-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-owner-dashboard-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Real UAT still needs live Meta webhook delivery, real WhatsApp numbers, and valid server-side env vars configured in the deployment target.
- Media download is intentionally not implemented in Phase 1; CRM stores WhatsApp media ids/raw metadata and shows safe placeholders, but does not expose temporary media URLs.
- Status updates only apply when the outgoing CRM row has the returned WhatsApp external message id.
- Staff response ranking attribution remains Phase 1 behavior by outbound sender name because CRM messages do not yet store staff user id.

## 2026-06-24 - Stock label print button wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock label print/reprint action buttons so `Print label` and `PDF fallback` can wrap helper text at phone width while keeping icons visible.
- Kept generated barcode format, label size, print/PDF behavior, stock save behavior, RLS, and stock movement behavior unchanged.
- Extended Stock label and mobile UX coverage plus Stock QA docs for label action readability around 390px width.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm label print/reprint buttons stay readable and do not create horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock scanner message wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated shared Stock scanner helper, setup-blocked, connection-lost, camera-error, and success message cards so long text breaks inside the scanner field and camera sheet at phone width.
- Kept scanner decoding, duplicate prevention, online-only blocking, server validation, RLS, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for scanner message wrapping around 390px width.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - failed on unrelated `lib/whatsapp-crm/webhook.ts(644,9)`: `statusesUpdated` is missing from a `WhatsappWebhookProcessResult` return object.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long setup-blocked, connection-lost, camera-error, and success messages stay inside scanner cards.
- The standalone typecheck gate is currently blocked by the unrelated WhatsApp CRM type error above; this Stock-only pass did not edit that module.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock transfer return stock-take button wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Transfer/Receive, Return, and Stock Take quick location/session buttons so long labels break inside the button at phone width.
- Kept transfer, receive-transfer, return, stock-take validation, scanner behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for transfer/receive, return, and stock-take quick-button wrapping around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long transfer/receive, return, and stock-take location/session labels stay inside quick buttons.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Orders page permission and finance exposure fix

Task completed:

- Debugged the admin `/orders` visual QA error: `permission denied for table files; permission denied for table customer_categories; permission denied for table finance_invoices`.
- Reproduced the failure through the local app with `qa.admin@example.test`.
- Identified the failing loader in `lib/orders/data.ts`: Orders was loading `files`, `customer_categories`, and `finance_invoices` as required dashboard data.
- Removed the Orders dashboard dependency on `finance_invoices`; Order V1 no longer reads invoice rows to compute credit warnings and does not expose payment status or customer outstanding amounts.
- Removed the Orders dashboard dependency on `files`; Delivery remains responsible for proof files, and Order V1 does not need file metadata for the dashboard.
- Added a narrow migration granting authenticated read access to `customer_categories` with RLS still enabled, because Orders needs customer category names/credit terms as lookup data.
- Fixed a migration ordering issue by moving the customer-category migration from duplicate version `202606240009` to `202606240010`.
- Confirmed `qa.noorders@example.test` still renders the module-disabled Orders view and cannot access the create form.

Files changed in this pass:

- `lib/orders/data.ts`
- `supabase/migrations/202606240010_order_customer_category_read_grant_v1.sql`
- `HANDOFF.md`

Commands run and results:

- `npx.cmd supabase db reset --local --yes` - passed after renaming the duplicate migration version.
- Admin route check for `/orders` - passed with no permission error and no finance leakage.
- Retail worker route check for `/orders` - passed with no permission error and no finance leakage.
- No-orders route check for `/orders/create` - passed; module-disabled view rendered.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Order V1 credit-overdue warnings now only display if a future safe customer-level boolean/summary field is provided; it intentionally no longer derives warnings by reading full finance invoices.
- No Order Module business flow was changed.

## 2026-06-24 - Stock outbound quick button wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound job/type, quick ready-order, and quick-remark buttons so long labels break inside the button at phone width.
- Kept outbound validation, order selection rules, scanner behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for outbound quick-button wrapping around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long outbound order/customer/remark labels stay inside quick buttons.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Barcode inbound quick button wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound recent template and quick product, brand, origin, and location buttons so long labels break inside the button at phone width.
- Kept inbound validation, barcode rules, scanner behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for inbound quick-button wrapping around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long inbound product, brand, origin, and location labels stay inside quick buttons.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock worker feedback card wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Transfer, Receive Transfer, Return, Damage/Spoilage, and Return Supplier local feedback cards so long worker messages wrap inside the card at phone width.
- Kept transfer, receive, return, damage, supplier-return validation, scanner behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for local feedback card wrapping around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm transfer, receive-transfer, return, damage/spoilage, and return-supplier local feedback text wraps cleanly.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock take blocked message wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Take blocked scan feedback so long setup/offline alert text wraps inside the red alert card.
- Kept stock-take validation, scanner behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for Stock Take blocked-message wrapping around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm Stock Take blocked setup/offline messages wrap cleanly.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock outbound blocked alert details

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound missing, blocked-status, and wrong-destination red alert cards to use assertive alerts and wrapping text.
- Updated the blocked-barcode alert list to show the barcode beside the reason so phone workers can identify which scan to remove.
- Kept outbound validation, scanner behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for blocked outbound alert details around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm missing, blocked-status, and wrong-destination outbound alerts identify long barcode scans clearly.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock outbound duplicate alert

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound duplicate batch scan feedback to `Duplicate barcode. Outbound is blocked.`
- Made the outbound scan-error card assertive and wrapping so phone workers get a clear red blocked message.
- Kept outbound validation, scanner behavior, RLS, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage and Stock QA docs for duplicate outbound scans around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm duplicate outbound scans show the red alert and do not add a second row.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - WhatsApp CRM Task 14 Desktop 3-Column Layout

Task completed:

- Refined the WhatsApp CRM desktop layout into a cleaner CRM workspace.
- Mobile remains the same single-column WhatsApp-style flow: customer list -> chat -> customer profile/order drawer.
- Tablet now uses a two-column layout with customer list and chat, with the CRM panel flowing below when space is limited.
- Desktop now uses a full three-column layout: customer list, WhatsApp-style chat, and customer profile/latest order/action panel.
- Customer list rows were kept compact on desktop while preserving the stable mobile inbox row layout.
- Chat remains the visual focus and keeps existing message bubbles, reply flow, price-list/file/location buttons, order flow, and suggest-reply entry point.
- Right CRM panel now includes profile details, tags, complaint/payment/order information, latest order state, and quick actions including Suggest reply.
- Added polished desktop empty states for no selected customer: a neutral chat prompt and CRM summary/instruction panel.
- Kept Task 13 owner dashboard metrics and local recomputation behavior intact.
- Added source coverage for responsive layout, desktop empty states, right-panel content/actions, mobile flow preservation, and local update preservation.

Files changed in this pass:

- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `scripts/whatsapp-crm-desktop-layout-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-desktop-layout-coverage.mjs` - passed.
- `node scripts\whatsapp-crm-owner-dashboard-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Manual browser/device QA is still recommended at phone width, tablet width, and desktop width to confirm the layout feels right with real data volume.
- The Phase 1 staff ranking limitation remains unchanged: response ranking is attributed by outbound message sender name because CRM messages do not yet store staff user id.
- The right-panel Suggest reply quick action focuses the chat Suggest Reply control; the actual AI suggestion still stays in the chat composer flow so staff approval remains explicit.

## 2026-06-24 - Stock damage photo message wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Damage/Spoilage selected-photo messages so long phone camera filenames or fallback photo references wrap inside the green selected-photo card.
- Kept photo-required validation, damage approval rules, RLS, server actions, scanner behavior, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage so selected-photo wrapping is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks long damage photo names around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed with two unrelated WhatsApp CRM unused-variable warnings.
- First `npm.cmd run typecheck` - failed on unrelated WhatsApp CRM `onSuggestReply` prop mismatch; final rerun after build - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long photo filenames wrap in both direct outbound damage/spoilage and Return/Damage forms.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Local QA login dashboard redirect fix

Task completed:

- Debugged the local QA login path where `qa.admin@example.test` authenticated successfully but `/dashboard` redirected back to `/login`.
- Confirmed the login action and middleware use the same Supabase SSR server-client cookie pattern.
- Confirmed `.env.local` points the app at local Supabase on `http://127.0.0.1:54321`.
- Found the actual failure in dashboard profile loading: authenticated users had RLS policies but lacked base `select` grants on profile/scope lookup tables, so `getCurrentProfile()` treated the profile load as missing and `requireCurrentProfile()` redirected to `/login`.
- Added a narrow migration granting authenticated read access to the profile/session lookup tables needed by the dashboard while leaving RLS policies in place.
- Applied the grants to the running local Supabase database and verified `qa.admin@example.test` can load `/dashboard` and `/orders` with Supabase SSR cookies.

Files changed in this pass:

- `supabase/migrations/202606240008_auth_profile_dashboard_read_grants_v1.sql`
- `HANDOFF.md`

Commands run and results:

- Local Supabase RLS/profile-loader SQL verification - passed.
- SSR cookie route check for `qa.admin@example.test` - `/dashboard` returned 200 and `/orders` returned 200.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser manual visual QA should retry the same QA admin login in the running app and confirm the dashboard renders as expected.
- No Order Module business logic was changed.

## 2026-06-24 - Stock outbound direct remark prefill

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound direct mode so the first quick remark is selected automatically when workers switch to direct outbound or choose a direct outbound type.
- Kept the remarks field editable and did not change required direct-outbound remarks, RLS, server actions, scanner behavior, outbound validation, or stock movement behavior.
- Extended Stock mobile UX coverage so direct outbound no-typing remarks are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks direct outbound remarks around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to confirm the selected quick remark is visible and editable during live direct outbound scans.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock outbound scanned barcode wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound scanned rows so long barcode text wraps fully instead of being truncated.
- Kept outbound validation, duplicate/missing/blocked barcode handling, RLS, scanner behavior, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage so outbound scanned barcode wrapping is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks long outbound scan rows around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long outbound scan rows with live order/direct outbound scans.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Barcode inbound blocked scan wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound blocked/error session rows so long barcode text wraps cleanly at phone width.
- Kept duplicate blocking, no-weight handling, validation, RLS, scanner behavior, server actions, and stock movement behavior unchanged.
- Extended Stock mobile UX coverage so the wrapped blocked/error scan row is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks duplicate/no-weight/error rows around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm long blocked/error scan rows with live duplicate, no-weight, and connection-error cases.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - WhatsApp CRM Task 13 Owner Dashboard

Task completed:

- Built a mobile-first WhatsApp CRM owner dashboard above the inbox.
- Added KPI cards for customers waiting, unread over 30 minutes, new messages today, average response time, new orders today, and complaints.
- Added staff response ranking with average response minutes, replies today, under-30-minute rate, and waiting customer count.
- Added shared dashboard metric logic in `lib/whatsapp-crm/dashboard.ts` so the data loader and client UI use the same calculations.
- Response time is calculated from an inbound customer message to the next outbound staff reply in the same conversation.
- The dashboard recomputes from current customer, message, and order state so local sends and new simple orders can update the owner view without a reload.
- Demo CRM data now includes the expanded dashboard fields and ranking examples.
- Added source coverage for dashboard types, shared calculations, data loader wiring, UI labels, demo data, and smoke wiring.
- Used the Manager Dashboard UX lens for this change; no database schema or role/scope restrictions were changed.

Files changed in this pass:

- `lib/whatsapp-crm/dashboard.ts`
- `lib/whatsapp-crm/types.ts`
- `lib/whatsapp-crm/data.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `data/mock/whatsapp-crm.ts`
- `scripts/whatsapp-crm-owner-dashboard-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-owner-dashboard-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Live Supabase data still needs manual QA with real owner/admin users to confirm response ranking matches business expectations.
- Staff ranking currently attributes replies by outbound message sender name because Phase 1 CRM messages do not store a staff user id.
- Average response time is based on same-conversation inbound-to-next-outbound cycles; future reporting can add more formal SLA windows if needed.

## 2026-06-24 - Stock worker message wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated shared Stock success, error, warning, and offline scan messages so long worker-facing text wraps on phone-width screens.
- Kept validation, RLS, server actions, scanner behavior, stock movements, routes, and permissions unchanged.
- Extended Stock mobile UX coverage so the wrapping classes are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks long action messages around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- First parallel `npm.cmd run typecheck` - failed while `npm.cmd run build` was regenerating `.next/types` files.
- `npm.cmd run build` - passed.
- Final `npm.cmd run typecheck` rerun after build - passed.

Risks / remaining checks:

- Real 390px browser/device QA is still required to visually confirm message wrapping with live Stock success/error/warning/offline states.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock worker home shortcut wrapping

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved Stock worker home shortcut buttons so icons do not shrink and labels wrap inside the button at phone width.
- Kept the same six Stock worker actions, links, role checks, RLS, server actions, and stock movement behavior.
- Attempted local 390px browser QA by starting the app through shell and the persistent browser runtime, but the Windows sandbox blocked process launch with `CreateProcessAsUserW failed: 5`.
- Extended Stock mobile UX coverage so the worker shortcut icon/label wrapping is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the six buttons at 390px with no horizontal scrolling.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Authenticated 390px browser QA could not be captured from this Windows sandbox because local server/browser process launch is blocked by `CreateProcessAsUserW failed: 5`.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - WhatsApp CRM Task 12 AI Suggest Reply

Task completed:

- Added a staff-only AI suggested reply endpoint at `/api/whatsapp/ai-suggest`.
- Added `lib/whatsapp-crm/ai-suggestions.ts` for role/scope checks, recent-message loading, optional OpenAI Responses API generation, local fallback suggestions, order-detail detection, English/Chinese/Iban translation fields, follow-up recommendation, Supabase persistence, and audit logging.
- Kept the OpenAI key server-only through `OPENAI_API_KEY`; the CRM falls back to local suggestion logic when the key is not configured.
- Added `OPENAI_CRM_AI_MODEL` as an optional model override, with GPT-5.2 as the default model.
- Extended AI suggestion types and data loading so saved suggestion metadata is parsed back into the inbox.
- Added a mobile-first `Suggest Reply` button in the chat composer for reply-capable roles.
- The suggestion panel now shows the proposed reply, source, reason, detected order details, translations, and follow-up recommendation.
- Staff must still press `Use draft` or `Edit first`, review the draft, and manually send; AI suggestions are never auto-sent.
- Account role remains view-only and cannot request AI suggestions.
- Demo CRM data now includes richer AI suggestion examples.
- Added source coverage for route auth, role checks, OpenAI/fallback generation, order detection, translation fields, follow-up recommendation, persistence, audit logging, UI controls, and smoke wiring.

Files changed in this pass:

- `app/api/whatsapp/ai-suggest/route.ts`
- `lib/whatsapp-crm/ai-suggestions.ts`
- `lib/whatsapp-crm/types.ts`
- `lib/whatsapp-crm/data.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `data/mock/whatsapp-crm.ts`
- `scripts/whatsapp-crm-ai-suggestion-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-ai-suggestion-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- Initial `npm.cmd run build` attempts were blocked by an existing Next build lock.
- `npm.cmd run build` - passed after the existing build workers cleared.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Live AI QA needs `OPENAI_API_KEY` configured server-side; otherwise the local fallback suggestion path is expected.
- Staging Supabase still needs the WhatsApp CRM Phase 1 migration applied before AI suggestions and audit rows can persist.
- Manual QA with real owner/admin/sales/customer service/account users is still needed to confirm live RLS, assigned-customer scope, and account view-only behavior.
- AI translation quality for Chinese and Iban should be reviewed by staff before sending to customers.

## 2026-06-24 - Stock scanner manual-entry helper copy

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated shared Stock barcode field helper text; current copy is `Type barcode if needed.`
- Kept camera scanning as the primary worker action and manual barcode entry as fallback.
- Kept scanner startup, validation rules, RLS, migrations, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the new helper is source-guarded and the stale typing-first wording is rejected.
- Updated Stock QA docs so owner/Vercel testing checks the manual-entry helper around 390px width.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full `npm.cmd run smoke` passed after updating the stale Stock acceptance guard to the current scanner helper copy.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - Stock scanner browser-unavailable copy

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the reusable Stock scanner browser-unavailable message to `Camera scanning is not available. Use manual entry.`
- Kept scanner startup, detection, validation rules, RLS, migrations, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the new copy is source-guarded and the old browser-specific wording is rejected.
- Updated Stock QA docs so owner/Vercel testing checks the browser-unavailable camera state and manual fallback.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Delivery migration guard: `202606240006_delivery_completion_rls_fix_v1.sql:7 policy "delivery users can update scoped deliveries" must be immediately preceded by drop policy if exists "delivery users can update scoped deliveries" on public.deliveries;`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full `npm.cmd run smoke` is currently blocked before Stock checks by the unrelated Delivery migration guard in `202606240006_delivery_completion_rls_fix_v1.sql`; this Stock-only pass did not edit Delivery.
- Real phone camera, laptop camera, Bluetooth/PDF label printing, authenticated Vercel 390px QA, and live Supabase/RLS Stock workflow evidence remain owner/manual checks.

## 2026-06-24 - WhatsApp CRM Task 11 Broadcast Price List

Task completed:

- Added owner/admin-only image price list broadcast sending at `/api/whatsapp/broadcasts`.
- Added `lib/whatsapp-crm/broadcasts.ts` to filter active CRM customers by customer type, area, tag, and assigned staff.
- Broadcast sends use WhatsApp Cloud API image media messages through the existing Cloud API helper.
- Broadcast attempts create `crm_broadcasts`, `crm_broadcast_recipients`, outbound `crm_messages` with `is_price_list = true`, and `WHATSAPP_PRICE_LIST_BROADCAST` audit logs.
- Recipient status records distinguish `SENT`, `FAILED`, and `SKIPPED`; missing Cloud API token or missing phone number id is recorded as skipped/failed at recipient level instead of silently succeeding.
- Added broadcast filter options to the WhatsApp CRM data loader from the currently visible customer set.
- Tightened app-side broadcast capability to owner/admin, matching the requested default; account, sales, customer service, and director are not granted the send route by app logic.
- Added a mobile-first broadcast form in the WhatsApp CRM inbox with image media ID, image label, caption, customer type, area, tag, and assigned staff filters.
- New broadcasts are added to the on-screen broadcast queue without requiring a page refresh.
- Added source coverage for route auth, owner/admin-only permission, filters, Cloud API image send, recipient/message/audit persistence, UI controls, and smoke wiring.

Files changed in this pass:

- `app/api/whatsapp/broadcasts/route.ts`
- `lib/whatsapp-crm/broadcasts.ts`
- `lib/whatsapp-crm/types.ts`
- `lib/whatsapp-crm/data.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `data/mock/whatsapp-crm.ts`
- `package.json`
- `scripts/whatsapp-crm-broadcast-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-broadcast-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - failed before WhatsApp CRM checks on unrelated Delivery migration guard: `202606240006_delivery_completion_rls_fix_v1.sql:7 policy "delivery users can update scoped deliveries" must be immediately preceded by drop policy if exists "delivery users can update scoped deliveries" on public.deliveries;`.

Risks / remaining checks:

- Full smoke remains blocked by the unrelated Delivery migration ordering issue above; this pass did not modify Delivery migrations.
- Staging Supabase still needs the WhatsApp CRM Phase 1 migration applied before broadcast and recipient rows can persist.
- Live broadcast QA needs a valid `WHATSAPP_CLOUD_API_TOKEN`, a Meta image media ID, connected WhatsApp account phone-number ids, and real owner/admin users.
- Confirm with the business whether director should remain blocked from broadcast sending; this task implements the requested owner/admin-only default.

## 2026-06-24 - Stock scanner camera error copy

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated reusable Stock camera scanner error messages so they point workers to `Use manual entry`.
- Removed stale `Type barcode manually` wording from camera startup errors.
- Kept scanner startup, validation rules, RLS, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the corrected error copy is source-guarded and the stale wording is rejected.
- Updated Stock QA docs so owner/Vercel testing checks camera error copy and manual fallback.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Delivery migration guard: `202606240006_delivery_completion_rls_fix_v1.sql:7 policy "delivery users can update scoped deliveries" must be immediately preceded by drop policy if exists "delivery users can update scoped deliveries" on public.deliveries;`. Not fixed in this Stock-only pass.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full smoke is currently blocked by an unrelated Delivery migration policy-drop guard in the dirty worktree.
- Real phone QA is required to confirm camera errors point to `Use manual entry` and the fallback works around 390px width.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock scanner permission helper copy

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the reusable Stock camera scanner permission helper so it says `If permission is blocked, tap Use manual entry.`
- Removed stale guidance that told workers to close the scanner before using manual fallback.
- Kept scanner startup, validation rules, RLS, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the corrected helper copy is source-guarded and the stale close-window wording is rejected.
- Updated Stock QA docs so owner/Vercel testing checks the permission helper copy.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on unrelated Delivery migration guard: `202606240006_delivery_completion_rls_fix_v1.sql:7 policy "delivery users can update scoped deliveries" must be immediately preceded by drop policy if exists "delivery users can update scoped deliveries" on public.deliveries;`. Not fixed in this Stock-only pass.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full smoke is currently blocked by an unrelated Delivery migration policy-drop guard in the dirty worktree.
- Real phone QA is required to confirm the helper copy appears and the fallback works around 390px width.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - WhatsApp CRM Task 10 Notifications

Task completed:

- Added shared WhatsApp CRM notification helpers in `lib/whatsapp-crm/notifications.ts`.
- Incoming webhook messages now create `NEW_MESSAGE` notification rows.
- Complaint-detected inbound messages now create `COMPLAINT` notification rows.
- Simple order creation now creates a `NEW_ORDER` notification row.
- The CRM data loader now reads unread `crm_notifications` rows and merges derived alerts for `UNREAD_30_MIN` and `PAYMENT_REMINDER`.
- Added CRM notification types and alert data to the shared WhatsApp CRM TypeScript model.
- Added a mobile-first Alerts strip above the inbox so staff can tap an alert and jump to the affected customer chat.
- New simple orders created in the UI also add an immediate local alert without requiring a page refresh.
- Demo CRM data now includes sample alerts for new message, unread over 30 minutes, new order, complaint, and payment reminder.
- Added source coverage for all five requested alert types, persisted notification writers, derived alerts, data loading, UI display, and smoke wiring.

Files changed in this pass:

- `lib/whatsapp-crm/notifications.ts`
- `lib/whatsapp-crm/types.ts`
- `lib/whatsapp-crm/webhook.ts`
- `lib/whatsapp-crm/orders.ts`
- `lib/whatsapp-crm/data.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `data/mock/whatsapp-crm.ts`
- `package.json`
- `scripts/whatsapp-crm-notification-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-notification-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- First `npm.cmd run build` was blocked by an existing Next build lock.
- `npm.cmd run build` - passed on retry.
- `npm.cmd run smoke` - failed before WhatsApp CRM checks on unrelated Delivery migration guard: `202606240006_delivery_completion_rls_fix_v1.sql:7 policy "delivery users can update scoped deliveries" must be immediately preceded by drop policy if exists "delivery users can update scoped deliveries" on public.deliveries;`.

Risks / remaining checks:

- Full smoke remains blocked by the unrelated Delivery migration ordering issue above; this pass did not modify Delivery migrations.
- Staging Supabase still needs the WhatsApp CRM Phase 1 migration applied before CRM notification rows can persist.
- Payment reminder and unread-over-30 alerts are derived at page load for Phase 1; a later scheduler can persist/escalate them if the business wants timed push behavior.
- Manual QA with real owner/admin/sales/customer service/account users is still needed to confirm alert visibility under RLS.

## 2026-06-24 - Stock scanner starting status

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so `Starting camera...` is announced as a polite status while the camera stream opens.
- Kept camera startup logic, scanner detection, validation rules, RLS, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the startup status is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the startup overlay before the video feed is ready.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm startup feedback appears and feels right around 390px width.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - WhatsApp CRM Task 9 Auto Assignment

Task completed:

- Added shared WhatsApp CRM assignment logic in `lib/whatsapp-crm/assignments.ts`.
- Incoming messages now keep the customer's current assigned staff when one exists.
- If a customer has no current assigned staff, inbound processing restores the latest active `crm_assignments` staff as the previous handler.
- New customers use `WHATSAPP_CRM_FALLBACK_STAFF_ID` as a fallback assignment rule when configured.
- New customers without a fallback staff stay in the admin queue, receive an `ADMIN_QUEUE` CRM notification, and write a `WHATSAPP_CUSTOMER_ADMIN_QUEUE` audit log.
- Staff replies now establish the previous handler for unassigned customers by setting `assigned_staff_id`, creating a `crm_assignments` row with `staff_reply`, and updating the conversation assignment.
- Auto assignments write `WHATSAPP_CUSTOMER_AUTO_ASSIGNMENT` audit logs.
- The inbox now labels unassigned CRM customers as `Admin queue` instead of `Unassigned`.
- Added source coverage for previous-handler restore, fallback assignment, admin queue notification/audit, staff-reply assignment, and smoke wiring.

Files changed in this pass:

- `lib/whatsapp-crm/assignments.ts`
- `lib/whatsapp-crm/webhook.ts`
- `lib/whatsapp-crm/send.ts`
- `lib/whatsapp-crm/data.ts`
- `package.json`
- `scripts/whatsapp-crm-assignment-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-assignment-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Staging Supabase still needs the WhatsApp CRM Phase 1 migration applied before `crm_assignments`, CRM notifications, and audit logs can persist.
- If the business wants automatic fallback to a specific staff member, configure `WHATSAPP_CRM_FALLBACK_STAFF_ID` with that staff profile id; otherwise new customers intentionally remain in the admin queue.
- Manual QA with real owner/admin/sales/customer service/account users is still needed to confirm assignment visibility and RLS behavior.

## 2026-06-24 - Stock scanner error manual fallback

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so camera error cards show both `Try camera again` and `Use manual entry`.
- `Use manual entry` reuses the existing fallback path: it closes the camera sheet and focuses the manual barcode input.
- Kept server-side duplicate barcode prevention, page-level duplicate warnings, validation rules, RLS, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the inline error fallback path is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks manual fallback directly from the camera error card.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm the inline error fallback closes the scanner and focuses the manual barcode input around 390px width.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - WhatsApp CRM Task 8 Simple Order Creation

Task completed:

- Added Phase 1 simple order creation from the WhatsApp CRM chat profile area.
- Staff can enter product, weight/quantity, price, delivery or pickup, address, date, location, and remarks.
- Added status updating from the same latest-order panel inside the mobile drawer and desktop right profile panel.
- Pickup orders are limited to `New Order -> Confirmed -> Preparing -> Ready for Pickup -> Completed`, with `Failed` and `Cancelled` allowed as terminal outcomes.
- Delivery orders are limited to `New Order -> Confirmed -> Preparing -> Out for Delivery -> Completed`, with `Failed` and `Cancelled` allowed as terminal outcomes.
- Account role remains view-only; owner/admin/director can manage all CRM orders, and sales/customer service can manage only assigned customers.
- Added `POST` and `PATCH` support at `/api/whatsapp/orders`, with server-side role/scope validation in `lib/whatsapp-crm/orders.ts`.
- Creating or updating a CRM order updates the customer `last_order_date` and `latest_order_status`, then writes audit logs.
- The inbox keeps local order state so new orders and status changes immediately update the customer row badge without a reload.
- Added source coverage for the route, order service permissions, pickup/delivery status flow, audit logging, customer latest-order sync, and chat UI order controls; wired it into `npm.cmd run smoke`.
- Kept ERP reservation/sync, quotation, invoice, payment gateway, and full order module integration out of Phase 1.

Files changed in this pass:

- `app/api/whatsapp/orders/route.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `lib/whatsapp-crm/orders.ts`
- `lib/whatsapp-crm/types.ts`
- `package.json`
- `scripts/whatsapp-crm-order-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-order-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Staging Supabase still needs the WhatsApp CRM Phase 1 migration applied before real CRM order rows can persist.
- Manual QA with real owner/admin/sales/customer service/account users is still needed to confirm live RLS, role scope, and WhatsApp CRM assignment behavior.
- Later ERP/order module integration remains intentionally pending; this task only records simple CRM orders and latest status.

## 2026-06-24 - Stock scanner camera retry

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so a worker sees `Try camera again` inside the scanner sheet when camera startup fails.
- The retry action clears scanner-only error/last-scan state and restarts the camera session without closing the sheet.
- Kept server-side duplicate barcode prevention, page-level duplicate warnings, validation rules, RLS, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the camera retry path is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks retry after camera permission or startup failure.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm camera retry works after permission/camera startup issues around 390px width.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock scanner reopen reset

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so opening `Scan Barcode` clears stale camera error text and resets the camera-only recent-detection debounce.
- This prevents a quick close/reopen from ignoring the first camera read of the same barcode because of old scanner UI state.
- Kept server-side duplicate barcode prevention, page-level duplicate warnings, validation rules, RLS, and stock movement behavior unchanged.
- Extended Stock scanner/mobile UX coverage so the reopen reset behavior is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks close/reopen scanner readiness.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed with two unrelated WhatsApp CRM unused-variable warnings.
- `npm.cmd run typecheck` - failed on unrelated `components/whatsapp-crm/whatsapp-crm-inbox.tsx` `onOrderSaved` prop typing in the dirty worktree. Not fixed in this Stock-only pass.
- `npm.cmd run build` - failed on the same unrelated WhatsApp CRM `onOrderSaved` type error. Not fixed in this Stock-only pass.

Risks / remaining checks:

- Full handoff gates are not all green because unrelated WhatsApp CRM dirty-worktree type errors currently block standalone typecheck/build.
- Real phone QA is required to confirm the scanner is immediately ready after close/reopen around 390px width.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Inbound generated label attach cue

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved Barcode Inbound generated-label feedback by showing `Print and attach saved labels before moving stock.` beside the print actions when saved labels are available.
- Kept the change to worker guidance only; label generation, auto-save, print/PDF fallback, undo, and stock movement behavior are unchanged.
- Extended Stock label/mobile UX coverage so the attach cue is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the cue after generating an internal label.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode generation rules, label print behavior, undo behavior, or stock movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full handoff gates passed in this pass.
- Real phone QA is required to confirm the cue is visible after generating a label around 390px width.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - WhatsApp CRM Task 7 Customer Profile

Task completed:

- Added in-chat customer profile editing from both the mobile profile drawer and desktop profile panel.
- Staff can edit name, phone, company name, customer type, birthday, area, address, tags, and remarks.
- The profile view now has Edit, Save, and Cancel states with mobile-friendly form controls.
- Account role remains view-only and sees a clear blocked edit message.
- Added a server-checked `PATCH /api/whatsapp/customer-profile` route.
- Customer profile updates are validated and handled in `lib/whatsapp-crm/customers.ts`.
- Owner/admin/director can edit all CRM customer profiles; sales/customer service can edit only assigned customers.
- Profile updates write `updated_by` and create `WHATSAPP_CUSTOMER_PROFILE_UPDATE` audit logs.
- The chat UI updates the customer list/profile locally after a successful save without a full page reload.
- Added source coverage for the route authentication, role/scope checks, editable fields, audit logging, account blocked state, and UI save flow; wired it into `npm.cmd run smoke`.

Files changed in this pass:

- `app/api/whatsapp/customer-profile/route.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `lib/whatsapp-crm/customers.ts`
- `lib/whatsapp-crm/types.ts`
- `package.json`
- `scripts/whatsapp-crm-profile-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-profile-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- First `npm.cmd run lint` found a React effect set-state issue; replaced the effect with keyed profile remounts.
- `npm.cmd run lint` - passed after the fix.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- Local profile endpoint check `PATCH http://127.0.0.1:3000/api/whatsapp/customer-profile` - returned HTTP 401 `{"error":"Sign in required."}`, confirming the route requires a signed-in staff session.

Risks / remaining checks:

- Staging Supabase still needs the WhatsApp CRM migration applied before real CRM profile rows can persist.
- Real role QA should confirm owner/admin/director can edit globally, sales/customer service can edit only assigned customers, and account remains view-only.
- Manual mobile QA around 390px width should confirm the edit form scrolls comfortably in the drawer and save/cancel buttons stay easy to tap.
- Assignment changes are intentionally not part of this task; assigned staff remains read-only in the profile panel.

Next recommended task:

- Add assigned-staff reassignment controls for owner/admin and preserve the existing previous-handler auto-assignment rule for normal inbound chats.

## 2026-06-24 - Stock scanner continuous success cue

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so continuous scanning shows `Detected. Ready for next scan.` after a successful camera read.
- Kept the wording honest: the camera sheet confirms detection, while page-level success/error messages still show whether the workflow save passed or was blocked.
- Extended Stock scanner/mobile UX coverage so the continuous success cue is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the cue at 390px width during continuous scanning.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode decode rules, duplicate rules, or stock movement behavior.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - failed on unrelated `components/whatsapp-crm/whatsapp-crm-inbox.tsx` React hook lint in the dirty worktree. Not fixed in this Stock-only pass.
- `npm.cmd run typecheck` - failed on unrelated `components/whatsapp-crm/whatsapp-crm-inbox.tsx` missing `canEditProfile` / `onCustomerUpdated` props in the dirty worktree. Not fixed in this Stock-only pass.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full handoff gates are not all green because unrelated WhatsApp CRM dirty-worktree errors currently block standalone lint/typecheck.
- Real phone QA is required to confirm the cue is visible and readable around 390px width while scanning real labels.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - WhatsApp CRM Task 6 Sending Messages

Task completed:

- Allowed staff to send WhatsApp text replies from the CRM chat composer.
- Replaced the old `/api/whatsapp/send` trust-on-client payload with a server-checked send flow.
- The browser now sends only customer, conversation, message type, and body intent.
- The server now loads the signed-in staff profile, checks reply role access, checks assigned-customer scope for staff roles, loads the WhatsApp account/customer from Supabase, sends through WhatsApp Cloud API, and stores the approved outbound CRM message.
- Account role remains blocked from sending replies.
- Owner/admin/director can reply across all customers; sales and customer service can reply only to assigned customers.
- Outbound messages are inserted with `approved_by_staff = true`, delivery status, audit log, and customer/conversation updates on successful send.
- Failed WhatsApp delivery attempts are still stored as failed outbound chat bubbles so staff can see what happened.
- Chat UI now shows sending feedback and appends returned outbound messages to the active conversation after the API returns.
- Added source coverage for route authentication, permission checks, approved outbound persistence, no browser-supplied phone/staff trust, and UI send behavior; wired it into `npm.cmd run smoke`.

Files changed in this pass:

- `app/api/whatsapp/send/route.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `lib/whatsapp-crm/send.ts`
- `package.json`
- `scripts/whatsapp-crm-send-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-send-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed after removing one unused import warning.
- First two `npm.cmd run build` attempts were blocked by an existing Next build lock; retry after the other process cleared passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- Local send endpoint check `POST http://127.0.0.1:3000/api/whatsapp/send` - returned HTTP 401 `{"error":"Sign in required."}`, confirming the route requires a signed-in staff session.

Risks / remaining checks:

- Live WhatsApp sending still needs staging credentials: `WHATSAPP_CLOUD_API_TOKEN`, `WHATSAPP_CLOUD_API_VERSION` if not default, `NEXT_PUBLIC_SUPABASE_URL`, and `SUPABASE_SERVICE_ROLE_KEY`.
- Staging Supabase still needs the WhatsApp CRM migration applied before real CRM send rows can persist.
- Media sending is prepared server-side for image/file/audio but the Phase 1 composer currently sends text replies only; attachment upload/send UI remains a later task.
- Real role QA should confirm owner/admin/director can reply globally, sales/customer service can reply only to assigned customers, and account receives a blocked state.

Next recommended task:

- Run staging send QA with one owner/admin, one assigned sales/customer service user, one unassigned staff user, and one account user, then verify the outbound WhatsApp message appears in Meta and in `/whatsapp-crm`.

## 2026-06-24 - Retail V1 final browser/mobile QA attempt

Task completed:

- Attempted the final authenticated browser/mobile click-through QA required for Retail V1 UAT sign-off.
- Confirmed the remaining blocker is evidence collection, not the previously fixed Retail API/storage/export implementation.
- Documented the browser/mobile QA attempt in `docs/RETAIL_V1_E2E_TEST_RESULTS.md`.

Result:

- Retail V1 remains not ready for UAT because full authenticated browser/mobile click-through evidence is still missing.
- Codex in-app browser automation could not start in this Windows sandbox: `CreateProcessAsUserW failed: 5`.
- Chrome automation was not available through a separate usable control path.
- Local fallback automation packages are not installed: `playwright`, `@playwright/test`, and `puppeteer` are missing.
- No real phone browser is available from this execution environment, so the mobile file upload interaction could not be verified.

Files changed in this pass:

- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- Browser automation availability check - blocked; in-app browser runtime failed with `CreateProcessAsUserW failed: 5`.
- Local browser package check - `playwright`, `@playwright/test`, and `puppeteer` are missing.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Run a real authenticated browser or phone QA pass for worker Outlet A, manager Outlet A, worker Outlet B, manager Outlet B, admin, and director.
- Capture evidence for mobile expense receipt upload, selected filename display, uploading state, outlet isolation, report export visibility, navigation, and logout.
- After that pass, update the Retail E2E results document with screenshots or written operator evidence and change the UAT recommendation only if all checks pass.

## 2026-06-24 - WhatsApp CRM Task 5 Cloud API Webhook

Task completed:

- Connected incoming WhatsApp Cloud API message payloads to canonical Supabase CRM tables.
- Kept the webhook route thin and moved message normalization/persistence into `lib/whatsapp-crm/webhook.ts`.
- Webhook processing now stores raw inbound rows in `crm_webhook_inbox`.
- Webhook processing now upserts or creates `whatsapp_accounts`, `crm_customers`, `crm_conversations`, and `crm_messages` so new messages can display in the CRM inbox.
- Supported inbound text, image, document/file, audio, and location messages.
- Added duplicate protection using WhatsApp `external_message_id` so webhook retries do not duplicate chat bubbles or unread counts.
- Preserved previous customer staff assignment by carrying `assigned_staff_id` into the active conversation.
- Added complaint keyword detection for inbound messages and updates `has_open_complaint` for the red complaint tag.
- Updated inbox loading so unread messages naturally become 30-minute reminders from `last_message_at`.
- Added audit logging for normalized inbound CRM messages.
- Added source coverage for webhook normalization, canonical table writes, duplicate protection, complaint detection, assignment preservation, and inbox aging; wired it into `npm.cmd run smoke`.

Files changed in this pass:

- `app/api/whatsapp/webhook/route.ts`
- `lib/whatsapp-crm/webhook.ts`
- `lib/whatsapp-crm/data.ts`
- `package.json`
- `scripts/whatsapp-crm-webhook-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-webhook-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- Local webhook route check `POST http://127.0.0.1:3000/api/whatsapp/webhook` - returned HTTP 200 with `{"received":true,"stored":false}` because local service-role Supabase storage was not active for the lightweight endpoint check.

Risks / remaining checks:

- Live WhatsApp Cloud API webhook QA still needs a configured public webhook URL, `WHATSAPP_WEBHOOK_VERIFY_TOKEN`, `NEXT_PUBLIC_SUPABASE_URL`, and `SUPABASE_SERVICE_ROLE_KEY`.
- Staging Supabase still needs the WhatsApp CRM migration applied before canonical CRM rows can be stored.
- Real media download/storage is not implemented in Phase 1; image/file/audio rows store the WhatsApp media ID or filename as the chat label.
- Staff should manually confirm that owner/admin/sales/customer service/account RLS views show the newly created webhook rows as intended after staging setup.

Next recommended task:

- Configure staging WhatsApp webhook credentials, send live test messages for each supported type, and verify they appear in `/whatsapp-crm` under the correct WhatsApp number and customer.

## 2026-06-24 - Retail file upload UI and report export

Task completed:

- Replaced raw Retail file input usage with a shared mobile-friendly `FileUploadField`.
- Retail file upload fields now show the selected filename.
- Retail forms now show an `Uploading...` pending state when submitted with selected files.
- Kept required receipt upload for Retail Expenses and optional uploads for Daily Sales AutoCount attachment, Cleaning photo, and Processing wastage photo.
- Added authorized Retail file viewing through `/api/retail/files`; the route checks `retail_files` through RLS and creates a short-lived signed URL only after access is allowed.
- Added authorized file links in Retail tables for Daily Sales attachments, Expense receipts, Cleaning photos, and Processing wastage photos.
- Kept Retail Reports CSV export controls for report viewers; worker users remain blocked from full reports/export by the existing report permission gate.
- Added smoke coverage for upload field UX, signed file viewing, and export permission markers.
- Reran authenticated staging QA; expanded Retail storage checks still pass.

Files changed in this pass:

- `app/api/retail/files/route.ts`
- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `components/stock/data-table.tsx`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\retail-v1-staging-real-qa.mjs` - passed against linked staging Supabase.
- `npm.cmd run lint` - passed after rerun.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Full authenticated browser/mobile click-through is still required before Retail V1 UAT sign-off.
- The local browser connector could not previously start in this Windows sandbox, so real phone or external browser evidence is still needed for the mobile file-upload interaction.

## 2026-06-24 - WhatsApp CRM Task 4 Chat Screen

Task completed:

- Built typed WhatsApp-style chat message rendering for text, image, file, audio, location, and price list image messages.
- Added visual chat cards for image previews, file attachments, audio voice-note style messages, and shared locations.
- Added an explicit `isPriceList` message flag so price list images do not rely on filename guessing.
- Added file and audio demo messages and marked the demo price list image as a price list.
- Added a mobile customer profile drawer from the chat header while preserving the desktop 3-column customer list, chat, and profile layout.
- Reused the same profile/order/action content in the desktop side panel and the mobile drawer.
- Kept account role reply/broadcast restrictions unchanged.
- Added source coverage for the chat message types, price list flag, drawer, and demo fixtures; wired it into `npm.cmd run smoke`.

Files changed in this pass:

- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `data/mock/whatsapp-crm.ts`
- `lib/whatsapp-crm/data.ts`
- `lib/whatsapp-crm/types.ts`
- `package.json`
- `scripts/whatsapp-crm-chat-screen-coverage.mjs`
- `supabase/migrations/202606240003_whatsapp_crm_phase1.sql`
- `HANDOFF.md`

Migration SQL added:

- None. Updated existing un-applied WhatsApp CRM migration `202606240003_whatsapp_crm_phase1.sql` with `is_price_list` for CRM messages and webhook inbox rows.

Commands run and results:

- `node scripts\whatsapp-crm-chat-screen-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- Local route check `http://127.0.0.1:3000/whatsapp-crm` - returned HTTP 200.

Risks / remaining checks:

- Real mobile/browser QA around 390px width is still needed to confirm bubble sizing, drawer scroll, and touch targets with live data and real media filenames.
- The updated CRM migration still needs to be applied to staging before live Supabase rows can store `is_price_list`.
- Message attachment buttons are still Phase 1 UI affordances; actual upload/send mutation flows should be wired after live WhatsApp/Supabase staging setup.

Next recommended task:

- Wire staff-approved chat sending and attachment sending to the WhatsApp Cloud API route with role checks and audit logging.

## 2026-06-24 - WhatsApp CRM Task 3 Mobile Inbox UI

Task completed:

- Improved the WhatsApp CRM mobile-first customer inbox list.
- Customer rows now use a stable WhatsApp-style mobile layout with avatar initials, customer name, phone/WhatsApp number, last message, timestamp, unread badge, assigned staff, last order date, customer type, and order status badge.
- Added unread total badge in the customer list header.
- Kept mobile flow starting at customer list before chat/profile.
- Extended customer search to include assigned staff, order status, and last order date.
- Added source coverage for the required mobile inbox fields and wired it into `npm.cmd run smoke`.
- Did not change database schema, RLS, authentication, message sending, chat reply behavior, order mutations, or broadcast permissions.

Files changed in this pass:

- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `scripts/whatsapp-crm-mobile-inbox-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\whatsapp-crm-mobile-inbox-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Real browser/mobile QA around 390px width is still needed to confirm row density, touch targets, and text truncation feel right with live customer names.
- Live Supabase data should be checked after CRM migration/seed so assigned staff, last order date, unread count, and order status render correctly from real rows.

Next recommended task:

- Wire the customer list to seeded staging CRM records and capture mobile screenshots for Owner/Admin/Sales/Customer Service/Account role views.

## 2026-06-24 - Retail storage isolation hardening

Task completed:

- Tightened Retail storage isolation beyond the first outlet-path fix.
- Added dedicated `retail_files` metadata with outlet, record type, record ID, bucket, storage path, original filename, MIME type, uploader, and upload timestamp.
- Updated Retail upload paths to `retail/<outlet-id>/<record-type>/<record-id>/<filename>`.
- Updated Retail upload actions so Daily Sales, Expenses, Cleaning, and Processing create Retail file metadata when files are uploaded.
- Hardened storage helper policies so Retail reads require a valid record-scoped path and matching metadata.
- Blocked old non-record Retail paths such as `retail/<outlet-id>/qa/<filename>`.
- Expanded staging QA to verify same-outlet worker/manager file reads, cross-outlet worker/manager read blocks, admin/director reads, cross-outlet upload block, and old-path upload block.
- Applied `202606240004_retail_file_metadata_rls_v1.sql` to linked staging Supabase after inspecting migration status. No reset was performed. Unrelated pending `202606240003` was not applied.

Files changed in this pass:

- `lib/retail/actions.ts`
- `scripts/retail-v1-staging-real-qa.mjs`
- `supabase/migrations/202606240004_retail_file_metadata_rls_v1.sql`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\retail-v1-staging-real-qa.mjs` - passed against linked staging Supabase with expanded storage checks.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Retail storage isolation is fixed in staging.
- Full authenticated browser/mobile UAT evidence remains a separate Retail readiness gap documented in `docs/RETAIL_V1_E2E_TEST_RESULTS.md`.

## 2026-06-24 - WhatsApp CRM Task 2 Authentication and Role Access

Task completed:

- Built on the existing Supabase email/password login flow for WhatsApp CRM role access.
- Updated login copy so staff understand access is based on assigned role.
- Added `sales` as the canonical Sales role while keeping `sales_staff` as a backwards-compatible alias from Task 1.
- Added Owner global module access in app-side module gating.
- Removed duplicate `whatsapp_crm` from the app module-key list.
- Updated WhatsApp CRM navigation and home shortcut access for Owner, Admin, Sales, Customer Service, and Account.
- Updated WhatsApp CRM role-mode mapping:
  - Owner/Admin keep full CRM access.
  - Sales/Customer Service use assigned-customer staff access.
  - Account uses view-focused access and cannot reply or broadcast by default.
- Updated CRM migration role helpers and RLS to include `sales`.
- Tightened account access so Account does not receive chat-message or broadcast-read policy by default; Account remains able to view customer/order/payment-reminder data.
- Added `scripts/whatsapp-crm-auth-coverage.mjs` and wired it into `npm.cmd run smoke` to guard login/role access expectations.

Files changed in this pass:

- `app/(auth)/login/page.tsx`
- `components/auth/login-form.tsx`
- `components/dashboard/home-page.tsx`
- `components/erp/app-shell.tsx`
- `lib/auth/access.ts`
- `lib/auth/types.ts`
- `lib/whatsapp-crm/data.ts`
- `package.json`
- `scripts/whatsapp-crm-auth-coverage.mjs`
- `supabase/migrations/202606240003_whatsapp_crm_phase1.sql`
- `HANDOFF.md`

Migration SQL added:

- None. Updated existing un-applied WhatsApp CRM migration `202606240003_whatsapp_crm_phase1.sql`.

Commands run and results:

- `node scripts\whatsapp-crm-auth-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- First `npm.cmd run build` - blocked by an existing Next build lock.
- `npm.cmd run smoke` - passed, including WhatsApp CRM auth coverage.
- Rerun `npm.cmd run build` - passed.

Risks / remaining checks:

- The updated CRM migration still needs to be applied to staging Supabase.
- Real Supabase Auth users for Owner, Admin, Sales, Customer Service, and Account still need staging QA to confirm RLS behavior with live sessions.
- Account role should be manually verified to confirm it can view customer/order/payment reminder data but cannot reply or send broadcasts.

Next recommended task:

- Seed staging Auth users for the five CRM roles and run role-by-role Supabase/browser QA on `/login`, `/dashboard`, and `/whatsapp-crm`.

## 2026-06-24 - WhatsApp CRM Phase 1

Task completed:

- Built a new mobile-first WhatsApp CRM module at `/whatsapp-crm`.
- Added WhatsApp-style mobile flow: customer list -> chat -> customer profile/order actions.
- Added desktop 3-column layout: customer list, chat, profile/latest order/follow-up panel.
- Supported multiple company WhatsApp numbers in the UI/data model.
- Added customer list fields for name, phone, unread count, assigned staff, last order date, last message, customer type, order status, complaint badge, and contacted WhatsApp number.
- Added profile fields for address, customer type, area, tags, remarks, birthday, company name, assigned staff, last order, latest order status, and payment reminder status.
- Added simple chat order display/actions for product, weight/quantity, price, fulfillment, address, date, location, remarks, and status.
- Added AI suggested reply UI with staff approval before draft sending.
- Added UI affordances for text, image, file/audio/location messages and image price-list sending.
- Added owner/admin-only broadcast queue and disabled broadcast action for non-broadcast roles by default.
- Added owner dashboard response-speed KPIs inside the CRM page.
- Added server-only WhatsApp Cloud API send helper and `/api/whatsapp/send` route.
- Added WhatsApp webhook verification/receive endpoint at `/api/whatsapp/webhook`.
- Added server-only Supabase admin client helper for webhook storage; service-role key is not imported by frontend components.
- Added Phase 1 migration for CRM roles, module access, WhatsApp accounts, CRM customers/conversations/messages, assignments, products, product prices, simple CRM orders/items, broadcasts/recipients, notifications, follow-ups, AI suggestions, audit logs, and webhook inbox.
- Renamed the CRM migration to version `202606240003` to avoid colliding with existing Retail migrations in the dirty worktree.
- Tightened CRM message RLS so authenticated staff can insert only approved outbound messages; inbound raw webhook writes stay on the service-role webhook path.
- Tightened CRM order item RLS so staff access follows owner/admin scope, order creator scope, or assigned-customer scope.
- Prepared ERP integration by linking CRM customers/orders to existing ERP customer/order records through nullable reference columns.

Files changed in this pass:

- `.env.example`
- `app/(erp)/whatsapp-crm/page.tsx`
- `app/api/whatsapp/send/route.ts`
- `app/api/whatsapp/webhook/route.ts`
- `components/dashboard/home-page.tsx`
- `components/erp/app-shell.tsx`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `components/whatsapp-crm/whatsapp-crm-page.tsx`
- `data/mock/whatsapp-crm.ts`
- `lib/auth/access.ts`
- `lib/auth/types.ts`
- `lib/supabase/admin.ts`
- `lib/whatsapp-crm/cloud-api.ts`
- `lib/whatsapp-crm/data.ts`
- `lib/whatsapp-crm/types.ts`
- `supabase/migrations/202606240003_whatsapp_crm_phase1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606240003_whatsapp_crm_phase1.sql`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- Local dev route check: `http://127.0.0.1:3000/whatsapp-crm` returned HTTP 200.

Risks / remaining checks:

- The migration has not been applied to a live Supabase project in this pass.
- WhatsApp Cloud API credentials and webhook verify token still need to be configured in the deployment environment.
- Webhook receive currently stores raw inbound payload rows in `crm_webhook_inbox`; a later worker/RPC should reconcile them into canonical customers, conversations, and messages with auto-assignment rules.
- UI buttons for send/order/status/broadcast are Phase 1 surfaces; full mutation flows should be wired after live Supabase CRM data is seeded.
- Browser visual automation could not run in this Windows sandbox because the browser runtime failed with `CreateProcessAsUserW failed: 5`; manual browser/mobile QA is still required.
- Real mobile QA should verify 390px customer list -> chat -> profile flow, touch targets, no horizontal scrolling, and desktop 3-column usability.

Next recommended task:

- Apply the CRM migration to staging, seed one WhatsApp account and three CRM customers, then wire staff reply/order/status actions to server actions and the WhatsApp send route with role checks.

## Current State

This handoff reflects the codebase inspection for the existing frozen pork / meat processing ERP. No business behavior was changed in this documentation pass.

The repository already contains a large dirty worktree from previous ERP work. Treat existing modified and untracked app files as in-progress work unless the owner explicitly asks to revert them.

## 2026-06-24 - Order Module V1 manual visual QA prep

Task completed:

- Started the local app for manual visual QA at `http://127.0.0.1:3000`.
- Confirmed the local app responds at `/login` with HTTP 200.
- Started the app with local Supabase environment overrides:
  - `NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321`
  - local anon key present but not printed.
- Added `docs/ORDER_VISUAL_QA_CHECKLIST.md` with page-by-page desktop/mobile visual QA steps, QA accounts, expected UI, actions, and result fields.
- No business logic changes were made for this visual QA prep pass.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - blocked once by the active dev server lock, then passed after stopping the QA dev server.
- `npm.cmd run smoke` - passed.

Remaining checks:

- Manual visual QA still needs tester screenshots/findings for desktop and mobile responsive layouts.
- If issues are found, fix only visual/layout/action-visibility defects in the existing Order/Delivery flows.

## 2026-06-24 - Order Module V1 Acceptance QA unblock

Task completed:

- Validated local Supabase fresh reset from scratch with Docker Desktop running.
- Repaired migration ordering issues for delivery permission hardening and retail enum default/update usage.
- Repaired local QA Auth seeding so documented QA users can log in with the documented local/dev password.
- Fixed Order Module base-table grants so authenticated users reach RLS policies instead of raw permission errors.
- Added database guards for manual-weight reason requirement and order/order-line edit lock after picking starts.
- Added delivery handoff permission and status-sync fixes so ready delivery jobs are visible to drivers, can be accepted, and sync `OUT_FOR_DELIVERY` back to the linked order.
- Moved the Supabase admin client helper to `server/` so shared `lib/` no longer references service-role credentials.
- Fixed unrelated lint/type blockers in WhatsApp CRM encountered by the required full-project checks.

QA results:

- `npx.cmd supabase db reset --local --yes` - passed after fixes; migrations and seed apply without manual patching.
- QA Auth login - passed for `qa.retail.worker.jc@example.test`, `qa.retail.manager.jc@example.test`, `qa.delivery.worker.jc@example.test`, `qa.admin@example.test`, `qa.director@example.test`, and `qa.noorders@example.test`.
- RLS - Outlet 10 worker sees only Outlet 10 orders; Outlet 11 worker sees none of Outlet 10 test orders; `qa.noorders@example.test` sees zero order rows and cannot create orders; admin/director see broader scoped rows.
- Workflow SQL QA - pickup, delivery, internal transfer creation; quick customer create/search; immediate confirmed `order_v1_status`; active reservation; stock-not-enough picking visibility; barcode pick; manual pick with reason; mismatch log; duplicate barcode block; missing manual reason block; 10kg tolerance; cancel releases reservation; pickup completion; ready delivery handoff; driver accept; delivery job status sync to order all passed.
- Browser/mobile visual QA - blocked by local in-app browser runtime failure: `CreateProcessAsUserW failed: 5`. Route reachability check showed protected order/delivery pages redirect unauthenticated users to `/login`.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first attempt hit a transient existing Next build lock; retry passed.
- `npm.cmd run smoke` - passed.
- `npx.cmd supabase db reset --local --yes` - passed on the final schema.

Files changed in this pass:

- `supabase/seed.sql`
- `supabase/migrations/202606230008_order_module_v1.sql`
- `supabase/migrations/202606230015_delivery_permissions_audit_hardening_v1.sql`
- `supabase/migrations/202606230021_retail_processing_v1.sql`
- `supabase/migrations/202606230022_retail_processing_bom_master_v1.sql`
- `supabase/migrations/202606230023_retail_daily_cash_closing_v1.sql`
- `supabase/migrations/202606230024_retail_audit_trail_v1.sql`
- `supabase/migrations/202606230026_order_reservation_logic_v1.sql`
- `lib/auth/access.ts`
- `server/supabase-admin.ts`
- `app/api/whatsapp/send/route.ts`
- `app/api/whatsapp/webhook/route.ts`
- `components/whatsapp-crm/whatsapp-crm-inbox.tsx`
- `HANDOFF.md`

Remaining risks / manual checks:

- Visual browser/mobile QA still needs to be rerun after the Windows browser runtime permission issue is fixed.
- Delivery proof photo/GPS completion remains a Delivery Module responsibility and was not re-tested with real camera/upload in this pass.
- The local Supabase gateway may need a restart after `db reset` if Docker assigns Auth a new container IP; this was handled during QA with `docker restart supabase_kong_elite-meat-erp`.

## 2026-06-24 - Retail V1 staging QA blocker fixes

Task completed:

- Continued the Retail Module V1 real staging QA goal.
- Fixed Retail file upload UX by replacing staff-facing path inputs with actual file inputs for Daily Sales AutoCount attachment, Expense receipt, Cleaning photo, and Processing wastage photo.
- Added server-side Retail uploads to the existing `erp-files` bucket using outlet-scoped paths: `retail/<outlet-id>/<folder>/...`.
- Added Retail file metadata with `files.outlet_id` and outlet-aware storage/RLS policies so Retail files are scoped by outlet.
- Added Retail Reports CSV export.
- Fixed repeated same-day cleaning completion upserts by adding the missing `retail_cleaning_completions` update policy.
- Applied `202606240001_retail_file_upload_isolation_v1.sql` and `202606240002_retail_cleaning_completion_upsert_policy_v1.sql` to the linked staging Supabase project after inspecting migration status. No reset was performed.
- Reran authenticated staging QA with real worker, manager, admin, and director users. Retail-specific staging QA passed, including cross-outlet storage download blocking.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `scripts/retail-v1-e2e-coverage.mjs`
- `scripts/retail-v1-staging-real-qa.mjs`
- `supabase/migrations/202606240001_retail_file_upload_isolation_v1.sql`
- `supabase/migrations/202606240002_retail_cleaning_completion_upsert_policy_v1.sql`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `node scripts\retail-v1-staging-real-qa.mjs` - passed against linked staging Supabase.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Retail V1 is not ready for UAT until full authenticated browser/mobile QA is completed.
- Full authenticated browser/mobile QA still needs to be completed on a real phone or 390px browser session.
- Non-Retail pending migrations remain outside this pass and were not applied through full `db push`.

## 2026-06-24 - Stock scanner session count

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so the camera sheet shows `Camera session scans` after successful camera reads.
- The session count resets every time the scanner opens and increments only after an accepted camera decode is passed to the existing scan handler.
- Extended Stock scanner/mobile UX coverage so the camera-session count is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the session count at 390px width during continuous scanning.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode decode rules, duplicate rules, or stock movement behavior.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt hit an existing Next build lock; retry after waiting passed.

Risks / remaining checks:

- Real phone QA is required to confirm the session count fits cleanly around 390px width and increments with real camera scans.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock label action helper text

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock label print/reprint action buttons for workers on phones.
- `Print label` now shows `Bluetooth printer` as helper text.
- `PDF fallback` now shows `Save as PDF` as helper text.
- Kept the existing browser print-sheet behavior for both actions.
- Extended Stock label/mobile UX coverage so the helper text is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the clearer label action text at 390px width.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode generation, label page size, or stock movement behavior.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm the helper text fits cleanly around 390px width and the phone print/PDF sheet behaves as expected.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock scanner manual-entry fallback focus

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so workers can tap `Use manual entry` inside the scanner sheet.
- `Use manual entry` closes the camera sheet and focuses the manual barcode input, so workers can recover quickly when camera permission is blocked or the camera is unavailable.
- Kept the existing `Close` behavior returning focus to the `Scan Barcode` button.
- Extended Stock scanner/mobile UX coverage so the manual-entry fallback focus path is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the manual-entry fallback from the scanner sheet.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode decode logic, continuous scan logic, camera stream cleanup, or stock movement behavior.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm `Use manual entry` closes the camera sheet and places the cursor in the barcode input on iOS/Android browsers.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock scanner focus return

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner so focus returns to the `Scan Barcode` button after the camera sheet closes.
- This helps keyboard and assistive-technology users stay in the same Stock workflow after scanning or closing the modal.
- Extended Stock scanner/mobile UX coverage so scanner trigger focus restoration is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks that focus returns after closing the scanner where possible.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode decode logic, continuous scan logic, camera stream cleanup, or stock movement behavior.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone/keyboard/screen-reader QA is required to confirm focus returns to the scan button after closing the camera sheet.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock scanner modal scroll lock

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner on mobile by locking background page scroll while the scanner modal is open.
- Restores the previous page scroll style when the scanner closes or unmounts.
- Extended Stock scanner/mobile UX coverage so the scroll lock and restore behavior are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks that the page behind the scanner does not scroll.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode decode logic, continuous scan logic, camera stream cleanup, or stock movement behavior.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm the page behind the camera sheet does not scroll while scanning and that page scrolling returns after close.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock scanner dialog accessibility

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the reusable Stock camera scanner dialog for phone/accessibility testing.
- The scanner modal now uses dialog semantics, links its title/description, and moves focus to the scanner sheet when opened.
- Extended Stock scanner/mobile UX coverage so the dialog accessibility behavior is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the scanner announcement/focus behavior where possible.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, barcode decode logic, continuous scan logic, camera stream cleanup, or stock movement behavior.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm the camera sheet opens cleanly, announces the expected title/help text with assistive tech, and remains easy to close.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock inbound batch-number keyboard hint

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the advanced Barcode Inbound batch-number field for phone workers.
- The auto-generated, editable batch number now requests a mobile `Done` action and disables browser autofill.
- Extended Stock mobile UX coverage so the batch-number keyboard hint is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the batch-number keyboard on a real phone.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, approval workflows, barcode scanning behavior, or stock movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm iOS/Android browsers show the expected Done action for the batch-number field.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock reference-number keyboard hints

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved optional Stock reference-number typing for phone workers.
- Barcode Inbound, Stock Outbound, Transfer, Receive Transfer, and Stock Return optional reference-number fields now request a mobile `Done` action and disable browser autofill.
- Extended Stock mobile UX coverage so these optional paperwork fields are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks reference-number keyboards on a real phone.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, validation rules, approval workflows, or stock movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm iOS/Android browsers show the expected Done action for optional reference-number fields.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock return/damage fallback keyboard hints

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved Return/Damage fallback typing for phone workers.
- Direct outbound damage photo reference, return/damage photo reference, direct outbound supplier name, and return-supplier supplier name now request a mobile `Done` action and disable browser autofill.
- Extended Stock mobile UX coverage so these specific fallback fields are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the supplier/photo fallback keyboards on a real phone.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, damage photo requirement, supplier requirement, approval workflows, validation rules, or stock movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed in unrelated Retail coverage: `Retail expense submission must require a receipt.`
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm iOS/Android browsers show the expected Done action for these fallback fields.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.
- Full smoke remains blocked by the unrelated Retail receipt coverage failure above; no Retail files were changed in this Stock-only pass.

## 2026-06-24 - Stock inbound search keyboard hints

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved Barcode Inbound product search so phone browsers can show a search keyboard/action.
- Added a `Done` keyboard hint to the quick-create product name fallback in the inbound flow.
- Extended Stock mobile UX coverage so the inbound search and quick-create keyboard hints are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the product search keyboard on a real phone.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, product filtering behavior, item creation rules, validation rules, or stock movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm iOS/Android browsers show the expected search and Done keyboard actions.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock inbound weight keyboard hints

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved Barcode Inbound weight entry for the no-weight/internal-label path.
- `Net weight kg` and fallback fixed-weight inputs now request a decimal phone keyboard with a `Done` action.
- Barcode weight-rule position, digit, and decimal fields now request a numeric phone keyboard with a `Done` action.
- Extended Stock label/mobile UX coverage so these inbound mobile input hints are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the weight and rule fields on a real phone.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, barcode decoding, scanner camera behavior, validation rules, or stock movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm iOS/Android browsers show decimal/numeric keyboards and Done actions for these fields.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Retail V1 Staging Supabase QA

Task completed:

- Treated linked project `aikfqnbsshflbtuakwrz` as safe non-production/staging per owner instruction.
- Inspected remote migration status before applying Retail migrations.
- Applied Retail migration chain `202606230016` through `202606230025` to staging using Retail-only `supabase db query --linked --file`.
- Repaired remote migration history for Retail versions `202606230016` through `202606230025`.
- Verified Retail V1 tables exist with RLS enabled.
- Verified unique indexes for one daily sales summary per outlet/date and one cash closing per outlet/date.
- Seeded real Retail QA Auth users through Supabase Admin API and assigned profiles, roles, outlets, and Retail module access.
- Ran authenticated staging API QA for worker, manager, admin, and director roles.
- Verified tested outlet isolation, cross-outlet read/edit blocking, daily sales, expenses, cleaning, processing, cash closing, audit logs, and admin/director all-outlet visibility.
- Found Retail V1 is not ready for UAT due to file upload/export blockers.

Fixes applied during QA:

- Fixed `202606230018_retail_daily_sales_summary_v1.sql` UUID aggregation by casting UUIDs before `min`/`max`.
- Added missing `public.can_view_processing()` helper in `202606230021_retail_processing_v1.sql`.

Files changed in this pass:

- `supabase/migrations/202606230018_retail_daily_sales_summary_v1.sql`
- `supabase/migrations/202606230021_retail_processing_v1.sql`
- `scripts/retail-v1-staging-qa-seed.sql`
- `scripts/retail-v1-staging-auth-admin-seed.mjs`
- `scripts/retail-v1-staging-real-qa.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npx.cmd supabase migration list` - Retail `202606230016` through `202606230025` now applied remotely.
- `node scripts\retail-v1-staging-real-qa.mjs` - completed with three UAT blockers.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Retail V1 is not ready for UAT.
- Blocker: Retail forms use attachment/photo path text inputs instead of actual file inputs and storage uploads.
- Blocker: `erp-files` storage policies are too broad; Outlet B worker could download Outlet A uploaded QA file.
- Blocker: Retail Reports V1 has no export control.
- Full authenticated browser/mobile QA should be rerun after these blockers are fixed.

## 2026-06-24 - Stock manual barcode Done key

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the shared Stock manual barcode fallback field so phone browsers can show a numeric keyboard with a `Done` action.
- Added a numeric input pattern to further encourage numeric barcode entry on mobile keyboards.
- Extended `scripts/stock-scanner-coverage.mjs` so the manual barcode input keyboard hints are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the numeric keyboard with Done key on a real phone.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, barcode decoding, scanner camera behavior, validation rules, or stock movement behavior.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm iOS/Android browsers display the numeric keyboard and Done key as expected.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock mobile QA preparation

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Reviewed the Stock mobile QA surfaces for worker home buttons, inbound, continuous scanning, duplicate/no-weight warnings, label generation/reprint, order/direct outbound, damage/spoilage request, transfer, receive-transfer wrong-location blocking, stock-take progress, unknown-barcode exceptions, and online-only scan blocking.
- Confirmed existing source guards cover the requested Stock mobile checklist through Stock mobile UX, scanner, label, and owner-QA documentation coverage scripts.
- Confirmed the local app can start in foreground at `http://127.0.0.1:3100`, but authenticated 390px browser automation could not run in this Windows sandbox because the in-app browser runtime returned `CreateProcessAsUserW failed: 5`.
- Updated Stock QA evidence/checklist docs to make clear what is source-verified and what still needs owner/device evidence on Vercel, phone camera, and Bluetooth/PDF label printing.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, stock movement behavior, role access, scanner validation, or label generation behavior.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - first parallel run failed while `npm.cmd run build` was regenerating `.next/types`; rerun by itself passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required.
- Real phone camera scanning, camera permission, rear-camera preference, connection-loss scanner stop, sound/vibration, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - No-barcode inbound mobile link

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the no-barcode stock guidance card so `Open Barcode Inbound` is a full-width phone-size action around 390px width.
- The action now uses `next/link` in-app navigation to `/stock/inbound`.
- This keeps the MVP path clear: generate/attach a barcode label first, then inbound as barcode stock.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the no-barcode guidance card and phone-size in-app link are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks `/stock/no-barcode-inbound`.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, loose-stock behavior, scanner behavior, validation, or stock movement behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm `Open Barcode Inbound` is easy to tap and routes smoothly to `/stock/inbound`.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock manual barcode keyboard

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the shared Stock manual barcode fallback input to request the mobile numeric keyboard.
- Disabled autocorrect/capitalization on the manual barcode field so long barcode typing or pasting is less error-prone.
- This applies to Stock scan workflows that use the shared `BarcodeField`.
- Extended `scripts/stock-scanner-coverage.mjs` so these manual-entry mobile attributes are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the manual barcode fallback keyboard on a real phone.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, stock movement behavior, scanner camera behavior, or validation logic.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is required to confirm the numeric keyboard appears for manual barcode fallback on iOS/Android browsers.
- Real phone camera scanning, Bluetooth printer output, PDF label output, authenticated 390px browser QA, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock in-app navigation links

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock worker shortcut buttons to use `next/link` in-app navigation.
- Updated overdue transfer `Open receive` and barcode unit `Open / reprint` actions to use `next/link`.
- This keeps phone movement between Stock pages smoother without changing routes, permissions, validation, or stock workflows.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the worker shortcuts and stock-unit reprint link are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks Stock shortcut and reprint navigation around 390px width.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, stock movement behavior, scanner behavior, or validation logic.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm shortcut and reprint navigation feels smooth on phone.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Inbound finish button mobile size

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound `Finish Inbound Session` to use a full-width phone-size button around 390px width.
- The button switches back to compact desktop sizing on larger screens, matching the shared Stock submit button pattern.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the finish-session mobile button sizing is source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the finish button at phone width.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, stock movement behavior, session-finish behavior, or validation logic.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the finish-session action is easy to tap after a saved inbound scan.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock scan pages recent movement cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added mobile movement cards to the shared `Recent movements` card shown after Stock scan workflows.
- This covers `/stock/inbound`, `/stock/outbound`, `/stock/transfer`, `/stock/receive-transfer`, `/stock/return`, and `/stock/no-barcode-inbound`.
- Workers can now confirm recent stock actions around 390px width before reaching the full horizontal table.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the scan-page recent-movement cards are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the scan-page recent-movement cards around 390px width.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, stock movement behavior, scanner behavior, or validation logic.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm recent-movement cards are readable on scan pages before the table.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock dashboard latest movement cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added mobile movement cards to `/stock/dashboard` inside the `Latest movements` card before the full movement table.
- The cards reuse the existing mobile movement-history layout with movement type, item, barcode, quantity, weight, from/to location, time, and reference.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the dashboard latest-movement mobile cards are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the dashboard latest-movement cards around 390px width.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, stock movement behavior, or dashboard KPI logic.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm dashboard latest-movement cards are readable before the table.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Mobile item master cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added mobile-only item master cards on `/stock/items` before the full Stock Item Master table.
- Each card shows item code, product name, category/section, brand, barcode requirement, and active status.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the item master mobile cards are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the item master mobile cards around 390px width.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, item-master permissions, or stock movement workflows.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm item master cards are readable before the table.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Mobile barcode unit cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added mobile-only barcode stock unit cards on `/stock/balance` before the full barcode unit table.
- Each card shows barcode, item, brand/origin, status, weight, location, received date, and a large `Open / reprint` action.
- The action opens `/stock/units/[id]` so workers can inspect the unit or reprint a label without using the wide table first.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the barcode unit mobile cards are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the barcode unit mobile cards and `Open / reprint` path around 390px width.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, stock movement workflows, or stock-unit edit behavior.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm barcode unit cards are readable and the `Open / reprint` action opens the right stock-unit detail page.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock mobile select touch targets

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock item edit category/default-brand dropdowns to use phone-size controls instead of the older short select height.
- Updated Stock movement/report filter dropdowns to use phone-size controls.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so it guards against the old short Stock select class returning in Stock worker forms or Stock page filters.
- Updated Stock QA docs so owner/Vercel testing checks item edit and filter dropdown touch targets around 390px width.
- Retried local in-app browser QA, but the browser connection was blocked by the Windows sandbox permission error `CreateProcessAsUserW failed: 5`; real authenticated 390px evidence still needs owner/Vercel testing.
- Did not change server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm item edit and Stock filter dropdowns are easy to tap.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Receive/Return assigned stock-location defaults

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated receive-transfer so an active `profile.stockLocationId` is preferred for the receiving location when the page opens.
- Updated normal Stock Return so an active `profile.stockLocationId` is preferred for the return location when the page opens.
- Added short green worker cues beside the location pickers: `Default receiving location: [location]. You can change it.` and `Default return location: [location]. You can change it.`
- Kept quick location buttons and dropdowns editable before scanning.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the receive-transfer and return assigned-location defaults are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the receive-transfer and return assigned-location defaults at phone width.
- Did not change receive-transfer validation, wrong-location blocking, return validation, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm receive-transfer and return default to the live scoped worker's assigned stock location and remain editable before scanning.
- Real wrong-location receive-transfer, return-save, phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock Take assigned stock-location default

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Take session creation so an active `profile.stockLocationId` is preferred for the new session location.
- Added a short green worker cue beside the Stock Take location picker: `Default stock take location: [location]. You can change it.`
- Kept quick location buttons and the location dropdown editable before starting the session.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the Stock Take assigned-location default and worker cue are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the Stock Take assigned-location default at phone width.
- Did not change stock-take scan rules, approval rules, exception handling, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the assigned location appears first for a live scoped stock worker and remains editable before starting stock take.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Inbound assigned stock-location default

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound initial setup so an active `profile.stockLocationId` is preferred when the page opens, even if browser local storage has an older saved inbound location.
- Added a short green worker cue beside the inbound location picker: `Default location: [location]. You can change it.`
- Kept location editable before the first saved scan and kept the existing session lock after scanning.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the assigned-location default and worker cue are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the assigned-location default at phone width.
- Did not change inbound save rules, barcode validation, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the assigned location appears first for a live scoped stock worker and remains editable before scanning.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Outbound blocked scan row visibility

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound scanned rows so missing, blocked-status, or wrong-transfer-destination barcodes turn red in the scanned list itself and show `Remove before confirm.` guidance beside the row.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the row-level blocked scan styling and messages are source-guarded.
- Updated Stock QA docs so owner/Vercel testing checks the red row-level blocked scan state at phone width.
- Did not change outbound business rules, substitution rules, transfer rules, barcode validation, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm red blocked outbound rows are readable without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock Take scan-rule 390px cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the Stock Take `Barcode-only count.`, `Wrong item/brand blocked.`, and `Unknown barcode is exception.` rule cards to use a three-column layout at the 390px worker-phone breakpoint.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the Stock Take scan-rule 390px layout marker is source-guarded.
- Did not change stock-take business rules, barcode validation, exception handling, approvals, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm Stock Take rule cards fit without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Outbound order comparison 390px layout

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound requested-vs-scanned warning rows to use a two-column layout at the 390px worker-phone breakpoint.
- Updated order item checklist rows to show item details and scanned totals side by side at the 390px breakpoint.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the outbound comparison 390px layout markers are source-guarded.
- Did not change outbound business rules, substitution rules, weight-difference allowance, barcode validation, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm order checklist and warning rows fit without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Inbound saved scan 390px cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound recent saved-scan cards to use a two-column grid at the 390px worker-phone breakpoint.
- Updated the finished inbound session detail grid to use the same 390px breakpoint.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the saved-scan and session-summary 390px layout markers are source-guarded.
- Did not change inbound business rules, undo behavior, label generation, barcode parsing, scan submission, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm saved scan cards and finished-session details fit without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Inbound recent template 390px grid

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound recent templates to use a two-column grid at the 390px worker-phone breakpoint.
- Added the worker helper `Tap Brand + Product to scan faster.` under recent inbound templates.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so the recent-template helper is source-guarded.
- Did not change inbound business rules, barcode parsing, scan submission, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm recent templates fit two per row without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Barcode field online-only guard

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Extended the shared Stock barcode field so offline protection covers both manual barcode input and the `Scan Barcode` camera button.
- If the browser is offline, the barcode input and camera button are disabled with `Connection lost. Please reconnect before scanning.`
- The shared camera scanner still stops if the connection drops while the scanner is open, and detected values are not forwarded to the Stock workflow.
- Extended `scripts/stock-scanner-coverage.mjs` and `scripts/stock-mobile-ux-coverage.mjs` so barcode-field online blocking is source-guarded.
- Did not change barcode decoding, scan submission, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone connection-loss QA is still required to confirm manual input and camera scan are disabled consistently on iOS/Android browsers and no scan is saved while offline.
- Real authenticated 390px browser QA, phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Scanner-level online-only guard

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added an online-only guard directly inside the shared Stock camera scanner.
- If the browser is offline, the `Scan Barcode` button is disabled with `Connection lost. Please reconnect before scanning.`
- If the connection drops while the camera scanner is open, the camera controls stop and detected values are not forwarded to the Stock workflow.
- Extended `scripts/stock-scanner-coverage.mjs` and `scripts/stock-mobile-ux-coverage.mjs` so scanner-level online blocking is source-guarded.
- Did not change scanner barcode decoding, scan submission, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after rerun by itself. The first parallel run overlapped with `npm.cmd run build` while `.next/types` was regenerating and produced transient missing `.next/types` file errors.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone connection-loss QA is still required to confirm the scanner stops cleanly on iOS/Android browsers and no scan is saved while offline.
- Real authenticated 390px browser QA, phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Scanner modal aim guidance

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the shared Stock barcode scanner modal with rear-camera guidance and a visible scan frame that says `Keep barcode inside the box.`
- Camera startup errors now announce assertively to workers.
- Extended `scripts/stock-scanner-coverage.mjs` and `scripts/stock-mobile-ux-coverage.mjs` so the scanner guidance is guarded by smoke.
- Did not change scanner business behavior, barcode decoding, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera QA is still required to confirm the scan frame helps with actual barcode detection and the rear camera is selected by the browser/device.
- Real authenticated 390px browser QA, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Return and damage mobile scan context cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a green `Returning to` card above the Stock Return scanner after a return location is selected.
- Added an amber `Requesting damage` card above the Damage/Spoilage scanner after a damage photo is selected.
- Added an amber `Returning supplier` card above the Return Supplier scanner after a supplier is selected.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so these return/damage context cards are guarded by smoke.
- Did not change return, damage/spoilage, or return-supplier business rules, approval rules, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the cards appear above the scanners without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Transfer mobile scan context cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a green `Sending to` card above the Transfer scanner after a destination stock location is selected.
- Added a green `Receiving at` card above the Receive Transfer scanner after a receiving stock location is selected.
- The receive card reminds workers: `Scan only barcodes for this destination.`
- Extended `scripts/stock-mobile-ux-coverage.mjs` so both transfer context cards are guarded by smoke.
- Did not change transfer/receive-transfer business rules, wrong-location blocking, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the cards appear above the scanners without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Outbound mobile scan context card

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a green `Scanning outbound` card above the Stock Outbound scanner once the outbound scan setup is ready.
- The card shows the selected order/customer for order outbound, the selected direct outbound type for direct outbound, transfer destination when applicable, and the worker reminder `Scan multiple barcodes, then confirm.`
- Extended `scripts/stock-mobile-ux-coverage.mjs` so this outbound context card is guarded by smoke.
- Did not change outbound business rules, substitution rules, damage/spoilage approval behavior, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the card appears above the scanner without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Inbound mobile scan context card

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a green `Scanning inbound` card above the Barcode Inbound scanner once product, brand, origin, and location are ready.
- The card shows the active brand, product, origin, and location before workers scan, and shows `Session locked. Finish before changing setup.` after the first saved scan.
- Extended `scripts/stock-mobile-ux-coverage.mjs` so this inbound context card is guarded by smoke.
- Did not change inbound save logic, duplicate blocking, label generation, undo, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the card appears above the scanner without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock take mobile active-scope card

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a green mobile `Scanning for` card above the Stock Take scanner so workers can confirm the active item, brand, and location before scanning.
- Added short rule copy in the same card: `Wrong item or brand is blocked.`
- Extended `scripts/stock-mobile-ux-coverage.mjs` so this active-scope card is guarded by smoke.
- Did not change stock-take business rules, approval rules, exception handling, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required to confirm the card appears before the scanner without horizontal scrolling.
- Real phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock mobile requirement audit

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md` so each confirmed Stock Mobile MVP rule has a current status, source evidence, and remaining manual QA proof.
- Wired the new audit into `scripts/stock-owner-qa-doc-coverage.mjs`, which runs through `npm.cmd run smoke`.
- Linked the audit from the owner and Vercel Stock QA checklists.
- Did not change Stock business logic, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- The audit confirms source-guarded status, not real device completion.
- Real authenticated 390px browser QA, phone camera scanning, Bluetooth printer output, PDF label output, and live Supabase/RLS workflow evidence remain required.

## 2026-06-24 - Stock mobile QA preparation re-run

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Re-ran the local Stock mobile source coverage for the requested worker QA areas: Stock home buttons, inbound session, continuous scanning UI, duplicate warning, no-weight warning, label generation, label reprint, outbound by order, direct outbound, damage/spoilage request, transfer, receive-transfer wrong-location block, stock-take progress, unknown-barcode exception, and online-only connection message.
- Confirmed from source that damage/spoilage photo capture uses phone-friendly image inputs with rear-camera hint, `Photo selected:` feedback, `Photo reference fallback`, and the short `Add damage photo first.` scanner block.
- Confirmed this run did not change Stock business logic, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.
- Real authenticated 390px browser QA, phone camera scanning, Bluetooth printer output, and live Supabase workflow evidence still need owner/device testing.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser automation was not available in this environment, so visual 390px evidence must still be captured from Vercel or a local browser.
- Real phone camera permission, rear-camera selection, continuous scan comfort, sound/vibration, and scanner stream stop remain owner/device QA.
- Bluetooth label printer selection/output and PDF 50mm x 30mm output remain owner/device QA.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Outbound warning announcements

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound setup/confirm warnings, weight-difference warnings, and substitution reminders so they announce as polite status text.
- Kept the confirmed outbound rules unchanged: meat weight differences are allowed, substitution is warning-only, and no typed substitution reason is required.
- Updated Stock mobile coverage guards and QA docs for outbound warning announcements.
- Did not change outbound business rules, barcode validation, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm outbound warnings are readable and announced as status text.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Stock scan feedback live announcements

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added explicit live-region priority to local Stock worker scan feedback for transfer, receive-transfer, return, damage/spoilage, return supplier, and stock take.
- Blocked/setup messages now announce assertively and success/progress messages stay polite.
- Attempted in-app browser setup for stronger 390px verification, but the browser runtime is still blocked by the Windows sandbox with `CreateProcessAsUserW failed: 5`.
- Updated Stock mobile coverage guards and QA docs for the scan feedback announcement behavior.
- Did not change stock business rules, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- Browser verification attempt - blocked by Windows sandbox: `CreateProcessAsUserW failed: 5`.
- `node scripts\stock-mobile-ux-coverage.mjs` - failed once on an indentation-sensitive guard, then passed after making the guard check the `aria-live` pattern by regex.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required because local browser automation remains blocked.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Receive-transfer wrong-location reminder

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the receive-transfer mobile reminder from `Wrong location is blocked.` to `Wrong location is blocked. Receive only at the barcode destination.`
- Marked the reminder as polite status text so phone workers have clearer guidance before scanning.
- Kept the server-side wrong-location block and destination-specific error unchanged.
- Updated Stock mobile coverage guards and QA docs for the clearer reminder.
- Did not change transfer/receive-transfer business rules, server actions, RPCs, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm the receive-transfer reminder is readable before scanning and the dynamic wrong-location error still appears when blocked.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Stock take warning announcement

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the existing Stock Take active warning so the yellow `Stock take is active for this item/brand/location. You can continue, but this movement will be recorded.` message is announced as polite status text.
- Kept the confirmed rule unchanged: active stock-take warnings do not block other stock movement forms.
- Updated Stock mobile coverage guards and QA docs for the warning announcement.
- Did not change stock-take approval rules, exception handling, server actions, RLS policies, database schema, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm the Stock Take warning is visible and exposed as status text.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Inbound duplicate alert semantics

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound duplicate warning so the red `Duplicate barcode. Inbound is blocked.` message uses urgent alert semantics.
- Kept duplicate detection and save-blocking behavior unchanged.
- Updated Stock mobile coverage guards and QA docs for the duplicate blocked alert.
- Did not change inbound business rules, stock save logic, duplicate validation, database schema, server actions, RLS policies, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm the duplicate blocked alert appears immediately.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Inbound session summary 390px layout

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Barcode Inbound live counters so `Saved scans` and `Saved weight` use the 390px mobile breakpoint.
- Updated the finished inbound session summary so total units, total weight, and duplicate/error scans use the 390px mobile breakpoint.
- This helps workers compare inbound count and weight while scanning and reviewing a finished session on common phone widths.
- Updated Stock mobile coverage guards and QA docs for the inbound counter/summary layout.
- Did not change inbound business rules, session locking, duplicate blocking, no-weight handling, label generation, undo/audit behavior, database schema, server actions, RLS policies, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm inbound live counters and finished summary fit without horizontal scrolling.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Outbound scan summary 390px layout

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Outbound scan summary so `Scanned units`, `Known weight`, and `Unknown scans` use the 390px mobile breakpoint.
- This helps workers compare outbound batch status at a glance while scanning on common phone widths.
- Updated Stock mobile coverage guards and QA docs for the outbound summary layout.
- Did not change outbound business rules, barcode validation, substitution handling, damage/spoilage request flow, database schema, server actions, RLS policies, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm outbound summary metrics fit side by side without horizontal scrolling.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Stock label print guidance card

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Changed the Stock label print note from plain helper text into a green worker guidance card.
- The card now says Bluetooth label printer first, PDF fallback if unavailable, 50mm x 30mm, and one label per page.
- Updated Stock label/mobile coverage guards and QA docs for the clearer label guidance.
- Did not change label generation, barcode format, stock auto-save, print/PDF behavior, database schema, server actions, RLS policies, migrations, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm the label guidance card is readable without horizontal scrolling.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Stock take progress 390px layout

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock Take barcode/weight progress so the two progress cards use the 390px mobile breakpoint.
- This helps workers compare barcode count and total weight progress side by side while scanning on common phone widths.
- Updated Stock mobile coverage guards and QA docs for the 390px Stock Take progress layout.
- Did not change Stock business rules, database schema, server actions, RLS policies, migrations, barcode validation, role access, stock-take exceptions, approval rules, or stock movement workflows.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm Stock Take progress cards fit side by side without horizontal scrolling.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Receive-transfer mobile wording

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the shared Stock transfer location picker to support context-specific wording.
- Kept transfer and direct transfer using destination-location wording.
- Changed receive-transfer to show `Quick receiving locations`, `Tap receiving location to scan faster.`, and a green `Selected receiving location` confirmation before barcode scanning.
- Updated Stock mobile coverage guards and QA docs for the receive-transfer wording.
- Did not change Stock business rules, database schema, server actions, RLS policies, migrations, barcode validation, role access, or transfer/receive-transfer state transitions.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required to confirm the receive-transfer location wording fits without horizontal scrolling.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Stock scanner blocked reason announcement

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the shared Stock barcode field so disabled scan reasons are linked to the manual barcode field and camera scan button.
- Changed connection-loss blocked scan messaging to announce urgently as an alert while keeping normal setup blockers as polite status messages.
- Updated Stock mobile coverage guards and QA docs for the linked blocked-reason behavior.
- Did not change Stock business rules, database schema, server actions, RLS policies, migrations, barcode validation, role access, or stock movement workflows.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Stock mobile QA preparation pass

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Ran a Stock mobile QA preparation pass for the requested worker areas: Stock home buttons, inbound session, continuous scanning UI, duplicate warning, no-weight warning, label generation, label reprint, outbound by order, direct outbound, damage/spoilage request, transfer, receive-transfer wrong-location block, stock-take progress, unknown-barcode exception, and online-only connection message.
- Confirmed the automated Stock smoke suite covers mobile UX, scanner, label, stock-take, report, RLS-policy, security, and owner-QA document guards.
- Confirmed the local app can start in foreground with a temporary dev server command, but authenticated 390px browser-route QA was not completed because a stable background local browser server was not available in this run.
- Did not change Stock app code, business logic, database schema, server actions, RLS policies, migrations, role access, or workflow rules.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Vercel/local browser QA is still required.
- Real phone camera permission, rear camera selection, continuous scan comfort, sound/vibration, and scanner stream stop are still owner/device QA.
- Bluetooth label printer output and 50mm x 30mm PDF label output still need real device/file evidence.
- Live Supabase RLS/data evidence for worker, manager, admin, and director scopes remains pending.

## 2026-06-24 - Stock movement history mobile cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added mobile summary cards to Stock Movement History before the full movement table.
- Cards show movement type, item, barcode, weight, quantity, from location, to location, time, and reference where available.
- No stock value, cost, or finance data was added.
- Kept the existing movement table for full detail review.
- Kept stock data, RLS, server actions, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm Stock Movement History cards are readable and show no cost/value/finance data.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-24 - Retail V1 Real Supabase QA Blocked

Task completed:

- Continued the Retail Module V1 validation goal.
- Checked the available Supabase configuration without printing secret values.
- Found the workspace points to remote Supabase project ref `aikfqnbsshflbtuakwrz`, but the project is not confirmed as staging.
- Found only public Supabase anon configuration in `.env.local`; no staging service/admin credential or database connection was available for migration application, QA user creation, or live RLS validation.
- Did not apply migrations and did not modify the remote Supabase project because production use is not approved.
- Updated Retail V1 E2E evidence with the blocked real-environment QA status and final recommendation.

Files changed in this pass:

- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npx.cmd supabase --version` - passed outside sandbox; Supabase CLI `2.106.0`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Retail V1 remains not ready for UAT until a confirmed non-production Supabase staging project is provided and tested with real QA users.
- Pending: apply migrations to staging, seed/verify QA users, test outlet isolation and RLS, test worker/manager/admin/director workflows, test uploads, and run mobile QA.

## 2026-06-24 - Stock balance mobile cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added mobile summary cards to Stock Balance before the full balance table.
- Cards show item, location, category, barcode units, total kg, total quantity, barcode kg, and alert state.
- No stock value, cost, or finance data was added.
- Kept the existing balance table for full detail review.
- Kept stock data, RLS, server actions, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm Stock Balance cards are readable and show no cost/value/finance data.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-24 - Stock unit mobile movement summary

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added mobile movement summary cards to Stock unit detail before the full movement table.
- The cards show recent movement type, time, weight, from location, to location, and reference where available.
- Kept the existing movement table for full audit/detail review.
- Kept stock-unit data, movement data, label reprint logic, server actions, RLS, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/stock-unit-detail.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm movement cards are readable and the full table remains available below.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-24 - Stock unit mobile back button

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock unit detail so `Back to stock balance` is a full-width phone-size button.
- This makes the mobile label reprint/history flow easier to leave after a worker reprints a label.
- Kept stock-unit data, label reprint logic, movement history, server actions, RLS, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/stock-unit-detail.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the stock-unit back button is easy to tap and returns to `/stock/balance`.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-24 - Stock label print action grid

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated Stock label print actions so `Print label` and `PDF fallback` use a two-column layout at about 390px width.
- Kept `Print label` as the primary phone/Bluetooth print path and kept `PDF fallback` available.
- Kept label generation logic, barcode content, print CSS, server actions, RLS, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm label print actions fit side by side without horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-24 - Stock scanner phone header layout

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the reusable Stock barcode scanner field so the label and large `Scan Barcode` button stay stacked on phone widths.
- The scanner field switches to a row only from the larger `sm` breakpoint, avoiding squeezed controls around 390px.
- Kept scanner camera behavior, manual fallback, server actions, RLS, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm Stock scanner fields are not squeezed and have no horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-24 - Stock worker home 390px shortcut grid

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the Stock worker shortcut grid so the six worker actions use a two-column layout at about 390px width.
- Kept the same worker actions: Inbound, Outbound, Transfer, Receive, Return / Damage, and Stock Take.
- Allowed shortcut labels to wrap cleanly so `Return / Damage` stays readable without horizontal scrolling.
- Kept Stock routing, role access, server actions, RLS, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the two-column Stock worker home has no horizontal scrolling and feels easy to tap.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-24 - Stock mobile submit button touch targets

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated the shared Stock workflow submit button so final actions are full-width on phone screens and compact on desktop.
- This makes Stock worker actions such as submit, confirm, send, receive, review, approve, and save easier to tap around 390px width.
- Kept Stock server actions, RLS, migrations, validation, and workflow rules unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the full-width action buttons feel right on each Stock worker page.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock inbound mobile undo button

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/inbound` current-session saved scan cards so `Undo scan` is a full-width phone-size button.
- Added a short worker-facing hint: `Undo before finishing this session.`
- Kept inbound undo server action, audit trail behavior, `VOIDED` status, `INBOUND_VOID` movement, RLS, and stock movement logic unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the bigger undo button is easy to tap and the finished-session lockout is clear.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock outbound mobile remove scan button

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/outbound` scanned barcode rows so the `Remove` action is a full-width phone-size button.
- Added a short worker-facing hint: `Remove a wrong scan before final confirm.`
- Kept outbound server actions, substitution rules, validation, RLS, and stock movement logic unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the bigger remove button is easy to tap and scanned count/weight update clearly.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock damage and supplier previous scan feedback

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/return` damage/spoilage and supplier-return request forms so workers see the barcode from the previous successful request.
- Added `Previous damage scan` and `Previous supplier return scan` feedback cards after successful request submissions.
- Kept damage/supplier-return server actions, photo-required validation, approval rules, RLS, and stock movement logic unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the previous-request feedback is visible without horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock return previous scan feedback

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/return` so workers see the barcode from the previous successful normal return scan.
- Added a `Previous return scan` feedback card after successful return submissions.
- Kept return server action, return validation, customer-return inspection behavior, RLS, and stock movement logic unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the previous-return feedback is visible without horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock transfer previous scan feedback

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/transfer` and `/stock/receive-transfer` so workers see the barcode from the previous successful scan.
- Added `Previous transfer scan` and `Previous receive scan` feedback cards after successful submissions.
- Kept transfer/receive server actions, wrong-location receive blocking, RLS, transfer status rules, and stock movement logic unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the previous-scan feedback is visible without horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock damage photo-first mobile flow

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved direct outbound damage/spoilage and `/stock/return` damage/spoilage so the phone photo picker is the primary worker action.
- Added clear photo status: `Photo selected:` after choosing an image, or `Take photo before scanning.` before a photo exists.
- Moved manual photo reference typing into a collapsed `Photo reference fallback` section for the rare case where the picker does not fill the file name.
- Kept the same submitted photo fields, server actions, photo-required validation, approval rules, RLS, and stock movement logic unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the photo picker fills the selected-photo state on phone browsers.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock take active session auto-select

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/stock-take` so the scan form selects the first active draft session automatically when one exists.
- If the current selected session is not scannable and an active draft session exists, the scan form switches back to the first active draft session.
- Added a short worker-facing message: `First active session is selected automatically.`
- Kept stock-take server actions, barcode-only scanning, wrong item/brand blocking, unknown-barcode exceptions, manager/director approval, RLS, and stock movement logic unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed after replacing synchronous effect state-setting with a derived selected-session value.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the first active draft session is selected automatically and remains easy to change by tapping another active session.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock outbound optional reference tuck-away

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/outbound` by moving the optional reference number into a collapsed `Reference no. (optional)` section.
- Kept the main worker path focused on order/direct setup and barcode scanning.
- Kept all server actions, validation, RLS, stock movement logic, and outbound business rules unchanged.
- Attempted a local 390px browser setup; the app starts in a foreground check, but this sandbox does not keep a localhost dev server alive across tool calls. Real authenticated browser/device QA remains owner/manual QA.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required to confirm the collapsed optional reference section improves the outbound scan path without horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock mobile QA source pass

Task completed:

- Ran/prepared Stock Module Mobile UX QA for the requested worker flows without changing Stock business behavior.
- Source-checked the 390px QA checklist areas: Stock home buttons, inbound session, continuous scanning UI, duplicate warning, no-weight warning, label generation, label reprint, outbound by order, direct outbound, damage/spoilage request, transfer, receive-transfer wrong-location block, stock-take progress, unknown-barcode exception, and online-only connection message.
- Updated Stock QA evidence/checklist docs so owner testing can be done from Vercel or local app with clear phone, camera, and label-printing steps.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser/device QA is still required because local source checks cannot prove tap comfort, camera permission, label printer selection, or live Supabase/RLS behavior.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, and complete Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock inbound larger internal label action

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the `/stock/inbound` no-weight barcode path by making `Generate internal label` a larger full-width mobile action.
- Current guidance now says to enter kg, generate/print/attach the label, then scan the printed label before stock saves.
- Kept the existing barcode generation, server action, RLS, stock movement logic, label format, print/PDF flow, and label reprint flow unchanged.
- Updated Stock mobile and stock label source coverage plus owner/Vercel QA docs to check the larger internal-label action at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - failed once on a stale Stock label coverage string, then passed after updating the guard.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm the internal-label action is easy to tap and the label preview/print actions fit without horizontal scrolling.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock inbound quick product buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/inbound` setup so workers can tap quick product buttons instead of relying only on search/dropdown when no recent template is used.
- Kept the existing recent templates, product search, product dropdown fallback, new-product panel, lock-after-first-scan behavior, server action, RLS, item-master rules, and stock movement logic unchanged.
- Updated Stock mobile source coverage and owner/Vercel QA docs to check the quick inbound product buttons at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm quick inbound product buttons fit without horizontal scrolling and fill setup product with one tap.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock inbound quick location buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/inbound` setup so workers can tap quick location buttons instead of relying only on the location dropdown.
- Kept the existing assigned stock-location default, editable location dropdown fallback, lock-after-first-scan behavior, server action, RLS, stock-location permission logic, and stock movement logic unchanged.
- Updated Stock mobile source coverage and owner/Vercel QA docs to check the quick inbound location buttons at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm quick inbound location buttons fit without horizontal scrolling and fill setup location with one tap.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock inbound quick brand and origin buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/inbound` setup so workers can tap quick brand and origin buttons instead of relying only on dropdowns.
- Kept the existing brand/origin required validation, dropdown fallbacks, Other/custom brand and origin inputs, barcode rule behavior, server action, RLS, and stock movement logic unchanged.
- Updated Stock mobile source coverage and owner/Vercel QA docs to check the quick inbound brand/origin buttons at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm quick inbound brand/origin buttons fit without horizontal scrolling and fill setup fields with one tap.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock take quick item and brand buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/stock-take` session creation so workers can tap quick item and brand buttons after choosing the count location.
- Kept the existing item/brand dropdown fallbacks, item+brand scope rule, scan-only count behavior, approval rules, server action, RLS, and stock movement logic unchanged.
- Updated Stock mobile source coverage and owner/Vercel QA docs to check the quick stock-take item and brand buttons at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm quick stock-take item and brand buttons fit without horizontal scrolling and fill the setup fields with one tap.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Retail Module V1 E2E Scenario Coverage

Task completed:

- Continued the Retail Module V1 goal only.
- Added automated Retail V1 E2E scenario coverage for the 40 requested Task 11 scenarios.
- Covered Access, Daily Sales, Expenses, Cleaning, Processing, Cash Closing, and Reports using:
  - Retail page permission guards.
  - Retail server action role checks and calculations.
  - Supabase migration constraints, RLS policies, and audit triggers.
  - Retail report filtering and KPI logic.
  - Shared status badge success/destructive status styling.
  - Deterministic fixtures for daily sales totals, expected cash, variance, yield %, wastage %, and unaccounted difference.
- Wired the new Retail E2E coverage into `npm run smoke`.
- Added a Retail V1 E2E test evidence note.

Files changed in this pass:

- `scripts/retail-v1-e2e-coverage.mjs`
- `docs/RETAIL_V1_E2E_TEST_RESULTS.md`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-v1-e2e-coverage.mjs` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This pass is automated source, migration, and calculation-fixture coverage; it did not apply migrations to live Supabase.
- Real authenticated QA is still needed with retail worker, retail manager, admin, and director accounts against a migrated live Supabase project.

## 2026-06-23 - Stock take quick location buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/stock-take` session creation so workers can tap large quick stock-take location buttons before choosing item/brand and starting the session.
- Kept the existing location dropdown fallback, item/brand scope selection, scan-only count behavior, approval rules, server action, RLS, and stock movement logic unchanged.
- Updated Stock mobile source coverage and owner/Vercel QA docs to check the quick stock-take location buttons at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed with unrelated Retail script warnings in `scripts/retail-v1-e2e-coverage.mjs`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm quick stock-take location buttons fit without horizontal scrolling and fill the stock-take location with one tap.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock return quick location buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved normal `/stock/return` so workers can tap large quick return-location buttons before scanning.
- Kept the existing location dropdown fallback, scan-and-done return behavior, server action, RLS, stock movement logic, and return validation unchanged.
- Updated Stock mobile source coverage and owner/Vercel QA docs to check the quick return-location buttons at about 390px width.
- Attempted to connect the in-app browser for 390px visual QA, but browser automation is still blocked by the Windows sandbox permission error `CreateProcessAsUserW failed: 5`.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm quick return-location buttons fit without horizontal scrolling and fill the return location with one tap.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Retail Database Tables V1

Task completed:

- Continued the Retail Module V1 goal only.
- Added a non-destructive Supabase refinement migration for the canonical Retail database table set requested in Task 10.
- Preserved existing Retail operational tables and added/synced the V1 names needed by the app:
  - `retail_daily_sales`
  - `retail_expenses`
  - `retail_expense_categories`
  - `retail_cleaning_tasks`
  - `retail_cleaning_completions`
  - `retail_processing_boms`
  - `retail_processing_records`
  - `retail_processing_raw_lines`
  - `retail_processing_finished_lines`
  - `retail_cash_closings`
  - `retail_audit_logs`
- Added/confirmed the one-summary-per-outlet/date constraint for Retail daily sales.
- Added the one-closing-per-outlet/date constraint for canonical Retail cash closings.
- Added canonical processing records and cash closings as compatibility tables populated from the existing Retail processing batch and daily closing tables.
- Added sync triggers so legacy/current Retail table writes keep the new canonical columns or tables aligned.
- Added/refined RLS policies so outlet users can only access their outlet Retail data while admin/director can access all outlet data.
- Updated Retail audit mapping to read the new `field_name` column while keeping the existing `field_changed` fallback.
- Extended Retail V1 coverage checks for the Task 10 schema, canonical fields, constraints, sync triggers, and outlet/admin RLS patterns.

Files changed in this pass:

- `supabase/migrations/202606230025_retail_database_tables_v1.sql`
- `lib/retail/data.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230025_retail_database_tables_v1.sql`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Migration was added locally but not applied to a live Supabase project in this pass.
- Live Supabase RLS should still be verified with real retail worker, retail manager, admin, and director accounts after migration deployment.
- Canonical tables are synced from the existing Retail V1 tables for compatibility; future app code can gradually move reads/writes to the canonical names.

## 2026-06-23 - Stock mobile QA preparation refresh

Task completed:

- Ran or prepared Stock mobile QA for the current Stock Module Mobile UX goal.
- Confirmed the owner/Vercel QA checklist covers the requested 390px flows:
  - Stock home buttons.
  - Inbound session and continuous scanning UI.
  - Duplicate and no-weight warnings.
  - Label generation and stock-unit label reprint.
  - Outbound by order and direct outbound.
  - Damage/spoilage request.
  - Transfer and receive-transfer wrong-location block.
  - Stock take progress and unknown barcode exception.
  - Online-only connection message.
- Updated Stock QA docs to replace stale blocked-check notes with the current clean smoke/lint/typecheck/build result.
- Did not change app behavior, server actions, RLS, schema, migrations, live Supabase data, deployment, camera permission, or printer behavior.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA still needs owner/device evidence.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.
- Use `docs/STOCK_REMOTE_QA_VERCEL.md` first for Vercel testing and record results in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-23 - Retail Audit Trail V1

Task completed:

- Continued the Retail Module V1 goal only.
- Added a dedicated field-level `retail_audit_logs` table for important Retail record updates.
- Added one audit row per changed field with:
  - table name, record ID, field changed, old value, new value, edited by, edited at, optional reason, and outlet scope.
- Added database triggers to audit updates on:
  - `retail_daily_sales`
  - `retail_expenses`
  - `retail_cleaning_tasks`
  - `retail_processing_batches`
  - `retail_daily_closings`
- Kept operational records no-hard-delete aligned: audit trail adds no delete policy and existing operational flows continue to use Cancelled, Rejected, Reviewed, or inactive statuses.
- Added RLS so admin/director can view all Retail audit logs and retail managers can view audit logs for their own outlet.
- Added Retail Settings audit table so manager/admin/director users can review the audit trail inside the Retail module.
- Extended Retail data loading and demo data to include audit logs.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230024_retail_audit_trail_v1.sql`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed after applying the migration to confirm managers only see own-outlet audit logs while admin/director see all.
- Live edit QA is still needed for daily sales, expense status, cleaning task master, processing review/cancel/reject, and cash closing edit/reopen to confirm each changed field creates one audit row.
- Real mobile QA around 390px width is still needed for the wide audit table in Retail Settings.

## 2026-06-23 - Stock Direct Return Supplier Quick Supplier Buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Direct outbound `Return Supplier` now shows recent supplier names as large tap buttons when previous supplier-return requests exist.
- The supplier-first scan/confirm block remains in place, so workers still must choose or enter supplier before scanning.
- Kept server actions, RLS, supplier hold status, approval flow, stock movement logic, and deduction timing unchanged.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm direct Return Supplier recent supplier buttons fit and fill supplier name with one tap.
- Real Supabase/RLS QA is still needed to confirm the submitted request goes to supplier hold pending approval.

## 2026-06-23 - Stock Direct Return Supplier Supplier-First Outbound UX

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved direct outbound `Return Supplier` so the scanner/manual barcode field and final confirm stay blocked until supplier name is filled.
- Added worker-facing helper copy: `Choose supplier before scanning.`
- Kept server-side supplier validation, return-supplier hold status, manager approval, RLS, stock movement logic, and deduction timing unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm direct Return Supplier shows `Choose supplier first.`, then scans after supplier entry and moves stock to supplier hold pending approval.
- Real phone camera, Bluetooth label printer, and live Supabase/RLS evidence remain owner/manual QA.

## 2026-06-23 - Retail Reports V1

Task completed:

- Continued the Retail Module V1 goal only.
- Reworked `/retail/reports` into a Retail Reports V1 dashboard for retail manager/admin/director users.
- Retail workers are blocked from the full reports page with a clear role message; they still keep operational Retail pages.
- Retail manager reports stay scoped to the manager's assigned outlet/team from profile data.
- Admin/director reports can view all outlets and can filter by outlet/team.
- Added KPI cards first for manager view:
  - Today total sales, cash received, payment method breakdown, cash expenses, cash variance, missing cash closing, missing AutoCount attachment, cleaning completion, missing cleaning tasks, raw material used, finished product weight, wastage weight, and processing yield.
- Added KPI cards first for admin/director view:
  - All outlet total sales, outlet comparison, cash variance by outlet, payment method breakdown, daily sales trend, monthly sales trend, missing closing records, expenses summary, cleaning missing tasks, processing yield by outlet, and wastage by outlet.
- Added report filters for date, date range, outlet/team for admin/director only, payment method, status, processing item, and processing type/BOM.
- Added report tables for daily sales summaries, expense list, cleaning missing tasks, cash closing records, processing records, and abnormal processing warnings.
- Moved Retail Reports calculations into `lib/retail/reports.ts` so page components stay presentation-focused.

Files changed in this pass:

- `app/(erp)/retail/reports/page.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/reports.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run typecheck` - failed once on a date-filter TypeScript narrowing issue, then passed after tightening the helper.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed with retail worker, retail manager, admin, and director users to prove report route access and outlet scoping in the target project.
- Real outlet data QA is still needed to confirm management accepts the KPI definitions for trends and outlet comparison.
- Real mobile QA around 390px width is still needed for the filter form and wide report tables.

## 2026-06-23 - Stock Direct Damage Photo-First Outbound UX

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved direct outbound `Damage/Spoilage` so the scanner/manual barcode field and final confirm stay blocked until a damage photo reference exists.
- Updated worker-facing copy to `Photo required. Request only; stock is not deducted now.`
- Kept server-side damage request validation, manager review, director approval, RLS, stock movement logic, and no-immediate-deduction behavior unchanged.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed with unrelated Retail warnings in `components/retail/retail-page.tsx`.
- `npm.cmd run typecheck` - failed in unrelated Retail report files:
  - `app/(erp)/retail/reports/page.tsx:11`: `RetailPage` does not accept a `filters` prop.
  - `lib/retail/reports.ts:55-56`: `date` is possibly undefined.
- `npm.cmd run build` - compiled, then failed during TypeScript on the same `app/(erp)/retail/reports/page.tsx:11` Retail issue.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm direct Damage/Spoilage shows `Add damage photo first.`, then scans after photo selection and creates a pending request without deducting stock.
- Real phone camera, Bluetooth label printer, and live Supabase/RLS evidence remain owner/manual QA.
- Full-project typecheck/build need the unrelated Retail report typing issues fixed before release.

## 2026-06-23 - Retail Daily Cash Closing V1

Task completed:

- Continued the Retail Module V1 goal only.
- Reworked `/retail/cash-closing` daily closing into a V1 daily cash closing flow.
- Retail manager/admin/director can save one closing per outlet/date; worker users still cannot submit cash closing.
- Cash closing form now displays pulled Daily Sales Summary numbers for cash, bank transfer, e-wallet/DuitNow, and credit sales.
- Cash closing form now displays pulled cash expenses from Retail Expenses where payment method is Cash and status is not rejected/cancelled.
- Server action recalculates pulled sales/expense values on submit, then calculates:
  - Expected Cash = Opening Cash + Cash Sales - Cash Expenses.
  - Variance = Actual Cash Counted - Expected Cash.
- Non-zero variance shows a warning in the form and table, but does not block submission.
- Added Reviewed status and reviewed-by/reviewed-at fields while keeping old approved fields compatible.
- Manager same-day edit/reopen and admin/director past edit/reopen are supported through the upsert action and audit log.
- Removed old daily-closing hard-delete policy; closing records should use status changes instead.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `components/ui/status-badge.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230023_retail_daily_cash_closing_v1.sql`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - failed once on migration policy ordering, then passed after reordering matching drop/create policy statements.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed with retail manager, admin, director, and worker users to confirm submit/review/reopen scope behavior.
- Real outlet data QA is needed to confirm Daily Sales Summary and Cash expenses totals match AutoCount/manual expense expectations for the selected outlet/date.
- Real mobile QA around 390px width is still needed for the pulled-number display and variance warning.

## 2026-06-23 - Stock Scan Disabled Reason UX

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved shared Stock barcode fields so disabled scanner/manual barcode areas show the short block reason directly under the field.
- Covered inbound, outbound, transfer, receive-transfer, normal return, inspection release, damage/spoilage, return supplier, and stock take.
- Offline reasons use red blocked styling; missing setup reasons use yellow warning styling.
- Tried to run a real 390px browser pass. Next.js starts in foreground, but this Windows sandbox did not keep a detached dev server alive, and the in-app browser runtime could not start, so real phone/browser evidence remains owner/manual QA.
- No migration, RLS policy, server action, stock movement rule, approval rule, or production data was changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - first run passed before a newer Retail migration appeared; final rerun failed in unrelated Retail migration `supabase/migrations/202606230023_retail_daily_cash_closing_v1.sql` because policy `"retail managers can insert daily cash closings"` is not immediately preceded by `drop policy if exists`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - failed in unrelated Retail files:
  - `components/retail/retail-forms.tsx:734`: local `SelectField` type does not accept `value` / `onChange`.
  - `components/retail/retail-page.tsx:299`: `RetailDailyClosing` has no `approvedByName`.
- `npm.cmd run build` - compiled, then failed during TypeScript on the same `components/retail/retail-forms.tsx:734` Retail issue.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm disabled reasons are readable under each Stock scanner.
- Real phone camera, Bluetooth label printer, and Supabase/RLS workflow evidence are still owner/manual QA.
- Full-project smoke/typecheck/build need the unrelated Retail migration and typing issues fixed before release.

## 2026-06-23 - Retail Processing BOM Master V1

Task completed:

- Continued the Retail Module V1 goal only.
- Added a Retail Processing BOM / processing type master for standard processing names.
- Admin/director users can manage BOM master records from `/retail/settings`.
- Retail managers and workers can select active BOMs when creating daily retail processing records.
- The processing form still supports manual processing type entry when no BOM is selected.
- Inactive BOMs are hidden from new processing records and blocked server-side if submitted.
- BOM master supports global records and optional outlet-specific records for future use.
- Optional expected yield minimum, expected yield maximum, and expected wastage fields are stored nullable and are not enforced in V1.
- Processing records now keep an optional `processing_bom_id` while storing the processing type name at save time for stable reporting.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230022_retail_processing_bom_master_v1.sql`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed before and after lint cleanup.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed after changing one never-reassigned variable to `const`.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed with admin/director and retail manager users to confirm BOM management is admin-only while active BOM use is available in retail processing.
- Real mobile QA around 390px width is still needed to confirm the BOM dropdown plus manual processing type fallback remains comfortable in the daily processing form.
- Future outlet-specific BOM request/create workflow is not built yet; V1 supports the data shape only.

## 2026-06-23 - Retail Processing V1

Task completed:

- Continued the Retail Module V1 goal only.
- Reworked `/retail/processing` from a single raw-to-finished batch entry into a daily processing record flow.
- Workers can create Draft or Submitted processing records for their assigned outlet without selecting outlet repeatedly.
- Processing records now support processing date, outlet scope, processing type/BOM name, multiple raw-material lines, multiple finished-product lines, wastage weight/reason/photo/remarks, and record remarks.
- Server action calculates total raw weight, total finished weight, yield %, wastage %, accounted weight, unaccounted difference, and warning-only abnormal flags.
- Retail manager/admin/director can review, reject, or cancel Submitted processing records; rejected records require a rejection reason.
- Added line tables for raw materials and finished products while keeping V1 record-only behavior: no barcode labels, no stock inbound/outbound, and no stock movement creation.
- Removed old hard-delete processing policies so Cancelled is the path instead of deletion.
- Kept compatibility for older `COMPLETED` processing batches so legacy records can still be reviewed.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `lib/processing/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230021_retail_processing_v1.sql`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed before and after final cleanup.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed after removing one unused helper warning.
- `npm.cmd run build` - first attempt was blocked by an existing Next build lock; retry passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed with retail worker, retail manager, admin, and director users to confirm outlet-scoped processing creation and review.
- Real mobile QA around 390px width is still needed to confirm the four raw-line/four finished-line entry form is comfortable for outlet staff.
- Future BOM settings can replace the free-text processing type/BOM field; V1 stores the entered name only.

## 2026-06-23 - Stock ERP Home Shortcut QA

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Updated ERP Home so stock users see a simple `Stock` shortcut to `/stock`, not a worker-facing `Stock Dashboard` shortcut.
- Kept the advanced `Stock Reports` shortcut role-restricted for managers/admin/director-style access.
- Updated Stock mobile UX source coverage and owner/Vercel QA docs to verify this at about 390px width.
- No migration, RLS policy, server action, stock movement rule, or production data was changed.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm ERP Home shows `Stock`, opens the six-button worker Stock home, and hides/blocks Stock Reports/Settings for worker access.

## 2026-06-23 - Stock damage photo-first scan UX

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved damage/spoilage request on `/stock/return` so workers choose a reason and add/take photo first, then camera scan the barcode to submit automatically.
- Added worker success feedback: `Damage request sent. Scan next.`
- Kept `Submit damage request` as the fallback for typed/manual barcodes.
- Kept damage server action, RLS, schema, validation, manager review, director approval, and no-immediate-deduction logic unchanged.
- Updated Stock mobile UX source coverage and owner/Vercel QA docs to verify photo-first scan behavior at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - failed in unrelated Processing code: `lib/processing/data.ts:71` assigns `batch.status` values such as `DRAFT`/`REJECTED` to `ProcessingBatchStatus`.
- `npm.cmd run build` - compiled, then failed during TypeScript on the same `lib/processing/data.ts:71` Processing status typing issue.

Risks / remaining checks:

- Fix the unrelated Processing type issue before treating the whole project as release-ready.
- Real authenticated 390px phone/browser QA is still needed to prove photo-first damage scanning works comfortably on `/stock/return`.
- Real Supabase/RLS QA is still needed to confirm damage requests do not deduct stock until manager review and director approval.

## 2026-06-23 - Stock return supplier supplier-first scan UX

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved return-supplier request on `/stock/return` so workers choose/tap supplier first, then camera scan the barcode to submit automatically.
- Added worker success feedback: `Supplier return requested. Scan next.`
- Kept `Submit return supplier` as the fallback for typed/manual barcodes.
- Kept return-supplier server action, RLS, schema, validation, supplier-hold behavior, manager approval, and stock deduction logic unchanged.
- Updated Stock mobile UX source coverage and owner/Vercel QA docs to verify supplier-first scan behavior at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove supplier-first scan works comfortably on `/stock/return`.
- Real Supabase/RLS QA is still needed to confirm stock goes to supplier hold and is deducted only after manager approval.

## 2026-06-23 - Retail Cleaning Checklist V1

Task completed:

- Continued the Retail Module V1 goal only.
- Added retail cleaning task master support for task name, outlet/team scope, frequency, active/inactive state, and audit fields.
- Reworked `/retail/cleaning` into a mobile-friendly today's cleaning checklist with one large `Complete` action per required active task.
- Workers can complete scoped outlet cleaning tasks with optional photo path and remarks.
- Retail manager/admin/director can manage cleaning task master records for their permitted outlet scope.
- Cleaning tables and reports now show required tasks with green completed status and red missing status, using daily/weekly/monthly/quarterly recurrence logic.
- Added a completion history table designed for future 30-day cleaning matrix reporting.
- Kept outlet/team scoping from the user profile; workers are not asked to select an outlet every time.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230020_retail_cleaning_checklist_v1.sql`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt was blocked by an existing Next build lock; retry passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed with retail worker, retail manager, admin, and director users to confirm outlet-scope visibility and completion permissions.
- Real mobile QA around 390px width is still needed to confirm the large cleaning completion cards are comfortable for outlet staff.
- Future report work can read `retail_cleaning_completions` directly for the advanced 30-day cleaning matrix.

## 2026-06-23 - Stock normal return scan-and-done UX

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved normal `/stock/return` so workers choose/confirm return location first, then a camera scan saves the return automatically and shows `Return saved. Scan next barcode.`
- Kept `Save return` as the fallback for typed/manual barcodes.
- Kept return server action, RLS, schema, validation, stock movement logic, customer-return inspection rules, and manager/admin inspection release unchanged.
- Updated Stock mobile UX source coverage and owner/Vercel QA docs to verify normal return scan-and-done behavior at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed with one existing Retail warning: `components/retail/retail-page.tsx` has unused `missingCleaningTasks`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove normal return scan-and-done works comfortably on `/stock/return`.
- Real Supabase/RLS QA is still needed to confirm eligible normal returns become `IN_STOCK` and customer returns still follow `HOLD`/`INSPECTION` rules.

## 2026-06-23 - Stock transfer receive scan-and-done UX

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/transfer` so after a destination stock location is selected, a camera scan submits the transfer automatically and then shows `Transfer sent. Scan next barcode.`
- Improved `/stock/receive-transfer` so after a receiving stock location is selected, a camera scan submits the receive automatically and then shows `Received. Scan next barcode.`
- Kept `Send transfer` and `Receive barcode` buttons as fallback for typed/manual barcodes.
- Kept transfer server actions, wrong-location receive-transfer blocking, RLS, schema, movement logic, validation, and overdue-transfer alert behavior unchanged.
- Updated Stock mobile UX source coverage and owner/Vercel QA docs to verify scan-and-done behavior at about 390px width.
- Fixed one unrelated Retail lint gate issue by escaping `Today's` as `Today&apos;s`; no Retail business behavior was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `components/retail/retail-forms.tsx` (lint gate text escape only)
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after final edits.
- `npm.cmd run lint` - passed with one existing Retail warning: `components/retail/retail-page.tsx` has unused `missingCleaningTasks`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove camera scan auto-submit works comfortably on `/stock/transfer` and `/stock/receive-transfer`.
- Real Supabase/RLS QA is still needed to confirm transfer status, receive location changes, and wrong-location blocking with real users and real stock units.

## 2026-06-23 - Stock take latest scan feedback

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a mobile `Previous stock take scan` feedback card to `/stock/stock-take`.
- The card shows the latest stock-take barcode, item name, weight, and worker-friendly state:
  - `Counted` for normal counted scans.
  - `Unknown barcode exception` for unknown-barcode exception lines.
  - `Wrong location exception` for wrong-location exception lines.
- Added `createdAt` to the existing StockTakeLine data contract and sorted stock-take lines newest-first so the feedback card uses the latest scanned line reliably.
- Kept stock-take server actions, approval rules, RLS, schema, movement logic, exception handling, manager review, and director approval unchanged.
- Updated Stock mobile UX source coverage and owner/Vercel QA docs to verify the latest stock-take scan card at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove the latest stock-take scan card is readable during active scanning.
- Real Supabase/RLS QA is still needed to confirm stock-take exceptions, manager review, director approval, and adjustment movement behavior with real users.

## 2026-06-23 - Retail Expenses V1

Task completed:

- Reworked Retail Expenses around the V1 workflow: worker submits, retail manager/admin/director reviews, no hard delete.
- Expense submission now captures date, assigned outlet, category, amount, payment method, supplier/payee, required receipt path, and remarks.
- Expense review is limited to Submitted -> Reviewed, Rejected, or Cancelled, with rejection reason required for Rejected.
- Expense tables now show receipt status, submitted by/at, reviewed by/at, and rejection reason.
- Cash-session expected cash now adds same-day daily cash sales and subtracts same-day cash expenses for the outlet; non-cash expenses remain report-only.
- Added configurable retail expense categories in Retail Settings for manager/admin/director use.
- Added Supabase migration for Expenses V1 columns, `CANCELLED` status, receipt/rejection checks, category master table/RLS, manager-only review helper, and removal of expense hard-delete policy.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `supabase/migrations/202606230019_retail_expenses_v1.sql`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Receipt handling is still a path/URL field in V1; full storage upload UX is not part of this task.
- Live Supabase QA should confirm old Approved/Paid expense rows migrate to Reviewed as expected and cash expense subtraction matches outlet cash-closing expectations.

## 2026-06-23 - Stock mobile QA prepared run

Task completed:

- Ran/prepared the requested Stock Module Mobile UX QA pass without changing app behavior.
- Confirmed the Stock source guards cover the requested 390px checklist: Stock home buttons, inbound session, continuous scanning UI, duplicate and no-weight warnings, label generation/reprint, outbound by order, direct outbound, damage/spoilage request, transfer, receive-transfer wrong-location block, stock-take progress, unknown-barcode exception, and online-only connection message.
- Updated owner-facing Stock QA evidence/checklist docs with the current pass/fail status and manual phone/printer steps.
- Did not run live Supabase SQL, migrations, seed, deployment, camera permission, Bluetooth printer, PDF output, or production data actions.
- Did not fix unrelated Retail type errors because this task was Stock-module-only.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - failed in unrelated Retail files:
  - `components/retail/retail-page.tsx` references missing `RetailExpense.vendor`, `approvedByName`, and `paidByName`.
  - `lib/retail/actions.ts` has narrowed expense-status comparisons for `APPROVED`/`PAID`.
- `npm.cmd run build` - compiled successfully, then failed during TypeScript on the same Retail `expense.vendor` error.

Risks / remaining checks:

- Fix the Retail type errors before treating the full project as release-ready.
- Real authenticated 390px phone/browser QA is still needed for Stock.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.
- Use `docs/STOCK_TEST_LIST_FOR_OWNER.md`, `docs/STOCK_REMOTE_QA_VERCEL.md`, and `docs/STOCK_QA_EVIDENCE.md` for the first owner testing pass.

## 2026-06-23 - Stock outbound latest scan feedback

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a `Previous outbound scan` feedback panel to `/stock/outbound` so workers can immediately see the latest scanned barcode, item name, and weight during order or direct outbound batch scanning.
- The feedback panel uses green success styling for found stock units and red blocked styling for unknown barcodes.
- Kept existing barcode batch state, outbound confirmation, duplicate/missing/blocked barcode validation, RLS, schema, and server-side actions unchanged.
- Updated Stock mobile UX source coverage and owner QA docs to check latest outbound scan feedback at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove the latest-scan feedback is readable during continuous outbound scanning.
- Live Supabase/RLS QA is still needed to confirm all barcode block states remain enforced server-side.

## 2026-06-23 - Retail Daily Sales Summary

Task completed:

- Converted Retail Daily Sales V1 from payment-type rows into one AutoCount daily sales summary per outlet/date.
- Added manager/admin/director-only entry fields for cash sales, bank transfer sales, e-wallet / DuitNow sales, credit sales, auto-calculated total sales, optional AutoCount attachment path, and remarks.
- Retail workers remain view-only for daily sales summaries.
- Daily sales tables now show the four payment buckets, calculated total, missing attachment warning badge, created by/at, and updated by/at.
- Server action recalculates total sales from the four buckets, upserts by outlet/date, writes created/updated user fields, and records audit log entries for create/update.
- Added a Supabase migration to add the new summary columns, consolidate older payment-code rows, enforce one summary per outlet/date, and check total equals the four component sales fields.

Files changed in this pass:

- `components/retail/retail-forms.tsx`
- `components/retail/retail-page.tsx`
- `components/ui/status-badge.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `supabase/migrations/202606230018_retail_daily_sales_summary_v1.sql`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Live Supabase migration QA should confirm older payment-code daily sales rows consolidate correctly into one outlet/date summary before production use.
- Attachment is stored as a path/URL in V1; full upload/storage handling is not built in this task.

## 2026-06-23 - Order Module V1 QA environment unblock attempt

Task completed:

- Investigated why documented QA users failed login during Order Module Acceptance QA.
- Confirmed `docs/SUPABASE_QA_PLAN.md` documented manual Auth-user creation, but `supabase/seed.sql` did not create those Supabase Auth users.
- Repaired the local/dev seed path so `supabase/seed.sql` now creates or repairs confirmed QA Auth users, matching `profiles`, exact intended `profile_roles`, outlet, department, stock-location, and module-access scope.
- Added stable seeded order outlet codes:
  - `JALAN CHANNEL` = `10`
  - `SUNGAI MERAH` = `11`
  - `WONDERFUL` = `12`
  - `SUNGAI MAAW` = `13`
  - `QA NO ORDERS` = `98`
  - `DIRECTOR` = `99`
- Added `qa.noorders@example.test` in the `QA NO ORDERS` outlet as the negative account for no-order-access tests.
- Updated `docs/SUPABASE_QA_PLAN.md` so the QA user matrix is now seeded for local/dev QA and so the Order checklist reflects immediate V1 reservation instead of the older picking-time reservation wording.
- Added `scripts/order-qa-env-coverage.mjs` and wired it into `npm run smoke` to guard the QA Auth seed, outlet order codes, no-orders account, and QA-plan documentation.
- Fixed a build-blocking Retail table mapping mismatch in `components/retail/retail-page.tsx`; this was only to keep the required project build gate passing.

QA users repaired for local/dev seed:

- All documented operational QA users from `docs/SUPABASE_QA_PLAN.md`.
- Global users: `qa.account.global@example.test`, `qa.admin@example.test`, `qa.director@example.test`.
- Negative no-orders user: `qa.noorders@example.test`.
- Local/dev password documented in the QA plan: `ChangeMe-QA-2026!`. Do not use this password in production.

Fresh migration / real RLS status:

- Docker Desktop remains a machine-level blocker. `com.docker.service` is stopped, `docker version` reports `Docker Desktop is unable to start`, and `npx.cmd supabase db reset --local --no-seed --yes` fails before migrations run.
- Attempting to start `com.docker.service` from this session failed with `Cannot open com.docker.service service on computer '.'`.
- Because local Supabase could not start, real seeded-user login, RLS, workflow, and browser/mobile QA could not be proven in this pass.

Files changed in this pass:

- `supabase/seed.sql`
- `docs/SUPABASE_QA_PLAN.md`
- `scripts/order-qa-env-coverage.mjs`
- `package.json`
- `components/retail/retail-page.tsx`
- `HANDOFF.md`

Commands run and results:

- `node scripts\order-qa-env-coverage.mjs` - passed.
- `Start-Service -Name com.docker.service` - failed: `Cannot open com.docker.service service on computer '.'`.
- `npx.cmd supabase status` - failed: `Docker Desktop is unable to start`.
- `docker version` - failed after printing client details: `Docker Desktop is unable to start`.
- `npx.cmd supabase db reset --local --no-seed --yes` - failed: `Docker Desktop is unable to start`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- First `npm.cmd run build` - failed on `components/retail/retail-page.tsx` because `RetailDailySale.paymentCode` no longer exists; fixed the row mapping.
- Final `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.
- Final `npm.cmd run typecheck` - passed.
- Final `npm.cmd run lint` - passed.
- `git diff --check` - passed with CRLF warnings only.

Risks / remaining checks:

- User must start/repair Docker Desktop or provide a reachable non-production Supabase environment where local/dev QA Auth seed can be applied.
- After Docker/Supabase is healthy, rerun `npx.cmd supabase db reset --local --yes` to prove every migration and seed from scratch.
- Then run real login/RLS checks for outlet isolation, admin/director scope, general worker order creation, no-order-access denial, picking scope, delivery handoff scope, and all requested order workflows.
- Do not mark Order Module V1 ready for UAT until the real seeded-user/RLS checks and fresh migration reset pass.

## 2026-06-23 - Stock direct damage photo picker

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added a phone-friendly image picker to direct outbound Damage/Spoilage so workers can take or choose a photo and auto-fill the existing required photo file name/reference field.
- Kept the existing direct outbound damage request action, required `damagePhotoPath`, approval-only flow, no-immediate-deduction rule, RLS, schema, and server-side validation unchanged.
- Updated Stock mobile UX source coverage and owner QA docs to check the direct Damage/Spoilage photo picker at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove camera/gallery selection fills the direct Damage/Spoilage photo file name/reference on `/stock/outbound`.
- This pass still stores the existing photo file name/reference only; Supabase Storage upload remains a separate future workflow.

## 2026-06-23 - Retail Home access control

Task completed:

- Tightened Retail Home and Retail V1 route controls so retail workers keep operational access while manager/admin/director controls stay gated.
- Retail workers can see today's scoped outlet records and can still submit expenses, complete cleaning, create processing records, and open shop cash.
- Daily sales summary creation, daily cash closing submission, cash-session closing, expense review, processing review, cleaning task master setup, and Settings remain manager/admin/director-only in the Retail UI.
- Added server-action role gates so workers cannot submit manager-only daily sales or cash closing actions directly.
- Added a Supabase RLS hardening migration to replace older broad daily-sales, daily-closing, and cash-session update policies with manager/admin/director scoped policies.
- Kept outlet selection implicit through the existing profile outlet/team scope; no repeated outlet picker was added for scoped users.

Files changed in this pass:

- `components/retail/retail-page.tsx`
- `lib/retail/actions.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `supabase/migrations/202606230017_retail_home_access_control_v1.sql`
- `HANDOFF.md`

Commands run and results:

- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed on retry. The first attempt reported another Next build process was already running; no Node process was active, and the immediate retry completed successfully.

Risks / remaining checks:

- Live Supabase/RLS QA is still needed with real retail worker, retail manager, admin, and director accounts.
- The worker view intentionally shows only today's scoped rows in the Retail UI; the underlying data loader still relies on existing profile/RLS outlet scope for non-admin users.

## 2026-06-23 - Stock outbound quick ready-order buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added large quick ready-order buttons to order-based outbound so workers can tap a ready order before scanning instead of relying only on a dropdown.
- Kept the existing order dropdown fallback, hidden form data, selected-order scan gate, substitution warning, barcode batch validation, RLS, schema, and server-side outbound actions unchanged.
- Updated Stock mobile UX source coverage and owner QA docs to check quick ready-order buttons at about 390px width.
- Added a tiny Retail daily-closing compile repair after typecheck exposed a missing local `approving` boolean in the existing dirty worktree; this was only to keep the full project gate passing and did not change Stock behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`
- `lib/retail/actions.ts` (project typecheck compatibility only; no Stock behavior change)

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after the Retail compile repair.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove quick ready-order buttons fit and fill the order selector on `/stock/outbound`.
- Live Supabase/RLS QA is still needed to confirm order-outbound remains scoped and server-side validation still blocks invalid barcode batches.

## 2026-06-23 - Stock transfer quick destination buttons

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Added large quick destination buttons to the shared transfer/receive-transfer stock-location selector so workers can tap a stock location before scanning instead of relying only on a dropdown.
- Kept the existing hidden form value, dropdown fallback, transfer action, receive-transfer action, wrong-location block, RLS, schema, and server-side validation unchanged.
- Updated Stock mobile UX source coverage and owner QA docs to check quick transfer/receive destination buttons at about 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after rerun by itself. The first parallel run raced with `npm.cmd run build` while Next regenerated `.next/types`.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to prove the quick destination buttons fit and fill the selector on `/stock/transfer` and `/stock/receive-transfer`.
- Live Supabase/RLS QA is still needed to confirm wrong-location receive-transfer remains blocked for scoped users.

## 2026-06-23 - Retail Module V1 daily control build

Task completed:

- Rebuilt the active Retail surface as Retail Daily Control V1 around AutoCount instead of ERP POS.
- `/retail` now opens the main daily-control buttons: Daily Sales, Expenses, Cleaning, Processing, Cash Closing, Reports, and manager/admin/director Settings.
- Daily Sales now records AutoCount daily payment summaries and shows daily summary records instead of item-by-item retail sales.
- Retail Reports now summarize daily sales, cash closing, expenses, cleaning alerts, and retail processing yield/loss.
- Retail Processing now records raw/finished quantity and weight, yield, and loss without consuming stock, creating stock movements, creating inbound/outbound records, or generating finished barcode labels.
- Retail Cleaning and Retail Processing now authorize through scoped Retail V1 access for outlet staff while keeping manager/admin/director-only setup/review controls.
- Old `/retail/pos`, `/retail/payments`, and `/retail/prices` routes redirect into V1 routes.
- Added source smoke coverage for Retail V1 buttons, settings gate, redirects, daily sales/closings, disabled POS sale action, scoped RLS migration, and no retail stock movement creation.

Files changed in this pass:

- `app/(erp)/retail/page.tsx`
- `app/(erp)/retail/dashboard/page.tsx`
- `app/(erp)/retail/pos/page.tsx`
- `app/(erp)/retail/payments/page.tsx`
- `app/(erp)/retail/prices/page.tsx`
- `app/(erp)/retail/reports/page.tsx`
- `app/(erp)/retail/settings/page.tsx`
- `components/erp/app-shell.tsx`
- `components/retail/retail-page.tsx`
- `components/retail/retail-forms.tsx`
- `lib/retail/actions.ts`
- `lib/retail/data.ts`
- `lib/retail/demo-data.ts`
- `lib/retail/types.ts`
- `scripts/retail-module-v1-coverage.mjs`
- `package.json`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230016_retail_daily_control_v1.sql`

Commands run and results:

- `npm.cmd run typecheck` - passed after the Retail page/data/action changes.
- `node scripts\retail-module-v1-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- Final `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Migration `202606230016` has not been applied to a real Supabase project by Codex.
- Real Supabase/RLS QA is needed for retail worker, retail manager, admin, and director users across at least two outlets.
- Real phone-width QA around 390px is still needed for the main buttons, daily sales, expenses, cleaning update, processing entry, cash closing, reports, and settings.
- Live workflow QA should verify AutoCount summary entry, same-day restrictions, different-checker daily closing/expense review, and retail processing records do not create stock movements.

## 2026-06-23 - Order Module V1 acceptance QA pass

Task completed:

- Performed an Order Module acceptance QA pass focused on bugs, route cleanup, requirement mismatches, and verification.
- Fixed duplicate operational routes:
  - `/orders/new` now redirects to `/orders/create`.
  - `/orders/prepare` now redirects to `/orders/picking`.
- Removed old `/orders/new` and `/orders/prepare` entries from the app-shell route access list so canonical routes drive navigation.
- Updated internal Processing and Stock/Orders revalidation references to use `/orders/picking`.
- Updated smoke coverage and docs to expect canonical `/orders/create` and `/orders/picking`.
- Added an app-level reservation expiry mechanism: loading Orders data now opportunistically calls the existing `expire_order_reservations_v1` RPC so order-capable staff/admin sessions release expired active reservations without waiting for a manual button.
- Kept the expiry call non-blocking for view-only roles or partially migrated environments; it does not prevent Orders pages from loading if the RPC is unavailable or unauthorized.

Acceptance QA findings:

- Real Supabase Auth/RLS workflow QA could not be completed because the documented QA users in `docs/SUPABASE_QA_PLAN.md` failed login against the configured Supabase project with `Invalid login credentials`.
- Fresh local migration reset could not be completed because Docker Desktop could not start: `failed to inspect service: Error response from daemon: Docker Desktop is unable to start`.
- Static migration dependency review found `can_access_customer_order` and `can_manage_customer_order_record` are defined in earlier `2026061000xx` migrations before the newer Order V1 migrations reference them.
- Source checks confirmed Order V1 code paths for total-order-price-only creation, Manual ERP source, order number RPC, immediate reservation, required manual pick reasons, mismatch logging, duplicate barcode warning, 10kg tolerance, Ready action, and Delivery Module handoff.

Files changed in this pass:

- `app/(erp)/orders/new/page.tsx`
- `app/(erp)/orders/prepare/page.tsx`
- `components/erp/app-shell.tsx`
- `components/processing/processing-page.tsx`
- `lib/orders/actions.ts`
- `lib/orders/data.ts`
- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `docs/MODULE_STATUS.md`
- `docs/workflow-acceptance-checklist.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npx.cmd supabase --version` - passed, version `2.106.0`.
- `npx.cmd supabase status` - failed because Docker Desktop could not start.
- `npx.cmd supabase db reset --local --no-seed --yes` - failed because Docker Desktop could not start.
- Read-only Supabase QA-user probe - reached Supabase after network approval, but documented QA users failed login with `Invalid login credentials`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- First `npm.cmd run build` - failed with Next.js parallel/stale build guard: `Another next build process is already running.`
- Re-run `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Create or repair the documented QA Auth users, then rerun real RLS/workflow QA for outlet isolation, broader admin/director scope, order-access creation, and no-order-access denial.
- Start Docker Desktop, then rerun `npx.cmd supabase db reset --local --no-seed --yes` to prove fresh migration order.
- Live browser/device QA is still needed for mobile order creation, picking scanner behavior, ready delivery handoff, pickup completion, and order detail usability.

## 2026-06-23 - Order Module V1 reports, alerts, and final QA follow-up

Task completed:

- Completed the Orders dashboard/reporting pass for V1.
- Kept the existing `/orders` dashboard filters for date, outlet, status, customer, and salesperson.
- Added report coverage for orders by customer, item, salesperson/staff, pending/ready/failed queue, status, and Delivery Module performance.
- Kept dashboard alerts for stock not enough, processing overdue, order ready, delivery failed, and credit overdue warning without showing customer outstanding amounts.
- Changed new order notification events to V1 in-app/dashboard notifications instead of WhatsApp placeholders.
- Normalized older skipped WhatsApp placeholder rows in the Orders UI as in-app shown events so staff do not see WhatsApp as an active V1 method.
- Updated Orders report and notification wording in the dashboard table.
- Updated `docs/MODULE_STATUS.md` for the newer Orders routes, immediate reservation behavior, reports/alerts, and remaining live QA needs.

Files changed in this pass:

- `lib/orders/data.ts`
- `lib/orders/actions.ts`
- `components/orders/orders-table-client.tsx`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Final QA status:

- Automated/source/build QA passed for the app routes and coverage checks available in this repository.
- Live workflow QA still needs owner or seeded Supabase execution for: create pickup order, create delivery order, create internal transfer order, quick customer creation, stock enough reservation, stock-not-enough order creation, barcode picking, manual-weight picking with reason, cancellation reservation release, ready delivery handoff to Delivery Module, pickup Picked Up flow, and team/outlet isolation.

Risks / remaining checks:

- Real Supabase/RLS QA is still required before treating Order V1 as production-verified.
- Delivery performance reports depend on linked canonical `deliveries`/`delivery_orders` rows from the Task 8 handoff.
- In-app notifications are stored in the existing order notification table and surfaced through the dashboard; no external push, WhatsApp, print, or export flow was added for V1.

## 2026-06-23 - Stock damage photo mobile picker

Task completed:

- Kept scope to the Stock module mobile UX goal.
- Attempted another browser verification route through the Node REPL, but the REPL kernel could not launch browser/test processes in this Windows sandbox and returned `CreateProcessAsUserW failed: 5`.
- Added a phone-friendly image picker to the Damage/Spoilage request form.
- Choosing or taking an image fills the existing required `Photo file name / reference` field so workers do not have to type the file name manually.
- Kept existing damage/spoilage barcode scan, photo-reference requirement, manager review, director approval, stock deduction timing, RLS, schema, and server validation unchanged.
- Updated Stock mobile QA docs and source coverage so the damage photo picker path is checked.
- Restored the existing Retail/Processing compatibility link to order picking after smoke caught the missing source marker; this was only to keep the project gate passing and did not change Stock behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/retail/retail-page.tsx` (project smoke compatibility only; no Stock behavior change)
- `lib/retail/actions.ts` (project check compatibility only; no Stock behavior change)
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA, phone camera permission, actual image selection/capture, and live Supabase/RLS approval QA still need owner/device evidence.
- This pass does not add Supabase Storage upload for damage photos; the current action still stores the required photo file name/reference.
- Bluetooth label printing and PDF label fallback still need real phone/printer/browser evidence.

## 2026-06-23 - Stock return location scanner gate

Task completed:

- Kept scope to the Stock module mobile UX goal.
- Attempted local 390px/browser verification setup. `agent-browser` was not available on PATH, and hidden/background Next dev server starts did not stay reachable from this tool session. The foreground dev command did start successfully, but the command timeout stopped it before browser automation could run.
- Tightened `/stock/return` so the barcode scanner and manual barcode entry stay disabled until a return location is selected.
- Added a short worker-facing setup message: `Choose return location first.`
- Kept existing return server action, stock movement logic, RLS, schema, and migrations unchanged.
- Updated Stock mobile QA docs and source coverage so the return-location scan gate is checked.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA, phone camera permission, and Bluetooth/PDF label printer checks still need owner/device evidence.
- Verify from Vercel that `/stock/return` blocks scanning if no return location is selected and allows scanning after location is selected.

## 2026-06-23 - Stock return supplier recent supplier buttons

Task completed:

- Kept scope to the Stock module mobile UX goal.
- Added one-tap `Recent suppliers` buttons to the Return Supplier request form when previous supplier-return requests exist.
- Buttons only prefill the existing supplier name field to reduce worker typing on mobile.
- Kept existing barcode scan, supplier-name requirement, hold status, manager approval, stock deduction timing, RLS, schema, and server validation unchanged.
- Updated Stock mobile QA docs and source coverage so the recent supplier path is checked.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still needed to confirm the recent supplier buttons fit and fill the supplier field on a phone.
- Live Supabase/RLS QA is still needed to confirm supplier-return requests remain approval-gated before stock deduction.

## 2026-06-23 - Stock take active-session scanner gate

Task completed:

- Kept scope to the Stock module mobile UX goal.
- Tightened `/stock/stock-take` so the barcode scanner and manual barcode entry stay disabled until an active `DRAFT` stock-take session is selected.
- Added a short worker-facing setup message: `Select active stock take session first.`
- Kept existing stock-take server actions, wrong item/brand blocking, unknown barcode exception handling, review/approval rules, RLS, schema, and migrations unchanged.
- Updated Stock mobile QA docs and source coverage so the active-session scan gate is checked.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA, phone camera permission, and Bluetooth/PDF label printer checks still need owner/device evidence.
- Verify from Vercel that `/stock/stock-take` blocks scanning until an active draft session is selected, then keeps count/weight progress readable after scans.

## 2026-06-23 - Stock mobile scan setup gates for outbound and transfer

Task completed:

- Kept scope to the Stock module mobile UX goal.
- Tightened `/stock/outbound` so Order outbound scanner/manual entry stays disabled until a ready order is selected.
- Tightened direct Transfer outbound so scanner/manual entry stays disabled until a destination stock location is selected.
- Tightened `/stock/transfer` so scanner/manual entry stays disabled until a destination stock location is selected.
- Tightened `/stock/receive-transfer` so scanner/manual entry stays disabled until a receiving stock location is selected.
- Kept existing server-side validation, stock movement logic, RLS, schema, and migrations unchanged.
- Updated Stock mobile QA docs and source coverage so these setup-required scan gates are checked.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- First `npm.cmd run build` - failed with Next.js stale/parallel build guard: `Another next build process is already running.`
- Re-run `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA, phone camera permission, and Bluetooth/PDF label printer checks still need owner/device evidence.
- Verify from Vercel that outbound, transfer, and receive-transfer scan controls become available only after the required order/location choice is complete.

## 2026-06-23 - Order Module V1 delivery handoff follow-up

Task completed:

- Connected the active Order Module ready-delivery handoff to the existing canonical Delivery Module flow.
- Kept the existing `create_delivery_from_customer_order` RPC as the handoff source so ready delivery orders create/reuse `deliveries` rows with `AVAILABLE` status for `/delivery/driver`.
- Added Orders page revalidation from Delivery actions so `/orders`, `/orders/ready`, `/orders/picking`, and linked order detail pages refresh after driver status changes.
- Synced canonical driver lifecycle changes back to linked order records where the Order Module supports the status: `OUT_FOR_DELIVERY`, `DELIVERED`, and `FAILED`.
- Kept accept/load as Delivery-only states while leaving the linked customer order in its ready-for-delivery state, preserving the existing order status contract.
- When delivered proof includes GPS, the Delivery Module now applies proof/GPS to linked customer orders through the existing `complete_customer_order_delivery_with_proof` RPC.
- Customer remarks are now clearly labelled on the mobile driver delivery card. Drivers still cannot edit order items or weights.
- No WhatsApp notifications, partial delivery, or multi-day delivery behavior was added.

Files changed in this pass:

- `lib/delivery/actions.ts`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA should verify a ready delivery order appears in `/delivery/driver`, can be accepted/loaded/started/completed, and updates the linked `/orders/[id]` status.
- Live proof QA should verify the delivery proof photo upload stores proof records and updates linked customer GPS only when driver GPS is available.
- Existing older `delivery_jobs` compatibility remains untouched; the active driver page uses canonical `deliveries`.

## 2026-06-23 - Stock mobile inbound setup gate QA prep

Task completed:

- Kept scope to the Stock module mobile UX goal.
- Tightened `/stock/inbound` so the barcode scanner and manual barcode field stay disabled until product, brand, origin, and location are chosen.
- Added a short worker-facing setup message: `Choose product, brand, origin, and location first.`
- Kept existing server-side/business validation unchanged; this is a UI safety gate that matches the existing inbound requirements.
- Updated Stock mobile QA docs so the owner can verify the setup gate from Vercel or a local app at about 390px width.
- Updated Stock mobile UX coverage so the setup gate is source-guarded.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA, phone camera permission, and Bluetooth/PDF label printer checks still need owner/device evidence.
- Verify from Vercel that `/stock/inbound` blocks scanning before setup and allows scanning immediately after the setup is complete.

## 2026-06-23 - Order Module V1 detail, cancel, ready, pickup follow-up

Task completed:

- Built richer `/orders/[id]` order detail sections inside the existing Orders page shell.
- Detail now shows order number, customer, order type, required date/time, fulfillment-specific pickup/delivery/internal-transfer details, total price, outlet, creator, reservation expiry, remarks, and customer remarks.
- Added detail alerts for stock-not-enough, processing required/overdue, customer remarks, cancellation, and failed delivery follow-up.
- Added item cards with estimated weight, picked weight, remaining weight, item remarks, processing flags, and stock status.
- Added status timeline from order creation, stock reservation records, picking logs, notification placeholders, delivery handoff records, pickup completion, and cancellation context.
- Kept edit/picking/ready/cancel/pickup actions on the detail page context-aware: edit remains disabled after picking starts, picking only appears for open picking orders, Ready only appears before ready handoff, and cancel/pickup use existing server actions.
- Rebuilt `/orders/ready` as a ready-orders board with separate pickup and delivery lists, plus internal-transfer visibility.
- Ready delivery cards show linked delivery handoff details when available and link into the Delivery module.
- Pickup orders remain completable by staff through the existing Picked Up action with no photo/signature requirement.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed for detail visibility and ready-page lists across normal staff, manager, admin, and director scopes.
- Real workflow QA should verify cancel releases active reservations in the live database and that pickup completion displays as `PICKED_UP`.
- Delivery handoff display depends on Task 8's Delivery Module integration for complete driver assignment/status behavior.

## 2026-06-23 - Order Module V1 picking flow follow-up

Task completed:

- Built the `/orders/picking` workstation flow for selecting an open order, scanning barcodes, entering manual picked weight, reviewing item progress, and marking orders Ready.
- Added mobile-friendly picking KPIs for estimated weight, picked weight, remaining weight, and the 10kg tolerance.
- Added per-item picking cards showing estimated kg, picked kg, remaining kg, stock-not-enough flags, processing flags, item remarks, and tolerance warnings.
- Added manual weight entry with required predefined reason and optional notes.
- Added recent picking log display for barcode scans, manual entries, and mismatch records.
- Barcode scan input now refocuses after every scan attempt and clears after a successful pick for continuous scanner use.
- Tightened barcode duplicate handling so any previous barcode entry on the same order is treated as a duplicate warning.
- Linked successful barcode scans back to active `order_stock_reservations` by assigning the exact `stock_unit`/barcode during picking while keeping reservation weight-first before picking.
- Kept order edit-lock behavior through existing status transitions: picking updates the order to `PREPARING`, and existing edit actions block once picking has started.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `components/orders/orders-page.tsx`
- `lib/orders/actions.ts`
- `HANDOFF.md`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- No new migration was needed in this pass; it relies on Order Task 5 reservation columns and picking entry tables.
- Real scanner QA should verify keyboard-wedge barcode devices keep focus on the scan field after each submit in the target browser/device.
- Real Supabase QA should verify RLS permits pickers to insert picking logs and assign active reservations only within their allowed team/outlet scope.

## 2026-06-23 - Delivery Task 10 future truck GPS preparation

Task completed:

- Prepared future truck GPS provider integration without building a live map dashboard.
- Added delivery GPS provider schema for provider name, API base/reference, active flag, 10-second sync interval, last sync timestamp, and provider metadata.
- Extended `vehicles` with optional GPS provider reference fields: provider id, provider vehicle ref, enabled flag, and metadata.
- Extended `truck_gps_snapshots` with provider id/snapshot ref, outlet/team scope, heading, odometer, engine state, battery percent, raw payload, ETA/delay, fuel, sync timestamp, and 3-day expiry indexing.
- Added `vehicle_current_locations` as the latest-location table for future manager map/customer tracking work.
- Added RLS for provider/current-location/snapshot reads and writes. Non-manager tracking reads are restricted to linked `OUT_FOR_DELIVERY` deliveries; manager/admin/director follow delivery scope patterns.
- Added service stubs in `lib/delivery/truck-gps.ts`:
  - `syncTruckGpsSnapshot()`
  - `getVehicleCurrentLocation()`
  - `estimateDeliveryDelay()`
  - `getTruckGpsTrail()`
- The stubs do not call a real provider API and do not implement a live map. They are safe placeholders backed by the prepared schema.
- Existing delivery vehicle queries now expose optional GPS provider metadata.

Files changed in this pass:

- `lib/delivery/types.ts`
- `lib/delivery/queries.ts`
- `lib/delivery/truck-gps.ts`
- `supabase/migrations/202606230014_truck_gps_future_prep_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230014_truck_gps_future_prep_v1.sql`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `node scripts/smoke-routes.mjs` - passed.

Risks / remaining checks:

- Migration `202606230014` has not been applied to a real Supabase project by Codex.
- Real provider API credentials, authentication, webhook/polling job, 10-second scheduler, cleanup job for expired trails, and live map/dashboard UI remain future scope.
- Supabase QA should verify RLS for driver/customer tracking only during `OUT_FOR_DELIVERY`, manager scoped review, admin provider writes, and 3-day trail reads.

## 2026-06-23 - Order Module V1 stock reservation logic follow-up

Task completed:

- Implemented reservation-aware available stock for Orders V1: available stock now equals physical `stock_units` weight minus active, unexpired `order_stock_reservations`.
- Kept reservation immediate on order creation and weight-first by item/location, without assigning exact barcodes.
- Refactored order creation to insert order lines first, then reserve each line through shared reservation logic.
- Stock-short orders still save successfully and mark `stock_not_enough` on the order, lines, and reservation rows.
- Added server-side helpers/actions for reserving missing order stock, releasing reservations, expiring end-of-day reservations, and recalculating order stock status.
- Updated cancellation to use the shared release helper in addition to the existing database trigger release safety net.
- Added forward-only migration `202606230026_order_reservation_logic_v1.sql` with database helper functions for available stock, expiry, release, and recalculation.
- Removed the old admin/director delete policy on `customer_orders` so app users cancel orders instead of deleting them.
- Reused existing Stock Module tables: `stock_units`, `customer_order_items`, and `order_stock_reservations`.

Files changed in this pass:

- `lib/orders/actions.ts`
- `supabase/migrations/202606230026_order_reservation_logic_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230026_order_reservation_logic_v1.sql`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `node scripts/smoke-routes.mjs` - passed.

Risks / remaining checks:

- Migration `202606230026` has not been applied to a real Supabase project by Codex.
- Real Supabase QA is still needed for reservation math under RLS with multiple simultaneous orders for the same item/location.
- Real operations QA should verify end-of-day expiry timing in the Malaysia timezone and confirm whether expired reservations should be re-reserved automatically or manually recalculated the next day.

## 2026-06-23 - Delivery Task 9 expenses submission and review

Task completed:

- Completed the Delivery Expenses V1 workflow for driver submission and manager/admin review.
- Driver `/delivery/driver` Expenses tab now shows submitted expenses with Pending/Approved/Rejected status, amount, vehicle, receipt link, review note, and rejection reason.
- Receipt upload remains camera/image-only and is stored in the `delivery-expenses` Supabase Storage bucket through server actions.
- Added `/delivery/expenses` manager review page with filters for date, driver, vehicle, expense type, and status.
- Manager/admin review supports approving pending expenses and rejecting pending expenses with a required rejection reason.
- Review writes `reviewed_by`, `reviewed_at`, `review_note`, `rejected_reason`, and audit log entries.
- Delivery dashboard now links to Expense Review.
- Delivery detail expense cards now show signed receipt-photo links plus review/rejection notes.
- Expense query layer now signs receipt URLs, maps driver/vehicle names, and respects delivery manager outlet/team scope while allowing admin/director global view.
- Added forward-only migration for expense `outlet_id`, review notes, rejection reason, extra indexes, and updated expense RLS policies using outlet/team scope.
- Made a minimal compile fix in `components/stock/workflow-forms.tsx` where `stockDamageReasons` was imported type-only but used as a runtime value.

Files changed in this pass:

- `app/(erp)/delivery/expenses/page.tsx`
- `components/delivery/delivery-expense-review-page.tsx`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `components/delivery/delivery-detail-page.tsx`
- `components/delivery/manager-delivery-dashboard.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `components/stock/workflow-forms.tsx`
- `supabase/migrations/202606230013_delivery_expense_review_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230013_delivery_expense_review_v1.sql`

Commands run and results:

- `npm.cmd run typecheck` - passed after the unrelated stock import fix.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed; build output includes `/delivery/expenses`.
- `node scripts/smoke-routes.mjs` - passed.

Risks / remaining checks:

- Migration `202606230013` has not been applied to a real Supabase project by Codex.
- Real Supabase QA is still needed for manager expense review against outlet/team RLS and signed Storage receipt access.
- Real phone QA is still needed for driver receipt capture/upload and expense history at phone width.

## 2026-06-23 - Order Module V1 create order flow follow-up

Task completed:

- Finished the `/orders/create` flow in the existing Order Module path.
- Kept order creation available to order-access staff roles, including general workers through the existing Orders module gate and server-action role list.
- Kept Manual ERP orders immediately confirmed on save using the existing canonical `customer_orders.status = NEW` plus V1 display/status mapping to `CONFIRMED`.
- Kept source as Manual ERP and removed delivery-job creation from the create step; delivery handoff remains after Ready.
- Preserved immediate stock reservation by creating `order_stock_reservations` rows for every order line during order creation.
- Added server validation that every order line has estimated kg so reservation is weight-first.
- Added server validation that carton, packet, and quantity orders include quantity plus estimated kg.
- Added server validation for pickup location fallback, internal transfer from/to locations, and different transfer source/destination.
- Kept stock-short orders creatable, with `stock_not_enough` warning state on the order, line, and reservation.
- Improved the create form with recent item buttons, item search, category filters, unit-aware quantity labels, required estimated kg, and clearer pickup/delivery/transfer date-time labels.
- Kept total order price as the only price entry visible to normal workers.
- Fixed a Delivery expense data/demo type mismatch needed to restore project typecheck without changing delivery workflow behavior.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `lib/orders/actions.ts`
- `lib/delivery/data.ts`
- `lib/delivery/demo-data.ts`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `node scripts/smoke-routes.mjs` - passed.

Risks / remaining checks:

- Real Supabase QA is still needed for RLS-scoped order creation by general workers, especially stock reservation inserts.
- Real stock QA is still needed to compare available stock calculations against current reservation rows, because V1 currently checks stock-unit availability directly.
- Phone-width QA is still needed for the recent item buttons and multi-line item entry form around 390px.

## 2026-06-23 - Order Module V1 customer selection and quick customer create follow-up

Task completed:

- Kept staff order creation on the customer master list, with search by customer name and phone.
- Kept quick customer creation inside `/orders/create`, requiring only name and phone when no existing customer is selected.
- Added a clearer empty-search state so staff know to quick add when no customer matches.
- Made delivery address a controlled order field and required it for DELIVERY orders even when the selected customer has no saved address.
- Added server-side validation so DELIVERY orders cannot be created without a delivery address.
- Removed Order Module V1 GPS capture from the order creation form; customer GPS remains owned by the Delivery Module.
- Kept customer overdue credit as a warning only and did not expose outstanding amount.
- Stored quick-add customer remarks and active status from the customer form.
- Synced selected customer remarks into the order remarks field so pickers can see them on the order.
- Passed customer remarks into linked delivery notes for driver visibility during delivery handoff.
- Expanded order server-action access to match the Orders navigation/module access pattern for retail, delivery, processing, account, and admin roles.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `lib/orders/actions.ts`
- `supabase/migrations/202606230010_order_delivery_integration_v1.sql`
- `HANDOFF.md`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase QA is still needed after applying the Order Module V1 migrations, especially for quick customer creation under outlet/team RLS.
- Real phone-width QA is still needed for the customer search, quick-add, and delivery-address-required flow.

## 2026-06-23 - Delivery Task 8 address issue and GPS suggestion approval

Task completed:

- Extended the driver Address Issue workflow on `/delivery/driver` and `/delivery/[id]`.
- Driver Address Issue now attempts current phone GPS during submit, allows a better address, allows an optional short note, allows an optional photo, and saves only a pending suggestion.
- Added `Save Current Location as Suggested Customer GPS` button to driver delivery views.
- Drivers still cannot directly update customer master records.
- Proof GPS continues to create a pending suggestion only and does not overwrite official customer GPS.
- Added signed URL loading and photo previews for address/GPS suggestion photos in manager dashboard/detail views.
- Manager/admin approval now approves or rejects pending suggestions.
- On approval, official `customers.address`, `customers.latitude`, and `customers.longitude` update only when suggested values exist.
- On reject, official customer master stays untouched.
- Approval records `reviewed_by`, `reviewed_at`, and an audit log.
- Added forward-only migration for optional address suggestion photo metadata.

Files changed in this pass:

- `components/delivery/delivery-detail-page.tsx`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `components/delivery/manager-delivery-dashboard.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `supabase/migrations/202606230012_delivery_address_suggestion_photo_approval_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230012_delivery_address_suggestion_photo_approval_v1.sql`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `node scripts/smoke-routes.mjs` - passed.

Risks / remaining checks:

- Migration `202606230012` has not been applied to a real Supabase project by Codex.
- Real Supabase QA is still needed for manager/admin approval against customer RLS and outlet/team scope.
- Real phone QA is still needed for Address Issue GPS allowed/denied, optional photo upload, and long-button wrapping at phone width.

## 2026-06-23 - Order Module V1 navigation and dashboard follow-up

Task completed:

- Confirmed Order routes are present for `/orders`, `/orders/create`, `/orders/[id]`, `/orders/picking`, `/orders/ready`, and `/orders/customers`.
- Kept Orders in the ERP sidebar and aligned the Orders page access gate with the sidebar role/module access pattern, including delivery, retail, processing, account, admin, and director view access where applicable.
- Updated the mobile/home shortcut so `Orders` opens the Orders dashboard, with a separate `Create Order` shortcut for operators.
- Updated the `/orders` KPI cards to match the V1 dashboard contract: Today Orders, Pending/Confirmed Orders, Stock Not Enough, Picking, Ready, Out for Delivery, Failed, and Completed/Picked Up.
- Updated the status filter to use the V1 order status names, including `STOCK_NOT_ENOUGH`, `PICKING`, `READY`, and `PICKED_UP`.
- Updated the order dashboard table/list to include Order No, Customer, Order Type, Required Date/Time, Outlet, Total Estimated Weight, Total Price, Stock Status, Order Status, Created By, and Actions.
- Added mobile card rendering for the order list with badges and large action buttons.
- Kept dashboard alerts for stock not enough, processing overdue, order ready, delivery failed, and credit overdue warning without showing any customer outstanding amount.
- Did not add print/export to Orders V1.

Files changed in this pass:

- `components/dashboard/home-page.tsx`
- `components/orders/orders-page.tsx`
- `components/orders/orders-table-client.tsx`
- `lib/orders/data.ts`
- `HANDOFF.md`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated phone-width QA is still needed for the `/orders` dashboard cards and filters.
- Supabase role/module QA should verify Orders route visibility for retail, delivery, processing, account, admin, and director profiles.

## 2026-06-23 - Delivery Task 7 detail page

Task completed:

- Built `/delivery/[id]` as the canonical Delivery detail page.
- Added reusable `components/delivery/delivery-detail-page.tsx`.
- Extended canonical delivery detail data to include linked orders, delivery items, proof photos with short-lived signed Storage URLs, address/GPS suggestions, expenses, and status timeline.
- Added active delivery driver lookup for manager reassignment, scoped to delivery-role profiles.
- Added manager/admin server actions to change delivery driver, change vehicle, cancel delivery, and approve/reject address/GPS suggestions.
- Driver detail view is mobile-first and action-based:
  - Accept Delivery
  - Loaded
  - Start Delivery
  - Upload Delivered Proof
  - Upload Failed Proof
  - Address Issue
  - Google Maps / Call / WhatsApp
- Manager/admin view shows assignment controls, cancel action, address suggestion review, expenses, failed reason, status logs, and proof photos.
- Updated manager dashboard Review links to open `/delivery/[id]`.
- Driver-facing detail avoids price, stock cost/value, customer credit, profit, and accounting data. Expense amounts are shown only to manager/admin users on this detail page.
- Route access uses the existing delivery module guard, Supabase RLS, and canonical `getDeliveryById`; unavailable/out-of-scope deliveries show a safe unavailable state.

Files changed in this pass:

- `app/(erp)/delivery/[id]/page.tsx`
- `components/delivery/delivery-detail-page.tsx`
- `components/delivery/manager-delivery-dashboard.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed; build output includes `/delivery/[id]`.
- `node scripts/smoke-routes.mjs` - passed.

Risks / remaining checks:

- Real Supabase QA is still needed for driver RLS on available vs own deliveries, manager outlet/team scope, and admin/director visibility.
- Real phone QA is still needed for camera capture, GPS allowed/denied, watermark readability, signed proof photo display, and action button ergonomics.
- Address/GPS suggestion approval currently records approval/rejection only; it does not overwrite official customer GPS, as required by Delivery V1.

## 2026-06-23 - Order Module V1 database layer follow-up

Task completed:

- Added forward-only migration `supabase/migrations/202606230011_order_module_v1_database_layer.sql`.
- Kept the existing canonical order tables (`customer_orders`, `customer_order_items`, `order_stock_reservations`, `order_picking_entries`, `order_notification_events`) and added compatibility views named `orders`, `order_lines`, `order_reservations`, `order_pick_logs`, and `order_notifications`.
- Added canonical V1 order status support through `customer_orders.order_v1_status` with the requested statuses: `CONFIRMED`, `STOCK_NOT_ENOUGH`, `PICKING`, `READY`, `OUT_FOR_DELIVERY`, `PICKED_UP`, `DELIVERED`, `FAILED`, and `CANCELLED`.
- Added `order_status_logs` with scoped RLS and automatic insert/update status logging.
- Added database-side reservation release when an order becomes `CANCELLED`.
- Added end-of-day reservation expiry defaults and optional barcode/stock-unit assignment fields for later picking.
- Expanded order-create/manage eligibility to order-access staff roles including retail, delivery, processing, account, and admin while preserving director view-only behavior.
- Added indexes for order date, outlet, V1 status, customer, salesperson, order number, reservation expiry, reservation assignment, and status-log lookups.
- Extended `customers` with `remarks` and updated seed customers/orders/items/reservations to populate V1 fields.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Migration `202606230011` has not been applied to a real Supabase database by Codex.
- Supabase migration QA should verify RLS behavior for retail, delivery, processing, account, admin, and director profiles.
- The compatibility views are read-only by design; existing write paths continue using the established canonical tables and server actions.
- Legacy seed order numbers remain `ORD-SEED-*` for existing demo references; production order creation still uses `next_customer_order_no_v1` for `ORD-YYYYMMDD[OutletCode][RunningNo]`.

## 2026-06-23 - Order Module V1

Task completed:

- Built Order Module V1 on the existing Next.js/Supabase ERP structure.
- Added `/orders/create`, `/orders/picking`, `/orders/ready`, and `/orders/customers`; kept `/orders/new` and `/orders/prepare` as legacy-compatible entry points.
- Added dashboard filters for date, outlet, status, customer, and salesperson.
- Added order KPIs, alerts, and reports for customer/item/staff/status, stock not enough, processing overdue, ready orders, delivery failed, and credit warnings.
- Added mobile/home shortcut label `Orders` and expanded sidebar order navigation.
- Added customer search and quick-add customer flow with name + phone.
- Added Manual ERP confirmed-order creation with item lines, total order price, customer/staff remarks, delivery/internal-transfer fields, and category-filtered item selection.
- Added immediate stock reservation at order creation by item estimated weight, with stock-not-enough warnings and end-of-day reservation expiry.
- Added picking by barcode scan or manual weight, manual reason enforcement, duplicate barcode warning, and wrong-item mismatch recording.
- Added 10kg ready tolerance, ready pickup/delivery handoff, pickup completion, and cancellation that releases active reservations.
- Updated Delivery handoff trigger so delivery jobs are created only when delivery/internal-transfer orders become `READY_FOR_DELIVERY`.
- Updated Orders smoke-route coverage from the previous picking-time reservation behavior to Order Module V1.

Files changed in this pass:

- `app/(erp)/orders/page.tsx`
- `app/(erp)/orders/new/page.tsx`
- `app/(erp)/orders/prepare/page.tsx`
- `app/(erp)/orders/create/page.tsx`
- `app/(erp)/orders/picking/page.tsx`
- `app/(erp)/orders/ready/page.tsx`
- `app/(erp)/orders/customers/page.tsx`
- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `components/orders/orders-table-client.tsx`
- `components/erp/app-shell.tsx`
- `components/dashboard/home-page.tsx`
- `lib/orders/actions.ts`
- `lib/orders/data.ts`
- `lib/orders/types.ts`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `supabase/migrations/202606230008_order_module_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230008_order_module_v1.sql`

Exact migration order:

- Run all existing migrations in filename order through `supabase/migrations/202606230007_delivery_database_storage_rls_v1.sql`.
- Then run `supabase/migrations/202606230008_order_module_v1.sql`.
- Run `supabase/seed.sql` only for safe demo/staging data.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - Orders smoke-route section passed, then the full smoke chain failed in existing Stock coverage: `Acceptance 2 duplicate inbound missing: Remove it before saving.` This appears outside the Order Module V1 change.

Risks / remaining checks:

- Migration `202606230008` has not been applied to a real Supabase database by Codex.
- Real role/module/outlet QA is still needed for retail, delivery, processing, account, admin, and director users.
- Real stock availability and reservation behavior must be tested against staged stock units and active reservations.
- Barcode picking currently records selected barcodes and picked weights for the order; it does not deduct stock or change stock-unit status. Existing outbound/delivery stock workflows still own physical stock deduction.
- Order creation uses server actions with multiple Supabase writes; if a mid-action write fails, staging QA should verify whether partial records need cleanup before production use.
- Real phone-width QA around 390px is still needed for create, picking, ready, and customer screens.

## 2026-06-23 - Delivery Task 6 order integration

Task completed:

- Added the database integration function `create_delivery_from_customer_order` to create a canonical `deliveries` job from a customer order or return the existing linked delivery when one already exists.
- Auto-creates a Delivery job when a new order is created with fulfillment type `DELIVERY` or `INTERNAL_TRANSFER`.
- Reuses the same create-or-return-existing path when an order is marked ready for delivery, so the ready action does not create duplicate Delivery jobs.
- Added a manual `Create Delivery` order action for delivery/internal-transfer orders that have no linked delivery yet.
- Pickup orders are explicitly kept inside Orders and are blocked from Delivery job creation.
- Added linked Delivery visibility to the Orders data loader and order detail page:
  - delivery no
  - delivery status
  - driver
  - proof status/count
  - Delivery dashboard link
- Order detail hides duplicate creation once a linked non-cancelled delivery exists.
- Tightened routine order action/page access so delivery worker roles do not get general Orders access where price/order details are shown, and pending SQL no longer re-opens routine order management to delivery/account roles.
- Delivery completion and failed proof status updates continue to flow through the Delivery service layer, which updates linked customer orders to `DELIVERED` or `FAILED` and does not deduct stock.
- Fixed the narrow existing Orders typing issue for order status/manual pick reason mapping that previously blocked typecheck/build.

Files changed in this pass:

- `components/orders/orders-page.tsx`
- `components/orders/orders-forms.tsx`
- `lib/orders/actions.ts`
- `lib/orders/data.ts`
- `lib/orders/types.ts`
- `lib/delivery/actions.ts`
- `supabase/migrations/202606230008_order_module_v1.sql`
- `supabase/migrations/202606230010_order_delivery_integration_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230010_order_delivery_integration_v1.sql`

Exact migration order:

- `202606230007_delivery_database_storage_rls_v1.sql`
- `202606230008_order_module_v1.sql`
- `202606230009_delivery_proof_uploaded_at_v1.sql`
- `202606230010_order_delivery_integration_v1.sql`

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run smoke` - Orders smoke checks passed, then the broader suite failed later on unrelated Stock acceptance coverage: `Acceptance 2 duplicate inbound missing: Remove it before saving.`

Risks / remaining checks:

- Migration `202606230010` has not been applied to a real Supabase project by Codex.
- Real Supabase QA is still needed for delivery/internal-transfer order creation, manual duplicate prevention, ready action reuse, driver proof completion, failed proof completion, and role/outlet/team isolation.
- Stock deduction remains intentionally outside delivery completion and must continue to be validated through the Stock outbound/loading flow.

## 2026-06-23 - Delivery Task 5 manager delivery dashboard

Task completed:

- Rebuilt `/delivery` and `/delivery/dashboard` as a canonical manager Delivery dashboard.
- Added manager/admin/director-only route access using the existing module guard; normal drivers should use `/delivery/driver`.
- Added simple KPI cards for:
  - Today Deliveries
  - Pending / Available
  - Loaded
  - Out for Delivery
  - Delivered Today
  - Failed Today
  - Overdue
  - Total Weight Today
  - Driver Performance
  - Delivery Weight by Driver
- Added GET filters for date, driver, status, customer, outlet, and team.
- Added dashboard delivery list showing delivery no, customer, address, driver, status, total weight, item count, requested date, and a review action.
- Added manager review sections for failed deliveries, GPS unavailable, address/GPS suggestions, late deliveries, and driver took too long.
- Kept V1 scope explicit: no dispatch board was added.
- Extended canonical Delivery dashboard query data with review queues, pending address/GPS suggestions, loaded/out-for-delivery driver metrics, and slow-delivery detection.
- Dashboard access and data scope rely on existing auth/module checks plus Supabase RLS from Delivery Task 1:
  - delivery manager sees own outlet/team through RLS
  - admin/director follow the existing global ERP permission pattern
  - driver role is not allowed into this manager dashboard route

Files changed in this pass:

- `app/(erp)/delivery/page.tsx`
- `app/(erp)/delivery/dashboard/page.tsx`
- `components/delivery/manager-delivery-dashboard.tsx`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed, with one pre-existing/unrelated warning in `lib/orders/data.ts`: `closedStatuses` is assigned a value but never used.
- `npm.cmd run typecheck` - failed on unrelated existing dirty Orders files:
  - `lib/orders/data.ts(216,5): Type 'string' is not assignable to type CustomerOrderStatus.`
  - `lib/orders/data.ts(217,34): Argument of type 'string' is not assignable to parameter of type CustomerOrderStatus.`
  - `lib/orders/data.ts(354,34): manualReason may be null but is passed where ManualPickReason is required.`
- `npm.cmd run build` - failed during TypeScript on the same unrelated `lib/orders/data.ts(216,5)` error.
- Existing dev server returned HTTP 200 for `/delivery`.
- Existing dev server returned HTTP 200 for `/delivery/dashboard`.

Risks / remaining checks:

- Typecheck/build are blocked by unrelated dirty Orders type errors; Delivery dashboard files were not the failing files.
- Real Supabase role QA is still needed for delivery manager, admin, director, and driver access.
- Outlet/team filter labels currently use IDs where scoped names are not loaded into the canonical Delivery DTO; future polish can add friendly outlet/team names.

## 2026-06-23 - Delivery Task 4 proof photo, camera, GPS, and watermark

Task completed:

- Tightened the `/delivery/driver` proof workflow for canonical `deliveries`.
- Delivered proof flow now opens the camera from the `Upload Delivered Proof` button, attempts phone GPS, watermarks the image, uploads it, and calls the canonical Delivered completion action.
- Failed proof flow now shows big failed-reason buttons first, then opens the camera from `Upload Failed Proof`, attempts phone GPS, watermarks the image, uploads it, and calls the canonical Failed completion action.
- Failed reasons are:
  - Customer not available
  - Wrong address
  - Customer rejected
  - Goods issue
  - Vehicle issue
  - Other
- `Other` requires a remark in the UI and in server action validation.
- V1 proof flow does not collect customer signature or receiver name.
- Watermark now includes delivery no, customer name, driver name, date/time, and GPS coordinates or `GPS unavailable`.
- Server action proof validation confirms a photo file exists, Failed has a failed reason, `Other` has remarks, delivery has started, and closed deliveries cannot be changed.
- Canonical proof upload stores metadata in `delivery_proofs`: delivery id, proof kind, file path/object path, uploader, upload timestamp, latitude, longitude, GPS unavailable flag, and failed reason where applicable.
- Added forward-only migration `202606230008_delivery_proof_uploaded_at_v1.sql` to add `delivery_proofs.uploaded_at`.
- GPS failure or denied permission still allows completion and records `gps_unavailable`.
- Linked customer order status continues to update to Delivered or Failed through the canonical service action.

Files changed in this pass:

- `app/(erp)/delivery/driver/page.tsx`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `supabase/migrations/202606230008_delivery_proof_uploaded_at_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230008_delivery_proof_uploaded_at_v1.sql`

Exact migration order:

- Run all pending migrations in filename order.
- `202606230008_delivery_proof_uploaded_at_v1.sql` must run after `202606230007_delivery_database_storage_rls_v1.sql`.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- Existing dev server at `http://localhost:3000` returned HTTP 200 for `/delivery/driver`.

Risks / remaining checks:

- Migration `202606230008` has not been applied to a real Supabase project by Codex.
- Real phone QA is still required for camera-only behavior, GPS allowed, GPS denied, watermark readability, Delivered proof, Failed proof, `Other` remark blocking, Storage upload, and linked order status updates.
- Browser automation screenshot QA remains unavailable in this Windows sandbox.

## 2026-06-23 - Delivery Task 3 driver mobile UI

Task completed:

- Rebuilt `/delivery/driver` as the main mobile-first driver page using canonical Task 2 Delivery services.
- Added `components/delivery/driver-mobile-delivery-page.tsx` with big tabs and cards only; no dense tables.
- Added driver tabs:
  - Available
  - My Deliveries
  - Completed
  - Failed
  - Expenses
- Available deliveries show today/unscheduled available canonical deliveries allowed by RLS; drivers can tap `Accept Delivery` with no typing.
- Added optional `Change Vehicle` control before accept; default vehicle is still applied automatically when no override is chosen.
- My Deliveries cards show customer name, delivery note, address, phone, total weight, item count, and status badge.
- Driver cards intentionally do not render price, customer credit, stock cost, stock value, profit, finance, or accounting fields.
- Added big card actions for Google Maps, Call, WhatsApp, Loaded, Start Delivery, Upload Delivered Proof, Upload Failed Proof, and Address Issue.
- Google Maps opens coordinates when available, otherwise falls back to address search.
- Proof upload uses camera file input, attempts phone GPS, watermarks the photo client-side, and calls the canonical Delivered/Failed completion actions.
- Failed proof includes one-tap failed reason and remarks field for `Other`.
- Completed and Failed tabs show proof/status summary cards.
- Expenses tab uses big expense type buttons for Petrol, Parking, Toll, Vehicle Repair, and Other, plus amount, receipt photo, optional remark, and submit.
- Added query helpers for active delivery vehicles and today driver expenses.

Files changed in this pass:

- `app/(erp)/delivery/driver/page.tsx`
- `components/delivery/driver-mobile-delivery-page.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- Existing dev server at `http://localhost:3000` returned HTTP 200 for `/delivery/driver`.

Risks / remaining checks:

- Browser automation screenshot QA could not run because the local Node REPL/browser automation kernel was blocked by the Windows sandbox.
- Real phone QA is still needed at around 390px width for camera capture, GPS allowed/denied, Google Maps, Call, WhatsApp, proof upload, failed proof, address issue, and expense receipt upload.
- Real Supabase role/storage QA is still needed after the pending Delivery migrations are applied.

## 2026-06-23 - Delivery Task 2 service layer and server actions

Task completed:

- Added canonical Delivery service/read layer for the Task 1 `deliveries` foundation.
- Added `lib/delivery/queries.ts` as a server-only data access layer returning delivery-safe DTOs for available jobs, driver jobs, dashboard data, and delivery detail.
- Extended `lib/delivery/types.ts` with canonical delivery, proof, linked-order, item, dashboard, GPS, manual-delivery, address-issue, and expense payload/result types.
- Added server actions in `lib/delivery/actions.ts`:
  - `createDeliveryFromOrder(orderId)`
  - `createManualDelivery(payload)`
  - `getAvailableDeliveries()`
  - `getTodayDriverDeliveries()`
  - `acceptDelivery(deliveryId)`
  - `markDeliveryLoaded(deliveryId)`
  - `startDelivery(deliveryId)`
  - `uploadDeliveredProofAndComplete(deliveryId, file, gps)`
  - `uploadFailedProofAndComplete(deliveryId, file, gps, failedReason, remarks)`
  - `reportAddressIssue(deliveryId, payload)`
  - `saveSuggestedCustomerGps(deliveryId, gps)`
  - `createDeliveryExpense(payload)`
  - `getDeliveryDashboard(filters)`
  - `getDeliveryById(deliveryId)`
- Added validation so delivery completion requires proof, failed completion requires failed reason, `Other` requires remarks, completion requires `OUT_FOR_DELIVERY`, and closed/cancelled deliveries cannot be updated by drivers.
- Completion updates canonical `deliveries`, linked `delivery_orders`, and linked `customer_orders`; it does not touch Stock tables or stock deduction workflows.
- Proof upload writes to `delivery-proofs`; expense upload writes to `delivery-expenses`.
- GPS unavailable is allowed, recorded on proof/delivery rows, and audit-logged.
- Captured proof GPS and manual GPS/address reports create `delivery_address_suggestions` only; official customer GPS is not overwritten.
- Every canonical status transition writes `delivery_status_logs`.

Files changed in this pass:

- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `lib/delivery/types.ts`
- `HANDOFF.md`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Service actions have not been exercised against a real Supabase project because Task 1 migrations are still pending in the shared migration queue.
- Real role/outlet/team QA is still needed with driver, delivery manager, admin, and director users.
- Real proof and expense upload QA is still needed to validate Storage RLS with the new buckets.
- Delivery-from-order currently uses the customer order's existing customer name/phone/remarks/date fields; customer master address/GPS enrichment is left for the UI/integration pass.

## What Already Exists

- Next.js App Router project with Supabase Auth and Supabase PostgreSQL.
- Public anon Supabase env usage only; no frontend service-role key was found in the documented architecture.
- Main ERP modules:
  - Dashboard/home shortcuts.
  - Stock/inventory with barcode scanning, item master, inbound, outbound, transfer, receive-transfer, return, no-barcode stock, stock take, reports.
  - Orders with order creation, item preparation, ready statuses, order reservations, notification placeholder events.
  - Delivery with delivery orders, vehicles, driver updates, payment status, proof photo metadata, and Orders-module handoff.
  - Attendance with work locations, clock in/out, late/no-clock-out/on-leave states.
  - OA actions for advances, claims, leave, payslips, and approval timelines.
  - Retail sales, POS-like sale entry, payments, cash sessions, daily closing, expenses, price rules, cleaning, and processing surfaces.
  - Processing batch entry/review and yield/loss tracking.
  - Cleaning task tracking with frequency and 30-day matrix.
  - Accounting/finance AR/AP invoices, containers, aging, payment lifecycle.
  - Director dashboard, approvals, print/WhatsApp/CSV report surfaces.
  - Admin settings for user scope, outlet module access, configurable lists, customers, customer price rules, and barcode rules.
- Migrations `202606100001` through `202606100032`.
- Seed data for internal testing.
- Documentation already present under `docs/` for testing, roles, access matrix, known limitations, and workflow acceptance.

## What Is Missing

- Real Supabase Auth test users for every role/outlet/department combination.
- Browser-based QA evidence for each route and workflow.
- Real camera testing on mobile/laptop devices.
- Real storage bucket policy validation for uploaded photos/PDFs/images.
- Production WhatsApp integration.
- Automated unit/integration tests for core business workflows.
- Clear ownership of the current dirty worktree and whether all untracked migrations should be committed together.

## Risky Logic

- Migration order matters. Later migrations tighten RLS and workflow behavior from earlier broad V1 policies.
- Role/scope isolation depends on both frontend route/module checks and Supabase RLS/server actions.
- Demo mode returns an admin/director profile when Supabase is not configured; good for local review, risky if misunderstood as production auth.
- Order outbound updates stock through an RPC and must remain atomic.
- Stock transfer location should change only after receive-transfer; changes to stock statuses can easily break this rule.
- Delivery has both standalone delivery orders and Orders-module delivery handoff records; these should not be confused.
- Retail same-day edit restrictions and "different checker than submitter" rules are business-critical and should be regression-tested.
- File upload fields currently store paths/metadata; do not assume uploads are production-safe until Storage policies are tested.

## Recommended Next Tasks

1. Freeze the current dirty worktree into a reviewed branch/commit once the owner confirms scope.
2. Run SQL migrations on a fresh Supabase project in exact filename order, then run seed data.
3. Create test Auth users for every role and assign outlet/department/stock scope.
4. Execute `docs/manual-qa-checklist.md` end to end.
5. Add focused automated tests for:
   - duplicate inbound barcode block,
   - order outbound missing/sold/wrong-location block,
   - receive-transfer location change,
   - stock take approval-only adjustment,
   - retail past-day edit block,
   - OA approval chains,
   - cross-outlet/team isolation.
6. Validate real file upload and camera scanner behavior on HTTPS or localhost.

## Current Validation Commands

Run after documentation or code changes:

```bash
npm run lint
npm run typecheck
npm run build
```

Optional existing project check:

```bash
npm run smoke
```

## Latest Documentation Pass

Task completed:

- Updated `AGENTS.md`.
- Created `HANDOFF.md`.
- Created `TASKS.md`.
- Created `docs/BUSINESS_RULES.md`.
- Created `docs/DATA_MODEL.md`.
- Created `docs/MODULE_STATUS.md`.

Files changed in this pass:

- `AGENTS.md`
- `HANDOFF.md`
- `TASKS.md`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - first parallel run failed because `.next/types/**/*.ts` files were temporarily missing while `npm.cmd run build` was running at the same time.
- `npm.cmd run build` - passed and regenerated `.next/types`.
- `npm.cmd run typecheck` - rerun by itself after build, passed.

Remaining issues:

- No runtime behavior was changed in this pass.
- The repository still has many pre-existing modified and untracked ERP files from prior work.
- Browser/device QA and real Supabase RLS testing remain pending.

## Latest Processing Module Pass

Task completed:

- Improved the Processing module only.
- Added centralized `calculateYield` and `calculateLoss` helpers under `lib/processing/`.
- Replaced `/processing/dashboard` and `/processing/batches` rendering so those routes use a Processing-specific page instead of the Retail page wrapper.
- Processing now shows raw weight, finished weight, calculated loss kg, and calculated yield percentage in KPI cards, mobile-friendly batch cards, and a scrollable table.
- Abnormal yield is highlighted using item thresholds where available, plus yield above 100%.

Files changed:

- `app/(erp)/processing/dashboard/page.tsx`
- `app/(erp)/processing/batches/page.tsx`
- `components/processing/processing-page.tsx`
- `lib/processing/calculations.ts`
- `lib/processing/data.ts`
- `lib/processing/types.ts`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm run lint` failed in PowerShell because `npm.ps1` is blocked by local execution policy.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks:

- Processing still adapts existing retail processing batch data/actions because there is not yet a separate Processing database/API layer.
- Current processing records are still one raw item to one finished item; the full multi-raw to multi-finished production rule remains future work.
- The page was compile-checked but not browser-tested at phone width in this pass.

What the next agent should check:

- Open `/processing/dashboard` and `/processing/batches` on mobile width and confirm no horizontal page scroll outside the batch table.
- Test processing batch creation and review with real processing roles.
- Confirm abnormal-yield highlights match management expectations for each item threshold.
- Plan a later Processing data-model pass for true multiple raw items to multiple finished items.

## Latest Overnight QA And Progress Audit

Task completed:

- Inspected the current codebase without adding features.
- Identified existing pages and modules.
- Classified modules as internal-test complete, partial, or demo/mock-backed.
- Checked business-rule coverage against `docs/BUSINESS_RULES.md`.
- Checked duplicated logic hotspots.
- Checked large/risky files.
- Ran lint, typecheck, and build.
- Updated `docs/MODULE_STATUS.md` with audit findings and progress scoring.
- Updated this handoff.

Files changed:

- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Progress estimate:

- Overall completion: 58%.
- MVP completion: 68%.
- Production readiness: 32%.

Scoring detail:

- Project foundation and navigation: 8/10.
- Data model and mock data: 7/10.
- Dashboard: 6/10.
- Inventory: 7/10.
- Processing: 5/10.
- Cleaning: 6/10.
- Import/container tracking: 4/10.
- Orders and delivery: 6/10.
- Barcode scanning: 6/10.
- Reports, testing, and deployment readiness: 3/10.

Must-fix issues:

- Delivery proof is not fully enforced before `DELIVERED` or `FAILED`.
- Order reservation insert in `lib/orders/actions.ts` does not check the insert error.
- Processing remains one raw item to one finished item through `retail_processing_batches`.
- Customer/category price rules are configurable but not fully applied during order/sale entry.
- Real Supabase Auth users, RLS, Storage, barcode camera, and mobile browser QA remain unverified.

Top next tasks:

1. Fix delivery proof enforcement for standalone delivery and customer-order delivery handoff.
2. Check and fail loudly when order reservation insertion fails.
3. Run all migrations and seed data on a fresh Supabase project.
4. Create real Auth users for every role/outlet/department/stock-location scope.
5. Execute role/scope RLS QA for stock, orders, delivery, retail, processing, finance, and settings.
6. Verify barcode scanner on phone and laptop camera.
7. Validate file upload/storage policies for proof photos, invoices, claims, payslips, and reports.
8. Apply customer/category price rules during order and retail sale entry.
9. Decide and implement the canonical processing data model for multiple raw items to multiple finished items.
10. Add smoke/browser tests and server-action/RLS regression checks for blocked paths.

Suggested next agent task:

- Use a Logic/Data agent to fix the two highest-risk workflow gaps first: proof-required delivery completion and checked order reservation creation. Do not start visual redesign or broad refactors before those workflow guards are corrected and tested.

## Latest Logic/Data Workflow Guard Pass

Task completed:

- Fixed delivery proof enforcement in server actions.
- Fixed order reservation insert error handling.
- Added smoke-script regression checks for both workflow guards.
- Updated module status and this handoff.

Files changed:

- `lib/delivery/actions.ts`
- `lib/orders/actions.ts`
- `scripts/smoke-routes.mjs`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

What changed:

- Standalone delivery status updates now read `proof_file_id` and reject `DELIVERED` or `FAILED` when proof is missing.
- Customer-order delivery status updates now reject `DELIVERED` or `FAILED` when `proof_file_id` is missing.
- Order item creation now checks the order status update error and the `order_stock_reservations` insert error.
- If reservation insertion fails, the action returns a clear error instead of reporting success without a reservation.

Fixed status:

- Delivery proof requirement: fixed at server-action layer for standalone delivery and customer-order delivery.
- Order reservation insert checking: fixed for fail-loud behavior.

Remaining risks:

- Order item creation and reservation creation are still separate database writes. If the reservation insert fails after the order item insert, the user sees an error, but a partial order item can remain. Recommended follow-up is an atomic RPC/transaction.
- Delivery proof enforcement was added in server actions. Direct database writes still depend on RLS/triggers and should be verified with real role-scoped Supabase users.
- File upload behavior still depends on real Supabase Storage bucket policies.

Recommended next task:

- Implement an atomic database RPC for adding an order item and reservation together, or add a safe compensating cleanup strategy if RPC scope is deferred.

## Latest Atomic Order Item Reservation Pass

Task completed:

- Added an atomic Supabase RPC for customer order item creation plus stock reservation creation.
- Updated the order item server action to call the RPC instead of separate `customer_order_items` and `order_stock_reservations` writes.
- Updated smoke checks and module status documentation.

Files changed:

- `lib/orders/actions.ts`
- `scripts/smoke-routes.mjs`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100033_order_item_reservation_rpc_v1.sql`

Exact migration order:

- Run all existing migrations in filename order.
- Run `supabase/migrations/202606100033_order_item_reservation_rpc_v1.sql` after `supabase/migrations/202606100032_admin_settings_customer_pricing_v1.sql`.
- Then run `supabase/seed.sql` if setting up demo data.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

What changed:

- New RPC: `public.add_customer_order_item_with_reservation(...)`.
- The RPC validates the order exists and is accessible through caller RLS.
- The RPC rejects closed/non-editable orders, negative requested values, and missing requested quantity/weight.
- The RPC inserts `customer_order_items`, updates the order to `PREPARING`, and inserts `order_stock_reservations` in one PostgreSQL function call.
- If any insert/update fails, PostgreSQL rolls back the whole function call, so the order item should not remain without a matching reservation.
- `addCustomerOrderItemAction` now returns `Could not add order item and reserve stock: ...` for RPC failures.

Assumptions documented:

- `profiles.id` matches `auth.uid()` for authenticated Supabase users.
- Existing RLS policies on `customer_orders`, `customer_order_items`, and `order_stock_reservations` are the intended authorization layer.
- The RPC is `security invoker` by design so it does not bypass role/outlet/department scope.
- `order_stock_reservations.location_id` may be null because the existing table and action allowed null stock location scope.

Remaining issues:

- The RPC has not been executed against a real Supabase project in this pass.
- Live testing must confirm scoped users can call the RPC after all migrations are applied.
- If the new migration is not applied, adding order items through the app will fail because the server action now depends on the RPC.

Next recommended task:

- Apply migrations on a fresh Supabase project, seed demo data, then test adding order items as retail, processing, admin, and an out-of-scope user to verify success and blocked paths.

## Latest Supabase QA Plan Pass

Task completed:

- Created a production-readiness Supabase QA plan for Auth, RLS, Storage, delivery proof enforcement, order reservation RPC validation, and barcode device testing.
- No business features or app behavior were changed.

Files changed:

- `docs/SUPABASE_QA_PLAN.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

What changed:

- Documented all ERP roles.
- Documented seeded outlets, departments, stock locations, and outlet module access.
- Added positive and negative QA Auth user matrix.
- Listed RLS-enabled tables that need verification.
- Listed Storage upload workflows and current `erp-files` policy risk.
- Added checklists for Auth users, RLS allowed/denied cases, delivery proof upload, processing upload absence, barcode phone/laptop QA, order reservation creation, and delivery completion proof enforcement.

Remaining issues:

- The plan has not yet been executed against a real Supabase staging project.
- Current Storage object policies are broad for authenticated users and need explicit production sign-off or later tightening.

Next recommended task:

- Execute `docs/SUPABASE_QA_PLAN.md` on a fresh Supabase staging project, then automate the RLS allowed/denied checks with seeded QA Auth users.

## Latest Confirmed Business Rules + Picking-Time Reservation Pass

Task completed:

- Updated project direction documents with the latest confirmed business rules.
- Implemented the next safest business-logic improvement: customer order item entry no longer reserves stock; picking/preparation now creates the stock reservation atomically.

Files changed:

- `AGENTS.md`
- `README.md`
- `TASKS.md`
- `HANDOFF.md`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/SUPABASE_QA_PLAN.md`
- `docs/manual-qa-checklist.md`
- `docs/role-team-access-matrix.md`
- `docs/workflow-acceptance-checklist.md`
- `components/orders/orders-forms.tsx`
- `lib/orders/actions.ts`
- `scripts/smoke-routes.mjs`

Migration SQL added:

- `supabase/migrations/202606100034_order_reservation_on_picking_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100034_order_reservation_on_picking_v1.sql`.
- `202606100033_order_item_reservation_rpc_v1.sql` must still run before `202606100034_order_reservation_on_picking_v1.sql`; migration `034` drops the obsolete add-item reservation RPC and creates the picking-time reservation RPC.
- Then run `supabase/seed.sql` if setting up demo data.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

What changed:

- Confirmed rules documented:
  - stock is reserved only when picking/preparation starts,
  - no free order editing after reservation,
  - temporary negative stock is allowed but needs alerts,
  - manual batch/barcode selection remains for now,
  - damaged/spoiled stock requires director approval before deduction,
  - stock take adjustment needs department manager approval before director final approval,
  - partial delivery is not allowed for MVP,
  - cancelled-after-reservation stock remains reserved until manual release,
  - overdue credit customer is warning-only,
  - finance/aging reports are director/account only,
  - multi-outlet/location staff access is a confirmed future model.
- `addCustomerOrderItemAction` now inserts only `customer_order_items`.
- `addCustomerOrderItemAction` blocks adding more items once the order already has an active reservation.
- New RPC `public.prepare_customer_order_item_with_reservation(...)` atomically:
  - locks the order item,
  - validates the order/item can still start picking,
  - marks the item prepared,
  - writes the preparation log,
  - updates the order to `PREPARING`,
  - creates the active `order_stock_reservations` row.
- `prepareCustomerOrderItemAction` now calls that RPC and returns a clear error if preparation/reservation fails.

Remaining issues:

- Multi-outlet/multi-location access is documented but not implemented.
- Temporary negative stock alerts were documented but not implemented at that time; see the later negative stock alert pass below.
- Manager then director stock-take adjustment approval is documented but not implemented.
- Damaged/spoiled stock director approval before deduction is documented but not implemented.
- Manual reservation release after customer cancellation is documented but not implemented.
- Overdue credit customer warning is documented but not implemented.
- The new migration has not been run against a real Supabase staging project in this pass.

Next recommended task:

- Implement temporary negative stock alerts in stock balance/report surfaces, because temporary negative stock is now allowed but must be highly visible.

## Latest Supabase Production QA Plan Refresh

Task completed:

- Refreshed the Supabase production-readiness QA plan for Auth, RLS, Storage uploads, delivery proof enforcement, order reservation RPC checks, and barcode device testing.
- No business features or app behavior were changed.

Files changed:

- `docs/SUPABASE_QA_PLAN.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

What changed:

- Expanded the QA Auth matrix so each operational role is tested across `JALAN CHANNEL`, `SUNGAI MERAH`, `WONDERFUL`, and `SUNGAI MAAW` outlet/stock-location scopes.
- Kept separate global control users for `account`, `admin`, and `director`.
- Added cross-scope denial pairs for retail, stock, orders, delivery, processing, attendance, OA, and finance.
- Added an explicit delivery proof upload checklist separate from delivery completion proof enforcement.
- Clarified Auth/profile setup checks, including the need to confirm the Auth trigger or manually repair profile rows before assigning roles.

Remaining issues:

- The QA plan has not yet been executed against a real Supabase staging project.
- Current Storage policies still allow broad authenticated read/upload to `erp-files`; the plan flags this for production sign-off or later policy tightening.
- The plan is manual. A future automated RLS test script is still recommended.

Next recommended task:

- Execute `docs/SUPABASE_QA_PLAN.md` on a fresh Supabase staging project, then automate the allowed/denied RLS checks using the QA Auth matrix.

## Latest Stock Alert Pass

Task completed:

- Implemented the next confirmed stock rule gap: temporary negative stock remains allowed, but stock users now get a clear alert when balance data goes below zero.
- Implemented stock age alerts from barcode unit inbound dates for stock older than 6 months and 12 months.

Files changed:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `components/stock/stock-page.tsx`
- `components/ui/status-badge.tsx`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

What changed:

- Added centralized negative stock alert derivation from stock balance rows.
- Added centralized stock age alert derivation from `stock_units.received_at`.
- Balance rows now include combined quantity/weight and negative stock metadata.
- Stock dashboard KPIs now include the number of negative stock alerts and stock age alerts.
- Stock pages show a top-level `Negative stock alert` panel when negative balances exist.
- Stock pages show a top-level `Stock age alert` panel when stockable barcode units are older than 6 months or 12 months.
- Stock balance table shows total quantity, total kg, and `NEGATIVE STOCK`/`OK` status.
- Stock WhatsApp report summaries include negative stock and stock age alert counts.
- Demo data includes one negative no-barcode balance and two aged barcode units so local/demo mode can verify both alert paths.

Remaining issues:

- Negative stock alerting is application-derived and has not yet been verified against real Supabase role-scoped data.
- Stock age alerting is application-derived and has not yet been verified against real Supabase role-scoped data.
- No database notification/escalation workflow exists yet for negative stock; this pass only makes it visible in stock balance/report surfaces.
- Manager then director stock-take approval, damaged/spoiled director approval, manual reservation release, overdue credit warning, and multi-scope access remain open confirmed rule gaps.

Next recommended task:

- Implement the stock-take adjustment approval chain: department manager review first, then director final approval before adjustment is applied.

## Latest Part 2 Delivery / Barcode / Processing Direction Pass

Task completed:

- Updated project direction for PART 2 rules covering delivery, processing, pricing/customer, barcode, cleaning, storage, and Supabase/RLS expectations.
- Implemented the safest workflow slices:
  - delivery proof metadata and auto-delivered proof completion,
  - atomic customer-order proof completion plus customer GPS update,
  - delivery e-wallet payment type support,
  - delivery status/proof/payment access for assigned non-director staff who have delivery module access,
  - centralized barcode weight decoder with GS1 `3102`/`3103`, 1-3 decimal position rules, fixed-weight fallback, and manual confirmation messaging,
  - default processing abnormal-yield alert below 85%.

Files changed:

- `AGENTS.md`
- `README.md`
- `TASKS.md`
- `HANDOFF.md`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/SUPABASE_QA_PLAN.md`
- `components/delivery/delivery-forms.tsx`
- `components/orders/orders-forms.tsx`
- `components/stock/workflow-forms.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/types.ts`
- `lib/orders/actions.ts`
- `lib/processing/calculations.ts`
- `lib/processing/data.ts`
- `lib/stock/barcode-weight.ts`
- `scripts/smoke-routes.mjs`
- `supabase/seed.sql`

Migration SQL added:

- `supabase/migrations/202606100035_part2_delivery_proof_metadata_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100035_part2_delivery_proof_metadata_v1.sql`.
- Then run `supabase/seed.sql` for demo data.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

What changed:

- Delivery proof upload now requires receiver name, latitude, longitude, and image file.
- Standalone delivery proof upload updates `delivery_orders` with proof metadata and marks the order `DELIVERED`.
- Customer-order proof upload now calls `public.complete_customer_order_delivery_with_proof`, which locks the order row, checks scoped delivery access, updates `customer_orders` proof metadata, marks the order `DELIVERED`, and updates linked `customers.latitude` / `customers.longitude` in one database transaction.
- Delivery payment type now includes `EWALLET`.
- Delivery status/proof actions now allow assigned non-director staff with delivery module access; vehicle setup and new standalone delivery order entry remain delivery manager/admin workflows.
- Delivery payment entry now allows delivery general workers, delivery managers, account, and admin.
- Migration `202606100035_part2_delivery_proof_metadata_v1.sql` overrides delivery access helper functions idempotently so RLS matches the delivery-access workflow for scoped users.
- VIP customer category is added idempotently; legacy categories are not destructively removed.
- Barcode inbound now decodes GS1 `3102` and `3103`, supports 1-3 decimal position rules, supports fixed-weight fallback, and blocks auto-save when manual confirmation is required.
- Processing abnormal-yield checks default to below 85% when an item-specific minimum is absent.
- MVP storage scope is documented as delivery proof and import documents first; receipts and stock photos remain later.

Remaining issues:

- Failed delivery reinbound/return stock workflow is documented but not implemented.
- Multiple raw items to multiple finished items is documented but not implemented.
- Finished-product barcode generation from Processing output is documented but not implemented.
- Staff price override reason enforcement is documented but not implemented.
- AutoCount-format aging buckets are documented but not implemented.
- Import document upload remains represented by finance invoice upload; a dedicated import document workflow is not implemented.
- Standalone delivery proof upload plus status log insert are still separate writes; a future RPC should make standalone proof completion and logging atomic.

Next recommended task:

- Implement failed-delivery reinbound/return workflow first, because it protects stock accuracy after unsuccessful deliveries.

## Latest Part 3 Reservation Release Pass

Task completed:

- Updated docs/task queue with confirmed Part 3 rules.
- Selected one high-priority implementation task: manual release of reserved stock after a customer order is cancelled.
- Confirmed order creation/item entry does not reserve stock; picking/preparation still creates reservations through `prepare_customer_order_item_with_reservation`.
- Added manual reservation release for cancelled customer orders.

Files changed:

- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `components/orders/orders-forms.tsx`
- `components/orders/orders-page.tsx`
- `lib/orders/actions.ts`
- `lib/orders/data.ts`
- `lib/orders/types.ts`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None. Existing `order_stock_reservations.status` already supports `RELEASED`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

What changed:

- Added `releaseOrderReservationsAction`.
- The release action requires the order to be `CANCELLED`.
- The release action updates only `ACTIVE` reservations to `RELEASED`.
- If the order is not cancelled or no active reservation exists, staff get a clear error.
- Orders data now loads and maps `order_stock_reservations`.
- Orders UI now shows a stock reservations table.
- Orders prepare/detail UI now includes a `Release reserved stock` form for cancelled orders with active reservations.
- Demo orders now include a cancelled order with an active reservation so the release surface is visible in demo mode.
- Smoke checks now cover the release action, release form, reservation data loading, and the no-auto-release rule text.

Remaining issues:

- Release workflow has not been verified against real Supabase RLS with seeded Auth users.
- Reservation release is a single-table update plus audit log, not a database RPC. If audit logging must be atomic with the release, add an RPC later.
- Cancelling a normal non-delivery order is still not a dedicated Orders-module action; current cancellation paths are limited by existing delivery/order status flows.

Next recommended task:

- Implement failed-delivery reinbound/return stock workflow and log linkage, because it is now the highest stock-accuracy gap after reservation release.

## Latest Failed Delivery Return Workflow Pass

Task completed:

- Implemented failed customer-order delivery return/reinbound workflow for linked barcode stock.
- Kept standalone delivery safe by recording `NO_STOCK_LINK` instead of pretending stock was returned.
- Blocked plain `FAILED` status updates so staff must upload failed proof with photo, receiver/contact name, and GPS.
- Surfaced failed-return status in delivery and order views.

Files changed:

- `README.md`
- `TASKS.md`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `components/delivery/delivery-forms.tsx`
- `components/delivery/delivery-page.tsx`
- `components/orders/orders-forms.tsx`
- `components/orders/orders-page.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/data.ts`
- `lib/delivery/demo-data.ts`
- `lib/delivery/types.ts`
- `lib/orders/actions.ts`
- `lib/orders/data.ts`
- `lib/orders/types.ts`
- `scripts/smoke-routes.mjs`
- `supabase/migrations/202606100036_failed_delivery_return_workflow_v1.sql`

Migration SQL added:

- `supabase/migrations/202606100036_failed_delivery_return_workflow_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100036_failed_delivery_return_workflow_v1.sql`.
- Then run `supabase/seed.sql`.

What changed:

- Added `failed_return_status`, `failed_return_required_units`, `failed_return_completed_units`, and `failed_return_logged_at` to `customer_orders` and `delivery_orders`.
- Added `public.fail_customer_order_delivery_with_proof`.
- The failed-delivery RPC locks the customer order, requires delivery scope access, finds linked `stock_outbound_batch_lines` where `outbound_type = 'SALES'`, returns linked barcode `stock_units` to `IN_STOCK`, inserts `stock_movements` with `movement_type = 'RETURN'`, inserts `barcode_scan_logs` with `action = 'RETURN'`, updates failed-return counters, and writes an audit log.
- The failed-delivery RPC does not release or delete active `order_stock_reservations`.
- Customer-order proof upload can now submit `DELIVERED` or `FAILED`; `FAILED` calls the return RPC and does not create a delivered notification event.
- Standalone delivery proof upload can mark `FAILED`, stores proof metadata, records `NO_STOCK_LINK`, and logs a status message requiring manual return follow-up.
- Direct status update to `FAILED` is blocked for standalone deliveries and customer-order deliveries.
- Delivery and Orders pages show failed-return status so staff can see `Returned`, `Pending`, or `No linked stock`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Failed standalone deliveries still have no normalized stock link; staff must follow up manually when `NO_STOCK_LINK` is shown.
- Failed delivery return currently handles barcode stock linked through sales outbound batch lines. Loose/no-barcode stock return needs a later workflow.
- The failed customer-order proof action uploads the Storage object and file metadata before calling the return RPC. If the RPC fails, uploaded proof metadata/object cleanup is not automatic.
- Real Supabase RLS, Storage policy, and device/browser QA are still required with seeded Auth users.

Next recommended task:

- Add real Supabase QA scripts or browser/manual evidence for the full order -> outbound -> failed delivery -> return movement flow, then implement no-barcode/standalone failed-return stock resolution if the business needs it for MVP.

## Latest Local Demo Profile Scope Fix

Task completed:

- Fixed local/demo profile scope for `clementkcl@elitempsb.com`.
- Checked the possible email variants `clementkc`, `clementkl`, and `clementkcl`.
- Kept production RLS/access logic unchanged.

Files changed:

- `README.md`
- `supabase/seed.sql`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

What changed:

- Demo seed now enables every current ERP module in `outlet_module_access` for every demo outlet.
- Existing profiles with these emails are updated to a valid broad testing scope:
  - `clementkc@elitempsb.com`
  - `clementkl@elitempsb.com`
  - `clementkcl@elitempsb.com`
- Matching Clement profiles are assigned the `admin` and `director` roles only, replacing stale/default role rows.
- Matching Clement profiles are scoped to the `DIRECTOR` outlet, `Management` department, and `DIRECTOR` stock location. Admin/director roles still give global access in the app.

Root cause:

- The Auth user could sign in, but the seed did not include or upgrade `clementkcl@elitempsb.com`.
- The Auth trigger-created profile therefore had no outlet, department, or stock location.
- With no explicit role rows, the app fell back to `retail_team_general_worker`, and with no outlet the user had no module access rows to read.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Required local reseed command:

- `supabase db seed`

Remaining issue:

- The seed updates existing Auth-triggered profile rows. If the Auth user does not exist yet, sign in/create `clementkcl@elitempsb.com` first, then run `supabase db seed` again.

## Latest Admin Settings User Access UI Pass

Task completed:

- Improved `/settings` so admin can assign user outlet, department, stock location, roles, and selected outlet module access from one User Access form.
- Kept production RLS and access helpers unchanged.
- Reused existing `profiles`, `profile_roles`, and `outlet_module_access` tables.

Files changed:

- `components/settings/settings-page.tsx`
- `lib/settings/actions.ts`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`
- `docs/codex-current-status.md`

What changed:

- User Access form now includes module checkboxes for the selected outlet.
- Saving User Access updates:
  - `profiles.outlet_id`
  - `profiles.department_id`
  - `profiles.stock_location_id`
  - `profile_roles`
  - `outlet_module_access` for the selected outlet
- The module access UI clearly notes that module toggles are outlet-level and apply to users assigned to that outlet.
- Existing separate Outlet Module Access form remains available for outlet-level edits.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issue:

- Module access is still outlet-level, not per-user. This matches the current data model and RLS design. A future per-user module override would need a new table and RLS plan.

## Latest UX Agent Workflow Documentation Pass

Task completed:

- Added UX-focused Codex agent workflow documentation.
- No app features, business logic, database schema, RLS, or migrations were changed.

Files changed:

- `AGENTS.md`
- `docs/AGENT_WORKFLOW.md`
- `docs/UX_AGENT_GUIDE.md`
- `docs/USER_FRIENDLY_CHECKLIST.md`
- `HANDOFF.md`

What changed:

- Added Worker UX Agent.
- Added Mobile Scanner UX Agent.
- Added Manager Dashboard UX Agent.
- Added Driver UX Agent.
- Added Error and Empty State Agent.
- Added Accessibility and Language Agent.
- Added User Journey QA Agent.
- Documented UX agent rules, acceptance checks, and required commands.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining risks:

- These are workflow docs only. No usability changes have been applied to the app yet.
- Future UX tasks must still verify role/scope restrictions and avoid changing business logic unless a usability-blocking bug requires it.

Next recommended task:

- Start with Mobile Scanner UX Agent on `/stock/inbound` and `/stock/outbound`, because scanning speed and clarity are high-impact worker workflows.

## Latest Stock Inbound Barcode / Label Pass

Task completed:

- Advanced the Stock Module goal by hardening stock inbound, item master, barcode rule scope, and label printing.
- Kept work scoped to stock files, stock docs, seed data, and stock smoke checks.

Files changed:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/smoke-routes.mjs`
- `supabase/seed.sql`
- `supabase/migrations/202606100037_stock_inbound_labels_rules_v1.sql`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100037_stock_inbound_labels_rules_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100037_stock_inbound_labels_rules_v1.sql`.
- Then run `supabase/seed.sql`.
- This migration has not been run against the live Supabase project by Codex.

What changed:

- Item master now uses numeric-only item codes in the server action and UI, with next-code style `0001`, `0002`, etc.
- Migration `037` converts old non-numeric `items.item_code` values to numeric codes and adds a numeric-only check constraint.
- Item master now supports editable Chinese name, Iban name, and default low stock kg fields.
- Stock operators can create/edit item master, brands, and origins; delete remains admin/director-only through RLS.
- Inactive items are blocked from new barcode inbound.
- Inbound sources now include supplier/import, processing output, customer return, transfer received, manual adjustment, and other.
- Brand and origin selectors on inbound allow Other/custom values.
- Barcode weight rules are now saved globally by item + brand + origin using `barcode_weight_rules.location_id = null`; old location-specific rules can remain for compatibility.
- Inbound form can generate numeric internal barcode labels based on date + numeric item code + weight in grams + serial.
- Inbound form shows duplicate barcode warning before submit and still relies on server-side duplicate block.
- Recent successful inbound labels are listed and can be printed/exported as PDF labels using 50mm x 30mm pages.
- Success feedback now includes optional vibration/sound in supported browsers.
- Seed stock item codes were updated to numeric values so fresh seed runs remain compatible with migration `037`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- The stock-unit detail/reprint page does not exist yet; label reprint is currently available from recent inbound labels only.
- Browser print/PDF export is implemented, but real thermal printer layout needs device testing.
- Phone camera scanner and vibration/sound feedback still need real device QA.
- Existing migrated projects may still contain old location-specific barcode weight rules; new saves are global by item + brand + origin.
- No-barcode route still exists for compatibility, but the MVP direction is to generate labels and use barcode stock instead.
- Damage/spoilage approval, return-supplier approval, and manager-then-director stock-take adjustment are still open stock workflow gaps.

Next recommended task:

- Implement the dedicated stock-unit detail/reprint page and then continue with damaged/spoilage and return-supplier approval workflows.

## Latest Stock Take Scoped Approval Pass

Task completed:

- Implemented item+brand-scoped stock take sessions.
- Added manager review and director final approval signatures for stock take.
- Added stock-operation locking while a scoped stock take is open.

Files changed:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/smoke-routes.mjs`
- `supabase/migrations/202606100038_stock_take_scoped_approval_v1.sql`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100038_stock_take_scoped_approval_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100038_stock_take_scoped_approval_v1.sql`.
- Migration `037` must run before `038`.
- Then run `supabase/seed.sql`.
- This migration has not been run against the live Supabase project by Codex.

What changed:

- `stock_take_sessions` now supports `item_id`, `brand_id`, manager review fields, and director approval fields.
- Managers/admin create stock take sessions for one location and one item+brand.
- Stock take barcode scans must match the session location, item, and brand.
- Manual stock take lines are constrained to the selected session item+brand.
- While a stock take is open in `DRAFT`, `SUBMITTED`, or `REVIEWED`, inbound/outbound/transfer/return actions are blocked only for that item+brand at that location.
- Manager review requires a manager signature and moves `SUBMITTED` to `REVIEWED`.
- Director approval requires a director signature and only applies adjustment movements after the session is `REVIEWED`.
- Rejection is split by stage: manager/admin can reject `SUBMITTED`; director/admin can reject `REVIEWED`.
- UI permissions now separate stock operators, stock managers, and director final approval.

Commands run and results:

- `npm.cmd run smoke` - passed after updating stock smoke checks.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still needed for manager-created scoped sessions and director approval.
- The signature values are simple electronic signature strings in the current UI; a richer signing/audit UX can come later.
- Damage/spoilage approval and return-supplier approval remain open stock workflow gaps.
- Direct outbound support remains open; current outbound is still order-based batch outbound plus existing transfer/return forms.
- Dedicated stock-unit detail/reprint page remains open.

Next recommended task:

- Implement damage/spoilage request -> manager review -> director approval -> stock deduction, including required photo and reason list.

## Latest Stock Damage / Spoilage Approval Pass

Task completed:

- Implemented damage/spoilage request -> manager review -> director approval -> stock deduction.
- Removed direct `SPOILED / DAMAGE` from order outbound so ordinary stock outbound no longer deducts damaged stock immediately.

Files changed:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/smoke-routes.mjs`
- `supabase/migrations/202606100039_stock_damage_approval_v1.sql`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100039_stock_damage_approval_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100039_stock_damage_approval_v1.sql`.
- Migration `039` depends on helper functions from `038`, so run `037`, `038`, then `039` in order.
- Then run `supabase/seed.sql`.
- This migration has not been run against the live Supabase project by Codex.

What changed:

- Added `stock_damage_requests` with required `photo_path`, reason, status, manager signature, director signature, and final movement linkage.
- Damage reasons: expired, broken packaging, smell, wrong temperature, customer rejected, other.
- Staff with stock access can submit a damage request for an active barcode in their stock location.
- A barcode can have only one open damage request.
- Damage requests are blocked if the selected item+brand/location is under an open stock take.
- Managers/admin can review submitted damage requests.
- Director/admin can approve manager-reviewed damage requests.
- Director approval updates the stock unit to `DAMAGED`, writes an `OUTBOUND_SPOILED` stock movement, writes a scan log, and links the movement back to the request.
- Damage requests now appear in the stock return page and in stock report rows as `Damage/spoilage`.

Commands run and results:

- `npm.cmd run typecheck` - passed before documentation update.
- Full smoke/lint/typecheck/build still needs final rerun after this handoff entry.

Remaining issues:

- Damage approval updates the stock unit/movement and then the request in separate writes. A future RPC should make this fully atomic.
- Real Supabase/RLS QA is still needed for damage request/review/approval roles.
- Real file upload/storage integration for damage photos is not implemented; the current MVP field records a photo path/reference.
- Return-supplier approval workflow remains open.
- Direct outbound support remains open.
- Dedicated stock-unit detail/reprint page remains open.

Next recommended task:

- Implement return-supplier request -> manager review -> stock deduction, or add a stock-unit detail/reprint page if label operations are the next priority.

## Latest Stock Return Supplier Approval Pass

Task completed:

- Implemented return-supplier request -> manager approval -> stock deduction.

Files changed:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/smoke-routes.mjs`
- `supabase/migrations/202606100040_stock_return_supplier_approval_v1.sql`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100040_stock_return_supplier_approval_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100040_stock_return_supplier_approval_v1.sql`.
- Migration `040` adds the `OUTBOUND_RETURN_SUPPLIER` stock movement enum value and the `stock_return_supplier_requests` table.
- Then run `supabase/seed.sql`.
- This migration has not been run against the live Supabase project by Codex.

What changed:

- Staff with stock access can submit a supplier return request for an active barcode in their stock location.
- Supplier name is required.
- A barcode can have only one open supplier return request.
- Supplier return requests are blocked if the selected item+brand/location is under an open stock take.
- Managers/admin can approve or reject submitted supplier return requests.
- Manager approval updates the stock unit to `OUTBOUNDED`, writes an `OUTBOUND_RETURN_SUPPLIER` stock movement, writes a scan log, and links the movement back to the request.
- Supplier return requests now appear on the stock return page and in stock report rows as `Return supplier`.

Commands run and results:

- Full smoke/lint/typecheck/build still needs final rerun after this handoff entry.

Remaining issues:

- Return-supplier approval updates the stock unit/movement and then the request in separate writes. A future RPC should make this fully atomic.
- Real Supabase/RLS QA is still needed for return supplier request/approval roles.
- Direct outbound support remains open.
- Dedicated stock-unit detail/reprint page remains open.

Next recommended task:

- Implement a stock-unit detail/reprint page, then continue direct outbound support and real browser/device QA.

## 2026-06-12 - Stock unit detail and label reprint page

Task completed:

- Added a scoped stock-unit detail page so staff can open a barcode stock unit from Stock Balance, review its details/movement history, and reprint/export the 50mm x 30mm thermal label after inbound.

Files changed:

- `app/(erp)/stock/units/[id]/page.tsx`
- `components/stock/stock-page.tsx`
- `components/stock/stock-unit-detail.tsx`
- `lib/stock/data.ts`
- `scripts/smoke-routes.mjs`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100040_stock_return_supplier_approval_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/balance` now includes a `Barcode stock units` table.
- Barcode stock unit rows open `/stock/units/[id]`.
- `/stock/units/[id]` uses the existing stock module access guard and the scoped stock data loader. If a scoped user cannot see a stock unit, the detail route returns not found.
- The detail page shows item, item code, brand, origin, location, status, weight, inbound source, batch, received date, and movement history.
- The detail page includes a print/export action for a 50mm x 30mm label with company name, product name, weight, and barcode.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real browser/device QA is still needed for the stock-unit label print/export flow.
- Direct outbound support remains open.
- Return-supplier approval still needs a future atomic RPC if production risk tolerance requires it.

Next recommended task:

- Implement direct outbound support or run real browser/device QA for scanner and label print flows.

## 2026-06-12 - Direct outbound batch workflow

Task completed:

- Added direct stock outbound support for `SALES`, `TRANSFER`, and `PROCESSING` without requiring a customer order.

Files changed:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `supabase/migrations/202606100041_direct_outbound_batches_v1.sql`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/workflow-acceptance-checklist.md`
- `docs/manual-qa-checklist.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100041_direct_outbound_batches_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100041_direct_outbound_batches_v1.sql`.
- Migration `041` makes `stock_outbound_batches.order_id` and `stock_outbound_batch_lines.order_id` nullable for direct outbound, updates outbound batch RLS to allow scoped direct batches, and adds `public.confirm_direct_outbound_batch`.
- Then run `supabase/seed.sql`.
- This migration has not been run against the live Supabase project by Codex.

What changed:

- `/stock/outbound` now has an `Outbound mode` selector for `Order-based` or `Direct outbound`.
- Order-based outbound keeps the ready-customer-order requirement and existing atomic RPC.
- Direct outbound uses the same batch scanner UI and calls `confirm_direct_outbound_batch`.
- Direct outbound creates a stock outbound batch and batch lines with `order_id = null`.
- Direct `SALES` marks stock units `SOLD`; direct `PROCESSING` marks units `OUTBOUNDED`; direct `TRANSFER` marks units `TRANSFER_PENDING` and keeps the current location until receive-transfer scan.
- Direct outbound blocks duplicate, missing, inactive-status, wrong-location, mixed-location, same-destination transfer, and open stock-take item+brand/location scans.
- Damage/spoilage and supplier return remain approval workflows, not direct outbound types.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is needed for direct outbound with normal stock users and admin/director.
- Real browser/device QA is still needed for scanner behavior on the direct outbound mode.
- Return-supplier approval still needs a future atomic RPC if production risk tolerance requires it.

Next recommended task:

- Run real browser/device QA for inbound/outbound scanner flows or add automated regression coverage around stock actions.

## 2026-06-12 - No-barcode-to-barcode stock flow

Task completed:

- Aligned the Stock module with the MVP rule that barcode stock is the main model and new no-barcode balances should not be created.

Files changed:

- `app/(erp)/stock/no-barcode-inbound/page.tsx`
- `components/erp/app-shell.tsx`
- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/workflow-acceptance-checklist.md`
- `docs/manual-qa-checklist.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100041_direct_outbound_batches_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/no-barcode-inbound` redirects to `/stock/inbound`.
- The no-barcode sidebar item was removed.
- The old no-barcode inbound server action now returns a clear blocked message instead of inserting/updating `no_barcode_stock`.
- The old `NoBarcodeInboundForm` component is now guidance that tells staff to generate/print a barcode label first and then use Barcode Inbound.
- Legacy no-barcode records remain readable in data/report surfaces for compatibility.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Legacy no-barcode data remains in balance/report calculations until old records are migrated or depleted.
- Other non-stock modules may still reference no-barcode stock; this slice intentionally changed only Stock-module creation flow.
- Real browser/device QA is still needed for the label-first inbound path.

Next recommended task:

- Add automated stock workflow regression tests beyond static smoke checks, or run real device QA for inbound/outbound scanners.

## 2026-06-12 - Stock workflow regression checks

Task completed:

- Added executable stock workflow regression checks for barcode weight parsing and internal numeric barcode label generation.

Files changed:

- `lib/stock/barcode-label.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-workflow-regression.mjs`
- `scripts/smoke-routes.mjs`
- `package.json`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100041_direct_outbound_batches_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Moved internal generated barcode construction to `lib/stock/barcode-label.ts`.
- Stock inbound now uses the shared barcode label helper.
- Added `scripts/stock-workflow-regression.mjs`.
- `npm.cmd run smoke` now runs both `scripts/smoke-routes.mjs` and `scripts/stock-workflow-regression.mjs`.
- Regression coverage checks:
  - Imported position-rule sample barcode decodes to 26.78 kg.
  - GS1 `3102` sample barcode decodes to 14.46 kg.
  - GS1 `3103` sample barcode decodes to 17.670 kg.
  - Fixed-weight sample requires manual confirmation at 10 kg.
  - Generated internal barcode is numeric-only and follows date + numeric item code + grams + serial.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- These are pure regression checks, not database-backed Supabase/RLS tests.
- Real scanner/device QA is still needed.
- Real migrated Supabase QA is still needed for stock action policies and RPCs.

Next recommended task:

- Add database-backed stock action tests against a seeded local Supabase project, or run real device QA for scanner/label flows.

## 2026-06-12 - Atomic stock approval RPCs

Task completed:

- Replaced split-write approval flows for damage/spoilage and return supplier with atomic Supabase RPCs.

Files changed:

- `lib/stock/actions.ts`
- `supabase/migrations/202606100042_atomic_stock_approval_rpcs_v1.sql`
- `scripts/smoke-routes.mjs`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100042_atomic_stock_approval_rpcs_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100042_atomic_stock_approval_rpcs_v1.sql`.
- Migration `042` adds:
  - `public.approve_stock_damage_request(uuid, text)`
  - `public.approve_stock_return_supplier_request(uuid, text)`
- Then run `supabase/seed.sql`.
- This migration has not been run against the live Supabase project by Codex.

What changed:

- Damage/spoilage director approval now calls `approve_stock_damage_request`.
- Return-supplier manager approval now calls `approve_stock_return_supplier_request`.
- Each RPC locks the request and stock unit, checks role/location/status, updates the stock unit, writes stock movement, writes scan log, writes audit log, updates the request, and returns the movement id in one transaction.
- Existing request creation/review/reject behavior is unchanged.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real migrated Supabase/RLS QA is still needed for manager/director approval users.
- Stock take approval is still action-layer multi-step and should be reviewed for atomicity separately.
- Real scanner/device QA is still needed.

Next recommended task:

- Review stock-take director approval for atomicity, or run real Supabase/RLS QA for stock approval RPCs.

## 2026-06-12 - Atomic stock-take final approval RPC

Task completed:

- Replaced split-write stock-take director final approval with an atomic Supabase RPC.

Files changed:

- `lib/stock/actions.ts`
- `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`
- `scripts/smoke-routes.mjs`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`.
- Migration `043` adds `public.approve_stock_take_session(uuid, text)`.
- Then run `supabase/seed.sql`.
- This migration has not been run against the live Supabase project by Codex.

What changed:

- `approveStockTakeAction` now calls `approve_stock_take_session`.
- The RPC locks the reviewed stock-take session and lines, checks director/admin permission and location scope, verifies each line matches the session item+brand scope, writes non-zero `STOCK_TAKE_ADJUSTMENT` movements, marks the session `APPROVED`, saves director signature fields, and writes an audit log in one transaction.
- Manager review, rejection, line entry, and scoped stock-take locks are unchanged.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real migrated Supabase/RLS QA is still needed for manager-created sessions, worker scan/count, manager review, and director approval.
- Real scanner/device QA is still needed.

Next recommended task:

- Run real Supabase/RLS QA or add database-backed stock action tests for the stock take approval RPC.

## 2026-06-12 - Stock report coverage

Task completed:

- Expanded Stock Reports so CSV/print/WhatsApp reporting covers stock balance, stock-take variance, damage/spoilage, and return-supplier summaries.

Files changed:

- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/workflow-acceptance-checklist.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Stock report rows now include:
  - `Stock by location`
  - `Stock by category`
  - `Stock take variance`
  - `Damage/spoilage`
  - `Return supplier`
- Stock-take variance report rows use the linked stock-take session location where available.
- Stock Reports page description now names the included report types.
- Smoke checks now assert these report categories and the report export toolbar are present.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Report numbers still need real Supabase data QA.
- Browser print/PDF and WhatsApp-copy behavior still need manual browser QA.

Next recommended task:

- Run real browser QA for stock report print/CSV/WhatsApp, or continue with database-backed RLS tests.

## 2026-06-12 - Legacy no-barcode wording

Task completed:

- Tightened Stock dashboard and balance wording so no-barcode balances are shown as legacy/old-record visibility, not as an active MVP stock workflow.

Files changed:

- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/smoke-routes.mjs`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Stock dashboard now labels the old loose-stock KPI as `Legacy no-barcode weight`.
- Stock balance table columns now use `Legacy qty` and `Legacy kg`.
- Stock balance help text now says barcode stock is primary and legacy loose balances are visible for old records only.
- Smoke checks now guard this wording so no-barcode does not drift back into a first-class MVP workflow.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Legacy no-barcode records still remain in calculations until old records are migrated, depleted, or cleaned up by a future data migration.
- Real Supabase/RLS and device scanner QA are still needed.

Next recommended task:

- Run real Supabase/RLS QA for stock workflows or continue with database-backed stock action tests.

## 2026-06-12 - Transfer receive overdue alert

Task completed:

- Added alert-only visibility for transfers scanned out but not received after 3 days.

Files changed:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Stock dashboard data now calculates `transferPendingAlerts` by matching `TRANSFER_PENDING` stock units to their latest `OUTBOUND_TRANSFER` movement by barcode.
- Transfers pending for 3 days or more now show a `Transfer receive overdue` alert panel.
- Stock KPI text now shows how many pending transfers are overdue.
- Stock report WhatsApp summary now includes `Overdue transfer alerts`.
- Smoke checks now guard the transfer-overdue alert calculation and UI/report summary wiring.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed after fixing JSX text escaping in the transfer alert panel.
- `npm.cmd run typecheck` - passed after fixing JSX text escaping in the transfer alert panel.
- `npm.cmd run build` - passed.

Remaining issues:

- This is an alert-only implementation. It does not notify staff externally or auto-resolve transfers.
- Real Supabase data QA is still needed to confirm movement barcode matching covers all migrated transfer rows.
- Real device scanner QA is still needed.

Next recommended task:

- Continue with stock workflow edge-case QA, especially direct/order outbound scanning warnings and stock-take lock behavior in a real Supabase project.

## 2026-06-12 - Order outbound requested-vs-scanned warning

Task completed:

- Added warning-only order outbound comparison so staff can see requested quantity/weight, scanned quantity/weight, and substitutions before confirming an outbound batch.

Files changed:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/outbound` now passes order item lines into the outbound scanning form.
- In order-based outbound mode, scanned barcodes are compared against the selected order's requested quantity and requested weight.
- The UI shows a warning if scanned quantity/weight differs or if a scanned stock unit is not part of the original order item list.
- Substitution remains allowed for MVP. The original ordered item stays on the order item line, and the actual scanned item remains recorded on the outbound batch line.
- Smoke checks now guard this warning and substitution visibility.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is warning-only by confirmed business rule; it does not require a manager override.
- Real Supabase QA is still needed to verify the outbound batch RPC records substituted item lines as expected under RLS.

Next recommended task:

- Continue with real Supabase/RLS QA for order outbound substitutions, transfer receive, and stock-take locks.

## 2026-06-12 - Item master all-role edit access

Task completed:

- Aligned Stock item master with the rule that every ERP role can create/edit items when their outlet has Stock module access, while item deletion remains admin/director only.

Files changed:

- `lib/stock/actions.ts`
- `components/stock/stock-page.tsx`
- `components/erp/app-shell.tsx`
- `components/dashboard/home-page.tsx`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `supabase/migrations/202606100044_item_master_all_roles_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100044_item_master_all_roles_v1.sql`

Exact migration order:

1. Run existing migrations in filename order through `supabase/migrations/202606100043_atomic_stock_take_approval_rpc_v1.sql`.
2. Run `supabase/migrations/202606100044_item_master_all_roles_v1.sql`.
3. Run `supabase/seed.sql`.

No migration has been run against the live Supabase project by Codex.

What changed:

- Added `public.can_edit_item_master()` helper that includes all current ERP roles.
- Replaced `items` insert/update RLS policies with item-master policies that use `public.can_edit_item_master()`.
- Kept `items` delete policy restricted to `public.can_administer_stock()`.
- Updated stock item server actions so `account` and all other business roles can create/update item master when Stock module access is present.
- Updated sidebar, route guard, and Home shortcut so `/stock/items` is reachable by all business roles with Stock module access without opening routine stock operation routes.
- Updated the item form numeric placeholder and local next-code reset so it stays in the `0001`, `0002` workflow after a successful create.
- Clarified that item master identity is category plus product section/name, while brand is captured on barcode stock units, barcode rules, price rules, stock-take scope, and stock request workflows.
- Smoke checks now guard the migration, route guard, and item-master shortcut.

Commands run and results:

- `npm.cmd run smoke` - passed after reordering the item delete policy block so the matching `DROP POLICY IF EXISTS` immediately precedes `CREATE POLICY`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still needed for each role to confirm item create/update succeeds and item delete remains blocked for non-admin/director.
- This does not grant Stock module access by itself; admin still needs to assign Stock module access at outlet/module level.

Next recommended task:

- Run role-by-role Supabase QA for item master, then continue Stock acceptance testing on real device scanner and stock RLS isolation.

## 2026-06-12 - Item master default brand

Task completed:

- Added default-brand support to item master so item identity can follow category + brand + product section/name while actual stock brand remains captured on barcode stock units and stock workflows.

Files changed:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/actions.ts`
- `lib/stock/demo-data.ts`
- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `supabase/seed.sql`
- `supabase/migrations/202606100045_item_master_default_brand_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100045_item_master_default_brand_v1.sql`

Exact migration order:

1. Run existing migrations in filename order through `supabase/migrations/202606100044_item_master_all_roles_v1.sql`.
2. Run `supabase/migrations/202606100045_item_master_default_brand_v1.sql`.
3. Run `supabase/seed.sql`.

No migration has been run against the live Supabase project by Codex.

What changed:

- Added `items.default_brand_id`.
- Replaced old item uniqueness on category/section/name with category/default-brand/section/name uniqueness.
- Added a default-brand field to item create/edit forms.
- Added Brand to the item master table.
- Seed data now assigns default brands to the demo item master rows where useful.
- Smoke checks now guard the migration and UI wiring.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA is still needed to verify the uniqueness replacement on existing data and all-role item edit policies after migrations are applied.
- The field is a default/master brand only. Barcode stock units, barcode rules, stock take, pricing, damage, and return workflows still store the actual stock brand separately.

Next recommended task:

- Continue Stock acceptance testing, especially real RLS/location isolation and mobile scanner device QA.

## 2026-06-12 - Stock acceptance coverage smoke guard

Task completed:

- Added an automated stock acceptance coverage script and wired it into `npm.cmd run smoke`.

Files changed:

- `scripts/stock-acceptance-coverage.mjs`
- `package.json`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100045_item_master_default_brand_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `npm.cmd run smoke` now runs:
  - `scripts/smoke-routes.mjs`
  - `scripts/stock-workflow-regression.mjs`
  - `scripts/stock-acceptance-coverage.mjs`
- The new acceptance coverage guard checks the implementation evidence for:
  - inbound scan
  - duplicate barcode block
  - barcode weight-rule save/reuse
  - barcode label printing/reprint
  - order-based outbound
  - transfer/receive
  - damage/spoilage approval
  - stock take manager/director approval
  - no-barcode-to-barcode flow
  - reports/export
  - role/location isolation hooks
  - mobile scanner camera/manual fallback hooks
  - item master numeric/default-brand/all-role edit rules

Commands run and results:

- `npm.cmd run smoke` - passed. It now runs route checks, barcode/label regression checks, and stock acceptance coverage checks.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is still source-level automated coverage, not a real database transaction test.
- Real Supabase/RLS and real phone camera QA remain required before marking the Stock module complete.

Next recommended task:

- Add database-backed Supabase QA or run manual role/location/device QA on a seeded project.

## 2026-06-12 - Stock data load error hardening

Task completed:

- Hardened stock data loading so configured Supabase query errors do not silently fall back to demo data.

Files changed:

- `lib/stock/data.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100045_item_master_default_brand_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `loadRows()` now throws `Stock data could not load <table>: <error>` when Supabase is configured and a stock query fails.
- Demo stock data fallback remains available only when no Supabase server client is configured.
- The stock acceptance coverage script now guards that configured database errors surface and that stock-unit detail lookup is based on scoped `getStockPageData()`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still needed. This change makes those failures visible instead of hiding them behind demo data.
- Real device scanner QA is still needed.

Next recommended task:

- Run the stock module against a seeded Supabase project and verify role/location-specific user accounts.

## 2026-06-12 - Mobile scanner field hardening

Task completed:

- Improved the reusable stock barcode field for phone-width scanner workflows.

Files changed:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100045_item_master_default_brand_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `BarcodeField` now shows a larger `Scan Barcode` button that fits phone-width workflows.
- Added clear manual fallback text under every barcode input.
- Added recent scan display after a camera-detected barcode.
- Added scanner modal guidance telling staff to allow camera permission or close and use manual fallback.
- Kept rear-camera preference and stream cleanup behavior.
- Stock acceptance coverage now guards these scanner UX hooks.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real phone/laptop camera QA is still needed under HTTPS or localhost.
- This is UI hardening only; it does not prove actual camera permission behavior on physical devices.

Next recommended task:

- Run real device QA for `/stock/inbound`, `/stock/outbound`, `/stock/transfer`, `/stock/receive-transfer`, `/stock/return`, and `/stock/stock-take`.

## 2026-06-12 - Stock QA runbook

Task completed:

- Added a dedicated Stock QA runbook that maps the Stock goal's 13 acceptance tests to concrete Supabase, RLS, phone, desktop, and database evidence.

Files changed:

- `docs/STOCK_QA_RUNBOOK.md`
- `docs/manual-qa-checklist.md`
- `docs/MODULE_STATUS.md`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100045_item_master_default_brand_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `docs/STOCK_QA_RUNBOOK.md` now defines the Stock-only completion evidence required for:
  - all 13 stock acceptance tests,
  - blocked/invalid paths,
  - phone scanner QA,
  - desktop scanner/label QA,
  - table-level evidence to capture.
- Manual QA checklist now links to the Stock runbook.
- Module status now lists passing runbook evidence as a remaining Stock completion gate.
- Smoke checks now require the Stock QA runbook and its key sections.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- The runbook is a test plan. It does not replace actually running the real Supabase/RLS and device QA.

Next recommended task:

- Execute `docs/STOCK_QA_RUNBOOK.md` against a seeded Supabase staging project and capture pass/fail evidence.

## 2026-06-12 - Stock Part 2 rule capture and QA evidence log

Task completed:

- Captured the latest Stock Part 2 business rules for outbound, transfer, damage/spoilage, customer return, failed-delivery return, stock take, stock reports, and permissions.
- Added a dedicated Stock QA evidence log so real Supabase/RLS/device results can be recorded against the 13 acceptance tests.
- Added smoke coverage to require the evidence log and its main sections.

Files changed:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/BUSINESS_RULES.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/manual-qa-checklist.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this documentation/QA guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100045_item_master_default_brand_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Business rules now explicitly state:
  - outbound supports order-based and direct batches;
  - outbound types are `SALES`, `TRANSFER`, `PROCESSING`, `DAMAGE`/`SPOILED`, and `RETURN_SUPPLIER`;
  - direct outbound remains limited to `SALES`, `TRANSFER`, and `PROCESSING`;
  - damage/spoilage and supplier return must use approval workflows before deduction;
  - transfer cannot be cancelled after scanned out;
  - customer return after sale goes to hold/inspection first;
  - failed delivery return restores linked barcode stock to `IN_STOCK`;
  - stock take is barcode-only and scoped by selected item+brand+location;
  - stock reports group by item, brand, location, and inbound age.
- `docs/STOCK_QA_EVIDENCE.md` provides a fillable evidence template for:
  - all 13 Stock acceptance tests;
  - outbound workflow paths;
  - transfer evidence;
  - damage and return evidence;
  - stock take evidence;
  - RLS/scope evidence;
  - phone and desktop scanner evidence;
  - final sign-off.
- `scripts/smoke-routes.mjs` now checks that the evidence log exists and includes the required sections.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This slice documents and guards QA evidence expectations. It does not run real Supabase/RLS tests or real device scanner tests.
- Transfer cancellation blocking, customer-return hold/inspection, and all approval paths still need real seeded Supabase verification.

Next recommended task:

- Run `npm.cmd run smoke`, `npm.cmd run lint`, `npm.cmd run typecheck`, and `npm.cmd run build`, then execute `docs/STOCK_QA_RUNBOOK.md` and fill `docs/STOCK_QA_EVIDENCE.md` on a seeded Supabase staging project.

## 2026-06-12 - Customer return inspection status

Task completed:

- Implemented the Stock rule that customer returns after sale must go to inspection before becoming sellable stock.
- Added safe enum migration values for `HOLD` and `INSPECTION`.
- Changed barcode inbound so `inboundSource = customer_return` creates stock units with `status = INSPECTION` instead of `IN_STOCK`.
- Added a worker-facing inbound message explaining that customer returns are held for inspection first.
- Added seeded/demo inspection barcode evidence for Stock QA.

Files changed:

- `lib/stock/actions.ts`
- `lib/stock/types.ts`
- `lib/stock/demo-data.ts`
- `components/stock/workflow-forms.tsx`
- `supabase/seed.sql`
- `supabase/migrations/202606100046_customer_return_inspection_status_v1.sql`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/DATA_MODEL.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100046_customer_return_inspection_status_v1.sql`
  - Adds `HOLD` and `INSPECTION` to `public.stock_unit_status`.
  - Adds a comment documenting that customer returns use hold/inspection before sellable stock.

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100046_customer_return_inspection_status_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Normal inbound still creates `IN_STOCK` barcode units.
- Customer-return inbound now creates `INSPECTION` barcode units.
- `INSPECTION` is intentionally excluded from stockable/outbound-active statuses because `activeStockStatus()` still only accepts `IN_STOCK`, `TRANSFERRED`, and `RETURNED`.
- `supabase/seed.sql` now includes `EM-SEED-RETURN-INSPECTION-001` for real Supabase QA.
- Demo mode includes `EM-RET-INSP-001` with `status = INSPECTION`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- There is not yet a release-from-inspection workflow. Staff can see inspection stock, but moving it from inspection to sellable stock needs a future manager/admin review action.
- Real Supabase/RLS and browser/device QA remain required.

Next recommended task:

- Add a small manager/admin release-from-inspection workflow for customer returns, or run seeded QA first to confirm the inspection block behaves as expected.

## 2026-06-12 - Customer return inspection release

Task completed:

- Added a controlled release path for customer-return stock held in `INSPECTION` or `HOLD`.
- Managers/admin can scan an inspection barcode on `/stock/return`, add notes, and release it back to `IN_STOCK`.
- The release action keeps stock-location scope checks, blocks non-held barcodes, respects open stock-take locks, writes a `MANUAL_ADJUSTMENT` movement, logs a barcode scan, and writes an audit log.
- Updated docs and smoke/acceptance guards for the inspection release path.

Files changed:

- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/DATA_MODEL.md`
- `docs/manual-qa-checklist.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this release workflow.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100046_customer_return_inspection_status_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New server action: `releaseInspectionStockAction`.
- New UI card: `InspectionReleaseForm` on `/stock/return`.
- Customer-return inspection stock remains unavailable for normal outbound until a manager/admin releases it.
- Seeded test barcode `EM-SEED-RETURN-INSPECTION-001` can be used to test blocked outbound before release and `IN_STOCK` availability after release.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still required to prove manager/admin release and worker denial with actual users.
- Real phone/laptop scanner QA is still required.

Next recommended task:

- Run seeded Supabase QA for `EM-SEED-RETURN-INSPECTION-001`: confirm outbound is blocked while `INSPECTION`, release it as manager/admin, then confirm it becomes available as `IN_STOCK`.

## 2026-06-12 - Stock report inbound-age grouping

Task completed:

- Added stock report rows grouped by item, brand, location, and inbound age.
- The report builder now creates `Stock by inbound age` rows from barcode units that are actually stockable.
- Inbound age buckets are `Under 6 months`, `6 to 12 months`, and `Over 12 months`.
- Updated report UI copy, QA docs, smoke checks, and stock acceptance coverage.

Files changed:

- `lib/stock/data.ts`
- `components/stock/stock-page.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this report data slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100046_customer_return_inspection_status_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/reports` now includes `Stock by inbound age` rows.
- CSV, print/PDF-ready view, and WhatsApp summary now include those rows through the existing report export path.
- Demo data includes older 6-month and 12-month stock units, so the report can show age buckets locally.

Commands run and results:

- `npm.cmd run smoke` - passed after updating the report smoke guard to the new inbound-age wording.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Report output still needs real Supabase QA with seeded barcode units and scoped users.
- Real print/PDF export should be manually checked in the browser.

Next recommended task:

- Verify `/stock/reports` with seeded Supabase data and confirm CSV/print/WhatsApp output includes `Stock by inbound age`.

## 2026-06-12 - Atomic transfer and receive-transfer RPCs

Task completed:

- Hardened the single-barcode transfer and receive-transfer workflows so stock unit updates and audit records succeed or fail together.
- Added atomic RPCs for transfer scan and destination receive scan.
- Updated `transferAction` and `receiveTransferAction` to call the RPCs instead of doing separate stock-unit update, movement insert, scan-log insert, and audit-log insert steps.
- Updated smoke and stock acceptance coverage to guard the RPC definitions and action wiring.

Files changed:

- `lib/stock/actions.ts`
- `supabase/migrations/202606100047_atomic_transfer_receive_rpcs_v1.sql`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100047_atomic_transfer_receive_rpcs_v1.sql`
  - Adds `public.transfer_stock_unit(text, uuid, text, text)`.
  - Adds `public.receive_stock_transfer(text, uuid, text, text)`.
  - Both functions lock the barcode stock unit, validate role/location/status/stock-take lock, update the unit, write `stock_movements`, write `barcode_scan_logs`, and write `audit_logs` in one transaction.

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100047_atomic_transfer_receive_rpcs_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/transfer` still leaves `location_id` unchanged and sets `TRANSFER_PENDING`.
- `/stock/receive-transfer` changes `location_id` only after the destination receive scan succeeds.
- Transfer and receive-transfer audit rows are now written atomically with the stock unit change.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still required to prove scoped users can transfer/receive only allowed locations.
- Real phone/laptop scanner QA is still required.

Next recommended task:

- Test `/stock/transfer` and `/stock/receive-transfer` with a seeded Supabase user and verify unit status/location plus movement, scan-log, and audit-log rows.

## 2026-06-12 - Atomic stock return RPC

Task completed:

- Hardened the normal barcode stock-return workflow so returning a barcode to `IN_STOCK` and writing audit records succeed or fail together.
- Added an atomic RPC for normal stock return.
- Updated `returnStockAction` to call the RPC instead of doing separate stock-unit update, movement insert, scan-log insert, and audit-log insert steps.
- Kept customer-return `HOLD`/`INSPECTION` stock out of the normal return path; those barcodes must use manager/admin inspection release.
- Updated smoke and stock acceptance coverage to guard the RPC definition and action wiring.

Files changed:

- `lib/stock/actions.ts`
- `supabase/migrations/202606100048_atomic_stock_return_rpc_v1.sql`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100048_atomic_stock_return_rpc_v1.sql`
  - Adds `public.return_stock_unit(text, uuid, text, text)`.
  - The function locks the barcode stock unit, validates role/location/status/stock-take lock, updates the unit to `IN_STOCK`, writes `stock_movements`, writes `barcode_scan_logs`, and writes `audit_logs` in one transaction.

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100048_atomic_stock_return_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/return` now uses `public.return_stock_unit`.
- Normal return still restores stock to `IN_STOCK`.
- `HOLD` and `INSPECTION` stock cannot be returned through the normal return form; manager/admin inspection release remains the required path.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still required to prove scoped users can return only allowed locations.
- Real phone/laptop scanner QA is still required.

Next recommended task:

- Test `/stock/return` with a seeded Supabase user and verify unit status plus movement, scan-log, and audit-log rows.

## 2026-06-12 - Atomic inspection release RPC

Task completed:

- Hardened the customer-return inspection-release workflow so moving `HOLD`/`INSPECTION` stock back to `IN_STOCK` and writing audit records succeed or fail together.
- Added an atomic RPC for inspection release.
- Updated `releaseInspectionStockAction` to call the RPC instead of doing separate stock-unit update, movement insert, scan-log insert, and audit-log insert steps.
- Updated smoke and stock acceptance coverage to guard the RPC definition and action wiring.

Files changed:

- `lib/stock/actions.ts`
- `supabase/migrations/202606100049_atomic_inspection_release_rpc_v1.sql`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100049_atomic_inspection_release_rpc_v1.sql`
  - Adds `public.release_inspection_stock_unit(text, text)`.
  - The function locks the barcode stock unit, validates role/location/status/stock-take lock, updates the unit to `IN_STOCK`, writes `stock_movements`, writes `barcode_scan_logs`, and writes `audit_logs` in one transaction.

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100049_atomic_inspection_release_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/return` inspection release now uses `public.release_inspection_stock_unit`.
- Customer-return inspection release still requires manager/admin role and stock-location scope.
- `HOLD`/`INSPECTION` stock is released to `IN_STOCK` with movement, scan-log, and audit-log rows.

Commands run and results:

- `npm.cmd run smoke` - passed after updating old source-level guards to check the new RPC-backed implementation.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still required to prove scoped manager/admin release and worker denial with actual users.
- Real phone/laptop scanner QA is still required.

Next recommended task:

- Test `EM-SEED-RETURN-INSPECTION-001` with a seeded Supabase manager/admin: blocked outbound while `INSPECTION`, release on `/stock/return`, then verify `IN_STOCK`, movement, scan-log, and audit-log rows.

## 2026-06-12 - Atomic barcode inbound RPC

Task completed:

- Hardened barcode inbound so successful scans create all required audit records together.
- Added an atomic RPC for barcode inbound.
- Updated `barcodeInboundAction` to call the RPC instead of doing separate stock-unit insert, movement insert, optional rule upsert, scan-log insert, and audit-log insert steps.
- Kept duplicate inbound pre-check and failed scan logging for clear worker feedback.
- Updated smoke and stock acceptance coverage to guard the RPC definition and action wiring.

Files changed:

- `lib/stock/actions.ts`
- `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`
  - Adds `public.inbound_stock_unit(...)`.
  - The function validates role/location/item/status/stock-take lock, creates `stock_units`, creates `stock_movements`, optionally saves the global item+brand+origin barcode weight rule, writes `barcode_scan_logs`, and writes `audit_logs` in one transaction.

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/inbound` now uses `public.inbound_stock_unit`.
- Successful inbound now atomically creates stock unit, inbound movement, scan log, audit log, and optional barcode weight rule.
- Customer-return inbound still starts as `INSPECTION`; normal inbound still starts as `IN_STOCK`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed after removing the obsolete local `insertMovement` helper.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is still required to prove scoped inbound and cross-location denial with actual users.
- Real phone/laptop scanner QA is still required.

Next recommended task:

- Test `/stock/inbound` with a seeded Supabase user: scan a new barcode with save-rule enabled and verify stock unit, movement, scan-log, audit-log, and barcode weight-rule rows all exist.

## 2026-06-12 - Stock state-transition coverage guard

Task completed:

- Audited the stock action file for remaining direct `stock_units` writes.
- Confirmed the only direct `stock_units` access in `lib/stock/actions.ts` is a read helper by barcode.
- Added a smoke-time source guard to keep stock unit state transitions behind atomic database RPCs.
- Updated Stock QA documentation so automated coverage includes state-transition coverage.

Files changed:

- `package.json`
- `scripts/stock-state-transition-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `docs/workflow-acceptance-checklist.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-state-transition-coverage.mjs` checks that stock state-changing server actions use the expected RPCs for inbound, order outbound, direct outbound, transfer, receive-transfer, return, inspection release, damage approval, supplier return approval, and stock-take approval.
- The script blocks future direct `insert`, `update`, `delete`, or `upsert` calls against `stock_units` in `lib/stock/actions.ts`.
- The script verifies the relevant migrations contain atomic transaction markers and row locking where expected.
- `npm.cmd run smoke` now runs the new guard after the existing route, barcode regression, and stock acceptance coverage checks.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Documentation cleanup after validation:

- `docs/BUSINESS_RULES.md` now labels stock-take and damage approval as needing real Supabase evidence instead of missing source/schema work.
- `docs/DATA_MODEL.md` now reflects the current migration range through `202606100050_atomic_barcode_inbound_rpc_v1.sql`.
- `docs/workflow-acceptance-checklist.md` now includes global item+brand+origin barcode rules, expanded inbound sources, manager/director stock-take approval, damage/spoilage approval, return-supplier approval, customer-return inspection, and failed-delivery return checks.

Remaining issues:

- This is source-level coverage. It does not replace real Supabase/RLS testing or real phone/laptop barcode scanner QA.
- Real seeded QA is still required for failed paths such as wrong-location scans, duplicate inbound, damage approval, stock take approval, and transfer receive.

Next recommended task:

- Run the full local command set, then execute `docs/STOCK_QA_RUNBOOK.md` on a seeded Supabase staging project and fill `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Outbound rule regression helpers

Task completed:

- Moved pure outbound rules out of `lib/stock/actions.ts` into a testable Stock helper.
- Added regression coverage for outbound barcode list parsing, duplicate detection, and ready-order gating.
- Updated stock acceptance coverage so order-based outbound checks include the helper and regression tests.

Files changed:

- `lib/stock/actions.ts`
- `lib/stock/outbound-rules.ts`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this helper/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `parseOutboundBarcodes`, `duplicateOutboundBarcode`, and `assertCustomerOrderReadyForOutbound` now live in `lib/stock/outbound-rules.ts`.
- `lib/stock/actions.ts` imports those helpers without changing outbound business behavior.
- `scripts/stock-workflow-regression.mjs` now tests:
  - trimming and blank removal for outbound scanned barcode lists;
  - invalid scanned-list JSON rejection;
  - empty scanned-list rejection;
  - duplicate barcode detection;
  - `READY_FOR_PICKUP` and `READY_FOR_DELIVERY` outbound allowance;
  - non-ready order outbound blocking.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- These are pure local regression tests. They do not replace real database-backed Supabase tests for missing, wrong-status, wrong-location, already-outbounded, or cross-scope barcodes.
- Real scanner/device QA remains required.

Next recommended task:

- Add database-backed Supabase/RLS workflow tests after a staging project with seeded users is available, or continue extracting small pure stock rules where they can be tested safely.

## 2026-06-12 - Stock take rule regression helpers

Task completed:

- Moved pure stock-take scope/signature validation out of `lib/stock/actions.ts` into a testable Stock helper.
- Added regression coverage for item+brand stock-take scope matching and required manager/director signatures.
- Updated stock acceptance coverage so stock-take approval checks include the helper and regression tests.

Files changed:

- `lib/stock/actions.ts`
- `lib/stock/stock-take-rules.ts`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this helper/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `sameNullableId`, `requireStockTakeScopeMatch`, and `requireSignature` now live in `lib/stock/stock-take-rules.ts`.
- `lib/stock/actions.ts` imports those helpers without changing stock-take workflow behavior.
- `scripts/stock-workflow-regression.mjs` now tests:
  - nullable brand matching;
  - matching item+brand stock-take scan allowance;
  - item mismatch blocking;
  - brand mismatch blocking;
  - signature trimming;
  - blank director signature rejection.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- These are pure local regression tests. They do not replace real database-backed tests for stock-take row creation, manager review, director approval, adjustment movement creation, or RLS denial paths.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Add similar pure regression coverage for item-code normalization/duplicate guard boundaries, or run the full seeded Supabase stock QA plan when a staging project is available.

## 2026-06-12 - Item code rule regression helpers

Task completed:

- Centralized pure item-code rules for the Stock item master.
- Updated the item master form and server actions to use the shared helper for normalization, numeric checks, next-code generation, and generated-code suggestions.
- Added regression coverage for numeric-only item-code behavior.

Files changed:

- `lib/stock/actions.ts`
- `lib/stock/item-code.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this helper/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New helper `lib/stock/item-code.ts` provides:
  - `normalizeItemCode`;
  - `isNumericItemCode`;
  - `nextItemCode`;
  - `generatedItemCode`.
- `ItemMasterForm` now uses `isNumericItemCode`, `nextItemCode`, and `generatedItemCode`.
- `createItemAction` and `updateItemAction` now use shared `normalizeItemCode`.
- `scripts/stock-workflow-regression.mjs` now tests:
  - trimming item-code input;
  - accepting numeric codes;
  - rejecting non-numeric codes;
  - preserving four-digit next-code padding;
  - resetting invalid next-code suggestions to `0001`;
  - ignoring old non-numeric item codes when suggesting the next numeric code.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Duplicate item-code blocking still depends on the database query and unique/constraint behavior; it must be tested against seeded Supabase data.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then test item master create/edit and duplicate item-code blocking with seeded Supabase users.

## 2026-06-12 - Damage and return-supplier approval rule regression helpers

Task completed:

- Centralized pure status-gate rules for damage/spoilage and return-supplier approval workflows.
- Updated stock server actions to use the shared helper without changing role checks, RPC usage, or database writes.
- Added regression coverage for manager/director approval-chain status gates.
- Added explicit return-supplier approval coverage to the stock acceptance script.

Files changed:

- `lib/stock/actions.ts`
- `lib/stock/approval-rules.ts`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this helper/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New helper `lib/stock/approval-rules.ts` provides:
  - `assertDamageCanBeManagerReviewed`;
  - `damageRejectionSignatureLabel`;
  - `assertReturnSupplierCanBeRejected`.
- `reviewDamageRequestAction`, `rejectDamageRequestAction`, and `rejectReturnSupplierRequestAction` now use those helpers.
- `scripts/stock-workflow-regression.mjs` now tests:
  - submitted damage requests can be manager reviewed;
  - manager-reviewed damage requests cannot be manager reviewed again;
  - submitted damage rejection requires manager signature;
  - manager-reviewed damage rejection requires director signature;
  - director-approved damage requests cannot be rejected;
  - submitted return-supplier requests can be rejected by manager;
  - reviewed return-supplier requests cannot be rejected again.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- These are pure local regression tests. They do not replace real Supabase tests for damage request creation, manager review, director approval RPC stock deduction, return-supplier manager approval RPC stock deduction, RLS denial, or file/photo evidence.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then test damage/spoilage and return-supplier workflows with seeded Supabase manager/director users.

## 2026-06-12 - Stock report export regression helpers

Task completed:

- Moved pure stock report CSV and WhatsApp summary builders out of `components/stock/stock-page.tsx` into a reusable Stock helper.
- Added regression coverage for CSV escaping and WhatsApp summary totals/alert counts.
- Updated stock acceptance coverage so report/export checks include the helper and regression tests.

Files changed:

- `components/stock/stock-page.tsx`
- `lib/stock/report-export.ts`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this helper/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New helper `lib/stock/report-export.ts` provides:
  - `buildCsv`;
  - `buildStockWhatsappSummary`;
  - `StockReportTableRow`.
- `/stock/reports` still uses the same report toolbar behavior, but now imports the pure export helpers from `lib/stock/report-export.ts`.
- `scripts/stock-workflow-regression.mjs` now tests:
  - blank CSV output for empty rows;
  - CSV headers and quote escaping;
  - WhatsApp summary title;
  - location count;
  - total report row count;
  - total stock count and weight;
  - negative stock, stock age, and overdue transfer alert counts.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- These are pure local regression tests. They do not replace browser testing for CSV download, print/PDF rendering, or WhatsApp copy behavior on the actual `/stock/reports` page.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then manually test `/stock/reports` CSV download, print/PDF view, and WhatsApp summary on desktop and phone width.

## 2026-06-12 - Stockable status regression helpers

Task completed:

- Centralized stockable barcode-unit status rules and outbound movement-type mapping.
- Updated stock actions and stock data/report calculations to use the same stockable status list.
- Added regression coverage to keep `INSPECTION`, `HOLD`, `TRANSFER_PENDING`, `DAMAGED`, and `SOLD` out of normal outbound-active stock.

Files changed:

- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/unit-status-rules.ts`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this helper/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New helper `lib/stock/unit-status-rules.ts` provides:
  - `stockableStatuses`;
  - `activeStockStatus`;
  - `movementTypeForOutboundType`.
- `lib/stock/actions.ts` now imports `activeStockStatus` and `movementTypeForOutboundType`.
- `lib/stock/data.ts` now imports `stockableStatuses`, keeping stock balance/report calculations aligned with server-action status checks.
- `scripts/stock-workflow-regression.mjs` now tests:
  - only `IN_STOCK`, `TRANSFERRED`, and `RETURNED` are stockable;
  - `INSPECTION`, `HOLD`, `TRANSFER_PENDING`, `DAMAGED`, and `SOLD` are not active for normal outbound;
  - `SALES`, `TRANSFER`, and `PROCESSING` outbound types map to the correct stock movement types.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed after removing an unused action-file type alias from the helper extraction.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- These are pure local regression tests. They do not replace real Supabase tests for wrong-status outbound blocking, transfer receive behavior, or customer-return inspection release.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then test wrong-status outbound blocking and customer-return inspection behavior against seeded Supabase data.

## 2026-06-12 - Numeric barcode label item-code guard

Task completed:

- Tightened internal barcode label generation so labels are generated only when the item code is numeric.
- Added regression coverage for the non-numeric item-code case.
- Updated stock acceptance coverage and QA docs for numeric-only generated labels.

Files changed:

- `lib/stock/barcode-label.ts`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this helper/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `makeInternalBarcode` now returns an empty string if `item.itemCode` is missing or non-numeric.
- This prevents accidental label generation from old non-numeric codes such as `AB-7`.
- `scripts/stock-workflow-regression.mjs` now verifies that a valid weight with a non-numeric item code still produces no generated barcode.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is local helper coverage. Real label printing/export and duplicate barcode checks still need Supabase/browser QA.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then manually test label generation and label reprint in `/stock/inbound` and `/stock/units/[id]`.

## 2026-06-12 - Stock migration safety guard

Task completed:

- Added automated safety checks for stock-related migrations.
- Wired the migration safety guard into `npm.cmd run smoke`.
- Updated stock QA docs and evidence templates so acceptance test 13 includes migration safety coverage.

Files changed:

- `package.json`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-migration-safety.mjs` scans stock-related migrations and fails if it finds:
  - `drop table`;
  - `truncate`;
  - `alter table ... drop column`;
  - `delete from public.*`;
  - `delete from storage.*`.
- The script also checks every `CREATE POLICY` in those stock migrations is immediately preceded by the matching `DROP POLICY IF EXISTS`.
- `npm.cmd run smoke` now runs this script after route, workflow regression, stock acceptance, and state-transition coverage checks.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is a static migration safety guard. It does not replace actually applying migrations to a fresh Supabase project.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then apply migrations through `202606100050_atomic_barcode_inbound_rpc_v1.sql` on a fresh Supabase staging project and execute `docs/STOCK_QA_RUNBOOK.md`.

## 2026-06-12 - Stock role/scope coverage guard

Task completed:

- Added automated source coverage for Stock role, module, route, and stock-location guard expectations.
- Wired the role/scope guard into `npm.cmd run smoke`.
- Updated stock QA docs and evidence templates so acceptance test 13 includes role/scope coverage.

Files changed:

- `package.json`
- `scripts/stock-role-scope-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-role-scope-coverage.mjs` checks:
  - Stock actions still require `canAccessModule(profile, "stock")`.
  - Stock location guard messages and checks remain present.
  - Routine stock operator roles do not include director.
  - Item editor roles include broad ERP item-master access.
  - Director approval roles remain director/admin.
  - Item create/update use item editor roles.
  - Routine stock actions use operator roles plus stock-location guards.
  - Manager approval/setup actions use manager roles plus stock-location guards.
  - Director approval actions use director/admin roles and RPCs.
  - Stock route role mapping keeps item master separate from routine operations.
- `npm.cmd run smoke` now runs this script after route, workflow regression, stock acceptance, state-transition, and migration safety checks.

Commands run and results:

- `npm.cmd run smoke` - passed after aligning the direct outbound static guard with the actual action labels.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is source-level role/scope coverage. It does not replace real Supabase RLS tests with scoped users and cross-location denial evidence.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then execute real RLS allowed/denied tests from `docs/STOCK_QA_RUNBOOK.md` and record them in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock scanner coverage guard

Task completed:

- Added automated source coverage for Stock scanner/mobile workflow expectations.
- Wired the scanner coverage guard into `npm.cmd run smoke`.
- Updated stock QA docs and evidence templates so acceptance test 12 and automated test coverage include scanner source checks.

Files changed:

- `package.json`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-scanner-coverage.mjs` checks:
  - the scanner uses `@zxing/browser`;
  - it asks for camera access and prefers rear camera;
  - it has a large phone-friendly `Scan Barcode` button;
  - it includes manual fallback and recent scan display;
  - it shows camera permission/error guidance;
  - it stops camera tracks and clears the video stream on close;
  - stock scan workflows include at least six `BarcodeField` usages;
  - inbound, outbound, transfer, receive-transfer, return, and stock-take routes remain present.
- `npm.cmd run smoke` now runs this script after route, workflow regression, stock acceptance, state-transition, migration safety, and role/scope coverage checks.

Commands run and results:

- `npm.cmd run smoke` - passed after aligning the scanner route guard with the actual Stock route title structure.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is source-level scanner coverage. It does not replace real phone/laptop camera testing, browser permission testing, or visual verification at 390px width.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then test scanner behavior on a real phone and laptop and record results in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock RLS policy coverage guard

Task completed:

- Added automated source coverage for Stock RLS policy expectations.
- Wired the RLS policy guard into `npm.cmd run smoke`.
- Updated stock QA docs and evidence templates so acceptance test 13 includes Stock RLS policy coverage.

Files changed:

- `package.json`
- `scripts/stock-rls-policy-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-rls-policy-coverage.mjs` checks:
  - stock hardening migrations do not create broad `FOR ALL` policies;
  - `can_administer_stock()` still resolves through admin/director scope;
  - stock location-scope helper functions remain present;
  - key stock table policies remain split by select, insert, update, and delete;
  - scoped stock policies still use stock-location or stock-unit access helpers;
  - stock take, damage, and return-supplier approval policies keep manager/director gates;
  - delete policies for stock master/workflow tables use `can_administer_stock()` and do not grant delete through operator or manager helpers.
- `npm.cmd run smoke` now runs this script after route, workflow regression, stock acceptance, state-transition, migration safety, role/scope, and scanner coverage checks.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is static SQL-source coverage. It does not replace real Supabase RLS tests with scoped Auth users.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then execute real RLS allowed/denied tests from `docs/STOCK_QA_RUNBOOK.md` and record results in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock seed coverage and QA fixtures

Task completed:

- Improved `supabase/seed.sql` with Stock QA fixtures for approval and scan workflows.
- Added automated source coverage so `npm.cmd run smoke` fails if key Stock seed fixtures are removed.
- Updated Stock QA docs with the exact seeded fixture identifiers for manual Supabase testing.

Files changed:

- `supabase/seed.sql`
- `package.json`
- `scripts/stock-seed-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `docs/DATA_MODEL.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this seed/test slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Added seeded barcode stock units:
  - `EM-SEED-DAMAGE-001`;
  - `EM-SEED-DAMAGE-APPROVE-001`;
  - `EM-SEED-RETURN-SUPPLIER-001`;
  - `EM-SEED-STOCK-TAKE-001`.
- Added seeded workflow rows:
  - `DMG-SEED-SUBMITTED-001` for manager damage review;
  - `DMG-SEED-REVIEWED-001` for director damage approval;
  - `RS-SEED-SUBMITTED-001` for manager return-supplier approval;
  - `ST-SEED-DRAFT-001`, `ST-SEED-SUBMITTED-001`, and `ST-SEED-REVIEWED-001` for stock-take scan/review/approval QA.
- New script `scripts/stock-seed-coverage.mjs` checks Stock seed fixtures for:
  - demo outlets, Stock module access, and Clement admin/director test access;
  - item master and default-brand setup;
  - global barcode weight rule setup;
  - outbound, inspection, damage, return-supplier, and stock-take barcodes;
  - ready pickup/delivery order fixtures;
  - idempotent seed patterns.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is static seed coverage. It does not prove the seed executes successfully on a fresh Supabase project.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Apply migrations and seed on a fresh Supabase staging project, then execute `docs/STOCK_QA_RUNBOOK.md` using the listed fixture identifiers and record results in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock security and no-barcode MVP guard

Task completed:

- Added automated source coverage for Stock service-role secret safety and no-barcode MVP workflow rules.
- Wired the guard into `npm.cmd run smoke`.
- Updated Stock QA docs so automated acceptance coverage includes the security/no-service-role guard.

Files changed:

- `package.json`
- `scripts/stock-security-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-security-coverage.mjs` checks:
  - app/component/lib source files do not reference Supabase service-role credentials;
  - Supabase clients use `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`;
  - `/stock/no-barcode-inbound` redirects to `/stock/inbound`;
  - `noBarcodeInboundAction` blocks new loose no-barcode stock creation and does not insert into `no_barcode_stock`;
  - the sidebar does not advertise no-barcode inbound as an active Stock workflow;
  - worker-facing Stock UI explains the label-first flow and does not expose `Save no-barcode inbound`;
  - legacy no-barcode data remains visible only as legacy stock visibility.

Commands run and results:

- `npm.cmd run smoke` - passed after aligning the guard with existing Stock UI copy and data-layer labels.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is static source coverage. It does not replace environment review on the deployed host.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then execute real Supabase and device QA from `docs/STOCK_QA_RUNBOOK.md`.

## 2026-06-12 - Stock item-master coverage and local route probe

Task completed:

- Added automated source coverage for the Stock item-master contract.
- Ran a local dev-server HTTP probe for Stock routes; routes responded with unauthenticated redirects, confirming the dev server and route/proxy layer responded but not proving authenticated page rendering.
- Updated Stock QA docs so automated acceptance coverage includes item-master coverage.

Files changed:

- `package.json`
- `scripts/stock-item-master-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-item-master-coverage.mjs` checks:
  - legacy non-numeric item-code conversion and numeric-only constraint migration;
  - category + default brand + section/name uniqueness migration;
  - all ERP roles can create/edit item master while delete remains admin/director;
  - create/update actions preserve item code, category, default brand, section/name, Chinese/Iban names, barcode requirement, active status, and low-stock level;
  - duplicate item-code guard remains in server actions;
  - inactive items remain blocked from new inbound stock;
  - item-master form has numeric item-code controls and required worker-facing controls;
  - item-code and label-generation helper regression coverage remains wired.
- Local route probe:
  - Started Next dev server locally.
  - Checked `/stock`, `/stock/dashboard`, `/stock/items`, `/stock/inbound`, `/stock/outbound`, `/stock/transfer`, `/stock/receive-transfer`, `/stock/return`, `/stock/no-barcode-inbound`, `/stock/balance`, `/stock/movements`, `/stock/stock-take`, `/stock/reports`, and `/stock/settings`.
  - All returned HTTP `307` without an authenticated browser session, which is expected for protected routes. This does not replace authenticated browser QA.

Commands run and results:

- `npm.cmd run smoke` - passed after aligning the guard with the current duplicate item-code message.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated browser rendering was not verified because no logged-in browser session was available to the automation layer.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then execute authenticated Stock route/browser QA with seeded users and record evidence in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock label coverage guard

Task completed:

- Added automated source coverage for generated barcode labels and label print/reprint surfaces.
- Wired the label guard into `npm.cmd run smoke`.
- Updated Stock QA docs so automated acceptance coverage includes label coverage.

Files changed:

- `package.json`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-label-coverage.mjs` checks:
  - generated internal barcodes use date + numeric item code + weight in grams + serial;
  - generated barcode helper does not include `KG` text;
  - generated barcodes are numeric-only and reject non-numeric item codes;
  - inbound label printing uses 50mm x 30mm print CSS and supports multiple labels;
  - inbound has `Generate label barcode`, recent labels, and PDF/print controls;
  - stock-unit detail page has 50mm x 30mm label preview and `Print / Export PDF label` reprint.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is static source coverage. It does not replace real browser print/PDF preview testing with a printer/PDF target.
- Real Supabase/RLS and phone scanner QA remain required before the Stock module can be called complete.

Next recommended task:

- Run the full local command set, then manually test inbound label export and stock-unit label reprint in a browser.

## 2026-06-12 - Stock completion audit document

Task completed:

- Created a conservative Stock completion audit mapping every acceptance test to current automated/source evidence and manual evidence still required.
- Linked the audit from the Stock QA runbook, evidence log, and module status.

Files changed:

- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this documentation slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New `docs/STOCK_COMPLETION_AUDIT.md` documents:
  - local evidence currently available;
  - what `npm.cmd run smoke` covers;
  - each of the 13 Stock acceptance tests;
  - the current automated/source evidence for each test;
  - the manual Supabase/browser/device evidence still required;
  - core Stock rule audit;
  - item-master audit;
  - the remaining completion gate.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- The audit confirms the Stock module remains partial until real Supabase/RLS, authenticated browser, print/PDF, and phone/laptop scanner evidence is recorded.

Next recommended task:

- Execute the fresh Supabase migration/seed and real Stock QA pass, then fill `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock-take selected-scope lock coverage

Task completed:

- Added automated source coverage for the Stock take selected item+brand lock rule.
- Improved Stock take worker guidance so staff can see which operations are blocked for the locked scope.
- Wired the stock-take lock guard into `npm.cmd run smoke`.

Files changed:

- `components/stock/workflow-forms.tsx`
- `package.json`
- `scripts/stock-take-lock-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard/UI-copy slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-take-lock-coverage.mjs` checks:
  - `assertStockNotLockedByTake` filters open stock take sessions by location and only blocks matching item+brand;
  - inbound, order outbound, direct outbound, transfer, receive-transfer, return, inspection release, damage request, and return-supplier request call the stock-take lock helper;
  - stock-take barcode scanning rejects wrong item, wrong brand, and wrong location;
  - worker UI explains that inbound, outbound, transfer, return, and damage requests for that exact scope are blocked.

Commands run and results:

- `npm.cmd run smoke` - passed after aligning the guard with helper delegation and JSX text.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is source/UI-copy coverage. Real Supabase QA still needs to prove the lock blocks only the selected item+brand in the selected location and leaves unrelated stock movable.

Next recommended task:

- Execute the stock-take lock manual tests from `docs/STOCK_QA_RUNBOOK.md` and record evidence in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock report coverage guard

Task completed:

- Added automated source coverage for Stock report/export requirements.
- Wired the report guard into `npm.cmd run smoke`.
- Updated Stock QA docs so automated acceptance coverage includes report coverage.

Files changed:

- `package.json`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New script `scripts/stock-report-coverage.mjs` checks:
  - report data sources include stock balance, inbound age, stock take variance, damage/spoilage, return supplier, and age thresholds;
  - `/stock/reports` builds CSV and WhatsApp summary data;
  - `ReportToolbar` receives CSV and WhatsApp content;
  - report page is print-ready;
  - CSV escaping and WhatsApp summary regression coverage remains wired.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is source/helper coverage. Real browser CSV download, print/PDF, and WhatsApp copy still need manual evidence.

Next recommended task:

- Browser-test `/stock/reports` with seeded data and record CSV/print/WhatsApp evidence in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock Details Part 2 rule review

Task completed:

- Reviewed the confirmed Stock Details Part 2 rules for outbound, transfer, damage/spoilage, customer return, failed delivery return, stock take, reports, permissions, and scanner UX.
- Confirmed the current business/data docs already capture the main workflow rules.
- Updated `AGENTS.md` where the short agent guide still described barcode rules as item+brand instead of item+brand+origin and clarified manager/director approval and stock-take lock rules for future agents.

Files changed:

- `AGENTS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this documentation review.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Future agents are now pointed at:
  - barcode rules saved by item + brand + origin;
  - damage/spoilage staff photo request -> manager review -> director approval;
  - transfer outbound `TRANSFER_PENDING` and no cancellation after scanned out;
  - customer returns through inspection, failed-delivery barcode returns to `IN_STOCK`;
  - stock-take locks only the selected item+brand at the selected location.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This was a documentation alignment pass. The workflows still need real Supabase/browser/device evidence before final Stock completion.

Next recommended task:

- Run the full local checks, then manually QA outbound batch mode, transfer receive, damage approval, stock take lock, and report export against seeded Supabase data.

## 2026-06-12 - Stock scanner continuous mode and feedback

Task completed:

- Added optional continuous camera scanning to the reusable Stock barcode scanner.
- Added success feedback for scans using vibration and a short browser beep where supported.
- Enabled continuous scan mode for:
  - barcode inbound auto-save;
  - outbound batch scan list;
  - stock-take barcode recording.
- Updated scanner source coverage and Stock QA docs for repeated scan and feedback evidence.

Files changed:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this scanner UX slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `BarcodeScanner` now keeps a stable detection callback, supports `continuous`, debounces the same barcode for 1.5 seconds in continuous mode, and stops the camera stream on close.
- `BarcodeField` exposes `continuousScan`.
- Inbound scanning can keep the camera open while auto-saving decoded scans.
- Outbound batch scanning can add multiple barcodes without reopening the scanner.
- Stock take scanning can submit each detected barcode from the scanner workflow.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is source/browser-build verified only. Real phone and laptop camera tests still need to prove permission prompts, rear camera preference, repeated scan behavior, success feedback, and camera shutoff.

Next recommended task:

- Run `/stock/inbound`, `/stock/outbound`, and `/stock/stock-take` on a real phone over HTTPS/localhost tunnel and record continuous scanner evidence in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Stock scanner recent scan list

Task completed:

- Changed the reusable Stock barcode field from one recent scan value to a recent scan list.
- The list keeps the five most recent unique scanned barcodes and wraps on mobile widths.
- Updated scanner coverage and QA docs to verify the recent scan list requirement.

Files changed:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/manual-qa-checklist.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this scanner UI slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `BarcodeField` now stores `recentScans`, de-duplicates repeated values, keeps the newest five entries, and renders them as compact barcode chips.
- Scanner coverage now guards `Recent scans` and the five-entry list behavior.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- This is source/build verified only until a real phone and laptop confirm the list updates after physical scans.

Next recommended task:

- Device-test the scanner routes and record whether recent scan chips remain readable at about 390px width.

## 2026-06-12 - Outbound blocked-status pre-confirm warning

Task completed:

- Added a shared outbound helper that returns a staff-facing block reason when a scanned barcode is not in an available stock status.
- Added a pre-confirm warning in the outbound batch screen for known unavailable barcode statuses such as `TRANSFER_PENDING` and `DAMAGED`.
- Kept the server/RPC as the final enforcement layer; this is an early worker-facing warning, not a replacement for server validation.
- Updated regression and acceptance coverage plus Stock QA docs.

Files changed:

- `lib/stock/outbound-rules.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this outbound UI/rule slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `outboundUnitBlockReason` returns a clear message such as `Barcode EM-002 is TRANSFER_PENDING and cannot be outbounded.`
- The outbound batch form lists blocked known barcodes before confirm.
- Regression coverage verifies the helper matches active stock status rules for available and unavailable statuses.
- Acceptance coverage now checks the blocked-scan warning text for order outbound.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to scan missing, already-outbounded, wrong-status, and wrong-location barcodes and confirm both UI warnings and server/RPC blocks.

Next recommended task:

- Use seeded unavailable barcode fixtures in `/stock/outbound` and record blocked-scan evidence in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-12 - Outbound missing-barcode pre-confirm warning

Task completed:

- Added a pre-confirm missing-barcode warning in the outbound batch screen.
- Confirmation is disabled until missing or blocked scans are removed from the batch.
- Updated acceptance coverage and Stock QA docs for the missing-barcode warning.

Files changed:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this outbound UI slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/outbound` now lists scanned barcodes that are not present in the loaded stock-unit list under `Missing barcode scan`.
- The confirm button is disabled while missing or blocked scans remain in the batch.
- The existing server/RPC validation remains the final enforcement layer if the UI is bypassed or stale.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to scan a truly missing barcode and confirm the UI warning plus server-side `Barcode was not found.` behavior if submitted through a stale/bypassed UI.

Next recommended task:

- Browser-test `/stock/outbound` with one valid barcode, one missing barcode, and one unavailable-status barcode to record warning and disable-state evidence.

## 2026-06-12 - Inbound duplicate auto-save prevention

Task completed:

- Tightened barcode inbound duplicate handling for camera auto-save mode.
- A locally known duplicate barcode now shows a clear duplicate warning and does not auto-submit after camera detection.
- Manual save remains disabled while the known duplicate barcode is in the field.
- Server/RPC duplicate validation remains the final enforcement layer.

Files changed:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this inbound UI guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Added `isDuplicateInboundBarcode` in the inbound form.
- `handleBarcodeChange(value, true)` now stops before `requestSubmit()` when the scanned barcode already exists in the loaded stock unit list.
- Acceptance coverage checks the local duplicate warning, auto-save prevention, manual save disable, and server duplicate rejection strings.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to prove the database unique constraint/RPC prevents duplicates when the client list is stale.

Next recommended task:

- Browser-test `/stock/inbound` with auto-save on by scanning an existing barcode and confirm no new inbound submit happens before server validation.

## 2026-06-12 - Stock take barcode-only enforcement

Task completed:

- Removed the manual stock-take count entry form from the Stock take UI.
- Added a barcode-only guidance card so workers know count lines are created by scanning barcode units.
- Changed the legacy `addStockTakeLineAction` server action to return a clear error if called directly.
- Updated stock-take coverage and QA docs to guard barcode-only count behavior.

Files changed:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/actions.ts`
- `scripts/stock-take-lock-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this stock-take UI/action guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/stock-take` no longer renders `Add actual stock count` / `Add count line`.
- `addStockTakeLineAction` now returns: `Stock take is barcode scanning only. Scan each barcode instead of adding manual count lines.`
- `scripts/stock-take-lock-coverage.mjs` now fails if the manual count form returns.
- Acceptance coverage now checks barcode-only UI and server-action behavior.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to prove scanned barcode lines, manager review, director approval, and `STOCK_TAKE_ADJUSTMENT` movement application against seeded data.

Next recommended task:

- Browser-test `/stock/stock-take` with seeded sessions and prove workers can only add count lines by barcode scan.

## 2026-06-12 - Stock take empty submit guard

Task completed:

- Added a server-side guard so a draft stock take session cannot be submitted for manager review until it has at least one scanned barcode line.
- Updated stock-take source coverage and QA docs for the empty-session blocked path.

Files changed:

- `lib/stock/actions.ts`
- `scripts/stock-take-lock-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this stock-take action guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100050_atomic_barcode_inbound_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `submitStockTakeAction` now counts `stock_take_lines` for the selected session before submission.
- Empty sessions return `Scan at least one barcode before submitting stock take for review.`
- Coverage now guards the count query and staff-facing error text.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to prove the empty-submit block and the normal scanned-session submit path with seeded users.

Next recommended task:

- Browser-test one empty draft stock take session and one draft session with a scanned barcode line.

## 2026-06-12 - Stock take empty review and approval guard

Task completed:

- Added app-side guards so manager review and director approval require at least one scanned stock-take line.
- Added migration `202606100051_stock_take_approval_requires_lines_v1.sql` to update `public.approve_stock_take_session` with the same no-empty-approval guard for direct RPC calls.
- Updated stock-take coverage, migration safety coverage, and stock docs for the new migration order.

Files changed:

- `lib/stock/actions.ts`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-state-transition-coverage.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/DATA_MODEL.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`
- `supabase/migrations/202606100051_stock_take_approval_requires_lines_v1.sql`

Migration SQL added:

- `supabase/migrations/202606100051_stock_take_approval_requires_lines_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100051_stock_take_approval_requires_lines_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `reviewStockTakeAction` and `approveStockTakeAction` now call `requireStockTakeLineCount`.
- `public.approve_stock_take_session` now raises `Scan at least one barcode before approving stock take.` when a reviewed session has no lines.
- The approval RPC audit payload now includes `lineCount`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to apply migration `051` and test empty submit, empty review, empty direct approval, plus the normal scanned-session approval path.

Next recommended task:

- Run fresh Supabase migrations through `051`, seed, then test stock-take empty and scanned approval paths.

## 2026-06-12 - Damage and return-supplier approval app-side guards

Task completed:

- Added app-side status and stock-location scope checks before damage director approval calls the atomic RPC.
- Added app-side status and stock-location scope checks before return-supplier manager approval calls the atomic RPC.
- Kept the existing database RPC checks as the final enforcement layer.
- Updated acceptance, role/scope coverage, and QA docs.

Files changed:

- `lib/stock/actions.ts`
- `scripts/stock-role-scope-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/MODULE_STATUS.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this action guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100051_stock_take_approval_requires_lines_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- Damage approval now rejects anything other than `MANAGER_REVIEWED` before RPC call and checks stock-location access.
- Return-supplier approval now rejects anything other than `SUBMITTED` before RPC call and checks stock-location access.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to prove cross-location denial and stale-status denial with seeded manager/director users.

Next recommended task:

- Test damage and return-supplier approval with wrong-status and wrong-location requests in staging Supabase.

## 2026-06-12 - Stock take barcode variance approval

Task completed:

- Fixed the barcode-only stock-take approval gap where a reviewed session with scanned lines could approve with no adjustment even when expected barcodes in the selected item+brand+location were not scanned.
- Added an idempotent migration that replaces `public.approve_stock_take_session`.
- Updated stock-take smoke/source coverage and QA docs for missing-barcode variance behavior.

Files changed:

- `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-state-transition-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/manual-qa-checklist.md`
- `docs/workflow-acceptance-checklist.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `public.approve_stock_take_session` still requires a reviewed session, director/admin permission, matching stock-location scope, a director signature, and at least one scanned barcode line.
- During director final approval, the RPC now compares active expected barcode stock units in the session location/item/brand with scanned `stock_take_lines`.
- Missing expected barcodes are inserted as variance lines with `system_count = 1`, `actual_count = 0`, `system_weight_kg = stock_units.net_weight_kg`, and `actual_weight_kg = 0`.
- Missing barcode stock units are updated to `ADJUSTED_OUT`.
- Missing barcode adjustments write `STOCK_TAKE_ADJUSTMENT` movements and `barcode_scan_logs`.
- The approval audit payload now includes `missingCount` and `barcodeVarianceComputed`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase QA still needs to prove the migration runs on a fresh project and that a director-approved stock take with one intentionally missed expected barcode creates the variance line, sets the stock unit to `ADJUSTED_OUT`, and writes the adjustment movement/log.
- Real RLS QA still needs manager/director users for cross-location approval denial and happy-path approval.

Next recommended task:

- Run fresh Supabase migrations through `052`, seed, then manually QA stock take with: empty session blocked, scanned session approval, wrong-location scan blocked, and missing expected barcode adjusted out only after director approval.

## 2026-06-12 - Stock dashboard today movement KPIs

Task completed:

- Added `Today inbound` and `Today outbound` KPI cards to the Stock dashboard data builder.
- Today inbound includes inbound, transfer received, return, and legacy no-barcode inbound movements for the current date.
- Today outbound includes sales, transfer, processing, damage/spoilage, supplier return, and legacy no-barcode outbound movements for the current date.
- Updated stock coverage and QA docs so these KPIs are part of the Stock Module acceptance surface.

Files changed:

- `lib/stock/data.ts`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this dashboard KPI slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `buildDashboard` now calculates current-day inbound and outbound movement weights from already loaded `stock_movements`.
- Stock report/acceptance coverage now checks that the dashboard data source includes today inbound/outbound KPI labels and helper logic.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real browser QA still needs to verify the KPI cards are readable at phone width and match seeded/current-day movement rows.
- Real Supabase QA remains required for RLS, scanner devices, migration execution, and stock workflow evidence.

Next recommended task:

- Browser-test `/stock/dashboard` at desktop and 390px mobile width, then continue with seeded Supabase QA for stock inbound/outbound/transfer/stock-take workflows.

## 2026-06-12 - Stock local route protection check

Task completed:

- Started the local Next dev server inside a verification script.
- Requested key Stock routes without an authenticated session.
- Confirmed the routes redirect to `/login` instead of rendering protected Stock content.
- Recorded the result in `docs/STOCK_QA_EVIDENCE.md`.

Files changed:

- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this route-protection QA slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `/stock/dashboard`, `/stock/inbound`, `/stock/reports`, and `/stock/stock-take` returned HTTP `307` with `Location: /login` when requested locally without an authenticated session.

Commands run and results:

- Local route probe - passed for unauthenticated route protection.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated visual/browser QA was not completed because the in-app browser automation failed with a Windows sandbox permission error: `CreateProcessAsUserW failed: 5`.
- Phone/laptop camera QA still needs manual testing with an authenticated session.

Next recommended task:

- Use an authenticated local browser session to test `/stock/dashboard`, `/stock/inbound`, `/stock/outbound`, `/stock/reports`, and `/stock/stock-take` at desktop and 390px phone width.

## 2026-06-12 - Stock dashboard workflow shortcuts

Task completed:

- Added Stock dashboard shortcut buttons for daily worker workflows.
- Shortcuts link to inbound, outbound, transfer, receive-transfer, and return.
- Used icons from `lucide-react` and responsive wrapping so the controls stay usable on phone width.
- Updated Stock coverage and docs to guard the shortcut surface.

Files changed:

- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `TASKS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this dashboard shortcut slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `DashboardView` now renders `StockShortcutButtons` below the KPI cards.
- The shortcut strip includes `Inbound`, `Outbound`, `Transfer`, `Receive`, and `Return`.
- `scripts/stock-report-coverage.mjs` now checks the shortcut component and route links.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated visual QA still needs to verify the shortcuts at desktop and 390px mobile width.
- Real phone/laptop scanner QA and live Supabase/RLS workflow QA are still required before the Stock Module goal can be considered complete.

Next recommended task:

- With an authenticated session, browser-test `/stock/dashboard` shortcuts and then run the stock workflow QA paths from `docs/STOCK_QA_RUNBOOK.md`.

## 2026-06-12 - Stock dashboard shortcut role alignment

Task completed:

- Aligned Stock dashboard workflow shortcut visibility with existing stock operator route/action permissions.
- Director/view-only Stock users can still view the dashboard, KPIs, alerts, charts, and reports, but no longer see routine operation shortcuts that lead to blocked inbound/outbound/transfer/receive/return pages.
- Updated Stock coverage and QA docs for this role behavior.

Files changed:

- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this dashboard role-alignment slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `DashboardView` now receives `canOperateStock`.
- `StockShortcutButtons` only renders when `canOperateStock` is true.
- Coverage checks now verify the dashboard shortcut role guard is present.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated browser QA still needs to prove directors do not see routine operation shortcuts and stock operators/admins do see them.
- Real phone/laptop scanner QA and live Supabase/RLS workflow QA are still required before the Stock Module goal can be considered complete.

Next recommended task:

- Browser-test Stock dashboard with one director user and one stock-operator/admin user, then continue seeded workflow QA from `docs/STOCK_QA_RUNBOOK.md`.

## 2026-06-12 - Internal barcode label uniqueness guard

Task completed:

- Hardened generated internal barcode labels so they do not wrap 4-digit serial numbers and do not knowingly reuse an existing barcode.
- Added a helper that finds the next unused label barcode from current stock units and labels generated in the current inbound session.
- Updated Barcode Inbound to generate an unused internal numeric barcode before printing/saving.
- Updated regression and coverage checks for the uniqueness behavior.

Files changed:

- `lib/stock/barcode-label.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `docs/BUSINESS_RULES.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this label uniqueness slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `makeInternalBarcode` now rejects serials outside `1..9999` instead of wrapping with modulo.
- New `makeUniqueInternalBarcode` skips existing stock-unit barcodes and recent labels.
- Barcode Inbound uses `makeUniqueInternalBarcode` and shows a clearer error if a label cannot be generated.
- Regression now checks no serial wraparound and skip-to-next-serial behavior.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Server-side duplicate barcode guards still remain the final enforcement layer and need real Supabase QA.
- Browser QA still needs to prove the printed label flow works after refresh and at phone width.

Next recommended task:

- In an authenticated browser, generate two labels for the same item/weight, refresh the page, generate another label, and confirm it skips existing stock-unit barcodes before printing.

## 2026-06-12 - Outbound same-destination transfer precheck

Task completed:

- Added a pre-confirm outbound batch warning for transfer scans where a scanned barcode is already at the selected destination.
- The confirm button now stays disabled until those barcodes are removed or a different destination is selected.
- Kept the existing server/RPC same-destination transfer guard as final enforcement.
- Updated source coverage and QA docs for this blocked path.

Files changed:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-workflow-regression.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this outbound UI precheck slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `OutboundSalesForm` computes `sameDestinationTransferUnits` for transfer batches.
- Same-destination transfer scans now show `Invalid transfer destination` with the affected barcodes.
- `confirmDisabled` includes same-destination transfer scans.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated browser QA still needs to verify the warning with seeded barcode units and locations.
- Real Supabase/RLS QA remains required for the RPC-side transfer denial.

Next recommended task:

- In an authenticated browser, test `/stock/outbound` transfer mode by selecting a destination that matches a scanned barcode's current location and confirm the UI blocks before submit.

## 2026-06-12 - Item master update no-row guard

Task completed:

- Tightened item master update behavior so stale/tampered item ids cannot report a false success.
- `updateItemAction` now selects the updated item id and requires a returned row.
- Added item-master coverage for the no-row/stale-id guard.

Files changed:

- `lib/stock/actions.ts`
- `scripts/stock-item-master-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this item-master action guard slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `updateItemAction` now calls `.select("id").single()` after update.
- If Supabase returns no row, the action returns `Item was not found or your role cannot update it.`

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated Supabase QA still needs to prove item update success, duplicate code rejection, stale-id failure, and inactive-item inbound blocking with real RLS.

Next recommended task:

- In an authenticated browser, test item master create/edit with duplicate item codes, inactive item inbound blocking, and a stale item id via direct action/RLS QA if practical.

## 2026-06-12 - Stock unit movement history linkage

Task completed:

- Improved stock-unit detail movement history so it includes movements linked by `stock_unit_id` as well as matching barcode text.
- Added `stockUnitId` to the `StockMovement` data contract.
- Updated demo movement data and coverage for the linkage.

Files changed:

- `lib/stock/types.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `scripts/stock-label-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this stock-unit detail data-contract slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `mapMovement` now reads `stock_movements.stock_unit_id`.
- `getStockUnitDetailData` now includes movement rows where `movement.stockUnitId === unit.id` or `movement.barcode === unit.barcode`.
- Demo movement rows now include `stockUnitId` values.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated browser QA still needs to open `/stock/units/[id]` for a real stock unit and confirm movement history appears for rows linked by stock unit id.

Next recommended task:

- In Supabase QA, create or inspect one stock movement with `stock_unit_id` and no barcode text, then confirm the stock-unit detail page still shows it.

## 2026-06-12 - Stock report linked-unit weights

Task completed:

- Improved Stock report rows for damage/spoilage and return-supplier workflows.
- These report rows now include linked barcode stock-unit weight when the request references an existing stock unit.
- Updated report coverage and QA docs.

Files changed:

- `lib/stock/data.ts`
- `scripts/stock-report-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this report data slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `buildReports` now looks up `units.find((unit) => unit.id === request.stockUnitId)` for damage/spoilage and return-supplier report rows.
- Report row `weightKg` now uses the linked unit `netWeightKg` when available, instead of always `0`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated Supabase QA still needs to verify `/stock/reports` CSV/print/WhatsApp output against real damage and return-supplier requests.

Next recommended task:

- In seeded Supabase QA, approve or inspect one damage request and one return-supplier request, then confirm their report rows include the barcode unit weight.

## 2026-06-13 - Continuous inbound scan count and weight feedback

Task completed:

- Improved the Stock Module continuous barcode inbound worker screen.
- The inbound form now always shows the current session's saved scan count and saved total weight.
- Recent successful inbound scans are labelled as recent inbound scans while still supporting 50mm x 30mm label PDF/print output.
- Added source coverage so the stock label/inbound guard checks for the scan count and weight feedback.
- Updated stock rules/status/QA docs for the inbound session summary.

Files changed:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-label-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration is needed for this UI/coverage slice.
- Existing stock migrations should still be run in filename order through `supabase/migrations/202606100052_stock_take_barcode_variance_rpc_v1.sql`, then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- `BarcodeInboundForm` now derives `recentInboundCount` and `recentInboundWeightKg` from the successful inbound labels for the current session.
- The form displays `Saved scans` and `Saved weight` cards on mobile and desktop.
- The recent label panel is now titled `Recent inbound scans` and still explains label PDF/print output.
- `scripts/stock-label-coverage.mjs` now guards the saved scan count/weight UI strings.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Authenticated browser QA still needs to confirm the count/weight increments after real Supabase saves on `/stock/inbound`.
- Real phone/laptop camera QA still needs to confirm continuous scans update the recent scan list and saved weight at 390px phone width.

Next recommended task:

- In a seeded Supabase staging project, test `/stock/inbound` with three different barcodes under the same item/brand/origin/location preset and confirm stock unit, movement, scan log, recent list, saved scan count, saved weight, and label print output all match.

## 2026-06-13 - Stock inbound templates, session summary, and undo

Task completed:

- Improved `/stock/inbound` for the preferred mobile worker flow.
- Added recent inbound templates at the top of the form, showing simple brand + product labels.
- Added product search and quick product creation inside inbound. New quick-created products use the next numeric item code automatically.
- Defaulted inbound location from the signed-in user's assigned stock location while keeping it editable under normal location restrictions.
- Auto-generates an inbound batch number when the page opens; no Start Batch step is required.
- Keeps inbound source defaulted to Supplier/import.
- Enforces required brand and origin on the server for barcode inbound.
- Uses the newest matching item+brand+origin barcode weight rule automatically.
- Blocks camera scans with no confident weight and tells workers to use generated label printing instead of saving the supplier barcode.
- Shows previous saved scan weight, current-session saved scan count, saved total weight, recent scan list with barcode/product/weight/time/scanned-by/location/status, and duplicate/error scans.
- Added Finish Inbound Session summary with total barcode units, total weight, item/brand/origin, location, scanned by, start/end time, and duplicate/error count.
- Added current-session undo that calls an atomic RPC to void the stock unit and write `INBOUND_VOID`, scan-log, and audit-log rows instead of deleting data.

Files changed:

- `components/stock/workflow-forms.tsx`
- `components/stock/stock-page.tsx`
- `lib/stock/action-state.ts`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-migration-safety.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`

Exact migration order:

- Run all existing migrations in filename order through `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`.
- Then run `supabase/seed.sql`.
- No migration has been run against the live Supabase project by Codex.

What changed:

- New `VOIDED` stock-unit status and `INBOUND_VOID` movement type.
- New `public.void_inbound_stock_unit(uuid, text, text)` RPC.
- The undo RPC checks auth, stock access, location access, active unmoved status, current batch number, and original inbound movement before voiding.
- Undo updates `stock_units.status = VOIDED`, writes a negative `INBOUND_VOID` stock movement, writes a barcode scan log, and writes a `BARCODE_INBOUND_VOID` audit log.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real Supabase/RLS QA is needed for the new undo RPC with worker, manager, admin, and cross-location users.
- The app hides undo after Finish Inbound Session, but a deeper persisted inbound-session table would be needed to enforce "worker undo only before finish" at database level across browser refreshes.
- The requested separate barcode label printing page is not split out yet; label generation remains in `/stock/inbound` and stock-unit reprint remains at `/stock/units/[id]`.
- Real phone/laptop camera QA is still needed for 390px scanning, continuous auto-save, sound/vibration, and finish summary.

Next recommended task:

- Apply migrations through `053` in a staging Supabase project, then run `/stock/inbound` on a phone with one saved-rule template, one no-rule template, one duplicate barcode, one no-weight barcode, and one undo before finish.

## 2026-06-13 - Stock inbound remote QA and deploy handoff

Task completed:

- Reviewed the latest Stock Inbound changes for remote Vercel QA.
- Confirmed migration `202606100053_stock_inbound_session_undo_v1.sql` is idempotent at the object level, uses no destructive table/data drops, and keeps auth/stock/location checks inside the security-definer RPC.
- Added a remote QA checklist for testing Stock Inbound through the deployed Vercel app.
- Confirmed no frontend app code references Supabase service-role credentials; only local guard scripts/docs mention service-role checks.

Files changed:

- `docs/REMOTE_QA_STOCK_INBOUND.md`
- `HANDOFF.md`

Migration SQL added:

- No new migration in this deploy-prep slice.
- Existing migration `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql` still needs to be applied before undo works in the deployed app database.

Exact migration order:

- Run all migrations in filename order through `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`.
- Then run `supabase/seed.sql` if reseeding a demo/staging database.
- Codex did not run live Supabase SQL.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Git push and Vercel deployment verification happen after this handoff entry if commit/push succeeds.
- Production/staging Supabase still needs migration `053` applied manually or by the configured deployment process.
- Remote phone camera QA remains manual through `https://elite-meat-erp.vercel.app/stock/inbound`.

Next recommended task:

- After Vercel deploys the pushed branch, run the first 10 steps in `docs/REMOTE_QA_STOCK_INBOUND.md`.

## 2026-06-13 - Supabase migration handoff for Stock Inbound QA

Task completed:

- Inspected local Supabase configuration before attempting live database work.
- Confirmed local `.env.local` points to `https://aikfqnbsshflbtuakwrz.supabase.co`.
- Confirmed Supabase CLI is not installed in this environment.
- Confirmed no local Supabase link metadata exists in `supabase/config.toml` or `supabase/.temp`.
- Did not run live Supabase migrations because the CLI project link and remote migration history could not be verified safely.
- Created exact SQL Editor migration handoff through migration `053`.

Files changed:

- `docs/SUPABASE_MIGRATION_HANDOFF.md`
- `HANDOFF.md`

Migration SQL added:

- None in this handoff slice.
- Existing required latest migration remains `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`.

Exact migration order:

- Run every migration in filename order through `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`.
- Then run `supabase/seed.sql` only for a safe demo/staging reseed.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Remote migration history could not be checked because Supabase CLI is unavailable and no safe database admin connection is configured.
- Migration `053` still needs to be applied to the target Supabase project before Stock Inbound undo works in Vercel.

Next recommended task:

- In Supabase SQL Editor for `https://aikfqnbsshflbtuakwrz.supabase.co`, follow `docs/SUPABASE_MIGRATION_HANDOFF.md`, then test `/stock/inbound` on Vercel.

## 2026-06-13 - Supabase CLI local setup attempt

Task completed:

- Checked Node version: `v24.16.0`, which satisfies the Node 20+ requirement.
- Installed Supabase CLI as a local dev dependency with `npm.cmd install supabase --save-dev`.
- Confirmed local CLI version with `npx.cmd supabase --version`: `2.106.0`.
- Ran `npx.cmd supabase init` because `supabase/config.toml` was missing.
- Confirmed target project ref before link attempt: `aikfqnbsshflbtuakwrz`.
- Attempted link only to the confirmed target: `npx.cmd supabase link --project-ref aikfqnbsshflbtuakwrz`.
- Link did not complete because the CLI needs authentication.
- Did not run `supabase db push`.
- Did not run `supabase db seed`.

Files changed:

- `package.json`
- `package-lock.json`
- `supabase/config.toml`
- `supabase/.gitignore`
- `docs/SUPABASE_MIGRATION_HANDOFF.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- Still run every migration in filename order through `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`.
- Then run `supabase/seed.sql` only for safe demo/staging data.

Commands run and results:

- `node --version` - passed, `v24.16.0`.
- `npm.cmd install supabase --save-dev` - passed after rerun with registry/cache access.
- `npx.cmd supabase --version` - passed, `2.106.0`.
- `npx.cmd supabase init` - passed.
- `npx.cmd supabase link --project-ref aikfqnbsshflbtuakwrz` - stopped with `Access token not provided`.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Supabase login/access token is required before the project can be linked and migration status can be checked.
- Migration `053` still has not been applied to the target project.

Next exact command:

- `npx.cmd supabase login`
- Then `npx.cmd supabase link --project-ref aikfqnbsshflbtuakwrz`
- Then `npx.cmd supabase migration list`

## 2026-06-14 - Safe Stock Module QA preparation while Supabase login is blocked

Task completed:

- Prepared owner-facing Stock Module manual QA documents for later Vercel and local testing.
- Added a Stock migration checklist for migration `053` and the Supabase CLI login/link gate.
- Added a smoke coverage script that verifies the new owner QA docs, Stock Inbound undo migration expectations, label printing coverage, migration safety coverage, and no service-role frontend guard coverage.
- Did not run live Supabase migrations.
- Did not run `supabase db push`.
- Did not run seed data.
- Did not deploy production.

Files changed:

- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MIGRATION_CHECKLIST.md`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `package.json`
- `HANDOFF.md`

Migration SQL added:

- None.

Exact migration order:

- No new migration was created in this pass.
- Existing migration order remains every migration in filename order through `supabase/migrations/202606100053_stock_inbound_session_undo_v1.sql`.
- Then run `supabase/seed.sql` only for a safe demo/staging reseed.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Supabase CLI still needs owner login with `npx.cmd supabase login`.
- Remote migration history still cannot be checked until CLI login/link succeeds.
- Migration `053` still needs to be applied to the target Supabase project before Stock Inbound undo works against that database.
- Real phone camera QA still needs to be performed by the owner from the Vercel app.

Next exact commands when owner regains access:

- `npx.cmd supabase login`
- `npx.cmd supabase link --project-ref aikfqnbsshflbtuakwrz`
- `npx.cmd supabase migration list`
- Only after confirming the link points to `aikfqnbsshflbtuakwrz`, run `npx.cmd supabase db push` if you are ready to apply pending migrations.

## 2026-06-14 - Stock outbound, direct outbound, transfer hardening

Task completed:

- Improved Stock outbound by order with an order item checklist showing required quantity/weight versus scanned quantity/weight.
- Added explicit substitution confirmation before order outbound can be confirmed with scanned items that are not on the order.
- Kept original ordered items intact while scanned/substituted items continue to be recorded in outbound batch lines.
- Expanded direct outbound UI and server schema for `PROCESSING`, `DAMAGE_SPOILAGE`, `RETURN_SUPPLIER`, `TRANSFER`, and `SAMPLE_TESTING`.
- Made direct outbound remarks required.
- Added direct damage/spoilage request creation from scanned barcodes with reason and photo path required; stock remains `IN_STOCK` but open damage requests block normal outbound.
- Added direct return-supplier request creation from scanned barcodes with supplier name required; stock moves to `HOLD_RETURN_SUPPLIER` until manager approval or rejection.
- Added sample/testing direct outbound support through `OUTBOUND_SAMPLE_TESTING`.
- Updated transfer labels so workers select a destination outlet/default stock location.
- Updated receive-transfer server action and RPC so wrong receiving location is allowed but logged as a wrong-location exception.
- Added source and regression coverage for substitution confirmation, direct outbound hardening, return-supplier hold status, sample/testing movement type, migration safety, stock-take lock helper refactors, and role/scope helper refactors.

Files changed:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/outbound-rules.ts`
- `lib/stock/types.ts`
- `lib/stock/unit-status-rules.ts`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-role-scope-coverage.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `scripts/stock-workflow-regression.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_MIGRATION_CHECKLIST.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/SUPABASE_MIGRATION_HANDOFF.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100054_stock_outbound_transfer_hardening_v1.sql`

Exact migration order:

- Run every migration in filename order through `supabase/migrations/202606100054_stock_outbound_transfer_hardening_v1.sql`.
- Migration `054` must run after `053`.
- Then run `supabase/seed.sql` only for safe demo/staging data.
- No live migration, seed, db push, or production deploy was run by Codex.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Migration `054` has not been applied to the target Supabase project.
- Return-supplier batch request creation still performs one request per barcode from the server action; each stock hold is RPC-protected, but a future fully atomic multi-line request RPC would be stronger for very large batches.
- Order outbound's app/server action enforces substitution confirmation and open-request blocking before calling the order outbound RPC; direct database calls to older RPCs should still be validated during real RLS/RPC QA.
- Destination outlet behavior depends on `stock_locations.outlet_id` being backfilled by matching stock location names to outlet names. Sites with multiple stock locations per outlet still need an explicit default-location model later.
- Real Supabase RLS, phone camera, and Vercel QA remain required.

First manual Vercel tests:

- `/stock/outbound`: ready order, scan matching item, confirm checklist totals.
- `/stock/outbound`: scan item not on order, confirm submit is blocked until substitution checkbox is ticked.
- `/stock/outbound`: direct `PROCESSING` without remarks, confirm blocked; add remarks and confirm.
- `/stock/outbound`: direct `SAMPLE/TESTING`, confirm `OUTBOUND_SAMPLE_TESTING` movement after migration `054`.
- `/stock/outbound`: direct `DAMAGE/SPOILAGE`, confirm reason/photo are required and stock is not deducted immediately.
- `/stock/outbound`: direct `RETURN_SUPPLIER`, confirm supplier is required and unit becomes `HOLD_RETURN_SUPPLIER`.
- `/stock/transfer`: transfer one barcode and confirm status becomes `TRANSFER_PENDING`.
- `/stock/receive-transfer`: receive at expected location and confirm normal receive.
- `/stock/receive-transfer`: receive at wrong allowed location and confirm wrong-location exception is recorded.

## 2026-06-14 - Transfer destination outlet-default completion

Task completed:

- Completed the remaining transfer destination gap from the Stock outbound/transfer goal.
- Stock data now loads outlets alongside stock locations.
- Stock locations now carry optional `outletId` and `isDefaultForOutlet`.
- Transfer, receive-transfer, and transfer outbound screens now show a destination/receiving outlet selector and submit the resolved default stock location behind the scenes.
- Server actions now check that transfer and receive-transfer locations are the selected outlet's default stock location.
- Migration `054` now adds `stock_locations.is_default_for_outlet`, backfills one default stock location per outlet, adds a unique one-default-per-outlet index, adds `public.is_default_outlet_stock_location(uuid)`, and adds a trigger that blocks `TRANSFER_PENDING` destinations that are not an outlet default stock location.
- The same stock-unit trigger now also blocks outbound/transfer status changes when a barcode has an open damage or return-supplier request, keeping the app-side request guard backed by database enforcement.
- Receive-transfer RPC also rejects non-default receiving locations while still allowing wrong-location exceptions when the receiver chooses the wrong outlet's default stock location.
- Transfer overdue alerts now explicitly call out sender outlet manager, receiver outlet manager, and admin.

Files changed:

- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `supabase/migrations/202606100054_stock_outbound_transfer_hardening_v1.sql`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_MIGRATION_CHECKLIST.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/SUPABASE_MIGRATION_HANDOFF.md`
- `HANDOFF.md`

Migration SQL added:

- No additional migration file was added in this continuation.
- Existing un-applied migration `supabase/migrations/202606100054_stock_outbound_transfer_hardening_v1.sql` was expanded before application.

Exact migration order:

- Run every migration in filename order through `supabase/migrations/202606100054_stock_outbound_transfer_hardening_v1.sql`.
- Migration `054` must run after `053`.
- Then run `supabase/seed.sql` only for safe demo/staging data.
- No live migration, seed, db push, or production deploy was run by Codex.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Migration `054` has not been applied to the target Supabase project.
- Real Supabase RLS/RPC QA is still required to prove default transfer destination blocking and wrong-location receive exceptions with authenticated users.
- The default stock location is automatically selected/backfilled by outlet name or first active stock location. If the business wants a different default for an outlet with multiple locations, update `stock_locations.is_default_for_outlet` after migration.

First manual Vercel tests:

- `/stock/transfer`: confirm destination dropdown shows outlets and displays the default stock location.
- `/stock/transfer`: transfer to an outlet and verify the barcode becomes `TRANSFER_PENDING` with `transfer_to_location_id` set to that outlet's default stock location.
- `/stock/receive-transfer`: receive at the expected outlet default stock location and confirm normal receive.
- `/stock/receive-transfer`: receive at a different allowed outlet default stock location and confirm wrong-location exception is logged.
- `/stock/dashboard`: force/inspect a transfer pending more than 3 days and confirm the alert names sender manager, receiver manager, and admin.

## 2026-06-15 - Stock take exceptions and report/dashboard completion

Task completed:

- Added stock-take exception handling for unknown barcodes and wrong-location barcodes.
- Unknown stock-take barcodes are recorded as pending `UNKNOWN_BARCODE` exceptions during counting and only create stock units after manager review and director approval.
- Wrong-location stock-take barcodes are recorded as pending `WRONG_LOCATION` exceptions and only move to the counted location after director approval.
- Migration `055` adds stock-take exception columns and replaces `public.approve_stock_take_session` so exception resolution, `STOCK_TAKE_ADJUSTMENT` movement creation, scan logs, and audit metadata happen in the director approval transaction.
- Stock-take session cards now show pending/resolved exception badges.
- Stock reports now include formal rows for stock balance, movement history, inbound, outbound, transfer pending, old stock 6 months, stock take variance, damage/spoilage, return supplier, and barcode scan errors.
- Reports page now includes filters for date range, outlet/location, item, brand, origin, status, user, and movement type.
- Dashboard KPIs now include pending damage approval, pending stock-take approval, duplicate scan attempts, and barcode decode errors, with a recent barcode scan alerts card.
- Documented that stock-unit fields should not be directly edited after inbound; corrections must flow through movement/adjustment/void/return/stock-take workflows.

Files changed:

- `app/(erp)/stock/reports/page.tsx`
- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `lib/stock/data.ts`
- `lib/stock/demo-data.ts`
- `lib/stock/types.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `supabase/migrations/202606100055_stock_take_exceptions_v1.sql`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_MIGRATION_CHECKLIST.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/SUPABASE_MIGRATION_HANDOFF.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606100055_stock_take_exceptions_v1.sql`

Exact migration order:

- Run every migration in filename order through `supabase/migrations/202606100055_stock_take_exceptions_v1.sql`.
- Migration `055` must run after `054`.
- Run `supabase/seed.sql` only for safe demo/staging data.
- No live migration, seed, db push, or production deploy was run by Codex.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after fixing stock-age report field names.
- `npm.cmd run build` - passed.

Remaining issues:

- Migrations `053`, `054`, and `055` have not been applied to the target Supabase project from this session.
- Real Supabase RLS/RPC QA is still required for stock-take unknown/wrong-location exceptions with real authenticated manager/director users.
- Unknown stock-take barcodes currently create zero-weight stock units unless a counted weight is captured later; this is acceptable for exception tracking but should be reviewed before production stock valuation.
- Report filters are text/date filters over the current stock report row data; a richer dedicated reports table/query layer can be added later.

First manual Vercel tests:

- `/stock/stock-take`: scan an unknown barcode and confirm an `UNKNOWN BARCODE` exception badge appears without creating a stock unit immediately.
- `/stock/stock-take`: scan a same item+brand barcode from another location and confirm a `WRONG LOCATION` exception badge appears.
- `/stock/stock-take`: submit, manager review, and director approve; verify unknown barcode stock is created and wrong-location stock moves only after approval.
- `/stock/reports`: confirm report rows include movement history, inbound, outbound, transfer pending, old stock, stock take variance, damage/spoilage, return supplier, and barcode scan errors.
- `/stock/reports`: test date range, location, item, brand, origin, status, user, and movement type filters; export CSV and print/save PDF.
- `/stock/dashboard`: confirm duplicate scan/decode-error and pending approval KPI cards appear when matching scan/request data exists.

## 2026-06-23 - Orders runtime client-boundary fix

Task completed:

- Fixed the Orders page runtime error caused by passing `getRowHref={(row) => ...}` from the server-rendered Orders page into the client `DataTable`.
- Added a nearby `OrdersTableClient` wrapper with `"use client"` for Orders table columns, row formatting, and row navigation callback.
- Kept `components/orders/orders-page.tsx` as the server-side data loading/composition component.
- Preserved the existing Orders cards, table labels, row navigation behavior, and detail filtering.

Files changed:

- `components/orders/orders-page.tsx`
- `components/orders/orders-table-client.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Browser verification of `/orders` and `/orders/[id]` row click navigation was not run in this pass.

## 2026-06-23 - Stock unit table client-boundary fix

Task completed:

- Fixed the Stock balance page runtime risk caused by passing `getRowHref={(row) => ...}` from server-rendered `StockPage` into the client `DataTable`.
- Confirmed `components/stock/data-table.tsx` already starts with `"use client"` and owns row click, keyboard navigation, `tabIndex`, and router push behavior.
- Added a nearby `StockUnitsTableClient` wrapper with `"use client"` for barcode stock-unit table columns and row navigation.
- Kept `components/stock/stock-page.tsx` as the server-side data loading/composition component.
- Preserved the existing Stock balance UI and stock-unit row navigation to `/stock/units/[id]`.

Files changed:

- `components/stock/stock-page.tsx`
- `components/stock/stock-units-table-client.tsx`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Browser verification of `/stock/balance` row click navigation to `/stock/units/[id]` was not run in this pass.

## 2026-06-23 - Stock schema repair migration for missing request tables

Task completed:

- Added a forward-only idempotent repair migration for Stock deployments that missed `stock_damage_requests` or related Stock workflow schema.
- Did not edit old migration files.
- The repair migration safely ensures current Stock code dependencies exist for:
  - `stock_damage_requests`
  - `stock_return_supplier_requests`
  - `stock_take_sessions`
  - `stock_take_lines`
  - `stock_movements`
  - `barcode_scan_logs`
- The migration adds missing enum values, table columns, indexes, RLS policies, update triggers, and the Stock approval/hold RPCs needed by current `lib/stock/data.ts` and `lib/stock/actions.ts`.
- Updated smoke coverage so client-wrapper table row links in Orders and Stock are checked in their new boundary-safe files.

Files changed:

- `supabase/migrations/202606230001_stock_schema_repair_v1.sql`
- `scripts/smoke-routes.mjs`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230001_stock_schema_repair_v1.sql`

Exact migration order:

- Run all existing migrations in filename order through `supabase/migrations/202606100055_stock_take_exceptions_v1.sql`.
- Then run `supabase/migrations/202606230001_stock_schema_repair_v1.sql`.
- This repair migration is idempotent and uses non-destructive `create table if not exists`, `alter table add column if not exists`, indexes, policies, and `create or replace function`.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- The repair migration was not applied to the live Supabase project in this pass.
- After applying it, refresh PostgREST schema cache or restart/redeploy if the app still reports stale schema metadata.

## 2026-06-23 - Stock mobile-first worker UX pass

Task completed:

- Improved only Stock-module worker UX and directly related Stock server/migration/test/docs surfaces.
- Added a general-worker Stock dashboard mode that shows six big action buttons only: Inbound, Outbound, Transfer, Receive, Return / Damage, and Stock Take.
- Restricted Stock reports/settings routes to manager/admin/director roles while keeping routine stock operation routes available to stock operators.
- Hardened continuous inbound for mobile workers:
  - session setup locks product, brand, origin, location, and source after the first saved scan;
  - offline scanning/saving is blocked with short worker-facing messages;
  - duplicate barcode and no-weight barcode blocks remain immediate and red;
  - generated internal numeric label barcodes now auto-submit/save the stock unit, then guide the worker to print/attach the label;
  - current-session count, weight, previous scan, undo, and finish-summary behavior remains intact.
- Added direct `SALES` as a direct outbound option while keeping damage/spoilage and return-supplier as request/approval workflows.
- Changed receive-transfer MVP behavior so wrong receiving locations are blocked instead of accepted as wrong-location exceptions.
- Changed active stock-take item+brand/location behavior to warning-only audit logging in app actions instead of hard-blocking routine stock operations.
- Let stock operators create scoped draft stock-take sessions while preserving manager review and director final approval gates.
- Added stock-take scan progress display with barcode count and total scanned weight.
- Added forward migration `202606230002_stock_mobile_worker_mvp_v1.sql` for the stock-take create RLS policy and receive-transfer RPC hardening.
- Updated Stock QA docs and smoke/static coverage scripts for the new mobile MVP rules.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `supabase/migrations/202606230002_stock_mobile_worker_mvp_v1.sql`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-report-coverage.mjs`
- `scripts/stock-rls-policy-coverage.mjs`
- `scripts/stock-role-scope-coverage.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_COMPLETION_AUDIT.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/STOCK_MIGRATION_CHECKLIST.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230002_stock_mobile_worker_mvp_v1.sql`

Exact migration order:

- Run all existing migrations in filename order through `supabase/migrations/202606100055_stock_take_exceptions_v1.sql`.
- Run `supabase/migrations/202606230001_stock_schema_repair_v1.sql`.
- Run `supabase/migrations/202606230002_stock_mobile_worker_mvp_v1.sql`.
- Do not run live migrations until Supabase CLI is logged in and linked to the confirmed project.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues and risks:

- Migration `202606230002_stock_mobile_worker_mvp_v1.sql` was not applied to live Supabase in this pass.
- The transfer UI still uses the existing outlet-default stock-location selector internally, even though labels now say destination stock location. This preserves current database constraints but should be revisited if non-default destination locations are required.
- Bluetooth label printing still relies on browser/OS print/download flow; real phone + Bluetooth printer QA is still required.
- Real phone camera QA at about 390px width is still required for inbound, outbound, transfer, receive-transfer, return/damage, and stock-take scanning.
- Existing dirty worktree files from earlier tasks were left untouched unless they were Stock/mobile coverage files needed for this pass.

Next recommended task:

- Apply migrations through `202606230002` in a confirmed Supabase project, then run Vercel/mobile QA starting with `/stock`, `/stock/inbound`, `/stock/outbound`, `/stock/transfer`, `/stock/receive-transfer`, and `/stock/stock-take`.

## 2026-06-23 - Stock mobile UX audit only

Task completed:

- Audited the current Stock module only against the active Stock Mobile UX goal.
- Did not implement new workflow changes in this pass.
- Verified current code paths for:
  - Stock home/dashboard
  - Inbound
  - Outbound
  - Transfer
  - Receive-transfer
  - Return/Damage
  - Stock Take
  - Label print/reprint
  - Scanner component
  - Worker role visibility

Already implemented:

- Stock worker dashboard mode exists and shows six large shortcut buttons: Inbound, Outbound, Transfer, Receive, Return / Damage, and Stock Take.
- Stock route guards restrict `/stock/reports` and `/stock/settings` to advanced roles in `components/stock/stock-page.tsx`.
- Inbound has recent templates, product search, quick item create, required brand/origin controls, default/editable location, auto batch number, continuous camera scan, duplicate blocking, no-weight blocking, saved count/weight, previous saved scan, finish summary, current-session undo, and PDF/print label output.
- Inbound locks item/brand/origin/location/source after the first saved scan until the worker finishes the session.
- Inbound blocks scan/save/generate label when offline with short non-technical messages.
- Generated internal label barcode submits the inbound form immediately and then shows the saved label for print/export.
- Outbound supports order-based and direct modes. Direct options include `SALES`, `PROCESSING`, `DAMAGE_SPOILAGE`, `RETURN_SUPPLIER`, `TRANSFER`, and `SAMPLE_TESTING`.
- Order outbound has checklist, scan batch, missing/blocked barcode warnings, substitution warning/confirmation checkbox, and allowed weight difference warning.
- Damage/spoilage requires a photo path and creates a request for manager/director approval instead of immediate deduction.
- Receive-transfer server action and migration `202606230002_stock_mobile_worker_mvp_v1.sql` block wrong receiving locations.
- Stock take can be created by stock operators, scan is barcode-only, wrong item/brand remains server-blocked, unknown barcode is recorded as exception, progress count/weight is shown, and manager/director signatures are required for review/approval.
- Scanner component uses `@zxing/browser`, prefers rear camera, stops stream on close/unmount, shows camera/permission guidance, keeps manual fallback, recent scans, and success vibration/beep where supported.
- Stock unit detail page supports mobile-accessible label reprint/export without requiring a reason.

Missing or incomplete:

- Sidebar and ERP Home still expose Stock Reports to all stock roles, even though the Stock page route guard blocks workers. This conflicts with "hide advanced reports/settings from workers" and creates confusing worker navigation.
- Transfer and receive-transfer labels say "stock location", but the control is still `DestinationOutletSelect` and submits the destination outlet's default stock location. This does not fully satisfy "select destination stock location" if non-default locations are required.
- Bluetooth label printer flow is not implemented as an explicit primary path. Current behavior is browser `window.print()` / PDF fallback.
- Inbound still exposes many typing-heavy setup fields on the main worker screen: weight rule positions, fixed kg, batch no, reference no, notes, and quick item create. It works, but is not yet the minimal worker flow.
- Previous saved scan summary shows weight and barcode, but not the previous successful item name in the dedicated previous-scan banner.
- Stock take create still requires selecting item/brand/location from full dropdowns; no recent/simple worker buttons yet.
- Transfer/receive/return pages still use generic form cards and smaller submit controls; not fully optimized for one-handed mobile scanning.
- Real 390px browser/device QA was not run in this pass.
- Real phone camera and Bluetooth/PDF label printer QA remain unverified.
- Migration `202606230002_stock_mobile_worker_mvp_v1.sql` has not been applied to the live Supabase project.

Files that need likely next changes:

- `components/erp/app-shell.tsx` - hide Stock Reports from worker sidebar roles and optionally simplify worker Stock nav labels.
- `components/dashboard/home-page.tsx` - hide Stock Reports home shortcut from workers.
- `components/stock/workflow-forms.tsx` - simplify mobile inbound setup, add previous item name to previous-scan banner, improve transfer/receive/return scan-first layout, and add explicit label printer/PDF fallback wording.
- `components/stock/stock-page.tsx` - keep worker dashboard minimal and consider hiding advanced table panels on worker operation pages.
- `components/stock/barcode-scanner.tsx` - real-device QA and possible small mobile scanner affordance improvements.
- `components/stock/stock-unit-detail.tsx` - label reprint QA and possible printer-friendly mobile copy.
- `lib/stock/actions.ts` - no immediate safety conflict found, but keep server-side validation checks when UX is simplified.
- `docs/STOCK_REMOTE_QA_VERCEL.md` and `docs/STOCK_TEST_LIST_FOR_OWNER.md` - add explicit 390px, camera, and printer QA evidence steps after the next UX pass.

Business rule conflicts found:

- Worker report visibility is inconsistent: `/stock/reports` route is advanced-role guarded, but sidebar and home shortcuts still advertise Stock Reports to general workers.
- Transfer destination UX is not fully aligned with "select destination stock location"; current implementation is destination outlet/default stock location.
- Bluetooth label printer being primary is not actually implemented; only browser print/PDF is present.
- Inbound does not fully meet "avoid typing as much as possible" because advanced fields are visible during normal worker scanning.
- Dedicated previous scan display does not include product name.

Recommended implementation order:

1. Fix worker visibility only: hide Stock Reports from sidebar and Home shortcuts for general workers while preserving manager/admin/director access.
2. Simplify inbound mobile worker screen: keep recent templates/search/scanner/summary prominent, move advanced weight-rule/batch/reference/notes fields behind a compact settings area, and add product name to previous-scan banner.
3. Improve label print UX copy and controls: make "Print label" primary, explain Bluetooth printer/browser print, keep PDF/export fallback, and test reprint from stock unit detail.
4. Decide transfer destination model: either rename honestly to outlet/default location or implement true stock-location destination selection with matching server/RLS/RPC support.
5. Tighten transfer/receive/return/stock-take mobile layouts: larger primary scan/submit actions, less typing, clearer success/error panels.
6. Run real 390px browser QA and phone camera/label-printer QA, then update Stock QA docs with evidence.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- No code changes were made for the audit findings in this pass.
- Existing dirty worktree files from prior Stock/ERP work remain; this pass only appended the audit to `HANDOFF.md`.

## 2026-06-23 - Stock mobile home worker visibility

Task completed:

- Improved the Stock mobile home only, plus directly related worker navigation visibility.
- General worker `/stock/dashboard` now renders only the six large Stock action buttons:
  - Inbound
  - Outbound
  - Transfer
  - Receive
  - Return / Damage
  - Stock Take
- Removed the Stock dashboard page header and alert/KPI/report surfaces from the general-worker Stock dashboard path so workers do not see value/cost/finance/report-style content there.
- Kept manager/admin/director dashboard behavior intact, including existing KPI/report access where roles allow it.
- Hid Stock Reports from general-worker sidebar navigation by changing its nav roles to manager/admin/director roles.
- Added explicit route-access entries for `/stock/reports` and `/stock/settings` in the app shell before the generic `/stock` rule.
- Hid Stock Reports from general-worker ERP Home shortcuts while preserving manager/admin/director visibility.
- Updated Stock QA docs to include 390px worker-home checks and report/settings visibility checks.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `components/erp/app-shell.tsx`
- `components/dashboard/home-page.tsx`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Remaining issues:

- Real 390px browser/phone verification was not run in this pass.
- Transfer destination UX, inbound field simplification, Bluetooth label-printer flow, and previous-scan product-name display remain open Stock Mobile UX follow-ups.

## 2026-06-23 - Stock inbound mobile UX simplification

Task completed:

- Improved Stock Inbound mobile UX only.
- Kept recent inbound templates and product search as the first worker setup path.
- Kept brand and origin required before scanning/saving.
- Kept location defaulting to the worker stock location while editable before the first saved scan.
- Kept one inbound session locked to item + brand + origin + location after the first saved scan until `Finish Inbound Session`.
- Made camera-decoded successful scans submit continuously without requiring a separate auto-save checkbox.
- Moved quick product creation behind a compact `New product` panel.
- Moved inbound source, weight rule, batch number, reference, notes, and save-rule controls under `Weight rule and notes`.
- Added a clearer `Previous scan` card that shows product name, weight, and barcode.
- Kept saved scan count and saved total weight visible near the scan controls.
- Shortened duplicate and label-weight messages for workers.
- Updated Stock owner/remote QA docs for 390px inbound testing, previous product/weight display, short duplicate warning, and advanced-field placement.
- Updated smoke coverage to check the new inbound labels.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera testing was not performed in this pass.
- Browser print/Bluetooth label-printer behavior still needs real device QA.
- Current inbound page is simpler, but `components/stock/workflow-forms.tsx` remains a large file and should only be split after critical workflow tests are stable.

## 2026-06-23 - Stock label generation and reprint mobile UX

Task completed:

- Improved Stock label generation and reprint UX only.
- Added a reusable Stock label component for preview, print actions, print note, and print-only label output.
- Kept current label size at `50mm x 30mm` and structured the component with a `stockLabelSizes` map so more sizes can be added later.
- Kept generated internal label behavior aligned with the current inbound flow: worker enters weight, taps `Generate internal label`, and the generated numeric barcode saves stock immediately through the inbound action.
- Changed label actions to make `Print label` the primary worker action and `PDF fallback` the secondary action.
- Added worker-facing copy explaining that phone/browser print should be used for a Bluetooth label printer when supported, and PDF should be used if Bluetooth printing is unavailable or unreliable.
- Reused the same label preview/print path for old stock-unit label reprint.
- Confirmed old-label reprint does not ask workers for a reason.
- Updated Stock QA docs with phone-width generated-label, Bluetooth print sheet, PDF fallback, and old-label reprint checks.
- Updated label smoke/coverage scripts for the shared label component and new worker copy.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `components/stock/workflow-forms.tsx`
- `components/stock/stock-unit-detail.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser Bluetooth printing support is device/browser dependent; the app now documents this and keeps PDF fallback.
- The printed barcode visual remains the existing simple numeric/readable label presentation. A true barcode-rendering dependency can be considered later if physical scanner readability requires it.
- Real phone, Bluetooth printer, and PDF-save QA were not performed in this pass.

## 2026-06-23 - Stock outbound mobile UX

Task completed:

- Improved Stock Outbound mobile UX only.
- Removed automatic fallback to the first ready order; order outbound now requires the worker to select a ready order first.
- Replaced outbound mode/type dropdown-first flow with large mobile-friendly buttons for:
  - Order outbound
  - Direct outbound
  - Sales
  - Processing
  - Transfer
  - Sample/Testing
  - Damage/Spoilage
  - Return Supplier
- Kept server-side validation and existing outbound actions unchanged.
- Kept direct outbound available for general stock workers.
- Added short worker-facing hints:
  - Sales: no customer name
  - Sample/Testing: no photo
  - Damage/Spoilage: photo required
- Kept damage/spoilage as approval request only; the UI now says stock goes to approval.
- Kept substitution as warning/confirmation only and removed any need for a typed substitution reason.
- Kept meat weight differences warning-only with short copy.
- Shortened blocked/invalid outbound messages for mobile workers.
- Made manual `Add barcode` button larger for phone use.
- Updated Stock QA docs for mobile outbound, direct outbound, substitution, weight difference, damage photo, and approval-only behavior.
- Updated smoke/coverage scripts to guard the new worker-facing outbound copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone-width QA at 390px was not performed in this pass.
- Direct damage photo still uses the current photo reference/path input; actual camera/file upload remains a broader Storage/upload workflow.
- Server-side direct outbound remarks remain required.

## 2026-06-23 - Stock transfer and receive-transfer mobile UX

Task completed:

- Updated Stock transfer and receive-transfer only.
- Changed the destination selector wording so workers choose a stock location first, with outlet shown as supporting context.
- Reworked `/stock/transfer` into a simple mobile flow:
  - choose destination stock location
  - scan barcode
  - tap `Send transfer`
- Reworked `/stock/receive-transfer` into a simple mobile flow:
  - choose receiving stock location
  - scan barcode
  - tap `Receive barcode`
- Moved reference number and notes behind a compact `Reference and notes` panel.
- Added large full-width final action buttons for phone use.
- Kept server-side action validation and access checks intact.
- Updated the server action wrong-location message to include the expected destination location:
  - `Wrong location. This barcode must be received at [destination location].`
- Added forward-only migration `202606230003_stock_receive_transfer_wrong_location_block_v1.sql` to replace the receive-transfer RPC and block wrong-location receive at the database layer.
- Confirmed the new migration keeps `wrongLocationException` as `false` in audit metadata and does not create a wrong-location receive exception path.
- Updated Stock QA docs and migration checklists through migration `202606230003`.
- Updated smoke/coverage scripts for the new strict wrong-location receive behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `lib/stock/actions.ts`
- `supabase/migrations/202606230003_stock_receive_transfer_wrong_location_block_v1.sql`
- `scripts/stock-migration-safety.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MIGRATION_CHECKLIST.md`
- `docs/SUPABASE_MIGRATION_HANDOFF.md`
- `docs/DATA_MODEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230003_stock_receive_transfer_wrong_location_block_v1.sql`

Exact migration order:

- Run every migration in filename order through `supabase/migrations/202606230003_stock_receive_transfer_wrong_location_block_v1.sql`.
- Migration `202606230003` must run after `202606230002`.
- Run `supabase/seed.sql` only for safe demo/staging data.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Migration `202606230003` has not been applied to the target Supabase project by Codex.
- Real 390px phone QA was not performed in this pass.
- Transfer destination choices are presented as stock locations, while existing database validation still requires outlet-default stock locations where outlet defaults are configured.

Latest re-check:

- Re-verified this transfer/receive-transfer slice after the Stock Mobile UX request.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.
- No additional app behavior changes were needed during the re-check.

## 2026-06-23 - Stock take mobile UX

Task completed:

- Improved Stock Take mobile UX only.
- Kept server-side stock-take validation and approval gates intact.
- Added shared online/offline status handling to the reusable barcode scanner field and Stock Take scan form.
- Stock Take scanning now blocks while offline with a short red message.
- Stock Take create copy now tells workers to choose one location, item, and brand, then scan only barcodes for that scope.
- Added warning copy that other stock operations warn only for the selected item + brand + location.
- Stock Take scan form now shows:
  - selected counting scope
  - barcode count progress
  - weight progress
  - barcode-only count
  - wrong item/brand blocked
  - unknown barcode exception
- Stock Take scan submit button is full-width and mobile-friendly.
- Stock Take approval action buttons are easier to tap on mobile.
- Manual count guidance now says missing barcode adjustment waits for manager review and director approval.
- Updated Stock QA docs for 390px stock-take testing, offline block, wrong item/brand block, unknown exception, progress, and approval gates.
- Updated smoke/coverage checks for the new stock-take worker wording.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `scripts/stock-take-lock-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px phone QA was not performed in this pass.
- Stock Take still uses dropdowns for session/location/item/brand selection; a future pass could add recent/common session shortcuts if needed.

Latest Stock Take follow-up:

- Kept the scope to Stock Take mobile UX and approval rules.
- Added a `warning` field to stock action results so matching stock movements during an active stock take can succeed while showing a yellow worker warning.
- Updated `warnIfStockTakeOpen` to keep audit logging and return the exact worker warning:
  - `Stock take is active for this item/brand/location. You can continue, but this movement will be recorded.`
- The warning remains non-blocking; stock operations continue and the audit log records `STOCK_TAKE_OPERATION_WARNING`.
- Stock Take scan rules remain barcode-only, wrong item/brand blocked, unknown barcode as exception, and manager review before director final approval.

Files changed in latest follow-up:

- `lib/stock/action-state.ts`
- `lib/stock/actions.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-take-lock-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Latest follow-up commands:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

## 2026-06-23 - Stock return and damage mobile UX

Task completed:

- Improved `/stock/return` worker-facing UX only.
- Reworked normal stock return into a scan-first mobile flow:
  - scan barcode
  - choose return location
  - tap `Save return`
- Moved normal return reference number and notes into an optional `Reference and notes` panel.
- Added short return guidance that customer returns may need inspection before normal outbound.
- Made manager inspection release scanner-capable and gave it a full-width mobile action button.
- Reworked damage/spoilage request into a scan-first mobile flow with reason, photo reference, optional notes, and a full-width `Submit damage request` button.
- Kept the server-side damage rule intact: photo is required, the staff request does not deduct stock, manager review and director approval are still required.
- Reworked return-supplier request into a scan-first mobile flow with supplier name, optional notes, and a full-width `Submit return supplier` button.
- Kept the server-side return-supplier rule intact: stock goes to supplier hold and is deducted only after manager approval.
- Enlarged damage and return-supplier approval buttons for mobile manager/admin/director use.
- Updated Stock QA docs for 390px return/damage/return-supplier testing.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/smoke-routes.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px phone QA was not performed in this pass.
- Damage photo capture/upload still uses the existing photo reference/path input; actual camera/storage upload remains a broader storage workflow.

## 2026-06-23 - Stock mobile UX source coverage

Task completed:

- Added a stock-only source regression guard for the active Stock Mobile UX goal.
- New script `scripts/stock-mobile-ux-coverage.mjs` checks:
  - the worker Stock home remains six large shortcut buttons,
  - worker Stock home does not render dashboard/table/report/settings/value/cost/finance content,
  - Stock worker forms keep scan-first/mobile wording for inbound, outbound, transfer, receive-transfer, return/damage, and stock take,
  - scanner copy still includes large Scan Barcode, manual fallback, permission guidance, continuous mode, and feedback hooks,
  - stock-unit label reprint keeps mobile print/PDF fallback copy,
  - Stock QA docs still tell the owner to test at 390px and capture the key phone/printer paths.
- Wired the script into `npm.cmd run smoke`.

Files changed in this pass:

- `package.json`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- This is source-level coverage only. Real phone camera, printer, and no-horizontal-scroll QA still need to be performed on a device or authenticated browser session.

Latest 390px browser QA attempt:

- Started a local Next dev server and confirmed the local app responded at `/stock`.
- Attempted to connect to the in-app browser automation for 390px Stock route verification.
- Browser automation could not launch in this Windows sandbox; the runtime reported `CreateProcessAsUserW failed: 5`.
- Plain Node REPL execution also failed with the same sandbox launch error, so browser automation was not available in this session.
- Updated `docs/STOCK_QA_EVIDENCE.md` with this attempted verification and the remaining manual phone QA steps.
- No live Supabase SQL, seed, deployment, or production data action was performed.

## 2026-06-23 - Stock scanner online-only blocking

Task completed:

- Improved Stock worker online-only behavior across scan-heavy forms.
- Added shared worker message:
  - `No internet connection. Reconnect, then scan again.`
- Kept existing inbound and stock-take offline blocking.
- Added offline scan/save blocking to:
  - `/stock/outbound`
  - `/stock/transfer`
  - `/stock/receive-transfer`
  - `/stock/return`
  - damage/spoilage request
  - return-supplier request
  - inspected-return release
- Offline states use red blocked/error styling.
- Server-side validation and RLS were not changed.
- Updated Stock QA docs so phone QA checks offline blocking across all main scanner flows.
- Updated source coverage so `npm.cmd run smoke` checks the online-only worker pattern.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real offline behavior still needs device/browser QA with authenticated Stock user sessions.

## 2026-06-23 - Stock worker-friendly error messages

Task completed:

- Improved Stock action error handling for normal worker screens.
- Added `friendlyStockErrorMessage` in `lib/stock/actions.ts`.
- Common database/RLS/system messages such as duplicate-key, row-level-security, permission, schema, column, relation, JSON, syntax, and fetch errors now show:
  - `Action could not be saved. Check the details and try again.`
- Explicit business-rule messages still pass through unchanged, such as duplicate barcode, wrong location, wrong status, or missing profile scope.
- Updated Stock mobile source coverage and QA docs to check that blocked saves show short non-technical messages.
- No RLS, server-side validation, database schema, or business workflow logic was weakened.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed to confirm all blocked database paths return the intended friendly message in deployed environments.

## 2026-06-23 - Stock worker advanced access guard coverage

Task completed:

- Audited Stock worker visibility for advanced report/settings surfaces.
- Confirmed the Stock worker home remains action-only with six big buttons and no KPI, report, settings, value, cost, or finance content.
- Confirmed Stock Reports and Stock Settings are role-gated away from general workers in navigation and route access.
- Added smoke coverage so future edits fail if worker Stock home exposes advanced content or if Stock route role guards for reports/settings are removed.
- Updated Stock QA docs to include direct URL checks for `/stock/reports` and `/stock/settings` as a general worker.
- No Stock business logic, RLS, server validation, or database schema was changed.

Files changed in this pass:

- `scripts/stock-role-scope-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed. Note: an earlier parallel typecheck attempt failed before `.next/types` had been regenerated; the final ordered run passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated worker/manager/director browser QA is still needed to prove deployed route blocking and mobile layout at 390px.

## 2026-06-23 - Stock online-only scanner safety wording

Task completed:

- Standardized Stock worker offline scanner/save blocking to the approved short message:
  - `Connection lost. Please reconnect before scanning.`
- Applied the shared message across Stock scan-heavy workflows:
  - inbound
  - outbound
  - transfer
  - receive-transfer
  - return/damage
  - return supplier
  - inspection release
  - stock take
- Kept online-only MVP behavior; no offline mode was added.
- Kept duplicate prevention, server validation, and RLS/security logic unchanged.
- Updated Stock QA docs so phone/Vercel tests check the exact offline scanner message.
- Strengthened Stock mobile source coverage so `npm.cmd run smoke` fails if old technical wording returns or if scan forms stop using the shared online-status guard.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone/browser QA is still needed to confirm browser online/offline events fire consistently on Vercel and that every scanner screen blocks as expected while disconnected.

## 2026-06-23 - Stock mobile QA preparation pass

Task completed:

- Prepared the Stock mobile QA evidence/checklist docs for the requested 390px worker flows.
- Attempted to use local browser automation, but the Windows sandbox browser/node runner failed with `CreateProcessAsUserW failed: 5`.
- Confirmed local app was already responding on port `3000`, but authenticated rendered 390px QA could not be completed from this environment.
- Updated `docs/STOCK_QA_EVIDENCE.md` with a Stock Mobile QA Preparation matrix for:
  - Stock home buttons
  - inbound session
  - continuous scanning UI
  - duplicate warning
  - no-weight warning
  - label generation
  - label reprint
  - outbound by order
  - direct outbound
  - damage/spoilage request
  - transfer
  - receive-transfer wrong-location block
  - stock take progress
  - unknown barcode exception
  - online-only connection message
- Added first-pass 390px mobile QA steps to owner and Vercel Stock QA docs.
- Updated `docs/MODULE_STATUS.md` to reflect prepared mobile QA docs and the remaining need for real phone camera, Bluetooth/PDF label printer, authenticated 390px browser, and RLS QA evidence.
- No app behavior, RLS, server validation, or database schema was changed.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera scanning and Bluetooth label printer testing cannot be proven from this Codex environment.
- Owner should run the first 390px mobile QA pass from `docs/STOCK_REMOTE_QA_VERCEL.md` on Vercel and record results in `docs/STOCK_QA_EVIDENCE.md`.

## 2026-06-23 - Stock transfer mobile stock-location wording

Task completed:

- Tightened Stock transfer worker copy so the mobile flow consistently says destination stock location, not destination outlet.
- Updated the order/direct outbound transfer destination label to `Destination stock location`.
- Updated transfer blocked copy to:
  - `Choose destination stock location.`
  - `Choose destination stock location and scan barcode.`
- Added a friendly server-action message for current destination validation:
  - `Choose an allowed stock location.`
- Kept existing transfer/receive-transfer server validation and RLS/security unchanged.
- Updated Stock mobile coverage and QA docs so the stock-location wording and short rejection message are checked.
- Removed an unreachable `/delivery/driver` render block and stale Delivery smoke expectation after TypeScript flagged it during the required checks. This was compile/lint cleanup only; `/delivery/driver` already returns `DriverDeliveryPage` earlier in the component.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/delivery/delivery-page.tsx`
- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npm.cmd run typecheck` - passed. Note: an earlier parallel typecheck attempt failed before `.next/types` had been regenerated; the final ordered run passed after build.

Risks / remaining checks:

- Existing database/RPC validation may still restrict some destinations depending on configured stock locations. This pass improves the worker-facing UX/error message without changing transfer schema rules.

## 2026-06-23 - Delivery Module V1

Task completed:

- Built Delivery Module V1 foundations and UI.
- Added Delivery Jobs as the driver-facing workflow layer for customer delivery, internal transfer delivery, and return collection.
- Added auto-created delivery jobs for delivery-required customer orders and standalone/manual delivery orders.
- Added `/delivery/driver` as a mobile-first, big-button driver workflow with tabs:
  - Available
  - My Deliveries
  - Completed
  - Failed
  - Expenses
- Driver cards show customer, note, address, phone actions, Google Maps, total weight, item count, order count, vehicle, driver, and status.
- Driver actions now support Accept Delivery, Loaded, Start Delivery, delivered proof upload, failed proof upload, goods issue reporting, address issue suggestion, and expenses.
- Proof upload uses camera-only file inputs, browser-side photo watermarking, phone GPS attempt, and completion allowed when GPS is unavailable.
- Failed proof requires one-tap failed reason; `Other` requires a remark.
- Goods issue supports Item missing, Wrong item, Weight mismatch, Packaging damaged, Not ready, and Other; `Other` requires a remark.
- Driver expenses support Petrol, Parking, Toll, Vehicle Repair, and Other with receipt photo and Pending/Approved/Rejected status.
- Updated manager `/delivery` dashboard with operational KPIs, manager review queue, driver performance, and delivery weight by driver.
- Added address suggestions for manager review and stopped the delivered-proof RPC from overwriting official customer GPS directly.
- Added future-ready truck GPS current-location and 3-day trail tables for later provider sync, ETA/delay, and fuel data.
- Added Delivery navigation entries for manager dashboard and driver delivery.

Files changed in this pass:

- `components/delivery/delivery-page.tsx`
- `components/delivery/driver-delivery-page.tsx`
- `components/erp/app-shell.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/data.ts`
- `lib/delivery/demo-data.ts`
- `lib/delivery/types.ts`
- `supabase/migrations/202606230004_delivery_module_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230004_delivery_module_v1.sql`

Exact migration order:

- Run all existing migrations in filename order through `supabase/migrations/202606230003_stock_receive_transfer_wrong_location_block_v1.sql`.
- Then run `supabase/migrations/202606230004_delivery_module_v1.sql`.
- Run `supabase/seed.sql` only for safe demo/staging data.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - first attempt was blocked by an existing Next build lock; retry after waiting passed.
- `npm.cmd run smoke` - passed.

Risks / remaining checks:

- Migration `202606230004` has not been applied to a real Supabase project by Codex.
- Real Supabase Storage bucket policy validation is still required for delivery proof, goods issue, receipt, and address-suggestion uploads.
- Real phone QA is still required for camera capture, GPS denied/unavailable, GPS allowed, Google Maps, Call, WhatsApp, and 390px layout.
- The V1 UI supports one job containing multiple linked orders through schema/data shape, but no manager dispatch board is included by design.
- Truck GPS tables are prepared only; provider sync, 10-second polling, customer tracking map, ETA/delay calculation, and fuel monitoring UI are future work.

## 2026-06-23 - Delivery Task 1 database, storage, and RLS

Task completed:

- Added canonical Delivery database/storage/RLS foundation only.
- Added idempotent migration `202606230007_delivery_database_storage_rls_v1.sql`.
- Added/ensured database foundation for:
  - `deliveries`
  - `delivery_orders`
  - `delivery_items`
  - `delivery_proofs`
  - `delivery_status_logs`
  - `delivery_address_suggestions`
  - `delivery_expenses`
  - `vehicles`
  - `truck_gps_snapshots`
- Added support columns for delivery no, delivery type, status, outlet/team, driver, default vehicle, vehicle, customer, address, phone, delivery note, total weight, item count, completed GPS, GPS unavailable, failed reason, remarks, timestamps, and created/updated users.
- Added storage buckets:
  - `delivery-proofs`
  - `delivery-expenses`
- Added indexes for date/status, driver/status, customer, outlet/team, delivery no, proof lookup, expense lookup, and truck GPS snapshots.
- Added RLS helper functions:
  - `public.can_access_delivery_scope(...)`
  - `public.can_review_delivery_scope(...)`
  - `public.can_access_delivery(...)`
  - `public.can_mutate_delivery(...)`
  - `public.can_access_delivery_job(...)`
  - `public.can_access_delivery_order_record(...)`
  - `public.can_access_delivery_storage_object(...)`
- Added RLS so drivers can see available deliveries in their allowed scope and their own accepted/completed/failed deliveries.
- Added RLS so delivery managers see their own outlet/team, while admin/director follow global access.
- Added scoped RLS for proof rows, status logs, items, address suggestions, expenses, storage objects, and truck GPS snapshots.
- Tightened older `delivery_orders`, `delivery_order_items`, and `delivery_status_logs` policies so previous broad scoped policies do not widen access beyond the new driver/manager rules.
- Updated the new driver job proof/goods-issue uploads to use `delivery-proofs`.
- Updated the new delivery expense receipt upload to use `delivery-expenses`.

Files changed in this pass:

- `lib/delivery/actions.ts`
- `supabase/migrations/202606230007_delivery_database_storage_rls_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230007_delivery_database_storage_rls_v1.sql`

Exact migration order:

- Remote migration status is applied through `202606100055`.
- Local pending migrations currently are:
  - `202606230001_stock_schema_repair_v1.sql`
  - `202606230002_stock_mobile_worker_mvp_v1.sql`
  - `202606230003_stock_receive_transfer_wrong_location_block_v1.sql`
  - `202606230004_delivery_module_v1.sql`
  - `202606230006_stock_transfer_any_location_v1.sql`
  - `202606230007_delivery_database_storage_rls_v1.sql`
- `202606230007` must run after `202606230004`.

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `npx.cmd supabase migration list` - passed after escalation for Supabase CLI user-profile access.
- `npx.cmd supabase db push --dry-run` - passed and listed the pending migrations; no remote changes were applied.
- `npx.cmd supabase status` - failed because Docker Desktop is unable to start, so local Supabase migration execution was unavailable.
- `npm.cmd run smoke` - failed on an existing Stock guard expectation: `Stock action missing: Transfer destination was not found or is inactive.`

Why migrations were not pushed:

- A normal `supabase db push` would apply all pending migrations in order, including unrelated pending Stock migrations `202606230001`, `202606230002`, `202606230003`, and `202606230006`.
- This task was Delivery-only, so Codex did not apply unrelated Stock migrations to the remote database automatically.
- Local migration execution could not be used because Docker Desktop was unavailable.

Risks / remaining checks:

- `202606230007` has not been executed against a real database yet.
- SQL should be applied on a staging database in full migration order, then proof and expense Storage policies should be tested with real delivery driver, delivery manager, admin, and director users.
- Driver proof/expense uploads now target the new buckets for the new driver job flow; older standalone proof upload still uses `erp-files` for compatibility until the UI is fully moved to canonical `deliveries`.

## 2026-06-23 - Stock transfer active stock-location support

Task completed:

- Added a forward-only Stock migration so transfers can target any active/scoped stock location instead of only outlet-default locations.
- Kept receive-transfer wrong-location blocking for the mobile MVP with the worker-safe message:
  - `Wrong location. This barcode must be received at [destination location].`
- Updated Stock server-action destination validation to check active stock locations while preserving source-location access and existing duplicate/open-request/stock-take guards.
- Kept transfer location changes deferred until receive-transfer scan.
- Updated Stock coverage scripts and owner QA docs so the active stock-location transfer rule and migration order are checked.
- Renamed the Stock migration to `202606230006` so it has a unique migration version and runs before the later Delivery database/storage/RLS migration.
- No live Supabase migration, seed, db push, production deploy, or service-role-key use was performed.

Files changed in this pass:

- `lib/stock/actions.ts`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-migration-safety.mjs`
- `docs/STOCK_MIGRATION_CHECKLIST.md`
- `docs/SUPABASE_MIGRATION_HANDOFF.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `supabase/migrations/202606230006_stock_transfer_any_location_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230006_stock_transfer_any_location_v1.sql`

Exact migration order:

- Run all existing migrations in filename order through `supabase/migrations/202606230003_stock_receive_transfer_wrong_location_block_v1.sql`.
- Run `supabase/migrations/202606230004_delivery_module_v1.sql`.
- Run `supabase/migrations/202606230006_stock_transfer_any_location_v1.sql`.
- Run `supabase/migrations/202606230007_delivery_database_storage_rls_v1.sql`.
- Run `supabase/seed.sql` only for safe demo/staging data.

Commands run and results:

- `node scripts/stock-migration-safety.mjs` - passed.
- `node scripts/stock-acceptance-coverage.mjs` - passed.
- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Migration `202606230006` has not been applied to a real Supabase project by Codex.
- Real authenticated 390px browser QA is still needed for transfer, receive-transfer wrong-location blocking, and Stock mobile flows.
- Real phone camera scanning and Bluetooth/PDF label printer QA remain owner/manual checks.

## 2026-06-23 - Stock transfer business-rule alignment

Task completed:

- Updated the Stock business rules to match the mobile MVP transfer flow:
  - Workers select a destination stock location.
  - The destination must be active and allowed by stock scope.
  - Receive-transfer still changes the real location only after the destination receive scan.
- Added owner QA doc coverage so smoke now checks the stock-location transfer rule, migration `202606230006`, and the `transferDestinationAnyActiveLocation` audit marker.
- Added a guard so stale outlet-default transfer wording fails coverage if it is reintroduced into `docs/BUSINESS_RULES.md`.
- No app behavior, database schema, RLS, or server validation changed in this pass.

Files changed in this pass:

- `docs/BUSINESS_RULES.md`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still needed for transfer and receive-transfer.
- Migration `202606230006` still needs to be applied to the target Supabase project before remote QA can prove active stock-location transfers against live data.

## 2026-06-23 - Stock worker form touch targets

Task completed:

- Increased shared Stock workflow select controls to mobile-friendly touch height and text sizing.
- Increased default Stock workflow submit buttons to at least `min-h-11` with clearer icon/text spacing.
- This applies across Stock worker forms that reuse `NativeSelect` and `SubmitButton`, including inbound, outbound, transfer, receive-transfer, return/damage, and stock take surfaces.
- Added Stock mobile UX coverage so smoke checks the larger touch-target classes.
- Updated Stock QA evidence to include the larger touch target guard.
- No business logic, database schema, RLS, or server validation changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed after rerun; an earlier parallel run with `build` hit a transient missing `.next/types` race while Next regenerated route types.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px authenticated browser QA is still needed to visually confirm every Stock worker form has no horizontal scrolling and the larger controls fit comfortably.

## 2026-06-23 - Stock scanner phone-width touch targets

Task completed:

- Improved the reusable Stock barcode scanner field for phone-width worker use.
- The `Scan Barcode` camera button now stays full-width on phone layouts and uses a taller `min-h-12` touch target before switching to auto width on larger screens.
- The manual barcode fallback input now uses a taller `min-h-11` touch target and mobile-readable text size.
- Updated Stock mobile UX coverage so smoke checks the full-width scan button and larger manual fallback input.
- Updated Stock QA evidence to note the stronger source guard for scanner touch targets.
- No business logic, database schema, RLS, or server validation changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed after updating `scripts/stock-scanner-coverage.mjs` to expect the new phone-width full-width scanner button.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone camera QA is still needed to confirm camera permission, rear camera selection, scanner close behavior, vibration/beep, and no horizontal scrolling around 390px width.

## 2026-06-23 - Stock scanner worker-friendly camera errors

Task completed:

- Replaced raw browser camera scanner errors with short worker-friendly messages in the reusable Stock barcode scanner.
- Added scanner messages for blocked camera permission, missing camera, busy camera, insecure browser page, and generic camera-start failure.
- Added coverage so scanner checks fail if `scanError.message` is shown directly to workers again.
- Updated Stock acceptance coverage and Stock QA evidence for the worker-friendly camera error behavior.
- No business logic, database schema, RLS, or server validation changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-scanner-coverage.mjs` - passed.
- `node scripts/stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone/laptop camera QA is still needed to confirm each browser permission state maps to the expected short message.

## 2026-06-23 - Stock scanner modal close touch targets

Task completed:

- Improved scanner modal close controls for phone-width Stock worker use.
- The top `Close scanner` icon button now has at least `min-h-11 min-w-11`.
- The bottom `Close` button now stays full-width on phone layouts and switches to auto width on larger screens.
- Updated Stock scanner and Stock mobile UX coverage so smoke guards the larger close controls.
- Updated Stock QA evidence to include large scanner close buttons.
- No business logic, database schema, RLS, or server validation changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-scanner-coverage.mjs` - passed.
- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is still needed to confirm scanner modal close controls are comfortable at about 390px width and camera stream stops after closing.

## 2026-06-23 - Stock scanner long barcode wrapping

Task completed:

- Improved scanner modal handling for long barcode values on phone-width layouts.
- The scanner `Last scan` value now renders as a wrapped monospace block instead of inline text, reducing horizontal overflow risk for long supplier/GS1 barcodes.
- Updated Stock scanner and Stock mobile UX coverage so smoke guards the wrapped last-scan display.
- Updated Stock QA evidence to include wrapped long barcode display.
- No business logic, database schema, RLS, or server validation changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts/stock-scanner-coverage.mjs` - passed.
- `node scripts/stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is still needed to scan a long barcode and confirm the scanner modal does not horizontally scroll around 390px width.

## 2026-06-23 - Stock mobile QA preparation re-run

Task completed:

- Prepared the requested Stock Mobile UX QA pass without changing app behavior.
- Confirmed the local `/stock` route responds.
- Attempted authenticated 390px browser QA, but the browser automation runtime could not launch in the Windows sandbox and returned `CreateProcessAsUserW failed: 5`.
- Updated Stock QA evidence, Vercel remote QA checklist, owner test list, Stock QA runbook, and module status to separate local source coverage from real phone/printer/Supabase evidence.
- No live Supabase migration, seed, production data change, deploy, camera permission approval, or printer action was performed.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_RUNBOOK.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - failed in `lib/orders/data.ts`; order data mapping/demo records are missing fields required by the current order TypeScript types.
- `npm.cmd run build` - compiled successfully, then failed during TypeScript for the same `lib/orders/data.ts` issue.

Risks / remaining checks:

- This Stock QA pass did not run real authenticated 390px browser tests because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and stock workflow evidence still need owner/manual QA.
- The unrelated Orders TypeScript mismatch blocks a clean full typecheck/build and should be fixed in an Orders-focused pass before handoff/deploy.

## 2026-06-23 - Stock scanner accessible feedback pass

Task completed:

- Improved the reusable Stock barcode scanner feedback without changing Stock business logic.
- Added explicit accessible labels to the large `Scan Barcode` button and scanner close controls.
- Added `role="alert"` to scanner error messages so blocked camera/error states are announced.
- Added `aria-live="polite"` to the `Last scan` success block so continuous scan feedback is easier to follow.
- Updated Stock scanner/mobile coverage so future changes must keep the announced success/error feedback.
- Updated Stock QA evidence and module status with the latest command results.
- No database schema, RLS, server validation, migration, Supabase data, deployment, camera permission approval, or printer action was changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before the Stock chain on an Orders smoke assertion: `Order data must expose scope options and only offer active stock items for new order lines`.
- `npm.cmd run lint` - passed with existing non-Stock warnings in `components/delivery/manager-delivery-dashboard.tsx` and `lib/orders/data.ts`.
- `npm.cmd run typecheck` - failed in `lib/orders/data.ts` on order status/manual-reason typing.
- `npm.cmd run build` - compiled successfully, then failed during TypeScript for the same `lib/orders/data.ts` status typing issue.

Risks / remaining checks:

- Real 390px authenticated browser QA still could not be completed because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.
- A separate Orders-focused fix is needed before the required full `smoke`, `typecheck`, and `build` gates can pass again.

## 2026-06-23 - Stock inbound duplicate blocked-state pass

Task completed:

- Improved Stock Inbound duplicate feedback for mobile workers.
- Changed the auto-scan duplicate message from the softer `Duplicate barcode. Remove it before saving.` to the immediate blocked message `Duplicate barcode. Inbound is blocked.`.
- Changed the current-session duplicate/error list from amber warning styling to red blocked/error styling.
- Added `role="alert"` to the current-session blocked/error list so duplicate and save-blocking feedback is announced.
- Updated Stock mobile UX coverage to reject the old duplicate wording and require the blocked/error session list.
- Updated Stock QA evidence and module status with the latest command results.
- No database schema, RLS, server validation, migration, Supabase data, deployment, camera permission approval, or printer action was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before the Stock chain on an Orders smoke assertion: `Order data must expose scope options and only offer active stock items for new order lines`.
- `npm.cmd run lint` - passed with an existing non-Stock warning in `lib/orders/data.ts`.
- `npm.cmd run typecheck` - failed in `lib/orders/data.ts` on order status/manual-reason typing.
- `npm.cmd run build` - failed before typecheck on an Orders syntax error in `lib/orders/actions.ts`: `Nullish coalescing operator(??) requires parens when mixing with logical operators`.

Risks / remaining checks:

- Real 390px authenticated browser QA still could not be completed because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.
- A separate Orders-focused fix is needed before the required full `smoke`, `typecheck`, and `build` gates can pass again.

## 2026-06-23 - Stock form feedback announcement pass

Task completed:

- Improved Stock form feedback for mobile workers without changing business logic.
- `ActionMessage` success/error/warning panels now use `role` and `aria-live` so saved, blocked, and warning states are announced.
- The inbound `Previous scan` success panel now uses `aria-live="polite"` so successful continuous scans are easier to follow.
- The inbound decode feedback panel now uses `role="alert"` with assertive live announcements for blocked/error states and `role="status"` for success/warning states.
- Updated Stock mobile UX coverage to guard these announced feedback states.
- Updated Stock QA evidence and module status with the latest command results.
- No database schema, RLS, server validation, migration, Supabase data, deployment, camera permission approval, or printer action was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before the Stock chain on an Orders assertion: `Order action missing: addCustomerOrderItemAction`.
- `npm.cmd run lint` - passed with an existing non-Stock warning in `lib/orders/data.ts`.
- `npm.cmd run typecheck` - failed in Orders files because exported order actions are missing, `??` is mixed with `||` in `lib/orders/actions.ts`, and `lib/orders/data.ts` has status/manual-reason typing errors.
- `npm.cmd run build` - failed before typecheck on an Orders syntax error in `lib/orders/actions.ts`: `Nullish coalescing operator(??) requires parens when mixing with logical operators`.

Risks / remaining checks:

- Real 390px authenticated browser QA still could not be completed because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.
- A separate Orders-focused fix is needed before the required full `smoke`, `typecheck`, and `build` gates can pass again.

## 2026-06-23 - Stock shared offline alert pass

Task completed:

- Improved Stock online-only scan feedback without changing business logic.
- Added a shared `OfflineScanAlert` inside `components/stock/workflow-forms.tsx`.
- Replaced repeated connection-loss red blocks across Stock Inbound, Outbound, Transfer, Receive Transfer, Return, Inspection Release, Damage/Spoilage, Return Supplier, and Stock Take scan forms.
- The shared alert uses `role="alert"` and the required worker message: `Connection lost. Please reconnect before scanning.`
- Updated Stock mobile UX coverage to guard the shared offline alert and its usage across worker scan forms.
- Updated Stock QA evidence and module status with the latest command results.
- No database schema, RLS, server validation, migration, Supabase data, deployment, camera permission approval, or printer action was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before the Stock chain on an Orders assertion: `Order action missing: addCustomerOrderItemAction`.
- `npm.cmd run lint` - passed with existing non-Stock warnings in `lib/delivery/actions.ts`.
- `npm.cmd run typecheck` - failed in non-Stock files: `lib/orders/actions.ts` mixes `??` with `||`, and `lib/orders/data.ts` has linked-delivery proof-status typing errors.
- `npm.cmd run build` - failed before typecheck on an Orders syntax error in `lib/orders/actions.ts`: `Nullish coalescing operator(??) requires parens when mixing with logical operators`.

Risks / remaining checks:

- Real 390px authenticated browser QA still could not be completed because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.
- A separate Orders-focused fix is needed before the required full `smoke`, `typecheck`, and `build` gates can pass again.

## 2026-06-23 - Stock outbound blocked-alert pass

Task completed:

- Improved Stock Outbound and Stock Take blocked scan feedback without changing business logic.
- Added `role="alert"` to outbound scan error, barcode-not-found, blocked-barcode, and wrong-destination panels.
- Added `role="alert"` to the Stock Take local blocked scan message panel.
- Updated Stock mobile UX coverage to guard outbound blocked states: `Barcode not found`, `Blocked barcode`, and `Wrong destination`.
- Updated Stock QA evidence and module status with the latest command results.
- No database schema, RLS, server validation, migration, Supabase data, deployment, camera permission approval, or printer action was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before the Stock chain on a non-Stock Orders assertion: `Orders data loader must only use demo fallback when Supabase is not configured and surface real query errors`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px authenticated browser QA still could not be completed because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.
- A separate Orders-focused smoke fix is needed before the required full `smoke` gate can pass again.

## 2026-06-23 - Stock workflow input touch-target pass

Task completed:

- Improved Stock workflow form touch targets without changing business logic.
- Added a Stock-only `Input` wrapper in `components/stock/workflow-forms.tsx` that keeps existing form behavior but applies `min-h-11 text-base sm:text-sm`.
- This improves mobile typing targets across Stock worker forms such as inbound, outbound, transfer, receive-transfer, return/damage, return supplier, and stock take without changing global app inputs.
- Updated Stock mobile UX coverage to guard the Stock-only input wrapper.
- Updated stale Stock coverage checks to match the current blocked duplicate/session wording:
  - `Duplicate barcode. Inbound is blocked.`
  - `Blocked/error scans this session`
- Updated Stock QA evidence and module status with the latest command results.
- No database schema, RLS, server validation, migration, Supabase data, deployment, camera permission approval, or printer action was changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px authenticated browser QA still could not be completed because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock 390px browser QA attempt

Task completed:

- Rechecked local Stock route reachability.
- Local `/stock` returned HTTP 200.
- Attempted to connect the in-app browser for 390px visual QA.
- Browser automation could not launch in this Windows sandbox and returned `CreateProcessAsUserW failed: 5`.
- Updated Stock QA evidence with the current browser-attempt result.
- No app code, database schema, RLS, server validation, migration, Supabase data, deployment, camera permission approval, or printer action was changed.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- Local `/stock` HTTP check - passed, returned `200`.
- In-app browser automation - failed with Windows sandbox permission error `CreateProcessAsUserW failed: 5`.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA remains manual.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock label mobile print controls pass

Task completed:

- Improved the reusable Stock label print/reprint controls without changing label generation, business logic, server validation, RLS, or schema.
- Made `Print label` and `PDF fallback` explicit full-width phone touch targets.
- Added accessible labels clarifying that `Print label` opens the phone print sheet/Bluetooth-printer path and `PDF fallback` opens the save-as-PDF fallback.
- Extended Stock label coverage so future changes must keep the mobile print/PDF labels and touch-target classes.
- Updated Stock QA evidence and module status with the latest Stock label/mobile pass.

Files changed in this pass:

- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone Bluetooth print-sheet and PDF-save QA still need owner/device evidence.
- Real authenticated 390px browser QA still could not be completed because browser automation is blocked by the Windows sandbox.
- Real phone camera, laptop camera, live Supabase RLS, and Stock workflow evidence remain owner/manual QA.

## 2026-06-23 - Stock label Code 128 rendering pass

Task completed:

- Improved Stock label generation/reprint output so labels render machine-readable Code 128 SVG bars instead of decorative placeholder stripes.
- Added `lib/stock/code128.ts`, a small dependency-free encoder with Code Set C for even numeric barcodes and Code Set B fallback for printable supplier/reprint barcode values.
- Updated the reusable 50mm x 30mm Stock label preview/print component to use the Code 128 renderer for inbound generated labels and stock-unit reprints.
- Kept the human-readable barcode number under the bars for worker checking.
- Updated Stock label coverage so future changes must keep the Code 128 encoder, SVG bar rendering, print/PDF surface, and no decorative stripe fallback.
- Updated Stock business rules and QA docs so owner testing includes scanning the printed/reprinted label.

Files changed in this pass:

- `lib/stock/code128.ts`
- `components/stock/stock-label.tsx`
- `scripts/stock-label-coverage.mjs`
- `docs/BUSINESS_RULES.md`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-label-coverage.mjs` - passed.
- `npx.cmd tsc --noEmit --pretty false` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real printed-label scanner readability still needs owner/device QA with the actual Bluetooth label printer, PDF output, phone camera, and/or handheld scanner.

## 2026-06-23 - Stock take active-session mobile shortcut pass

Task completed:

- Improved Stock Take mobile worker flow without changing stock-take business logic, RLS, approval rules, or schema.
- Added large `Tap active session` buttons before the stock-take session dropdown so workers can quickly choose an active draft count on a phone.
- Kept the existing dropdown as a fallback for longer session lists.
- Added a short empty state: `Start a stock take above, then scan.`
- Preserved barcode-only counting, wrong item/brand blocking, unknown/wrong-location exceptions, online-only scan blocking, manager review, director final approval, and warning-only active stock-take behavior.
- Updated Stock mobile UX coverage and owner/Vercel QA docs to verify the active-session tap flow.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-take-lock-coverage.mjs` - passed.
- Initial `npm.cmd run typecheck` after the Stock component edit - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed with two existing non-Stock Delivery warnings:
  - `lib/delivery/actions.ts`: unused `DeliveryExpenseStatus`.
  - `lib/delivery/actions.ts`: unused `managerExpenseReviewSchema`.
- Final `npm.cmd run typecheck` - failed in non-Stock Delivery files:
  - `lib/delivery/data.ts`: mapped expense object is missing required `DeliveryExpense` fields such as `deliveryId`, `outletId`, `deliveryTeamId`, and `bucketId`.
  - `lib/delivery/demo-data.ts`: demo expense object is missing the same required `DeliveryExpense` fields.
- `npm.cmd run build` - failed during TypeScript on the same non-Stock Delivery `DeliveryExpense` mismatch after compilation succeeded.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA is still needed to confirm the active-session buttons fit without horizontal scrolling.
- A Delivery-focused pass is needed to repair the current `DeliveryExpense` type/data mismatch before full typecheck/build can pass again. This Stock-only pass did not edit Delivery files.

## 2026-06-23 - Stock damage reason mobile buttons pass

Task completed:

- Improved Stock damage/spoilage worker UX without changing server validation, approval flow, RLS, or schema.
- Replaced small damage reason dropdowns with large tap buttons in:
  - direct outbound damage/spoilage request path,
  - standalone damage/spoilage request path on `/stock/return`.
- Kept the same hidden form field names and values (`damageReason` and `reason`) so existing server actions continue to validate and store the same business data.
- Preserved the photo-required approval-only rule: damage/spoilage creates a pending approval request and does not deduct stock immediately.
- Updated Stock mobile UX coverage and QA docs to check the large reason-button flow.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px phone/browser QA is still needed to confirm the reason buttons fit comfortably and do not cause horizontal scrolling.

## 2026-06-23 - Stock mobile QA refresh

Task completed:

- Prepared and revalidated the Stock mobile QA evidence for the requested worker flows without changing app behavior.
- Confirmed the existing Stock source/coverage checks cover:
  - Stock home buttons,
  - inbound session and continuous scanning UI,
  - duplicate and no-weight barcode warnings,
  - label generation and label reprint,
  - outbound by order and direct outbound,
  - damage/spoilage request copy,
  - transfer and receive-transfer wrong-location block copy,
  - stock-take progress and unknown-barcode exception copy,
  - online-only scanner blocking message.
- Updated Stock QA evidence and module status with the latest passing command results.
- Did not run live Supabase SQL, seed data, migrations, deployment, camera permission, Bluetooth printer, or production data actions.

Files changed in this pass:

- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px phone/browser QA still needs owner/device evidence.
- Real phone camera, laptop camera, Bluetooth label printer, PDF label output, live Supabase RLS, and real Stock workflow evidence remain owner/manual QA.
- Use `docs/STOCK_TEST_LIST_FOR_OWNER.md`, `docs/STOCK_REMOTE_QA_VERCEL.md`, and `docs/STOCK_QA_EVIDENCE.md` for the first owner testing pass.

## 2026-06-23 - Stock direct outbound quick remarks pass

Task completed:

- Improved Stock direct outbound mobile UX only.
- Added large `Quick remarks` buttons above the direct outbound remarks box.
- Preserved the existing server-side rule that direct outbound requires remarks.
- Kept manual custom remarks available for cases where the preset text is not enough.
- Added preset remarks for Sales, Processing, Transfer, Sample/Testing, Damage/Spoilage, and Return Supplier.
- Kept Stock movement logic, RLS, schema, approval rules, and server validation unchanged.
- Updated Stock mobile coverage and QA docs so future checks include the one-tap remarks path.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px phone/browser QA is still needed to confirm the quick remark buttons fit comfortably and the remarks box fills as expected.
- Live Supabase/RLS QA is still needed to confirm direct outbound submission succeeds only for permitted stock users and still rejects missing remarks server-side.

## 2026-06-23 - Stock take worker-start evidence pass

Task completed:

- Audited the current Stock Take start path against the active Stock Mobile UX goal.
- Confirmed the server action uses `stockOperatorRoles`, so stock workers can create scoped draft stock-take sessions through the action.
- Confirmed later mobile-worker migration `202606230002_stock_mobile_worker_mvp_v1.sql` re-creates the `stock_take_sessions` insert policy with `public.can_manage_stock()`, allowed stock location scope, and `status = 'DRAFT'`.
- Confirmed manager review and director approval remain separate protected server actions with signatures.
- Updated stale business-rule wording from manager/admin-created stock take to worker/manager/admin-created stock take.
- Extended stock-take coverage so smoke checks worker-created scoped drafts and protected review/approval gates.
- Updated owner/Vercel QA docs to test stock-worker session creation and to confirm normal workers cannot manager-review or director-approve.
- Aligned one existing Orders picking helper sentence with the existing smoke guard text after smoke failed before reaching the Stock checks; no Orders behavior was changed.

Files changed in this pass:

- `components/orders/orders-forms.tsx`
- `docs/BUSINESS_RULES.md`
- `scripts/stock-take-lock-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-take-lock-coverage.mjs` - passed.
- Initial `npm.cmd run smoke` - failed before Stock checks on an Orders copy guard: `Order form UX guard missing: Wrong item scans are recorded as mismatches.`
- `npm.cmd run smoke` - passed after the copy alignment.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real Supabase/RLS QA is still needed with a normal stock worker to prove session creation works in the target project after migrations are applied.
- Real 390px phone/browser QA is still needed for stock-take start, active-session buttons, scanning progress, exceptions, submit, manager review, and director approval.

## 2026-06-23 - Stock scanner disabled-state safety pass

Task completed:

- Improved Stock scanner online-only safety without changing stock movement logic, RLS, schema, or server validation.
- Updated the reusable Stock barcode scanner so when a workflow disables scanning, for example after connection loss, any open scanner controls stop, the scanner modal closes, and the starting-camera state clears.
- Kept the existing page-level worker message: `Connection lost. Please reconnect before scanning.`
- Extended scanner coverage so smoke checks the disabled-state scanner stop/close behavior.
- Updated owner/Vercel QA docs and Stock QA evidence to test that an open camera scanner closes or stops when connection is lost.
- Reordered matching `DROP POLICY IF EXISTS` statements immediately before `CREATE POLICY` statements in `202606230015_delivery_permissions_audit_hardening_v1.sql` after smoke failed before the Stock checks; no Delivery policy business logic was changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `supabase/migrations/202606230015_delivery_permissions_audit_hardening_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-scanner-coverage.mjs` - passed.
- Initial `npm.cmd run smoke` - failed before Stock checks because `202606230015_delivery_permissions_audit_hardening_v1.sql` had paired legacy/canonical policy drops in the wrong order for the smoke guard.
- `npm.cmd run smoke` - passed after reordering matching policy drops.
- `npm.cmd run lint` - passed with four existing non-Stock Orders warnings in `components/orders/orders-page.tsx`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real phone QA is still needed to confirm the camera permission sheet, active camera stream, disabled scanner close/stop behavior, and no saved scan after connection loss.

## 2026-06-23 - Stock overdue transfer alert action pass

Task completed:

- Audited the Stock overdue transfer alert surface against the Stock Mobile UX goal.
- Confirmed overdue transfer alerts already flag sender manager, receiver manager, admin, and director.
- Added a large `Open receive` action inside each overdue transfer alert card so permitted users can jump directly to `/stock/receive-transfer`.
- Kept transfer workflow logic, receive-transfer validation, RLS, schema, and server actions unchanged.
- Extended Stock report/page coverage so smoke checks the alert title, audience text, and `Open receive` action.
- Updated owner/Vercel QA docs and Stock QA evidence with the actionable overdue transfer alert step.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-report-coverage.mjs`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-report-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real 390px manager/admin/director QA is still needed to confirm overdue transfer alerts fit well on phone width and `Open receive` opens the receive-transfer workflow.

## 2026-06-23 - Delivery permissions and isolation audit

Task completed:

- Audited Delivery Module pages, server actions, query helpers, navigation visibility, RLS policies, storage policies, and legacy delivery surfaces.
- Tightened legacy Delivery routes so drivers no longer receive dashboard/order/payment/vehicle management pages or the home dashboard shortcut.
- Restricted legacy delivery payment creation and updates to delivery manager/admin roles only.
- Stopped driver delivery detail pages from fetching the full driver list, even though the manager-only controls were already hidden.
- Scoped vehicle query results by outlet/team for non-admin users and removed truck GPS provider metadata from driver-facing vehicle data.
- Added route-specific app-shell access rules so manager dashboard, payments, vehicles, legacy orders, and new-order routes are not exposed to drivers.
- Added an idempotent hardening migration for delivery RLS, storage, status transition guards, expense isolation, proof upload access, address suggestion approval, and legacy delivery tables.
- Moved the older manager proof upload action from the broad ERP file bucket into the protected `delivery-proofs` bucket and allowed legacy delivery-order proof paths only through delivery-order access checks.
- Fixed one existing lint blocker in the stock barcode scanner so the required checks could complete; no stock movement behavior was changed.

Access rules implemented:

- Drivers can read available deliveries only in their allowed outlet/team and can read accepted/completed/failed deliveries only when assigned to themselves.
- Drivers cannot see other drivers' accepted delivery work, delivery management pages, delivery payments, driver lists, or vehicle GPS provider metadata.
- Drivers cannot create or review delivery payments and cannot approve/reject expenses or address/GPS suggestions.
- Driver proof uploads require the driver to own the delivery/job, the delivery/job to be out for delivery, and the uploaded row/object to identify the current user.
- Driver expense rows must use the current user as `driver_id`; drivers can read only their own expenses.
- Manager review, address/GPS suggestion approval, expense review, and payment mutation are scoped to the manager's outlet/team.
- Admin keeps the existing broad management pattern; director keeps read/review visibility where existing patterns allow it but is not allowed to approve address/GPS suggestions through the new approval mutation policy.
- Official customer GPS/address updates remain manager/admin-approved only; delivery proof GPS and driver address issues stay as suggestions until reviewed.
- Direct database delivery status updates now have RLS/trigger guards for driver status order, closed delivery protection, and proof-before-complete checks.
- Storage access now separates read from upload: delivery proof uploads require allowed delivery/job/order state, and expense receipt uploads are namespaced to the current user.

Files changed in this pass:

- `app/(erp)/delivery/[id]/page.tsx`
- `app/(erp)/delivery/driver/page.tsx`
- `components/dashboard/home-page.tsx`
- `components/delivery/delivery-page.tsx`
- `components/erp/app-shell.tsx`
- `components/stock/barcode-scanner.tsx`
- `lib/delivery/actions.ts`
- `lib/delivery/queries.ts`
- `supabase/migrations/202606230015_delivery_permissions_audit_hardening_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `supabase/migrations/202606230015_delivery_permissions_audit_hardening_v1.sql`

Commands run and results:

- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.
- `node scripts\smoke-routes.mjs` - passed.

Risks / remaining checks:

- The new migration still needs to be applied to the target Supabase project and verified with real driver, manager, admin, and director users.
- Supabase Storage bucket behavior should be manually tested after migration because upload paths must match the app's delivery proof and expense receipt conventions.

## 2026-06-23 - Delivery Module V1 testing, build, and handoff

Task completed:

- Added `scripts/delivery-module-coverage.mjs` and wired it into `npm run smoke`.
- The delivery smoke coverage now guards the requested V1 flows at source level: auto-create from ready delivery order, manual Create Delivery from order detail, duplicate delivery reuse, pickup exclusion, available-delivery query, current-driver delivery query, accept, loaded, start, delivered proof completion, failed proof reason validation, `Other` remark validation, GPS-unavailable completion, linked order status sync, no stock deduction during delivery completion, address/GPS suggestion creation, manager suggestion approval, expense submission, expense review, driver route isolation, no driver finance/price/cost/credit/profit/payment wording, outlet/team scoped vehicle/driver queries, RLS/storage hardening, and manager dashboard review/KPI sections.
- Confirmed the Orders handoff path uses `create_delivery_from_customer_order` with `p_allow_related: false`, reuses existing linked deliveries, and blocks customer pickup from Delivery Module creation.
- Confirmed Delivery completion actions do not touch `stock_units`, `stock_movements`, `order_stock_reservations`, or `no_barcode_stock`; stock remains handled by Stock outbound/loading and failed-return workflows.
- Confirmed proof GPS creates manager-review address/GPS suggestions instead of overwriting official customer master fields.
- Confirmed the full smoke suite, typecheck, lint, and production build pass.

Files changed in this pass:

- `package.json`
- `scripts/delivery-module-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\delivery-module-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Known limitations:

- Delivery smoke coverage is static/source-level and does not log in as real Supabase users or upload real camera/receipt files.
- Live Supabase migrations, RLS, and Storage policies still need to be applied and tested with real driver, manager, admin, and director users.
- Phone GPS/camera permission behavior, proof watermark rendering, Google Maps/Call/WhatsApp links, and receipt/proof signed URLs still need device/browser QA.

Remaining tasks:

- Run real role/outlet/team QA for available delivery visibility, own-driver delivery visibility, manager dashboard scope, proof upload, expense upload, and address/GPS approval.
- Capture owner/device evidence for the driver mobile flow from accept through proof completion and expense submission.
- Verify the target Supabase project has all Delivery migrations applied in filename order, especially the Task 11 RLS/storage hardening migration.

Next recommended improvements:

- Add seeded database integration tests for driver/manager/admin/director RLS after a stable Supabase test project is available.
- Add browser automation for `/delivery/driver`, `/delivery`, and `/delivery/[id]` after authenticated browser tooling is available.
- Add live Storage upload checks for `delivery-proofs` and `delivery-expenses` buckets with expected object path conventions.

## 2026-06-23 - Delivery V1 stabilization and production-readiness review

Task completed:

- Audited Delivery V1 canonical model usage across `/delivery`, `/delivery/driver`, `/delivery/[id]`, `/delivery/expenses`, delivery service actions, delivery queries, proof upload, expense review, and Orders handoff.
- Confirmed the official V1 runtime path uses `deliveries`, `delivery_orders`, `delivery_items`, `delivery_proofs`, `delivery_status_logs`, `delivery_address_suggestions`, and `delivery_expenses`.
- Documented legacy compatibility surfaces that still use `delivery_jobs`, `delivery_job_orders`, `delivery_goods_issues`, older standalone `delivery_orders`, `delivery_payments`, and `driver_locations`.
- Added manager-facing warning copy on old Delivery pages so legacy standalone order/payment/vehicle surfaces are not mistaken for the canonical V1 driver workflow.
- Updated stale Delivery business rules so V1 proof/GPS/customer-master/payment/stock-deduction rules match the implemented module.
- Added `docs/DELIVERY_PRODUCTION_READINESS.md` with canonical model audit, staging migration order, RLS account/test guidance, mobile phone QA checklist, and Storage verification checklist.
- Updated module status to reference the new production-readiness checklist.

Files changed in this pass:

- `components/delivery/delivery-page.tsx`
- `docs/BUSINESS_RULES.md`
- `docs/DELIVERY_PRODUCTION_READINESS.md`
- `docs/MODULE_STATUS.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\delivery-module-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed on unrelated Processing smoke guard: `Processing page must link to order preparation and clarify stock entry`.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Legacy Delivery pages remain available for manager/admin historical compatibility. They should be archived or redirected only after live data review confirms they are no longer needed.
- Real staging Supabase RLS/Storage verification is still required with driver, manager, admin, and director users.
- Real phone QA is still required for camera, GPS allowed/denied, maps/call/WhatsApp links, proof upload, failed proof, address issue, and expense receipt upload.

## 2026-06-24 - Delivery V1 staging QA users and RLS workflow verification

Task completed:

- Created login-capable staging Auth users for Delivery QA through the linked Supabase database:
  - `delivery.driver@example.test`
  - `delivery.manager@example.test`
  - `delivery.other@example.test` for driver isolation checks.
- Created matching profiles, profile roles, outlet/team scope, Delivery module access, and default QA vehicle.
- Seeded non-production QA Delivery data:
  - `QA Delivery Customer A`
  - `QA-DEL-ORDER-001` with Delivery `DL-20260624-B40743`
  - `QA-DEL-ORDER-FAILED-001` with Delivery `DL-20260624-43C8BE`
  - `QA-DEL-OTHER-001` assigned to the other driver.
- Verified Delivery RLS with authenticated JWT claims:
  - Driver sees available scoped deliveries and does not see another driver's accepted delivery.
  - Manager sees scoped Delivery records.
  - Driver can accept, mark loaded, start, submit proof metadata, complete Delivered/Failed, submit expense, and submit address/GPS suggestion.
  - Driver cannot approve expense or address/GPS suggestion.
  - Manager can approve expense and address/GPS suggestion.
  - Unauthorized proof and expense storage object writes are blocked.
- Found and fixed a Delivery completion RLS bug that prevented drivers from completing after proof existed.
- Found and fixed a direct proof-metadata validation gap so FAILED proofs require `failed_reason`, and `OTHER` requires remarks at the database boundary.

Files changed in this pass:

- `supabase/migrations/202606240006_delivery_completion_rls_fix_v1.sql`
- `supabase/migrations/202606240007_delivery_failed_proof_constraints_v1.sql`
- `HANDOFF.md`

Migration SQL added:

- `202606240006_delivery_completion_rls_fix_v1.sql` - fixes `deliveries` update RLS proof match from an impossible self-comparison to `proof.delivery_id = deliveries.id`.
- `202606240007_delivery_failed_proof_constraints_v1.sql` - adds database constraints for failed-proof reason and Other-remark requirements.

Commands run and results:

- Multiple `npx.cmd supabase db query --linked ...` checks against staging - completed; two expected negative RLS/constraint checks returned errors by design.
- Applied the two Delivery-only hotfix SQL files directly with `npx.cmd supabase db query --linked --file ...`; no broad `supabase db push` was run.
- `npm.cmd run smoke` - Delivery coverage passed, then full smoke failed on unrelated Stock coverage: `Acceptance 12 mobile scanner missing: Camera blocked. Allow camera permission or type barcode.`
- `npm.cmd run typecheck` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Browser-style login QA could not be completed from this workspace because `.env.local` points to local Supabase, and retrieving the staging anon key through CLI was blocked because it would expose project API keys. Owner should run manual login/device QA on the staging app or provide a safe staging app environment.
- The two hotfix SQL files were applied directly to staging but are not recorded in the remote Supabase migration ledger yet because unrelated local `20260624` migrations are pending ahead of them. Plan a controlled migration-ledger alignment before the next broad staging push.
- Direct database updates to `deliveries.status` do not update linked `customer_orders.status`; current order sync appears to live in server actions rather than a database trigger. Full server-action/browser QA is still needed.
- A direct RLS check showed the Delivery driver can read `customer_orders.total_order_price` through existing Order table access. Delivery UI/source smoke hides finance fields, but Order RLS/column exposure should be reviewed before production.

## 2026-06-24 - Delivery V1 remaining staging blocker stabilization

Task completed:

- Fixed the remaining Delivery driver finance exposure by removing Delivery-operator read-through access from `customer_orders`.
- Kept driver operational visibility through canonical Delivery tables: `deliveries`, `delivery_orders`, and `delivery_items`.
- Verified as `delivery.driver@example.test` via staging JWT claims that direct `customer_orders` visibility for the seeded linked orders is now `0` rows.
- Verified the same driver can still read operational Delivery fields: delivery/order no, customer name, address, phone, note, item name, quantity, weight, and status.
- Verified a non-persistent linked-order update still succeeds under the delivery driver update policy so server-action order status sync can continue without exposing SELECT access.
- Renamed the new finance hardening migration from duplicate local version `202606240008` to `202606240009`.
- Applied `202606240009_order_delivery_finance_exposure_fix_v1.sql` directly to staging and repaired migration history for manually applied hotfixes `202606240005`, `202606240006`, `202606240007`, and `202606240009`.
- Confirmed `npm.cmd run smoke` now passes; the earlier Stock scanner copy failure was already resolved in the current worktree, so no Stock behavior change was needed.
- Added safe deployed staging app/server-action QA steps to `docs/DELIVERY_PRODUCTION_READINESS.md`.

Files changed in this pass:

- `supabase/migrations/202606240009_order_delivery_finance_exposure_fix_v1.sql`
- `docs/DELIVERY_PRODUCTION_READINESS.md`
- `HANDOFF.md`

Migration SQL added:

- `202606240009_order_delivery_finance_exposure_fix_v1.sql` - updates `can_access_customer_order(uuid)` and the `customer_orders` SELECT policy so Delivery operators no longer read order rows that include finance-sensitive fields.

Commands run and results:

- `npx.cmd supabase db query --linked --file supabase\migrations\202606240009_order_delivery_finance_exposure_fix_v1.sql` - passed.
- `npx.cmd supabase migration repair --linked --status applied 202606240005 202606240006 202606240007 202606240009` - passed.
- `npx.cmd supabase migration list --linked` - passed; `202606240003` and `202606240008` remain pending unrelated migrations.
- `npx.cmd supabase db push --linked --dry-run` - failed by design because unrelated `202606240003` and `202606240008` are pending before the last remote migration.
- `npx.cmd supabase db push --linked --include-all --dry-run` - passed and would push only `202606240003_whatsapp_crm_phase1.sql` and `202606240008_auth_profile_dashboard_read_grants_v1.sql`.
- `npm.cmd run smoke` - passed.
- `npm.cmd run build` - passed after waiting for a stale Next build lock to clear.
- `npm.cmd run typecheck` - passed after build regenerated `.next/types`.
- `npm.cmd run lint` - passed.

Risks / remaining checks:

- Deployed staging app/server-action QA is still required with real browser login because local `.env.local` points to local Supabase and staging API keys were not exposed locally.
- Normal `supabase db push --linked --dry-run` still reports unrelated pending migrations. Use the documented include-all staging plan when the owner is ready to apply `202606240003` and `202606240008`.
- Full proof upload with camera/GPS and linked `customer_orders.status` sync must still be verified through the deployed app server actions.

## 2026-06-24 - Stock Mobile UX QA preparation: movement card wrapping

Task completed:

- Continued the Stock Module Mobile UX goal without changing Stock business logic.
- Improved mobile Stock movement-history cards so movement type/item and weight stack on very narrow phones, then use side-by-side alignment at the 390px breakpoint.
- Extended the Stock mobile UX source guard to catch regressions in the movement-card layout.
- Updated owner QA docs so remote/manual phone testing includes Stock dashboard, `/stock/movements`, and `/stock/units/[id]` movement card readability.
- Updated Stock QA evidence with the source-check result and remaining real-device QA items.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - first attempt hit an existing Next build lock; retry after waiting passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required from Vercel/local with a stock user.
- Real phone camera permission/rear-camera scan behavior, Bluetooth printer selection, PDF label fallback output, and live Supabase/RLS Stock workflow evidence remain manual owner/device checks.
- The worktree still contains many unrelated module changes from other active work; this pass intentionally touched Stock UI/Stock QA docs only.

## 2026-06-24 - Stock Mobile UX QA preparation: stock-take review cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/stock-take` review-session cards so long session numbers, location/item/brand scope text, exception barcodes, signatures, and approval actions wrap cleanly around 390px width.
- Kept stock-take business logic, approval rules, RLS, server actions, migrations, and stock movement behavior unchanged.
- Extended Stock mobile UX source coverage for the review-card mobile classes.
- Updated Stock QA docs so owner/Vercel testing includes manager/director review-card readability.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required from Vercel/local with worker, manager, admin/director stock users.
- Real phone camera permission/rear-camera scan behavior, Bluetooth printer selection, PDF label fallback output, and live Supabase/RLS Stock workflow evidence remain manual owner/device checks.

## 2026-06-25 - Stock worker-speed inbound barcode rule learning

Task completed:

- Continued the Stock worker-speed refinement goal only.
- Added conservative barcode weight-rule inference for supplier/import barcodes: after a worker scans a barcode and manually enters actual kg once, the inbound form tries to find one clear 5-digit imported weight position.
- When a clear position is found, Barcode Inbound fills the weight start/length/decimals fields and turns on `Save weight rule for future scans`.
- Kept the existing inbound save path, barcode uniqueness, stock unit/movement/scan-log creation, RLS, migrations, and server validation unchanged.
- Added regression coverage for unique rule inference and ambiguous barcode refusal.
- Updated Stock QA docs so owner/Vercel testing includes the one-manual-weight rule-learning path.

Files changed in this pass:

- `lib/stock/barcode-weight.ts`
- `components/stock/workflow-forms.tsx`
- `scripts/stock-workflow-regression.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts\stock-workflow-regression.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Rule inference is deliberately conservative. Ambiguous barcodes still require manual rule setup or internal-label flow.
- Real authenticated Vercel/local QA is still required to confirm the learned rule is saved by the existing RPC and reused on the next same-format barcode.

## 2026-06-24 - Stock Mobile UX QA preparation: stock take scan cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/stock-take` scan-scope, counting-scope, session-number, and previous-scan item text wrapping so long item/brand/location/session values stay inside cards around 390px width.
- Kept Stock Take business logic, barcode-only counting, exception handling, manager/director approval rules, RLS, server actions, migrations, and stock movement behavior unchanged.
- Extended Stock mobile UX source coverage for the Stock Take scan/progress card wrapping classes.
- Updated Stock QA docs so owner/Vercel testing includes Stock Take long-value readability around 390px width.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - failed before Stock checks on an unrelated Order guard: `Order form UX guard missing: Create confirmed order`.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px Stock Take browser QA is still required from Vercel/local with worker, manager, admin/director stock users.
- Real phone camera permission/rear-camera scan behavior and live Supabase/RLS Stock Take workflow evidence remain manual owner/device checks.
- Full `npm.cmd run smoke` needs the unrelated Orders guard text restored or aligned in an Orders-focused pass before it can be green again.

## 2026-06-24 - Stock Mobile UX QA preparation: scanner success feedback

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved the shared Stock scanner sheet so `Last scan` and `Camera session scans` stay stacked at 390px worker-phone width and only switch side-by-side on wider screens.
- Kept Stock business logic, duplicate prevention, online-only blocking, server actions, RLS, migrations, and stock movement behavior unchanged.
- Extended Stock mobile UX source coverage so the scanner feedback layout does not regress to a cramped 390px two-column layout.
- Updated Stock QA docs so owner/Vercel testing includes scanner success-feedback readability.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required from Vercel/local with worker, manager, admin/director stock users.
- Real phone camera permission/rear-camera scan behavior, Bluetooth printer selection, PDF label fallback output, and live Supabase/RLS Stock workflow evidence remain manual owner/device checks.

## 2026-06-24 - Stock Mobile UX QA preparation: outbound feedback cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/outbound` worker feedback cards so long order/customer/destination/product text wraps in the green `Scanning outbound` card and the `Previous outbound scan` card.
- Kept outbound business logic, substitution rules, weight-difference warning, direct outbound types, RLS, server actions, migrations, and stock movement behavior unchanged.
- Extended Stock mobile UX source coverage for outbound context and previous-scan wrapping.
- Updated Stock QA docs so owner/Vercel testing includes outbound feedback-card readability.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required from Vercel/local with worker, manager, admin/director stock users.
- Real phone camera permission/rear-camera scan behavior, Bluetooth printer selection, PDF label fallback output, and live Supabase/RLS Stock workflow evidence remain manual owner/device checks.

## 2026-06-24 - Stock Mobile UX QA preparation: mobile card status badges

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved Stock mobile item-master, balance, and barcode-unit card headers so long item text and status badges stack on very narrow phones and align side-by-side at 390px.
- Updated status badges to wrap within cards instead of squeezing long item/location text.
- Kept Stock business logic, RLS, server actions, migrations, stock movement behavior, and permissions unchanged.
- Extended Stock mobile UX source coverage for the stacked card headers and wrapping badge classes.
- Updated Stock QA docs so owner/Vercel testing includes mobile card status-badge readability.

Files changed in this pass:

- `components/stock/stock-page.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required from Vercel/local with worker, manager, admin/director stock users.
- Real phone camera permission/rear-camera scan behavior, Bluetooth printer selection, PDF label fallback output, and live Supabase/RLS Stock workflow evidence remain manual owner/device checks.

## 2026-06-24 - Stock Mobile UX QA preparation: inbound scan cards

Task completed:

- Continued the Stock Module Mobile UX goal only.
- Improved `/stock/inbound` previous-scan and recent saved-scan cards so long product names and status badges wrap cleanly around 390px width.
- Recent saved-scan card headers now stack on very narrow phones and align side-by-side at 390px.
- Kept inbound business logic, duplicate blocking, label generation, undo, RLS, server actions, migrations, and stock movement behavior unchanged.
- Extended Stock mobile UX source coverage for the inbound scan-card wrapping classes.
- Updated Stock QA docs so owner/Vercel testing includes inbound previous/recent scan card readability.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/MODULE_STATUS.md`
- `docs/STOCK_QA_EVIDENCE.md`
- `docs/STOCK_TEST_LIST_FOR_OWNER.md`
- `docs/STOCK_REMOTE_QA_VERCEL.md`
- `docs/STOCK_MOBILE_UX_REQUIREMENT_AUDIT.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-owner-qa-doc-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required from Vercel/local with worker, manager, admin/director stock users.
- Real phone camera permission/rear-camera scan behavior, Bluetooth printer selection, PDF label fallback output, and live Supabase/RLS Stock workflow evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound summary whole-session undo copy

Task completed:

- Continued the Stock Module mobile/inbound refinement only.
- Reviewed the finished inbound session summary and found the copy contradicted the available whole-session undo action.
- Updated the warning so workers understand `Undo/Delete whole session` is for a mistaken current batch, while one-scan or later corrections should still go to a manager/admin.
- Kept the existing confirmation, void-with-audit behavior, stock movement behavior, RLS, server actions, and migrations unchanged.
- Extended Stock mobile UX source coverage for the clarified whole-session undo copy.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real authenticated 390px browser QA is still required from Vercel/local.
- Only use the whole-session undo acceptance path on safe demo stock until live Supabase/RLS evidence is captured.

## 2026-06-29 - Stock Inbound external scanner instruction

Task completed:

- Continued the active guided Stock Inbound goal.
- Confirmed the shared barcode field already handles keyboard-wedge/external scanner input through the Enter key.
- Added a short instruction beside the inbound barcode field so workers know to keep the cursor in the barcode box and scan with the external scanner.
- Kept the existing Enter-triggered scan behavior, auto-save behavior, duplicate blocking, barcode-rule handling, stock movements, RLS, and server actions unchanged.
- Extended Stock mobile UX source coverage for the external scanner instruction.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real keyboard-wedge scanner QA is still required because source checks cannot prove scanner device timing in the target browser.
- Real phone camera, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound manual summary count label

Task completed:

- Continued the active guided Stock Inbound goal.
- Updated the finished inbound summary so supplier-barcode sessions still show `Total barcode units`, while no-supplier-barcode/manual-weight sessions show `Total count`.
- This aligns the manual-weight guided flow with the requested Page 3 summary wording.
- Kept total weight, product/manufacturer display, print summary, whole-session undo, stock movements, RLS, and server actions unchanged.
- Extended Stock mobile UX source coverage for the mode-aware summary label.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should finish one supplier-barcode session and one manual-weight session to confirm the mode-aware label appears correctly.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound manual weight continuous-entry guard

Task completed:

- Continued the active guided Stock Inbound goal by checking the no-supplier-barcode/manual-weight continuous-entry requirement.
- Confirmed current app logic clears the kg field, keeps the worker on Manual Weight, shows `Saved. Enter next weight.`, and refocuses the kg field after a printed-label save.
- Strengthened Stock mobile UX source coverage so these continuous-entry mechanics are explicitly guarded.
- No runtime app logic, server actions, RLS, stock movement behavior, barcode uniqueness, approvals, or migrations changed in this pass.

Files changed in this pass:

- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA is still needed to confirm focus behavior after save in the target browser and scanner setup.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound guided-flow acceptance coverage

Task completed:

- Continued the active guided Stock Inbound goal with stronger automated evidence.
- Added an acceptance coverage block for the guided inbound objective in `scripts/stock-acceptance-coverage.mjs`.
- The smoke suite now guards product + manufacturer display naming, Page 1 setup, Page 2 barcode-rule setup, Page 3 continuous scanner mechanics, external scanner Enter handling, previous-scan undo, Page 4 summary/print/whole-session undo, manual-weight continuous entry, and manufacturer-aware stock balance/report display.
- Tried to start a local Next.js dev server on port `3900` for a 390px browser check. The server reported ready in logs, but it did not remain reachable across tool calls in this desktop thread; the in-app browser received connection refused.
- No app runtime logic, server actions, RLS, stock movement behavior, barcode uniqueness, approvals, or migrations changed in this pass.

Files changed in this pass:

- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser/device QA is still needed for the complete guided inbound flow at 390px, camera permission, external scanner timing, and Bluetooth/PDF label printing.
- Live Supabase/RLS evidence remains an owner/manual check.

## 2026-06-29 - Stock Inbound first-time barcode-rule scan gate

Task completed:

- Continued the active guided Stock Inbound goal with a small behavior fix.
- Found that Page 2 `Open scanner` could be used before a saved barcode rule existed for a first-time supplier-barcode setup.
- Changed that button to stay disabled and show `Save rule first` until `currentBarcodeWeightRule` exists.
- This keeps first-time workers on the intended flow: scan sample barcode, enter actual kg, save the rule, then continue scanning.
- Existing saved-rule template behavior, successful rule-save continuation, duplicate blocking, stock movements, RLS, and server actions were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for the no-bypass gate.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm a no-rule setup cannot open scanner before saving a rule, while a saved-rule template still opens scanner directly.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound first-time barcode-rule continuity

Task completed:

- Continued the active guided Stock Inbound goal with a post-save continuity fix.
- After a first-time barcode-rule save succeeds, the client now marks the rule as saved for the current session instead of waiting for `barcodeWeightRules` props to refresh.
- The Barcode Rule page can immediately show `Rule saved for this session` and allow `Open scanner` after the sample save.
- The learned sample barcode length is kept locally so the next scan can still warn when barcode length differs before a full server refresh.
- Scope changes and new sessions reset the local session-saved rule state.
- Existing persisted-rule templates, server action behavior, RLS, duplicate prevention, stock movements, approvals, and migrations were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for this continuity behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should save a first-time barcode rule and confirm scanner continuation plus barcode-length warning without page reload.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound first-time barcode-rule required save

Task completed:

- Continued the active guided Stock Inbound goal with a first-sample rule-save hardening.
- First-time barcode-rule learning now forces the save-rule control on and labels it `Save weight rule for first scan`.
- A hidden `saveWeightRule=true` field is rendered while the checked control is disabled, so the server still receives the required rule-save request.
- Once a saved rule exists, the normal `Save weight rule for future scans` behavior remains available.
- Existing server action behavior, RLS, duplicate prevention, stock movements, approvals, and migrations were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for the mandatory first-scan rule save.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm the first-time rule checkbox is forced on and the saved-rule flow still behaves normally.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound safe back navigation

Task completed:

- Continued the active guided Stock Inbound goal with explicit safe back controls.
- Supplier-barcode scan now shows `Back to Barcode Rule` in the worker action row.
- No-supplier/manual-weight mode now shows `Back to setup` while it is still safe to change setup.
- The manual setup back action is disabled once saved scans or a pending label lock the session.
- Existing step navigation, server actions, duplicate blocking, undo behavior, RLS, stock movements, approvals, and migrations were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for these explicit back paths.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm the new back buttons appear in the expected scan/manual states and do not allow unsafe setup edits after scans.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound barcode rule save-once behavior

Task completed:

- Continued the active guided Stock Inbound goal with a rule-learning repeat-scan fix.
- Saved barcode rules now load with `saveWeightRule` off, so continuous scans use the rule without re-saving it on every barcode.
- After a first sample successfully saves a new rule, the client turns `saveWeightRule` off before continuing into scan mode.
- First-time rule learning still forces `saveWeightRule=true` for the sample save.
- Existing server actions, persisted rule schema, duplicate blocking, stock movements, RLS, approvals, and migrations were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for the save-once behavior.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should save a first sample, then scan a second barcode and confirm it uses the rule without re-entering first-rule learning.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound session-saved rule guard consistency

Task completed:

- Continued the active guided Stock Inbound goal with a consistency fix for rules saved during the current session.
- Updated rule-page guards to use `canUseBarcodeRuleForSession` instead of checking only persisted `currentBarcodeWeightRule`.
- After a first-time rule is saved in the session, the page no longer reopens the first-sample teaching panel or requires actual kg again as though no rule exists.
- The scan path still requires sample barcode + actual kg until either a persisted rule or session-saved rule exists.
- Existing server actions, persisted rule schema, duplicate blocking, stock movements, RLS, approvals, and migrations were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for the combined-rule guard.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should save a first-time rule, go back to Barcode Rule, and confirm the page stays in usable-rule mode.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound rule-page submit hidden after rule ready

Task completed:

- Continued the active guided Stock Inbound goal with a Page 2 action cleanup.
- `Save rule and inbound` now appears only while a barcode rule is still being learned.
- Once a persisted or session-saved rule is usable, Page 2 shows `Open scanner` without also showing the save submit action.
- This avoids accidental blank or repeated rule-save submissions after the first rule is already ready.
- Existing server actions, persisted rule schema, duplicate blocking, stock movements, RLS, approvals, and migrations were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for this rule-page action guard.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should confirm `Save rule and inbound` disappears after the rule is usable and `Open scanner` remains available.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-06-29 - Stock Inbound barcode-length warning precedence

Task completed:

- Continued the active guided Stock Inbound goal with barcode-length warning hardening.
- Barcode length mismatch is now checked even when the barcode also fails weight decoding.
- If both happen, the worker sees the expected/got length warning plus internal-label guidance instead of only a generic no-weight message.
- The length mismatch is logged as `BARCODE_LENGTH_MISMATCH`; decode failure still guides the worker to no-barcode/internal-label inbound.
- Existing server actions, persisted rule schema, duplicate blocking, stock movements, RLS, approvals, and migrations were preserved.
- Extended Stock mobile UX and Stock acceptance coverage for length-warning precedence.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real browser QA should scan a different-length, non-decodable barcode and confirm length warning plus internal-label guidance appears.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.

## 2026-07-09 - Current version deploy preparation

Task completed:

- Prepared the current workspace version for Vercel deployment.
- Verified local checks before publishing.
- Added local scratch-file ignores so deployment commits do not include temporary dev logs or worktrees.
- No Supabase migrations were applied from this machine.

Files changed in this pass:

- `.gitignore`
- `HANDOFF.md`

Migration SQL added:

- None in this pass.

Commands run and results:

- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Vercel deployment still needs to finish building after push.
- Supabase migration SQL has not been applied from this machine.
- Real device Stock Inbound QA remains required for camera, external scanner, Bluetooth/PDF label printing, and live RLS behavior.

## 2026-07-09 - Stock Inbound short retry copy

Task completed:

- Continued the active Stock Inbound mobile workflow cleanup.
- Replaced the long save-error recovery hint with the short worker message `Connection issue. Retry this barcode.`
- Updated the manual-label scanner helper; current copy is `Weight saves label.`
- Added source coverage so the old long internet helper does not come back.
- No stock actions, RLS, migrations, barcode uniqueness, or movement logic changed.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in `/stock/inbound` QA should trigger a save error and confirm the short retry copy fits cleanly on a 390px phone.

## 2026-07-09 - Stock Inbound setup-only draft resume

Task completed:

- Continued the active Stock Inbound multi-page workflow hardening.
- Fixed unfinished inbound draft handling so a valid setup-only session can be resumed before the first saved barcode/unit.
- The draft now persists after product, manufacturer, origin, and location are selected, without accepting unresolved Other/custom placeholders.
- The current unfinished session card now appears for a ready setup even when saved count is still zero.
- Shortened the reusable label print note to `Bluetooth first. PDF fallback.` plus label size/page count.
- Updated the phone scanner success frame to prefer live scan feedback, and inbound auto-save feedback now includes the decoded weight.
- Updated the phone scanner failure frame to prefer live scan feedback before falling back to `Scan failed. Try again.`
- Updated the phone scanner warning frame to prefer live scan feedback before falling back to `Check scan before saving.`
- No stock movement, barcode uniqueness, RLS, or migration logic changed.

Files changed in this pass:

- `components/stock/barcode-scanner.tsx`
- `components/stock/workflow-forms.tsx`
- `components/stock/stock-label.tsx`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/smoke-routes.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-label-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `scripts/stock-owner-qa-doc-coverage.mjs`
- `scripts/stock-scanner-coverage.mjs`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-label-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in `/stock/inbound` QA should choose setup, leave before scanning, return, and confirm the same unfinished session can continue.

## 2026-07-09 - Stock Inbound manager-approved session delete copy

Task completed:

- Continued the active guided Stock Inbound goal with a summary-page safety wording cleanup.
- The whole-session delete warning now says `Manager-approved Delete Whole Session only. Audit kept.`
- The destructive confirmation title now says `Confirm manager-approved Delete Whole Session`.
- The Barcode Rule page no longer repeats long digit-position hints in the active scan cue; it now says `Rule ready. Keep scanning.` or `Rule saved. Keep scanning.`
- The shared scanner popup no longer says all continuous scans auto-save; it now says `Scanner stays open until Close.` so barcode-rule sample scanning is not misleading.
- The Manual Weight Entry top summary is now sticky on phone, matching the active scanner summary behavior.
- Inbound Session History now renders only on the setup page, keeping active rule/scanner/manual pages focused on the sticky session summary and current task.
- The whole-session delete confirmation body now uses the same manager-approved wording as the summary warning.
- The Barcode Rule save button now says `Save first barcode + rule` so workers know the first sample barcode is saved while the rule is learned.
- No stock movement, barcode uniqueness, RLS, permission, migration, or undo logic changed.
- Updated Stock scanner/inbound/mobile/acceptance coverage and QA evidence to enforce the manager-approved copy, shorter scan cue, scanner-popup close behavior, rule-save button label, sticky manual-entry summary, and setup-only session history.

Files changed in this pass:

- `components/stock/workflow-forms.tsx`
- `components/stock/barcode-scanner.tsx`
- `scripts/stock-scanner-coverage.mjs`
- `scripts/stock-inbound-guided-flow-coverage.mjs`
- `scripts/stock-acceptance-coverage.mjs`
- `scripts/stock-mobile-ux-coverage.mjs`
- `docs/STOCK_QA_EVIDENCE.md`
- `HANDOFF.md`

Migration SQL added:

- None.

Commands run and results:

- `node scripts\stock-inbound-guided-flow-coverage.mjs` - passed.
- `node scripts\stock-acceptance-coverage.mjs` - passed.
- `node scripts\stock-mobile-ux-coverage.mjs` - passed.
- `node scripts\stock-scanner-coverage.mjs` - passed.
- `npm.cmd run smoke` - passed.
- `npm.cmd run lint` - passed.
- `npm.cmd run typecheck` - passed.
- `npm.cmd run build` - passed.

Risks / remaining checks:

- Real logged-in `/stock/inbound` QA should finish a session and confirm workers see the manager-approved delete message while only permitted roles can open the destructive action.
- Real phone camera, external scanner, Bluetooth/PDF label printing, and live Supabase/RLS evidence remain manual owner/device checks.
